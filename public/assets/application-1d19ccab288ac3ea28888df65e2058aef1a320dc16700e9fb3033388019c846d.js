/*!
 * jQuery JavaScript Library v1.12.4
 * http://jquery.com/
 *
 * Includes Sizzle.js
 * http://sizzlejs.com/
 *
 * Copyright jQuery Foundation and other contributors
 * Released under the MIT license
 * http://jquery.org/license
 *
 * Date: 2016-05-20T17:17Z
 */


(function( global, factory ) {

	if ( typeof module === "object" && typeof module.exports === "object" ) {
		// For CommonJS and CommonJS-like environments where a proper `window`
		// is present, execute the factory and get jQuery.
		// For environments that do not have a `window` with a `document`
		// (such as Node.js), expose a factory as module.exports.
		// This accentuates the need for the creation of a real `window`.
		// e.g. var jQuery = require("jquery")(window);
		// See ticket #14549 for more info.
		module.exports = global.document ?
			factory( global, true ) :
			function( w ) {
				if ( !w.document ) {
					throw new Error( "jQuery requires a window with a document" );
				}
				return factory( w );
			};
	} else {
		factory( global );
	}

// Pass this if window is not defined yet
}(typeof window !== "undefined" ? window : this, function( window, noGlobal ) {

// Support: Firefox 18+
// Can't be in strict mode, several libs including ASP.NET trace
// the stack via arguments.caller.callee and Firefox dies if
// you try to trace through "use strict" call chains. (#13335)
//"use strict";
var deletedIds = [];

var document = window.document;

var slice = deletedIds.slice;

var concat = deletedIds.concat;

var push = deletedIds.push;

var indexOf = deletedIds.indexOf;

var class2type = {};

var toString = class2type.toString;

var hasOwn = class2type.hasOwnProperty;

var support = {};



var
	version = "1.12.4",

	// Define a local copy of jQuery
	jQuery = function( selector, context ) {

		// The jQuery object is actually just the init constructor 'enhanced'
		// Need init if jQuery is called (just allow error to be thrown if not included)
		return new jQuery.fn.init( selector, context );
	},

	// Support: Android<4.1, IE<9
	// Make sure we trim BOM and NBSP
	rtrim = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g,

	// Matches dashed string for camelizing
	rmsPrefix = /^-ms-/,
	rdashAlpha = /-([\da-z])/gi,

	// Used by jQuery.camelCase as callback to replace()
	fcamelCase = function( all, letter ) {
		return letter.toUpperCase();
	};

jQuery.fn = jQuery.prototype = {

	// The current version of jQuery being used
	jquery: version,

	constructor: jQuery,

	// Start with an empty selector
	selector: "",

	// The default length of a jQuery object is 0
	length: 0,

	toArray: function() {
		return slice.call( this );
	},

	// Get the Nth element in the matched element set OR
	// Get the whole matched element set as a clean array
	get: function( num ) {
		return num != null ?

			// Return just the one element from the set
			( num < 0 ? this[ num + this.length ] : this[ num ] ) :

			// Return all the elements in a clean array
			slice.call( this );
	},

	// Take an array of elements and push it onto the stack
	// (returning the new matched element set)
	pushStack: function( elems ) {

		// Build a new jQuery matched element set
		var ret = jQuery.merge( this.constructor(), elems );

		// Add the old object onto the stack (as a reference)
		ret.prevObject = this;
		ret.context = this.context;

		// Return the newly-formed element set
		return ret;
	},

	// Execute a callback for every element in the matched set.
	each: function( callback ) {
		return jQuery.each( this, callback );
	},

	map: function( callback ) {
		return this.pushStack( jQuery.map( this, function( elem, i ) {
			return callback.call( elem, i, elem );
		} ) );
	},

	slice: function() {
		return this.pushStack( slice.apply( this, arguments ) );
	},

	first: function() {
		return this.eq( 0 );
	},

	last: function() {
		return this.eq( -1 );
	},

	eq: function( i ) {
		var len = this.length,
			j = +i + ( i < 0 ? len : 0 );
		return this.pushStack( j >= 0 && j < len ? [ this[ j ] ] : [] );
	},

	end: function() {
		return this.prevObject || this.constructor();
	},

	// For internal use only.
	// Behaves like an Array's method, not like a jQuery method.
	push: push,
	sort: deletedIds.sort,
	splice: deletedIds.splice
};

jQuery.extend = jQuery.fn.extend = function() {
	var src, copyIsArray, copy, name, options, clone,
		target = arguments[ 0 ] || {},
		i = 1,
		length = arguments.length,
		deep = false;

	// Handle a deep copy situation
	if ( typeof target === "boolean" ) {
		deep = target;

		// skip the boolean and the target
		target = arguments[ i ] || {};
		i++;
	}

	// Handle case when target is a string or something (possible in deep copy)
	if ( typeof target !== "object" && !jQuery.isFunction( target ) ) {
		target = {};
	}

	// extend jQuery itself if only one argument is passed
	if ( i === length ) {
		target = this;
		i--;
	}

	for ( ; i < length; i++ ) {

		// Only deal with non-null/undefined values
		if ( ( options = arguments[ i ] ) != null ) {

			// Extend the base object
			for ( name in options ) {
				src = target[ name ];
				copy = options[ name ];

				// Prevent never-ending loop
				if ( target === copy ) {
					continue;
				}

				// Recurse if we're merging plain objects or arrays
				if ( deep && copy && ( jQuery.isPlainObject( copy ) ||
					( copyIsArray = jQuery.isArray( copy ) ) ) ) {

					if ( copyIsArray ) {
						copyIsArray = false;
						clone = src && jQuery.isArray( src ) ? src : [];

					} else {
						clone = src && jQuery.isPlainObject( src ) ? src : {};
					}

					// Never move original objects, clone them
					target[ name ] = jQuery.extend( deep, clone, copy );

				// Don't bring in undefined values
				} else if ( copy !== undefined ) {
					target[ name ] = copy;
				}
			}
		}
	}

	// Return the modified object
	return target;
};

jQuery.extend( {

	// Unique for each copy of jQuery on the page
	expando: "jQuery" + ( version + Math.random() ).replace( /\D/g, "" ),

	// Assume jQuery is ready without the ready module
	isReady: true,

	error: function( msg ) {
		throw new Error( msg );
	},

	noop: function() {},

	// See test/unit/core.js for details concerning isFunction.
	// Since version 1.3, DOM methods and functions like alert
	// aren't supported. They return false on IE (#2968).
	isFunction: function( obj ) {
		return jQuery.type( obj ) === "function";
	},

	isArray: Array.isArray || function( obj ) {
		return jQuery.type( obj ) === "array";
	},

	isWindow: function( obj ) {
		/* jshint eqeqeq: false */
		return obj != null && obj == obj.window;
	},

	isNumeric: function( obj ) {

		// parseFloat NaNs numeric-cast false positives (null|true|false|"")
		// ...but misinterprets leading-number strings, particularly hex literals ("0x...")
		// subtraction forces infinities to NaN
		// adding 1 corrects loss of precision from parseFloat (#15100)
		var realStringObj = obj && obj.toString();
		return !jQuery.isArray( obj ) && ( realStringObj - parseFloat( realStringObj ) + 1 ) >= 0;
	},

	isEmptyObject: function( obj ) {
		var name;
		for ( name in obj ) {
			return false;
		}
		return true;
	},

	isPlainObject: function( obj ) {
		var key;

		// Must be an Object.
		// Because of IE, we also have to check the presence of the constructor property.
		// Make sure that DOM nodes and window objects don't pass through, as well
		if ( !obj || jQuery.type( obj ) !== "object" || obj.nodeType || jQuery.isWindow( obj ) ) {
			return false;
		}

		try {

			// Not own constructor property must be Object
			if ( obj.constructor &&
				!hasOwn.call( obj, "constructor" ) &&
				!hasOwn.call( obj.constructor.prototype, "isPrototypeOf" ) ) {
				return false;
			}
		} catch ( e ) {

			// IE8,9 Will throw exceptions on certain host objects #9897
			return false;
		}

		// Support: IE<9
		// Handle iteration over inherited properties before own properties.
		if ( !support.ownFirst ) {
			for ( key in obj ) {
				return hasOwn.call( obj, key );
			}
		}

		// Own properties are enumerated firstly, so to speed up,
		// if last one is own, then all properties are own.
		for ( key in obj ) {}

		return key === undefined || hasOwn.call( obj, key );
	},

	type: function( obj ) {
		if ( obj == null ) {
			return obj + "";
		}
		return typeof obj === "object" || typeof obj === "function" ?
			class2type[ toString.call( obj ) ] || "object" :
			typeof obj;
	},

	// Workarounds based on findings by Jim Driscoll
	// http://weblogs.java.net/blog/driscoll/archive/2009/09/08/eval-javascript-global-context
	globalEval: function( data ) {
		if ( data && jQuery.trim( data ) ) {

			// We use execScript on Internet Explorer
			// We use an anonymous function so that context is window
			// rather than jQuery in Firefox
			( window.execScript || function( data ) {
				window[ "eval" ].call( window, data ); // jscs:ignore requireDotNotation
			} )( data );
		}
	},

	// Convert dashed to camelCase; used by the css and data modules
	// Microsoft forgot to hump their vendor prefix (#9572)
	camelCase: function( string ) {
		return string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );
	},

	nodeName: function( elem, name ) {
		return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
	},

	each: function( obj, callback ) {
		var length, i = 0;

		if ( isArrayLike( obj ) ) {
			length = obj.length;
			for ( ; i < length; i++ ) {
				if ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {
					break;
				}
			}
		} else {
			for ( i in obj ) {
				if ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {
					break;
				}
			}
		}

		return obj;
	},

	// Support: Android<4.1, IE<9
	trim: function( text ) {
		return text == null ?
			"" :
			( text + "" ).replace( rtrim, "" );
	},

	// results is for internal usage only
	makeArray: function( arr, results ) {
		var ret = results || [];

		if ( arr != null ) {
			if ( isArrayLike( Object( arr ) ) ) {
				jQuery.merge( ret,
					typeof arr === "string" ?
					[ arr ] : arr
				);
			} else {
				push.call( ret, arr );
			}
		}

		return ret;
	},

	inArray: function( elem, arr, i ) {
		var len;

		if ( arr ) {
			if ( indexOf ) {
				return indexOf.call( arr, elem, i );
			}

			len = arr.length;
			i = i ? i < 0 ? Math.max( 0, len + i ) : i : 0;

			for ( ; i < len; i++ ) {

				// Skip accessing in sparse arrays
				if ( i in arr && arr[ i ] === elem ) {
					return i;
				}
			}
		}

		return -1;
	},

	merge: function( first, second ) {
		var len = +second.length,
			j = 0,
			i = first.length;

		while ( j < len ) {
			first[ i++ ] = second[ j++ ];
		}

		// Support: IE<9
		// Workaround casting of .length to NaN on otherwise arraylike objects (e.g., NodeLists)
		if ( len !== len ) {
			while ( second[ j ] !== undefined ) {
				first[ i++ ] = second[ j++ ];
			}
		}

		first.length = i;

		return first;
	},

	grep: function( elems, callback, invert ) {
		var callbackInverse,
			matches = [],
			i = 0,
			length = elems.length,
			callbackExpect = !invert;

		// Go through the array, only saving the items
		// that pass the validator function
		for ( ; i < length; i++ ) {
			callbackInverse = !callback( elems[ i ], i );
			if ( callbackInverse !== callbackExpect ) {
				matches.push( elems[ i ] );
			}
		}

		return matches;
	},

	// arg is for internal usage only
	map: function( elems, callback, arg ) {
		var length, value,
			i = 0,
			ret = [];

		// Go through the array, translating each of the items to their new values
		if ( isArrayLike( elems ) ) {
			length = elems.length;
			for ( ; i < length; i++ ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret.push( value );
				}
			}

		// Go through every key on the object,
		} else {
			for ( i in elems ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret.push( value );
				}
			}
		}

		// Flatten any nested arrays
		return concat.apply( [], ret );
	},

	// A global GUID counter for objects
	guid: 1,

	// Bind a function to a context, optionally partially applying any
	// arguments.
	proxy: function( fn, context ) {
		var args, proxy, tmp;

		if ( typeof context === "string" ) {
			tmp = fn[ context ];
			context = fn;
			fn = tmp;
		}

		// Quick check to determine if target is callable, in the spec
		// this throws a TypeError, but we will just return undefined.
		if ( !jQuery.isFunction( fn ) ) {
			return undefined;
		}

		// Simulated bind
		args = slice.call( arguments, 2 );
		proxy = function() {
			return fn.apply( context || this, args.concat( slice.call( arguments ) ) );
		};

		// Set the guid of unique handler to the same of original handler, so it can be removed
		proxy.guid = fn.guid = fn.guid || jQuery.guid++;

		return proxy;
	},

	now: function() {
		return +( new Date() );
	},

	// jQuery.support is not used in Core but other projects attach their
	// properties to it so it needs to exist.
	support: support
} );

// JSHint would error on this code due to the Symbol not being defined in ES5.
// Defining this global in .jshintrc would create a danger of using the global
// unguarded in another place, it seems safer to just disable JSHint for these
// three lines.
/* jshint ignore: start */
if ( typeof Symbol === "function" ) {
	jQuery.fn[ Symbol.iterator ] = deletedIds[ Symbol.iterator ];
}
/* jshint ignore: end */

// Populate the class2type map
jQuery.each( "Boolean Number String Function Array Date RegExp Object Error Symbol".split( " " ),
function( i, name ) {
	class2type[ "[object " + name + "]" ] = name.toLowerCase();
} );

function isArrayLike( obj ) {

	// Support: iOS 8.2 (not reproducible in simulator)
	// `in` check used to prevent JIT error (gh-2145)
	// hasOwn isn't used here due to false negatives
	// regarding Nodelist length in IE
	var length = !!obj && "length" in obj && obj.length,
		type = jQuery.type( obj );

	if ( type === "function" || jQuery.isWindow( obj ) ) {
		return false;
	}

	return type === "array" || length === 0 ||
		typeof length === "number" && length > 0 && ( length - 1 ) in obj;
}
var Sizzle =
/*!
 * Sizzle CSS Selector Engine v2.2.1
 * http://sizzlejs.com/
 *
 * Copyright jQuery Foundation and other contributors
 * Released under the MIT license
 * http://jquery.org/license
 *
 * Date: 2015-10-17
 */
(function( window ) {

var i,
	support,
	Expr,
	getText,
	isXML,
	tokenize,
	compile,
	select,
	outermostContext,
	sortInput,
	hasDuplicate,

	// Local document vars
	setDocument,
	document,
	docElem,
	documentIsHTML,
	rbuggyQSA,
	rbuggyMatches,
	matches,
	contains,

	// Instance-specific data
	expando = "sizzle" + 1 * new Date(),
	preferredDoc = window.document,
	dirruns = 0,
	done = 0,
	classCache = createCache(),
	tokenCache = createCache(),
	compilerCache = createCache(),
	sortOrder = function( a, b ) {
		if ( a === b ) {
			hasDuplicate = true;
		}
		return 0;
	},

	// General-purpose constants
	MAX_NEGATIVE = 1 << 31,

	// Instance methods
	hasOwn = ({}).hasOwnProperty,
	arr = [],
	pop = arr.pop,
	push_native = arr.push,
	push = arr.push,
	slice = arr.slice,
	// Use a stripped-down indexOf as it's faster than native
	// http://jsperf.com/thor-indexof-vs-for/5
	indexOf = function( list, elem ) {
		var i = 0,
			len = list.length;
		for ( ; i < len; i++ ) {
			if ( list[i] === elem ) {
				return i;
			}
		}
		return -1;
	},

	booleans = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped",

	// Regular expressions

	// http://www.w3.org/TR/css3-selectors/#whitespace
	whitespace = "[\\x20\\t\\r\\n\\f]",

	// http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier
	identifier = "(?:\\\\.|[\\w-]|[^\\x00-\\xa0])+",

	// Attribute selectors: http://www.w3.org/TR/selectors/#attribute-selectors
	attributes = "\\[" + whitespace + "*(" + identifier + ")(?:" + whitespace +
		// Operator (capture 2)
		"*([*^$|!~]?=)" + whitespace +
		// "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
		"*(?:'((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\"|(" + identifier + "))|)" + whitespace +
		"*\\]",

	pseudos = ":(" + identifier + ")(?:\\((" +
		// To reduce the number of selectors needing tokenize in the preFilter, prefer arguments:
		// 1. quoted (capture 3; capture 4 or capture 5)
		"('((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\")|" +
		// 2. simple (capture 6)
		"((?:\\\\.|[^\\\\()[\\]]|" + attributes + ")*)|" +
		// 3. anything else (capture 2)
		".*" +
		")\\)|)",

	// Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
	rwhitespace = new RegExp( whitespace + "+", "g" ),
	rtrim = new RegExp( "^" + whitespace + "+|((?:^|[^\\\\])(?:\\\\.)*)" + whitespace + "+$", "g" ),

	rcomma = new RegExp( "^" + whitespace + "*," + whitespace + "*" ),
	rcombinators = new RegExp( "^" + whitespace + "*([>+~]|" + whitespace + ")" + whitespace + "*" ),

	rattributeQuotes = new RegExp( "=" + whitespace + "*([^\\]'\"]*?)" + whitespace + "*\\]", "g" ),

	rpseudo = new RegExp( pseudos ),
	ridentifier = new RegExp( "^" + identifier + "$" ),

	matchExpr = {
		"ID": new RegExp( "^#(" + identifier + ")" ),
		"CLASS": new RegExp( "^\\.(" + identifier + ")" ),
		"TAG": new RegExp( "^(" + identifier + "|[*])" ),
		"ATTR": new RegExp( "^" + attributes ),
		"PSEUDO": new RegExp( "^" + pseudos ),
		"CHILD": new RegExp( "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + whitespace +
			"*(even|odd|(([+-]|)(\\d*)n|)" + whitespace + "*(?:([+-]|)" + whitespace +
			"*(\\d+)|))" + whitespace + "*\\)|)", "i" ),
		"bool": new RegExp( "^(?:" + booleans + ")$", "i" ),
		// For use in libraries implementing .is()
		// We use this for POS matching in `select`
		"needsContext": new RegExp( "^" + whitespace + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" +
			whitespace + "*((?:-\\d)?\\d*)" + whitespace + "*\\)|)(?=[^-]|$)", "i" )
	},

	rinputs = /^(?:input|select|textarea|button)$/i,
	rheader = /^h\d$/i,

	rnative = /^[^{]+\{\s*\[native \w/,

	// Easily-parseable/retrievable ID or TAG or CLASS selectors
	rquickExpr = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/,

	rsibling = /[+~]/,
	rescape = /'|\\/g,

	// CSS escapes http://www.w3.org/TR/CSS21/syndata.html#escaped-characters
	runescape = new RegExp( "\\\\([\\da-f]{1,6}" + whitespace + "?|(" + whitespace + ")|.)", "ig" ),
	funescape = function( _, escaped, escapedWhitespace ) {
		var high = "0x" + escaped - 0x10000;
		// NaN means non-codepoint
		// Support: Firefox<24
		// Workaround erroneous numeric interpretation of +"0x"
		return high !== high || escapedWhitespace ?
			escaped :
			high < 0 ?
				// BMP codepoint
				String.fromCharCode( high + 0x10000 ) :
				// Supplemental Plane codepoint (surrogate pair)
				String.fromCharCode( high >> 10 | 0xD800, high & 0x3FF | 0xDC00 );
	},

	// Used for iframes
	// See setDocument()
	// Removing the function wrapper causes a "Permission Denied"
	// error in IE
	unloadHandler = function() {
		setDocument();
	};

// Optimize for push.apply( _, NodeList )
try {
	push.apply(
		(arr = slice.call( preferredDoc.childNodes )),
		preferredDoc.childNodes
	);
	// Support: Android<4.0
	// Detect silently failing push.apply
	arr[ preferredDoc.childNodes.length ].nodeType;
} catch ( e ) {
	push = { apply: arr.length ?

		// Leverage slice if possible
		function( target, els ) {
			push_native.apply( target, slice.call(els) );
		} :

		// Support: IE<9
		// Otherwise append directly
		function( target, els ) {
			var j = target.length,
				i = 0;
			// Can't trust NodeList.length
			while ( (target[j++] = els[i++]) ) {}
			target.length = j - 1;
		}
	};
}

function Sizzle( selector, context, results, seed ) {
	var m, i, elem, nid, nidselect, match, groups, newSelector,
		newContext = context && context.ownerDocument,

		// nodeType defaults to 9, since context defaults to document
		nodeType = context ? context.nodeType : 9;

	results = results || [];

	// Return early from calls with invalid selector or context
	if ( typeof selector !== "string" || !selector ||
		nodeType !== 1 && nodeType !== 9 && nodeType !== 11 ) {

		return results;
	}

	// Try to shortcut find operations (as opposed to filters) in HTML documents
	if ( !seed ) {

		if ( ( context ? context.ownerDocument || context : preferredDoc ) !== document ) {
			setDocument( context );
		}
		context = context || document;

		if ( documentIsHTML ) {

			// If the selector is sufficiently simple, try using a "get*By*" DOM method
			// (excepting DocumentFragment context, where the methods don't exist)
			if ( nodeType !== 11 && (match = rquickExpr.exec( selector )) ) {

				// ID selector
				if ( (m = match[1]) ) {

					// Document context
					if ( nodeType === 9 ) {
						if ( (elem = context.getElementById( m )) ) {

							// Support: IE, Opera, Webkit
							// TODO: identify versions
							// getElementById can match elements by name instead of ID
							if ( elem.id === m ) {
								results.push( elem );
								return results;
							}
						} else {
							return results;
						}

					// Element context
					} else {

						// Support: IE, Opera, Webkit
						// TODO: identify versions
						// getElementById can match elements by name instead of ID
						if ( newContext && (elem = newContext.getElementById( m )) &&
							contains( context, elem ) &&
							elem.id === m ) {

							results.push( elem );
							return results;
						}
					}

				// Type selector
				} else if ( match[2] ) {
					push.apply( results, context.getElementsByTagName( selector ) );
					return results;

				// Class selector
				} else if ( (m = match[3]) && support.getElementsByClassName &&
					context.getElementsByClassName ) {

					push.apply( results, context.getElementsByClassName( m ) );
					return results;
				}
			}

			// Take advantage of querySelectorAll
			if ( support.qsa &&
				!compilerCache[ selector + " " ] &&
				(!rbuggyQSA || !rbuggyQSA.test( selector )) ) {

				if ( nodeType !== 1 ) {
					newContext = context;
					newSelector = selector;

				// qSA looks outside Element context, which is not what we want
				// Thanks to Andrew Dupont for this workaround technique
				// Support: IE <=8
				// Exclude object elements
				} else if ( context.nodeName.toLowerCase() !== "object" ) {

					// Capture the context ID, setting it first if necessary
					if ( (nid = context.getAttribute( "id" )) ) {
						nid = nid.replace( rescape, "\\$&" );
					} else {
						context.setAttribute( "id", (nid = expando) );
					}

					// Prefix every selector in the list
					groups = tokenize( selector );
					i = groups.length;
					nidselect = ridentifier.test( nid ) ? "#" + nid : "[id='" + nid + "']";
					while ( i-- ) {
						groups[i] = nidselect + " " + toSelector( groups[i] );
					}
					newSelector = groups.join( "," );

					// Expand context for sibling selectors
					newContext = rsibling.test( selector ) && testContext( context.parentNode ) ||
						context;
				}

				if ( newSelector ) {
					try {
						push.apply( results,
							newContext.querySelectorAll( newSelector )
						);
						return results;
					} catch ( qsaError ) {
					} finally {
						if ( nid === expando ) {
							context.removeAttribute( "id" );
						}
					}
				}
			}
		}
	}

	// All others
	return select( selector.replace( rtrim, "$1" ), context, results, seed );
}

/**
 * Create key-value caches of limited size
 * @returns {function(string, object)} Returns the Object data after storing it on itself with
 *	property name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)
 *	deleting the oldest entry
 */
function createCache() {
	var keys = [];

	function cache( key, value ) {
		// Use (key + " ") to avoid collision with native prototype properties (see Issue #157)
		if ( keys.push( key + " " ) > Expr.cacheLength ) {
			// Only keep the most recent entries
			delete cache[ keys.shift() ];
		}
		return (cache[ key + " " ] = value);
	}
	return cache;
}

/**
 * Mark a function for special use by Sizzle
 * @param {Function} fn The function to mark
 */
function markFunction( fn ) {
	fn[ expando ] = true;
	return fn;
}

/**
 * Support testing using an element
 * @param {Function} fn Passed the created div and expects a boolean result
 */
function assert( fn ) {
	var div = document.createElement("div");

	try {
		return !!fn( div );
	} catch (e) {
		return false;
	} finally {
		// Remove from its parent by default
		if ( div.parentNode ) {
			div.parentNode.removeChild( div );
		}
		// release memory in IE
		div = null;
	}
}

/**
 * Adds the same handler for all of the specified attrs
 * @param {String} attrs Pipe-separated list of attributes
 * @param {Function} handler The method that will be applied
 */
function addHandle( attrs, handler ) {
	var arr = attrs.split("|"),
		i = arr.length;

	while ( i-- ) {
		Expr.attrHandle[ arr[i] ] = handler;
	}
}

/**
 * Checks document order of two siblings
 * @param {Element} a
 * @param {Element} b
 * @returns {Number} Returns less than 0 if a precedes b, greater than 0 if a follows b
 */
function siblingCheck( a, b ) {
	var cur = b && a,
		diff = cur && a.nodeType === 1 && b.nodeType === 1 &&
			( ~b.sourceIndex || MAX_NEGATIVE ) -
			( ~a.sourceIndex || MAX_NEGATIVE );

	// Use IE sourceIndex if available on both nodes
	if ( diff ) {
		return diff;
	}

	// Check if b follows a
	if ( cur ) {
		while ( (cur = cur.nextSibling) ) {
			if ( cur === b ) {
				return -1;
			}
		}
	}

	return a ? 1 : -1;
}

/**
 * Returns a function to use in pseudos for input types
 * @param {String} type
 */
function createInputPseudo( type ) {
	return function( elem ) {
		var name = elem.nodeName.toLowerCase();
		return name === "input" && elem.type === type;
	};
}

/**
 * Returns a function to use in pseudos for buttons
 * @param {String} type
 */
function createButtonPseudo( type ) {
	return function( elem ) {
		var name = elem.nodeName.toLowerCase();
		return (name === "input" || name === "button") && elem.type === type;
	};
}

/**
 * Returns a function to use in pseudos for positionals
 * @param {Function} fn
 */
function createPositionalPseudo( fn ) {
	return markFunction(function( argument ) {
		argument = +argument;
		return markFunction(function( seed, matches ) {
			var j,
				matchIndexes = fn( [], seed.length, argument ),
				i = matchIndexes.length;

			// Match elements found at the specified indexes
			while ( i-- ) {
				if ( seed[ (j = matchIndexes[i]) ] ) {
					seed[j] = !(matches[j] = seed[j]);
				}
			}
		});
	});
}

/**
 * Checks a node for validity as a Sizzle context
 * @param {Element|Object=} context
 * @returns {Element|Object|Boolean} The input node if acceptable, otherwise a falsy value
 */
function testContext( context ) {
	return context && typeof context.getElementsByTagName !== "undefined" && context;
}

// Expose support vars for convenience
support = Sizzle.support = {};

/**
 * Detects XML nodes
 * @param {Element|Object} elem An element or a document
 * @returns {Boolean} True iff elem is a non-HTML XML node
 */
isXML = Sizzle.isXML = function( elem ) {
	// documentElement is verified for cases where it doesn't yet exist
	// (such as loading iframes in IE - #4833)
	var documentElement = elem && (elem.ownerDocument || elem).documentElement;
	return documentElement ? documentElement.nodeName !== "HTML" : false;
};

/**
 * Sets document-related variables once based on the current document
 * @param {Element|Object} [doc] An element or document object to use to set the document
 * @returns {Object} Returns the current document
 */
setDocument = Sizzle.setDocument = function( node ) {
	var hasCompare, parent,
		doc = node ? node.ownerDocument || node : preferredDoc;

	// Return early if doc is invalid or already selected
	if ( doc === document || doc.nodeType !== 9 || !doc.documentElement ) {
		return document;
	}

	// Update global variables
	document = doc;
	docElem = document.documentElement;
	documentIsHTML = !isXML( document );

	// Support: IE 9-11, Edge
	// Accessing iframe documents after unload throws "permission denied" errors (jQuery #13936)
	if ( (parent = document.defaultView) && parent.top !== parent ) {
		// Support: IE 11
		if ( parent.addEventListener ) {
			parent.addEventListener( "unload", unloadHandler, false );

		// Support: IE 9 - 10 only
		} else if ( parent.attachEvent ) {
			parent.attachEvent( "onunload", unloadHandler );
		}
	}

	/* Attributes
	---------------------------------------------------------------------- */

	// Support: IE<8
	// Verify that getAttribute really returns attributes and not properties
	// (excepting IE8 booleans)
	support.attributes = assert(function( div ) {
		div.className = "i";
		return !div.getAttribute("className");
	});

	/* getElement(s)By*
	---------------------------------------------------------------------- */

	// Check if getElementsByTagName("*") returns only elements
	support.getElementsByTagName = assert(function( div ) {
		div.appendChild( document.createComment("") );
		return !div.getElementsByTagName("*").length;
	});

	// Support: IE<9
	support.getElementsByClassName = rnative.test( document.getElementsByClassName );

	// Support: IE<10
	// Check if getElementById returns elements by name
	// The broken getElementById methods don't pick up programatically-set names,
	// so use a roundabout getElementsByName test
	support.getById = assert(function( div ) {
		docElem.appendChild( div ).id = expando;
		return !document.getElementsByName || !document.getElementsByName( expando ).length;
	});

	// ID find and filter
	if ( support.getById ) {
		Expr.find["ID"] = function( id, context ) {
			if ( typeof context.getElementById !== "undefined" && documentIsHTML ) {
				var m = context.getElementById( id );
				return m ? [ m ] : [];
			}
		};
		Expr.filter["ID"] = function( id ) {
			var attrId = id.replace( runescape, funescape );
			return function( elem ) {
				return elem.getAttribute("id") === attrId;
			};
		};
	} else {
		// Support: IE6/7
		// getElementById is not reliable as a find shortcut
		delete Expr.find["ID"];

		Expr.filter["ID"] =  function( id ) {
			var attrId = id.replace( runescape, funescape );
			return function( elem ) {
				var node = typeof elem.getAttributeNode !== "undefined" &&
					elem.getAttributeNode("id");
				return node && node.value === attrId;
			};
		};
	}

	// Tag
	Expr.find["TAG"] = support.getElementsByTagName ?
		function( tag, context ) {
			if ( typeof context.getElementsByTagName !== "undefined" ) {
				return context.getElementsByTagName( tag );

			// DocumentFragment nodes don't have gEBTN
			} else if ( support.qsa ) {
				return context.querySelectorAll( tag );
			}
		} :

		function( tag, context ) {
			var elem,
				tmp = [],
				i = 0,
				// By happy coincidence, a (broken) gEBTN appears on DocumentFragment nodes too
				results = context.getElementsByTagName( tag );

			// Filter out possible comments
			if ( tag === "*" ) {
				while ( (elem = results[i++]) ) {
					if ( elem.nodeType === 1 ) {
						tmp.push( elem );
					}
				}

				return tmp;
			}
			return results;
		};

	// Class
	Expr.find["CLASS"] = support.getElementsByClassName && function( className, context ) {
		if ( typeof context.getElementsByClassName !== "undefined" && documentIsHTML ) {
			return context.getElementsByClassName( className );
		}
	};

	/* QSA/matchesSelector
	---------------------------------------------------------------------- */

	// QSA and matchesSelector support

	// matchesSelector(:active) reports false when true (IE9/Opera 11.5)
	rbuggyMatches = [];

	// qSa(:focus) reports false when true (Chrome 21)
	// We allow this because of a bug in IE8/9 that throws an error
	// whenever `document.activeElement` is accessed on an iframe
	// So, we allow :focus to pass through QSA all the time to avoid the IE error
	// See http://bugs.jquery.com/ticket/13378
	rbuggyQSA = [];

	if ( (support.qsa = rnative.test( document.querySelectorAll )) ) {
		// Build QSA regex
		// Regex strategy adopted from Diego Perini
		assert(function( div ) {
			// Select is set to empty string on purpose
			// This is to test IE's treatment of not explicitly
			// setting a boolean content attribute,
			// since its presence should be enough
			// http://bugs.jquery.com/ticket/12359
			docElem.appendChild( div ).innerHTML = "<a id='" + expando + "'></a>" +
				"<select id='" + expando + "-\r\\' msallowcapture=''>" +
				"<option selected=''></option></select>";

			// Support: IE8, Opera 11-12.16
			// Nothing should be selected when empty strings follow ^= or $= or *=
			// The test attribute must be unknown in Opera but "safe" for WinRT
			// http://msdn.microsoft.com/en-us/library/ie/hh465388.aspx#attribute_section
			if ( div.querySelectorAll("[msallowcapture^='']").length ) {
				rbuggyQSA.push( "[*^$]=" + whitespace + "*(?:''|\"\")" );
			}

			// Support: IE8
			// Boolean attributes and "value" are not treated correctly
			if ( !div.querySelectorAll("[selected]").length ) {
				rbuggyQSA.push( "\\[" + whitespace + "*(?:value|" + booleans + ")" );
			}

			// Support: Chrome<29, Android<4.4, Safari<7.0+, iOS<7.0+, PhantomJS<1.9.8+
			if ( !div.querySelectorAll( "[id~=" + expando + "-]" ).length ) {
				rbuggyQSA.push("~=");
			}

			// Webkit/Opera - :checked should return selected option elements
			// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
			// IE8 throws error here and will not see later tests
			if ( !div.querySelectorAll(":checked").length ) {
				rbuggyQSA.push(":checked");
			}

			// Support: Safari 8+, iOS 8+
			// https://bugs.webkit.org/show_bug.cgi?id=136851
			// In-page `selector#id sibing-combinator selector` fails
			if ( !div.querySelectorAll( "a#" + expando + "+*" ).length ) {
				rbuggyQSA.push(".#.+[+~]");
			}
		});

		assert(function( div ) {
			// Support: Windows 8 Native Apps
			// The type and name attributes are restricted during .innerHTML assignment
			var input = document.createElement("input");
			input.setAttribute( "type", "hidden" );
			div.appendChild( input ).setAttribute( "name", "D" );

			// Support: IE8
			// Enforce case-sensitivity of name attribute
			if ( div.querySelectorAll("[name=d]").length ) {
				rbuggyQSA.push( "name" + whitespace + "*[*^$|!~]?=" );
			}

			// FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)
			// IE8 throws error here and will not see later tests
			if ( !div.querySelectorAll(":enabled").length ) {
				rbuggyQSA.push( ":enabled", ":disabled" );
			}

			// Opera 10-11 does not throw on post-comma invalid pseudos
			div.querySelectorAll("*,:x");
			rbuggyQSA.push(",.*:");
		});
	}

	if ( (support.matchesSelector = rnative.test( (matches = docElem.matches ||
		docElem.webkitMatchesSelector ||
		docElem.mozMatchesSelector ||
		docElem.oMatchesSelector ||
		docElem.msMatchesSelector) )) ) {

		assert(function( div ) {
			// Check to see if it's possible to do matchesSelector
			// on a disconnected node (IE 9)
			support.disconnectedMatch = matches.call( div, "div" );

			// This should fail with an exception
			// Gecko does not error, returns false instead
			matches.call( div, "[s!='']:x" );
			rbuggyMatches.push( "!=", pseudos );
		});
	}

	rbuggyQSA = rbuggyQSA.length && new RegExp( rbuggyQSA.join("|") );
	rbuggyMatches = rbuggyMatches.length && new RegExp( rbuggyMatches.join("|") );

	/* Contains
	---------------------------------------------------------------------- */
	hasCompare = rnative.test( docElem.compareDocumentPosition );

	// Element contains another
	// Purposefully self-exclusive
	// As in, an element does not contain itself
	contains = hasCompare || rnative.test( docElem.contains ) ?
		function( a, b ) {
			var adown = a.nodeType === 9 ? a.documentElement : a,
				bup = b && b.parentNode;
			return a === bup || !!( bup && bup.nodeType === 1 && (
				adown.contains ?
					adown.contains( bup ) :
					a.compareDocumentPosition && a.compareDocumentPosition( bup ) & 16
			));
		} :
		function( a, b ) {
			if ( b ) {
				while ( (b = b.parentNode) ) {
					if ( b === a ) {
						return true;
					}
				}
			}
			return false;
		};

	/* Sorting
	---------------------------------------------------------------------- */

	// Document order sorting
	sortOrder = hasCompare ?
	function( a, b ) {

		// Flag for duplicate removal
		if ( a === b ) {
			hasDuplicate = true;
			return 0;
		}

		// Sort on method existence if only one input has compareDocumentPosition
		var compare = !a.compareDocumentPosition - !b.compareDocumentPosition;
		if ( compare ) {
			return compare;
		}

		// Calculate position if both inputs belong to the same document
		compare = ( a.ownerDocument || a ) === ( b.ownerDocument || b ) ?
			a.compareDocumentPosition( b ) :

			// Otherwise we know they are disconnected
			1;

		// Disconnected nodes
		if ( compare & 1 ||
			(!support.sortDetached && b.compareDocumentPosition( a ) === compare) ) {

			// Choose the first element that is related to our preferred document
			if ( a === document || a.ownerDocument === preferredDoc && contains(preferredDoc, a) ) {
				return -1;
			}
			if ( b === document || b.ownerDocument === preferredDoc && contains(preferredDoc, b) ) {
				return 1;
			}

			// Maintain original order
			return sortInput ?
				( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
				0;
		}

		return compare & 4 ? -1 : 1;
	} :
	function( a, b ) {
		// Exit early if the nodes are identical
		if ( a === b ) {
			hasDuplicate = true;
			return 0;
		}

		var cur,
			i = 0,
			aup = a.parentNode,
			bup = b.parentNode,
			ap = [ a ],
			bp = [ b ];

		// Parentless nodes are either documents or disconnected
		if ( !aup || !bup ) {
			return a === document ? -1 :
				b === document ? 1 :
				aup ? -1 :
				bup ? 1 :
				sortInput ?
				( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
				0;

		// If the nodes are siblings, we can do a quick check
		} else if ( aup === bup ) {
			return siblingCheck( a, b );
		}

		// Otherwise we need full lists of their ancestors for comparison
		cur = a;
		while ( (cur = cur.parentNode) ) {
			ap.unshift( cur );
		}
		cur = b;
		while ( (cur = cur.parentNode) ) {
			bp.unshift( cur );
		}

		// Walk down the tree looking for a discrepancy
		while ( ap[i] === bp[i] ) {
			i++;
		}

		return i ?
			// Do a sibling check if the nodes have a common ancestor
			siblingCheck( ap[i], bp[i] ) :

			// Otherwise nodes in our document sort first
			ap[i] === preferredDoc ? -1 :
			bp[i] === preferredDoc ? 1 :
			0;
	};

	return document;
};

Sizzle.matches = function( expr, elements ) {
	return Sizzle( expr, null, null, elements );
};

Sizzle.matchesSelector = function( elem, expr ) {
	// Set document vars if needed
	if ( ( elem.ownerDocument || elem ) !== document ) {
		setDocument( elem );
	}

	// Make sure that attribute selectors are quoted
	expr = expr.replace( rattributeQuotes, "='$1']" );

	if ( support.matchesSelector && documentIsHTML &&
		!compilerCache[ expr + " " ] &&
		( !rbuggyMatches || !rbuggyMatches.test( expr ) ) &&
		( !rbuggyQSA     || !rbuggyQSA.test( expr ) ) ) {

		try {
			var ret = matches.call( elem, expr );

			// IE 9's matchesSelector returns false on disconnected nodes
			if ( ret || support.disconnectedMatch ||
					// As well, disconnected nodes are said to be in a document
					// fragment in IE 9
					elem.document && elem.document.nodeType !== 11 ) {
				return ret;
			}
		} catch (e) {}
	}

	return Sizzle( expr, document, null, [ elem ] ).length > 0;
};

Sizzle.contains = function( context, elem ) {
	// Set document vars if needed
	if ( ( context.ownerDocument || context ) !== document ) {
		setDocument( context );
	}
	return contains( context, elem );
};

Sizzle.attr = function( elem, name ) {
	// Set document vars if needed
	if ( ( elem.ownerDocument || elem ) !== document ) {
		setDocument( elem );
	}

	var fn = Expr.attrHandle[ name.toLowerCase() ],
		// Don't get fooled by Object.prototype properties (jQuery #13807)
		val = fn && hasOwn.call( Expr.attrHandle, name.toLowerCase() ) ?
			fn( elem, name, !documentIsHTML ) :
			undefined;

	return val !== undefined ?
		val :
		support.attributes || !documentIsHTML ?
			elem.getAttribute( name ) :
			(val = elem.getAttributeNode(name)) && val.specified ?
				val.value :
				null;
};

Sizzle.error = function( msg ) {
	throw new Error( "Syntax error, unrecognized expression: " + msg );
};

/**
 * Document sorting and removing duplicates
 * @param {ArrayLike} results
 */
Sizzle.uniqueSort = function( results ) {
	var elem,
		duplicates = [],
		j = 0,
		i = 0;

	// Unless we *know* we can detect duplicates, assume their presence
	hasDuplicate = !support.detectDuplicates;
	sortInput = !support.sortStable && results.slice( 0 );
	results.sort( sortOrder );

	if ( hasDuplicate ) {
		while ( (elem = results[i++]) ) {
			if ( elem === results[ i ] ) {
				j = duplicates.push( i );
			}
		}
		while ( j-- ) {
			results.splice( duplicates[ j ], 1 );
		}
	}

	// Clear input after sorting to release objects
	// See https://github.com/jquery/sizzle/pull/225
	sortInput = null;

	return results;
};

/**
 * Utility function for retrieving the text value of an array of DOM nodes
 * @param {Array|Element} elem
 */
getText = Sizzle.getText = function( elem ) {
	var node,
		ret = "",
		i = 0,
		nodeType = elem.nodeType;

	if ( !nodeType ) {
		// If no nodeType, this is expected to be an array
		while ( (node = elem[i++]) ) {
			// Do not traverse comment nodes
			ret += getText( node );
		}
	} else if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {
		// Use textContent for elements
		// innerText usage removed for consistency of new lines (jQuery #11153)
		if ( typeof elem.textContent === "string" ) {
			return elem.textContent;
		} else {
			// Traverse its children
			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
				ret += getText( elem );
			}
		}
	} else if ( nodeType === 3 || nodeType === 4 ) {
		return elem.nodeValue;
	}
	// Do not include comment or processing instruction nodes

	return ret;
};

Expr = Sizzle.selectors = {

	// Can be adjusted by the user
	cacheLength: 50,

	createPseudo: markFunction,

	match: matchExpr,

	attrHandle: {},

	find: {},

	relative: {
		">": { dir: "parentNode", first: true },
		" ": { dir: "parentNode" },
		"+": { dir: "previousSibling", first: true },
		"~": { dir: "previousSibling" }
	},

	preFilter: {
		"ATTR": function( match ) {
			match[1] = match[1].replace( runescape, funescape );

			// Move the given value to match[3] whether quoted or unquoted
			match[3] = ( match[3] || match[4] || match[5] || "" ).replace( runescape, funescape );

			if ( match[2] === "~=" ) {
				match[3] = " " + match[3] + " ";
			}

			return match.slice( 0, 4 );
		},

		"CHILD": function( match ) {
			/* matches from matchExpr["CHILD"]
				1 type (only|nth|...)
				2 what (child|of-type)
				3 argument (even|odd|\d*|\d*n([+-]\d+)?|...)
				4 xn-component of xn+y argument ([+-]?\d*n|)
				5 sign of xn-component
				6 x of xn-component
				7 sign of y-component
				8 y of y-component
			*/
			match[1] = match[1].toLowerCase();

			if ( match[1].slice( 0, 3 ) === "nth" ) {
				// nth-* requires argument
				if ( !match[3] ) {
					Sizzle.error( match[0] );
				}

				// numeric x and y parameters for Expr.filter.CHILD
				// remember that false/true cast respectively to 0/1
				match[4] = +( match[4] ? match[5] + (match[6] || 1) : 2 * ( match[3] === "even" || match[3] === "odd" ) );
				match[5] = +( ( match[7] + match[8] ) || match[3] === "odd" );

			// other types prohibit arguments
			} else if ( match[3] ) {
				Sizzle.error( match[0] );
			}

			return match;
		},

		"PSEUDO": function( match ) {
			var excess,
				unquoted = !match[6] && match[2];

			if ( matchExpr["CHILD"].test( match[0] ) ) {
				return null;
			}

			// Accept quoted arguments as-is
			if ( match[3] ) {
				match[2] = match[4] || match[5] || "";

			// Strip excess characters from unquoted arguments
			} else if ( unquoted && rpseudo.test( unquoted ) &&
				// Get excess from tokenize (recursively)
				(excess = tokenize( unquoted, true )) &&
				// advance to the next closing parenthesis
				(excess = unquoted.indexOf( ")", unquoted.length - excess ) - unquoted.length) ) {

				// excess is a negative index
				match[0] = match[0].slice( 0, excess );
				match[2] = unquoted.slice( 0, excess );
			}

			// Return only captures needed by the pseudo filter method (type and argument)
			return match.slice( 0, 3 );
		}
	},

	filter: {

		"TAG": function( nodeNameSelector ) {
			var nodeName = nodeNameSelector.replace( runescape, funescape ).toLowerCase();
			return nodeNameSelector === "*" ?
				function() { return true; } :
				function( elem ) {
					return elem.nodeName && elem.nodeName.toLowerCase() === nodeName;
				};
		},

		"CLASS": function( className ) {
			var pattern = classCache[ className + " " ];

			return pattern ||
				(pattern = new RegExp( "(^|" + whitespace + ")" + className + "(" + whitespace + "|$)" )) &&
				classCache( className, function( elem ) {
					return pattern.test( typeof elem.className === "string" && elem.className || typeof elem.getAttribute !== "undefined" && elem.getAttribute("class") || "" );
				});
		},

		"ATTR": function( name, operator, check ) {
			return function( elem ) {
				var result = Sizzle.attr( elem, name );

				if ( result == null ) {
					return operator === "!=";
				}
				if ( !operator ) {
					return true;
				}

				result += "";

				return operator === "=" ? result === check :
					operator === "!=" ? result !== check :
					operator === "^=" ? check && result.indexOf( check ) === 0 :
					operator === "*=" ? check && result.indexOf( check ) > -1 :
					operator === "$=" ? check && result.slice( -check.length ) === check :
					operator === "~=" ? ( " " + result.replace( rwhitespace, " " ) + " " ).indexOf( check ) > -1 :
					operator === "|=" ? result === check || result.slice( 0, check.length + 1 ) === check + "-" :
					false;
			};
		},

		"CHILD": function( type, what, argument, first, last ) {
			var simple = type.slice( 0, 3 ) !== "nth",
				forward = type.slice( -4 ) !== "last",
				ofType = what === "of-type";

			return first === 1 && last === 0 ?

				// Shortcut for :nth-*(n)
				function( elem ) {
					return !!elem.parentNode;
				} :

				function( elem, context, xml ) {
					var cache, uniqueCache, outerCache, node, nodeIndex, start,
						dir = simple !== forward ? "nextSibling" : "previousSibling",
						parent = elem.parentNode,
						name = ofType && elem.nodeName.toLowerCase(),
						useCache = !xml && !ofType,
						diff = false;

					if ( parent ) {

						// :(first|last|only)-(child|of-type)
						if ( simple ) {
							while ( dir ) {
								node = elem;
								while ( (node = node[ dir ]) ) {
									if ( ofType ?
										node.nodeName.toLowerCase() === name :
										node.nodeType === 1 ) {

										return false;
									}
								}
								// Reverse direction for :only-* (if we haven't yet done so)
								start = dir = type === "only" && !start && "nextSibling";
							}
							return true;
						}

						start = [ forward ? parent.firstChild : parent.lastChild ];

						// non-xml :nth-child(...) stores cache data on `parent`
						if ( forward && useCache ) {

							// Seek `elem` from a previously-cached index

							// ...in a gzip-friendly way
							node = parent;
							outerCache = node[ expando ] || (node[ expando ] = {});

							// Support: IE <9 only
							// Defend against cloned attroperties (jQuery gh-1709)
							uniqueCache = outerCache[ node.uniqueID ] ||
								(outerCache[ node.uniqueID ] = {});

							cache = uniqueCache[ type ] || [];
							nodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];
							diff = nodeIndex && cache[ 2 ];
							node = nodeIndex && parent.childNodes[ nodeIndex ];

							while ( (node = ++nodeIndex && node && node[ dir ] ||

								// Fallback to seeking `elem` from the start
								(diff = nodeIndex = 0) || start.pop()) ) {

								// When found, cache indexes on `parent` and break
								if ( node.nodeType === 1 && ++diff && node === elem ) {
									uniqueCache[ type ] = [ dirruns, nodeIndex, diff ];
									break;
								}
							}

						} else {
							// Use previously-cached element index if available
							if ( useCache ) {
								// ...in a gzip-friendly way
								node = elem;
								outerCache = node[ expando ] || (node[ expando ] = {});

								// Support: IE <9 only
								// Defend against cloned attroperties (jQuery gh-1709)
								uniqueCache = outerCache[ node.uniqueID ] ||
									(outerCache[ node.uniqueID ] = {});

								cache = uniqueCache[ type ] || [];
								nodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];
								diff = nodeIndex;
							}

							// xml :nth-child(...)
							// or :nth-last-child(...) or :nth(-last)?-of-type(...)
							if ( diff === false ) {
								// Use the same loop as above to seek `elem` from the start
								while ( (node = ++nodeIndex && node && node[ dir ] ||
									(diff = nodeIndex = 0) || start.pop()) ) {

									if ( ( ofType ?
										node.nodeName.toLowerCase() === name :
										node.nodeType === 1 ) &&
										++diff ) {

										// Cache the index of each encountered element
										if ( useCache ) {
											outerCache = node[ expando ] || (node[ expando ] = {});

											// Support: IE <9 only
											// Defend against cloned attroperties (jQuery gh-1709)
											uniqueCache = outerCache[ node.uniqueID ] ||
												(outerCache[ node.uniqueID ] = {});

											uniqueCache[ type ] = [ dirruns, diff ];
										}

										if ( node === elem ) {
											break;
										}
									}
								}
							}
						}

						// Incorporate the offset, then check against cycle size
						diff -= last;
						return diff === first || ( diff % first === 0 && diff / first >= 0 );
					}
				};
		},

		"PSEUDO": function( pseudo, argument ) {
			// pseudo-class names are case-insensitive
			// http://www.w3.org/TR/selectors/#pseudo-classes
			// Prioritize by case sensitivity in case custom pseudos are added with uppercase letters
			// Remember that setFilters inherits from pseudos
			var args,
				fn = Expr.pseudos[ pseudo ] || Expr.setFilters[ pseudo.toLowerCase() ] ||
					Sizzle.error( "unsupported pseudo: " + pseudo );

			// The user may use createPseudo to indicate that
			// arguments are needed to create the filter function
			// just as Sizzle does
			if ( fn[ expando ] ) {
				return fn( argument );
			}

			// But maintain support for old signatures
			if ( fn.length > 1 ) {
				args = [ pseudo, pseudo, "", argument ];
				return Expr.setFilters.hasOwnProperty( pseudo.toLowerCase() ) ?
					markFunction(function( seed, matches ) {
						var idx,
							matched = fn( seed, argument ),
							i = matched.length;
						while ( i-- ) {
							idx = indexOf( seed, matched[i] );
							seed[ idx ] = !( matches[ idx ] = matched[i] );
						}
					}) :
					function( elem ) {
						return fn( elem, 0, args );
					};
			}

			return fn;
		}
	},

	pseudos: {
		// Potentially complex pseudos
		"not": markFunction(function( selector ) {
			// Trim the selector passed to compile
			// to avoid treating leading and trailing
			// spaces as combinators
			var input = [],
				results = [],
				matcher = compile( selector.replace( rtrim, "$1" ) );

			return matcher[ expando ] ?
				markFunction(function( seed, matches, context, xml ) {
					var elem,
						unmatched = matcher( seed, null, xml, [] ),
						i = seed.length;

					// Match elements unmatched by `matcher`
					while ( i-- ) {
						if ( (elem = unmatched[i]) ) {
							seed[i] = !(matches[i] = elem);
						}
					}
				}) :
				function( elem, context, xml ) {
					input[0] = elem;
					matcher( input, null, xml, results );
					// Don't keep the element (issue #299)
					input[0] = null;
					return !results.pop();
				};
		}),

		"has": markFunction(function( selector ) {
			return function( elem ) {
				return Sizzle( selector, elem ).length > 0;
			};
		}),

		"contains": markFunction(function( text ) {
			text = text.replace( runescape, funescape );
			return function( elem ) {
				return ( elem.textContent || elem.innerText || getText( elem ) ).indexOf( text ) > -1;
			};
		}),

		// "Whether an element is represented by a :lang() selector
		// is based solely on the element's language value
		// being equal to the identifier C,
		// or beginning with the identifier C immediately followed by "-".
		// The matching of C against the element's language value is performed case-insensitively.
		// The identifier C does not have to be a valid language name."
		// http://www.w3.org/TR/selectors/#lang-pseudo
		"lang": markFunction( function( lang ) {
			// lang value must be a valid identifier
			if ( !ridentifier.test(lang || "") ) {
				Sizzle.error( "unsupported lang: " + lang );
			}
			lang = lang.replace( runescape, funescape ).toLowerCase();
			return function( elem ) {
				var elemLang;
				do {
					if ( (elemLang = documentIsHTML ?
						elem.lang :
						elem.getAttribute("xml:lang") || elem.getAttribute("lang")) ) {

						elemLang = elemLang.toLowerCase();
						return elemLang === lang || elemLang.indexOf( lang + "-" ) === 0;
					}
				} while ( (elem = elem.parentNode) && elem.nodeType === 1 );
				return false;
			};
		}),

		// Miscellaneous
		"target": function( elem ) {
			var hash = window.location && window.location.hash;
			return hash && hash.slice( 1 ) === elem.id;
		},

		"root": function( elem ) {
			return elem === docElem;
		},

		"focus": function( elem ) {
			return elem === document.activeElement && (!document.hasFocus || document.hasFocus()) && !!(elem.type || elem.href || ~elem.tabIndex);
		},

		// Boolean properties
		"enabled": function( elem ) {
			return elem.disabled === false;
		},

		"disabled": function( elem ) {
			return elem.disabled === true;
		},

		"checked": function( elem ) {
			// In CSS3, :checked should return both checked and selected elements
			// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
			var nodeName = elem.nodeName.toLowerCase();
			return (nodeName === "input" && !!elem.checked) || (nodeName === "option" && !!elem.selected);
		},

		"selected": function( elem ) {
			// Accessing this property makes selected-by-default
			// options in Safari work properly
			if ( elem.parentNode ) {
				elem.parentNode.selectedIndex;
			}

			return elem.selected === true;
		},

		// Contents
		"empty": function( elem ) {
			// http://www.w3.org/TR/selectors/#empty-pseudo
			// :empty is negated by element (1) or content nodes (text: 3; cdata: 4; entity ref: 5),
			//   but not by others (comment: 8; processing instruction: 7; etc.)
			// nodeType < 6 works because attributes (2) do not appear as children
			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
				if ( elem.nodeType < 6 ) {
					return false;
				}
			}
			return true;
		},

		"parent": function( elem ) {
			return !Expr.pseudos["empty"]( elem );
		},

		// Element/input types
		"header": function( elem ) {
			return rheader.test( elem.nodeName );
		},

		"input": function( elem ) {
			return rinputs.test( elem.nodeName );
		},

		"button": function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return name === "input" && elem.type === "button" || name === "button";
		},

		"text": function( elem ) {
			var attr;
			return elem.nodeName.toLowerCase() === "input" &&
				elem.type === "text" &&

				// Support: IE<8
				// New HTML5 attribute values (e.g., "search") appear with elem.type === "text"
				( (attr = elem.getAttribute("type")) == null || attr.toLowerCase() === "text" );
		},

		// Position-in-collection
		"first": createPositionalPseudo(function() {
			return [ 0 ];
		}),

		"last": createPositionalPseudo(function( matchIndexes, length ) {
			return [ length - 1 ];
		}),

		"eq": createPositionalPseudo(function( matchIndexes, length, argument ) {
			return [ argument < 0 ? argument + length : argument ];
		}),

		"even": createPositionalPseudo(function( matchIndexes, length ) {
			var i = 0;
			for ( ; i < length; i += 2 ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"odd": createPositionalPseudo(function( matchIndexes, length ) {
			var i = 1;
			for ( ; i < length; i += 2 ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"lt": createPositionalPseudo(function( matchIndexes, length, argument ) {
			var i = argument < 0 ? argument + length : argument;
			for ( ; --i >= 0; ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"gt": createPositionalPseudo(function( matchIndexes, length, argument ) {
			var i = argument < 0 ? argument + length : argument;
			for ( ; ++i < length; ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		})
	}
};

Expr.pseudos["nth"] = Expr.pseudos["eq"];

// Add button/input type pseudos
for ( i in { radio: true, checkbox: true, file: true, password: true, image: true } ) {
	Expr.pseudos[ i ] = createInputPseudo( i );
}
for ( i in { submit: true, reset: true } ) {
	Expr.pseudos[ i ] = createButtonPseudo( i );
}

// Easy API for creating new setFilters
function setFilters() {}
setFilters.prototype = Expr.filters = Expr.pseudos;
Expr.setFilters = new setFilters();

tokenize = Sizzle.tokenize = function( selector, parseOnly ) {
	var matched, match, tokens, type,
		soFar, groups, preFilters,
		cached = tokenCache[ selector + " " ];

	if ( cached ) {
		return parseOnly ? 0 : cached.slice( 0 );
	}

	soFar = selector;
	groups = [];
	preFilters = Expr.preFilter;

	while ( soFar ) {

		// Comma and first run
		if ( !matched || (match = rcomma.exec( soFar )) ) {
			if ( match ) {
				// Don't consume trailing commas as valid
				soFar = soFar.slice( match[0].length ) || soFar;
			}
			groups.push( (tokens = []) );
		}

		matched = false;

		// Combinators
		if ( (match = rcombinators.exec( soFar )) ) {
			matched = match.shift();
			tokens.push({
				value: matched,
				// Cast descendant combinators to space
				type: match[0].replace( rtrim, " " )
			});
			soFar = soFar.slice( matched.length );
		}

		// Filters
		for ( type in Expr.filter ) {
			if ( (match = matchExpr[ type ].exec( soFar )) && (!preFilters[ type ] ||
				(match = preFilters[ type ]( match ))) ) {
				matched = match.shift();
				tokens.push({
					value: matched,
					type: type,
					matches: match
				});
				soFar = soFar.slice( matched.length );
			}
		}

		if ( !matched ) {
			break;
		}
	}

	// Return the length of the invalid excess
	// if we're just parsing
	// Otherwise, throw an error or return tokens
	return parseOnly ?
		soFar.length :
		soFar ?
			Sizzle.error( selector ) :
			// Cache the tokens
			tokenCache( selector, groups ).slice( 0 );
};

function toSelector( tokens ) {
	var i = 0,
		len = tokens.length,
		selector = "";
	for ( ; i < len; i++ ) {
		selector += tokens[i].value;
	}
	return selector;
}

function addCombinator( matcher, combinator, base ) {
	var dir = combinator.dir,
		checkNonElements = base && dir === "parentNode",
		doneName = done++;

	return combinator.first ?
		// Check against closest ancestor/preceding element
		function( elem, context, xml ) {
			while ( (elem = elem[ dir ]) ) {
				if ( elem.nodeType === 1 || checkNonElements ) {
					return matcher( elem, context, xml );
				}
			}
		} :

		// Check against all ancestor/preceding elements
		function( elem, context, xml ) {
			var oldCache, uniqueCache, outerCache,
				newCache = [ dirruns, doneName ];

			// We can't set arbitrary data on XML nodes, so they don't benefit from combinator caching
			if ( xml ) {
				while ( (elem = elem[ dir ]) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						if ( matcher( elem, context, xml ) ) {
							return true;
						}
					}
				}
			} else {
				while ( (elem = elem[ dir ]) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						outerCache = elem[ expando ] || (elem[ expando ] = {});

						// Support: IE <9 only
						// Defend against cloned attroperties (jQuery gh-1709)
						uniqueCache = outerCache[ elem.uniqueID ] || (outerCache[ elem.uniqueID ] = {});

						if ( (oldCache = uniqueCache[ dir ]) &&
							oldCache[ 0 ] === dirruns && oldCache[ 1 ] === doneName ) {

							// Assign to newCache so results back-propagate to previous elements
							return (newCache[ 2 ] = oldCache[ 2 ]);
						} else {
							// Reuse newcache so results back-propagate to previous elements
							uniqueCache[ dir ] = newCache;

							// A match means we're done; a fail means we have to keep checking
							if ( (newCache[ 2 ] = matcher( elem, context, xml )) ) {
								return true;
							}
						}
					}
				}
			}
		};
}

function elementMatcher( matchers ) {
	return matchers.length > 1 ?
		function( elem, context, xml ) {
			var i = matchers.length;
			while ( i-- ) {
				if ( !matchers[i]( elem, context, xml ) ) {
					return false;
				}
			}
			return true;
		} :
		matchers[0];
}

function multipleContexts( selector, contexts, results ) {
	var i = 0,
		len = contexts.length;
	for ( ; i < len; i++ ) {
		Sizzle( selector, contexts[i], results );
	}
	return results;
}

function condense( unmatched, map, filter, context, xml ) {
	var elem,
		newUnmatched = [],
		i = 0,
		len = unmatched.length,
		mapped = map != null;

	for ( ; i < len; i++ ) {
		if ( (elem = unmatched[i]) ) {
			if ( !filter || filter( elem, context, xml ) ) {
				newUnmatched.push( elem );
				if ( mapped ) {
					map.push( i );
				}
			}
		}
	}

	return newUnmatched;
}

function setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {
	if ( postFilter && !postFilter[ expando ] ) {
		postFilter = setMatcher( postFilter );
	}
	if ( postFinder && !postFinder[ expando ] ) {
		postFinder = setMatcher( postFinder, postSelector );
	}
	return markFunction(function( seed, results, context, xml ) {
		var temp, i, elem,
			preMap = [],
			postMap = [],
			preexisting = results.length,

			// Get initial elements from seed or context
			elems = seed || multipleContexts( selector || "*", context.nodeType ? [ context ] : context, [] ),

			// Prefilter to get matcher input, preserving a map for seed-results synchronization
			matcherIn = preFilter && ( seed || !selector ) ?
				condense( elems, preMap, preFilter, context, xml ) :
				elems,

			matcherOut = matcher ?
				// If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,
				postFinder || ( seed ? preFilter : preexisting || postFilter ) ?

					// ...intermediate processing is necessary
					[] :

					// ...otherwise use results directly
					results :
				matcherIn;

		// Find primary matches
		if ( matcher ) {
			matcher( matcherIn, matcherOut, context, xml );
		}

		// Apply postFilter
		if ( postFilter ) {
			temp = condense( matcherOut, postMap );
			postFilter( temp, [], context, xml );

			// Un-match failing elements by moving them back to matcherIn
			i = temp.length;
			while ( i-- ) {
				if ( (elem = temp[i]) ) {
					matcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);
				}
			}
		}

		if ( seed ) {
			if ( postFinder || preFilter ) {
				if ( postFinder ) {
					// Get the final matcherOut by condensing this intermediate into postFinder contexts
					temp = [];
					i = matcherOut.length;
					while ( i-- ) {
						if ( (elem = matcherOut[i]) ) {
							// Restore matcherIn since elem is not yet a final match
							temp.push( (matcherIn[i] = elem) );
						}
					}
					postFinder( null, (matcherOut = []), temp, xml );
				}

				// Move matched elements from seed to results to keep them synchronized
				i = matcherOut.length;
				while ( i-- ) {
					if ( (elem = matcherOut[i]) &&
						(temp = postFinder ? indexOf( seed, elem ) : preMap[i]) > -1 ) {

						seed[temp] = !(results[temp] = elem);
					}
				}
			}

		// Add elements to results, through postFinder if defined
		} else {
			matcherOut = condense(
				matcherOut === results ?
					matcherOut.splice( preexisting, matcherOut.length ) :
					matcherOut
			);
			if ( postFinder ) {
				postFinder( null, results, matcherOut, xml );
			} else {
				push.apply( results, matcherOut );
			}
		}
	});
}

function matcherFromTokens( tokens ) {
	var checkContext, matcher, j,
		len = tokens.length,
		leadingRelative = Expr.relative[ tokens[0].type ],
		implicitRelative = leadingRelative || Expr.relative[" "],
		i = leadingRelative ? 1 : 0,

		// The foundational matcher ensures that elements are reachable from top-level context(s)
		matchContext = addCombinator( function( elem ) {
			return elem === checkContext;
		}, implicitRelative, true ),
		matchAnyContext = addCombinator( function( elem ) {
			return indexOf( checkContext, elem ) > -1;
		}, implicitRelative, true ),
		matchers = [ function( elem, context, xml ) {
			var ret = ( !leadingRelative && ( xml || context !== outermostContext ) ) || (
				(checkContext = context).nodeType ?
					matchContext( elem, context, xml ) :
					matchAnyContext( elem, context, xml ) );
			// Avoid hanging onto element (issue #299)
			checkContext = null;
			return ret;
		} ];

	for ( ; i < len; i++ ) {
		if ( (matcher = Expr.relative[ tokens[i].type ]) ) {
			matchers = [ addCombinator(elementMatcher( matchers ), matcher) ];
		} else {
			matcher = Expr.filter[ tokens[i].type ].apply( null, tokens[i].matches );

			// Return special upon seeing a positional matcher
			if ( matcher[ expando ] ) {
				// Find the next relative operator (if any) for proper handling
				j = ++i;
				for ( ; j < len; j++ ) {
					if ( Expr.relative[ tokens[j].type ] ) {
						break;
					}
				}
				return setMatcher(
					i > 1 && elementMatcher( matchers ),
					i > 1 && toSelector(
						// If the preceding token was a descendant combinator, insert an implicit any-element `*`
						tokens.slice( 0, i - 1 ).concat({ value: tokens[ i - 2 ].type === " " ? "*" : "" })
					).replace( rtrim, "$1" ),
					matcher,
					i < j && matcherFromTokens( tokens.slice( i, j ) ),
					j < len && matcherFromTokens( (tokens = tokens.slice( j )) ),
					j < len && toSelector( tokens )
				);
			}
			matchers.push( matcher );
		}
	}

	return elementMatcher( matchers );
}

function matcherFromGroupMatchers( elementMatchers, setMatchers ) {
	var bySet = setMatchers.length > 0,
		byElement = elementMatchers.length > 0,
		superMatcher = function( seed, context, xml, results, outermost ) {
			var elem, j, matcher,
				matchedCount = 0,
				i = "0",
				unmatched = seed && [],
				setMatched = [],
				contextBackup = outermostContext,
				// We must always have either seed elements or outermost context
				elems = seed || byElement && Expr.find["TAG"]( "*", outermost ),
				// Use integer dirruns iff this is the outermost matcher
				dirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.random() || 0.1),
				len = elems.length;

			if ( outermost ) {
				outermostContext = context === document || context || outermost;
			}

			// Add elements passing elementMatchers directly to results
			// Support: IE<9, Safari
			// Tolerate NodeList properties (IE: "length"; Safari: <number>) matching elements by id
			for ( ; i !== len && (elem = elems[i]) != null; i++ ) {
				if ( byElement && elem ) {
					j = 0;
					if ( !context && elem.ownerDocument !== document ) {
						setDocument( elem );
						xml = !documentIsHTML;
					}
					while ( (matcher = elementMatchers[j++]) ) {
						if ( matcher( elem, context || document, xml) ) {
							results.push( elem );
							break;
						}
					}
					if ( outermost ) {
						dirruns = dirrunsUnique;
					}
				}

				// Track unmatched elements for set filters
				if ( bySet ) {
					// They will have gone through all possible matchers
					if ( (elem = !matcher && elem) ) {
						matchedCount--;
					}

					// Lengthen the array for every element, matched or not
					if ( seed ) {
						unmatched.push( elem );
					}
				}
			}

			// `i` is now the count of elements visited above, and adding it to `matchedCount`
			// makes the latter nonnegative.
			matchedCount += i;

			// Apply set filters to unmatched elements
			// NOTE: This can be skipped if there are no unmatched elements (i.e., `matchedCount`
			// equals `i`), unless we didn't visit _any_ elements in the above loop because we have
			// no element matchers and no seed.
			// Incrementing an initially-string "0" `i` allows `i` to remain a string only in that
			// case, which will result in a "00" `matchedCount` that differs from `i` but is also
			// numerically zero.
			if ( bySet && i !== matchedCount ) {
				j = 0;
				while ( (matcher = setMatchers[j++]) ) {
					matcher( unmatched, setMatched, context, xml );
				}

				if ( seed ) {
					// Reintegrate element matches to eliminate the need for sorting
					if ( matchedCount > 0 ) {
						while ( i-- ) {
							if ( !(unmatched[i] || setMatched[i]) ) {
								setMatched[i] = pop.call( results );
							}
						}
					}

					// Discard index placeholder values to get only actual matches
					setMatched = condense( setMatched );
				}

				// Add matches to results
				push.apply( results, setMatched );

				// Seedless set matches succeeding multiple successful matchers stipulate sorting
				if ( outermost && !seed && setMatched.length > 0 &&
					( matchedCount + setMatchers.length ) > 1 ) {

					Sizzle.uniqueSort( results );
				}
			}

			// Override manipulation of globals by nested matchers
			if ( outermost ) {
				dirruns = dirrunsUnique;
				outermostContext = contextBackup;
			}

			return unmatched;
		};

	return bySet ?
		markFunction( superMatcher ) :
		superMatcher;
}

compile = Sizzle.compile = function( selector, match /* Internal Use Only */ ) {
	var i,
		setMatchers = [],
		elementMatchers = [],
		cached = compilerCache[ selector + " " ];

	if ( !cached ) {
		// Generate a function of recursive functions that can be used to check each element
		if ( !match ) {
			match = tokenize( selector );
		}
		i = match.length;
		while ( i-- ) {
			cached = matcherFromTokens( match[i] );
			if ( cached[ expando ] ) {
				setMatchers.push( cached );
			} else {
				elementMatchers.push( cached );
			}
		}

		// Cache the compiled function
		cached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );

		// Save selector and tokenization
		cached.selector = selector;
	}
	return cached;
};

/**
 * A low-level selection function that works with Sizzle's compiled
 *  selector functions
 * @param {String|Function} selector A selector or a pre-compiled
 *  selector function built with Sizzle.compile
 * @param {Element} context
 * @param {Array} [results]
 * @param {Array} [seed] A set of elements to match against
 */
select = Sizzle.select = function( selector, context, results, seed ) {
	var i, tokens, token, type, find,
		compiled = typeof selector === "function" && selector,
		match = !seed && tokenize( (selector = compiled.selector || selector) );

	results = results || [];

	// Try to minimize operations if there is only one selector in the list and no seed
	// (the latter of which guarantees us context)
	if ( match.length === 1 ) {

		// Reduce context if the leading compound selector is an ID
		tokens = match[0] = match[0].slice( 0 );
		if ( tokens.length > 2 && (token = tokens[0]).type === "ID" &&
				support.getById && context.nodeType === 9 && documentIsHTML &&
				Expr.relative[ tokens[1].type ] ) {

			context = ( Expr.find["ID"]( token.matches[0].replace(runescape, funescape), context ) || [] )[0];
			if ( !context ) {
				return results;

			// Precompiled matchers will still verify ancestry, so step up a level
			} else if ( compiled ) {
				context = context.parentNode;
			}

			selector = selector.slice( tokens.shift().value.length );
		}

		// Fetch a seed set for right-to-left matching
		i = matchExpr["needsContext"].test( selector ) ? 0 : tokens.length;
		while ( i-- ) {
			token = tokens[i];

			// Abort if we hit a combinator
			if ( Expr.relative[ (type = token.type) ] ) {
				break;
			}
			if ( (find = Expr.find[ type ]) ) {
				// Search, expanding context for leading sibling combinators
				if ( (seed = find(
					token.matches[0].replace( runescape, funescape ),
					rsibling.test( tokens[0].type ) && testContext( context.parentNode ) || context
				)) ) {

					// If seed is empty or no tokens remain, we can return early
					tokens.splice( i, 1 );
					selector = seed.length && toSelector( tokens );
					if ( !selector ) {
						push.apply( results, seed );
						return results;
					}

					break;
				}
			}
		}
	}

	// Compile and execute a filtering function if one is not provided
	// Provide `match` to avoid retokenization if we modified the selector above
	( compiled || compile( selector, match ) )(
		seed,
		context,
		!documentIsHTML,
		results,
		!context || rsibling.test( selector ) && testContext( context.parentNode ) || context
	);
	return results;
};

// One-time assignments

// Sort stability
support.sortStable = expando.split("").sort( sortOrder ).join("") === expando;

// Support: Chrome 14-35+
// Always assume duplicates if they aren't passed to the comparison function
support.detectDuplicates = !!hasDuplicate;

// Initialize against the default document
setDocument();

// Support: Webkit<537.32 - Safari 6.0.3/Chrome 25 (fixed in Chrome 27)
// Detached nodes confoundingly follow *each other*
support.sortDetached = assert(function( div1 ) {
	// Should return 1, but returns 4 (following)
	return div1.compareDocumentPosition( document.createElement("div") ) & 1;
});

// Support: IE<8
// Prevent attribute/property "interpolation"
// http://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx
if ( !assert(function( div ) {
	div.innerHTML = "<a href='#'></a>";
	return div.firstChild.getAttribute("href") === "#" ;
}) ) {
	addHandle( "type|href|height|width", function( elem, name, isXML ) {
		if ( !isXML ) {
			return elem.getAttribute( name, name.toLowerCase() === "type" ? 1 : 2 );
		}
	});
}

// Support: IE<9
// Use defaultValue in place of getAttribute("value")
if ( !support.attributes || !assert(function( div ) {
	div.innerHTML = "<input/>";
	div.firstChild.setAttribute( "value", "" );
	return div.firstChild.getAttribute( "value" ) === "";
}) ) {
	addHandle( "value", function( elem, name, isXML ) {
		if ( !isXML && elem.nodeName.toLowerCase() === "input" ) {
			return elem.defaultValue;
		}
	});
}

// Support: IE<9
// Use getAttributeNode to fetch booleans when getAttribute lies
if ( !assert(function( div ) {
	return div.getAttribute("disabled") == null;
}) ) {
	addHandle( booleans, function( elem, name, isXML ) {
		var val;
		if ( !isXML ) {
			return elem[ name ] === true ? name.toLowerCase() :
					(val = elem.getAttributeNode( name )) && val.specified ?
					val.value :
				null;
		}
	});
}

return Sizzle;

})( window );



jQuery.find = Sizzle;
jQuery.expr = Sizzle.selectors;
jQuery.expr[ ":" ] = jQuery.expr.pseudos;
jQuery.uniqueSort = jQuery.unique = Sizzle.uniqueSort;
jQuery.text = Sizzle.getText;
jQuery.isXMLDoc = Sizzle.isXML;
jQuery.contains = Sizzle.contains;



var dir = function( elem, dir, until ) {
	var matched = [],
		truncate = until !== undefined;

	while ( ( elem = elem[ dir ] ) && elem.nodeType !== 9 ) {
		if ( elem.nodeType === 1 ) {
			if ( truncate && jQuery( elem ).is( until ) ) {
				break;
			}
			matched.push( elem );
		}
	}
	return matched;
};


var siblings = function( n, elem ) {
	var matched = [];

	for ( ; n; n = n.nextSibling ) {
		if ( n.nodeType === 1 && n !== elem ) {
			matched.push( n );
		}
	}

	return matched;
};


var rneedsContext = jQuery.expr.match.needsContext;

var rsingleTag = ( /^<([\w-]+)\s*\/?>(?:<\/\1>|)$/ );



var risSimple = /^.[^:#\[\.,]*$/;

// Implement the identical functionality for filter and not
function winnow( elements, qualifier, not ) {
	if ( jQuery.isFunction( qualifier ) ) {
		return jQuery.grep( elements, function( elem, i ) {
			/* jshint -W018 */
			return !!qualifier.call( elem, i, elem ) !== not;
		} );

	}

	if ( qualifier.nodeType ) {
		return jQuery.grep( elements, function( elem ) {
			return ( elem === qualifier ) !== not;
		} );

	}

	if ( typeof qualifier === "string" ) {
		if ( risSimple.test( qualifier ) ) {
			return jQuery.filter( qualifier, elements, not );
		}

		qualifier = jQuery.filter( qualifier, elements );
	}

	return jQuery.grep( elements, function( elem ) {
		return ( jQuery.inArray( elem, qualifier ) > -1 ) !== not;
	} );
}

jQuery.filter = function( expr, elems, not ) {
	var elem = elems[ 0 ];

	if ( not ) {
		expr = ":not(" + expr + ")";
	}

	return elems.length === 1 && elem.nodeType === 1 ?
		jQuery.find.matchesSelector( elem, expr ) ? [ elem ] : [] :
		jQuery.find.matches( expr, jQuery.grep( elems, function( elem ) {
			return elem.nodeType === 1;
		} ) );
};

jQuery.fn.extend( {
	find: function( selector ) {
		var i,
			ret = [],
			self = this,
			len = self.length;

		if ( typeof selector !== "string" ) {
			return this.pushStack( jQuery( selector ).filter( function() {
				for ( i = 0; i < len; i++ ) {
					if ( jQuery.contains( self[ i ], this ) ) {
						return true;
					}
				}
			} ) );
		}

		for ( i = 0; i < len; i++ ) {
			jQuery.find( selector, self[ i ], ret );
		}

		// Needed because $( selector, context ) becomes $( context ).find( selector )
		ret = this.pushStack( len > 1 ? jQuery.unique( ret ) : ret );
		ret.selector = this.selector ? this.selector + " " + selector : selector;
		return ret;
	},
	filter: function( selector ) {
		return this.pushStack( winnow( this, selector || [], false ) );
	},
	not: function( selector ) {
		return this.pushStack( winnow( this, selector || [], true ) );
	},
	is: function( selector ) {
		return !!winnow(
			this,

			// If this is a positional/relative selector, check membership in the returned set
			// so $("p:first").is("p:last") won't return true for a doc with two "p".
			typeof selector === "string" && rneedsContext.test( selector ) ?
				jQuery( selector ) :
				selector || [],
			false
		).length;
	}
} );


// Initialize a jQuery object


// A central reference to the root jQuery(document)
var rootjQuery,

	// A simple way to check for HTML strings
	// Prioritize #id over <tag> to avoid XSS via location.hash (#9521)
	// Strict HTML recognition (#11290: must start with <)
	rquickExpr = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]*))$/,

	init = jQuery.fn.init = function( selector, context, root ) {
		var match, elem;

		// HANDLE: $(""), $(null), $(undefined), $(false)
		if ( !selector ) {
			return this;
		}

		// init accepts an alternate rootjQuery
		// so migrate can support jQuery.sub (gh-2101)
		root = root || rootjQuery;

		// Handle HTML strings
		if ( typeof selector === "string" ) {
			if ( selector.charAt( 0 ) === "<" &&
				selector.charAt( selector.length - 1 ) === ">" &&
				selector.length >= 3 ) {

				// Assume that strings that start and end with <> are HTML and skip the regex check
				match = [ null, selector, null ];

			} else {
				match = rquickExpr.exec( selector );
			}

			// Match html or make sure no context is specified for #id
			if ( match && ( match[ 1 ] || !context ) ) {

				// HANDLE: $(html) -> $(array)
				if ( match[ 1 ] ) {
					context = context instanceof jQuery ? context[ 0 ] : context;

					// scripts is true for back-compat
					// Intentionally let the error be thrown if parseHTML is not present
					jQuery.merge( this, jQuery.parseHTML(
						match[ 1 ],
						context && context.nodeType ? context.ownerDocument || context : document,
						true
					) );

					// HANDLE: $(html, props)
					if ( rsingleTag.test( match[ 1 ] ) && jQuery.isPlainObject( context ) ) {
						for ( match in context ) {

							// Properties of context are called as methods if possible
							if ( jQuery.isFunction( this[ match ] ) ) {
								this[ match ]( context[ match ] );

							// ...and otherwise set as attributes
							} else {
								this.attr( match, context[ match ] );
							}
						}
					}

					return this;

				// HANDLE: $(#id)
				} else {
					elem = document.getElementById( match[ 2 ] );

					// Check parentNode to catch when Blackberry 4.6 returns
					// nodes that are no longer in the document #6963
					if ( elem && elem.parentNode ) {

						// Handle the case where IE and Opera return items
						// by name instead of ID
						if ( elem.id !== match[ 2 ] ) {
							return rootjQuery.find( selector );
						}

						// Otherwise, we inject the element directly into the jQuery object
						this.length = 1;
						this[ 0 ] = elem;
					}

					this.context = document;
					this.selector = selector;
					return this;
				}

			// HANDLE: $(expr, $(...))
			} else if ( !context || context.jquery ) {
				return ( context || root ).find( selector );

			// HANDLE: $(expr, context)
			// (which is just equivalent to: $(context).find(expr)
			} else {
				return this.constructor( context ).find( selector );
			}

		// HANDLE: $(DOMElement)
		} else if ( selector.nodeType ) {
			this.context = this[ 0 ] = selector;
			this.length = 1;
			return this;

		// HANDLE: $(function)
		// Shortcut for document ready
		} else if ( jQuery.isFunction( selector ) ) {
			return typeof root.ready !== "undefined" ?
				root.ready( selector ) :

				// Execute immediately if ready is not present
				selector( jQuery );
		}

		if ( selector.selector !== undefined ) {
			this.selector = selector.selector;
			this.context = selector.context;
		}

		return jQuery.makeArray( selector, this );
	};

// Give the init function the jQuery prototype for later instantiation
init.prototype = jQuery.fn;

// Initialize central reference
rootjQuery = jQuery( document );


var rparentsprev = /^(?:parents|prev(?:Until|All))/,

	// methods guaranteed to produce a unique set when starting from a unique set
	guaranteedUnique = {
		children: true,
		contents: true,
		next: true,
		prev: true
	};

jQuery.fn.extend( {
	has: function( target ) {
		var i,
			targets = jQuery( target, this ),
			len = targets.length;

		return this.filter( function() {
			for ( i = 0; i < len; i++ ) {
				if ( jQuery.contains( this, targets[ i ] ) ) {
					return true;
				}
			}
		} );
	},

	closest: function( selectors, context ) {
		var cur,
			i = 0,
			l = this.length,
			matched = [],
			pos = rneedsContext.test( selectors ) || typeof selectors !== "string" ?
				jQuery( selectors, context || this.context ) :
				0;

		for ( ; i < l; i++ ) {
			for ( cur = this[ i ]; cur && cur !== context; cur = cur.parentNode ) {

				// Always skip document fragments
				if ( cur.nodeType < 11 && ( pos ?
					pos.index( cur ) > -1 :

					// Don't pass non-elements to Sizzle
					cur.nodeType === 1 &&
						jQuery.find.matchesSelector( cur, selectors ) ) ) {

					matched.push( cur );
					break;
				}
			}
		}

		return this.pushStack( matched.length > 1 ? jQuery.uniqueSort( matched ) : matched );
	},

	// Determine the position of an element within
	// the matched set of elements
	index: function( elem ) {

		// No argument, return index in parent
		if ( !elem ) {
			return ( this[ 0 ] && this[ 0 ].parentNode ) ? this.first().prevAll().length : -1;
		}

		// index in selector
		if ( typeof elem === "string" ) {
			return jQuery.inArray( this[ 0 ], jQuery( elem ) );
		}

		// Locate the position of the desired element
		return jQuery.inArray(

			// If it receives a jQuery object, the first element is used
			elem.jquery ? elem[ 0 ] : elem, this );
	},

	add: function( selector, context ) {
		return this.pushStack(
			jQuery.uniqueSort(
				jQuery.merge( this.get(), jQuery( selector, context ) )
			)
		);
	},

	addBack: function( selector ) {
		return this.add( selector == null ?
			this.prevObject : this.prevObject.filter( selector )
		);
	}
} );

function sibling( cur, dir ) {
	do {
		cur = cur[ dir ];
	} while ( cur && cur.nodeType !== 1 );

	return cur;
}

jQuery.each( {
	parent: function( elem ) {
		var parent = elem.parentNode;
		return parent && parent.nodeType !== 11 ? parent : null;
	},
	parents: function( elem ) {
		return dir( elem, "parentNode" );
	},
	parentsUntil: function( elem, i, until ) {
		return dir( elem, "parentNode", until );
	},
	next: function( elem ) {
		return sibling( elem, "nextSibling" );
	},
	prev: function( elem ) {
		return sibling( elem, "previousSibling" );
	},
	nextAll: function( elem ) {
		return dir( elem, "nextSibling" );
	},
	prevAll: function( elem ) {
		return dir( elem, "previousSibling" );
	},
	nextUntil: function( elem, i, until ) {
		return dir( elem, "nextSibling", until );
	},
	prevUntil: function( elem, i, until ) {
		return dir( elem, "previousSibling", until );
	},
	siblings: function( elem ) {
		return siblings( ( elem.parentNode || {} ).firstChild, elem );
	},
	children: function( elem ) {
		return siblings( elem.firstChild );
	},
	contents: function( elem ) {
		return jQuery.nodeName( elem, "iframe" ) ?
			elem.contentDocument || elem.contentWindow.document :
			jQuery.merge( [], elem.childNodes );
	}
}, function( name, fn ) {
	jQuery.fn[ name ] = function( until, selector ) {
		var ret = jQuery.map( this, fn, until );

		if ( name.slice( -5 ) !== "Until" ) {
			selector = until;
		}

		if ( selector && typeof selector === "string" ) {
			ret = jQuery.filter( selector, ret );
		}

		if ( this.length > 1 ) {

			// Remove duplicates
			if ( !guaranteedUnique[ name ] ) {
				ret = jQuery.uniqueSort( ret );
			}

			// Reverse order for parents* and prev-derivatives
			if ( rparentsprev.test( name ) ) {
				ret = ret.reverse();
			}
		}

		return this.pushStack( ret );
	};
} );
var rnotwhite = ( /\S+/g );



// Convert String-formatted options into Object-formatted ones
function createOptions( options ) {
	var object = {};
	jQuery.each( options.match( rnotwhite ) || [], function( _, flag ) {
		object[ flag ] = true;
	} );
	return object;
}

/*
 * Create a callback list using the following parameters:
 *
 *	options: an optional list of space-separated options that will change how
 *			the callback list behaves or a more traditional option object
 *
 * By default a callback list will act like an event callback list and can be
 * "fired" multiple times.
 *
 * Possible options:
 *
 *	once:			will ensure the callback list can only be fired once (like a Deferred)
 *
 *	memory:			will keep track of previous values and will call any callback added
 *					after the list has been fired right away with the latest "memorized"
 *					values (like a Deferred)
 *
 *	unique:			will ensure a callback can only be added once (no duplicate in the list)
 *
 *	stopOnFalse:	interrupt callings when a callback returns false
 *
 */
jQuery.Callbacks = function( options ) {

	// Convert options from String-formatted to Object-formatted if needed
	// (we check in cache first)
	options = typeof options === "string" ?
		createOptions( options ) :
		jQuery.extend( {}, options );

	var // Flag to know if list is currently firing
		firing,

		// Last fire value for non-forgettable lists
		memory,

		// Flag to know if list was already fired
		fired,

		// Flag to prevent firing
		locked,

		// Actual callback list
		list = [],

		// Queue of execution data for repeatable lists
		queue = [],

		// Index of currently firing callback (modified by add/remove as needed)
		firingIndex = -1,

		// Fire callbacks
		fire = function() {

			// Enforce single-firing
			locked = options.once;

			// Execute callbacks for all pending executions,
			// respecting firingIndex overrides and runtime changes
			fired = firing = true;
			for ( ; queue.length; firingIndex = -1 ) {
				memory = queue.shift();
				while ( ++firingIndex < list.length ) {

					// Run callback and check for early termination
					if ( list[ firingIndex ].apply( memory[ 0 ], memory[ 1 ] ) === false &&
						options.stopOnFalse ) {

						// Jump to end and forget the data so .add doesn't re-fire
						firingIndex = list.length;
						memory = false;
					}
				}
			}

			// Forget the data if we're done with it
			if ( !options.memory ) {
				memory = false;
			}

			firing = false;

			// Clean up if we're done firing for good
			if ( locked ) {

				// Keep an empty list if we have data for future add calls
				if ( memory ) {
					list = [];

				// Otherwise, this object is spent
				} else {
					list = "";
				}
			}
		},

		// Actual Callbacks object
		self = {

			// Add a callback or a collection of callbacks to the list
			add: function() {
				if ( list ) {

					// If we have memory from a past run, we should fire after adding
					if ( memory && !firing ) {
						firingIndex = list.length - 1;
						queue.push( memory );
					}

					( function add( args ) {
						jQuery.each( args, function( _, arg ) {
							if ( jQuery.isFunction( arg ) ) {
								if ( !options.unique || !self.has( arg ) ) {
									list.push( arg );
								}
							} else if ( arg && arg.length && jQuery.type( arg ) !== "string" ) {

								// Inspect recursively
								add( arg );
							}
						} );
					} )( arguments );

					if ( memory && !firing ) {
						fire();
					}
				}
				return this;
			},

			// Remove a callback from the list
			remove: function() {
				jQuery.each( arguments, function( _, arg ) {
					var index;
					while ( ( index = jQuery.inArray( arg, list, index ) ) > -1 ) {
						list.splice( index, 1 );

						// Handle firing indexes
						if ( index <= firingIndex ) {
							firingIndex--;
						}
					}
				} );
				return this;
			},

			// Check if a given callback is in the list.
			// If no argument is given, return whether or not list has callbacks attached.
			has: function( fn ) {
				return fn ?
					jQuery.inArray( fn, list ) > -1 :
					list.length > 0;
			},

			// Remove all callbacks from the list
			empty: function() {
				if ( list ) {
					list = [];
				}
				return this;
			},

			// Disable .fire and .add
			// Abort any current/pending executions
			// Clear all callbacks and values
			disable: function() {
				locked = queue = [];
				list = memory = "";
				return this;
			},
			disabled: function() {
				return !list;
			},

			// Disable .fire
			// Also disable .add unless we have memory (since it would have no effect)
			// Abort any pending executions
			lock: function() {
				locked = true;
				if ( !memory ) {
					self.disable();
				}
				return this;
			},
			locked: function() {
				return !!locked;
			},

			// Call all callbacks with the given context and arguments
			fireWith: function( context, args ) {
				if ( !locked ) {
					args = args || [];
					args = [ context, args.slice ? args.slice() : args ];
					queue.push( args );
					if ( !firing ) {
						fire();
					}
				}
				return this;
			},

			// Call all the callbacks with the given arguments
			fire: function() {
				self.fireWith( this, arguments );
				return this;
			},

			// To know if the callbacks have already been called at least once
			fired: function() {
				return !!fired;
			}
		};

	return self;
};


jQuery.extend( {

	Deferred: function( func ) {
		var tuples = [

				// action, add listener, listener list, final state
				[ "resolve", "done", jQuery.Callbacks( "once memory" ), "resolved" ],
				[ "reject", "fail", jQuery.Callbacks( "once memory" ), "rejected" ],
				[ "notify", "progress", jQuery.Callbacks( "memory" ) ]
			],
			state = "pending",
			promise = {
				state: function() {
					return state;
				},
				always: function() {
					deferred.done( arguments ).fail( arguments );
					return this;
				},
				then: function( /* fnDone, fnFail, fnProgress */ ) {
					var fns = arguments;
					return jQuery.Deferred( function( newDefer ) {
						jQuery.each( tuples, function( i, tuple ) {
							var fn = jQuery.isFunction( fns[ i ] ) && fns[ i ];

							// deferred[ done | fail | progress ] for forwarding actions to newDefer
							deferred[ tuple[ 1 ] ]( function() {
								var returned = fn && fn.apply( this, arguments );
								if ( returned && jQuery.isFunction( returned.promise ) ) {
									returned.promise()
										.progress( newDefer.notify )
										.done( newDefer.resolve )
										.fail( newDefer.reject );
								} else {
									newDefer[ tuple[ 0 ] + "With" ](
										this === promise ? newDefer.promise() : this,
										fn ? [ returned ] : arguments
									);
								}
							} );
						} );
						fns = null;
					} ).promise();
				},

				// Get a promise for this deferred
				// If obj is provided, the promise aspect is added to the object
				promise: function( obj ) {
					return obj != null ? jQuery.extend( obj, promise ) : promise;
				}
			},
			deferred = {};

		// Keep pipe for back-compat
		promise.pipe = promise.then;

		// Add list-specific methods
		jQuery.each( tuples, function( i, tuple ) {
			var list = tuple[ 2 ],
				stateString = tuple[ 3 ];

			// promise[ done | fail | progress ] = list.add
			promise[ tuple[ 1 ] ] = list.add;

			// Handle state
			if ( stateString ) {
				list.add( function() {

					// state = [ resolved | rejected ]
					state = stateString;

				// [ reject_list | resolve_list ].disable; progress_list.lock
				}, tuples[ i ^ 1 ][ 2 ].disable, tuples[ 2 ][ 2 ].lock );
			}

			// deferred[ resolve | reject | notify ]
			deferred[ tuple[ 0 ] ] = function() {
				deferred[ tuple[ 0 ] + "With" ]( this === deferred ? promise : this, arguments );
				return this;
			};
			deferred[ tuple[ 0 ] + "With" ] = list.fireWith;
		} );

		// Make the deferred a promise
		promise.promise( deferred );

		// Call given func if any
		if ( func ) {
			func.call( deferred, deferred );
		}

		// All done!
		return deferred;
	},

	// Deferred helper
	when: function( subordinate /* , ..., subordinateN */ ) {
		var i = 0,
			resolveValues = slice.call( arguments ),
			length = resolveValues.length,

			// the count of uncompleted subordinates
			remaining = length !== 1 ||
				( subordinate && jQuery.isFunction( subordinate.promise ) ) ? length : 0,

			// the master Deferred.
			// If resolveValues consist of only a single Deferred, just use that.
			deferred = remaining === 1 ? subordinate : jQuery.Deferred(),

			// Update function for both resolve and progress values
			updateFunc = function( i, contexts, values ) {
				return function( value ) {
					contexts[ i ] = this;
					values[ i ] = arguments.length > 1 ? slice.call( arguments ) : value;
					if ( values === progressValues ) {
						deferred.notifyWith( contexts, values );

					} else if ( !( --remaining ) ) {
						deferred.resolveWith( contexts, values );
					}
				};
			},

			progressValues, progressContexts, resolveContexts;

		// add listeners to Deferred subordinates; treat others as resolved
		if ( length > 1 ) {
			progressValues = new Array( length );
			progressContexts = new Array( length );
			resolveContexts = new Array( length );
			for ( ; i < length; i++ ) {
				if ( resolveValues[ i ] && jQuery.isFunction( resolveValues[ i ].promise ) ) {
					resolveValues[ i ].promise()
						.progress( updateFunc( i, progressContexts, progressValues ) )
						.done( updateFunc( i, resolveContexts, resolveValues ) )
						.fail( deferred.reject );
				} else {
					--remaining;
				}
			}
		}

		// if we're not waiting on anything, resolve the master
		if ( !remaining ) {
			deferred.resolveWith( resolveContexts, resolveValues );
		}

		return deferred.promise();
	}
} );


// The deferred used on DOM ready
var readyList;

jQuery.fn.ready = function( fn ) {

	// Add the callback
	jQuery.ready.promise().done( fn );

	return this;
};

jQuery.extend( {

	// Is the DOM ready to be used? Set to true once it occurs.
	isReady: false,

	// A counter to track how many items to wait for before
	// the ready event fires. See #6781
	readyWait: 1,

	// Hold (or release) the ready event
	holdReady: function( hold ) {
		if ( hold ) {
			jQuery.readyWait++;
		} else {
			jQuery.ready( true );
		}
	},

	// Handle when the DOM is ready
	ready: function( wait ) {

		// Abort if there are pending holds or we're already ready
		if ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {
			return;
		}

		// Remember that the DOM is ready
		jQuery.isReady = true;

		// If a normal DOM Ready event fired, decrement, and wait if need be
		if ( wait !== true && --jQuery.readyWait > 0 ) {
			return;
		}

		// If there are functions bound, to execute
		readyList.resolveWith( document, [ jQuery ] );

		// Trigger any bound ready events
		if ( jQuery.fn.triggerHandler ) {
			jQuery( document ).triggerHandler( "ready" );
			jQuery( document ).off( "ready" );
		}
	}
} );

/**
 * Clean-up method for dom ready events
 */
function detach() {
	if ( document.addEventListener ) {
		document.removeEventListener( "DOMContentLoaded", completed );
		window.removeEventListener( "load", completed );

	} else {
		document.detachEvent( "onreadystatechange", completed );
		window.detachEvent( "onload", completed );
	}
}

/**
 * The ready event handler and self cleanup method
 */
function completed() {

	// readyState === "complete" is good enough for us to call the dom ready in oldIE
	if ( document.addEventListener ||
		window.event.type === "load" ||
		document.readyState === "complete" ) {

		detach();
		jQuery.ready();
	}
}

jQuery.ready.promise = function( obj ) {
	if ( !readyList ) {

		readyList = jQuery.Deferred();

		// Catch cases where $(document).ready() is called
		// after the browser event has already occurred.
		// Support: IE6-10
		// Older IE sometimes signals "interactive" too soon
		if ( document.readyState === "complete" ||
			( document.readyState !== "loading" && !document.documentElement.doScroll ) ) {

			// Handle it asynchronously to allow scripts the opportunity to delay ready
			window.setTimeout( jQuery.ready );

		// Standards-based browsers support DOMContentLoaded
		} else if ( document.addEventListener ) {

			// Use the handy event callback
			document.addEventListener( "DOMContentLoaded", completed );

			// A fallback to window.onload, that will always work
			window.addEventListener( "load", completed );

		// If IE event model is used
		} else {

			// Ensure firing before onload, maybe late but safe also for iframes
			document.attachEvent( "onreadystatechange", completed );

			// A fallback to window.onload, that will always work
			window.attachEvent( "onload", completed );

			// If IE and not a frame
			// continually check to see if the document is ready
			var top = false;

			try {
				top = window.frameElement == null && document.documentElement;
			} catch ( e ) {}

			if ( top && top.doScroll ) {
				( function doScrollCheck() {
					if ( !jQuery.isReady ) {

						try {

							// Use the trick by Diego Perini
							// http://javascript.nwbox.com/IEContentLoaded/
							top.doScroll( "left" );
						} catch ( e ) {
							return window.setTimeout( doScrollCheck, 50 );
						}

						// detach all dom ready events
						detach();

						// and execute any waiting functions
						jQuery.ready();
					}
				} )();
			}
		}
	}
	return readyList.promise( obj );
};

// Kick off the DOM ready check even if the user does not
jQuery.ready.promise();




// Support: IE<9
// Iteration over object's inherited properties before its own
var i;
for ( i in jQuery( support ) ) {
	break;
}
support.ownFirst = i === "0";

// Note: most support tests are defined in their respective modules.
// false until the test is run
support.inlineBlockNeedsLayout = false;

// Execute ASAP in case we need to set body.style.zoom
jQuery( function() {

	// Minified: var a,b,c,d
	var val, div, body, container;

	body = document.getElementsByTagName( "body" )[ 0 ];
	if ( !body || !body.style ) {

		// Return for frameset docs that don't have a body
		return;
	}

	// Setup
	div = document.createElement( "div" );
	container = document.createElement( "div" );
	container.style.cssText = "position:absolute;border:0;width:0;height:0;top:0;left:-9999px";
	body.appendChild( container ).appendChild( div );

	if ( typeof div.style.zoom !== "undefined" ) {

		// Support: IE<8
		// Check if natively block-level elements act like inline-block
		// elements when setting their display to 'inline' and giving
		// them layout
		div.style.cssText = "display:inline;margin:0;border:0;padding:1px;width:1px;zoom:1";

		support.inlineBlockNeedsLayout = val = div.offsetWidth === 3;
		if ( val ) {

			// Prevent IE 6 from affecting layout for positioned elements #11048
			// Prevent IE from shrinking the body in IE 7 mode #12869
			// Support: IE<8
			body.style.zoom = 1;
		}
	}

	body.removeChild( container );
} );


( function() {
	var div = document.createElement( "div" );

	// Support: IE<9
	support.deleteExpando = true;
	try {
		delete div.test;
	} catch ( e ) {
		support.deleteExpando = false;
	}

	// Null elements to avoid leaks in IE.
	div = null;
} )();
var acceptData = function( elem ) {
	var noData = jQuery.noData[ ( elem.nodeName + " " ).toLowerCase() ],
		nodeType = +elem.nodeType || 1;

	// Do not set data on non-element DOM nodes because it will not be cleared (#8335).
	return nodeType !== 1 && nodeType !== 9 ?
		false :

		// Nodes accept data unless otherwise specified; rejection can be conditional
		!noData || noData !== true && elem.getAttribute( "classid" ) === noData;
};




var rbrace = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/,
	rmultiDash = /([A-Z])/g;

function dataAttr( elem, key, data ) {

	// If nothing was found internally, try to fetch any
	// data from the HTML5 data-* attribute
	if ( data === undefined && elem.nodeType === 1 ) {

		var name = "data-" + key.replace( rmultiDash, "-$1" ).toLowerCase();

		data = elem.getAttribute( name );

		if ( typeof data === "string" ) {
			try {
				data = data === "true" ? true :
					data === "false" ? false :
					data === "null" ? null :

					// Only convert to a number if it doesn't change the string
					+data + "" === data ? +data :
					rbrace.test( data ) ? jQuery.parseJSON( data ) :
					data;
			} catch ( e ) {}

			// Make sure we set the data so it isn't changed later
			jQuery.data( elem, key, data );

		} else {
			data = undefined;
		}
	}

	return data;
}

// checks a cache object for emptiness
function isEmptyDataObject( obj ) {
	var name;
	for ( name in obj ) {

		// if the public data object is empty, the private is still empty
		if ( name === "data" && jQuery.isEmptyObject( obj[ name ] ) ) {
			continue;
		}
		if ( name !== "toJSON" ) {
			return false;
		}
	}

	return true;
}

function internalData( elem, name, data, pvt /* Internal Use Only */ ) {
	if ( !acceptData( elem ) ) {
		return;
	}

	var ret, thisCache,
		internalKey = jQuery.expando,

		// We have to handle DOM nodes and JS objects differently because IE6-7
		// can't GC object references properly across the DOM-JS boundary
		isNode = elem.nodeType,

		// Only DOM nodes need the global jQuery cache; JS object data is
		// attached directly to the object so GC can occur automatically
		cache = isNode ? jQuery.cache : elem,

		// Only defining an ID for JS objects if its cache already exists allows
		// the code to shortcut on the same path as a DOM node with no cache
		id = isNode ? elem[ internalKey ] : elem[ internalKey ] && internalKey;

	// Avoid doing any more work than we need to when trying to get data on an
	// object that has no data at all
	if ( ( !id || !cache[ id ] || ( !pvt && !cache[ id ].data ) ) &&
		data === undefined && typeof name === "string" ) {
		return;
	}

	if ( !id ) {

		// Only DOM nodes need a new unique ID for each element since their data
		// ends up in the global cache
		if ( isNode ) {
			id = elem[ internalKey ] = deletedIds.pop() || jQuery.guid++;
		} else {
			id = internalKey;
		}
	}

	if ( !cache[ id ] ) {

		// Avoid exposing jQuery metadata on plain JS objects when the object
		// is serialized using JSON.stringify
		cache[ id ] = isNode ? {} : { toJSON: jQuery.noop };
	}

	// An object can be passed to jQuery.data instead of a key/value pair; this gets
	// shallow copied over onto the existing cache
	if ( typeof name === "object" || typeof name === "function" ) {
		if ( pvt ) {
			cache[ id ] = jQuery.extend( cache[ id ], name );
		} else {
			cache[ id ].data = jQuery.extend( cache[ id ].data, name );
		}
	}

	thisCache = cache[ id ];

	// jQuery data() is stored in a separate object inside the object's internal data
	// cache in order to avoid key collisions between internal data and user-defined
	// data.
	if ( !pvt ) {
		if ( !thisCache.data ) {
			thisCache.data = {};
		}

		thisCache = thisCache.data;
	}

	if ( data !== undefined ) {
		thisCache[ jQuery.camelCase( name ) ] = data;
	}

	// Check for both converted-to-camel and non-converted data property names
	// If a data property was specified
	if ( typeof name === "string" ) {

		// First Try to find as-is property data
		ret = thisCache[ name ];

		// Test for null|undefined property data
		if ( ret == null ) {

			// Try to find the camelCased property
			ret = thisCache[ jQuery.camelCase( name ) ];
		}
	} else {
		ret = thisCache;
	}

	return ret;
}

function internalRemoveData( elem, name, pvt ) {
	if ( !acceptData( elem ) ) {
		return;
	}

	var thisCache, i,
		isNode = elem.nodeType,

		// See jQuery.data for more information
		cache = isNode ? jQuery.cache : elem,
		id = isNode ? elem[ jQuery.expando ] : jQuery.expando;

	// If there is already no cache entry for this object, there is no
	// purpose in continuing
	if ( !cache[ id ] ) {
		return;
	}

	if ( name ) {

		thisCache = pvt ? cache[ id ] : cache[ id ].data;

		if ( thisCache ) {

			// Support array or space separated string names for data keys
			if ( !jQuery.isArray( name ) ) {

				// try the string as a key before any manipulation
				if ( name in thisCache ) {
					name = [ name ];
				} else {

					// split the camel cased version by spaces unless a key with the spaces exists
					name = jQuery.camelCase( name );
					if ( name in thisCache ) {
						name = [ name ];
					} else {
						name = name.split( " " );
					}
				}
			} else {

				// If "name" is an array of keys...
				// When data is initially created, via ("key", "val") signature,
				// keys will be converted to camelCase.
				// Since there is no way to tell _how_ a key was added, remove
				// both plain key and camelCase key. #12786
				// This will only penalize the array argument path.
				name = name.concat( jQuery.map( name, jQuery.camelCase ) );
			}

			i = name.length;
			while ( i-- ) {
				delete thisCache[ name[ i ] ];
			}

			// If there is no data left in the cache, we want to continue
			// and let the cache object itself get destroyed
			if ( pvt ? !isEmptyDataObject( thisCache ) : !jQuery.isEmptyObject( thisCache ) ) {
				return;
			}
		}
	}

	// See jQuery.data for more information
	if ( !pvt ) {
		delete cache[ id ].data;

		// Don't destroy the parent cache unless the internal data object
		// had been the only thing left in it
		if ( !isEmptyDataObject( cache[ id ] ) ) {
			return;
		}
	}

	// Destroy the cache
	if ( isNode ) {
		jQuery.cleanData( [ elem ], true );

	// Use delete when supported for expandos or `cache` is not a window per isWindow (#10080)
	/* jshint eqeqeq: false */
	} else if ( support.deleteExpando || cache != cache.window ) {
		/* jshint eqeqeq: true */
		delete cache[ id ];

	// When all else fails, undefined
	} else {
		cache[ id ] = undefined;
	}
}

jQuery.extend( {
	cache: {},

	// The following elements (space-suffixed to avoid Object.prototype collisions)
	// throw uncatchable exceptions if you attempt to set expando properties
	noData: {
		"applet ": true,
		"embed ": true,

		// ...but Flash objects (which have this classid) *can* handle expandos
		"object ": "clsid:D27CDB6E-AE6D-11cf-96B8-444553540000"
	},

	hasData: function( elem ) {
		elem = elem.nodeType ? jQuery.cache[ elem[ jQuery.expando ] ] : elem[ jQuery.expando ];
		return !!elem && !isEmptyDataObject( elem );
	},

	data: function( elem, name, data ) {
		return internalData( elem, name, data );
	},

	removeData: function( elem, name ) {
		return internalRemoveData( elem, name );
	},

	// For internal use only.
	_data: function( elem, name, data ) {
		return internalData( elem, name, data, true );
	},

	_removeData: function( elem, name ) {
		return internalRemoveData( elem, name, true );
	}
} );

jQuery.fn.extend( {
	data: function( key, value ) {
		var i, name, data,
			elem = this[ 0 ],
			attrs = elem && elem.attributes;

		// Special expections of .data basically thwart jQuery.access,
		// so implement the relevant behavior ourselves

		// Gets all values
		if ( key === undefined ) {
			if ( this.length ) {
				data = jQuery.data( elem );

				if ( elem.nodeType === 1 && !jQuery._data( elem, "parsedAttrs" ) ) {
					i = attrs.length;
					while ( i-- ) {

						// Support: IE11+
						// The attrs elements can be null (#14894)
						if ( attrs[ i ] ) {
							name = attrs[ i ].name;
							if ( name.indexOf( "data-" ) === 0 ) {
								name = jQuery.camelCase( name.slice( 5 ) );
								dataAttr( elem, name, data[ name ] );
							}
						}
					}
					jQuery._data( elem, "parsedAttrs", true );
				}
			}

			return data;
		}

		// Sets multiple values
		if ( typeof key === "object" ) {
			return this.each( function() {
				jQuery.data( this, key );
			} );
		}

		return arguments.length > 1 ?

			// Sets one value
			this.each( function() {
				jQuery.data( this, key, value );
			} ) :

			// Gets one value
			// Try to fetch any internally stored data first
			elem ? dataAttr( elem, key, jQuery.data( elem, key ) ) : undefined;
	},

	removeData: function( key ) {
		return this.each( function() {
			jQuery.removeData( this, key );
		} );
	}
} );


jQuery.extend( {
	queue: function( elem, type, data ) {
		var queue;

		if ( elem ) {
			type = ( type || "fx" ) + "queue";
			queue = jQuery._data( elem, type );

			// Speed up dequeue by getting out quickly if this is just a lookup
			if ( data ) {
				if ( !queue || jQuery.isArray( data ) ) {
					queue = jQuery._data( elem, type, jQuery.makeArray( data ) );
				} else {
					queue.push( data );
				}
			}
			return queue || [];
		}
	},

	dequeue: function( elem, type ) {
		type = type || "fx";

		var queue = jQuery.queue( elem, type ),
			startLength = queue.length,
			fn = queue.shift(),
			hooks = jQuery._queueHooks( elem, type ),
			next = function() {
				jQuery.dequeue( elem, type );
			};

		// If the fx queue is dequeued, always remove the progress sentinel
		if ( fn === "inprogress" ) {
			fn = queue.shift();
			startLength--;
		}

		if ( fn ) {

			// Add a progress sentinel to prevent the fx queue from being
			// automatically dequeued
			if ( type === "fx" ) {
				queue.unshift( "inprogress" );
			}

			// clear up the last queue stop function
			delete hooks.stop;
			fn.call( elem, next, hooks );
		}

		if ( !startLength && hooks ) {
			hooks.empty.fire();
		}
	},

	// not intended for public consumption - generates a queueHooks object,
	// or returns the current one
	_queueHooks: function( elem, type ) {
		var key = type + "queueHooks";
		return jQuery._data( elem, key ) || jQuery._data( elem, key, {
			empty: jQuery.Callbacks( "once memory" ).add( function() {
				jQuery._removeData( elem, type + "queue" );
				jQuery._removeData( elem, key );
			} )
		} );
	}
} );

jQuery.fn.extend( {
	queue: function( type, data ) {
		var setter = 2;

		if ( typeof type !== "string" ) {
			data = type;
			type = "fx";
			setter--;
		}

		if ( arguments.length < setter ) {
			return jQuery.queue( this[ 0 ], type );
		}

		return data === undefined ?
			this :
			this.each( function() {
				var queue = jQuery.queue( this, type, data );

				// ensure a hooks for this queue
				jQuery._queueHooks( this, type );

				if ( type === "fx" && queue[ 0 ] !== "inprogress" ) {
					jQuery.dequeue( this, type );
				}
			} );
	},
	dequeue: function( type ) {
		return this.each( function() {
			jQuery.dequeue( this, type );
		} );
	},
	clearQueue: function( type ) {
		return this.queue( type || "fx", [] );
	},

	// Get a promise resolved when queues of a certain type
	// are emptied (fx is the type by default)
	promise: function( type, obj ) {
		var tmp,
			count = 1,
			defer = jQuery.Deferred(),
			elements = this,
			i = this.length,
			resolve = function() {
				if ( !( --count ) ) {
					defer.resolveWith( elements, [ elements ] );
				}
			};

		if ( typeof type !== "string" ) {
			obj = type;
			type = undefined;
		}
		type = type || "fx";

		while ( i-- ) {
			tmp = jQuery._data( elements[ i ], type + "queueHooks" );
			if ( tmp && tmp.empty ) {
				count++;
				tmp.empty.add( resolve );
			}
		}
		resolve();
		return defer.promise( obj );
	}
} );


( function() {
	var shrinkWrapBlocksVal;

	support.shrinkWrapBlocks = function() {
		if ( shrinkWrapBlocksVal != null ) {
			return shrinkWrapBlocksVal;
		}

		// Will be changed later if needed.
		shrinkWrapBlocksVal = false;

		// Minified: var b,c,d
		var div, body, container;

		body = document.getElementsByTagName( "body" )[ 0 ];
		if ( !body || !body.style ) {

			// Test fired too early or in an unsupported environment, exit.
			return;
		}

		// Setup
		div = document.createElement( "div" );
		container = document.createElement( "div" );
		container.style.cssText = "position:absolute;border:0;width:0;height:0;top:0;left:-9999px";
		body.appendChild( container ).appendChild( div );

		// Support: IE6
		// Check if elements with layout shrink-wrap their children
		if ( typeof div.style.zoom !== "undefined" ) {

			// Reset CSS: box-sizing; display; margin; border
			div.style.cssText =

				// Support: Firefox<29, Android 2.3
				// Vendor-prefix box-sizing
				"-webkit-box-sizing:content-box;-moz-box-sizing:content-box;" +
				"box-sizing:content-box;display:block;margin:0;border:0;" +
				"padding:1px;width:1px;zoom:1";
			div.appendChild( document.createElement( "div" ) ).style.width = "5px";
			shrinkWrapBlocksVal = div.offsetWidth !== 3;
		}

		body.removeChild( container );

		return shrinkWrapBlocksVal;
	};

} )();
var pnum = ( /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/ ).source;

var rcssNum = new RegExp( "^(?:([+-])=|)(" + pnum + ")([a-z%]*)$", "i" );


var cssExpand = [ "Top", "Right", "Bottom", "Left" ];

var isHidden = function( elem, el ) {

		// isHidden might be called from jQuery#filter function;
		// in that case, element will be second argument
		elem = el || elem;
		return jQuery.css( elem, "display" ) === "none" ||
			!jQuery.contains( elem.ownerDocument, elem );
	};



function adjustCSS( elem, prop, valueParts, tween ) {
	var adjusted,
		scale = 1,
		maxIterations = 20,
		currentValue = tween ?
			function() { return tween.cur(); } :
			function() { return jQuery.css( elem, prop, "" ); },
		initial = currentValue(),
		unit = valueParts && valueParts[ 3 ] || ( jQuery.cssNumber[ prop ] ? "" : "px" ),

		// Starting value computation is required for potential unit mismatches
		initialInUnit = ( jQuery.cssNumber[ prop ] || unit !== "px" && +initial ) &&
			rcssNum.exec( jQuery.css( elem, prop ) );

	if ( initialInUnit && initialInUnit[ 3 ] !== unit ) {

		// Trust units reported by jQuery.css
		unit = unit || initialInUnit[ 3 ];

		// Make sure we update the tween properties later on
		valueParts = valueParts || [];

		// Iteratively approximate from a nonzero starting point
		initialInUnit = +initial || 1;

		do {

			// If previous iteration zeroed out, double until we get *something*.
			// Use string for doubling so we don't accidentally see scale as unchanged below
			scale = scale || ".5";

			// Adjust and apply
			initialInUnit = initialInUnit / scale;
			jQuery.style( elem, prop, initialInUnit + unit );

		// Update scale, tolerating zero or NaN from tween.cur()
		// Break the loop if scale is unchanged or perfect, or if we've just had enough.
		} while (
			scale !== ( scale = currentValue() / initial ) && scale !== 1 && --maxIterations
		);
	}

	if ( valueParts ) {
		initialInUnit = +initialInUnit || +initial || 0;

		// Apply relative offset (+=/-=) if specified
		adjusted = valueParts[ 1 ] ?
			initialInUnit + ( valueParts[ 1 ] + 1 ) * valueParts[ 2 ] :
			+valueParts[ 2 ];
		if ( tween ) {
			tween.unit = unit;
			tween.start = initialInUnit;
			tween.end = adjusted;
		}
	}
	return adjusted;
}


// Multifunctional method to get and set values of a collection
// The value/s can optionally be executed if it's a function
var access = function( elems, fn, key, value, chainable, emptyGet, raw ) {
	var i = 0,
		length = elems.length,
		bulk = key == null;

	// Sets many values
	if ( jQuery.type( key ) === "object" ) {
		chainable = true;
		for ( i in key ) {
			access( elems, fn, i, key[ i ], true, emptyGet, raw );
		}

	// Sets one value
	} else if ( value !== undefined ) {
		chainable = true;

		if ( !jQuery.isFunction( value ) ) {
			raw = true;
		}

		if ( bulk ) {

			// Bulk operations run against the entire set
			if ( raw ) {
				fn.call( elems, value );
				fn = null;

			// ...except when executing function values
			} else {
				bulk = fn;
				fn = function( elem, key, value ) {
					return bulk.call( jQuery( elem ), value );
				};
			}
		}

		if ( fn ) {
			for ( ; i < length; i++ ) {
				fn(
					elems[ i ],
					key,
					raw ? value : value.call( elems[ i ], i, fn( elems[ i ], key ) )
				);
			}
		}
	}

	return chainable ?
		elems :

		// Gets
		bulk ?
			fn.call( elems ) :
			length ? fn( elems[ 0 ], key ) : emptyGet;
};
var rcheckableType = ( /^(?:checkbox|radio)$/i );

var rtagName = ( /<([\w:-]+)/ );

var rscriptType = ( /^$|\/(?:java|ecma)script/i );

var rleadingWhitespace = ( /^\s+/ );

var nodeNames = "abbr|article|aside|audio|bdi|canvas|data|datalist|" +
		"details|dialog|figcaption|figure|footer|header|hgroup|main|" +
		"mark|meter|nav|output|picture|progress|section|summary|template|time|video";



function createSafeFragment( document ) {
	var list = nodeNames.split( "|" ),
		safeFrag = document.createDocumentFragment();

	if ( safeFrag.createElement ) {
		while ( list.length ) {
			safeFrag.createElement(
				list.pop()
			);
		}
	}
	return safeFrag;
}


( function() {
	var div = document.createElement( "div" ),
		fragment = document.createDocumentFragment(),
		input = document.createElement( "input" );

	// Setup
	div.innerHTML = "  <link/><table></table><a href='/a'>a</a><input type='checkbox'/>";

	// IE strips leading whitespace when .innerHTML is used
	support.leadingWhitespace = div.firstChild.nodeType === 3;

	// Make sure that tbody elements aren't automatically inserted
	// IE will insert them into empty tables
	support.tbody = !div.getElementsByTagName( "tbody" ).length;

	// Make sure that link elements get serialized correctly by innerHTML
	// This requires a wrapper element in IE
	support.htmlSerialize = !!div.getElementsByTagName( "link" ).length;

	// Makes sure cloning an html5 element does not cause problems
	// Where outerHTML is undefined, this still works
	support.html5Clone =
		document.createElement( "nav" ).cloneNode( true ).outerHTML !== "<:nav></:nav>";

	// Check if a disconnected checkbox will retain its checked
	// value of true after appended to the DOM (IE6/7)
	input.type = "checkbox";
	input.checked = true;
	fragment.appendChild( input );
	support.appendChecked = input.checked;

	// Make sure textarea (and checkbox) defaultValue is properly cloned
	// Support: IE6-IE11+
	div.innerHTML = "<textarea>x</textarea>";
	support.noCloneChecked = !!div.cloneNode( true ).lastChild.defaultValue;

	// #11217 - WebKit loses check when the name is after the checked attribute
	fragment.appendChild( div );

	// Support: Windows Web Apps (WWA)
	// `name` and `type` must use .setAttribute for WWA (#14901)
	input = document.createElement( "input" );
	input.setAttribute( "type", "radio" );
	input.setAttribute( "checked", "checked" );
	input.setAttribute( "name", "t" );

	div.appendChild( input );

	// Support: Safari 5.1, iOS 5.1, Android 4.x, Android 2.3
	// old WebKit doesn't clone checked state correctly in fragments
	support.checkClone = div.cloneNode( true ).cloneNode( true ).lastChild.checked;

	// Support: IE<9
	// Cloned elements keep attachEvent handlers, we use addEventListener on IE9+
	support.noCloneEvent = !!div.addEventListener;

	// Support: IE<9
	// Since attributes and properties are the same in IE,
	// cleanData must set properties to undefined rather than use removeAttribute
	div[ jQuery.expando ] = 1;
	support.attributes = !div.getAttribute( jQuery.expando );
} )();


// We have to close these tags to support XHTML (#13200)
var wrapMap = {
	option: [ 1, "<select multiple='multiple'>", "</select>" ],
	legend: [ 1, "<fieldset>", "</fieldset>" ],
	area: [ 1, "<map>", "</map>" ],

	// Support: IE8
	param: [ 1, "<object>", "</object>" ],
	thead: [ 1, "<table>", "</table>" ],
	tr: [ 2, "<table><tbody>", "</tbody></table>" ],
	col: [ 2, "<table><tbody></tbody><colgroup>", "</colgroup></table>" ],
	td: [ 3, "<table><tbody><tr>", "</tr></tbody></table>" ],

	// IE6-8 can't serialize link, script, style, or any html5 (NoScope) tags,
	// unless wrapped in a div with non-breaking characters in front of it.
	_default: support.htmlSerialize ? [ 0, "", "" ] : [ 1, "X<div>", "</div>" ]
};

// Support: IE8-IE9
wrapMap.optgroup = wrapMap.option;

wrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
wrapMap.th = wrapMap.td;


function getAll( context, tag ) {
	var elems, elem,
		i = 0,
		found = typeof context.getElementsByTagName !== "undefined" ?
			context.getElementsByTagName( tag || "*" ) :
			typeof context.querySelectorAll !== "undefined" ?
				context.querySelectorAll( tag || "*" ) :
				undefined;

	if ( !found ) {
		for ( found = [], elems = context.childNodes || context;
			( elem = elems[ i ] ) != null;
			i++
		) {
			if ( !tag || jQuery.nodeName( elem, tag ) ) {
				found.push( elem );
			} else {
				jQuery.merge( found, getAll( elem, tag ) );
			}
		}
	}

	return tag === undefined || tag && jQuery.nodeName( context, tag ) ?
		jQuery.merge( [ context ], found ) :
		found;
}


// Mark scripts as having already been evaluated
function setGlobalEval( elems, refElements ) {
	var elem,
		i = 0;
	for ( ; ( elem = elems[ i ] ) != null; i++ ) {
		jQuery._data(
			elem,
			"globalEval",
			!refElements || jQuery._data( refElements[ i ], "globalEval" )
		);
	}
}


var rhtml = /<|&#?\w+;/,
	rtbody = /<tbody/i;

function fixDefaultChecked( elem ) {
	if ( rcheckableType.test( elem.type ) ) {
		elem.defaultChecked = elem.checked;
	}
}

function buildFragment( elems, context, scripts, selection, ignored ) {
	var j, elem, contains,
		tmp, tag, tbody, wrap,
		l = elems.length,

		// Ensure a safe fragment
		safe = createSafeFragment( context ),

		nodes = [],
		i = 0;

	for ( ; i < l; i++ ) {
		elem = elems[ i ];

		if ( elem || elem === 0 ) {

			// Add nodes directly
			if ( jQuery.type( elem ) === "object" ) {
				jQuery.merge( nodes, elem.nodeType ? [ elem ] : elem );

			// Convert non-html into a text node
			} else if ( !rhtml.test( elem ) ) {
				nodes.push( context.createTextNode( elem ) );

			// Convert html into DOM nodes
			} else {
				tmp = tmp || safe.appendChild( context.createElement( "div" ) );

				// Deserialize a standard representation
				tag = ( rtagName.exec( elem ) || [ "", "" ] )[ 1 ].toLowerCase();
				wrap = wrapMap[ tag ] || wrapMap._default;

				tmp.innerHTML = wrap[ 1 ] + jQuery.htmlPrefilter( elem ) + wrap[ 2 ];

				// Descend through wrappers to the right content
				j = wrap[ 0 ];
				while ( j-- ) {
					tmp = tmp.lastChild;
				}

				// Manually add leading whitespace removed by IE
				if ( !support.leadingWhitespace && rleadingWhitespace.test( elem ) ) {
					nodes.push( context.createTextNode( rleadingWhitespace.exec( elem )[ 0 ] ) );
				}

				// Remove IE's autoinserted <tbody> from table fragments
				if ( !support.tbody ) {

					// String was a <table>, *may* have spurious <tbody>
					elem = tag === "table" && !rtbody.test( elem ) ?
						tmp.firstChild :

						// String was a bare <thead> or <tfoot>
						wrap[ 1 ] === "<table>" && !rtbody.test( elem ) ?
							tmp :
							0;

					j = elem && elem.childNodes.length;
					while ( j-- ) {
						if ( jQuery.nodeName( ( tbody = elem.childNodes[ j ] ), "tbody" ) &&
							!tbody.childNodes.length ) {

							elem.removeChild( tbody );
						}
					}
				}

				jQuery.merge( nodes, tmp.childNodes );

				// Fix #12392 for WebKit and IE > 9
				tmp.textContent = "";

				// Fix #12392 for oldIE
				while ( tmp.firstChild ) {
					tmp.removeChild( tmp.firstChild );
				}

				// Remember the top-level container for proper cleanup
				tmp = safe.lastChild;
			}
		}
	}

	// Fix #11356: Clear elements from fragment
	if ( tmp ) {
		safe.removeChild( tmp );
	}

	// Reset defaultChecked for any radios and checkboxes
	// about to be appended to the DOM in IE 6/7 (#8060)
	if ( !support.appendChecked ) {
		jQuery.grep( getAll( nodes, "input" ), fixDefaultChecked );
	}

	i = 0;
	while ( ( elem = nodes[ i++ ] ) ) {

		// Skip elements already in the context collection (trac-4087)
		if ( selection && jQuery.inArray( elem, selection ) > -1 ) {
			if ( ignored ) {
				ignored.push( elem );
			}

			continue;
		}

		contains = jQuery.contains( elem.ownerDocument, elem );

		// Append to fragment
		tmp = getAll( safe.appendChild( elem ), "script" );

		// Preserve script evaluation history
		if ( contains ) {
			setGlobalEval( tmp );
		}

		// Capture executables
		if ( scripts ) {
			j = 0;
			while ( ( elem = tmp[ j++ ] ) ) {
				if ( rscriptType.test( elem.type || "" ) ) {
					scripts.push( elem );
				}
			}
		}
	}

	tmp = null;

	return safe;
}


( function() {
	var i, eventName,
		div = document.createElement( "div" );

	// Support: IE<9 (lack submit/change bubble), Firefox (lack focus(in | out) events)
	for ( i in { submit: true, change: true, focusin: true } ) {
		eventName = "on" + i;

		if ( !( support[ i ] = eventName in window ) ) {

			// Beware of CSP restrictions (https://developer.mozilla.org/en/Security/CSP)
			div.setAttribute( eventName, "t" );
			support[ i ] = div.attributes[ eventName ].expando === false;
		}
	}

	// Null elements to avoid leaks in IE.
	div = null;
} )();


var rformElems = /^(?:input|select|textarea)$/i,
	rkeyEvent = /^key/,
	rmouseEvent = /^(?:mouse|pointer|contextmenu|drag|drop)|click/,
	rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,
	rtypenamespace = /^([^.]*)(?:\.(.+)|)/;

function returnTrue() {
	return true;
}

function returnFalse() {
	return false;
}

// Support: IE9
// See #13393 for more info
function safeActiveElement() {
	try {
		return document.activeElement;
	} catch ( err ) { }
}

function on( elem, types, selector, data, fn, one ) {
	var origFn, type;

	// Types can be a map of types/handlers
	if ( typeof types === "object" ) {

		// ( types-Object, selector, data )
		if ( typeof selector !== "string" ) {

			// ( types-Object, data )
			data = data || selector;
			selector = undefined;
		}
		for ( type in types ) {
			on( elem, type, selector, data, types[ type ], one );
		}
		return elem;
	}

	if ( data == null && fn == null ) {

		// ( types, fn )
		fn = selector;
		data = selector = undefined;
	} else if ( fn == null ) {
		if ( typeof selector === "string" ) {

			// ( types, selector, fn )
			fn = data;
			data = undefined;
		} else {

			// ( types, data, fn )
			fn = data;
			data = selector;
			selector = undefined;
		}
	}
	if ( fn === false ) {
		fn = returnFalse;
	} else if ( !fn ) {
		return elem;
	}

	if ( one === 1 ) {
		origFn = fn;
		fn = function( event ) {

			// Can use an empty set, since event contains the info
			jQuery().off( event );
			return origFn.apply( this, arguments );
		};

		// Use same guid so caller can remove using origFn
		fn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );
	}
	return elem.each( function() {
		jQuery.event.add( this, types, fn, data, selector );
	} );
}

/*
 * Helper functions for managing events -- not part of the public interface.
 * Props to Dean Edwards' addEvent library for many of the ideas.
 */
jQuery.event = {

	global: {},

	add: function( elem, types, handler, data, selector ) {
		var tmp, events, t, handleObjIn,
			special, eventHandle, handleObj,
			handlers, type, namespaces, origType,
			elemData = jQuery._data( elem );

		// Don't attach events to noData or text/comment nodes (but allow plain objects)
		if ( !elemData ) {
			return;
		}

		// Caller can pass in an object of custom data in lieu of the handler
		if ( handler.handler ) {
			handleObjIn = handler;
			handler = handleObjIn.handler;
			selector = handleObjIn.selector;
		}

		// Make sure that the handler has a unique ID, used to find/remove it later
		if ( !handler.guid ) {
			handler.guid = jQuery.guid++;
		}

		// Init the element's event structure and main handler, if this is the first
		if ( !( events = elemData.events ) ) {
			events = elemData.events = {};
		}
		if ( !( eventHandle = elemData.handle ) ) {
			eventHandle = elemData.handle = function( e ) {

				// Discard the second event of a jQuery.event.trigger() and
				// when an event is called after a page has unloaded
				return typeof jQuery !== "undefined" &&
					( !e || jQuery.event.triggered !== e.type ) ?
					jQuery.event.dispatch.apply( eventHandle.elem, arguments ) :
					undefined;
			};

			// Add elem as a property of the handle fn to prevent a memory leak
			// with IE non-native events
			eventHandle.elem = elem;
		}

		// Handle multiple events separated by a space
		types = ( types || "" ).match( rnotwhite ) || [ "" ];
		t = types.length;
		while ( t-- ) {
			tmp = rtypenamespace.exec( types[ t ] ) || [];
			type = origType = tmp[ 1 ];
			namespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();

			// There *must* be a type, no attaching namespace-only handlers
			if ( !type ) {
				continue;
			}

			// If event changes its type, use the special event handlers for the changed type
			special = jQuery.event.special[ type ] || {};

			// If selector defined, determine special event api type, otherwise given type
			type = ( selector ? special.delegateType : special.bindType ) || type;

			// Update special based on newly reset type
			special = jQuery.event.special[ type ] || {};

			// handleObj is passed to all event handlers
			handleObj = jQuery.extend( {
				type: type,
				origType: origType,
				data: data,
				handler: handler,
				guid: handler.guid,
				selector: selector,
				needsContext: selector && jQuery.expr.match.needsContext.test( selector ),
				namespace: namespaces.join( "." )
			}, handleObjIn );

			// Init the event handler queue if we're the first
			if ( !( handlers = events[ type ] ) ) {
				handlers = events[ type ] = [];
				handlers.delegateCount = 0;

				// Only use addEventListener/attachEvent if the special events handler returns false
				if ( !special.setup ||
					special.setup.call( elem, data, namespaces, eventHandle ) === false ) {

					// Bind the global event handler to the element
					if ( elem.addEventListener ) {
						elem.addEventListener( type, eventHandle, false );

					} else if ( elem.attachEvent ) {
						elem.attachEvent( "on" + type, eventHandle );
					}
				}
			}

			if ( special.add ) {
				special.add.call( elem, handleObj );

				if ( !handleObj.handler.guid ) {
					handleObj.handler.guid = handler.guid;
				}
			}

			// Add to the element's handler list, delegates in front
			if ( selector ) {
				handlers.splice( handlers.delegateCount++, 0, handleObj );
			} else {
				handlers.push( handleObj );
			}

			// Keep track of which events have ever been used, for event optimization
			jQuery.event.global[ type ] = true;
		}

		// Nullify elem to prevent memory leaks in IE
		elem = null;
	},

	// Detach an event or set of events from an element
	remove: function( elem, types, handler, selector, mappedTypes ) {
		var j, handleObj, tmp,
			origCount, t, events,
			special, handlers, type,
			namespaces, origType,
			elemData = jQuery.hasData( elem ) && jQuery._data( elem );

		if ( !elemData || !( events = elemData.events ) ) {
			return;
		}

		// Once for each type.namespace in types; type may be omitted
		types = ( types || "" ).match( rnotwhite ) || [ "" ];
		t = types.length;
		while ( t-- ) {
			tmp = rtypenamespace.exec( types[ t ] ) || [];
			type = origType = tmp[ 1 ];
			namespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();

			// Unbind all events (on this namespace, if provided) for the element
			if ( !type ) {
				for ( type in events ) {
					jQuery.event.remove( elem, type + types[ t ], handler, selector, true );
				}
				continue;
			}

			special = jQuery.event.special[ type ] || {};
			type = ( selector ? special.delegateType : special.bindType ) || type;
			handlers = events[ type ] || [];
			tmp = tmp[ 2 ] &&
				new RegExp( "(^|\\.)" + namespaces.join( "\\.(?:.*\\.|)" ) + "(\\.|$)" );

			// Remove matching events
			origCount = j = handlers.length;
			while ( j-- ) {
				handleObj = handlers[ j ];

				if ( ( mappedTypes || origType === handleObj.origType ) &&
					( !handler || handler.guid === handleObj.guid ) &&
					( !tmp || tmp.test( handleObj.namespace ) ) &&
					( !selector || selector === handleObj.selector ||
						selector === "**" && handleObj.selector ) ) {
					handlers.splice( j, 1 );

					if ( handleObj.selector ) {
						handlers.delegateCount--;
					}
					if ( special.remove ) {
						special.remove.call( elem, handleObj );
					}
				}
			}

			// Remove generic event handler if we removed something and no more handlers exist
			// (avoids potential for endless recursion during removal of special event handlers)
			if ( origCount && !handlers.length ) {
				if ( !special.teardown ||
					special.teardown.call( elem, namespaces, elemData.handle ) === false ) {

					jQuery.removeEvent( elem, type, elemData.handle );
				}

				delete events[ type ];
			}
		}

		// Remove the expando if it's no longer used
		if ( jQuery.isEmptyObject( events ) ) {
			delete elemData.handle;

			// removeData also checks for emptiness and clears the expando if empty
			// so use it instead of delete
			jQuery._removeData( elem, "events" );
		}
	},

	trigger: function( event, data, elem, onlyHandlers ) {
		var handle, ontype, cur,
			bubbleType, special, tmp, i,
			eventPath = [ elem || document ],
			type = hasOwn.call( event, "type" ) ? event.type : event,
			namespaces = hasOwn.call( event, "namespace" ) ? event.namespace.split( "." ) : [];

		cur = tmp = elem = elem || document;

		// Don't do events on text and comment nodes
		if ( elem.nodeType === 3 || elem.nodeType === 8 ) {
			return;
		}

		// focus/blur morphs to focusin/out; ensure we're not firing them right now
		if ( rfocusMorph.test( type + jQuery.event.triggered ) ) {
			return;
		}

		if ( type.indexOf( "." ) > -1 ) {

			// Namespaced trigger; create a regexp to match event type in handle()
			namespaces = type.split( "." );
			type = namespaces.shift();
			namespaces.sort();
		}
		ontype = type.indexOf( ":" ) < 0 && "on" + type;

		// Caller can pass in a jQuery.Event object, Object, or just an event type string
		event = event[ jQuery.expando ] ?
			event :
			new jQuery.Event( type, typeof event === "object" && event );

		// Trigger bitmask: & 1 for native handlers; & 2 for jQuery (always true)
		event.isTrigger = onlyHandlers ? 2 : 3;
		event.namespace = namespaces.join( "." );
		event.rnamespace = event.namespace ?
			new RegExp( "(^|\\.)" + namespaces.join( "\\.(?:.*\\.|)" ) + "(\\.|$)" ) :
			null;

		// Clean up the event in case it is being reused
		event.result = undefined;
		if ( !event.target ) {
			event.target = elem;
		}

		// Clone any incoming data and prepend the event, creating the handler arg list
		data = data == null ?
			[ event ] :
			jQuery.makeArray( data, [ event ] );

		// Allow special events to draw outside the lines
		special = jQuery.event.special[ type ] || {};
		if ( !onlyHandlers && special.trigger && special.trigger.apply( elem, data ) === false ) {
			return;
		}

		// Determine event propagation path in advance, per W3C events spec (#9951)
		// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
		if ( !onlyHandlers && !special.noBubble && !jQuery.isWindow( elem ) ) {

			bubbleType = special.delegateType || type;
			if ( !rfocusMorph.test( bubbleType + type ) ) {
				cur = cur.parentNode;
			}
			for ( ; cur; cur = cur.parentNode ) {
				eventPath.push( cur );
				tmp = cur;
			}

			// Only add window if we got to document (e.g., not plain obj or detached DOM)
			if ( tmp === ( elem.ownerDocument || document ) ) {
				eventPath.push( tmp.defaultView || tmp.parentWindow || window );
			}
		}

		// Fire handlers on the event path
		i = 0;
		while ( ( cur = eventPath[ i++ ] ) && !event.isPropagationStopped() ) {

			event.type = i > 1 ?
				bubbleType :
				special.bindType || type;

			// jQuery handler
			handle = ( jQuery._data( cur, "events" ) || {} )[ event.type ] &&
				jQuery._data( cur, "handle" );

			if ( handle ) {
				handle.apply( cur, data );
			}

			// Native handler
			handle = ontype && cur[ ontype ];
			if ( handle && handle.apply && acceptData( cur ) ) {
				event.result = handle.apply( cur, data );
				if ( event.result === false ) {
					event.preventDefault();
				}
			}
		}
		event.type = type;

		// If nobody prevented the default action, do it now
		if ( !onlyHandlers && !event.isDefaultPrevented() ) {

			if (
				( !special._default ||
				 special._default.apply( eventPath.pop(), data ) === false
				) && acceptData( elem )
			) {

				// Call a native DOM method on the target with the same name name as the event.
				// Can't use an .isFunction() check here because IE6/7 fails that test.
				// Don't do default actions on window, that's where global variables be (#6170)
				if ( ontype && elem[ type ] && !jQuery.isWindow( elem ) ) {

					// Don't re-trigger an onFOO event when we call its FOO() method
					tmp = elem[ ontype ];

					if ( tmp ) {
						elem[ ontype ] = null;
					}

					// Prevent re-triggering of the same event, since we already bubbled it above
					jQuery.event.triggered = type;
					try {
						elem[ type ]();
					} catch ( e ) {

						// IE<9 dies on focus/blur to hidden element (#1486,#12518)
						// only reproducible on winXP IE8 native, not IE9 in IE8 mode
					}
					jQuery.event.triggered = undefined;

					if ( tmp ) {
						elem[ ontype ] = tmp;
					}
				}
			}
		}

		return event.result;
	},

	dispatch: function( event ) {

		// Make a writable jQuery.Event from the native event object
		event = jQuery.event.fix( event );

		var i, j, ret, matched, handleObj,
			handlerQueue = [],
			args = slice.call( arguments ),
			handlers = ( jQuery._data( this, "events" ) || {} )[ event.type ] || [],
			special = jQuery.event.special[ event.type ] || {};

		// Use the fix-ed jQuery.Event rather than the (read-only) native event
		args[ 0 ] = event;
		event.delegateTarget = this;

		// Call the preDispatch hook for the mapped type, and let it bail if desired
		if ( special.preDispatch && special.preDispatch.call( this, event ) === false ) {
			return;
		}

		// Determine handlers
		handlerQueue = jQuery.event.handlers.call( this, event, handlers );

		// Run delegates first; they may want to stop propagation beneath us
		i = 0;
		while ( ( matched = handlerQueue[ i++ ] ) && !event.isPropagationStopped() ) {
			event.currentTarget = matched.elem;

			j = 0;
			while ( ( handleObj = matched.handlers[ j++ ] ) &&
				!event.isImmediatePropagationStopped() ) {

				// Triggered event must either 1) have no namespace, or 2) have namespace(s)
				// a subset or equal to those in the bound event (both can have no namespace).
				if ( !event.rnamespace || event.rnamespace.test( handleObj.namespace ) ) {

					event.handleObj = handleObj;
					event.data = handleObj.data;

					ret = ( ( jQuery.event.special[ handleObj.origType ] || {} ).handle ||
						handleObj.handler ).apply( matched.elem, args );

					if ( ret !== undefined ) {
						if ( ( event.result = ret ) === false ) {
							event.preventDefault();
							event.stopPropagation();
						}
					}
				}
			}
		}

		// Call the postDispatch hook for the mapped type
		if ( special.postDispatch ) {
			special.postDispatch.call( this, event );
		}

		return event.result;
	},

	handlers: function( event, handlers ) {
		var i, matches, sel, handleObj,
			handlerQueue = [],
			delegateCount = handlers.delegateCount,
			cur = event.target;

		// Support (at least): Chrome, IE9
		// Find delegate handlers
		// Black-hole SVG <use> instance trees (#13180)
		//
		// Support: Firefox<=42+
		// Avoid non-left-click in FF but don't block IE radio events (#3861, gh-2343)
		if ( delegateCount && cur.nodeType &&
			( event.type !== "click" || isNaN( event.button ) || event.button < 1 ) ) {

			/* jshint eqeqeq: false */
			for ( ; cur != this; cur = cur.parentNode || this ) {
				/* jshint eqeqeq: true */

				// Don't check non-elements (#13208)
				// Don't process clicks on disabled elements (#6911, #8165, #11382, #11764)
				if ( cur.nodeType === 1 && ( cur.disabled !== true || event.type !== "click" ) ) {
					matches = [];
					for ( i = 0; i < delegateCount; i++ ) {
						handleObj = handlers[ i ];

						// Don't conflict with Object.prototype properties (#13203)
						sel = handleObj.selector + " ";

						if ( matches[ sel ] === undefined ) {
							matches[ sel ] = handleObj.needsContext ?
								jQuery( sel, this ).index( cur ) > -1 :
								jQuery.find( sel, this, null, [ cur ] ).length;
						}
						if ( matches[ sel ] ) {
							matches.push( handleObj );
						}
					}
					if ( matches.length ) {
						handlerQueue.push( { elem: cur, handlers: matches } );
					}
				}
			}
		}

		// Add the remaining (directly-bound) handlers
		if ( delegateCount < handlers.length ) {
			handlerQueue.push( { elem: this, handlers: handlers.slice( delegateCount ) } );
		}

		return handlerQueue;
	},

	fix: function( event ) {
		if ( event[ jQuery.expando ] ) {
			return event;
		}

		// Create a writable copy of the event object and normalize some properties
		var i, prop, copy,
			type = event.type,
			originalEvent = event,
			fixHook = this.fixHooks[ type ];

		if ( !fixHook ) {
			this.fixHooks[ type ] = fixHook =
				rmouseEvent.test( type ) ? this.mouseHooks :
				rkeyEvent.test( type ) ? this.keyHooks :
				{};
		}
		copy = fixHook.props ? this.props.concat( fixHook.props ) : this.props;

		event = new jQuery.Event( originalEvent );

		i = copy.length;
		while ( i-- ) {
			prop = copy[ i ];
			event[ prop ] = originalEvent[ prop ];
		}

		// Support: IE<9
		// Fix target property (#1925)
		if ( !event.target ) {
			event.target = originalEvent.srcElement || document;
		}

		// Support: Safari 6-8+
		// Target should not be a text node (#504, #13143)
		if ( event.target.nodeType === 3 ) {
			event.target = event.target.parentNode;
		}

		// Support: IE<9
		// For mouse/key events, metaKey==false if it's undefined (#3368, #11328)
		event.metaKey = !!event.metaKey;

		return fixHook.filter ? fixHook.filter( event, originalEvent ) : event;
	},

	// Includes some event props shared by KeyEvent and MouseEvent
	props: ( "altKey bubbles cancelable ctrlKey currentTarget detail eventPhase " +
		"metaKey relatedTarget shiftKey target timeStamp view which" ).split( " " ),

	fixHooks: {},

	keyHooks: {
		props: "char charCode key keyCode".split( " " ),
		filter: function( event, original ) {

			// Add which for key events
			if ( event.which == null ) {
				event.which = original.charCode != null ? original.charCode : original.keyCode;
			}

			return event;
		}
	},

	mouseHooks: {
		props: ( "button buttons clientX clientY fromElement offsetX offsetY " +
			"pageX pageY screenX screenY toElement" ).split( " " ),
		filter: function( event, original ) {
			var body, eventDoc, doc,
				button = original.button,
				fromElement = original.fromElement;

			// Calculate pageX/Y if missing and clientX/Y available
			if ( event.pageX == null && original.clientX != null ) {
				eventDoc = event.target.ownerDocument || document;
				doc = eventDoc.documentElement;
				body = eventDoc.body;

				event.pageX = original.clientX +
					( doc && doc.scrollLeft || body && body.scrollLeft || 0 ) -
					( doc && doc.clientLeft || body && body.clientLeft || 0 );
				event.pageY = original.clientY +
					( doc && doc.scrollTop  || body && body.scrollTop  || 0 ) -
					( doc && doc.clientTop  || body && body.clientTop  || 0 );
			}

			// Add relatedTarget, if necessary
			if ( !event.relatedTarget && fromElement ) {
				event.relatedTarget = fromElement === event.target ?
					original.toElement :
					fromElement;
			}

			// Add which for click: 1 === left; 2 === middle; 3 === right
			// Note: button is not normalized, so don't use it
			if ( !event.which && button !== undefined ) {
				event.which = ( button & 1 ? 1 : ( button & 2 ? 3 : ( button & 4 ? 2 : 0 ) ) );
			}

			return event;
		}
	},

	special: {
		load: {

			// Prevent triggered image.load events from bubbling to window.load
			noBubble: true
		},
		focus: {

			// Fire native event if possible so blur/focus sequence is correct
			trigger: function() {
				if ( this !== safeActiveElement() && this.focus ) {
					try {
						this.focus();
						return false;
					} catch ( e ) {

						// Support: IE<9
						// If we error on focus to hidden element (#1486, #12518),
						// let .trigger() run the handlers
					}
				}
			},
			delegateType: "focusin"
		},
		blur: {
			trigger: function() {
				if ( this === safeActiveElement() && this.blur ) {
					this.blur();
					return false;
				}
			},
			delegateType: "focusout"
		},
		click: {

			// For checkbox, fire native event so checked state will be right
			trigger: function() {
				if ( jQuery.nodeName( this, "input" ) && this.type === "checkbox" && this.click ) {
					this.click();
					return false;
				}
			},

			// For cross-browser consistency, don't fire native .click() on links
			_default: function( event ) {
				return jQuery.nodeName( event.target, "a" );
			}
		},

		beforeunload: {
			postDispatch: function( event ) {

				// Support: Firefox 20+
				// Firefox doesn't alert if the returnValue field is not set.
				if ( event.result !== undefined && event.originalEvent ) {
					event.originalEvent.returnValue = event.result;
				}
			}
		}
	},

	// Piggyback on a donor event to simulate a different one
	simulate: function( type, elem, event ) {
		var e = jQuery.extend(
			new jQuery.Event(),
			event,
			{
				type: type,
				isSimulated: true

				// Previously, `originalEvent: {}` was set here, so stopPropagation call
				// would not be triggered on donor event, since in our own
				// jQuery.event.stopPropagation function we had a check for existence of
				// originalEvent.stopPropagation method, so, consequently it would be a noop.
				//
				// Guard for simulated events was moved to jQuery.event.stopPropagation function
				// since `originalEvent` should point to the original event for the
				// constancy with other events and for more focused logic
			}
		);

		jQuery.event.trigger( e, null, elem );

		if ( e.isDefaultPrevented() ) {
			event.preventDefault();
		}
	}
};

jQuery.removeEvent = document.removeEventListener ?
	function( elem, type, handle ) {

		// This "if" is needed for plain objects
		if ( elem.removeEventListener ) {
			elem.removeEventListener( type, handle );
		}
	} :
	function( elem, type, handle ) {
		var name = "on" + type;

		if ( elem.detachEvent ) {

			// #8545, #7054, preventing memory leaks for custom events in IE6-8
			// detachEvent needed property on element, by name of that event,
			// to properly expose it to GC
			if ( typeof elem[ name ] === "undefined" ) {
				elem[ name ] = null;
			}

			elem.detachEvent( name, handle );
		}
	};

jQuery.Event = function( src, props ) {

	// Allow instantiation without the 'new' keyword
	if ( !( this instanceof jQuery.Event ) ) {
		return new jQuery.Event( src, props );
	}

	// Event object
	if ( src && src.type ) {
		this.originalEvent = src;
		this.type = src.type;

		// Events bubbling up the document may have been marked as prevented
		// by a handler lower down the tree; reflect the correct value.
		this.isDefaultPrevented = src.defaultPrevented ||
				src.defaultPrevented === undefined &&

				// Support: IE < 9, Android < 4.0
				src.returnValue === false ?
			returnTrue :
			returnFalse;

	// Event type
	} else {
		this.type = src;
	}

	// Put explicitly provided properties onto the event object
	if ( props ) {
		jQuery.extend( this, props );
	}

	// Create a timestamp if incoming event doesn't have one
	this.timeStamp = src && src.timeStamp || jQuery.now();

	// Mark it as fixed
	this[ jQuery.expando ] = true;
};

// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
// http://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
jQuery.Event.prototype = {
	constructor: jQuery.Event,
	isDefaultPrevented: returnFalse,
	isPropagationStopped: returnFalse,
	isImmediatePropagationStopped: returnFalse,

	preventDefault: function() {
		var e = this.originalEvent;

		this.isDefaultPrevented = returnTrue;
		if ( !e ) {
			return;
		}

		// If preventDefault exists, run it on the original event
		if ( e.preventDefault ) {
			e.preventDefault();

		// Support: IE
		// Otherwise set the returnValue property of the original event to false
		} else {
			e.returnValue = false;
		}
	},
	stopPropagation: function() {
		var e = this.originalEvent;

		this.isPropagationStopped = returnTrue;

		if ( !e || this.isSimulated ) {
			return;
		}

		// If stopPropagation exists, run it on the original event
		if ( e.stopPropagation ) {
			e.stopPropagation();
		}

		// Support: IE
		// Set the cancelBubble property of the original event to true
		e.cancelBubble = true;
	},
	stopImmediatePropagation: function() {
		var e = this.originalEvent;

		this.isImmediatePropagationStopped = returnTrue;

		if ( e && e.stopImmediatePropagation ) {
			e.stopImmediatePropagation();
		}

		this.stopPropagation();
	}
};

// Create mouseenter/leave events using mouseover/out and event-time checks
// so that event delegation works in jQuery.
// Do the same for pointerenter/pointerleave and pointerover/pointerout
//
// Support: Safari 7 only
// Safari sends mouseenter too often; see:
// https://code.google.com/p/chromium/issues/detail?id=470258
// for the description of the bug (it existed in older Chrome versions as well).
jQuery.each( {
	mouseenter: "mouseover",
	mouseleave: "mouseout",
	pointerenter: "pointerover",
	pointerleave: "pointerout"
}, function( orig, fix ) {
	jQuery.event.special[ orig ] = {
		delegateType: fix,
		bindType: fix,

		handle: function( event ) {
			var ret,
				target = this,
				related = event.relatedTarget,
				handleObj = event.handleObj;

			// For mouseenter/leave call the handler if related is outside the target.
			// NB: No relatedTarget if the mouse left/entered the browser window
			if ( !related || ( related !== target && !jQuery.contains( target, related ) ) ) {
				event.type = handleObj.origType;
				ret = handleObj.handler.apply( this, arguments );
				event.type = fix;
			}
			return ret;
		}
	};
} );

// IE submit delegation
if ( !support.submit ) {

	jQuery.event.special.submit = {
		setup: function() {

			// Only need this for delegated form submit events
			if ( jQuery.nodeName( this, "form" ) ) {
				return false;
			}

			// Lazy-add a submit handler when a descendant form may potentially be submitted
			jQuery.event.add( this, "click._submit keypress._submit", function( e ) {

				// Node name check avoids a VML-related crash in IE (#9807)
				var elem = e.target,
					form = jQuery.nodeName( elem, "input" ) || jQuery.nodeName( elem, "button" ) ?

						// Support: IE <=8
						// We use jQuery.prop instead of elem.form
						// to allow fixing the IE8 delegated submit issue (gh-2332)
						// by 3rd party polyfills/workarounds.
						jQuery.prop( elem, "form" ) :
						undefined;

				if ( form && !jQuery._data( form, "submit" ) ) {
					jQuery.event.add( form, "submit._submit", function( event ) {
						event._submitBubble = true;
					} );
					jQuery._data( form, "submit", true );
				}
			} );

			// return undefined since we don't need an event listener
		},

		postDispatch: function( event ) {

			// If form was submitted by the user, bubble the event up the tree
			if ( event._submitBubble ) {
				delete event._submitBubble;
				if ( this.parentNode && !event.isTrigger ) {
					jQuery.event.simulate( "submit", this.parentNode, event );
				}
			}
		},

		teardown: function() {

			// Only need this for delegated form submit events
			if ( jQuery.nodeName( this, "form" ) ) {
				return false;
			}

			// Remove delegated handlers; cleanData eventually reaps submit handlers attached above
			jQuery.event.remove( this, "._submit" );
		}
	};
}

// IE change delegation and checkbox/radio fix
if ( !support.change ) {

	jQuery.event.special.change = {

		setup: function() {

			if ( rformElems.test( this.nodeName ) ) {

				// IE doesn't fire change on a check/radio until blur; trigger it on click
				// after a propertychange. Eat the blur-change in special.change.handle.
				// This still fires onchange a second time for check/radio after blur.
				if ( this.type === "checkbox" || this.type === "radio" ) {
					jQuery.event.add( this, "propertychange._change", function( event ) {
						if ( event.originalEvent.propertyName === "checked" ) {
							this._justChanged = true;
						}
					} );
					jQuery.event.add( this, "click._change", function( event ) {
						if ( this._justChanged && !event.isTrigger ) {
							this._justChanged = false;
						}

						// Allow triggered, simulated change events (#11500)
						jQuery.event.simulate( "change", this, event );
					} );
				}
				return false;
			}

			// Delegated event; lazy-add a change handler on descendant inputs
			jQuery.event.add( this, "beforeactivate._change", function( e ) {
				var elem = e.target;

				if ( rformElems.test( elem.nodeName ) && !jQuery._data( elem, "change" ) ) {
					jQuery.event.add( elem, "change._change", function( event ) {
						if ( this.parentNode && !event.isSimulated && !event.isTrigger ) {
							jQuery.event.simulate( "change", this.parentNode, event );
						}
					} );
					jQuery._data( elem, "change", true );
				}
			} );
		},

		handle: function( event ) {
			var elem = event.target;

			// Swallow native change events from checkbox/radio, we already triggered them above
			if ( this !== elem || event.isSimulated || event.isTrigger ||
				( elem.type !== "radio" && elem.type !== "checkbox" ) ) {

				return event.handleObj.handler.apply( this, arguments );
			}
		},

		teardown: function() {
			jQuery.event.remove( this, "._change" );

			return !rformElems.test( this.nodeName );
		}
	};
}

// Support: Firefox
// Firefox doesn't have focus(in | out) events
// Related ticket - https://bugzilla.mozilla.org/show_bug.cgi?id=687787
//
// Support: Chrome, Safari
// focus(in | out) events fire after focus & blur events,
// which is spec violation - http://www.w3.org/TR/DOM-Level-3-Events/#events-focusevent-event-order
// Related ticket - https://code.google.com/p/chromium/issues/detail?id=449857
if ( !support.focusin ) {
	jQuery.each( { focus: "focusin", blur: "focusout" }, function( orig, fix ) {

		// Attach a single capturing handler on the document while someone wants focusin/focusout
		var handler = function( event ) {
			jQuery.event.simulate( fix, event.target, jQuery.event.fix( event ) );
		};

		jQuery.event.special[ fix ] = {
			setup: function() {
				var doc = this.ownerDocument || this,
					attaches = jQuery._data( doc, fix );

				if ( !attaches ) {
					doc.addEventListener( orig, handler, true );
				}
				jQuery._data( doc, fix, ( attaches || 0 ) + 1 );
			},
			teardown: function() {
				var doc = this.ownerDocument || this,
					attaches = jQuery._data( doc, fix ) - 1;

				if ( !attaches ) {
					doc.removeEventListener( orig, handler, true );
					jQuery._removeData( doc, fix );
				} else {
					jQuery._data( doc, fix, attaches );
				}
			}
		};
	} );
}

jQuery.fn.extend( {

	on: function( types, selector, data, fn ) {
		return on( this, types, selector, data, fn );
	},
	one: function( types, selector, data, fn ) {
		return on( this, types, selector, data, fn, 1 );
	},
	off: function( types, selector, fn ) {
		var handleObj, type;
		if ( types && types.preventDefault && types.handleObj ) {

			// ( event )  dispatched jQuery.Event
			handleObj = types.handleObj;
			jQuery( types.delegateTarget ).off(
				handleObj.namespace ?
					handleObj.origType + "." + handleObj.namespace :
					handleObj.origType,
				handleObj.selector,
				handleObj.handler
			);
			return this;
		}
		if ( typeof types === "object" ) {

			// ( types-object [, selector] )
			for ( type in types ) {
				this.off( type, selector, types[ type ] );
			}
			return this;
		}
		if ( selector === false || typeof selector === "function" ) {

			// ( types [, fn] )
			fn = selector;
			selector = undefined;
		}
		if ( fn === false ) {
			fn = returnFalse;
		}
		return this.each( function() {
			jQuery.event.remove( this, types, fn, selector );
		} );
	},

	trigger: function( type, data ) {
		return this.each( function() {
			jQuery.event.trigger( type, data, this );
		} );
	},
	triggerHandler: function( type, data ) {
		var elem = this[ 0 ];
		if ( elem ) {
			return jQuery.event.trigger( type, data, elem, true );
		}
	}
} );


var rinlinejQuery = / jQuery\d+="(?:null|\d+)"/g,
	rnoshimcache = new RegExp( "<(?:" + nodeNames + ")[\\s/>]", "i" ),
	rxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:-]+)[^>]*)\/>/gi,

	// Support: IE 10-11, Edge 10240+
	// In IE/Edge using regex groups here causes severe slowdowns.
	// See https://connect.microsoft.com/IE/feedback/details/1736512/
	rnoInnerhtml = /<script|<style|<link/i,

	// checked="checked" or checked
	rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i,
	rscriptTypeMasked = /^true\/(.*)/,
	rcleanScript = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g,
	safeFragment = createSafeFragment( document ),
	fragmentDiv = safeFragment.appendChild( document.createElement( "div" ) );

// Support: IE<8
// Manipulating tables requires a tbody
function manipulationTarget( elem, content ) {
	return jQuery.nodeName( elem, "table" ) &&
		jQuery.nodeName( content.nodeType !== 11 ? content : content.firstChild, "tr" ) ?

		elem.getElementsByTagName( "tbody" )[ 0 ] ||
			elem.appendChild( elem.ownerDocument.createElement( "tbody" ) ) :
		elem;
}

// Replace/restore the type attribute of script elements for safe DOM manipulation
function disableScript( elem ) {
	elem.type = ( jQuery.find.attr( elem, "type" ) !== null ) + "/" + elem.type;
	return elem;
}
function restoreScript( elem ) {
	var match = rscriptTypeMasked.exec( elem.type );
	if ( match ) {
		elem.type = match[ 1 ];
	} else {
		elem.removeAttribute( "type" );
	}
	return elem;
}

function cloneCopyEvent( src, dest ) {
	if ( dest.nodeType !== 1 || !jQuery.hasData( src ) ) {
		return;
	}

	var type, i, l,
		oldData = jQuery._data( src ),
		curData = jQuery._data( dest, oldData ),
		events = oldData.events;

	if ( events ) {
		delete curData.handle;
		curData.events = {};

		for ( type in events ) {
			for ( i = 0, l = events[ type ].length; i < l; i++ ) {
				jQuery.event.add( dest, type, events[ type ][ i ] );
			}
		}
	}

	// make the cloned public data object a copy from the original
	if ( curData.data ) {
		curData.data = jQuery.extend( {}, curData.data );
	}
}

function fixCloneNodeIssues( src, dest ) {
	var nodeName, e, data;

	// We do not need to do anything for non-Elements
	if ( dest.nodeType !== 1 ) {
		return;
	}

	nodeName = dest.nodeName.toLowerCase();

	// IE6-8 copies events bound via attachEvent when using cloneNode.
	if ( !support.noCloneEvent && dest[ jQuery.expando ] ) {
		data = jQuery._data( dest );

		for ( e in data.events ) {
			jQuery.removeEvent( dest, e, data.handle );
		}

		// Event data gets referenced instead of copied if the expando gets copied too
		dest.removeAttribute( jQuery.expando );
	}

	// IE blanks contents when cloning scripts, and tries to evaluate newly-set text
	if ( nodeName === "script" && dest.text !== src.text ) {
		disableScript( dest ).text = src.text;
		restoreScript( dest );

	// IE6-10 improperly clones children of object elements using classid.
	// IE10 throws NoModificationAllowedError if parent is null, #12132.
	} else if ( nodeName === "object" ) {
		if ( dest.parentNode ) {
			dest.outerHTML = src.outerHTML;
		}

		// This path appears unavoidable for IE9. When cloning an object
		// element in IE9, the outerHTML strategy above is not sufficient.
		// If the src has innerHTML and the destination does not,
		// copy the src.innerHTML into the dest.innerHTML. #10324
		if ( support.html5Clone && ( src.innerHTML && !jQuery.trim( dest.innerHTML ) ) ) {
			dest.innerHTML = src.innerHTML;
		}

	} else if ( nodeName === "input" && rcheckableType.test( src.type ) ) {

		// IE6-8 fails to persist the checked state of a cloned checkbox
		// or radio button. Worse, IE6-7 fail to give the cloned element
		// a checked appearance if the defaultChecked value isn't also set

		dest.defaultChecked = dest.checked = src.checked;

		// IE6-7 get confused and end up setting the value of a cloned
		// checkbox/radio button to an empty string instead of "on"
		if ( dest.value !== src.value ) {
			dest.value = src.value;
		}

	// IE6-8 fails to return the selected option to the default selected
	// state when cloning options
	} else if ( nodeName === "option" ) {
		dest.defaultSelected = dest.selected = src.defaultSelected;

	// IE6-8 fails to set the defaultValue to the correct value when
	// cloning other types of input fields
	} else if ( nodeName === "input" || nodeName === "textarea" ) {
		dest.defaultValue = src.defaultValue;
	}
}

function domManip( collection, args, callback, ignored ) {

	// Flatten any nested arrays
	args = concat.apply( [], args );

	var first, node, hasScripts,
		scripts, doc, fragment,
		i = 0,
		l = collection.length,
		iNoClone = l - 1,
		value = args[ 0 ],
		isFunction = jQuery.isFunction( value );

	// We can't cloneNode fragments that contain checked, in WebKit
	if ( isFunction ||
			( l > 1 && typeof value === "string" &&
				!support.checkClone && rchecked.test( value ) ) ) {
		return collection.each( function( index ) {
			var self = collection.eq( index );
			if ( isFunction ) {
				args[ 0 ] = value.call( this, index, self.html() );
			}
			domManip( self, args, callback, ignored );
		} );
	}

	if ( l ) {
		fragment = buildFragment( args, collection[ 0 ].ownerDocument, false, collection, ignored );
		first = fragment.firstChild;

		if ( fragment.childNodes.length === 1 ) {
			fragment = first;
		}

		// Require either new content or an interest in ignored elements to invoke the callback
		if ( first || ignored ) {
			scripts = jQuery.map( getAll( fragment, "script" ), disableScript );
			hasScripts = scripts.length;

			// Use the original fragment for the last item
			// instead of the first because it can end up
			// being emptied incorrectly in certain situations (#8070).
			for ( ; i < l; i++ ) {
				node = fragment;

				if ( i !== iNoClone ) {
					node = jQuery.clone( node, true, true );

					// Keep references to cloned scripts for later restoration
					if ( hasScripts ) {

						// Support: Android<4.1, PhantomJS<2
						// push.apply(_, arraylike) throws on ancient WebKit
						jQuery.merge( scripts, getAll( node, "script" ) );
					}
				}

				callback.call( collection[ i ], node, i );
			}

			if ( hasScripts ) {
				doc = scripts[ scripts.length - 1 ].ownerDocument;

				// Reenable scripts
				jQuery.map( scripts, restoreScript );

				// Evaluate executable scripts on first document insertion
				for ( i = 0; i < hasScripts; i++ ) {
					node = scripts[ i ];
					if ( rscriptType.test( node.type || "" ) &&
						!jQuery._data( node, "globalEval" ) &&
						jQuery.contains( doc, node ) ) {

						if ( node.src ) {

							// Optional AJAX dependency, but won't run scripts if not present
							if ( jQuery._evalUrl ) {
								jQuery._evalUrl( node.src );
							}
						} else {
							jQuery.globalEval(
								( node.text || node.textContent || node.innerHTML || "" )
									.replace( rcleanScript, "" )
							);
						}
					}
				}
			}

			// Fix #11809: Avoid leaking memory
			fragment = first = null;
		}
	}

	return collection;
}

function remove( elem, selector, keepData ) {
	var node,
		elems = selector ? jQuery.filter( selector, elem ) : elem,
		i = 0;

	for ( ; ( node = elems[ i ] ) != null; i++ ) {

		if ( !keepData && node.nodeType === 1 ) {
			jQuery.cleanData( getAll( node ) );
		}

		if ( node.parentNode ) {
			if ( keepData && jQuery.contains( node.ownerDocument, node ) ) {
				setGlobalEval( getAll( node, "script" ) );
			}
			node.parentNode.removeChild( node );
		}
	}

	return elem;
}

jQuery.extend( {
	htmlPrefilter: function( html ) {
		return html.replace( rxhtmlTag, "<$1></$2>" );
	},

	clone: function( elem, dataAndEvents, deepDataAndEvents ) {
		var destElements, node, clone, i, srcElements,
			inPage = jQuery.contains( elem.ownerDocument, elem );

		if ( support.html5Clone || jQuery.isXMLDoc( elem ) ||
			!rnoshimcache.test( "<" + elem.nodeName + ">" ) ) {

			clone = elem.cloneNode( true );

		// IE<=8 does not properly clone detached, unknown element nodes
		} else {
			fragmentDiv.innerHTML = elem.outerHTML;
			fragmentDiv.removeChild( clone = fragmentDiv.firstChild );
		}

		if ( ( !support.noCloneEvent || !support.noCloneChecked ) &&
				( elem.nodeType === 1 || elem.nodeType === 11 ) && !jQuery.isXMLDoc( elem ) ) {

			// We eschew Sizzle here for performance reasons: http://jsperf.com/getall-vs-sizzle/2
			destElements = getAll( clone );
			srcElements = getAll( elem );

			// Fix all IE cloning issues
			for ( i = 0; ( node = srcElements[ i ] ) != null; ++i ) {

				// Ensure that the destination node is not null; Fixes #9587
				if ( destElements[ i ] ) {
					fixCloneNodeIssues( node, destElements[ i ] );
				}
			}
		}

		// Copy the events from the original to the clone
		if ( dataAndEvents ) {
			if ( deepDataAndEvents ) {
				srcElements = srcElements || getAll( elem );
				destElements = destElements || getAll( clone );

				for ( i = 0; ( node = srcElements[ i ] ) != null; i++ ) {
					cloneCopyEvent( node, destElements[ i ] );
				}
			} else {
				cloneCopyEvent( elem, clone );
			}
		}

		// Preserve script evaluation history
		destElements = getAll( clone, "script" );
		if ( destElements.length > 0 ) {
			setGlobalEval( destElements, !inPage && getAll( elem, "script" ) );
		}

		destElements = srcElements = node = null;

		// Return the cloned set
		return clone;
	},

	cleanData: function( elems, /* internal */ forceAcceptData ) {
		var elem, type, id, data,
			i = 0,
			internalKey = jQuery.expando,
			cache = jQuery.cache,
			attributes = support.attributes,
			special = jQuery.event.special;

		for ( ; ( elem = elems[ i ] ) != null; i++ ) {
			if ( forceAcceptData || acceptData( elem ) ) {

				id = elem[ internalKey ];
				data = id && cache[ id ];

				if ( data ) {
					if ( data.events ) {
						for ( type in data.events ) {
							if ( special[ type ] ) {
								jQuery.event.remove( elem, type );

							// This is a shortcut to avoid jQuery.event.remove's overhead
							} else {
								jQuery.removeEvent( elem, type, data.handle );
							}
						}
					}

					// Remove cache only if it was not already removed by jQuery.event.remove
					if ( cache[ id ] ) {

						delete cache[ id ];

						// Support: IE<9
						// IE does not allow us to delete expando properties from nodes
						// IE creates expando attributes along with the property
						// IE does not have a removeAttribute function on Document nodes
						if ( !attributes && typeof elem.removeAttribute !== "undefined" ) {
							elem.removeAttribute( internalKey );

						// Webkit & Blink performance suffers when deleting properties
						// from DOM nodes, so set to undefined instead
						// https://code.google.com/p/chromium/issues/detail?id=378607
						} else {
							elem[ internalKey ] = undefined;
						}

						deletedIds.push( id );
					}
				}
			}
		}
	}
} );

jQuery.fn.extend( {

	// Keep domManip exposed until 3.0 (gh-2225)
	domManip: domManip,

	detach: function( selector ) {
		return remove( this, selector, true );
	},

	remove: function( selector ) {
		return remove( this, selector );
	},

	text: function( value ) {
		return access( this, function( value ) {
			return value === undefined ?
				jQuery.text( this ) :
				this.empty().append(
					( this[ 0 ] && this[ 0 ].ownerDocument || document ).createTextNode( value )
				);
		}, null, value, arguments.length );
	},

	append: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
				var target = manipulationTarget( this, elem );
				target.appendChild( elem );
			}
		} );
	},

	prepend: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
				var target = manipulationTarget( this, elem );
				target.insertBefore( elem, target.firstChild );
			}
		} );
	},

	before: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.parentNode ) {
				this.parentNode.insertBefore( elem, this );
			}
		} );
	},

	after: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.parentNode ) {
				this.parentNode.insertBefore( elem, this.nextSibling );
			}
		} );
	},

	empty: function() {
		var elem,
			i = 0;

		for ( ; ( elem = this[ i ] ) != null; i++ ) {

			// Remove element nodes and prevent memory leaks
			if ( elem.nodeType === 1 ) {
				jQuery.cleanData( getAll( elem, false ) );
			}

			// Remove any remaining nodes
			while ( elem.firstChild ) {
				elem.removeChild( elem.firstChild );
			}

			// If this is a select, ensure that it displays empty (#12336)
			// Support: IE<9
			if ( elem.options && jQuery.nodeName( elem, "select" ) ) {
				elem.options.length = 0;
			}
		}

		return this;
	},

	clone: function( dataAndEvents, deepDataAndEvents ) {
		dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
		deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;

		return this.map( function() {
			return jQuery.clone( this, dataAndEvents, deepDataAndEvents );
		} );
	},

	html: function( value ) {
		return access( this, function( value ) {
			var elem = this[ 0 ] || {},
				i = 0,
				l = this.length;

			if ( value === undefined ) {
				return elem.nodeType === 1 ?
					elem.innerHTML.replace( rinlinejQuery, "" ) :
					undefined;
			}

			// See if we can take a shortcut and just use innerHTML
			if ( typeof value === "string" && !rnoInnerhtml.test( value ) &&
				( support.htmlSerialize || !rnoshimcache.test( value )  ) &&
				( support.leadingWhitespace || !rleadingWhitespace.test( value ) ) &&
				!wrapMap[ ( rtagName.exec( value ) || [ "", "" ] )[ 1 ].toLowerCase() ] ) {

				value = jQuery.htmlPrefilter( value );

				try {
					for ( ; i < l; i++ ) {

						// Remove element nodes and prevent memory leaks
						elem = this[ i ] || {};
						if ( elem.nodeType === 1 ) {
							jQuery.cleanData( getAll( elem, false ) );
							elem.innerHTML = value;
						}
					}

					elem = 0;

				// If using innerHTML throws an exception, use the fallback method
				} catch ( e ) {}
			}

			if ( elem ) {
				this.empty().append( value );
			}
		}, null, value, arguments.length );
	},

	replaceWith: function() {
		var ignored = [];

		// Make the changes, replacing each non-ignored context element with the new content
		return domManip( this, arguments, function( elem ) {
			var parent = this.parentNode;

			if ( jQuery.inArray( this, ignored ) < 0 ) {
				jQuery.cleanData( getAll( this ) );
				if ( parent ) {
					parent.replaceChild( elem, this );
				}
			}

		// Force callback invocation
		}, ignored );
	}
} );

jQuery.each( {
	appendTo: "append",
	prependTo: "prepend",
	insertBefore: "before",
	insertAfter: "after",
	replaceAll: "replaceWith"
}, function( name, original ) {
	jQuery.fn[ name ] = function( selector ) {
		var elems,
			i = 0,
			ret = [],
			insert = jQuery( selector ),
			last = insert.length - 1;

		for ( ; i <= last; i++ ) {
			elems = i === last ? this : this.clone( true );
			jQuery( insert[ i ] )[ original ]( elems );

			// Modern browsers can apply jQuery collections as arrays, but oldIE needs a .get()
			push.apply( ret, elems.get() );
		}

		return this.pushStack( ret );
	};
} );


var iframe,
	elemdisplay = {

		// Support: Firefox
		// We have to pre-define these values for FF (#10227)
		HTML: "block",
		BODY: "block"
	};

/**
 * Retrieve the actual display of a element
 * @param {String} name nodeName of the element
 * @param {Object} doc Document object
 */

// Called only from within defaultDisplay
function actualDisplay( name, doc ) {
	var elem = jQuery( doc.createElement( name ) ).appendTo( doc.body ),

		display = jQuery.css( elem[ 0 ], "display" );

	// We don't have any data stored on the element,
	// so use "detach" method as fast way to get rid of the element
	elem.detach();

	return display;
}

/**
 * Try to determine the default display value of an element
 * @param {String} nodeName
 */
function defaultDisplay( nodeName ) {
	var doc = document,
		display = elemdisplay[ nodeName ];

	if ( !display ) {
		display = actualDisplay( nodeName, doc );

		// If the simple way fails, read from inside an iframe
		if ( display === "none" || !display ) {

			// Use the already-created iframe if possible
			iframe = ( iframe || jQuery( "<iframe frameborder='0' width='0' height='0'/>" ) )
				.appendTo( doc.documentElement );

			// Always write a new HTML skeleton so Webkit and Firefox don't choke on reuse
			doc = ( iframe[ 0 ].contentWindow || iframe[ 0 ].contentDocument ).document;

			// Support: IE
			doc.write();
			doc.close();

			display = actualDisplay( nodeName, doc );
			iframe.detach();
		}

		// Store the correct default display
		elemdisplay[ nodeName ] = display;
	}

	return display;
}
var rmargin = ( /^margin/ );

var rnumnonpx = new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );

var swap = function( elem, options, callback, args ) {
	var ret, name,
		old = {};

	// Remember the old values, and insert the new ones
	for ( name in options ) {
		old[ name ] = elem.style[ name ];
		elem.style[ name ] = options[ name ];
	}

	ret = callback.apply( elem, args || [] );

	// Revert the old values
	for ( name in options ) {
		elem.style[ name ] = old[ name ];
	}

	return ret;
};


var documentElement = document.documentElement;



( function() {
	var pixelPositionVal, pixelMarginRightVal, boxSizingReliableVal,
		reliableHiddenOffsetsVal, reliableMarginRightVal, reliableMarginLeftVal,
		container = document.createElement( "div" ),
		div = document.createElement( "div" );

	// Finish early in limited (non-browser) environments
	if ( !div.style ) {
		return;
	}

	div.style.cssText = "float:left;opacity:.5";

	// Support: IE<9
	// Make sure that element opacity exists (as opposed to filter)
	support.opacity = div.style.opacity === "0.5";

	// Verify style float existence
	// (IE uses styleFloat instead of cssFloat)
	support.cssFloat = !!div.style.cssFloat;

	div.style.backgroundClip = "content-box";
	div.cloneNode( true ).style.backgroundClip = "";
	support.clearCloneStyle = div.style.backgroundClip === "content-box";

	container = document.createElement( "div" );
	container.style.cssText = "border:0;width:8px;height:0;top:0;left:-9999px;" +
		"padding:0;margin-top:1px;position:absolute";
	div.innerHTML = "";
	container.appendChild( div );

	// Support: Firefox<29, Android 2.3
	// Vendor-prefix box-sizing
	support.boxSizing = div.style.boxSizing === "" || div.style.MozBoxSizing === "" ||
		div.style.WebkitBoxSizing === "";

	jQuery.extend( support, {
		reliableHiddenOffsets: function() {
			if ( pixelPositionVal == null ) {
				computeStyleTests();
			}
			return reliableHiddenOffsetsVal;
		},

		boxSizingReliable: function() {

			// We're checking for pixelPositionVal here instead of boxSizingReliableVal
			// since that compresses better and they're computed together anyway.
			if ( pixelPositionVal == null ) {
				computeStyleTests();
			}
			return boxSizingReliableVal;
		},

		pixelMarginRight: function() {

			// Support: Android 4.0-4.3
			if ( pixelPositionVal == null ) {
				computeStyleTests();
			}
			return pixelMarginRightVal;
		},

		pixelPosition: function() {
			if ( pixelPositionVal == null ) {
				computeStyleTests();
			}
			return pixelPositionVal;
		},

		reliableMarginRight: function() {

			// Support: Android 2.3
			if ( pixelPositionVal == null ) {
				computeStyleTests();
			}
			return reliableMarginRightVal;
		},

		reliableMarginLeft: function() {

			// Support: IE <=8 only, Android 4.0 - 4.3 only, Firefox <=3 - 37
			if ( pixelPositionVal == null ) {
				computeStyleTests();
			}
			return reliableMarginLeftVal;
		}
	} );

	function computeStyleTests() {
		var contents, divStyle,
			documentElement = document.documentElement;

		// Setup
		documentElement.appendChild( container );

		div.style.cssText =

			// Support: Android 2.3
			// Vendor-prefix box-sizing
			"-webkit-box-sizing:border-box;box-sizing:border-box;" +
			"position:relative;display:block;" +
			"margin:auto;border:1px;padding:1px;" +
			"top:1%;width:50%";

		// Support: IE<9
		// Assume reasonable values in the absence of getComputedStyle
		pixelPositionVal = boxSizingReliableVal = reliableMarginLeftVal = false;
		pixelMarginRightVal = reliableMarginRightVal = true;

		// Check for getComputedStyle so that this code is not run in IE<9.
		if ( window.getComputedStyle ) {
			divStyle = window.getComputedStyle( div );
			pixelPositionVal = ( divStyle || {} ).top !== "1%";
			reliableMarginLeftVal = ( divStyle || {} ).marginLeft === "2px";
			boxSizingReliableVal = ( divStyle || { width: "4px" } ).width === "4px";

			// Support: Android 4.0 - 4.3 only
			// Some styles come back with percentage values, even though they shouldn't
			div.style.marginRight = "50%";
			pixelMarginRightVal = ( divStyle || { marginRight: "4px" } ).marginRight === "4px";

			// Support: Android 2.3 only
			// Div with explicit width and no margin-right incorrectly
			// gets computed margin-right based on width of container (#3333)
			// WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right
			contents = div.appendChild( document.createElement( "div" ) );

			// Reset CSS: box-sizing; display; margin; border; padding
			contents.style.cssText = div.style.cssText =

				// Support: Android 2.3
				// Vendor-prefix box-sizing
				"-webkit-box-sizing:content-box;-moz-box-sizing:content-box;" +
				"box-sizing:content-box;display:block;margin:0;border:0;padding:0";
			contents.style.marginRight = contents.style.width = "0";
			div.style.width = "1px";

			reliableMarginRightVal =
				!parseFloat( ( window.getComputedStyle( contents ) || {} ).marginRight );

			div.removeChild( contents );
		}

		// Support: IE6-8
		// First check that getClientRects works as expected
		// Check if table cells still have offsetWidth/Height when they are set
		// to display:none and there are still other visible table cells in a
		// table row; if so, offsetWidth/Height are not reliable for use when
		// determining if an element has been hidden directly using
		// display:none (it is still safe to use offsets if a parent element is
		// hidden; don safety goggles and see bug #4512 for more information).
		div.style.display = "none";
		reliableHiddenOffsetsVal = div.getClientRects().length === 0;
		if ( reliableHiddenOffsetsVal ) {
			div.style.display = "";
			div.innerHTML = "<table><tr><td></td><td>t</td></tr></table>";
			div.childNodes[ 0 ].style.borderCollapse = "separate";
			contents = div.getElementsByTagName( "td" );
			contents[ 0 ].style.cssText = "margin:0;border:0;padding:0;display:none";
			reliableHiddenOffsetsVal = contents[ 0 ].offsetHeight === 0;
			if ( reliableHiddenOffsetsVal ) {
				contents[ 0 ].style.display = "";
				contents[ 1 ].style.display = "none";
				reliableHiddenOffsetsVal = contents[ 0 ].offsetHeight === 0;
			}
		}

		// Teardown
		documentElement.removeChild( container );
	}

} )();


var getStyles, curCSS,
	rposition = /^(top|right|bottom|left)$/;

if ( window.getComputedStyle ) {
	getStyles = function( elem ) {

		// Support: IE<=11+, Firefox<=30+ (#15098, #14150)
		// IE throws on elements created in popups
		// FF meanwhile throws on frame elements through "defaultView.getComputedStyle"
		var view = elem.ownerDocument.defaultView;

		if ( !view || !view.opener ) {
			view = window;
		}

		return view.getComputedStyle( elem );
	};

	curCSS = function( elem, name, computed ) {
		var width, minWidth, maxWidth, ret,
			style = elem.style;

		computed = computed || getStyles( elem );

		// getPropertyValue is only needed for .css('filter') in IE9, see #12537
		ret = computed ? computed.getPropertyValue( name ) || computed[ name ] : undefined;

		// Support: Opera 12.1x only
		// Fall back to style even without computed
		// computed is undefined for elems on document fragments
		if ( ( ret === "" || ret === undefined ) && !jQuery.contains( elem.ownerDocument, elem ) ) {
			ret = jQuery.style( elem, name );
		}

		if ( computed ) {

			// A tribute to the "awesome hack by Dean Edwards"
			// Chrome < 17 and Safari 5.0 uses "computed value"
			// instead of "used value" for margin-right
			// Safari 5.1.7 (at least) returns percentage for a larger set of values,
			// but width seems to be reliably pixels
			// this is against the CSSOM draft spec:
			// http://dev.w3.org/csswg/cssom/#resolved-values
			if ( !support.pixelMarginRight() && rnumnonpx.test( ret ) && rmargin.test( name ) ) {

				// Remember the original values
				width = style.width;
				minWidth = style.minWidth;
				maxWidth = style.maxWidth;

				// Put in the new values to get a computed value out
				style.minWidth = style.maxWidth = style.width = ret;
				ret = computed.width;

				// Revert the changed values
				style.width = width;
				style.minWidth = minWidth;
				style.maxWidth = maxWidth;
			}
		}

		// Support: IE
		// IE returns zIndex value as an integer.
		return ret === undefined ?
			ret :
			ret + "";
	};
} else if ( documentElement.currentStyle ) {
	getStyles = function( elem ) {
		return elem.currentStyle;
	};

	curCSS = function( elem, name, computed ) {
		var left, rs, rsLeft, ret,
			style = elem.style;

		computed = computed || getStyles( elem );
		ret = computed ? computed[ name ] : undefined;

		// Avoid setting ret to empty string here
		// so we don't default to auto
		if ( ret == null && style && style[ name ] ) {
			ret = style[ name ];
		}

		// From the awesome hack by Dean Edwards
		// http://erik.eae.net/archives/2007/07/27/18.54.15/#comment-102291

		// If we're not dealing with a regular pixel number
		// but a number that has a weird ending, we need to convert it to pixels
		// but not position css attributes, as those are
		// proportional to the parent element instead
		// and we can't measure the parent instead because it
		// might trigger a "stacking dolls" problem
		if ( rnumnonpx.test( ret ) && !rposition.test( name ) ) {

			// Remember the original values
			left = style.left;
			rs = elem.runtimeStyle;
			rsLeft = rs && rs.left;

			// Put in the new values to get a computed value out
			if ( rsLeft ) {
				rs.left = elem.currentStyle.left;
			}
			style.left = name === "fontSize" ? "1em" : ret;
			ret = style.pixelLeft + "px";

			// Revert the changed values
			style.left = left;
			if ( rsLeft ) {
				rs.left = rsLeft;
			}
		}

		// Support: IE
		// IE returns zIndex value as an integer.
		return ret === undefined ?
			ret :
			ret + "" || "auto";
	};
}




function addGetHookIf( conditionFn, hookFn ) {

	// Define the hook, we'll check on the first run if it's really needed.
	return {
		get: function() {
			if ( conditionFn() ) {

				// Hook not needed (or it's not possible to use it due
				// to missing dependency), remove it.
				delete this.get;
				return;
			}

			// Hook needed; redefine it so that the support test is not executed again.
			return ( this.get = hookFn ).apply( this, arguments );
		}
	};
}


var

		ralpha = /alpha\([^)]*\)/i,
	ropacity = /opacity\s*=\s*([^)]*)/i,

	// swappable if display is none or starts with table except
	// "table", "table-cell", or "table-caption"
	// see here for display values:
	// https://developer.mozilla.org/en-US/docs/CSS/display
	rdisplayswap = /^(none|table(?!-c[ea]).+)/,
	rnumsplit = new RegExp( "^(" + pnum + ")(.*)$", "i" ),

	cssShow = { position: "absolute", visibility: "hidden", display: "block" },
	cssNormalTransform = {
		letterSpacing: "0",
		fontWeight: "400"
	},

	cssPrefixes = [ "Webkit", "O", "Moz", "ms" ],
	emptyStyle = document.createElement( "div" ).style;


// return a css property mapped to a potentially vendor prefixed property
function vendorPropName( name ) {

	// shortcut for names that are not vendor prefixed
	if ( name in emptyStyle ) {
		return name;
	}

	// check for vendor prefixed names
	var capName = name.charAt( 0 ).toUpperCase() + name.slice( 1 ),
		i = cssPrefixes.length;

	while ( i-- ) {
		name = cssPrefixes[ i ] + capName;
		if ( name in emptyStyle ) {
			return name;
		}
	}
}

function showHide( elements, show ) {
	var display, elem, hidden,
		values = [],
		index = 0,
		length = elements.length;

	for ( ; index < length; index++ ) {
		elem = elements[ index ];
		if ( !elem.style ) {
			continue;
		}

		values[ index ] = jQuery._data( elem, "olddisplay" );
		display = elem.style.display;
		if ( show ) {

			// Reset the inline display of this element to learn if it is
			// being hidden by cascaded rules or not
			if ( !values[ index ] && display === "none" ) {
				elem.style.display = "";
			}

			// Set elements which have been overridden with display: none
			// in a stylesheet to whatever the default browser style is
			// for such an element
			if ( elem.style.display === "" && isHidden( elem ) ) {
				values[ index ] =
					jQuery._data( elem, "olddisplay", defaultDisplay( elem.nodeName ) );
			}
		} else {
			hidden = isHidden( elem );

			if ( display && display !== "none" || !hidden ) {
				jQuery._data(
					elem,
					"olddisplay",
					hidden ? display : jQuery.css( elem, "display" )
				);
			}
		}
	}

	// Set the display of most of the elements in a second loop
	// to avoid the constant reflow
	for ( index = 0; index < length; index++ ) {
		elem = elements[ index ];
		if ( !elem.style ) {
			continue;
		}
		if ( !show || elem.style.display === "none" || elem.style.display === "" ) {
			elem.style.display = show ? values[ index ] || "" : "none";
		}
	}

	return elements;
}

function setPositiveNumber( elem, value, subtract ) {
	var matches = rnumsplit.exec( value );
	return matches ?

		// Guard against undefined "subtract", e.g., when used as in cssHooks
		Math.max( 0, matches[ 1 ] - ( subtract || 0 ) ) + ( matches[ 2 ] || "px" ) :
		value;
}

function augmentWidthOrHeight( elem, name, extra, isBorderBox, styles ) {
	var i = extra === ( isBorderBox ? "border" : "content" ) ?

		// If we already have the right measurement, avoid augmentation
		4 :

		// Otherwise initialize for horizontal or vertical properties
		name === "width" ? 1 : 0,

		val = 0;

	for ( ; i < 4; i += 2 ) {

		// both box models exclude margin, so add it if we want it
		if ( extra === "margin" ) {
			val += jQuery.css( elem, extra + cssExpand[ i ], true, styles );
		}

		if ( isBorderBox ) {

			// border-box includes padding, so remove it if we want content
			if ( extra === "content" ) {
				val -= jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );
			}

			// at this point, extra isn't border nor margin, so remove border
			if ( extra !== "margin" ) {
				val -= jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
			}
		} else {

			// at this point, extra isn't content, so add padding
			val += jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );

			// at this point, extra isn't content nor padding, so add border
			if ( extra !== "padding" ) {
				val += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
			}
		}
	}

	return val;
}

function getWidthOrHeight( elem, name, extra ) {

	// Start with offset property, which is equivalent to the border-box value
	var valueIsBorderBox = true,
		val = name === "width" ? elem.offsetWidth : elem.offsetHeight,
		styles = getStyles( elem ),
		isBorderBox = support.boxSizing &&
			jQuery.css( elem, "boxSizing", false, styles ) === "border-box";

	// some non-html elements return undefined for offsetWidth, so check for null/undefined
	// svg - https://bugzilla.mozilla.org/show_bug.cgi?id=649285
	// MathML - https://bugzilla.mozilla.org/show_bug.cgi?id=491668
	if ( val <= 0 || val == null ) {

		// Fall back to computed then uncomputed css if necessary
		val = curCSS( elem, name, styles );
		if ( val < 0 || val == null ) {
			val = elem.style[ name ];
		}

		// Computed unit is not pixels. Stop here and return.
		if ( rnumnonpx.test( val ) ) {
			return val;
		}

		// we need the check for style in case a browser which returns unreliable values
		// for getComputedStyle silently falls back to the reliable elem.style
		valueIsBorderBox = isBorderBox &&
			( support.boxSizingReliable() || val === elem.style[ name ] );

		// Normalize "", auto, and prepare for extra
		val = parseFloat( val ) || 0;
	}

	// use the active box-sizing model to add/subtract irrelevant styles
	return ( val +
		augmentWidthOrHeight(
			elem,
			name,
			extra || ( isBorderBox ? "border" : "content" ),
			valueIsBorderBox,
			styles
		)
	) + "px";
}

jQuery.extend( {

	// Add in style property hooks for overriding the default
	// behavior of getting and setting a style property
	cssHooks: {
		opacity: {
			get: function( elem, computed ) {
				if ( computed ) {

					// We should always get a number back from opacity
					var ret = curCSS( elem, "opacity" );
					return ret === "" ? "1" : ret;
				}
			}
		}
	},

	// Don't automatically add "px" to these possibly-unitless properties
	cssNumber: {
		"animationIterationCount": true,
		"columnCount": true,
		"fillOpacity": true,
		"flexGrow": true,
		"flexShrink": true,
		"fontWeight": true,
		"lineHeight": true,
		"opacity": true,
		"order": true,
		"orphans": true,
		"widows": true,
		"zIndex": true,
		"zoom": true
	},

	// Add in properties whose names you wish to fix before
	// setting or getting the value
	cssProps: {

		// normalize float css property
		"float": support.cssFloat ? "cssFloat" : "styleFloat"
	},

	// Get and set the style property on a DOM Node
	style: function( elem, name, value, extra ) {

		// Don't set styles on text and comment nodes
		if ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {
			return;
		}

		// Make sure that we're working with the right name
		var ret, type, hooks,
			origName = jQuery.camelCase( name ),
			style = elem.style;

		name = jQuery.cssProps[ origName ] ||
			( jQuery.cssProps[ origName ] = vendorPropName( origName ) || origName );

		// gets hook for the prefixed version
		// followed by the unprefixed version
		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

		// Check if we're setting a value
		if ( value !== undefined ) {
			type = typeof value;

			// Convert "+=" or "-=" to relative numbers (#7345)
			if ( type === "string" && ( ret = rcssNum.exec( value ) ) && ret[ 1 ] ) {
				value = adjustCSS( elem, name, ret );

				// Fixes bug #9237
				type = "number";
			}

			// Make sure that null and NaN values aren't set. See: #7116
			if ( value == null || value !== value ) {
				return;
			}

			// If a number was passed in, add the unit (except for certain CSS properties)
			if ( type === "number" ) {
				value += ret && ret[ 3 ] || ( jQuery.cssNumber[ origName ] ? "" : "px" );
			}

			// Fixes #8908, it can be done more correctly by specifing setters in cssHooks,
			// but it would mean to define eight
			// (for every problematic property) identical functions
			if ( !support.clearCloneStyle && value === "" && name.indexOf( "background" ) === 0 ) {
				style[ name ] = "inherit";
			}

			// If a hook was provided, use that value, otherwise just set the specified value
			if ( !hooks || !( "set" in hooks ) ||
				( value = hooks.set( elem, value, extra ) ) !== undefined ) {

				// Support: IE
				// Swallow errors from 'invalid' CSS values (#5509)
				try {
					style[ name ] = value;
				} catch ( e ) {}
			}

		} else {

			// If a hook was provided get the non-computed value from there
			if ( hooks && "get" in hooks &&
				( ret = hooks.get( elem, false, extra ) ) !== undefined ) {

				return ret;
			}

			// Otherwise just get the value from the style object
			return style[ name ];
		}
	},

	css: function( elem, name, extra, styles ) {
		var num, val, hooks,
			origName = jQuery.camelCase( name );

		// Make sure that we're working with the right name
		name = jQuery.cssProps[ origName ] ||
			( jQuery.cssProps[ origName ] = vendorPropName( origName ) || origName );

		// gets hook for the prefixed version
		// followed by the unprefixed version
		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

		// If a hook was provided get the computed value from there
		if ( hooks && "get" in hooks ) {
			val = hooks.get( elem, true, extra );
		}

		// Otherwise, if a way to get the computed value exists, use that
		if ( val === undefined ) {
			val = curCSS( elem, name, styles );
		}

		//convert "normal" to computed value
		if ( val === "normal" && name in cssNormalTransform ) {
			val = cssNormalTransform[ name ];
		}

		// Return, converting to number if forced or a qualifier was provided and val looks numeric
		if ( extra === "" || extra ) {
			num = parseFloat( val );
			return extra === true || isFinite( num ) ? num || 0 : val;
		}
		return val;
	}
} );

jQuery.each( [ "height", "width" ], function( i, name ) {
	jQuery.cssHooks[ name ] = {
		get: function( elem, computed, extra ) {
			if ( computed ) {

				// certain elements can have dimension info if we invisibly show them
				// however, it must have a current display style that would benefit from this
				return rdisplayswap.test( jQuery.css( elem, "display" ) ) &&
					elem.offsetWidth === 0 ?
						swap( elem, cssShow, function() {
							return getWidthOrHeight( elem, name, extra );
						} ) :
						getWidthOrHeight( elem, name, extra );
			}
		},

		set: function( elem, value, extra ) {
			var styles = extra && getStyles( elem );
			return setPositiveNumber( elem, value, extra ?
				augmentWidthOrHeight(
					elem,
					name,
					extra,
					support.boxSizing &&
						jQuery.css( elem, "boxSizing", false, styles ) === "border-box",
					styles
				) : 0
			);
		}
	};
} );

if ( !support.opacity ) {
	jQuery.cssHooks.opacity = {
		get: function( elem, computed ) {

			// IE uses filters for opacity
			return ropacity.test( ( computed && elem.currentStyle ?
				elem.currentStyle.filter :
				elem.style.filter ) || "" ) ?
					( 0.01 * parseFloat( RegExp.$1 ) ) + "" :
					computed ? "1" : "";
		},

		set: function( elem, value ) {
			var style = elem.style,
				currentStyle = elem.currentStyle,
				opacity = jQuery.isNumeric( value ) ? "alpha(opacity=" + value * 100 + ")" : "",
				filter = currentStyle && currentStyle.filter || style.filter || "";

			// IE has trouble with opacity if it does not have layout
			// Force it by setting the zoom level
			style.zoom = 1;

			// if setting opacity to 1, and no other filters exist -
			// attempt to remove filter attribute #6652
			// if value === "", then remove inline opacity #12685
			if ( ( value >= 1 || value === "" ) &&
					jQuery.trim( filter.replace( ralpha, "" ) ) === "" &&
					style.removeAttribute ) {

				// Setting style.filter to null, "" & " " still leave "filter:" in the cssText
				// if "filter:" is present at all, clearType is disabled, we want to avoid this
				// style.removeAttribute is IE Only, but so apparently is this code path...
				style.removeAttribute( "filter" );

				// if there is no filter style applied in a css rule
				// or unset inline opacity, we are done
				if ( value === "" || currentStyle && !currentStyle.filter ) {
					return;
				}
			}

			// otherwise, set new filter values
			style.filter = ralpha.test( filter ) ?
				filter.replace( ralpha, opacity ) :
				filter + " " + opacity;
		}
	};
}

jQuery.cssHooks.marginRight = addGetHookIf( support.reliableMarginRight,
	function( elem, computed ) {
		if ( computed ) {
			return swap( elem, { "display": "inline-block" },
				curCSS, [ elem, "marginRight" ] );
		}
	}
);

jQuery.cssHooks.marginLeft = addGetHookIf( support.reliableMarginLeft,
	function( elem, computed ) {
		if ( computed ) {
			return (
				parseFloat( curCSS( elem, "marginLeft" ) ) ||

				// Support: IE<=11+
				// Running getBoundingClientRect on a disconnected node in IE throws an error
				// Support: IE8 only
				// getClientRects() errors on disconnected elems
				( jQuery.contains( elem.ownerDocument, elem ) ?
					elem.getBoundingClientRect().left -
						swap( elem, { marginLeft: 0 }, function() {
							return elem.getBoundingClientRect().left;
						} ) :
					0
				)
			) + "px";
		}
	}
);

// These hooks are used by animate to expand properties
jQuery.each( {
	margin: "",
	padding: "",
	border: "Width"
}, function( prefix, suffix ) {
	jQuery.cssHooks[ prefix + suffix ] = {
		expand: function( value ) {
			var i = 0,
				expanded = {},

				// assumes a single number if not a string
				parts = typeof value === "string" ? value.split( " " ) : [ value ];

			for ( ; i < 4; i++ ) {
				expanded[ prefix + cssExpand[ i ] + suffix ] =
					parts[ i ] || parts[ i - 2 ] || parts[ 0 ];
			}

			return expanded;
		}
	};

	if ( !rmargin.test( prefix ) ) {
		jQuery.cssHooks[ prefix + suffix ].set = setPositiveNumber;
	}
} );

jQuery.fn.extend( {
	css: function( name, value ) {
		return access( this, function( elem, name, value ) {
			var styles, len,
				map = {},
				i = 0;

			if ( jQuery.isArray( name ) ) {
				styles = getStyles( elem );
				len = name.length;

				for ( ; i < len; i++ ) {
					map[ name[ i ] ] = jQuery.css( elem, name[ i ], false, styles );
				}

				return map;
			}

			return value !== undefined ?
				jQuery.style( elem, name, value ) :
				jQuery.css( elem, name );
		}, name, value, arguments.length > 1 );
	},
	show: function() {
		return showHide( this, true );
	},
	hide: function() {
		return showHide( this );
	},
	toggle: function( state ) {
		if ( typeof state === "boolean" ) {
			return state ? this.show() : this.hide();
		}

		return this.each( function() {
			if ( isHidden( this ) ) {
				jQuery( this ).show();
			} else {
				jQuery( this ).hide();
			}
		} );
	}
} );


function Tween( elem, options, prop, end, easing ) {
	return new Tween.prototype.init( elem, options, prop, end, easing );
}
jQuery.Tween = Tween;

Tween.prototype = {
	constructor: Tween,
	init: function( elem, options, prop, end, easing, unit ) {
		this.elem = elem;
		this.prop = prop;
		this.easing = easing || jQuery.easing._default;
		this.options = options;
		this.start = this.now = this.cur();
		this.end = end;
		this.unit = unit || ( jQuery.cssNumber[ prop ] ? "" : "px" );
	},
	cur: function() {
		var hooks = Tween.propHooks[ this.prop ];

		return hooks && hooks.get ?
			hooks.get( this ) :
			Tween.propHooks._default.get( this );
	},
	run: function( percent ) {
		var eased,
			hooks = Tween.propHooks[ this.prop ];

		if ( this.options.duration ) {
			this.pos = eased = jQuery.easing[ this.easing ](
				percent, this.options.duration * percent, 0, 1, this.options.duration
			);
		} else {
			this.pos = eased = percent;
		}
		this.now = ( this.end - this.start ) * eased + this.start;

		if ( this.options.step ) {
			this.options.step.call( this.elem, this.now, this );
		}

		if ( hooks && hooks.set ) {
			hooks.set( this );
		} else {
			Tween.propHooks._default.set( this );
		}
		return this;
	}
};

Tween.prototype.init.prototype = Tween.prototype;

Tween.propHooks = {
	_default: {
		get: function( tween ) {
			var result;

			// Use a property on the element directly when it is not a DOM element,
			// or when there is no matching style property that exists.
			if ( tween.elem.nodeType !== 1 ||
				tween.elem[ tween.prop ] != null && tween.elem.style[ tween.prop ] == null ) {
				return tween.elem[ tween.prop ];
			}

			// passing an empty string as a 3rd parameter to .css will automatically
			// attempt a parseFloat and fallback to a string if the parse fails
			// so, simple values such as "10px" are parsed to Float.
			// complex values such as "rotate(1rad)" are returned as is.
			result = jQuery.css( tween.elem, tween.prop, "" );

			// Empty strings, null, undefined and "auto" are converted to 0.
			return !result || result === "auto" ? 0 : result;
		},
		set: function( tween ) {

			// use step hook for back compat - use cssHook if its there - use .style if its
			// available and use plain properties where available
			if ( jQuery.fx.step[ tween.prop ] ) {
				jQuery.fx.step[ tween.prop ]( tween );
			} else if ( tween.elem.nodeType === 1 &&
				( tween.elem.style[ jQuery.cssProps[ tween.prop ] ] != null ||
					jQuery.cssHooks[ tween.prop ] ) ) {
				jQuery.style( tween.elem, tween.prop, tween.now + tween.unit );
			} else {
				tween.elem[ tween.prop ] = tween.now;
			}
		}
	}
};

// Support: IE <=9
// Panic based approach to setting things on disconnected nodes

Tween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {
	set: function( tween ) {
		if ( tween.elem.nodeType && tween.elem.parentNode ) {
			tween.elem[ tween.prop ] = tween.now;
		}
	}
};

jQuery.easing = {
	linear: function( p ) {
		return p;
	},
	swing: function( p ) {
		return 0.5 - Math.cos( p * Math.PI ) / 2;
	},
	_default: "swing"
};

jQuery.fx = Tween.prototype.init;

// Back Compat <1.8 extension point
jQuery.fx.step = {};




var
	fxNow, timerId,
	rfxtypes = /^(?:toggle|show|hide)$/,
	rrun = /queueHooks$/;

// Animations created synchronously will run synchronously
function createFxNow() {
	window.setTimeout( function() {
		fxNow = undefined;
	} );
	return ( fxNow = jQuery.now() );
}

// Generate parameters to create a standard animation
function genFx( type, includeWidth ) {
	var which,
		attrs = { height: type },
		i = 0;

	// if we include width, step value is 1 to do all cssExpand values,
	// if we don't include width, step value is 2 to skip over Left and Right
	includeWidth = includeWidth ? 1 : 0;
	for ( ; i < 4 ; i += 2 - includeWidth ) {
		which = cssExpand[ i ];
		attrs[ "margin" + which ] = attrs[ "padding" + which ] = type;
	}

	if ( includeWidth ) {
		attrs.opacity = attrs.width = type;
	}

	return attrs;
}

function createTween( value, prop, animation ) {
	var tween,
		collection = ( Animation.tweeners[ prop ] || [] ).concat( Animation.tweeners[ "*" ] ),
		index = 0,
		length = collection.length;
	for ( ; index < length; index++ ) {
		if ( ( tween = collection[ index ].call( animation, prop, value ) ) ) {

			// we're done with this property
			return tween;
		}
	}
}

function defaultPrefilter( elem, props, opts ) {
	/* jshint validthis: true */
	var prop, value, toggle, tween, hooks, oldfire, display, checkDisplay,
		anim = this,
		orig = {},
		style = elem.style,
		hidden = elem.nodeType && isHidden( elem ),
		dataShow = jQuery._data( elem, "fxshow" );

	// handle queue: false promises
	if ( !opts.queue ) {
		hooks = jQuery._queueHooks( elem, "fx" );
		if ( hooks.unqueued == null ) {
			hooks.unqueued = 0;
			oldfire = hooks.empty.fire;
			hooks.empty.fire = function() {
				if ( !hooks.unqueued ) {
					oldfire();
				}
			};
		}
		hooks.unqueued++;

		anim.always( function() {

			// doing this makes sure that the complete handler will be called
			// before this completes
			anim.always( function() {
				hooks.unqueued--;
				if ( !jQuery.queue( elem, "fx" ).length ) {
					hooks.empty.fire();
				}
			} );
		} );
	}

	// height/width overflow pass
	if ( elem.nodeType === 1 && ( "height" in props || "width" in props ) ) {

		// Make sure that nothing sneaks out
		// Record all 3 overflow attributes because IE does not
		// change the overflow attribute when overflowX and
		// overflowY are set to the same value
		opts.overflow = [ style.overflow, style.overflowX, style.overflowY ];

		// Set display property to inline-block for height/width
		// animations on inline elements that are having width/height animated
		display = jQuery.css( elem, "display" );

		// Test default display if display is currently "none"
		checkDisplay = display === "none" ?
			jQuery._data( elem, "olddisplay" ) || defaultDisplay( elem.nodeName ) : display;

		if ( checkDisplay === "inline" && jQuery.css( elem, "float" ) === "none" ) {

			// inline-level elements accept inline-block;
			// block-level elements need to be inline with layout
			if ( !support.inlineBlockNeedsLayout || defaultDisplay( elem.nodeName ) === "inline" ) {
				style.display = "inline-block";
			} else {
				style.zoom = 1;
			}
		}
	}

	if ( opts.overflow ) {
		style.overflow = "hidden";
		if ( !support.shrinkWrapBlocks() ) {
			anim.always( function() {
				style.overflow = opts.overflow[ 0 ];
				style.overflowX = opts.overflow[ 1 ];
				style.overflowY = opts.overflow[ 2 ];
			} );
		}
	}

	// show/hide pass
	for ( prop in props ) {
		value = props[ prop ];
		if ( rfxtypes.exec( value ) ) {
			delete props[ prop ];
			toggle = toggle || value === "toggle";
			if ( value === ( hidden ? "hide" : "show" ) ) {

				// If there is dataShow left over from a stopped hide or show
				// and we are going to proceed with show, we should pretend to be hidden
				if ( value === "show" && dataShow && dataShow[ prop ] !== undefined ) {
					hidden = true;
				} else {
					continue;
				}
			}
			orig[ prop ] = dataShow && dataShow[ prop ] || jQuery.style( elem, prop );

		// Any non-fx value stops us from restoring the original display value
		} else {
			display = undefined;
		}
	}

	if ( !jQuery.isEmptyObject( orig ) ) {
		if ( dataShow ) {
			if ( "hidden" in dataShow ) {
				hidden = dataShow.hidden;
			}
		} else {
			dataShow = jQuery._data( elem, "fxshow", {} );
		}

		// store state if its toggle - enables .stop().toggle() to "reverse"
		if ( toggle ) {
			dataShow.hidden = !hidden;
		}
		if ( hidden ) {
			jQuery( elem ).show();
		} else {
			anim.done( function() {
				jQuery( elem ).hide();
			} );
		}
		anim.done( function() {
			var prop;
			jQuery._removeData( elem, "fxshow" );
			for ( prop in orig ) {
				jQuery.style( elem, prop, orig[ prop ] );
			}
		} );
		for ( prop in orig ) {
			tween = createTween( hidden ? dataShow[ prop ] : 0, prop, anim );

			if ( !( prop in dataShow ) ) {
				dataShow[ prop ] = tween.start;
				if ( hidden ) {
					tween.end = tween.start;
					tween.start = prop === "width" || prop === "height" ? 1 : 0;
				}
			}
		}

	// If this is a noop like .hide().hide(), restore an overwritten display value
	} else if ( ( display === "none" ? defaultDisplay( elem.nodeName ) : display ) === "inline" ) {
		style.display = display;
	}
}

function propFilter( props, specialEasing ) {
	var index, name, easing, value, hooks;

	// camelCase, specialEasing and expand cssHook pass
	for ( index in props ) {
		name = jQuery.camelCase( index );
		easing = specialEasing[ name ];
		value = props[ index ];
		if ( jQuery.isArray( value ) ) {
			easing = value[ 1 ];
			value = props[ index ] = value[ 0 ];
		}

		if ( index !== name ) {
			props[ name ] = value;
			delete props[ index ];
		}

		hooks = jQuery.cssHooks[ name ];
		if ( hooks && "expand" in hooks ) {
			value = hooks.expand( value );
			delete props[ name ];

			// not quite $.extend, this wont overwrite keys already present.
			// also - reusing 'index' from above because we have the correct "name"
			for ( index in value ) {
				if ( !( index in props ) ) {
					props[ index ] = value[ index ];
					specialEasing[ index ] = easing;
				}
			}
		} else {
			specialEasing[ name ] = easing;
		}
	}
}

function Animation( elem, properties, options ) {
	var result,
		stopped,
		index = 0,
		length = Animation.prefilters.length,
		deferred = jQuery.Deferred().always( function() {

			// don't match elem in the :animated selector
			delete tick.elem;
		} ),
		tick = function() {
			if ( stopped ) {
				return false;
			}
			var currentTime = fxNow || createFxNow(),
				remaining = Math.max( 0, animation.startTime + animation.duration - currentTime ),

				// Support: Android 2.3
				// Archaic crash bug won't allow us to use `1 - ( 0.5 || 0 )` (#12497)
				temp = remaining / animation.duration || 0,
				percent = 1 - temp,
				index = 0,
				length = animation.tweens.length;

			for ( ; index < length ; index++ ) {
				animation.tweens[ index ].run( percent );
			}

			deferred.notifyWith( elem, [ animation, percent, remaining ] );

			if ( percent < 1 && length ) {
				return remaining;
			} else {
				deferred.resolveWith( elem, [ animation ] );
				return false;
			}
		},
		animation = deferred.promise( {
			elem: elem,
			props: jQuery.extend( {}, properties ),
			opts: jQuery.extend( true, {
				specialEasing: {},
				easing: jQuery.easing._default
			}, options ),
			originalProperties: properties,
			originalOptions: options,
			startTime: fxNow || createFxNow(),
			duration: options.duration,
			tweens: [],
			createTween: function( prop, end ) {
				var tween = jQuery.Tween( elem, animation.opts, prop, end,
						animation.opts.specialEasing[ prop ] || animation.opts.easing );
				animation.tweens.push( tween );
				return tween;
			},
			stop: function( gotoEnd ) {
				var index = 0,

					// if we are going to the end, we want to run all the tweens
					// otherwise we skip this part
					length = gotoEnd ? animation.tweens.length : 0;
				if ( stopped ) {
					return this;
				}
				stopped = true;
				for ( ; index < length ; index++ ) {
					animation.tweens[ index ].run( 1 );
				}

				// resolve when we played the last frame
				// otherwise, reject
				if ( gotoEnd ) {
					deferred.notifyWith( elem, [ animation, 1, 0 ] );
					deferred.resolveWith( elem, [ animation, gotoEnd ] );
				} else {
					deferred.rejectWith( elem, [ animation, gotoEnd ] );
				}
				return this;
			}
		} ),
		props = animation.props;

	propFilter( props, animation.opts.specialEasing );

	for ( ; index < length ; index++ ) {
		result = Animation.prefilters[ index ].call( animation, elem, props, animation.opts );
		if ( result ) {
			if ( jQuery.isFunction( result.stop ) ) {
				jQuery._queueHooks( animation.elem, animation.opts.queue ).stop =
					jQuery.proxy( result.stop, result );
			}
			return result;
		}
	}

	jQuery.map( props, createTween, animation );

	if ( jQuery.isFunction( animation.opts.start ) ) {
		animation.opts.start.call( elem, animation );
	}

	jQuery.fx.timer(
		jQuery.extend( tick, {
			elem: elem,
			anim: animation,
			queue: animation.opts.queue
		} )
	);

	// attach callbacks from options
	return animation.progress( animation.opts.progress )
		.done( animation.opts.done, animation.opts.complete )
		.fail( animation.opts.fail )
		.always( animation.opts.always );
}

jQuery.Animation = jQuery.extend( Animation, {

	tweeners: {
		"*": [ function( prop, value ) {
			var tween = this.createTween( prop, value );
			adjustCSS( tween.elem, prop, rcssNum.exec( value ), tween );
			return tween;
		} ]
	},

	tweener: function( props, callback ) {
		if ( jQuery.isFunction( props ) ) {
			callback = props;
			props = [ "*" ];
		} else {
			props = props.match( rnotwhite );
		}

		var prop,
			index = 0,
			length = props.length;

		for ( ; index < length ; index++ ) {
			prop = props[ index ];
			Animation.tweeners[ prop ] = Animation.tweeners[ prop ] || [];
			Animation.tweeners[ prop ].unshift( callback );
		}
	},

	prefilters: [ defaultPrefilter ],

	prefilter: function( callback, prepend ) {
		if ( prepend ) {
			Animation.prefilters.unshift( callback );
		} else {
			Animation.prefilters.push( callback );
		}
	}
} );

jQuery.speed = function( speed, easing, fn ) {
	var opt = speed && typeof speed === "object" ? jQuery.extend( {}, speed ) : {
		complete: fn || !fn && easing ||
			jQuery.isFunction( speed ) && speed,
		duration: speed,
		easing: fn && easing || easing && !jQuery.isFunction( easing ) && easing
	};

	opt.duration = jQuery.fx.off ? 0 : typeof opt.duration === "number" ? opt.duration :
		opt.duration in jQuery.fx.speeds ?
			jQuery.fx.speeds[ opt.duration ] : jQuery.fx.speeds._default;

	// normalize opt.queue - true/undefined/null -> "fx"
	if ( opt.queue == null || opt.queue === true ) {
		opt.queue = "fx";
	}

	// Queueing
	opt.old = opt.complete;

	opt.complete = function() {
		if ( jQuery.isFunction( opt.old ) ) {
			opt.old.call( this );
		}

		if ( opt.queue ) {
			jQuery.dequeue( this, opt.queue );
		}
	};

	return opt;
};

jQuery.fn.extend( {
	fadeTo: function( speed, to, easing, callback ) {

		// show any hidden elements after setting opacity to 0
		return this.filter( isHidden ).css( "opacity", 0 ).show()

			// animate to the value specified
			.end().animate( { opacity: to }, speed, easing, callback );
	},
	animate: function( prop, speed, easing, callback ) {
		var empty = jQuery.isEmptyObject( prop ),
			optall = jQuery.speed( speed, easing, callback ),
			doAnimation = function() {

				// Operate on a copy of prop so per-property easing won't be lost
				var anim = Animation( this, jQuery.extend( {}, prop ), optall );

				// Empty animations, or finishing resolves immediately
				if ( empty || jQuery._data( this, "finish" ) ) {
					anim.stop( true );
				}
			};
			doAnimation.finish = doAnimation;

		return empty || optall.queue === false ?
			this.each( doAnimation ) :
			this.queue( optall.queue, doAnimation );
	},
	stop: function( type, clearQueue, gotoEnd ) {
		var stopQueue = function( hooks ) {
			var stop = hooks.stop;
			delete hooks.stop;
			stop( gotoEnd );
		};

		if ( typeof type !== "string" ) {
			gotoEnd = clearQueue;
			clearQueue = type;
			type = undefined;
		}
		if ( clearQueue && type !== false ) {
			this.queue( type || "fx", [] );
		}

		return this.each( function() {
			var dequeue = true,
				index = type != null && type + "queueHooks",
				timers = jQuery.timers,
				data = jQuery._data( this );

			if ( index ) {
				if ( data[ index ] && data[ index ].stop ) {
					stopQueue( data[ index ] );
				}
			} else {
				for ( index in data ) {
					if ( data[ index ] && data[ index ].stop && rrun.test( index ) ) {
						stopQueue( data[ index ] );
					}
				}
			}

			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this &&
					( type == null || timers[ index ].queue === type ) ) {

					timers[ index ].anim.stop( gotoEnd );
					dequeue = false;
					timers.splice( index, 1 );
				}
			}

			// start the next in the queue if the last step wasn't forced
			// timers currently will call their complete callbacks, which will dequeue
			// but only if they were gotoEnd
			if ( dequeue || !gotoEnd ) {
				jQuery.dequeue( this, type );
			}
		} );
	},
	finish: function( type ) {
		if ( type !== false ) {
			type = type || "fx";
		}
		return this.each( function() {
			var index,
				data = jQuery._data( this ),
				queue = data[ type + "queue" ],
				hooks = data[ type + "queueHooks" ],
				timers = jQuery.timers,
				length = queue ? queue.length : 0;

			// enable finishing flag on private data
			data.finish = true;

			// empty the queue first
			jQuery.queue( this, type, [] );

			if ( hooks && hooks.stop ) {
				hooks.stop.call( this, true );
			}

			// look for any active animations, and finish them
			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this && timers[ index ].queue === type ) {
					timers[ index ].anim.stop( true );
					timers.splice( index, 1 );
				}
			}

			// look for any animations in the old queue and finish them
			for ( index = 0; index < length; index++ ) {
				if ( queue[ index ] && queue[ index ].finish ) {
					queue[ index ].finish.call( this );
				}
			}

			// turn off finishing flag
			delete data.finish;
		} );
	}
} );

jQuery.each( [ "toggle", "show", "hide" ], function( i, name ) {
	var cssFn = jQuery.fn[ name ];
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return speed == null || typeof speed === "boolean" ?
			cssFn.apply( this, arguments ) :
			this.animate( genFx( name, true ), speed, easing, callback );
	};
} );

// Generate shortcuts for custom animations
jQuery.each( {
	slideDown: genFx( "show" ),
	slideUp: genFx( "hide" ),
	slideToggle: genFx( "toggle" ),
	fadeIn: { opacity: "show" },
	fadeOut: { opacity: "hide" },
	fadeToggle: { opacity: "toggle" }
}, function( name, props ) {
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return this.animate( props, speed, easing, callback );
	};
} );

jQuery.timers = [];
jQuery.fx.tick = function() {
	var timer,
		timers = jQuery.timers,
		i = 0;

	fxNow = jQuery.now();

	for ( ; i < timers.length; i++ ) {
		timer = timers[ i ];

		// Checks the timer has not already been removed
		if ( !timer() && timers[ i ] === timer ) {
			timers.splice( i--, 1 );
		}
	}

	if ( !timers.length ) {
		jQuery.fx.stop();
	}
	fxNow = undefined;
};

jQuery.fx.timer = function( timer ) {
	jQuery.timers.push( timer );
	if ( timer() ) {
		jQuery.fx.start();
	} else {
		jQuery.timers.pop();
	}
};

jQuery.fx.interval = 13;

jQuery.fx.start = function() {
	if ( !timerId ) {
		timerId = window.setInterval( jQuery.fx.tick, jQuery.fx.interval );
	}
};

jQuery.fx.stop = function() {
	window.clearInterval( timerId );
	timerId = null;
};

jQuery.fx.speeds = {
	slow: 600,
	fast: 200,

	// Default speed
	_default: 400
};


// Based off of the plugin by Clint Helfers, with permission.
// http://web.archive.org/web/20100324014747/http://blindsignals.com/index.php/2009/07/jquery-delay/
jQuery.fn.delay = function( time, type ) {
	time = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;
	type = type || "fx";

	return this.queue( type, function( next, hooks ) {
		var timeout = window.setTimeout( next, time );
		hooks.stop = function() {
			window.clearTimeout( timeout );
		};
	} );
};


( function() {
	var a,
		input = document.createElement( "input" ),
		div = document.createElement( "div" ),
		select = document.createElement( "select" ),
		opt = select.appendChild( document.createElement( "option" ) );

	// Setup
	div = document.createElement( "div" );
	div.setAttribute( "className", "t" );
	div.innerHTML = "  <link/><table></table><a href='/a'>a</a><input type='checkbox'/>";
	a = div.getElementsByTagName( "a" )[ 0 ];

	// Support: Windows Web Apps (WWA)
	// `type` must use .setAttribute for WWA (#14901)
	input.setAttribute( "type", "checkbox" );
	div.appendChild( input );

	a = div.getElementsByTagName( "a" )[ 0 ];

	// First batch of tests.
	a.style.cssText = "top:1px";

	// Test setAttribute on camelCase class.
	// If it works, we need attrFixes when doing get/setAttribute (ie6/7)
	support.getSetAttribute = div.className !== "t";

	// Get the style information from getAttribute
	// (IE uses .cssText instead)
	support.style = /top/.test( a.getAttribute( "style" ) );

	// Make sure that URLs aren't manipulated
	// (IE normalizes it by default)
	support.hrefNormalized = a.getAttribute( "href" ) === "/a";

	// Check the default checkbox/radio value ("" on WebKit; "on" elsewhere)
	support.checkOn = !!input.value;

	// Make sure that a selected-by-default option has a working selected property.
	// (WebKit defaults to false instead of true, IE too, if it's in an optgroup)
	support.optSelected = opt.selected;

	// Tests for enctype support on a form (#6743)
	support.enctype = !!document.createElement( "form" ).enctype;

	// Make sure that the options inside disabled selects aren't marked as disabled
	// (WebKit marks them as disabled)
	select.disabled = true;
	support.optDisabled = !opt.disabled;

	// Support: IE8 only
	// Check if we can trust getAttribute("value")
	input = document.createElement( "input" );
	input.setAttribute( "value", "" );
	support.input = input.getAttribute( "value" ) === "";

	// Check if an input maintains its value after becoming a radio
	input.value = "t";
	input.setAttribute( "type", "radio" );
	support.radioValue = input.value === "t";
} )();


var rreturn = /\r/g,
	rspaces = /[\x20\t\r\n\f]+/g;

jQuery.fn.extend( {
	val: function( value ) {
		var hooks, ret, isFunction,
			elem = this[ 0 ];

		if ( !arguments.length ) {
			if ( elem ) {
				hooks = jQuery.valHooks[ elem.type ] ||
					jQuery.valHooks[ elem.nodeName.toLowerCase() ];

				if (
					hooks &&
					"get" in hooks &&
					( ret = hooks.get( elem, "value" ) ) !== undefined
				) {
					return ret;
				}

				ret = elem.value;

				return typeof ret === "string" ?

					// handle most common string cases
					ret.replace( rreturn, "" ) :

					// handle cases where value is null/undef or number
					ret == null ? "" : ret;
			}

			return;
		}

		isFunction = jQuery.isFunction( value );

		return this.each( function( i ) {
			var val;

			if ( this.nodeType !== 1 ) {
				return;
			}

			if ( isFunction ) {
				val = value.call( this, i, jQuery( this ).val() );
			} else {
				val = value;
			}

			// Treat null/undefined as ""; convert numbers to string
			if ( val == null ) {
				val = "";
			} else if ( typeof val === "number" ) {
				val += "";
			} else if ( jQuery.isArray( val ) ) {
				val = jQuery.map( val, function( value ) {
					return value == null ? "" : value + "";
				} );
			}

			hooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];

			// If set returns undefined, fall back to normal setting
			if ( !hooks || !( "set" in hooks ) || hooks.set( this, val, "value" ) === undefined ) {
				this.value = val;
			}
		} );
	}
} );

jQuery.extend( {
	valHooks: {
		option: {
			get: function( elem ) {
				var val = jQuery.find.attr( elem, "value" );
				return val != null ?
					val :

					// Support: IE10-11+
					// option.text throws exceptions (#14686, #14858)
					// Strip and collapse whitespace
					// https://html.spec.whatwg.org/#strip-and-collapse-whitespace
					jQuery.trim( jQuery.text( elem ) ).replace( rspaces, " " );
			}
		},
		select: {
			get: function( elem ) {
				var value, option,
					options = elem.options,
					index = elem.selectedIndex,
					one = elem.type === "select-one" || index < 0,
					values = one ? null : [],
					max = one ? index + 1 : options.length,
					i = index < 0 ?
						max :
						one ? index : 0;

				// Loop through all the selected options
				for ( ; i < max; i++ ) {
					option = options[ i ];

					// oldIE doesn't update selected after form reset (#2551)
					if ( ( option.selected || i === index ) &&

							// Don't return options that are disabled or in a disabled optgroup
							( support.optDisabled ?
								!option.disabled :
								option.getAttribute( "disabled" ) === null ) &&
							( !option.parentNode.disabled ||
								!jQuery.nodeName( option.parentNode, "optgroup" ) ) ) {

						// Get the specific value for the option
						value = jQuery( option ).val();

						// We don't need an array for one selects
						if ( one ) {
							return value;
						}

						// Multi-Selects return an array
						values.push( value );
					}
				}

				return values;
			},

			set: function( elem, value ) {
				var optionSet, option,
					options = elem.options,
					values = jQuery.makeArray( value ),
					i = options.length;

				while ( i-- ) {
					option = options[ i ];

					if ( jQuery.inArray( jQuery.valHooks.option.get( option ), values ) > -1 ) {

						// Support: IE6
						// When new option element is added to select box we need to
						// force reflow of newly added node in order to workaround delay
						// of initialization properties
						try {
							option.selected = optionSet = true;

						} catch ( _ ) {

							// Will be executed only in IE6
							option.scrollHeight;
						}

					} else {
						option.selected = false;
					}
				}

				// Force browsers to behave consistently when non-matching value is set
				if ( !optionSet ) {
					elem.selectedIndex = -1;
				}

				return options;
			}
		}
	}
} );

// Radios and checkboxes getter/setter
jQuery.each( [ "radio", "checkbox" ], function() {
	jQuery.valHooks[ this ] = {
		set: function( elem, value ) {
			if ( jQuery.isArray( value ) ) {
				return ( elem.checked = jQuery.inArray( jQuery( elem ).val(), value ) > -1 );
			}
		}
	};
	if ( !support.checkOn ) {
		jQuery.valHooks[ this ].get = function( elem ) {
			return elem.getAttribute( "value" ) === null ? "on" : elem.value;
		};
	}
} );




var nodeHook, boolHook,
	attrHandle = jQuery.expr.attrHandle,
	ruseDefault = /^(?:checked|selected)$/i,
	getSetAttribute = support.getSetAttribute,
	getSetInput = support.input;

jQuery.fn.extend( {
	attr: function( name, value ) {
		return access( this, jQuery.attr, name, value, arguments.length > 1 );
	},

	removeAttr: function( name ) {
		return this.each( function() {
			jQuery.removeAttr( this, name );
		} );
	}
} );

jQuery.extend( {
	attr: function( elem, name, value ) {
		var ret, hooks,
			nType = elem.nodeType;

		// Don't get/set attributes on text, comment and attribute nodes
		if ( nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		// Fallback to prop when attributes are not supported
		if ( typeof elem.getAttribute === "undefined" ) {
			return jQuery.prop( elem, name, value );
		}

		// All attributes are lowercase
		// Grab necessary hook if one is defined
		if ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {
			name = name.toLowerCase();
			hooks = jQuery.attrHooks[ name ] ||
				( jQuery.expr.match.bool.test( name ) ? boolHook : nodeHook );
		}

		if ( value !== undefined ) {
			if ( value === null ) {
				jQuery.removeAttr( elem, name );
				return;
			}

			if ( hooks && "set" in hooks &&
				( ret = hooks.set( elem, value, name ) ) !== undefined ) {
				return ret;
			}

			elem.setAttribute( name, value + "" );
			return value;
		}

		if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {
			return ret;
		}

		ret = jQuery.find.attr( elem, name );

		// Non-existent attributes return null, we normalize to undefined
		return ret == null ? undefined : ret;
	},

	attrHooks: {
		type: {
			set: function( elem, value ) {
				if ( !support.radioValue && value === "radio" &&
					jQuery.nodeName( elem, "input" ) ) {

					// Setting the type on a radio button after the value resets the value in IE8-9
					// Reset value to default in case type is set after value during creation
					var val = elem.value;
					elem.setAttribute( "type", value );
					if ( val ) {
						elem.value = val;
					}
					return value;
				}
			}
		}
	},

	removeAttr: function( elem, value ) {
		var name, propName,
			i = 0,
			attrNames = value && value.match( rnotwhite );

		if ( attrNames && elem.nodeType === 1 ) {
			while ( ( name = attrNames[ i++ ] ) ) {
				propName = jQuery.propFix[ name ] || name;

				// Boolean attributes get special treatment (#10870)
				if ( jQuery.expr.match.bool.test( name ) ) {

					// Set corresponding property to false
					if ( getSetInput && getSetAttribute || !ruseDefault.test( name ) ) {
						elem[ propName ] = false;

					// Support: IE<9
					// Also clear defaultChecked/defaultSelected (if appropriate)
					} else {
						elem[ jQuery.camelCase( "default-" + name ) ] =
							elem[ propName ] = false;
					}

				// See #9699 for explanation of this approach (setting first, then removal)
				} else {
					jQuery.attr( elem, name, "" );
				}

				elem.removeAttribute( getSetAttribute ? name : propName );
			}
		}
	}
} );

// Hooks for boolean attributes
boolHook = {
	set: function( elem, value, name ) {
		if ( value === false ) {

			// Remove boolean attributes when set to false
			jQuery.removeAttr( elem, name );
		} else if ( getSetInput && getSetAttribute || !ruseDefault.test( name ) ) {

			// IE<8 needs the *property* name
			elem.setAttribute( !getSetAttribute && jQuery.propFix[ name ] || name, name );

		} else {

			// Support: IE<9
			// Use defaultChecked and defaultSelected for oldIE
			elem[ jQuery.camelCase( "default-" + name ) ] = elem[ name ] = true;
		}
		return name;
	}
};

jQuery.each( jQuery.expr.match.bool.source.match( /\w+/g ), function( i, name ) {
	var getter = attrHandle[ name ] || jQuery.find.attr;

	if ( getSetInput && getSetAttribute || !ruseDefault.test( name ) ) {
		attrHandle[ name ] = function( elem, name, isXML ) {
			var ret, handle;
			if ( !isXML ) {

				// Avoid an infinite loop by temporarily removing this function from the getter
				handle = attrHandle[ name ];
				attrHandle[ name ] = ret;
				ret = getter( elem, name, isXML ) != null ?
					name.toLowerCase() :
					null;
				attrHandle[ name ] = handle;
			}
			return ret;
		};
	} else {
		attrHandle[ name ] = function( elem, name, isXML ) {
			if ( !isXML ) {
				return elem[ jQuery.camelCase( "default-" + name ) ] ?
					name.toLowerCase() :
					null;
			}
		};
	}
} );

// fix oldIE attroperties
if ( !getSetInput || !getSetAttribute ) {
	jQuery.attrHooks.value = {
		set: function( elem, value, name ) {
			if ( jQuery.nodeName( elem, "input" ) ) {

				// Does not return so that setAttribute is also used
				elem.defaultValue = value;
			} else {

				// Use nodeHook if defined (#1954); otherwise setAttribute is fine
				return nodeHook && nodeHook.set( elem, value, name );
			}
		}
	};
}

// IE6/7 do not support getting/setting some attributes with get/setAttribute
if ( !getSetAttribute ) {

	// Use this for any attribute in IE6/7
	// This fixes almost every IE6/7 issue
	nodeHook = {
		set: function( elem, value, name ) {

			// Set the existing or create a new attribute node
			var ret = elem.getAttributeNode( name );
			if ( !ret ) {
				elem.setAttributeNode(
					( ret = elem.ownerDocument.createAttribute( name ) )
				);
			}

			ret.value = value += "";

			// Break association with cloned elements by also using setAttribute (#9646)
			if ( name === "value" || value === elem.getAttribute( name ) ) {
				return value;
			}
		}
	};

	// Some attributes are constructed with empty-string values when not defined
	attrHandle.id = attrHandle.name = attrHandle.coords =
		function( elem, name, isXML ) {
			var ret;
			if ( !isXML ) {
				return ( ret = elem.getAttributeNode( name ) ) && ret.value !== "" ?
					ret.value :
					null;
			}
		};

	// Fixing value retrieval on a button requires this module
	jQuery.valHooks.button = {
		get: function( elem, name ) {
			var ret = elem.getAttributeNode( name );
			if ( ret && ret.specified ) {
				return ret.value;
			}
		},
		set: nodeHook.set
	};

	// Set contenteditable to false on removals(#10429)
	// Setting to empty string throws an error as an invalid value
	jQuery.attrHooks.contenteditable = {
		set: function( elem, value, name ) {
			nodeHook.set( elem, value === "" ? false : value, name );
		}
	};

	// Set width and height to auto instead of 0 on empty string( Bug #8150 )
	// This is for removals
	jQuery.each( [ "width", "height" ], function( i, name ) {
		jQuery.attrHooks[ name ] = {
			set: function( elem, value ) {
				if ( value === "" ) {
					elem.setAttribute( name, "auto" );
					return value;
				}
			}
		};
	} );
}

if ( !support.style ) {
	jQuery.attrHooks.style = {
		get: function( elem ) {

			// Return undefined in the case of empty string
			// Note: IE uppercases css property names, but if we were to .toLowerCase()
			// .cssText, that would destroy case sensitivity in URL's, like in "background"
			return elem.style.cssText || undefined;
		},
		set: function( elem, value ) {
			return ( elem.style.cssText = value + "" );
		}
	};
}




var rfocusable = /^(?:input|select|textarea|button|object)$/i,
	rclickable = /^(?:a|area)$/i;

jQuery.fn.extend( {
	prop: function( name, value ) {
		return access( this, jQuery.prop, name, value, arguments.length > 1 );
	},

	removeProp: function( name ) {
		name = jQuery.propFix[ name ] || name;
		return this.each( function() {

			// try/catch handles cases where IE balks (such as removing a property on window)
			try {
				this[ name ] = undefined;
				delete this[ name ];
			} catch ( e ) {}
		} );
	}
} );

jQuery.extend( {
	prop: function( elem, name, value ) {
		var ret, hooks,
			nType = elem.nodeType;

		// Don't get/set properties on text, comment and attribute nodes
		if ( nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		if ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {

			// Fix name and attach hooks
			name = jQuery.propFix[ name ] || name;
			hooks = jQuery.propHooks[ name ];
		}

		if ( value !== undefined ) {
			if ( hooks && "set" in hooks &&
				( ret = hooks.set( elem, value, name ) ) !== undefined ) {
				return ret;
			}

			return ( elem[ name ] = value );
		}

		if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {
			return ret;
		}

		return elem[ name ];
	},

	propHooks: {
		tabIndex: {
			get: function( elem ) {

				// elem.tabIndex doesn't always return the
				// correct value when it hasn't been explicitly set
				// http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/
				// Use proper attribute retrieval(#12072)
				var tabindex = jQuery.find.attr( elem, "tabindex" );

				return tabindex ?
					parseInt( tabindex, 10 ) :
					rfocusable.test( elem.nodeName ) ||
						rclickable.test( elem.nodeName ) && elem.href ?
							0 :
							-1;
			}
		}
	},

	propFix: {
		"for": "htmlFor",
		"class": "className"
	}
} );

// Some attributes require a special call on IE
// http://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx
if ( !support.hrefNormalized ) {

	// href/src property should get the full normalized URL (#10299/#12915)
	jQuery.each( [ "href", "src" ], function( i, name ) {
		jQuery.propHooks[ name ] = {
			get: function( elem ) {
				return elem.getAttribute( name, 4 );
			}
		};
	} );
}

// Support: Safari, IE9+
// Accessing the selectedIndex property
// forces the browser to respect setting selected
// on the option
// The getter ensures a default option is selected
// when in an optgroup
if ( !support.optSelected ) {
	jQuery.propHooks.selected = {
		get: function( elem ) {
			var parent = elem.parentNode;

			if ( parent ) {
				parent.selectedIndex;

				// Make sure that it also works with optgroups, see #5701
				if ( parent.parentNode ) {
					parent.parentNode.selectedIndex;
				}
			}
			return null;
		},
		set: function( elem ) {
			var parent = elem.parentNode;
			if ( parent ) {
				parent.selectedIndex;

				if ( parent.parentNode ) {
					parent.parentNode.selectedIndex;
				}
			}
		}
	};
}

jQuery.each( [
	"tabIndex",
	"readOnly",
	"maxLength",
	"cellSpacing",
	"cellPadding",
	"rowSpan",
	"colSpan",
	"useMap",
	"frameBorder",
	"contentEditable"
], function() {
	jQuery.propFix[ this.toLowerCase() ] = this;
} );

// IE6/7 call enctype encoding
if ( !support.enctype ) {
	jQuery.propFix.enctype = "encoding";
}




var rclass = /[\t\r\n\f]/g;

function getClass( elem ) {
	return jQuery.attr( elem, "class" ) || "";
}

jQuery.fn.extend( {
	addClass: function( value ) {
		var classes, elem, cur, curValue, clazz, j, finalValue,
			i = 0;

		if ( jQuery.isFunction( value ) ) {
			return this.each( function( j ) {
				jQuery( this ).addClass( value.call( this, j, getClass( this ) ) );
			} );
		}

		if ( typeof value === "string" && value ) {
			classes = value.match( rnotwhite ) || [];

			while ( ( elem = this[ i++ ] ) ) {
				curValue = getClass( elem );
				cur = elem.nodeType === 1 &&
					( " " + curValue + " " ).replace( rclass, " " );

				if ( cur ) {
					j = 0;
					while ( ( clazz = classes[ j++ ] ) ) {
						if ( cur.indexOf( " " + clazz + " " ) < 0 ) {
							cur += clazz + " ";
						}
					}

					// only assign if different to avoid unneeded rendering.
					finalValue = jQuery.trim( cur );
					if ( curValue !== finalValue ) {
						jQuery.attr( elem, "class", finalValue );
					}
				}
			}
		}

		return this;
	},

	removeClass: function( value ) {
		var classes, elem, cur, curValue, clazz, j, finalValue,
			i = 0;

		if ( jQuery.isFunction( value ) ) {
			return this.each( function( j ) {
				jQuery( this ).removeClass( value.call( this, j, getClass( this ) ) );
			} );
		}

		if ( !arguments.length ) {
			return this.attr( "class", "" );
		}

		if ( typeof value === "string" && value ) {
			classes = value.match( rnotwhite ) || [];

			while ( ( elem = this[ i++ ] ) ) {
				curValue = getClass( elem );

				// This expression is here for better compressibility (see addClass)
				cur = elem.nodeType === 1 &&
					( " " + curValue + " " ).replace( rclass, " " );

				if ( cur ) {
					j = 0;
					while ( ( clazz = classes[ j++ ] ) ) {

						// Remove *all* instances
						while ( cur.indexOf( " " + clazz + " " ) > -1 ) {
							cur = cur.replace( " " + clazz + " ", " " );
						}
					}

					// Only assign if different to avoid unneeded rendering.
					finalValue = jQuery.trim( cur );
					if ( curValue !== finalValue ) {
						jQuery.attr( elem, "class", finalValue );
					}
				}
			}
		}

		return this;
	},

	toggleClass: function( value, stateVal ) {
		var type = typeof value;

		if ( typeof stateVal === "boolean" && type === "string" ) {
			return stateVal ? this.addClass( value ) : this.removeClass( value );
		}

		if ( jQuery.isFunction( value ) ) {
			return this.each( function( i ) {
				jQuery( this ).toggleClass(
					value.call( this, i, getClass( this ), stateVal ),
					stateVal
				);
			} );
		}

		return this.each( function() {
			var className, i, self, classNames;

			if ( type === "string" ) {

				// Toggle individual class names
				i = 0;
				self = jQuery( this );
				classNames = value.match( rnotwhite ) || [];

				while ( ( className = classNames[ i++ ] ) ) {

					// Check each className given, space separated list
					if ( self.hasClass( className ) ) {
						self.removeClass( className );
					} else {
						self.addClass( className );
					}
				}

			// Toggle whole class name
			} else if ( value === undefined || type === "boolean" ) {
				className = getClass( this );
				if ( className ) {

					// store className if set
					jQuery._data( this, "__className__", className );
				}

				// If the element has a class name or if we're passed "false",
				// then remove the whole classname (if there was one, the above saved it).
				// Otherwise bring back whatever was previously saved (if anything),
				// falling back to the empty string if nothing was stored.
				jQuery.attr( this, "class",
					className || value === false ?
					"" :
					jQuery._data( this, "__className__" ) || ""
				);
			}
		} );
	},

	hasClass: function( selector ) {
		var className, elem,
			i = 0;

		className = " " + selector + " ";
		while ( ( elem = this[ i++ ] ) ) {
			if ( elem.nodeType === 1 &&
				( " " + getClass( elem ) + " " ).replace( rclass, " " )
					.indexOf( className ) > -1
			) {
				return true;
			}
		}

		return false;
	}
} );




// Return jQuery for attributes-only inclusion


jQuery.each( ( "blur focus focusin focusout load resize scroll unload click dblclick " +
	"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +
	"change select submit keydown keypress keyup error contextmenu" ).split( " " ),
	function( i, name ) {

	// Handle event binding
	jQuery.fn[ name ] = function( data, fn ) {
		return arguments.length > 0 ?
			this.on( name, null, data, fn ) :
			this.trigger( name );
	};
} );

jQuery.fn.extend( {
	hover: function( fnOver, fnOut ) {
		return this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );
	}
} );


var location = window.location;

var nonce = jQuery.now();

var rquery = ( /\?/ );



var rvalidtokens = /(,)|(\[|{)|(}|])|"(?:[^"\\\r\n]|\\["\\\/bfnrt]|\\u[\da-fA-F]{4})*"\s*:?|true|false|null|-?(?!0\d)\d+(?:\.\d+|)(?:[eE][+-]?\d+|)/g;

jQuery.parseJSON = function( data ) {

	// Attempt to parse using the native JSON parser first
	if ( window.JSON && window.JSON.parse ) {

		// Support: Android 2.3
		// Workaround failure to string-cast null input
		return window.JSON.parse( data + "" );
	}

	var requireNonComma,
		depth = null,
		str = jQuery.trim( data + "" );

	// Guard against invalid (and possibly dangerous) input by ensuring that nothing remains
	// after removing valid tokens
	return str && !jQuery.trim( str.replace( rvalidtokens, function( token, comma, open, close ) {

		// Force termination if we see a misplaced comma
		if ( requireNonComma && comma ) {
			depth = 0;
		}

		// Perform no more replacements after returning to outermost depth
		if ( depth === 0 ) {
			return token;
		}

		// Commas must not follow "[", "{", or ","
		requireNonComma = open || comma;

		// Determine new depth
		// array/object open ("[" or "{"): depth += true - false (increment)
		// array/object close ("]" or "}"): depth += false - true (decrement)
		// other cases ("," or primitive): depth += true - true (numeric cast)
		depth += !close - !open;

		// Remove this token
		return "";
	} ) ) ?
		( Function( "return " + str ) )() :
		jQuery.error( "Invalid JSON: " + data );
};


// Cross-browser xml parsing
jQuery.parseXML = function( data ) {
	var xml, tmp;
	if ( !data || typeof data !== "string" ) {
		return null;
	}
	try {
		if ( window.DOMParser ) { // Standard
			tmp = new window.DOMParser();
			xml = tmp.parseFromString( data, "text/xml" );
		} else { // IE
			xml = new window.ActiveXObject( "Microsoft.XMLDOM" );
			xml.async = "false";
			xml.loadXML( data );
		}
	} catch ( e ) {
		xml = undefined;
	}
	if ( !xml || !xml.documentElement || xml.getElementsByTagName( "parsererror" ).length ) {
		jQuery.error( "Invalid XML: " + data );
	}
	return xml;
};


var
	rhash = /#.*$/,
	rts = /([?&])_=[^&]*/,

	// IE leaves an \r character at EOL
	rheaders = /^(.*?):[ \t]*([^\r\n]*)\r?$/mg,

	// #7653, #8125, #8152: local protocol detection
	rlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,
	rnoContent = /^(?:GET|HEAD)$/,
	rprotocol = /^\/\//,
	rurl = /^([\w.+-]+:)(?:\/\/(?:[^\/?#]*@|)([^\/?#:]*)(?::(\d+)|)|)/,

	/* Prefilters
	 * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
	 * 2) These are called:
	 *    - BEFORE asking for a transport
	 *    - AFTER param serialization (s.data is a string if s.processData is true)
	 * 3) key is the dataType
	 * 4) the catchall symbol "*" can be used
	 * 5) execution will start with transport dataType and THEN continue down to "*" if needed
	 */
	prefilters = {},

	/* Transports bindings
	 * 1) key is the dataType
	 * 2) the catchall symbol "*" can be used
	 * 3) selection will start with transport dataType and THEN go to "*" if needed
	 */
	transports = {},

	// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression
	allTypes = "*/".concat( "*" ),

	// Document location
	ajaxLocation = location.href,

	// Segment location into parts
	ajaxLocParts = rurl.exec( ajaxLocation.toLowerCase() ) || [];

// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
function addToPrefiltersOrTransports( structure ) {

	// dataTypeExpression is optional and defaults to "*"
	return function( dataTypeExpression, func ) {

		if ( typeof dataTypeExpression !== "string" ) {
			func = dataTypeExpression;
			dataTypeExpression = "*";
		}

		var dataType,
			i = 0,
			dataTypes = dataTypeExpression.toLowerCase().match( rnotwhite ) || [];

		if ( jQuery.isFunction( func ) ) {

			// For each dataType in the dataTypeExpression
			while ( ( dataType = dataTypes[ i++ ] ) ) {

				// Prepend if requested
				if ( dataType.charAt( 0 ) === "+" ) {
					dataType = dataType.slice( 1 ) || "*";
					( structure[ dataType ] = structure[ dataType ] || [] ).unshift( func );

				// Otherwise append
				} else {
					( structure[ dataType ] = structure[ dataType ] || [] ).push( func );
				}
			}
		}
	};
}

// Base inspection function for prefilters and transports
function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {

	var inspected = {},
		seekingTransport = ( structure === transports );

	function inspect( dataType ) {
		var selected;
		inspected[ dataType ] = true;
		jQuery.each( structure[ dataType ] || [], function( _, prefilterOrFactory ) {
			var dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );
			if ( typeof dataTypeOrTransport === "string" &&
				!seekingTransport && !inspected[ dataTypeOrTransport ] ) {

				options.dataTypes.unshift( dataTypeOrTransport );
				inspect( dataTypeOrTransport );
				return false;
			} else if ( seekingTransport ) {
				return !( selected = dataTypeOrTransport );
			}
		} );
		return selected;
	}

	return inspect( options.dataTypes[ 0 ] ) || !inspected[ "*" ] && inspect( "*" );
}

// A special extend for ajax options
// that takes "flat" options (not to be deep extended)
// Fixes #9887
function ajaxExtend( target, src ) {
	var deep, key,
		flatOptions = jQuery.ajaxSettings.flatOptions || {};

	for ( key in src ) {
		if ( src[ key ] !== undefined ) {
			( flatOptions[ key ] ? target : ( deep || ( deep = {} ) ) )[ key ] = src[ key ];
		}
	}
	if ( deep ) {
		jQuery.extend( true, target, deep );
	}

	return target;
}

/* Handles responses to an ajax request:
 * - finds the right dataType (mediates between content-type and expected dataType)
 * - returns the corresponding response
 */
function ajaxHandleResponses( s, jqXHR, responses ) {
	var firstDataType, ct, finalDataType, type,
		contents = s.contents,
		dataTypes = s.dataTypes;

	// Remove auto dataType and get content-type in the process
	while ( dataTypes[ 0 ] === "*" ) {
		dataTypes.shift();
		if ( ct === undefined ) {
			ct = s.mimeType || jqXHR.getResponseHeader( "Content-Type" );
		}
	}

	// Check if we're dealing with a known content-type
	if ( ct ) {
		for ( type in contents ) {
			if ( contents[ type ] && contents[ type ].test( ct ) ) {
				dataTypes.unshift( type );
				break;
			}
		}
	}

	// Check to see if we have a response for the expected dataType
	if ( dataTypes[ 0 ] in responses ) {
		finalDataType = dataTypes[ 0 ];
	} else {

		// Try convertible dataTypes
		for ( type in responses ) {
			if ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[ 0 ] ] ) {
				finalDataType = type;
				break;
			}
			if ( !firstDataType ) {
				firstDataType = type;
			}
		}

		// Or just use first one
		finalDataType = finalDataType || firstDataType;
	}

	// If we found a dataType
	// We add the dataType to the list if needed
	// and return the corresponding response
	if ( finalDataType ) {
		if ( finalDataType !== dataTypes[ 0 ] ) {
			dataTypes.unshift( finalDataType );
		}
		return responses[ finalDataType ];
	}
}

/* Chain conversions given the request and the original response
 * Also sets the responseXXX fields on the jqXHR instance
 */
function ajaxConvert( s, response, jqXHR, isSuccess ) {
	var conv2, current, conv, tmp, prev,
		converters = {},

		// Work with a copy of dataTypes in case we need to modify it for conversion
		dataTypes = s.dataTypes.slice();

	// Create converters map with lowercased keys
	if ( dataTypes[ 1 ] ) {
		for ( conv in s.converters ) {
			converters[ conv.toLowerCase() ] = s.converters[ conv ];
		}
	}

	current = dataTypes.shift();

	// Convert to each sequential dataType
	while ( current ) {

		if ( s.responseFields[ current ] ) {
			jqXHR[ s.responseFields[ current ] ] = response;
		}

		// Apply the dataFilter if provided
		if ( !prev && isSuccess && s.dataFilter ) {
			response = s.dataFilter( response, s.dataType );
		}

		prev = current;
		current = dataTypes.shift();

		if ( current ) {

			// There's only work to do if current dataType is non-auto
			if ( current === "*" ) {

				current = prev;

			// Convert response if prev dataType is non-auto and differs from current
			} else if ( prev !== "*" && prev !== current ) {

				// Seek a direct converter
				conv = converters[ prev + " " + current ] || converters[ "* " + current ];

				// If none found, seek a pair
				if ( !conv ) {
					for ( conv2 in converters ) {

						// If conv2 outputs current
						tmp = conv2.split( " " );
						if ( tmp[ 1 ] === current ) {

							// If prev can be converted to accepted input
							conv = converters[ prev + " " + tmp[ 0 ] ] ||
								converters[ "* " + tmp[ 0 ] ];
							if ( conv ) {

								// Condense equivalence converters
								if ( conv === true ) {
									conv = converters[ conv2 ];

								// Otherwise, insert the intermediate dataType
								} else if ( converters[ conv2 ] !== true ) {
									current = tmp[ 0 ];
									dataTypes.unshift( tmp[ 1 ] );
								}
								break;
							}
						}
					}
				}

				// Apply converter (if not an equivalence)
				if ( conv !== true ) {

					// Unless errors are allowed to bubble, catch and return them
					if ( conv && s[ "throws" ] ) { // jscs:ignore requireDotNotation
						response = conv( response );
					} else {
						try {
							response = conv( response );
						} catch ( e ) {
							return {
								state: "parsererror",
								error: conv ? e : "No conversion from " + prev + " to " + current
							};
						}
					}
				}
			}
		}
	}

	return { state: "success", data: response };
}

jQuery.extend( {

	// Counter for holding the number of active queries
	active: 0,

	// Last-Modified header cache for next request
	lastModified: {},
	etag: {},

	ajaxSettings: {
		url: ajaxLocation,
		type: "GET",
		isLocal: rlocalProtocol.test( ajaxLocParts[ 1 ] ),
		global: true,
		processData: true,
		async: true,
		contentType: "application/x-www-form-urlencoded; charset=UTF-8",
		/*
		timeout: 0,
		data: null,
		dataType: null,
		username: null,
		password: null,
		cache: null,
		throws: false,
		traditional: false,
		headers: {},
		*/

		accepts: {
			"*": allTypes,
			text: "text/plain",
			html: "text/html",
			xml: "application/xml, text/xml",
			json: "application/json, text/javascript"
		},

		contents: {
			xml: /\bxml\b/,
			html: /\bhtml/,
			json: /\bjson\b/
		},

		responseFields: {
			xml: "responseXML",
			text: "responseText",
			json: "responseJSON"
		},

		// Data converters
		// Keys separate source (or catchall "*") and destination types with a single space
		converters: {

			// Convert anything to text
			"* text": String,

			// Text to html (true = no transformation)
			"text html": true,

			// Evaluate text as a json expression
			"text json": jQuery.parseJSON,

			// Parse text as xml
			"text xml": jQuery.parseXML
		},

		// For options that shouldn't be deep extended:
		// you can add your own custom options here if
		// and when you create one that shouldn't be
		// deep extended (see ajaxExtend)
		flatOptions: {
			url: true,
			context: true
		}
	},

	// Creates a full fledged settings object into target
	// with both ajaxSettings and settings fields.
	// If target is omitted, writes into ajaxSettings.
	ajaxSetup: function( target, settings ) {
		return settings ?

			// Building a settings object
			ajaxExtend( ajaxExtend( target, jQuery.ajaxSettings ), settings ) :

			// Extending ajaxSettings
			ajaxExtend( jQuery.ajaxSettings, target );
	},

	ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
	ajaxTransport: addToPrefiltersOrTransports( transports ),

	// Main method
	ajax: function( url, options ) {

		// If url is an object, simulate pre-1.5 signature
		if ( typeof url === "object" ) {
			options = url;
			url = undefined;
		}

		// Force options to be an object
		options = options || {};

		var

			// Cross-domain detection vars
			parts,

			// Loop variable
			i,

			// URL without anti-cache param
			cacheURL,

			// Response headers as string
			responseHeadersString,

			// timeout handle
			timeoutTimer,

			// To know if global events are to be dispatched
			fireGlobals,

			transport,

			// Response headers
			responseHeaders,

			// Create the final options object
			s = jQuery.ajaxSetup( {}, options ),

			// Callbacks context
			callbackContext = s.context || s,

			// Context for global events is callbackContext if it is a DOM node or jQuery collection
			globalEventContext = s.context &&
				( callbackContext.nodeType || callbackContext.jquery ) ?
					jQuery( callbackContext ) :
					jQuery.event,

			// Deferreds
			deferred = jQuery.Deferred(),
			completeDeferred = jQuery.Callbacks( "once memory" ),

			// Status-dependent callbacks
			statusCode = s.statusCode || {},

			// Headers (they are sent all at once)
			requestHeaders = {},
			requestHeadersNames = {},

			// The jqXHR state
			state = 0,

			// Default abort message
			strAbort = "canceled",

			// Fake xhr
			jqXHR = {
				readyState: 0,

				// Builds headers hashtable if needed
				getResponseHeader: function( key ) {
					var match;
					if ( state === 2 ) {
						if ( !responseHeaders ) {
							responseHeaders = {};
							while ( ( match = rheaders.exec( responseHeadersString ) ) ) {
								responseHeaders[ match[ 1 ].toLowerCase() ] = match[ 2 ];
							}
						}
						match = responseHeaders[ key.toLowerCase() ];
					}
					return match == null ? null : match;
				},

				// Raw string
				getAllResponseHeaders: function() {
					return state === 2 ? responseHeadersString : null;
				},

				// Caches the header
				setRequestHeader: function( name, value ) {
					var lname = name.toLowerCase();
					if ( !state ) {
						name = requestHeadersNames[ lname ] = requestHeadersNames[ lname ] || name;
						requestHeaders[ name ] = value;
					}
					return this;
				},

				// Overrides response content-type header
				overrideMimeType: function( type ) {
					if ( !state ) {
						s.mimeType = type;
					}
					return this;
				},

				// Status-dependent callbacks
				statusCode: function( map ) {
					var code;
					if ( map ) {
						if ( state < 2 ) {
							for ( code in map ) {

								// Lazy-add the new callback in a way that preserves old ones
								statusCode[ code ] = [ statusCode[ code ], map[ code ] ];
							}
						} else {

							// Execute the appropriate callbacks
							jqXHR.always( map[ jqXHR.status ] );
						}
					}
					return this;
				},

				// Cancel the request
				abort: function( statusText ) {
					var finalText = statusText || strAbort;
					if ( transport ) {
						transport.abort( finalText );
					}
					done( 0, finalText );
					return this;
				}
			};

		// Attach deferreds
		deferred.promise( jqXHR ).complete = completeDeferred.add;
		jqXHR.success = jqXHR.done;
		jqXHR.error = jqXHR.fail;

		// Remove hash character (#7531: and string promotion)
		// Add protocol if not provided (#5866: IE7 issue with protocol-less urls)
		// Handle falsy url in the settings object (#10093: consistency with old signature)
		// We also use the url parameter if available
		s.url = ( ( url || s.url || ajaxLocation ) + "" )
			.replace( rhash, "" )
			.replace( rprotocol, ajaxLocParts[ 1 ] + "//" );

		// Alias method option to type as per ticket #12004
		s.type = options.method || options.type || s.method || s.type;

		// Extract dataTypes list
		s.dataTypes = jQuery.trim( s.dataType || "*" ).toLowerCase().match( rnotwhite ) || [ "" ];

		// A cross-domain request is in order when we have a protocol:host:port mismatch
		if ( s.crossDomain == null ) {
			parts = rurl.exec( s.url.toLowerCase() );
			s.crossDomain = !!( parts &&
				( parts[ 1 ] !== ajaxLocParts[ 1 ] || parts[ 2 ] !== ajaxLocParts[ 2 ] ||
					( parts[ 3 ] || ( parts[ 1 ] === "http:" ? "80" : "443" ) ) !==
						( ajaxLocParts[ 3 ] || ( ajaxLocParts[ 1 ] === "http:" ? "80" : "443" ) ) )
			);
		}

		// Convert data if not already a string
		if ( s.data && s.processData && typeof s.data !== "string" ) {
			s.data = jQuery.param( s.data, s.traditional );
		}

		// Apply prefilters
		inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

		// If request was aborted inside a prefilter, stop there
		if ( state === 2 ) {
			return jqXHR;
		}

		// We can fire global events as of now if asked to
		// Don't fire events if jQuery.event is undefined in an AMD-usage scenario (#15118)
		fireGlobals = jQuery.event && s.global;

		// Watch for a new set of requests
		if ( fireGlobals && jQuery.active++ === 0 ) {
			jQuery.event.trigger( "ajaxStart" );
		}

		// Uppercase the type
		s.type = s.type.toUpperCase();

		// Determine if request has content
		s.hasContent = !rnoContent.test( s.type );

		// Save the URL in case we're toying with the If-Modified-Since
		// and/or If-None-Match header later on
		cacheURL = s.url;

		// More options handling for requests with no content
		if ( !s.hasContent ) {

			// If data is available, append data to url
			if ( s.data ) {
				cacheURL = ( s.url += ( rquery.test( cacheURL ) ? "&" : "?" ) + s.data );

				// #9682: remove data so that it's not used in an eventual retry
				delete s.data;
			}

			// Add anti-cache in url if needed
			if ( s.cache === false ) {
				s.url = rts.test( cacheURL ) ?

					// If there is already a '_' parameter, set its value
					cacheURL.replace( rts, "$1_=" + nonce++ ) :

					// Otherwise add one to the end
					cacheURL + ( rquery.test( cacheURL ) ? "&" : "?" ) + "_=" + nonce++;
			}
		}

		// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
		if ( s.ifModified ) {
			if ( jQuery.lastModified[ cacheURL ] ) {
				jqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ cacheURL ] );
			}
			if ( jQuery.etag[ cacheURL ] ) {
				jqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ cacheURL ] );
			}
		}

		// Set the correct header, if data is being sent
		if ( s.data && s.hasContent && s.contentType !== false || options.contentType ) {
			jqXHR.setRequestHeader( "Content-Type", s.contentType );
		}

		// Set the Accepts header for the server, depending on the dataType
		jqXHR.setRequestHeader(
			"Accept",
			s.dataTypes[ 0 ] && s.accepts[ s.dataTypes[ 0 ] ] ?
				s.accepts[ s.dataTypes[ 0 ] ] +
					( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :
				s.accepts[ "*" ]
		);

		// Check for headers option
		for ( i in s.headers ) {
			jqXHR.setRequestHeader( i, s.headers[ i ] );
		}

		// Allow custom headers/mimetypes and early abort
		if ( s.beforeSend &&
			( s.beforeSend.call( callbackContext, jqXHR, s ) === false || state === 2 ) ) {

			// Abort if not done already and return
			return jqXHR.abort();
		}

		// aborting is no longer a cancellation
		strAbort = "abort";

		// Install callbacks on deferreds
		for ( i in { success: 1, error: 1, complete: 1 } ) {
			jqXHR[ i ]( s[ i ] );
		}

		// Get transport
		transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

		// If no transport, we auto-abort
		if ( !transport ) {
			done( -1, "No Transport" );
		} else {
			jqXHR.readyState = 1;

			// Send global event
			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );
			}

			// If request was aborted inside ajaxSend, stop there
			if ( state === 2 ) {
				return jqXHR;
			}

			// Timeout
			if ( s.async && s.timeout > 0 ) {
				timeoutTimer = window.setTimeout( function() {
					jqXHR.abort( "timeout" );
				}, s.timeout );
			}

			try {
				state = 1;
				transport.send( requestHeaders, done );
			} catch ( e ) {

				// Propagate exception as error if not done
				if ( state < 2 ) {
					done( -1, e );

				// Simply rethrow otherwise
				} else {
					throw e;
				}
			}
		}

		// Callback for when everything is done
		function done( status, nativeStatusText, responses, headers ) {
			var isSuccess, success, error, response, modified,
				statusText = nativeStatusText;

			// Called once
			if ( state === 2 ) {
				return;
			}

			// State is "done" now
			state = 2;

			// Clear timeout if it exists
			if ( timeoutTimer ) {
				window.clearTimeout( timeoutTimer );
			}

			// Dereference transport for early garbage collection
			// (no matter how long the jqXHR object will be used)
			transport = undefined;

			// Cache response headers
			responseHeadersString = headers || "";

			// Set readyState
			jqXHR.readyState = status > 0 ? 4 : 0;

			// Determine if successful
			isSuccess = status >= 200 && status < 300 || status === 304;

			// Get response data
			if ( responses ) {
				response = ajaxHandleResponses( s, jqXHR, responses );
			}

			// Convert no matter what (that way responseXXX fields are always set)
			response = ajaxConvert( s, response, jqXHR, isSuccess );

			// If successful, handle type chaining
			if ( isSuccess ) {

				// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
				if ( s.ifModified ) {
					modified = jqXHR.getResponseHeader( "Last-Modified" );
					if ( modified ) {
						jQuery.lastModified[ cacheURL ] = modified;
					}
					modified = jqXHR.getResponseHeader( "etag" );
					if ( modified ) {
						jQuery.etag[ cacheURL ] = modified;
					}
				}

				// if no content
				if ( status === 204 || s.type === "HEAD" ) {
					statusText = "nocontent";

				// if not modified
				} else if ( status === 304 ) {
					statusText = "notmodified";

				// If we have data, let's convert it
				} else {
					statusText = response.state;
					success = response.data;
					error = response.error;
					isSuccess = !error;
				}
			} else {

				// We extract error from statusText
				// then normalize statusText and status for non-aborts
				error = statusText;
				if ( status || !statusText ) {
					statusText = "error";
					if ( status < 0 ) {
						status = 0;
					}
				}
			}

			// Set data for the fake xhr object
			jqXHR.status = status;
			jqXHR.statusText = ( nativeStatusText || statusText ) + "";

			// Success/Error
			if ( isSuccess ) {
				deferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );
			} else {
				deferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );
			}

			// Status-dependent callbacks
			jqXHR.statusCode( statusCode );
			statusCode = undefined;

			if ( fireGlobals ) {
				globalEventContext.trigger( isSuccess ? "ajaxSuccess" : "ajaxError",
					[ jqXHR, s, isSuccess ? success : error ] );
			}

			// Complete
			completeDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );

			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );

				// Handle the global AJAX counter
				if ( !( --jQuery.active ) ) {
					jQuery.event.trigger( "ajaxStop" );
				}
			}
		}

		return jqXHR;
	},

	getJSON: function( url, data, callback ) {
		return jQuery.get( url, data, callback, "json" );
	},

	getScript: function( url, callback ) {
		return jQuery.get( url, undefined, callback, "script" );
	}
} );

jQuery.each( [ "get", "post" ], function( i, method ) {
	jQuery[ method ] = function( url, data, callback, type ) {

		// shift arguments if data argument was omitted
		if ( jQuery.isFunction( data ) ) {
			type = type || callback;
			callback = data;
			data = undefined;
		}

		// The url can be an options object (which then must have .url)
		return jQuery.ajax( jQuery.extend( {
			url: url,
			type: method,
			dataType: type,
			data: data,
			success: callback
		}, jQuery.isPlainObject( url ) && url ) );
	};
} );


jQuery._evalUrl = function( url ) {
	return jQuery.ajax( {
		url: url,

		// Make this explicit, since user can override this through ajaxSetup (#11264)
		type: "GET",
		dataType: "script",
		cache: true,
		async: false,
		global: false,
		"throws": true
	} );
};


jQuery.fn.extend( {
	wrapAll: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each( function( i ) {
				jQuery( this ).wrapAll( html.call( this, i ) );
			} );
		}

		if ( this[ 0 ] ) {

			// The elements to wrap the target around
			var wrap = jQuery( html, this[ 0 ].ownerDocument ).eq( 0 ).clone( true );

			if ( this[ 0 ].parentNode ) {
				wrap.insertBefore( this[ 0 ] );
			}

			wrap.map( function() {
				var elem = this;

				while ( elem.firstChild && elem.firstChild.nodeType === 1 ) {
					elem = elem.firstChild;
				}

				return elem;
			} ).append( this );
		}

		return this;
	},

	wrapInner: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each( function( i ) {
				jQuery( this ).wrapInner( html.call( this, i ) );
			} );
		}

		return this.each( function() {
			var self = jQuery( this ),
				contents = self.contents();

			if ( contents.length ) {
				contents.wrapAll( html );

			} else {
				self.append( html );
			}
		} );
	},

	wrap: function( html ) {
		var isFunction = jQuery.isFunction( html );

		return this.each( function( i ) {
			jQuery( this ).wrapAll( isFunction ? html.call( this, i ) : html );
		} );
	},

	unwrap: function() {
		return this.parent().each( function() {
			if ( !jQuery.nodeName( this, "body" ) ) {
				jQuery( this ).replaceWith( this.childNodes );
			}
		} ).end();
	}
} );


function getDisplay( elem ) {
	return elem.style && elem.style.display || jQuery.css( elem, "display" );
}

function filterHidden( elem ) {

	// Disconnected elements are considered hidden
	if ( !jQuery.contains( elem.ownerDocument || document, elem ) ) {
		return true;
	}
	while ( elem && elem.nodeType === 1 ) {
		if ( getDisplay( elem ) === "none" || elem.type === "hidden" ) {
			return true;
		}
		elem = elem.parentNode;
	}
	return false;
}

jQuery.expr.filters.hidden = function( elem ) {

	// Support: Opera <= 12.12
	// Opera reports offsetWidths and offsetHeights less than zero on some elements
	return support.reliableHiddenOffsets() ?
		( elem.offsetWidth <= 0 && elem.offsetHeight <= 0 &&
			!elem.getClientRects().length ) :
			filterHidden( elem );
};

jQuery.expr.filters.visible = function( elem ) {
	return !jQuery.expr.filters.hidden( elem );
};




var r20 = /%20/g,
	rbracket = /\[\]$/,
	rCRLF = /\r?\n/g,
	rsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,
	rsubmittable = /^(?:input|select|textarea|keygen)/i;

function buildParams( prefix, obj, traditional, add ) {
	var name;

	if ( jQuery.isArray( obj ) ) {

		// Serialize array item.
		jQuery.each( obj, function( i, v ) {
			if ( traditional || rbracket.test( prefix ) ) {

				// Treat each array item as a scalar.
				add( prefix, v );

			} else {

				// Item is non-scalar (array or object), encode its numeric index.
				buildParams(
					prefix + "[" + ( typeof v === "object" && v != null ? i : "" ) + "]",
					v,
					traditional,
					add
				);
			}
		} );

	} else if ( !traditional && jQuery.type( obj ) === "object" ) {

		// Serialize object item.
		for ( name in obj ) {
			buildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );
		}

	} else {

		// Serialize scalar item.
		add( prefix, obj );
	}
}

// Serialize an array of form elements or a set of
// key/values into a query string
jQuery.param = function( a, traditional ) {
	var prefix,
		s = [],
		add = function( key, value ) {

			// If value is a function, invoke it and return its value
			value = jQuery.isFunction( value ) ? value() : ( value == null ? "" : value );
			s[ s.length ] = encodeURIComponent( key ) + "=" + encodeURIComponent( value );
		};

	// Set traditional to true for jQuery <= 1.3.2 behavior.
	if ( traditional === undefined ) {
		traditional = jQuery.ajaxSettings && jQuery.ajaxSettings.traditional;
	}

	// If an array was passed in, assume that it is an array of form elements.
	if ( jQuery.isArray( a ) || ( a.jquery && !jQuery.isPlainObject( a ) ) ) {

		// Serialize the form elements
		jQuery.each( a, function() {
			add( this.name, this.value );
		} );

	} else {

		// If traditional, encode the "old" way (the way 1.3.2 or older
		// did it), otherwise encode params recursively.
		for ( prefix in a ) {
			buildParams( prefix, a[ prefix ], traditional, add );
		}
	}

	// Return the resulting serialization
	return s.join( "&" ).replace( r20, "+" );
};

jQuery.fn.extend( {
	serialize: function() {
		return jQuery.param( this.serializeArray() );
	},
	serializeArray: function() {
		return this.map( function() {

			// Can add propHook for "elements" to filter or add form elements
			var elements = jQuery.prop( this, "elements" );
			return elements ? jQuery.makeArray( elements ) : this;
		} )
		.filter( function() {
			var type = this.type;

			// Use .is(":disabled") so that fieldset[disabled] works
			return this.name && !jQuery( this ).is( ":disabled" ) &&
				rsubmittable.test( this.nodeName ) && !rsubmitterTypes.test( type ) &&
				( this.checked || !rcheckableType.test( type ) );
		} )
		.map( function( i, elem ) {
			var val = jQuery( this ).val();

			return val == null ?
				null :
				jQuery.isArray( val ) ?
					jQuery.map( val, function( val ) {
						return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
					} ) :
					{ name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
		} ).get();
	}
} );


// Create the request object
// (This is still attached to ajaxSettings for backward compatibility)
jQuery.ajaxSettings.xhr = window.ActiveXObject !== undefined ?

	// Support: IE6-IE8
	function() {

		// XHR cannot access local files, always use ActiveX for that case
		if ( this.isLocal ) {
			return createActiveXHR();
		}

		// Support: IE 9-11
		// IE seems to error on cross-domain PATCH requests when ActiveX XHR
		// is used. In IE 9+ always use the native XHR.
		// Note: this condition won't catch Edge as it doesn't define
		// document.documentMode but it also doesn't support ActiveX so it won't
		// reach this code.
		if ( document.documentMode > 8 ) {
			return createStandardXHR();
		}

		// Support: IE<9
		// oldIE XHR does not support non-RFC2616 methods (#13240)
		// See http://msdn.microsoft.com/en-us/library/ie/ms536648(v=vs.85).aspx
		// and http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html#sec9
		// Although this check for six methods instead of eight
		// since IE also does not support "trace" and "connect"
		return /^(get|post|head|put|delete|options)$/i.test( this.type ) &&
			createStandardXHR() || createActiveXHR();
	} :

	// For all other browsers, use the standard XMLHttpRequest object
	createStandardXHR;

var xhrId = 0,
	xhrCallbacks = {},
	xhrSupported = jQuery.ajaxSettings.xhr();

// Support: IE<10
// Open requests must be manually aborted on unload (#5280)
// See https://support.microsoft.com/kb/2856746 for more info
if ( window.attachEvent ) {
	window.attachEvent( "onunload", function() {
		for ( var key in xhrCallbacks ) {
			xhrCallbacks[ key ]( undefined, true );
		}
	} );
}

// Determine support properties
support.cors = !!xhrSupported && ( "withCredentials" in xhrSupported );
xhrSupported = support.ajax = !!xhrSupported;

// Create transport if the browser can provide an xhr
if ( xhrSupported ) {

	jQuery.ajaxTransport( function( options ) {

		// Cross domain only allowed if supported through XMLHttpRequest
		if ( !options.crossDomain || support.cors ) {

			var callback;

			return {
				send: function( headers, complete ) {
					var i,
						xhr = options.xhr(),
						id = ++xhrId;

					// Open the socket
					xhr.open(
						options.type,
						options.url,
						options.async,
						options.username,
						options.password
					);

					// Apply custom fields if provided
					if ( options.xhrFields ) {
						for ( i in options.xhrFields ) {
							xhr[ i ] = options.xhrFields[ i ];
						}
					}

					// Override mime type if needed
					if ( options.mimeType && xhr.overrideMimeType ) {
						xhr.overrideMimeType( options.mimeType );
					}

					// X-Requested-With header
					// For cross-domain requests, seeing as conditions for a preflight are
					// akin to a jigsaw puzzle, we simply never set it to be sure.
					// (it can always be set on a per-request basis or even using ajaxSetup)
					// For same-domain requests, won't change header if already provided.
					if ( !options.crossDomain && !headers[ "X-Requested-With" ] ) {
						headers[ "X-Requested-With" ] = "XMLHttpRequest";
					}

					// Set headers
					for ( i in headers ) {

						// Support: IE<9
						// IE's ActiveXObject throws a 'Type Mismatch' exception when setting
						// request header to a null-value.
						//
						// To keep consistent with other XHR implementations, cast the value
						// to string and ignore `undefined`.
						if ( headers[ i ] !== undefined ) {
							xhr.setRequestHeader( i, headers[ i ] + "" );
						}
					}

					// Do send the request
					// This may raise an exception which is actually
					// handled in jQuery.ajax (so no try/catch here)
					xhr.send( ( options.hasContent && options.data ) || null );

					// Listener
					callback = function( _, isAbort ) {
						var status, statusText, responses;

						// Was never called and is aborted or complete
						if ( callback && ( isAbort || xhr.readyState === 4 ) ) {

							// Clean up
							delete xhrCallbacks[ id ];
							callback = undefined;
							xhr.onreadystatechange = jQuery.noop;

							// Abort manually if needed
							if ( isAbort ) {
								if ( xhr.readyState !== 4 ) {
									xhr.abort();
								}
							} else {
								responses = {};
								status = xhr.status;

								// Support: IE<10
								// Accessing binary-data responseText throws an exception
								// (#11426)
								if ( typeof xhr.responseText === "string" ) {
									responses.text = xhr.responseText;
								}

								// Firefox throws an exception when accessing
								// statusText for faulty cross-domain requests
								try {
									statusText = xhr.statusText;
								} catch ( e ) {

									// We normalize with Webkit giving an empty statusText
									statusText = "";
								}

								// Filter status for non standard behaviors

								// If the request is local and we have data: assume a success
								// (success with no data won't get notified, that's the best we
								// can do given current implementations)
								if ( !status && options.isLocal && !options.crossDomain ) {
									status = responses.text ? 200 : 404;

								// IE - #1450: sometimes returns 1223 when it should be 204
								} else if ( status === 1223 ) {
									status = 204;
								}
							}
						}

						// Call complete if needed
						if ( responses ) {
							complete( status, statusText, responses, xhr.getAllResponseHeaders() );
						}
					};

					// Do send the request
					// `xhr.send` may raise an exception, but it will be
					// handled in jQuery.ajax (so no try/catch here)
					if ( !options.async ) {

						// If we're in sync mode we fire the callback
						callback();
					} else if ( xhr.readyState === 4 ) {

						// (IE6 & IE7) if it's in cache and has been
						// retrieved directly we need to fire the callback
						window.setTimeout( callback );
					} else {

						// Register the callback, but delay it in case `xhr.send` throws
						// Add to the list of active xhr callbacks
						xhr.onreadystatechange = xhrCallbacks[ id ] = callback;
					}
				},

				abort: function() {
					if ( callback ) {
						callback( undefined, true );
					}
				}
			};
		}
	} );
}

// Functions to create xhrs
function createStandardXHR() {
	try {
		return new window.XMLHttpRequest();
	} catch ( e ) {}
}

function createActiveXHR() {
	try {
		return new window.ActiveXObject( "Microsoft.XMLHTTP" );
	} catch ( e ) {}
}




// Install script dataType
jQuery.ajaxSetup( {
	accepts: {
		script: "text/javascript, application/javascript, " +
			"application/ecmascript, application/x-ecmascript"
	},
	contents: {
		script: /\b(?:java|ecma)script\b/
	},
	converters: {
		"text script": function( text ) {
			jQuery.globalEval( text );
			return text;
		}
	}
} );

// Handle cache's special case and global
jQuery.ajaxPrefilter( "script", function( s ) {
	if ( s.cache === undefined ) {
		s.cache = false;
	}
	if ( s.crossDomain ) {
		s.type = "GET";
		s.global = false;
	}
} );

// Bind script tag hack transport
jQuery.ajaxTransport( "script", function( s ) {

	// This transport only deals with cross domain requests
	if ( s.crossDomain ) {

		var script,
			head = document.head || jQuery( "head" )[ 0 ] || document.documentElement;

		return {

			send: function( _, callback ) {

				script = document.createElement( "script" );

				script.async = true;

				if ( s.scriptCharset ) {
					script.charset = s.scriptCharset;
				}

				script.src = s.url;

				// Attach handlers for all browsers
				script.onload = script.onreadystatechange = function( _, isAbort ) {

					if ( isAbort || !script.readyState || /loaded|complete/.test( script.readyState ) ) {

						// Handle memory leak in IE
						script.onload = script.onreadystatechange = null;

						// Remove the script
						if ( script.parentNode ) {
							script.parentNode.removeChild( script );
						}

						// Dereference the script
						script = null;

						// Callback if not abort
						if ( !isAbort ) {
							callback( 200, "success" );
						}
					}
				};

				// Circumvent IE6 bugs with base elements (#2709 and #4378) by prepending
				// Use native DOM manipulation to avoid our domManip AJAX trickery
				head.insertBefore( script, head.firstChild );
			},

			abort: function() {
				if ( script ) {
					script.onload( undefined, true );
				}
			}
		};
	}
} );




var oldCallbacks = [],
	rjsonp = /(=)\?(?=&|$)|\?\?/;

// Default jsonp settings
jQuery.ajaxSetup( {
	jsonp: "callback",
	jsonpCallback: function() {
		var callback = oldCallbacks.pop() || ( jQuery.expando + "_" + ( nonce++ ) );
		this[ callback ] = true;
		return callback;
	}
} );

// Detect, normalize options and install callbacks for jsonp requests
jQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {

	var callbackName, overwritten, responseContainer,
		jsonProp = s.jsonp !== false && ( rjsonp.test( s.url ) ?
			"url" :
			typeof s.data === "string" &&
				( s.contentType || "" )
					.indexOf( "application/x-www-form-urlencoded" ) === 0 &&
				rjsonp.test( s.data ) && "data"
		);

	// Handle iff the expected data type is "jsonp" or we have a parameter to set
	if ( jsonProp || s.dataTypes[ 0 ] === "jsonp" ) {

		// Get callback name, remembering preexisting value associated with it
		callbackName = s.jsonpCallback = jQuery.isFunction( s.jsonpCallback ) ?
			s.jsonpCallback() :
			s.jsonpCallback;

		// Insert callback into url or form data
		if ( jsonProp ) {
			s[ jsonProp ] = s[ jsonProp ].replace( rjsonp, "$1" + callbackName );
		} else if ( s.jsonp !== false ) {
			s.url += ( rquery.test( s.url ) ? "&" : "?" ) + s.jsonp + "=" + callbackName;
		}

		// Use data converter to retrieve json after script execution
		s.converters[ "script json" ] = function() {
			if ( !responseContainer ) {
				jQuery.error( callbackName + " was not called" );
			}
			return responseContainer[ 0 ];
		};

		// force json dataType
		s.dataTypes[ 0 ] = "json";

		// Install callback
		overwritten = window[ callbackName ];
		window[ callbackName ] = function() {
			responseContainer = arguments;
		};

		// Clean-up function (fires after converters)
		jqXHR.always( function() {

			// If previous value didn't exist - remove it
			if ( overwritten === undefined ) {
				jQuery( window ).removeProp( callbackName );

			// Otherwise restore preexisting value
			} else {
				window[ callbackName ] = overwritten;
			}

			// Save back as free
			if ( s[ callbackName ] ) {

				// make sure that re-using the options doesn't screw things around
				s.jsonpCallback = originalSettings.jsonpCallback;

				// save the callback name for future use
				oldCallbacks.push( callbackName );
			}

			// Call if it was a function and we have a response
			if ( responseContainer && jQuery.isFunction( overwritten ) ) {
				overwritten( responseContainer[ 0 ] );
			}

			responseContainer = overwritten = undefined;
		} );

		// Delegate to script
		return "script";
	}
} );




// data: string of html
// context (optional): If specified, the fragment will be created in this context,
// defaults to document
// keepScripts (optional): If true, will include scripts passed in the html string
jQuery.parseHTML = function( data, context, keepScripts ) {
	if ( !data || typeof data !== "string" ) {
		return null;
	}
	if ( typeof context === "boolean" ) {
		keepScripts = context;
		context = false;
	}
	context = context || document;

	var parsed = rsingleTag.exec( data ),
		scripts = !keepScripts && [];

	// Single tag
	if ( parsed ) {
		return [ context.createElement( parsed[ 1 ] ) ];
	}

	parsed = buildFragment( [ data ], context, scripts );

	if ( scripts && scripts.length ) {
		jQuery( scripts ).remove();
	}

	return jQuery.merge( [], parsed.childNodes );
};


// Keep a copy of the old load method
var _load = jQuery.fn.load;

/**
 * Load a url into a page
 */
jQuery.fn.load = function( url, params, callback ) {
	if ( typeof url !== "string" && _load ) {
		return _load.apply( this, arguments );
	}

	var selector, type, response,
		self = this,
		off = url.indexOf( " " );

	if ( off > -1 ) {
		selector = jQuery.trim( url.slice( off, url.length ) );
		url = url.slice( 0, off );
	}

	// If it's a function
	if ( jQuery.isFunction( params ) ) {

		// We assume that it's the callback
		callback = params;
		params = undefined;

	// Otherwise, build a param string
	} else if ( params && typeof params === "object" ) {
		type = "POST";
	}

	// If we have elements to modify, make the request
	if ( self.length > 0 ) {
		jQuery.ajax( {
			url: url,

			// If "type" variable is undefined, then "GET" method will be used.
			// Make value of this field explicit since
			// user can override it through ajaxSetup method
			type: type || "GET",
			dataType: "html",
			data: params
		} ).done( function( responseText ) {

			// Save response for use in complete callback
			response = arguments;

			self.html( selector ?

				// If a selector was specified, locate the right elements in a dummy div
				// Exclude scripts to avoid IE 'Permission Denied' errors
				jQuery( "<div>" ).append( jQuery.parseHTML( responseText ) ).find( selector ) :

				// Otherwise use the full result
				responseText );

		// If the request succeeds, this function gets "data", "status", "jqXHR"
		// but they are ignored because response was set above.
		// If it fails, this function gets "jqXHR", "status", "error"
		} ).always( callback && function( jqXHR, status ) {
			self.each( function() {
				callback.apply( this, response || [ jqXHR.responseText, status, jqXHR ] );
			} );
		} );
	}

	return this;
};




// Attach a bunch of functions for handling common AJAX events
jQuery.each( [
	"ajaxStart",
	"ajaxStop",
	"ajaxComplete",
	"ajaxError",
	"ajaxSuccess",
	"ajaxSend"
], function( i, type ) {
	jQuery.fn[ type ] = function( fn ) {
		return this.on( type, fn );
	};
} );




jQuery.expr.filters.animated = function( elem ) {
	return jQuery.grep( jQuery.timers, function( fn ) {
		return elem === fn.elem;
	} ).length;
};





/**
 * Gets a window from an element
 */
function getWindow( elem ) {
	return jQuery.isWindow( elem ) ?
		elem :
		elem.nodeType === 9 ?
			elem.defaultView || elem.parentWindow :
			false;
}

jQuery.offset = {
	setOffset: function( elem, options, i ) {
		var curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition,
			position = jQuery.css( elem, "position" ),
			curElem = jQuery( elem ),
			props = {};

		// set position first, in-case top/left are set even on static elem
		if ( position === "static" ) {
			elem.style.position = "relative";
		}

		curOffset = curElem.offset();
		curCSSTop = jQuery.css( elem, "top" );
		curCSSLeft = jQuery.css( elem, "left" );
		calculatePosition = ( position === "absolute" || position === "fixed" ) &&
			jQuery.inArray( "auto", [ curCSSTop, curCSSLeft ] ) > -1;

		// need to be able to calculate position if either top or left
		// is auto and position is either absolute or fixed
		if ( calculatePosition ) {
			curPosition = curElem.position();
			curTop = curPosition.top;
			curLeft = curPosition.left;
		} else {
			curTop = parseFloat( curCSSTop ) || 0;
			curLeft = parseFloat( curCSSLeft ) || 0;
		}

		if ( jQuery.isFunction( options ) ) {

			// Use jQuery.extend here to allow modification of coordinates argument (gh-1848)
			options = options.call( elem, i, jQuery.extend( {}, curOffset ) );
		}

		if ( options.top != null ) {
			props.top = ( options.top - curOffset.top ) + curTop;
		}
		if ( options.left != null ) {
			props.left = ( options.left - curOffset.left ) + curLeft;
		}

		if ( "using" in options ) {
			options.using.call( elem, props );
		} else {
			curElem.css( props );
		}
	}
};

jQuery.fn.extend( {
	offset: function( options ) {
		if ( arguments.length ) {
			return options === undefined ?
				this :
				this.each( function( i ) {
					jQuery.offset.setOffset( this, options, i );
				} );
		}

		var docElem, win,
			box = { top: 0, left: 0 },
			elem = this[ 0 ],
			doc = elem && elem.ownerDocument;

		if ( !doc ) {
			return;
		}

		docElem = doc.documentElement;

		// Make sure it's not a disconnected DOM node
		if ( !jQuery.contains( docElem, elem ) ) {
			return box;
		}

		// If we don't have gBCR, just use 0,0 rather than error
		// BlackBerry 5, iOS 3 (original iPhone)
		if ( typeof elem.getBoundingClientRect !== "undefined" ) {
			box = elem.getBoundingClientRect();
		}
		win = getWindow( doc );
		return {
			top: box.top  + ( win.pageYOffset || docElem.scrollTop )  - ( docElem.clientTop  || 0 ),
			left: box.left + ( win.pageXOffset || docElem.scrollLeft ) - ( docElem.clientLeft || 0 )
		};
	},

	position: function() {
		if ( !this[ 0 ] ) {
			return;
		}

		var offsetParent, offset,
			parentOffset = { top: 0, left: 0 },
			elem = this[ 0 ];

		// Fixed elements are offset from window (parentOffset = {top:0, left: 0},
		// because it is its only offset parent
		if ( jQuery.css( elem, "position" ) === "fixed" ) {

			// we assume that getBoundingClientRect is available when computed position is fixed
			offset = elem.getBoundingClientRect();
		} else {

			// Get *real* offsetParent
			offsetParent = this.offsetParent();

			// Get correct offsets
			offset = this.offset();
			if ( !jQuery.nodeName( offsetParent[ 0 ], "html" ) ) {
				parentOffset = offsetParent.offset();
			}

			// Add offsetParent borders
			parentOffset.top  += jQuery.css( offsetParent[ 0 ], "borderTopWidth", true );
			parentOffset.left += jQuery.css( offsetParent[ 0 ], "borderLeftWidth", true );
		}

		// Subtract parent offsets and element margins
		// note: when an element has margin: auto the offsetLeft and marginLeft
		// are the same in Safari causing offset.left to incorrectly be 0
		return {
			top:  offset.top  - parentOffset.top - jQuery.css( elem, "marginTop", true ),
			left: offset.left - parentOffset.left - jQuery.css( elem, "marginLeft", true )
		};
	},

	offsetParent: function() {
		return this.map( function() {
			var offsetParent = this.offsetParent;

			while ( offsetParent && ( !jQuery.nodeName( offsetParent, "html" ) &&
				jQuery.css( offsetParent, "position" ) === "static" ) ) {
				offsetParent = offsetParent.offsetParent;
			}
			return offsetParent || documentElement;
		} );
	}
} );

// Create scrollLeft and scrollTop methods
jQuery.each( { scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function( method, prop ) {
	var top = /Y/.test( prop );

	jQuery.fn[ method ] = function( val ) {
		return access( this, function( elem, method, val ) {
			var win = getWindow( elem );

			if ( val === undefined ) {
				return win ? ( prop in win ) ? win[ prop ] :
					win.document.documentElement[ method ] :
					elem[ method ];
			}

			if ( win ) {
				win.scrollTo(
					!top ? val : jQuery( win ).scrollLeft(),
					top ? val : jQuery( win ).scrollTop()
				);

			} else {
				elem[ method ] = val;
			}
		}, method, val, arguments.length, null );
	};
} );

// Support: Safari<7-8+, Chrome<37-44+
// Add the top/left cssHooks using jQuery.fn.position
// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084
// getComputedStyle returns percent when specified for top/left/bottom/right
// rather than make the css module depend on the offset module, we just check for it here
jQuery.each( [ "top", "left" ], function( i, prop ) {
	jQuery.cssHooks[ prop ] = addGetHookIf( support.pixelPosition,
		function( elem, computed ) {
			if ( computed ) {
				computed = curCSS( elem, prop );

				// if curCSS returns percentage, fallback to offset
				return rnumnonpx.test( computed ) ?
					jQuery( elem ).position()[ prop ] + "px" :
					computed;
			}
		}
	);
} );


// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods
jQuery.each( { Height: "height", Width: "width" }, function( name, type ) {
	jQuery.each( { padding: "inner" + name, content: type, "": "outer" + name },
	function( defaultExtra, funcName ) {

		// margin is only for outerHeight, outerWidth
		jQuery.fn[ funcName ] = function( margin, value ) {
			var chainable = arguments.length && ( defaultExtra || typeof margin !== "boolean" ),
				extra = defaultExtra || ( margin === true || value === true ? "margin" : "border" );

			return access( this, function( elem, type, value ) {
				var doc;

				if ( jQuery.isWindow( elem ) ) {

					// As of 5/8/2012 this will yield incorrect results for Mobile Safari, but there
					// isn't a whole lot we can do. See pull request at this URL for discussion:
					// https://github.com/jquery/jquery/pull/764
					return elem.document.documentElement[ "client" + name ];
				}

				// Get document width or height
				if ( elem.nodeType === 9 ) {
					doc = elem.documentElement;

					// Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height],
					// whichever is greatest
					// unfortunately, this causes bug #3838 in IE6/8 only,
					// but there is currently no good, small way to fix it.
					return Math.max(
						elem.body[ "scroll" + name ], doc[ "scroll" + name ],
						elem.body[ "offset" + name ], doc[ "offset" + name ],
						doc[ "client" + name ]
					);
				}

				return value === undefined ?

					// Get width or height on the element, requesting but not forcing parseFloat
					jQuery.css( elem, type, extra ) :

					// Set width or height on the element
					jQuery.style( elem, type, value, extra );
			}, type, chainable ? margin : undefined, chainable, null );
		};
	} );
} );


jQuery.fn.extend( {

	bind: function( types, data, fn ) {
		return this.on( types, null, data, fn );
	},
	unbind: function( types, fn ) {
		return this.off( types, null, fn );
	},

	delegate: function( selector, types, data, fn ) {
		return this.on( types, selector, data, fn );
	},
	undelegate: function( selector, types, fn ) {

		// ( namespace ) or ( selector, types [, fn] )
		return arguments.length === 1 ?
			this.off( selector, "**" ) :
			this.off( types, selector || "**", fn );
	}
} );

// The number of elements contained in the matched element set
jQuery.fn.size = function() {
	return this.length;
};

jQuery.fn.andSelf = jQuery.fn.addBack;




// Register as a named AMD module, since jQuery can be concatenated with other
// files that may use define, but not via a proper concatenation script that
// understands anonymous AMD modules. A named AMD is safest and most robust
// way to register. Lowercase jquery is used because AMD module names are
// derived from file names, and jQuery is normally delivered in a lowercase
// file name. Do this after creating the global so that if an AMD module wants
// to call noConflict to hide this version of jQuery, it will work.

// Note that for maximum portability, libraries that are not jQuery should
// declare themselves as anonymous modules, and avoid setting a global if an
// AMD loader is present. jQuery is a special case. For more information, see
// https://github.com/jrburke/requirejs/wiki/Updating-existing-libraries#wiki-anon

if ( typeof define === "function" && define.amd ) {
	define( "jquery", [], function() {
		return jQuery;
	} );
}



var

	// Map over jQuery in case of overwrite
	_jQuery = window.jQuery,

	// Map over the $ in case of overwrite
	_$ = window.$;

jQuery.noConflict = function( deep ) {
	if ( window.$ === jQuery ) {
		window.$ = _$;
	}

	if ( deep && window.jQuery === jQuery ) {
		window.jQuery = _jQuery;
	}

	return jQuery;
};

// Expose jQuery and $ identifiers, even in
// AMD (#7102#comment:10, https://github.com/jquery/jquery/pull/557)
// and CommonJS for browser emulators (#13566)
if ( !noGlobal ) {
	window.jQuery = window.$ = jQuery;
}

return jQuery;
}));
(function($, undefined) {

/**
 * Unobtrusive scripting adapter for jQuery
 * https://github.com/rails/jquery-ujs
 *
 * Requires jQuery 1.8.0 or later.
 *
 * Released under the MIT license
 *
 */

  // Cut down on the number of issues from people inadvertently including jquery_ujs twice
  // by detecting and raising an error when it happens.
  'use strict';

  if ( $.rails !== undefined ) {
    $.error('jquery-ujs has already been loaded!');
  }

  // Shorthand to make it a little easier to call public rails functions from within rails.js
  var rails;
  var $document = $(document);

  $.rails = rails = {
    // Link elements bound by jquery-ujs
    linkClickSelector: 'a[data-confirm], a[data-method], a[data-remote]:not([disabled]), a[data-disable-with], a[data-disable]',

    // Button elements bound by jquery-ujs
    buttonClickSelector: 'button[data-remote]:not([form]):not(form button), button[data-confirm]:not([form]):not(form button)',

    // Select elements bound by jquery-ujs
    inputChangeSelector: 'select[data-remote], input[data-remote], textarea[data-remote]',

    // Form elements bound by jquery-ujs
    formSubmitSelector: 'form',

    // Form input elements bound by jquery-ujs
    formInputClickSelector: 'form input[type=submit], form input[type=image], form button[type=submit], form button:not([type]), input[type=submit][form], input[type=image][form], button[type=submit][form], button[form]:not([type])',

    // Form input elements disabled during form submission
    disableSelector: 'input[data-disable-with]:enabled, button[data-disable-with]:enabled, textarea[data-disable-with]:enabled, input[data-disable]:enabled, button[data-disable]:enabled, textarea[data-disable]:enabled',

    // Form input elements re-enabled after form submission
    enableSelector: 'input[data-disable-with]:disabled, button[data-disable-with]:disabled, textarea[data-disable-with]:disabled, input[data-disable]:disabled, button[data-disable]:disabled, textarea[data-disable]:disabled',

    // Form required input elements
    requiredInputSelector: 'input[name][required]:not([disabled]), textarea[name][required]:not([disabled])',

    // Form file input elements
    fileInputSelector: 'input[name][type=file]:not([disabled])',

    // Link onClick disable selector with possible reenable after remote submission
    linkDisableSelector: 'a[data-disable-with], a[data-disable]',

    // Button onClick disable selector with possible reenable after remote submission
    buttonDisableSelector: 'button[data-remote][data-disable-with], button[data-remote][data-disable]',

    // Up-to-date Cross-Site Request Forgery token
    csrfToken: function() {
     return $('meta[name=csrf-token]').attr('content');
    },

    // URL param that must contain the CSRF token
    csrfParam: function() {
     return $('meta[name=csrf-param]').attr('content');
    },

    // Make sure that every Ajax request sends the CSRF token
    CSRFProtection: function(xhr) {
      var token = rails.csrfToken();
      if (token) xhr.setRequestHeader('X-CSRF-Token', token);
    },

    // Make sure that all forms have actual up-to-date tokens (cached forms contain old ones)
    refreshCSRFTokens: function(){
      $('form input[name="' + rails.csrfParam() + '"]').val(rails.csrfToken());
    },

    // Triggers an event on an element and returns false if the event result is false
    fire: function(obj, name, data) {
      var event = $.Event(name);
      obj.trigger(event, data);
      return event.result !== false;
    },

    // Default confirm dialog, may be overridden with custom confirm dialog in $.rails.confirm
    confirm: function(message) {
      return confirm(message);
    },

    // Default ajax function, may be overridden with custom function in $.rails.ajax
    ajax: function(options) {
      return $.ajax(options);
    },

    // Default way to get an element's href. May be overridden at $.rails.href.
    href: function(element) {
      return element[0].href;
    },

    // Checks "data-remote" if true to handle the request through a XHR request.
    isRemote: function(element) {
      return element.data('remote') !== undefined && element.data('remote') !== false;
    },

    // Submits "remote" forms and links with ajax
    handleRemote: function(element) {
      var method, url, data, withCredentials, dataType, options;

      if (rails.fire(element, 'ajax:before')) {
        withCredentials = element.data('with-credentials') || null;
        dataType = element.data('type') || ($.ajaxSettings && $.ajaxSettings.dataType);

        if (element.is('form')) {
          method = element.data('ujs:submit-button-formmethod') || element.attr('method');
          url = element.data('ujs:submit-button-formaction') || element.attr('action');
          data = $(element[0]).serializeArray();
          // memoized value from clicked submit button
          var button = element.data('ujs:submit-button');
          if (button) {
            data.push(button);
            element.data('ujs:submit-button', null);
          }
          element.data('ujs:submit-button-formmethod', null);
          element.data('ujs:submit-button-formaction', null);
        } else if (element.is(rails.inputChangeSelector)) {
          method = element.data('method');
          url = element.data('url');
          data = element.serialize();
          if (element.data('params')) data = data + '&' + element.data('params');
        } else if (element.is(rails.buttonClickSelector)) {
          method = element.data('method') || 'get';
          url = element.data('url');
          data = element.serialize();
          if (element.data('params')) data = data + '&' + element.data('params');
        } else {
          method = element.data('method');
          url = rails.href(element);
          data = element.data('params') || null;
        }

        options = {
          type: method || 'GET', data: data, dataType: dataType,
          // stopping the "ajax:beforeSend" event will cancel the ajax request
          beforeSend: function(xhr, settings) {
            if (settings.dataType === undefined) {
              xhr.setRequestHeader('accept', '*/*;q=0.5, ' + settings.accepts.script);
            }
            if (rails.fire(element, 'ajax:beforeSend', [xhr, settings])) {
              element.trigger('ajax:send', xhr);
            } else {
              return false;
            }
          },
          success: function(data, status, xhr) {
            element.trigger('ajax:success', [data, status, xhr]);
          },
          complete: function(xhr, status) {
            element.trigger('ajax:complete', [xhr, status]);
          },
          error: function(xhr, status, error) {
            element.trigger('ajax:error', [xhr, status, error]);
          },
          crossDomain: rails.isCrossDomain(url)
        };

        // There is no withCredentials for IE6-8 when
        // "Enable native XMLHTTP support" is disabled
        if (withCredentials) {
          options.xhrFields = {
            withCredentials: withCredentials
          };
        }

        // Only pass url to `ajax` options if not blank
        if (url) { options.url = url; }

        return rails.ajax(options);
      } else {
        return false;
      }
    },

    // Determines if the request is a cross domain request.
    isCrossDomain: function(url) {
      var originAnchor = document.createElement('a');
      originAnchor.href = location.href;
      var urlAnchor = document.createElement('a');

      try {
        urlAnchor.href = url;
        // This is a workaround to a IE bug.
        urlAnchor.href = urlAnchor.href;

        // If URL protocol is false or is a string containing a single colon
        // *and* host are false, assume it is not a cross-domain request
        // (should only be the case for IE7 and IE compatibility mode).
        // Otherwise, evaluate protocol and host of the URL against the origin
        // protocol and host.
        return !(((!urlAnchor.protocol || urlAnchor.protocol === ':') && !urlAnchor.host) ||
          (originAnchor.protocol + '//' + originAnchor.host ===
            urlAnchor.protocol + '//' + urlAnchor.host));
      } catch (e) {
        // If there is an error parsing the URL, assume it is crossDomain.
        return true;
      }
    },

    // Handles "data-method" on links such as:
    // <a href="/users/5" data-method="delete" rel="nofollow" data-confirm="Are you sure?">Delete</a>
    handleMethod: function(link) {
      var href = rails.href(link),
        method = link.data('method'),
        target = link.attr('target'),
        csrfToken = rails.csrfToken(),
        csrfParam = rails.csrfParam(),
        form = $('<form method="post" action="' + href + '"></form>'),
        metadataInput = '<input name="_method" value="' + method + '" type="hidden" />';

      if (csrfParam !== undefined && csrfToken !== undefined && !rails.isCrossDomain(href)) {
        metadataInput += '<input name="' + csrfParam + '" value="' + csrfToken + '" type="hidden" />';
      }

      if (target) { form.attr('target', target); }

      form.hide().append(metadataInput).appendTo('body');
      form.submit();
    },

    // Helper function that returns form elements that match the specified CSS selector
    // If form is actually a "form" element this will return associated elements outside the from that have
    // the html form attribute set
    formElements: function(form, selector) {
      return form.is('form') ? $(form[0].elements).filter(selector) : form.find(selector);
    },

    /* Disables form elements:
      - Caches element value in 'ujs:enable-with' data store
      - Replaces element text with value of 'data-disable-with' attribute
      - Sets disabled property to true
    */
    disableFormElements: function(form) {
      rails.formElements(form, rails.disableSelector).each(function() {
        rails.disableFormElement($(this));
      });
    },

    disableFormElement: function(element) {
      var method, replacement;

      method = element.is('button') ? 'html' : 'val';
      replacement = element.data('disable-with');

      if (replacement !== undefined) {
        element.data('ujs:enable-with', element[method]());
        element[method](replacement);
      }

      element.prop('disabled', true);
      element.data('ujs:disabled', true);
    },

    /* Re-enables disabled form elements:
      - Replaces element text with cached value from 'ujs:enable-with' data store (created in `disableFormElements`)
      - Sets disabled property to false
    */
    enableFormElements: function(form) {
      rails.formElements(form, rails.enableSelector).each(function() {
        rails.enableFormElement($(this));
      });
    },

    enableFormElement: function(element) {
      var method = element.is('button') ? 'html' : 'val';
      if (element.data('ujs:enable-with') !== undefined) {
        element[method](element.data('ujs:enable-with'));
        element.removeData('ujs:enable-with'); // clean up cache
      }
      element.prop('disabled', false);
      element.removeData('ujs:disabled');
    },

   /* For 'data-confirm' attribute:
      - Fires `confirm` event
      - Shows the confirmation dialog
      - Fires the `confirm:complete` event

      Returns `true` if no function stops the chain and user chose yes; `false` otherwise.
      Attaching a handler to the element's `confirm` event that returns a `falsy` value cancels the confirmation dialog.
      Attaching a handler to the element's `confirm:complete` event that returns a `falsy` value makes this function
      return false. The `confirm:complete` event is fired whether or not the user answered true or false to the dialog.
   */
    allowAction: function(element) {
      var message = element.data('confirm'),
          answer = false, callback;
      if (!message) { return true; }

      if (rails.fire(element, 'confirm')) {
        try {
          answer = rails.confirm(message);
        } catch (e) {
          (console.error || console.log).call(console, e.stack || e);
        }
        callback = rails.fire(element, 'confirm:complete', [answer]);
      }
      return answer && callback;
    },

    // Helper function which checks for blank inputs in a form that match the specified CSS selector
    blankInputs: function(form, specifiedSelector, nonBlank) {
      var foundInputs = $(),
        input,
        valueToCheck,
        radiosForNameWithNoneSelected,
        radioName,
        selector = specifiedSelector || 'input,textarea',
        requiredInputs = form.find(selector),
        checkedRadioButtonNames = {};

      requiredInputs.each(function() {
        input = $(this);
        if (input.is('input[type=radio]')) {

          // Don't count unchecked required radio as blank if other radio with same name is checked,
          // regardless of whether same-name radio input has required attribute or not. The spec
          // states https://www.w3.org/TR/html5/forms.html#the-required-attribute
          radioName = input.attr('name');

          // Skip if we've already seen the radio with this name.
          if (!checkedRadioButtonNames[radioName]) {

            // If none checked
            if (form.find('input[type=radio]:checked[name="' + radioName + '"]').length === 0) {
              radiosForNameWithNoneSelected = form.find(
                'input[type=radio][name="' + radioName + '"]');
              foundInputs = foundInputs.add(radiosForNameWithNoneSelected);
            }

            // We only need to check each name once.
            checkedRadioButtonNames[radioName] = radioName;
          }
        } else {
          valueToCheck = input.is('input[type=checkbox],input[type=radio]') ? input.is(':checked') : !!input.val();
          if (valueToCheck === nonBlank) {
            foundInputs = foundInputs.add(input);
          }
        }
      });
      return foundInputs.length ? foundInputs : false;
    },

    // Helper function which checks for non-blank inputs in a form that match the specified CSS selector
    nonBlankInputs: function(form, specifiedSelector) {
      return rails.blankInputs(form, specifiedSelector, true); // true specifies nonBlank
    },

    // Helper function, needed to provide consistent behavior in IE
    stopEverything: function(e) {
      $(e.target).trigger('ujs:everythingStopped');
      e.stopImmediatePropagation();
      return false;
    },

    //  Replace element's html with the 'data-disable-with' after storing original html
    //  and prevent clicking on it
    disableElement: function(element) {
      var replacement = element.data('disable-with');

      if (replacement !== undefined) {
        element.data('ujs:enable-with', element.html()); // store enabled state
        element.html(replacement);
      }

      element.bind('click.railsDisable', function(e) { // prevent further clicking
        return rails.stopEverything(e);
      });
      element.data('ujs:disabled', true);
    },

    // Restore element to its original state which was disabled by 'disableElement' above
    enableElement: function(element) {
      if (element.data('ujs:enable-with') !== undefined) {
        element.html(element.data('ujs:enable-with')); // set to old enabled state
        element.removeData('ujs:enable-with'); // clean up cache
      }
      element.unbind('click.railsDisable'); // enable element
      element.removeData('ujs:disabled');
    }
  };

  if (rails.fire($document, 'rails:attachBindings')) {

    $.ajaxPrefilter(function(options, originalOptions, xhr){ if ( !options.crossDomain ) { rails.CSRFProtection(xhr); }});

    // This event works the same as the load event, except that it fires every
    // time the page is loaded.
    //
    // See https://github.com/rails/jquery-ujs/issues/357
    // See https://developer.mozilla.org/en-US/docs/Using_Firefox_1.5_caching
    $(window).on('pageshow.rails', function () {
      $($.rails.enableSelector).each(function () {
        var element = $(this);

        if (element.data('ujs:disabled')) {
          $.rails.enableFormElement(element);
        }
      });

      $($.rails.linkDisableSelector).each(function () {
        var element = $(this);

        if (element.data('ujs:disabled')) {
          $.rails.enableElement(element);
        }
      });
    });

    $document.on('ajax:complete', rails.linkDisableSelector, function() {
        rails.enableElement($(this));
    });

    $document.on('ajax:complete', rails.buttonDisableSelector, function() {
        rails.enableFormElement($(this));
    });

    $document.on('click.rails', rails.linkClickSelector, function(e) {
      var link = $(this), method = link.data('method'), data = link.data('params'), metaClick = e.metaKey || e.ctrlKey;
      if (!rails.allowAction(link)) return rails.stopEverything(e);

      if (!metaClick && link.is(rails.linkDisableSelector)) rails.disableElement(link);

      if (rails.isRemote(link)) {
        if (metaClick && (!method || method === 'GET') && !data) { return true; }

        var handleRemote = rails.handleRemote(link);
        // Response from rails.handleRemote() will either be false or a deferred object promise.
        if (handleRemote === false) {
          rails.enableElement(link);
        } else {
          handleRemote.fail( function() { rails.enableElement(link); } );
        }
        return false;

      } else if (method) {
        rails.handleMethod(link);
        return false;
      }
    });

    $document.on('click.rails', rails.buttonClickSelector, function(e) {
      var button = $(this);

      if (!rails.allowAction(button) || !rails.isRemote(button)) return rails.stopEverything(e);

      if (button.is(rails.buttonDisableSelector)) rails.disableFormElement(button);

      var handleRemote = rails.handleRemote(button);
      // Response from rails.handleRemote() will either be false or a deferred object promise.
      if (handleRemote === false) {
        rails.enableFormElement(button);
      } else {
        handleRemote.fail( function() { rails.enableFormElement(button); } );
      }
      return false;
    });

    $document.on('change.rails', rails.inputChangeSelector, function(e) {
      var link = $(this);
      if (!rails.allowAction(link) || !rails.isRemote(link)) return rails.stopEverything(e);

      rails.handleRemote(link);
      return false;
    });

    $document.on('submit.rails', rails.formSubmitSelector, function(e) {
      var form = $(this),
        remote = rails.isRemote(form),
        blankRequiredInputs,
        nonBlankFileInputs;

      if (!rails.allowAction(form)) return rails.stopEverything(e);

      // Skip other logic when required values are missing or file upload is present
      if (form.attr('novalidate') === undefined) {
        if (form.data('ujs:formnovalidate-button') === undefined) {
          blankRequiredInputs = rails.blankInputs(form, rails.requiredInputSelector, false);
          if (blankRequiredInputs && rails.fire(form, 'ajax:aborted:required', [blankRequiredInputs])) {
            return rails.stopEverything(e);
          }
        } else {
          // Clear the formnovalidate in case the next button click is not on a formnovalidate button
          // Not strictly necessary to do here, since it is also reset on each button click, but just to be certain
          form.data('ujs:formnovalidate-button', undefined);
        }
      }

      if (remote) {
        nonBlankFileInputs = rails.nonBlankInputs(form, rails.fileInputSelector);
        if (nonBlankFileInputs) {
          // Slight timeout so that the submit button gets properly serialized
          // (make it easy for event handler to serialize form without disabled values)
          setTimeout(function(){ rails.disableFormElements(form); }, 13);
          var aborted = rails.fire(form, 'ajax:aborted:file', [nonBlankFileInputs]);

          // Re-enable form elements if event bindings return false (canceling normal form submission)
          if (!aborted) { setTimeout(function(){ rails.enableFormElements(form); }, 13); }

          return aborted;
        }

        rails.handleRemote(form);
        return false;

      } else {
        // Slight timeout so that the submit button gets properly serialized
        setTimeout(function(){ rails.disableFormElements(form); }, 13);
      }
    });

    $document.on('click.rails', rails.formInputClickSelector, function(event) {
      var button = $(this);

      if (!rails.allowAction(button)) return rails.stopEverything(event);

      // Register the pressed submit button
      var name = button.attr('name'),
        data = name ? {name:name, value:button.val()} : null;

      var form = button.closest('form');
      if (form.length === 0) {
        form = $('#' + button.attr('form'));
      }
      form.data('ujs:submit-button', data);

      // Save attributes from button
      form.data('ujs:formnovalidate-button', button.attr('formnovalidate'));
      form.data('ujs:submit-button-formaction', button.attr('formaction'));
      form.data('ujs:submit-button-formmethod', button.attr('formmethod'));
    });

    $document.on('ajax:send.rails', rails.formSubmitSelector, function(event) {
      if (this === event.target) rails.disableFormElements($(this));
    });

    $document.on('ajax:complete.rails', rails.formSubmitSelector, function(event) {
      if (this === event.target) rails.enableFormElements($(this));
    });

    $(function(){
      rails.refreshCSRFTokens();
    });
  }

})( jQuery );
/*!
 * Bootstrap v3.3.5 (http://getbootstrap.com)
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under the MIT license
 */


if (typeof jQuery === 'undefined') {
  throw new Error('Bootstrap\'s JavaScript requires jQuery')
}

+function ($) {
  'use strict';
  var version = $.fn.jquery.split(' ')[0].split('.')
  if ((version[0] < 2 && version[1] < 9) || (version[0] == 1 && version[1] == 9 && version[2] < 1)) {
    throw new Error('Bootstrap\'s JavaScript requires jQuery version 1.9.1 or higher')
  }
}(jQuery);

/* ========================================================================
 * Bootstrap: transition.js v3.3.5
 * http://getbootstrap.com/javascript/#transitions
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // CSS TRANSITION SUPPORT (Shoutout: http://www.modernizr.com/)
  // ============================================================

  function transitionEnd() {
    var el = document.createElement('bootstrap')

    var transEndEventNames = {
      WebkitTransition : 'webkitTransitionEnd',
      MozTransition    : 'transitionend',
      OTransition      : 'oTransitionEnd otransitionend',
      transition       : 'transitionend'
    }

    for (var name in transEndEventNames) {
      if (el.style[name] !== undefined) {
        return { end: transEndEventNames[name] }
      }
    }

    return false // explicit for ie8 (  ._.)
  }

  // http://blog.alexmaccaw.com/css-transitions
  $.fn.emulateTransitionEnd = function (duration) {
    var called = false
    var $el = this
    $(this).one('bsTransitionEnd', function () { called = true })
    var callback = function () { if (!called) $($el).trigger($.support.transition.end) }
    setTimeout(callback, duration)
    return this
  }

  $(function () {
    $.support.transition = transitionEnd()

    if (!$.support.transition) return

    $.event.special.bsTransitionEnd = {
      bindType: $.support.transition.end,
      delegateType: $.support.transition.end,
      handle: function (e) {
        if ($(e.target).is(this)) return e.handleObj.handler.apply(this, arguments)
      }
    }
  })

}(jQuery);

/* ========================================================================
 * Bootstrap: alert.js v3.3.5
 * http://getbootstrap.com/javascript/#alerts
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // ALERT CLASS DEFINITION
  // ======================

  var dismiss = '[data-dismiss="alert"]'
  var Alert   = function (el) {
    $(el).on('click', dismiss, this.close)
  }

  Alert.VERSION = '3.3.5'

  Alert.TRANSITION_DURATION = 150

  Alert.prototype.close = function (e) {
    var $this    = $(this)
    var selector = $this.attr('data-target')

    if (!selector) {
      selector = $this.attr('href')
      selector = selector && selector.replace(/.*(?=#[^\s]*$)/, '') // strip for ie7
    }

    var $parent = $(selector)

    if (e) e.preventDefault()

    if (!$parent.length) {
      $parent = $this.closest('.alert')
    }

    $parent.trigger(e = $.Event('close.bs.alert'))

    if (e.isDefaultPrevented()) return

    $parent.removeClass('in')

    function removeElement() {
      // detach from parent, fire event then clean up data
      $parent.detach().trigger('closed.bs.alert').remove()
    }

    $.support.transition && $parent.hasClass('fade') ?
      $parent
        .one('bsTransitionEnd', removeElement)
        .emulateTransitionEnd(Alert.TRANSITION_DURATION) :
      removeElement()
  }


  // ALERT PLUGIN DEFINITION
  // =======================

  function Plugin(option) {
    return this.each(function () {
      var $this = $(this)
      var data  = $this.data('bs.alert')

      if (!data) $this.data('bs.alert', (data = new Alert(this)))
      if (typeof option == 'string') data[option].call($this)
    })
  }

  var old = $.fn.alert

  $.fn.alert             = Plugin
  $.fn.alert.Constructor = Alert


  // ALERT NO CONFLICT
  // =================

  $.fn.alert.noConflict = function () {
    $.fn.alert = old
    return this
  }


  // ALERT DATA-API
  // ==============

  $(document).on('click.bs.alert.data-api', dismiss, Alert.prototype.close)

}(jQuery);

/* ========================================================================
 * Bootstrap: button.js v3.3.5
 * http://getbootstrap.com/javascript/#buttons
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // BUTTON PUBLIC CLASS DEFINITION
  // ==============================

  var Button = function (element, options) {
    this.$element  = $(element)
    this.options   = $.extend({}, Button.DEFAULTS, options)
    this.isLoading = false
  }

  Button.VERSION  = '3.3.5'

  Button.DEFAULTS = {
    loadingText: 'loading...'
  }

  Button.prototype.setState = function (state) {
    var d    = 'disabled'
    var $el  = this.$element
    var val  = $el.is('input') ? 'val' : 'html'
    var data = $el.data()

    state += 'Text'

    if (data.resetText == null) $el.data('resetText', $el[val]())

    // push to event loop to allow forms to submit
    setTimeout($.proxy(function () {
      $el[val](data[state] == null ? this.options[state] : data[state])

      if (state == 'loadingText') {
        this.isLoading = true
        $el.addClass(d).attr(d, d)
      } else if (this.isLoading) {
        this.isLoading = false
        $el.removeClass(d).removeAttr(d)
      }
    }, this), 0)
  }

  Button.prototype.toggle = function () {
    var changed = true
    var $parent = this.$element.closest('[data-toggle="buttons"]')

    if ($parent.length) {
      var $input = this.$element.find('input')
      if ($input.prop('type') == 'radio') {
        if ($input.prop('checked')) changed = false
        $parent.find('.active').removeClass('active')
        this.$element.addClass('active')
      } else if ($input.prop('type') == 'checkbox') {
        if (($input.prop('checked')) !== this.$element.hasClass('active')) changed = false
        this.$element.toggleClass('active')
      }
      $input.prop('checked', this.$element.hasClass('active'))
      if (changed) $input.trigger('change')
    } else {
      this.$element.attr('aria-pressed', !this.$element.hasClass('active'))
      this.$element.toggleClass('active')
    }
  }


  // BUTTON PLUGIN DEFINITION
  // ========================

  function Plugin(option) {
    return this.each(function () {
      var $this   = $(this)
      var data    = $this.data('bs.button')
      var options = typeof option == 'object' && option

      if (!data) $this.data('bs.button', (data = new Button(this, options)))

      if (option == 'toggle') data.toggle()
      else if (option) data.setState(option)
    })
  }

  var old = $.fn.button

  $.fn.button             = Plugin
  $.fn.button.Constructor = Button


  // BUTTON NO CONFLICT
  // ==================

  $.fn.button.noConflict = function () {
    $.fn.button = old
    return this
  }


  // BUTTON DATA-API
  // ===============

  $(document)
    .on('click.bs.button.data-api', '[data-toggle^="button"]', function (e) {
      var $btn = $(e.target)
      if (!$btn.hasClass('btn')) $btn = $btn.closest('.btn')
      Plugin.call($btn, 'toggle')
      if (!($(e.target).is('input[type="radio"]') || $(e.target).is('input[type="checkbox"]'))) e.preventDefault()
    })
    .on('focus.bs.button.data-api blur.bs.button.data-api', '[data-toggle^="button"]', function (e) {
      $(e.target).closest('.btn').toggleClass('focus', /^focus(in)?$/.test(e.type))
    })

}(jQuery);

/* ========================================================================
 * Bootstrap: carousel.js v3.3.5
 * http://getbootstrap.com/javascript/#carousel
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // CAROUSEL CLASS DEFINITION
  // =========================

  var Carousel = function (element, options) {
    this.$element    = $(element)
    this.$indicators = this.$element.find('.carousel-indicators')
    this.options     = options
    this.paused      = null
    this.sliding     = null
    this.interval    = null
    this.$active     = null
    this.$items      = null

    this.options.keyboard && this.$element.on('keydown.bs.carousel', $.proxy(this.keydown, this))

    this.options.pause == 'hover' && !('ontouchstart' in document.documentElement) && this.$element
      .on('mouseenter.bs.carousel', $.proxy(this.pause, this))
      .on('mouseleave.bs.carousel', $.proxy(this.cycle, this))
  }

  Carousel.VERSION  = '3.3.5'

  Carousel.TRANSITION_DURATION = 600

  Carousel.DEFAULTS = {
    interval: 5000,
    pause: 'hover',
    wrap: true,
    keyboard: true
  }

  Carousel.prototype.keydown = function (e) {
    if (/input|textarea/i.test(e.target.tagName)) return
    switch (e.which) {
      case 37: this.prev(); break
      case 39: this.next(); break
      default: return
    }

    e.preventDefault()
  }

  Carousel.prototype.cycle = function (e) {
    e || (this.paused = false)

    this.interval && clearInterval(this.interval)

    this.options.interval
      && !this.paused
      && (this.interval = setInterval($.proxy(this.next, this), this.options.interval))

    return this
  }

  Carousel.prototype.getItemIndex = function (item) {
    this.$items = item.parent().children('.item')
    return this.$items.index(item || this.$active)
  }

  Carousel.prototype.getItemForDirection = function (direction, active) {
    var activeIndex = this.getItemIndex(active)
    var willWrap = (direction == 'prev' && activeIndex === 0)
                || (direction == 'next' && activeIndex == (this.$items.length - 1))
    if (willWrap && !this.options.wrap) return active
    var delta = direction == 'prev' ? -1 : 1
    var itemIndex = (activeIndex + delta) % this.$items.length
    return this.$items.eq(itemIndex)
  }

  Carousel.prototype.to = function (pos) {
    var that        = this
    var activeIndex = this.getItemIndex(this.$active = this.$element.find('.item.active'))

    if (pos > (this.$items.length - 1) || pos < 0) return

    if (this.sliding)       return this.$element.one('slid.bs.carousel', function () { that.to(pos) }) // yes, "slid"
    if (activeIndex == pos) return this.pause().cycle()

    return this.slide(pos > activeIndex ? 'next' : 'prev', this.$items.eq(pos))
  }

  Carousel.prototype.pause = function (e) {
    e || (this.paused = true)

    if (this.$element.find('.next, .prev').length && $.support.transition) {
      this.$element.trigger($.support.transition.end)
      this.cycle(true)
    }

    this.interval = clearInterval(this.interval)

    return this
  }

  Carousel.prototype.next = function () {
    if (this.sliding) return
    return this.slide('next')
  }

  Carousel.prototype.prev = function () {
    if (this.sliding) return
    return this.slide('prev')
  }

  Carousel.prototype.slide = function (type, next) {
    var $active   = this.$element.find('.item.active')
    var $next     = next || this.getItemForDirection(type, $active)
    var isCycling = this.interval
    var direction = type == 'next' ? 'left' : 'right'
    var that      = this

    if ($next.hasClass('active')) return (this.sliding = false)

    var relatedTarget = $next[0]
    var slideEvent = $.Event('slide.bs.carousel', {
      relatedTarget: relatedTarget,
      direction: direction
    })
    this.$element.trigger(slideEvent)
    if (slideEvent.isDefaultPrevented()) return

    this.sliding = true

    isCycling && this.pause()

    if (this.$indicators.length) {
      this.$indicators.find('.active').removeClass('active')
      var $nextIndicator = $(this.$indicators.children()[this.getItemIndex($next)])
      $nextIndicator && $nextIndicator.addClass('active')
    }

    var slidEvent = $.Event('slid.bs.carousel', { relatedTarget: relatedTarget, direction: direction }) // yes, "slid"
    if ($.support.transition && this.$element.hasClass('slide')) {
      $next.addClass(type)
      $next[0].offsetWidth // force reflow
      $active.addClass(direction)
      $next.addClass(direction)
      $active
        .one('bsTransitionEnd', function () {
          $next.removeClass([type, direction].join(' ')).addClass('active')
          $active.removeClass(['active', direction].join(' '))
          that.sliding = false
          setTimeout(function () {
            that.$element.trigger(slidEvent)
          }, 0)
        })
        .emulateTransitionEnd(Carousel.TRANSITION_DURATION)
    } else {
      $active.removeClass('active')
      $next.addClass('active')
      this.sliding = false
      this.$element.trigger(slidEvent)
    }

    isCycling && this.cycle()

    return this
  }


  // CAROUSEL PLUGIN DEFINITION
  // ==========================

  function Plugin(option) {
    return this.each(function () {
      var $this   = $(this)
      var data    = $this.data('bs.carousel')
      var options = $.extend({}, Carousel.DEFAULTS, $this.data(), typeof option == 'object' && option)
      var action  = typeof option == 'string' ? option : options.slide

      if (!data) $this.data('bs.carousel', (data = new Carousel(this, options)))
      if (typeof option == 'number') data.to(option)
      else if (action) data[action]()
      else if (options.interval) data.pause().cycle()
    })
  }

  var old = $.fn.carousel

  $.fn.carousel             = Plugin
  $.fn.carousel.Constructor = Carousel


  // CAROUSEL NO CONFLICT
  // ====================

  $.fn.carousel.noConflict = function () {
    $.fn.carousel = old
    return this
  }


  // CAROUSEL DATA-API
  // =================

  var clickHandler = function (e) {
    var href
    var $this   = $(this)
    var $target = $($this.attr('data-target') || (href = $this.attr('href')) && href.replace(/.*(?=#[^\s]+$)/, '')) // strip for ie7
    if (!$target.hasClass('carousel')) return
    var options = $.extend({}, $target.data(), $this.data())
    var slideIndex = $this.attr('data-slide-to')
    if (slideIndex) options.interval = false

    Plugin.call($target, options)

    if (slideIndex) {
      $target.data('bs.carousel').to(slideIndex)
    }

    e.preventDefault()
  }

  $(document)
    .on('click.bs.carousel.data-api', '[data-slide]', clickHandler)
    .on('click.bs.carousel.data-api', '[data-slide-to]', clickHandler)

  $(window).on('load', function () {
    $('[data-ride="carousel"]').each(function () {
      var $carousel = $(this)
      Plugin.call($carousel, $carousel.data())
    })
  })

}(jQuery);

/* ========================================================================
 * Bootstrap: collapse.js v3.3.5
 * http://getbootstrap.com/javascript/#collapse
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // COLLAPSE PUBLIC CLASS DEFINITION
  // ================================

  var Collapse = function (element, options) {
    this.$element      = $(element)
    this.options       = $.extend({}, Collapse.DEFAULTS, options)
    this.$trigger      = $('[data-toggle="collapse"][href="#' + element.id + '"],' +
                           '[data-toggle="collapse"][data-target="#' + element.id + '"]')
    this.transitioning = null

    if (this.options.parent) {
      this.$parent = this.getParent()
    } else {
      this.addAriaAndCollapsedClass(this.$element, this.$trigger)
    }

    if (this.options.toggle) this.toggle()
  }

  Collapse.VERSION  = '3.3.5'

  Collapse.TRANSITION_DURATION = 350

  Collapse.DEFAULTS = {
    toggle: true
  }

  Collapse.prototype.dimension = function () {
    var hasWidth = this.$element.hasClass('width')
    return hasWidth ? 'width' : 'height'
  }

  Collapse.prototype.show = function () {
    if (this.transitioning || this.$element.hasClass('in')) return

    var activesData
    var actives = this.$parent && this.$parent.children('.panel').children('.in, .collapsing')

    if (actives && actives.length) {
      activesData = actives.data('bs.collapse')
      if (activesData && activesData.transitioning) return
    }

    var startEvent = $.Event('show.bs.collapse')
    this.$element.trigger(startEvent)
    if (startEvent.isDefaultPrevented()) return

    if (actives && actives.length) {
      Plugin.call(actives, 'hide')
      activesData || actives.data('bs.collapse', null)
    }

    var dimension = this.dimension()

    this.$element
      .removeClass('collapse')
      .addClass('collapsing')[dimension](0)
      .attr('aria-expanded', true)

    this.$trigger
      .removeClass('collapsed')
      .attr('aria-expanded', true)

    this.transitioning = 1

    var complete = function () {
      this.$element
        .removeClass('collapsing')
        .addClass('collapse in')[dimension]('')
      this.transitioning = 0
      this.$element
        .trigger('shown.bs.collapse')
    }

    if (!$.support.transition) return complete.call(this)

    var scrollSize = $.camelCase(['scroll', dimension].join('-'))

    this.$element
      .one('bsTransitionEnd', $.proxy(complete, this))
      .emulateTransitionEnd(Collapse.TRANSITION_DURATION)[dimension](this.$element[0][scrollSize])
  }

  Collapse.prototype.hide = function () {
    if (this.transitioning || !this.$element.hasClass('in')) return

    var startEvent = $.Event('hide.bs.collapse')
    this.$element.trigger(startEvent)
    if (startEvent.isDefaultPrevented()) return

    var dimension = this.dimension()

    this.$element[dimension](this.$element[dimension]())[0].offsetHeight

    this.$element
      .addClass('collapsing')
      .removeClass('collapse in')
      .attr('aria-expanded', false)

    this.$trigger
      .addClass('collapsed')
      .attr('aria-expanded', false)

    this.transitioning = 1

    var complete = function () {
      this.transitioning = 0
      this.$element
        .removeClass('collapsing')
        .addClass('collapse')
        .trigger('hidden.bs.collapse')
    }

    if (!$.support.transition) return complete.call(this)

    this.$element
      [dimension](0)
      .one('bsTransitionEnd', $.proxy(complete, this))
      .emulateTransitionEnd(Collapse.TRANSITION_DURATION)
  }

  Collapse.prototype.toggle = function () {
    this[this.$element.hasClass('in') ? 'hide' : 'show']()
  }

  Collapse.prototype.getParent = function () {
    return $(this.options.parent)
      .find('[data-toggle="collapse"][data-parent="' + this.options.parent + '"]')
      .each($.proxy(function (i, element) {
        var $element = $(element)
        this.addAriaAndCollapsedClass(getTargetFromTrigger($element), $element)
      }, this))
      .end()
  }

  Collapse.prototype.addAriaAndCollapsedClass = function ($element, $trigger) {
    var isOpen = $element.hasClass('in')

    $element.attr('aria-expanded', isOpen)
    $trigger
      .toggleClass('collapsed', !isOpen)
      .attr('aria-expanded', isOpen)
  }

  function getTargetFromTrigger($trigger) {
    var href
    var target = $trigger.attr('data-target')
      || (href = $trigger.attr('href')) && href.replace(/.*(?=#[^\s]+$)/, '') // strip for ie7

    return $(target)
  }


  // COLLAPSE PLUGIN DEFINITION
  // ==========================

  function Plugin(option) {
    return this.each(function () {
      var $this   = $(this)
      var data    = $this.data('bs.collapse')
      var options = $.extend({}, Collapse.DEFAULTS, $this.data(), typeof option == 'object' && option)

      if (!data && options.toggle && /show|hide/.test(option)) options.toggle = false
      if (!data) $this.data('bs.collapse', (data = new Collapse(this, options)))
      if (typeof option == 'string') data[option]()
    })
  }

  var old = $.fn.collapse

  $.fn.collapse             = Plugin
  $.fn.collapse.Constructor = Collapse


  // COLLAPSE NO CONFLICT
  // ====================

  $.fn.collapse.noConflict = function () {
    $.fn.collapse = old
    return this
  }


  // COLLAPSE DATA-API
  // =================

  $(document).on('click.bs.collapse.data-api', '[data-toggle="collapse"]', function (e) {
    var $this   = $(this)

    if (!$this.attr('data-target')) e.preventDefault()

    var $target = getTargetFromTrigger($this)
    var data    = $target.data('bs.collapse')
    var option  = data ? 'toggle' : $this.data()

    Plugin.call($target, option)
  })

}(jQuery);

/* ========================================================================
 * Bootstrap: dropdown.js v3.3.5
 * http://getbootstrap.com/javascript/#dropdowns
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // DROPDOWN CLASS DEFINITION
  // =========================

  var backdrop = '.dropdown-backdrop'
  var toggle   = '[data-toggle="dropdown"]'
  var Dropdown = function (element) {
    $(element).on('click.bs.dropdown', this.toggle)
  }

  Dropdown.VERSION = '3.3.5'

  function getParent($this) {
    var selector = $this.attr('data-target')

    if (!selector) {
      selector = $this.attr('href')
      selector = selector && /#[A-Za-z]/.test(selector) && selector.replace(/.*(?=#[^\s]*$)/, '') // strip for ie7
    }

    var $parent = selector && $(selector)

    return $parent && $parent.length ? $parent : $this.parent()
  }

  function clearMenus(e) {
    if (e && e.which === 3) return
    $(backdrop).remove()
    $(toggle).each(function () {
      var $this         = $(this)
      var $parent       = getParent($this)
      var relatedTarget = { relatedTarget: this }

      if (!$parent.hasClass('open')) return

      if (e && e.type == 'click' && /input|textarea/i.test(e.target.tagName) && $.contains($parent[0], e.target)) return

      $parent.trigger(e = $.Event('hide.bs.dropdown', relatedTarget))

      if (e.isDefaultPrevented()) return

      $this.attr('aria-expanded', 'false')
      $parent.removeClass('open').trigger('hidden.bs.dropdown', relatedTarget)
    })
  }

  Dropdown.prototype.toggle = function (e) {
    var $this = $(this)

    if ($this.is('.disabled, :disabled')) return

    var $parent  = getParent($this)
    var isActive = $parent.hasClass('open')

    clearMenus()

    if (!isActive) {
      if ('ontouchstart' in document.documentElement && !$parent.closest('.navbar-nav').length) {
        // if mobile we use a backdrop because click events don't delegate
        $(document.createElement('div'))
          .addClass('dropdown-backdrop')
          .insertAfter($(this))
          .on('click', clearMenus)
      }

      var relatedTarget = { relatedTarget: this }
      $parent.trigger(e = $.Event('show.bs.dropdown', relatedTarget))

      if (e.isDefaultPrevented()) return

      $this
        .trigger('focus')
        .attr('aria-expanded', 'true')

      $parent
        .toggleClass('open')
        .trigger('shown.bs.dropdown', relatedTarget)
    }

    return false
  }

  Dropdown.prototype.keydown = function (e) {
    if (!/(38|40|27|32)/.test(e.which) || /input|textarea/i.test(e.target.tagName)) return

    var $this = $(this)

    e.preventDefault()
    e.stopPropagation()

    if ($this.is('.disabled, :disabled')) return

    var $parent  = getParent($this)
    var isActive = $parent.hasClass('open')

    if (!isActive && e.which != 27 || isActive && e.which == 27) {
      if (e.which == 27) $parent.find(toggle).trigger('focus')
      return $this.trigger('click')
    }

    var desc = ' li:not(.disabled):visible a'
    var $items = $parent.find('.dropdown-menu' + desc)

    if (!$items.length) return

    var index = $items.index(e.target)

    if (e.which == 38 && index > 0)                 index--         // up
    if (e.which == 40 && index < $items.length - 1) index++         // down
    if (!~index)                                    index = 0

    $items.eq(index).trigger('focus')
  }


  // DROPDOWN PLUGIN DEFINITION
  // ==========================

  function Plugin(option) {
    return this.each(function () {
      var $this = $(this)
      var data  = $this.data('bs.dropdown')

      if (!data) $this.data('bs.dropdown', (data = new Dropdown(this)))
      if (typeof option == 'string') data[option].call($this)
    })
  }

  var old = $.fn.dropdown

  $.fn.dropdown             = Plugin
  $.fn.dropdown.Constructor = Dropdown


  // DROPDOWN NO CONFLICT
  // ====================

  $.fn.dropdown.noConflict = function () {
    $.fn.dropdown = old
    return this
  }


  // APPLY TO STANDARD DROPDOWN ELEMENTS
  // ===================================

  $(document)
    .on('click.bs.dropdown.data-api', clearMenus)
    .on('click.bs.dropdown.data-api', '.dropdown form', function (e) { e.stopPropagation() })
    .on('click.bs.dropdown.data-api', toggle, Dropdown.prototype.toggle)
    .on('keydown.bs.dropdown.data-api', toggle, Dropdown.prototype.keydown)
    .on('keydown.bs.dropdown.data-api', '.dropdown-menu', Dropdown.prototype.keydown)

}(jQuery);

/* ========================================================================
 * Bootstrap: modal.js v3.3.5
 * http://getbootstrap.com/javascript/#modals
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // MODAL CLASS DEFINITION
  // ======================

  var Modal = function (element, options) {
    this.options             = options
    this.$body               = $(document.body)
    this.$element            = $(element)
    this.$dialog             = this.$element.find('.modal-dialog')
    this.$backdrop           = null
    this.isShown             = null
    this.originalBodyPad     = null
    this.scrollbarWidth      = 0
    this.ignoreBackdropClick = false

    if (this.options.remote) {
      this.$element
        .find('.modal-content')
        .load(this.options.remote, $.proxy(function () {
          this.$element.trigger('loaded.bs.modal')
        }, this))
    }
  }

  Modal.VERSION  = '3.3.5'

  Modal.TRANSITION_DURATION = 300
  Modal.BACKDROP_TRANSITION_DURATION = 150

  Modal.DEFAULTS = {
    backdrop: true,
    keyboard: true,
    show: true
  }

  Modal.prototype.toggle = function (_relatedTarget) {
    return this.isShown ? this.hide() : this.show(_relatedTarget)
  }

  Modal.prototype.show = function (_relatedTarget) {
    var that = this
    var e    = $.Event('show.bs.modal', { relatedTarget: _relatedTarget })

    this.$element.trigger(e)

    if (this.isShown || e.isDefaultPrevented()) return

    this.isShown = true

    this.checkScrollbar()
    this.setScrollbar()
    this.$body.addClass('modal-open')

    this.escape()
    this.resize()

    this.$element.on('click.dismiss.bs.modal', '[data-dismiss="modal"]', $.proxy(this.hide, this))

    this.$dialog.on('mousedown.dismiss.bs.modal', function () {
      that.$element.one('mouseup.dismiss.bs.modal', function (e) {
        if ($(e.target).is(that.$element)) that.ignoreBackdropClick = true
      })
    })

    this.backdrop(function () {
      var transition = $.support.transition && that.$element.hasClass('fade')

      if (!that.$element.parent().length) {
        that.$element.appendTo(that.$body) // don't move modals dom position
      }

      that.$element
        .show()
        .scrollTop(0)

      that.adjustDialog()

      if (transition) {
        that.$element[0].offsetWidth // force reflow
      }

      that.$element.addClass('in')

      that.enforceFocus()

      var e = $.Event('shown.bs.modal', { relatedTarget: _relatedTarget })

      transition ?
        that.$dialog // wait for modal to slide in
          .one('bsTransitionEnd', function () {
            that.$element.trigger('focus').trigger(e)
          })
          .emulateTransitionEnd(Modal.TRANSITION_DURATION) :
        that.$element.trigger('focus').trigger(e)
    })
  }

  Modal.prototype.hide = function (e) {
    if (e) e.preventDefault()

    e = $.Event('hide.bs.modal')

    this.$element.trigger(e)

    if (!this.isShown || e.isDefaultPrevented()) return

    this.isShown = false

    this.escape()
    this.resize()

    $(document).off('focusin.bs.modal')

    this.$element
      .removeClass('in')
      .off('click.dismiss.bs.modal')
      .off('mouseup.dismiss.bs.modal')

    this.$dialog.off('mousedown.dismiss.bs.modal')

    $.support.transition && this.$element.hasClass('fade') ?
      this.$element
        .one('bsTransitionEnd', $.proxy(this.hideModal, this))
        .emulateTransitionEnd(Modal.TRANSITION_DURATION) :
      this.hideModal()
  }

  Modal.prototype.enforceFocus = function () {
    $(document)
      .off('focusin.bs.modal') // guard against infinite focus loop
      .on('focusin.bs.modal', $.proxy(function (e) {
        if (this.$element[0] !== e.target && !this.$element.has(e.target).length) {
          this.$element.trigger('focus')
        }
      }, this))
  }

  Modal.prototype.escape = function () {
    if (this.isShown && this.options.keyboard) {
      this.$element.on('keydown.dismiss.bs.modal', $.proxy(function (e) {
        e.which == 27 && this.hide()
      }, this))
    } else if (!this.isShown) {
      this.$element.off('keydown.dismiss.bs.modal')
    }
  }

  Modal.prototype.resize = function () {
    if (this.isShown) {
      $(window).on('resize.bs.modal', $.proxy(this.handleUpdate, this))
    } else {
      $(window).off('resize.bs.modal')
    }
  }

  Modal.prototype.hideModal = function () {
    var that = this
    this.$element.hide()
    this.backdrop(function () {
      that.$body.removeClass('modal-open')
      that.resetAdjustments()
      that.resetScrollbar()
      that.$element.trigger('hidden.bs.modal')
    })
  }

  Modal.prototype.removeBackdrop = function () {
    this.$backdrop && this.$backdrop.remove()
    this.$backdrop = null
  }

  Modal.prototype.backdrop = function (callback) {
    var that = this
    var animate = this.$element.hasClass('fade') ? 'fade' : ''

    if (this.isShown && this.options.backdrop) {
      var doAnimate = $.support.transition && animate

      this.$backdrop = $(document.createElement('div'))
        .addClass('modal-backdrop ' + animate)
        .appendTo(this.$body)

      this.$element.on('click.dismiss.bs.modal', $.proxy(function (e) {
        if (this.ignoreBackdropClick) {
          this.ignoreBackdropClick = false
          return
        }
        if (e.target !== e.currentTarget) return
        this.options.backdrop == 'static'
          ? this.$element[0].focus()
          : this.hide()
      }, this))

      if (doAnimate) this.$backdrop[0].offsetWidth // force reflow

      this.$backdrop.addClass('in')

      if (!callback) return

      doAnimate ?
        this.$backdrop
          .one('bsTransitionEnd', callback)
          .emulateTransitionEnd(Modal.BACKDROP_TRANSITION_DURATION) :
        callback()

    } else if (!this.isShown && this.$backdrop) {
      this.$backdrop.removeClass('in')

      var callbackRemove = function () {
        that.removeBackdrop()
        callback && callback()
      }
      $.support.transition && this.$element.hasClass('fade') ?
        this.$backdrop
          .one('bsTransitionEnd', callbackRemove)
          .emulateTransitionEnd(Modal.BACKDROP_TRANSITION_DURATION) :
        callbackRemove()

    } else if (callback) {
      callback()
    }
  }

  // these following methods are used to handle overflowing modals

  Modal.prototype.handleUpdate = function () {
    this.adjustDialog()
  }

  Modal.prototype.adjustDialog = function () {
    var modalIsOverflowing = this.$element[0].scrollHeight > document.documentElement.clientHeight

    this.$element.css({
      paddingLeft:  !this.bodyIsOverflowing && modalIsOverflowing ? this.scrollbarWidth : '',
      paddingRight: this.bodyIsOverflowing && !modalIsOverflowing ? this.scrollbarWidth : ''
    })
  }

  Modal.prototype.resetAdjustments = function () {
    this.$element.css({
      paddingLeft: '',
      paddingRight: ''
    })
  }

  Modal.prototype.checkScrollbar = function () {
    var fullWindowWidth = window.innerWidth
    if (!fullWindowWidth) { // workaround for missing window.innerWidth in IE8
      var documentElementRect = document.documentElement.getBoundingClientRect()
      fullWindowWidth = documentElementRect.right - Math.abs(documentElementRect.left)
    }
    this.bodyIsOverflowing = document.body.clientWidth < fullWindowWidth
    this.scrollbarWidth = this.measureScrollbar()
  }

  Modal.prototype.setScrollbar = function () {
    var bodyPad = parseInt((this.$body.css('padding-right') || 0), 10)
    this.originalBodyPad = document.body.style.paddingRight || ''
    if (this.bodyIsOverflowing) this.$body.css('padding-right', bodyPad + this.scrollbarWidth)
  }

  Modal.prototype.resetScrollbar = function () {
    this.$body.css('padding-right', this.originalBodyPad)
  }

  Modal.prototype.measureScrollbar = function () { // thx walsh
    var scrollDiv = document.createElement('div')
    scrollDiv.className = 'modal-scrollbar-measure'
    this.$body.append(scrollDiv)
    var scrollbarWidth = scrollDiv.offsetWidth - scrollDiv.clientWidth
    this.$body[0].removeChild(scrollDiv)
    return scrollbarWidth
  }


  // MODAL PLUGIN DEFINITION
  // =======================

  function Plugin(option, _relatedTarget) {
    return this.each(function () {
      var $this   = $(this)
      var data    = $this.data('bs.modal')
      var options = $.extend({}, Modal.DEFAULTS, $this.data(), typeof option == 'object' && option)

      if (!data) $this.data('bs.modal', (data = new Modal(this, options)))
      if (typeof option == 'string') data[option](_relatedTarget)
      else if (options.show) data.show(_relatedTarget)
    })
  }

  var old = $.fn.modal

  $.fn.modal             = Plugin
  $.fn.modal.Constructor = Modal


  // MODAL NO CONFLICT
  // =================

  $.fn.modal.noConflict = function () {
    $.fn.modal = old
    return this
  }


  // MODAL DATA-API
  // ==============

  $(document).on('click.bs.modal.data-api', '[data-toggle="modal"]', function (e) {
    var $this   = $(this)
    var href    = $this.attr('href')
    var $target = $($this.attr('data-target') || (href && href.replace(/.*(?=#[^\s]+$)/, ''))) // strip for ie7
    var option  = $target.data('bs.modal') ? 'toggle' : $.extend({ remote: !/#/.test(href) && href }, $target.data(), $this.data())

    if ($this.is('a')) e.preventDefault()

    $target.one('show.bs.modal', function (showEvent) {
      if (showEvent.isDefaultPrevented()) return // only register focus restorer if modal will actually get shown
      $target.one('hidden.bs.modal', function () {
        $this.is(':visible') && $this.trigger('focus')
      })
    })
    Plugin.call($target, option, this)
  })

}(jQuery);

/* ========================================================================
 * Bootstrap: tooltip.js v3.3.5
 * http://getbootstrap.com/javascript/#tooltip
 * Inspired by the original jQuery.tipsy by Jason Frame
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // TOOLTIP PUBLIC CLASS DEFINITION
  // ===============================

  var Tooltip = function (element, options) {
    this.type       = null
    this.options    = null
    this.enabled    = null
    this.timeout    = null
    this.hoverState = null
    this.$element   = null
    this.inState    = null

    this.init('tooltip', element, options)
  }

  Tooltip.VERSION  = '3.3.5'

  Tooltip.TRANSITION_DURATION = 150

  Tooltip.DEFAULTS = {
    animation: true,
    placement: 'top',
    selector: false,
    template: '<div class="tooltip" role="tooltip"><div class="tooltip-arrow"></div><div class="tooltip-inner"></div></div>',
    trigger: 'hover focus',
    title: '',
    delay: 0,
    html: false,
    container: false,
    viewport: {
      selector: 'body',
      padding: 0
    }
  }

  Tooltip.prototype.init = function (type, element, options) {
    this.enabled   = true
    this.type      = type
    this.$element  = $(element)
    this.options   = this.getOptions(options)
    this.$viewport = this.options.viewport && $($.isFunction(this.options.viewport) ? this.options.viewport.call(this, this.$element) : (this.options.viewport.selector || this.options.viewport))
    this.inState   = { click: false, hover: false, focus: false }

    if (this.$element[0] instanceof document.constructor && !this.options.selector) {
      throw new Error('`selector` option must be specified when initializing ' + this.type + ' on the window.document object!')
    }

    var triggers = this.options.trigger.split(' ')

    for (var i = triggers.length; i--;) {
      var trigger = triggers[i]

      if (trigger == 'click') {
        this.$element.on('click.' + this.type, this.options.selector, $.proxy(this.toggle, this))
      } else if (trigger != 'manual') {
        var eventIn  = trigger == 'hover' ? 'mouseenter' : 'focusin'
        var eventOut = trigger == 'hover' ? 'mouseleave' : 'focusout'

        this.$element.on(eventIn  + '.' + this.type, this.options.selector, $.proxy(this.enter, this))
        this.$element.on(eventOut + '.' + this.type, this.options.selector, $.proxy(this.leave, this))
      }
    }

    this.options.selector ?
      (this._options = $.extend({}, this.options, { trigger: 'manual', selector: '' })) :
      this.fixTitle()
  }

  Tooltip.prototype.getDefaults = function () {
    return Tooltip.DEFAULTS
  }

  Tooltip.prototype.getOptions = function (options) {
    options = $.extend({}, this.getDefaults(), this.$element.data(), options)

    if (options.delay && typeof options.delay == 'number') {
      options.delay = {
        show: options.delay,
        hide: options.delay
      }
    }

    return options
  }

  Tooltip.prototype.getDelegateOptions = function () {
    var options  = {}
    var defaults = this.getDefaults()

    this._options && $.each(this._options, function (key, value) {
      if (defaults[key] != value) options[key] = value
    })

    return options
  }

  Tooltip.prototype.enter = function (obj) {
    var self = obj instanceof this.constructor ?
      obj : $(obj.currentTarget).data('bs.' + this.type)

    if (!self) {
      self = new this.constructor(obj.currentTarget, this.getDelegateOptions())
      $(obj.currentTarget).data('bs.' + this.type, self)
    }

    if (obj instanceof $.Event) {
      self.inState[obj.type == 'focusin' ? 'focus' : 'hover'] = true
    }

    if (self.tip().hasClass('in') || self.hoverState == 'in') {
      self.hoverState = 'in'
      return
    }

    clearTimeout(self.timeout)

    self.hoverState = 'in'

    if (!self.options.delay || !self.options.delay.show) return self.show()

    self.timeout = setTimeout(function () {
      if (self.hoverState == 'in') self.show()
    }, self.options.delay.show)
  }

  Tooltip.prototype.isInStateTrue = function () {
    for (var key in this.inState) {
      if (this.inState[key]) return true
    }

    return false
  }

  Tooltip.prototype.leave = function (obj) {
    var self = obj instanceof this.constructor ?
      obj : $(obj.currentTarget).data('bs.' + this.type)

    if (!self) {
      self = new this.constructor(obj.currentTarget, this.getDelegateOptions())
      $(obj.currentTarget).data('bs.' + this.type, self)
    }

    if (obj instanceof $.Event) {
      self.inState[obj.type == 'focusout' ? 'focus' : 'hover'] = false
    }

    if (self.isInStateTrue()) return

    clearTimeout(self.timeout)

    self.hoverState = 'out'

    if (!self.options.delay || !self.options.delay.hide) return self.hide()

    self.timeout = setTimeout(function () {
      if (self.hoverState == 'out') self.hide()
    }, self.options.delay.hide)
  }

  Tooltip.prototype.show = function () {
    var e = $.Event('show.bs.' + this.type)

    if (this.hasContent() && this.enabled) {
      this.$element.trigger(e)

      var inDom = $.contains(this.$element[0].ownerDocument.documentElement, this.$element[0])
      if (e.isDefaultPrevented() || !inDom) return
      var that = this

      var $tip = this.tip()

      var tipId = this.getUID(this.type)

      this.setContent()
      $tip.attr('id', tipId)
      this.$element.attr('aria-describedby', tipId)

      if (this.options.animation) $tip.addClass('fade')

      var placement = typeof this.options.placement == 'function' ?
        this.options.placement.call(this, $tip[0], this.$element[0]) :
        this.options.placement

      var autoToken = /\s?auto?\s?/i
      var autoPlace = autoToken.test(placement)
      if (autoPlace) placement = placement.replace(autoToken, '') || 'top'

      $tip
        .detach()
        .css({ top: 0, left: 0, display: 'block' })
        .addClass(placement)
        .data('bs.' + this.type, this)

      this.options.container ? $tip.appendTo(this.options.container) : $tip.insertAfter(this.$element)
      this.$element.trigger('inserted.bs.' + this.type)

      var pos          = this.getPosition()
      var actualWidth  = $tip[0].offsetWidth
      var actualHeight = $tip[0].offsetHeight

      if (autoPlace) {
        var orgPlacement = placement
        var viewportDim = this.getPosition(this.$viewport)

        placement = placement == 'bottom' && pos.bottom + actualHeight > viewportDim.bottom ? 'top'    :
                    placement == 'top'    && pos.top    - actualHeight < viewportDim.top    ? 'bottom' :
                    placement == 'right'  && pos.right  + actualWidth  > viewportDim.width  ? 'left'   :
                    placement == 'left'   && pos.left   - actualWidth  < viewportDim.left   ? 'right'  :
                    placement

        $tip
          .removeClass(orgPlacement)
          .addClass(placement)
      }

      var calculatedOffset = this.getCalculatedOffset(placement, pos, actualWidth, actualHeight)

      this.applyPlacement(calculatedOffset, placement)

      var complete = function () {
        var prevHoverState = that.hoverState
        that.$element.trigger('shown.bs.' + that.type)
        that.hoverState = null

        if (prevHoverState == 'out') that.leave(that)
      }

      $.support.transition && this.$tip.hasClass('fade') ?
        $tip
          .one('bsTransitionEnd', complete)
          .emulateTransitionEnd(Tooltip.TRANSITION_DURATION) :
        complete()
    }
  }

  Tooltip.prototype.applyPlacement = function (offset, placement) {
    var $tip   = this.tip()
    var width  = $tip[0].offsetWidth
    var height = $tip[0].offsetHeight

    // manually read margins because getBoundingClientRect includes difference
    var marginTop = parseInt($tip.css('margin-top'), 10)
    var marginLeft = parseInt($tip.css('margin-left'), 10)

    // we must check for NaN for ie 8/9
    if (isNaN(marginTop))  marginTop  = 0
    if (isNaN(marginLeft)) marginLeft = 0

    offset.top  += marginTop
    offset.left += marginLeft

    // $.fn.offset doesn't round pixel values
    // so we use setOffset directly with our own function B-0
    $.offset.setOffset($tip[0], $.extend({
      using: function (props) {
        $tip.css({
          top: Math.round(props.top),
          left: Math.round(props.left)
        })
      }
    }, offset), 0)

    $tip.addClass('in')

    // check to see if placing tip in new offset caused the tip to resize itself
    var actualWidth  = $tip[0].offsetWidth
    var actualHeight = $tip[0].offsetHeight

    if (placement == 'top' && actualHeight != height) {
      offset.top = offset.top + height - actualHeight
    }

    var delta = this.getViewportAdjustedDelta(placement, offset, actualWidth, actualHeight)

    if (delta.left) offset.left += delta.left
    else offset.top += delta.top

    var isVertical          = /top|bottom/.test(placement)
    var arrowDelta          = isVertical ? delta.left * 2 - width + actualWidth : delta.top * 2 - height + actualHeight
    var arrowOffsetPosition = isVertical ? 'offsetWidth' : 'offsetHeight'

    $tip.offset(offset)
    this.replaceArrow(arrowDelta, $tip[0][arrowOffsetPosition], isVertical)
  }

  Tooltip.prototype.replaceArrow = function (delta, dimension, isVertical) {
    this.arrow()
      .css(isVertical ? 'left' : 'top', 50 * (1 - delta / dimension) + '%')
      .css(isVertical ? 'top' : 'left', '')
  }

  Tooltip.prototype.setContent = function () {
    var $tip  = this.tip()
    var title = this.getTitle()

    $tip.find('.tooltip-inner')[this.options.html ? 'html' : 'text'](title)
    $tip.removeClass('fade in top bottom left right')
  }

  Tooltip.prototype.hide = function (callback) {
    var that = this
    var $tip = $(this.$tip)
    var e    = $.Event('hide.bs.' + this.type)

    function complete() {
      if (that.hoverState != 'in') $tip.detach()
      that.$element
        .removeAttr('aria-describedby')
        .trigger('hidden.bs.' + that.type)
      callback && callback()
    }

    this.$element.trigger(e)

    if (e.isDefaultPrevented()) return

    $tip.removeClass('in')

    $.support.transition && $tip.hasClass('fade') ?
      $tip
        .one('bsTransitionEnd', complete)
        .emulateTransitionEnd(Tooltip.TRANSITION_DURATION) :
      complete()

    this.hoverState = null

    return this
  }

  Tooltip.prototype.fixTitle = function () {
    var $e = this.$element
    if ($e.attr('title') || typeof $e.attr('data-original-title') != 'string') {
      $e.attr('data-original-title', $e.attr('title') || '').attr('title', '')
    }
  }

  Tooltip.prototype.hasContent = function () {
    return this.getTitle()
  }

  Tooltip.prototype.getPosition = function ($element) {
    $element   = $element || this.$element

    var el     = $element[0]
    var isBody = el.tagName == 'BODY'

    var elRect    = el.getBoundingClientRect()
    if (elRect.width == null) {
      // width and height are missing in IE8, so compute them manually; see https://github.com/twbs/bootstrap/issues/14093
      elRect = $.extend({}, elRect, { width: elRect.right - elRect.left, height: elRect.bottom - elRect.top })
    }
    var elOffset  = isBody ? { top: 0, left: 0 } : $element.offset()
    var scroll    = { scroll: isBody ? document.documentElement.scrollTop || document.body.scrollTop : $element.scrollTop() }
    var outerDims = isBody ? { width: $(window).width(), height: $(window).height() } : null

    return $.extend({}, elRect, scroll, outerDims, elOffset)
  }

  Tooltip.prototype.getCalculatedOffset = function (placement, pos, actualWidth, actualHeight) {
    return placement == 'bottom' ? { top: pos.top + pos.height,   left: pos.left + pos.width / 2 - actualWidth / 2 } :
           placement == 'top'    ? { top: pos.top - actualHeight, left: pos.left + pos.width / 2 - actualWidth / 2 } :
           placement == 'left'   ? { top: pos.top + pos.height / 2 - actualHeight / 2, left: pos.left - actualWidth } :
        /* placement == 'right' */ { top: pos.top + pos.height / 2 - actualHeight / 2, left: pos.left + pos.width }

  }

  Tooltip.prototype.getViewportAdjustedDelta = function (placement, pos, actualWidth, actualHeight) {
    var delta = { top: 0, left: 0 }
    if (!this.$viewport) return delta

    var viewportPadding = this.options.viewport && this.options.viewport.padding || 0
    var viewportDimensions = this.getPosition(this.$viewport)

    if (/right|left/.test(placement)) {
      var topEdgeOffset    = pos.top - viewportPadding - viewportDimensions.scroll
      var bottomEdgeOffset = pos.top + viewportPadding - viewportDimensions.scroll + actualHeight
      if (topEdgeOffset < viewportDimensions.top) { // top overflow
        delta.top = viewportDimensions.top - topEdgeOffset
      } else if (bottomEdgeOffset > viewportDimensions.top + viewportDimensions.height) { // bottom overflow
        delta.top = viewportDimensions.top + viewportDimensions.height - bottomEdgeOffset
      }
    } else {
      var leftEdgeOffset  = pos.left - viewportPadding
      var rightEdgeOffset = pos.left + viewportPadding + actualWidth
      if (leftEdgeOffset < viewportDimensions.left) { // left overflow
        delta.left = viewportDimensions.left - leftEdgeOffset
      } else if (rightEdgeOffset > viewportDimensions.right) { // right overflow
        delta.left = viewportDimensions.left + viewportDimensions.width - rightEdgeOffset
      }
    }

    return delta
  }

  Tooltip.prototype.getTitle = function () {
    var title
    var $e = this.$element
    var o  = this.options

    title = $e.attr('data-original-title')
      || (typeof o.title == 'function' ? o.title.call($e[0]) :  o.title)

    return title
  }

  Tooltip.prototype.getUID = function (prefix) {
    do prefix += ~~(Math.random() * 1000000)
    while (document.getElementById(prefix))
    return prefix
  }

  Tooltip.prototype.tip = function () {
    if (!this.$tip) {
      this.$tip = $(this.options.template)
      if (this.$tip.length != 1) {
        throw new Error(this.type + ' `template` option must consist of exactly 1 top-level element!')
      }
    }
    return this.$tip
  }

  Tooltip.prototype.arrow = function () {
    return (this.$arrow = this.$arrow || this.tip().find('.tooltip-arrow'))
  }

  Tooltip.prototype.enable = function () {
    this.enabled = true
  }

  Tooltip.prototype.disable = function () {
    this.enabled = false
  }

  Tooltip.prototype.toggleEnabled = function () {
    this.enabled = !this.enabled
  }

  Tooltip.prototype.toggle = function (e) {
    var self = this
    if (e) {
      self = $(e.currentTarget).data('bs.' + this.type)
      if (!self) {
        self = new this.constructor(e.currentTarget, this.getDelegateOptions())
        $(e.currentTarget).data('bs.' + this.type, self)
      }
    }

    if (e) {
      self.inState.click = !self.inState.click
      if (self.isInStateTrue()) self.enter(self)
      else self.leave(self)
    } else {
      self.tip().hasClass('in') ? self.leave(self) : self.enter(self)
    }
  }

  Tooltip.prototype.destroy = function () {
    var that = this
    clearTimeout(this.timeout)
    this.hide(function () {
      that.$element.off('.' + that.type).removeData('bs.' + that.type)
      if (that.$tip) {
        that.$tip.detach()
      }
      that.$tip = null
      that.$arrow = null
      that.$viewport = null
    })
  }


  // TOOLTIP PLUGIN DEFINITION
  // =========================

  function Plugin(option) {
    return this.each(function () {
      var $this   = $(this)
      var data    = $this.data('bs.tooltip')
      var options = typeof option == 'object' && option

      if (!data && /destroy|hide/.test(option)) return
      if (!data) $this.data('bs.tooltip', (data = new Tooltip(this, options)))
      if (typeof option == 'string') data[option]()
    })
  }

  var old = $.fn.tooltip

  $.fn.tooltip             = Plugin
  $.fn.tooltip.Constructor = Tooltip


  // TOOLTIP NO CONFLICT
  // ===================

  $.fn.tooltip.noConflict = function () {
    $.fn.tooltip = old
    return this
  }

}(jQuery);

/* ========================================================================
 * Bootstrap: popover.js v3.3.5
 * http://getbootstrap.com/javascript/#popovers
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // POPOVER PUBLIC CLASS DEFINITION
  // ===============================

  var Popover = function (element, options) {
    this.init('popover', element, options)
  }

  if (!$.fn.tooltip) throw new Error('Popover requires tooltip.js')

  Popover.VERSION  = '3.3.5'

  Popover.DEFAULTS = $.extend({}, $.fn.tooltip.Constructor.DEFAULTS, {
    placement: 'right',
    trigger: 'click',
    content: '',
    template: '<div class="popover" role="tooltip"><div class="arrow"></div><h3 class="popover-title"></h3><div class="popover-content"></div></div>'
  })


  // NOTE: POPOVER EXTENDS tooltip.js
  // ================================

  Popover.prototype = $.extend({}, $.fn.tooltip.Constructor.prototype)

  Popover.prototype.constructor = Popover

  Popover.prototype.getDefaults = function () {
    return Popover.DEFAULTS
  }

  Popover.prototype.setContent = function () {
    var $tip    = this.tip()
    var title   = this.getTitle()
    var content = this.getContent()

    $tip.find('.popover-title')[this.options.html ? 'html' : 'text'](title)
    $tip.find('.popover-content').children().detach().end()[ // we use append for html objects to maintain js events
      this.options.html ? (typeof content == 'string' ? 'html' : 'append') : 'text'
    ](content)

    $tip.removeClass('fade top bottom left right in')

    // IE8 doesn't accept hiding via the `:empty` pseudo selector, we have to do
    // this manually by checking the contents.
    if (!$tip.find('.popover-title').html()) $tip.find('.popover-title').hide()
  }

  Popover.prototype.hasContent = function () {
    return this.getTitle() || this.getContent()
  }

  Popover.prototype.getContent = function () {
    var $e = this.$element
    var o  = this.options

    return $e.attr('data-content')
      || (typeof o.content == 'function' ?
            o.content.call($e[0]) :
            o.content)
  }

  Popover.prototype.arrow = function () {
    return (this.$arrow = this.$arrow || this.tip().find('.arrow'))
  }


  // POPOVER PLUGIN DEFINITION
  // =========================

  function Plugin(option) {
    return this.each(function () {
      var $this   = $(this)
      var data    = $this.data('bs.popover')
      var options = typeof option == 'object' && option

      if (!data && /destroy|hide/.test(option)) return
      if (!data) $this.data('bs.popover', (data = new Popover(this, options)))
      if (typeof option == 'string') data[option]()
    })
  }

  var old = $.fn.popover

  $.fn.popover             = Plugin
  $.fn.popover.Constructor = Popover


  // POPOVER NO CONFLICT
  // ===================

  $.fn.popover.noConflict = function () {
    $.fn.popover = old
    return this
  }

}(jQuery);

/* ========================================================================
 * Bootstrap: scrollspy.js v3.3.5
 * http://getbootstrap.com/javascript/#scrollspy
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // SCROLLSPY CLASS DEFINITION
  // ==========================

  function ScrollSpy(element, options) {
    this.$body          = $(document.body)
    this.$scrollElement = $(element).is(document.body) ? $(window) : $(element)
    this.options        = $.extend({}, ScrollSpy.DEFAULTS, options)
    this.selector       = (this.options.target || '') + ' .nav li > a'
    this.offsets        = []
    this.targets        = []
    this.activeTarget   = null
    this.scrollHeight   = 0

    this.$scrollElement.on('scroll.bs.scrollspy', $.proxy(this.process, this))
    this.refresh()
    this.process()
  }

  ScrollSpy.VERSION  = '3.3.5'

  ScrollSpy.DEFAULTS = {
    offset: 10
  }

  ScrollSpy.prototype.getScrollHeight = function () {
    return this.$scrollElement[0].scrollHeight || Math.max(this.$body[0].scrollHeight, document.documentElement.scrollHeight)
  }

  ScrollSpy.prototype.refresh = function () {
    var that          = this
    var offsetMethod  = 'offset'
    var offsetBase    = 0

    this.offsets      = []
    this.targets      = []
    this.scrollHeight = this.getScrollHeight()

    if (!$.isWindow(this.$scrollElement[0])) {
      offsetMethod = 'position'
      offsetBase   = this.$scrollElement.scrollTop()
    }

    this.$body
      .find(this.selector)
      .map(function () {
        var $el   = $(this)
        var href  = $el.data('target') || $el.attr('href')
        var $href = /^#./.test(href) && $(href)

        return ($href
          && $href.length
          && $href.is(':visible')
          && [[$href[offsetMethod]().top + offsetBase, href]]) || null
      })
      .sort(function (a, b) { return a[0] - b[0] })
      .each(function () {
        that.offsets.push(this[0])
        that.targets.push(this[1])
      })
  }

  ScrollSpy.prototype.process = function () {
    var scrollTop    = this.$scrollElement.scrollTop() + this.options.offset
    var scrollHeight = this.getScrollHeight()
    var maxScroll    = this.options.offset + scrollHeight - this.$scrollElement.height()
    var offsets      = this.offsets
    var targets      = this.targets
    var activeTarget = this.activeTarget
    var i

    if (this.scrollHeight != scrollHeight) {
      this.refresh()
    }

    if (scrollTop >= maxScroll) {
      return activeTarget != (i = targets[targets.length - 1]) && this.activate(i)
    }

    if (activeTarget && scrollTop < offsets[0]) {
      this.activeTarget = null
      return this.clear()
    }

    for (i = offsets.length; i--;) {
      activeTarget != targets[i]
        && scrollTop >= offsets[i]
        && (offsets[i + 1] === undefined || scrollTop < offsets[i + 1])
        && this.activate(targets[i])
    }
  }

  ScrollSpy.prototype.activate = function (target) {
    this.activeTarget = target

    this.clear()

    var selector = this.selector +
      '[data-target="' + target + '"],' +
      this.selector + '[href="' + target + '"]'

    var active = $(selector)
      .parents('li')
      .addClass('active')

    if (active.parent('.dropdown-menu').length) {
      active = active
        .closest('li.dropdown')
        .addClass('active')
    }

    active.trigger('activate.bs.scrollspy')
  }

  ScrollSpy.prototype.clear = function () {
    $(this.selector)
      .parentsUntil(this.options.target, '.active')
      .removeClass('active')
  }


  // SCROLLSPY PLUGIN DEFINITION
  // ===========================

  function Plugin(option) {
    return this.each(function () {
      var $this   = $(this)
      var data    = $this.data('bs.scrollspy')
      var options = typeof option == 'object' && option

      if (!data) $this.data('bs.scrollspy', (data = new ScrollSpy(this, options)))
      if (typeof option == 'string') data[option]()
    })
  }

  var old = $.fn.scrollspy

  $.fn.scrollspy             = Plugin
  $.fn.scrollspy.Constructor = ScrollSpy


  // SCROLLSPY NO CONFLICT
  // =====================

  $.fn.scrollspy.noConflict = function () {
    $.fn.scrollspy = old
    return this
  }


  // SCROLLSPY DATA-API
  // ==================

  $(window).on('load.bs.scrollspy.data-api', function () {
    $('[data-spy="scroll"]').each(function () {
      var $spy = $(this)
      Plugin.call($spy, $spy.data())
    })
  })

}(jQuery);

/* ========================================================================
 * Bootstrap: tab.js v3.3.5
 * http://getbootstrap.com/javascript/#tabs
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // TAB CLASS DEFINITION
  // ====================

  var Tab = function (element) {
    // jscs:disable requireDollarBeforejQueryAssignment
    this.element = $(element)
    // jscs:enable requireDollarBeforejQueryAssignment
  }

  Tab.VERSION = '3.3.5'

  Tab.TRANSITION_DURATION = 150

  Tab.prototype.show = function () {
    var $this    = this.element
    var $ul      = $this.closest('ul:not(.dropdown-menu)')
    var selector = $this.data('target')

    if (!selector) {
      selector = $this.attr('href')
      selector = selector && selector.replace(/.*(?=#[^\s]*$)/, '') // strip for ie7
    }

    if ($this.parent('li').hasClass('active')) return

    var $previous = $ul.find('.active:last a')
    var hideEvent = $.Event('hide.bs.tab', {
      relatedTarget: $this[0]
    })
    var showEvent = $.Event('show.bs.tab', {
      relatedTarget: $previous[0]
    })

    $previous.trigger(hideEvent)
    $this.trigger(showEvent)

    if (showEvent.isDefaultPrevented() || hideEvent.isDefaultPrevented()) return

    var $target = $(selector)

    this.activate($this.closest('li'), $ul)
    this.activate($target, $target.parent(), function () {
      $previous.trigger({
        type: 'hidden.bs.tab',
        relatedTarget: $this[0]
      })
      $this.trigger({
        type: 'shown.bs.tab',
        relatedTarget: $previous[0]
      })
    })
  }

  Tab.prototype.activate = function (element, container, callback) {
    var $active    = container.find('> .active')
    var transition = callback
      && $.support.transition
      && ($active.length && $active.hasClass('fade') || !!container.find('> .fade').length)

    function next() {
      $active
        .removeClass('active')
        .find('> .dropdown-menu > .active')
          .removeClass('active')
        .end()
        .find('[data-toggle="tab"]')
          .attr('aria-expanded', false)

      element
        .addClass('active')
        .find('[data-toggle="tab"]')
          .attr('aria-expanded', true)

      if (transition) {
        element[0].offsetWidth // reflow for transition
        element.addClass('in')
      } else {
        element.removeClass('fade')
      }

      if (element.parent('.dropdown-menu').length) {
        element
          .closest('li.dropdown')
            .addClass('active')
          .end()
          .find('[data-toggle="tab"]')
            .attr('aria-expanded', true)
      }

      callback && callback()
    }

    $active.length && transition ?
      $active
        .one('bsTransitionEnd', next)
        .emulateTransitionEnd(Tab.TRANSITION_DURATION) :
      next()

    $active.removeClass('in')
  }


  // TAB PLUGIN DEFINITION
  // =====================

  function Plugin(option) {
    return this.each(function () {
      var $this = $(this)
      var data  = $this.data('bs.tab')

      if (!data) $this.data('bs.tab', (data = new Tab(this)))
      if (typeof option == 'string') data[option]()
    })
  }

  var old = $.fn.tab

  $.fn.tab             = Plugin
  $.fn.tab.Constructor = Tab


  // TAB NO CONFLICT
  // ===============

  $.fn.tab.noConflict = function () {
    $.fn.tab = old
    return this
  }


  // TAB DATA-API
  // ============

  var clickHandler = function (e) {
    e.preventDefault()
    Plugin.call($(this), 'show')
  }

  $(document)
    .on('click.bs.tab.data-api', '[data-toggle="tab"]', clickHandler)
    .on('click.bs.tab.data-api', '[data-toggle="pill"]', clickHandler)

}(jQuery);

/* ========================================================================
 * Bootstrap: affix.js v3.3.5
 * http://getbootstrap.com/javascript/#affix
 * ========================================================================
 * Copyright 2011-2015 Twitter, Inc.
 * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
 * ======================================================================== */


+function ($) {
  'use strict';

  // AFFIX CLASS DEFINITION
  // ======================

  var Affix = function (element, options) {
    this.options = $.extend({}, Affix.DEFAULTS, options)

    this.$target = $(this.options.target)
      .on('scroll.bs.affix.data-api', $.proxy(this.checkPosition, this))
      .on('click.bs.affix.data-api',  $.proxy(this.checkPositionWithEventLoop, this))

    this.$element     = $(element)
    this.affixed      = null
    this.unpin        = null
    this.pinnedOffset = null

    this.checkPosition()
  }

  Affix.VERSION  = '3.3.5'

  Affix.RESET    = 'affix affix-top affix-bottom'

  Affix.DEFAULTS = {
    offset: 0,
    target: window
  }

  Affix.prototype.getState = function (scrollHeight, height, offsetTop, offsetBottom) {
    var scrollTop    = this.$target.scrollTop()
    var position     = this.$element.offset()
    var targetHeight = this.$target.height()

    if (offsetTop != null && this.affixed == 'top') return scrollTop < offsetTop ? 'top' : false

    if (this.affixed == 'bottom') {
      if (offsetTop != null) return (scrollTop + this.unpin <= position.top) ? false : 'bottom'
      return (scrollTop + targetHeight <= scrollHeight - offsetBottom) ? false : 'bottom'
    }

    var initializing   = this.affixed == null
    var colliderTop    = initializing ? scrollTop : position.top
    var colliderHeight = initializing ? targetHeight : height

    if (offsetTop != null && scrollTop <= offsetTop) return 'top'
    if (offsetBottom != null && (colliderTop + colliderHeight >= scrollHeight - offsetBottom)) return 'bottom'

    return false
  }

  Affix.prototype.getPinnedOffset = function () {
    if (this.pinnedOffset) return this.pinnedOffset
    this.$element.removeClass(Affix.RESET).addClass('affix')
    var scrollTop = this.$target.scrollTop()
    var position  = this.$element.offset()
    return (this.pinnedOffset = position.top - scrollTop)
  }

  Affix.prototype.checkPositionWithEventLoop = function () {
    setTimeout($.proxy(this.checkPosition, this), 1)
  }

  Affix.prototype.checkPosition = function () {
    if (!this.$element.is(':visible')) return

    var height       = this.$element.height()
    var offset       = this.options.offset
    var offsetTop    = offset.top
    var offsetBottom = offset.bottom
    var scrollHeight = Math.max($(document).height(), $(document.body).height())

    if (typeof offset != 'object')         offsetBottom = offsetTop = offset
    if (typeof offsetTop == 'function')    offsetTop    = offset.top(this.$element)
    if (typeof offsetBottom == 'function') offsetBottom = offset.bottom(this.$element)

    var affix = this.getState(scrollHeight, height, offsetTop, offsetBottom)

    if (this.affixed != affix) {
      if (this.unpin != null) this.$element.css('top', '')

      var affixType = 'affix' + (affix ? '-' + affix : '')
      var e         = $.Event(affixType + '.bs.affix')

      this.$element.trigger(e)

      if (e.isDefaultPrevented()) return

      this.affixed = affix
      this.unpin = affix == 'bottom' ? this.getPinnedOffset() : null

      this.$element
        .removeClass(Affix.RESET)
        .addClass(affixType)
        .trigger(affixType.replace('affix', 'affixed') + '.bs.affix')
    }

    if (affix == 'bottom') {
      this.$element.offset({
        top: scrollHeight - height - offsetBottom
      })
    }
  }


  // AFFIX PLUGIN DEFINITION
  // =======================

  function Plugin(option) {
    return this.each(function () {
      var $this   = $(this)
      var data    = $this.data('bs.affix')
      var options = typeof option == 'object' && option

      if (!data) $this.data('bs.affix', (data = new Affix(this, options)))
      if (typeof option == 'string') data[option]()
    })
  }

  var old = $.fn.affix

  $.fn.affix             = Plugin
  $.fn.affix.Constructor = Affix


  // AFFIX NO CONFLICT
  // =================

  $.fn.affix.noConflict = function () {
    $.fn.affix = old
    return this
  }


  // AFFIX DATA-API
  // ==============

  $(window).on('load', function () {
    $('[data-spy="affix"]').each(function () {
      var $spy = $(this)
      var data = $spy.data()

      data.offset = data.offset || {}

      if (data.offsetBottom != null) data.offset.bottom = data.offsetBottom
      if (data.offsetTop    != null) data.offset.top    = data.offsetTop

      Plugin.call($spy, data)
    })
  })

}(jQuery);
/*!
 * typeahead.js 0.11.1
 * https://github.com/twitter/typeahead.js
 * Copyright 2013-2015 Twitter, Inc. and other contributors; Licensed MIT
 */


(function(root, factory) {
    if (typeof define === "function" && define.amd) {
        define("bloodhound", [ "jquery" ], function(a0) {
            return root["Bloodhound"] = factory(a0);
        });
    } else if (typeof exports === "object") {
        module.exports = factory(require("jquery"));
    } else {
        root["Bloodhound"] = factory(jQuery);
    }
})(this, function($) {
    var _ = function() {
        "use strict";
        return {
            isMsie: function() {
                return /(msie|trident)/i.test(navigator.userAgent) ? navigator.userAgent.match(/(msie |rv:)(\d+(.\d+)?)/i)[2] : false;
            },
            isBlankString: function(str) {
                return !str || /^\s*$/.test(str);
            },
            escapeRegExChars: function(str) {
                return str.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
            },
            isString: function(obj) {
                return typeof obj === "string";
            },
            isNumber: function(obj) {
                return typeof obj === "number";
            },
            isArray: $.isArray,
            isFunction: $.isFunction,
            isObject: $.isPlainObject,
            isUndefined: function(obj) {
                return typeof obj === "undefined";
            },
            isElement: function(obj) {
                return !!(obj && obj.nodeType === 1);
            },
            isJQuery: function(obj) {
                return obj instanceof $;
            },
            toStr: function toStr(s) {
                return _.isUndefined(s) || s === null ? "" : s + "";
            },
            bind: $.proxy,
            each: function(collection, cb) {
                $.each(collection, reverseArgs);
                function reverseArgs(index, value) {
                    return cb(value, index);
                }
            },
            map: $.map,
            filter: $.grep,
            every: function(obj, test) {
                var result = true;
                if (!obj) {
                    return result;
                }
                $.each(obj, function(key, val) {
                    if (!(result = test.call(null, val, key, obj))) {
                        return false;
                    }
                });
                return !!result;
            },
            some: function(obj, test) {
                var result = false;
                if (!obj) {
                    return result;
                }
                $.each(obj, function(key, val) {
                    if (result = test.call(null, val, key, obj)) {
                        return false;
                    }
                });
                return !!result;
            },
            mixin: $.extend,
            identity: function(x) {
                return x;
            },
            clone: function(obj) {
                return $.extend(true, {}, obj);
            },
            getIdGenerator: function() {
                var counter = 0;
                return function() {
                    return counter++;
                };
            },
            templatify: function templatify(obj) {
                return $.isFunction(obj) ? obj : template;
                function template() {
                    return String(obj);
                }
            },
            defer: function(fn) {
                setTimeout(fn, 0);
            },
            debounce: function(func, wait, immediate) {
                var timeout, result;
                return function() {
                    var context = this, args = arguments, later, callNow;
                    later = function() {
                        timeout = null;
                        if (!immediate) {
                            result = func.apply(context, args);
                        }
                    };
                    callNow = immediate && !timeout;
                    clearTimeout(timeout);
                    timeout = setTimeout(later, wait);
                    if (callNow) {
                        result = func.apply(context, args);
                    }
                    return result;
                };
            },
            throttle: function(func, wait) {
                var context, args, timeout, result, previous, later;
                previous = 0;
                later = function() {
                    previous = new Date();
                    timeout = null;
                    result = func.apply(context, args);
                };
                return function() {
                    var now = new Date(), remaining = wait - (now - previous);
                    context = this;
                    args = arguments;
                    if (remaining <= 0) {
                        clearTimeout(timeout);
                        timeout = null;
                        previous = now;
                        result = func.apply(context, args);
                    } else if (!timeout) {
                        timeout = setTimeout(later, remaining);
                    }
                    return result;
                };
            },
            stringify: function(val) {
                return _.isString(val) ? val : JSON.stringify(val);
            },
            noop: function() {}
        };
    }();
    var VERSION = "0.11.1";
    var tokenizers = function() {
        "use strict";
        return {
            nonword: nonword,
            whitespace: whitespace,
            obj: {
                nonword: getObjTokenizer(nonword),
                whitespace: getObjTokenizer(whitespace)
            }
        };
        function whitespace(str) {
            str = _.toStr(str);
            return str ? str.split(/\s+/) : [];
        }
        function nonword(str) {
            str = _.toStr(str);
            return str ? str.split(/\W+/) : [];
        }
        function getObjTokenizer(tokenizer) {
            return function setKey(keys) {
                keys = _.isArray(keys) ? keys : [].slice.call(arguments, 0);
                return function tokenize(o) {
                    var tokens = [];
                    _.each(keys, function(k) {
                        tokens = tokens.concat(tokenizer(_.toStr(o[k])));
                    });
                    return tokens;
                };
            };
        }
    }();
    var LruCache = function() {
        "use strict";
        function LruCache(maxSize) {
            this.maxSize = _.isNumber(maxSize) ? maxSize : 100;
            this.reset();
            if (this.maxSize <= 0) {
                this.set = this.get = $.noop;
            }
        }
        _.mixin(LruCache.prototype, {
            set: function set(key, val) {
                var tailItem = this.list.tail, node;
                if (this.size >= this.maxSize) {
                    this.list.remove(tailItem);
                    delete this.hash[tailItem.key];
                    this.size--;
                }
                if (node = this.hash[key]) {
                    node.val = val;
                    this.list.moveToFront(node);
                } else {
                    node = new Node(key, val);
                    this.list.add(node);
                    this.hash[key] = node;
                    this.size++;
                }
            },
            get: function get(key) {
                var node = this.hash[key];
                if (node) {
                    this.list.moveToFront(node);
                    return node.val;
                }
            },
            reset: function reset() {
                this.size = 0;
                this.hash = {};
                this.list = new List();
            }
        });
        function List() {
            this.head = this.tail = null;
        }
        _.mixin(List.prototype, {
            add: function add(node) {
                if (this.head) {
                    node.next = this.head;
                    this.head.prev = node;
                }
                this.head = node;
                this.tail = this.tail || node;
            },
            remove: function remove(node) {
                node.prev ? node.prev.next = node.next : this.head = node.next;
                node.next ? node.next.prev = node.prev : this.tail = node.prev;
            },
            moveToFront: function(node) {
                this.remove(node);
                this.add(node);
            }
        });
        function Node(key, val) {
            this.key = key;
            this.val = val;
            this.prev = this.next = null;
        }
        return LruCache;
    }();
    var PersistentStorage = function() {
        "use strict";
        var LOCAL_STORAGE;
        try {
            LOCAL_STORAGE = window.localStorage;
            LOCAL_STORAGE.setItem("~~~", "!");
            LOCAL_STORAGE.removeItem("~~~");
        } catch (err) {
            LOCAL_STORAGE = null;
        }
        function PersistentStorage(namespace, override) {
            this.prefix = [ "__", namespace, "__" ].join("");
            this.ttlKey = "__ttl__";
            this.keyMatcher = new RegExp("^" + _.escapeRegExChars(this.prefix));
            this.ls = override || LOCAL_STORAGE;
            !this.ls && this._noop();
        }
        _.mixin(PersistentStorage.prototype, {
            _prefix: function(key) {
                return this.prefix + key;
            },
            _ttlKey: function(key) {
                return this._prefix(key) + this.ttlKey;
            },
            _noop: function() {
                this.get = this.set = this.remove = this.clear = this.isExpired = _.noop;
            },
            _safeSet: function(key, val) {
                try {
                    this.ls.setItem(key, val);
                } catch (err) {
                    if (err.name === "QuotaExceededError") {
                        this.clear();
                        this._noop();
                    }
                }
            },
            get: function(key) {
                if (this.isExpired(key)) {
                    this.remove(key);
                }
                return decode(this.ls.getItem(this._prefix(key)));
            },
            set: function(key, val, ttl) {
                if (_.isNumber(ttl)) {
                    this._safeSet(this._ttlKey(key), encode(now() + ttl));
                } else {
                    this.ls.removeItem(this._ttlKey(key));
                }
                return this._safeSet(this._prefix(key), encode(val));
            },
            remove: function(key) {
                this.ls.removeItem(this._ttlKey(key));
                this.ls.removeItem(this._prefix(key));
                return this;
            },
            clear: function() {
                var i, keys = gatherMatchingKeys(this.keyMatcher);
                for (i = keys.length; i--; ) {
                    this.remove(keys[i]);
                }
                return this;
            },
            isExpired: function(key) {
                var ttl = decode(this.ls.getItem(this._ttlKey(key)));
                return _.isNumber(ttl) && now() > ttl ? true : false;
            }
        });
        return PersistentStorage;
        function now() {
            return new Date().getTime();
        }
        function encode(val) {
            return JSON.stringify(_.isUndefined(val) ? null : val);
        }
        function decode(val) {
            return $.parseJSON(val);
        }
        function gatherMatchingKeys(keyMatcher) {
            var i, key, keys = [], len = LOCAL_STORAGE.length;
            for (i = 0; i < len; i++) {
                if ((key = LOCAL_STORAGE.key(i)).match(keyMatcher)) {
                    keys.push(key.replace(keyMatcher, ""));
                }
            }
            return keys;
        }
    }();
    var Transport = function() {
        "use strict";
        var pendingRequestsCount = 0, pendingRequests = {}, maxPendingRequests = 6, sharedCache = new LruCache(10);
        function Transport(o) {
            o = o || {};
            this.cancelled = false;
            this.lastReq = null;
            this._send = o.transport;
            this._get = o.limiter ? o.limiter(this._get) : this._get;
            this._cache = o.cache === false ? new LruCache(0) : sharedCache;
        }
        Transport.setMaxPendingRequests = function setMaxPendingRequests(num) {
            maxPendingRequests = num;
        };
        Transport.resetCache = function resetCache() {
            sharedCache.reset();
        };
        _.mixin(Transport.prototype, {
            _fingerprint: function fingerprint(o) {
                o = o || {};
                return o.url + o.type + $.param(o.data || {});
            },
            _get: function(o, cb) {
                var that = this, fingerprint, jqXhr;
                fingerprint = this._fingerprint(o);
                if (this.cancelled || fingerprint !== this.lastReq) {
                    return;
                }
                if (jqXhr = pendingRequests[fingerprint]) {
                    jqXhr.done(done).fail(fail);
                } else if (pendingRequestsCount < maxPendingRequests) {
                    pendingRequestsCount++;
                    pendingRequests[fingerprint] = this._send(o).done(done).fail(fail).always(always);
                } else {
                    this.onDeckRequestArgs = [].slice.call(arguments, 0);
                }
                function done(resp) {
                    cb(null, resp);
                    that._cache.set(fingerprint, resp);
                }
                function fail() {
                    cb(true);
                }
                function always() {
                    pendingRequestsCount--;
                    delete pendingRequests[fingerprint];
                    if (that.onDeckRequestArgs) {
                        that._get.apply(that, that.onDeckRequestArgs);
                        that.onDeckRequestArgs = null;
                    }
                }
            },
            get: function(o, cb) {
                var resp, fingerprint;
                cb = cb || $.noop;
                o = _.isString(o) ? {
                    url: o
                } : o || {};
                fingerprint = this._fingerprint(o);
                this.cancelled = false;
                this.lastReq = fingerprint;
                if (resp = this._cache.get(fingerprint)) {
                    cb(null, resp);
                } else {
                    this._get(o, cb);
                }
            },
            cancel: function() {
                this.cancelled = true;
            }
        });
        return Transport;
    }();
    var SearchIndex = window.SearchIndex = function() {
        "use strict";
        var CHILDREN = "c", IDS = "i";
        function SearchIndex(o) {
            o = o || {};
            if (!o.datumTokenizer || !o.queryTokenizer) {
                $.error("datumTokenizer and queryTokenizer are both required");
            }
            this.identify = o.identify || _.stringify;
            this.datumTokenizer = o.datumTokenizer;
            this.queryTokenizer = o.queryTokenizer;
            this.reset();
        }
        _.mixin(SearchIndex.prototype, {
            bootstrap: function bootstrap(o) {
                this.datums = o.datums;
                this.trie = o.trie;
            },
            add: function(data) {
                var that = this;
                data = _.isArray(data) ? data : [ data ];
                _.each(data, function(datum) {
                    var id, tokens;
                    that.datums[id = that.identify(datum)] = datum;
                    tokens = normalizeTokens(that.datumTokenizer(datum));
                    _.each(tokens, function(token) {
                        var node, chars, ch;
                        node = that.trie;
                        chars = token.split("");
                        while (ch = chars.shift()) {
                            node = node[CHILDREN][ch] || (node[CHILDREN][ch] = newNode());
                            node[IDS].push(id);
                        }
                    });
                });
            },
            get: function get(ids) {
                var that = this;
                return _.map(ids, function(id) {
                    return that.datums[id];
                });
            },
            search: function search(query) {
                var that = this, tokens, matches;
                tokens = normalizeTokens(this.queryTokenizer(query));
                _.each(tokens, function(token) {
                    var node, chars, ch, ids;
                    if (matches && matches.length === 0) {
                        return false;
                    }
                    node = that.trie;
                    chars = token.split("");
                    while (node && (ch = chars.shift())) {
                        node = node[CHILDREN][ch];
                    }
                    if (node && chars.length === 0) {
                        ids = node[IDS].slice(0);
                        matches = matches ? getIntersection(matches, ids) : ids;
                    } else {
                        matches = [];
                        return false;
                    }
                });
                return matches ? _.map(unique(matches), function(id) {
                    return that.datums[id];
                }) : [];
            },
            all: function all() {
                var values = [];
                for (var key in this.datums) {
                    values.push(this.datums[key]);
                }
                return values;
            },
            reset: function reset() {
                this.datums = {};
                this.trie = newNode();
            },
            serialize: function serialize() {
                return {
                    datums: this.datums,
                    trie: this.trie
                };
            }
        });
        return SearchIndex;
        function normalizeTokens(tokens) {
            tokens = _.filter(tokens, function(token) {
                return !!token;
            });
            tokens = _.map(tokens, function(token) {
                return token.toLowerCase();
            });
            return tokens;
        }
        function newNode() {
            var node = {};
            node[IDS] = [];
            node[CHILDREN] = {};
            return node;
        }
        function unique(array) {
            var seen = {}, uniques = [];
            for (var i = 0, len = array.length; i < len; i++) {
                if (!seen[array[i]]) {
                    seen[array[i]] = true;
                    uniques.push(array[i]);
                }
            }
            return uniques;
        }
        function getIntersection(arrayA, arrayB) {
            var ai = 0, bi = 0, intersection = [];
            arrayA = arrayA.sort();
            arrayB = arrayB.sort();
            var lenArrayA = arrayA.length, lenArrayB = arrayB.length;
            while (ai < lenArrayA && bi < lenArrayB) {
                if (arrayA[ai] < arrayB[bi]) {
                    ai++;
                } else if (arrayA[ai] > arrayB[bi]) {
                    bi++;
                } else {
                    intersection.push(arrayA[ai]);
                    ai++;
                    bi++;
                }
            }
            return intersection;
        }
    }();
    var Prefetch = function() {
        "use strict";
        var keys;
        keys = {
            data: "data",
            protocol: "protocol",
            thumbprint: "thumbprint"
        };
        function Prefetch(o) {
            this.url = o.url;
            this.ttl = o.ttl;
            this.cache = o.cache;
            this.prepare = o.prepare;
            this.transform = o.transform;
            this.transport = o.transport;
            this.thumbprint = o.thumbprint;
            this.storage = new PersistentStorage(o.cacheKey);
        }
        _.mixin(Prefetch.prototype, {
            _settings: function settings() {
                return {
                    url: this.url,
                    type: "GET",
                    dataType: "json"
                };
            },
            store: function store(data) {
                if (!this.cache) {
                    return;
                }
                this.storage.set(keys.data, data, this.ttl);
                this.storage.set(keys.protocol, location.protocol, this.ttl);
                this.storage.set(keys.thumbprint, this.thumbprint, this.ttl);
            },
            fromCache: function fromCache() {
                var stored = {}, isExpired;
                if (!this.cache) {
                    return null;
                }
                stored.data = this.storage.get(keys.data);
                stored.protocol = this.storage.get(keys.protocol);
                stored.thumbprint = this.storage.get(keys.thumbprint);
                isExpired = stored.thumbprint !== this.thumbprint || stored.protocol !== location.protocol;
                return stored.data && !isExpired ? stored.data : null;
            },
            fromNetwork: function(cb) {
                var that = this, settings;
                if (!cb) {
                    return;
                }
                settings = this.prepare(this._settings());
                this.transport(settings).fail(onError).done(onResponse);
                function onError() {
                    cb(true);
                }
                function onResponse(resp) {
                    cb(null, that.transform(resp));
                }
            },
            clear: function clear() {
                this.storage.clear();
                return this;
            }
        });
        return Prefetch;
    }();
    var Remote = function() {
        "use strict";
        function Remote(o) {
            this.url = o.url;
            this.prepare = o.prepare;
            this.transform = o.transform;
            this.transport = new Transport({
                cache: o.cache,
                limiter: o.limiter,
                transport: o.transport
            });
        }
        _.mixin(Remote.prototype, {
            _settings: function settings() {
                return {
                    url: this.url,
                    type: "GET",
                    dataType: "json"
                };
            },
            get: function get(query, cb) {
                var that = this, settings;
                if (!cb) {
                    return;
                }
                query = query || "";
                settings = this.prepare(query, this._settings());
                return this.transport.get(settings, onResponse);
                function onResponse(err, resp) {
                    err ? cb([]) : cb(that.transform(resp));
                }
            },
            cancelLastRequest: function cancelLastRequest() {
                this.transport.cancel();
            }
        });
        return Remote;
    }();
    var oParser = function() {
        "use strict";
        return function parse(o) {
            var defaults, sorter;
            defaults = {
                initialize: true,
                identify: _.stringify,
                datumTokenizer: null,
                queryTokenizer: null,
                sufficient: 5,
                sorter: null,
                local: [],
                prefetch: null,
                remote: null
            };
            o = _.mixin(defaults, o || {});
            !o.datumTokenizer && $.error("datumTokenizer is required");
            !o.queryTokenizer && $.error("queryTokenizer is required");
            sorter = o.sorter;
            o.sorter = sorter ? function(x) {
                return x.sort(sorter);
            } : _.identity;
            o.local = _.isFunction(o.local) ? o.local() : o.local;
            o.prefetch = parsePrefetch(o.prefetch);
            o.remote = parseRemote(o.remote);
            return o;
        };
        function parsePrefetch(o) {
            var defaults;
            if (!o) {
                return null;
            }
            defaults = {
                url: null,
                ttl: 24 * 60 * 60 * 1e3,
                cache: true,
                cacheKey: null,
                thumbprint: "",
                prepare: _.identity,
                transform: _.identity,
                transport: null
            };
            o = _.isString(o) ? {
                url: o
            } : o;
            o = _.mixin(defaults, o);
            !o.url && $.error("prefetch requires url to be set");
            o.transform = o.filter || o.transform;
            o.cacheKey = o.cacheKey || o.url;
            o.thumbprint = VERSION + o.thumbprint;
            o.transport = o.transport ? callbackToDeferred(o.transport) : $.ajax;
            return o;
        }
        function parseRemote(o) {
            var defaults;
            if (!o) {
                return;
            }
            defaults = {
                url: null,
                cache: true,
                prepare: null,
                replace: null,
                wildcard: null,
                limiter: null,
                rateLimitBy: "debounce",
                rateLimitWait: 300,
                transform: _.identity,
                transport: null
            };
            o = _.isString(o) ? {
                url: o
            } : o;
            o = _.mixin(defaults, o);
            !o.url && $.error("remote requires url to be set");
            o.transform = o.filter || o.transform;
            o.prepare = toRemotePrepare(o);
            o.limiter = toLimiter(o);
            o.transport = o.transport ? callbackToDeferred(o.transport) : $.ajax;
            delete o.replace;
            delete o.wildcard;
            delete o.rateLimitBy;
            delete o.rateLimitWait;
            return o;
        }
        function toRemotePrepare(o) {
            var prepare, replace, wildcard;
            prepare = o.prepare;
            replace = o.replace;
            wildcard = o.wildcard;
            if (prepare) {
                return prepare;
            }
            if (replace) {
                prepare = prepareByReplace;
            } else if (o.wildcard) {
                prepare = prepareByWildcard;
            } else {
                prepare = idenityPrepare;
            }
            return prepare;
            function prepareByReplace(query, settings) {
                settings.url = replace(settings.url, query);
                return settings;
            }
            function prepareByWildcard(query, settings) {
                settings.url = settings.url.replace(wildcard, encodeURIComponent(query));
                return settings;
            }
            function idenityPrepare(query, settings) {
                return settings;
            }
        }
        function toLimiter(o) {
            var limiter, method, wait;
            limiter = o.limiter;
            method = o.rateLimitBy;
            wait = o.rateLimitWait;
            if (!limiter) {
                limiter = /^throttle$/i.test(method) ? throttle(wait) : debounce(wait);
            }
            return limiter;
            function debounce(wait) {
                return function debounce(fn) {
                    return _.debounce(fn, wait);
                };
            }
            function throttle(wait) {
                return function throttle(fn) {
                    return _.throttle(fn, wait);
                };
            }
        }
        function callbackToDeferred(fn) {
            return function wrapper(o) {
                var deferred = $.Deferred();
                fn(o, onSuccess, onError);
                return deferred;
                function onSuccess(resp) {
                    _.defer(function() {
                        deferred.resolve(resp);
                    });
                }
                function onError(err) {
                    _.defer(function() {
                        deferred.reject(err);
                    });
                }
            };
        }
    }();
    var Bloodhound = function() {
        "use strict";
        var old;
        old = window && window.Bloodhound;
        function Bloodhound(o) {
            o = oParser(o);
            this.sorter = o.sorter;
            this.identify = o.identify;
            this.sufficient = o.sufficient;
            this.local = o.local;
            this.remote = o.remote ? new Remote(o.remote) : null;
            this.prefetch = o.prefetch ? new Prefetch(o.prefetch) : null;
            this.index = new SearchIndex({
                identify: this.identify,
                datumTokenizer: o.datumTokenizer,
                queryTokenizer: o.queryTokenizer
            });
            o.initialize !== false && this.initialize();
        }
        Bloodhound.noConflict = function noConflict() {
            window && (window.Bloodhound = old);
            return Bloodhound;
        };
        Bloodhound.tokenizers = tokenizers;
        _.mixin(Bloodhound.prototype, {
            __ttAdapter: function ttAdapter() {
                var that = this;
                return this.remote ? withAsync : withoutAsync;
                function withAsync(query, sync, async) {
                    return that.search(query, sync, async);
                }
                function withoutAsync(query, sync) {
                    return that.search(query, sync);
                }
            },
            _loadPrefetch: function loadPrefetch() {
                var that = this, deferred, serialized;
                deferred = $.Deferred();
                if (!this.prefetch) {
                    deferred.resolve();
                } else if (serialized = this.prefetch.fromCache()) {
                    this.index.bootstrap(serialized);
                    deferred.resolve();
                } else {
                    this.prefetch.fromNetwork(done);
                }
                return deferred.promise();
                function done(err, data) {
                    if (err) {
                        return deferred.reject();
                    }
                    that.add(data);
                    that.prefetch.store(that.index.serialize());
                    deferred.resolve();
                }
            },
            _initialize: function initialize() {
                var that = this, deferred;
                this.clear();
                (this.initPromise = this._loadPrefetch()).done(addLocalToIndex);
                return this.initPromise;
                function addLocalToIndex() {
                    that.add(that.local);
                }
            },
            initialize: function initialize(force) {
                return !this.initPromise || force ? this._initialize() : this.initPromise;
            },
            add: function add(data) {
                this.index.add(data);
                return this;
            },
            get: function get(ids) {
                ids = _.isArray(ids) ? ids : [].slice.call(arguments);
                return this.index.get(ids);
            },
            search: function search(query, sync, async) {
                var that = this, local;
                local = this.sorter(this.index.search(query));
                sync(this.remote ? local.slice() : local);
                if (this.remote && local.length < this.sufficient) {
                    this.remote.get(query, processRemote);
                } else if (this.remote) {
                    this.remote.cancelLastRequest();
                }
                return this;
                function processRemote(remote) {
                    var nonDuplicates = [];
                    _.each(remote, function(r) {
                        !_.some(local, function(l) {
                            return that.identify(r) === that.identify(l);
                        }) && nonDuplicates.push(r);
                    });
                    async && async(nonDuplicates);
                }
            },
            all: function all() {
                return this.index.all();
            },
            clear: function clear() {
                this.index.reset();
                return this;
            },
            clearPrefetchCache: function clearPrefetchCache() {
                this.prefetch && this.prefetch.clear();
                return this;
            },
            clearRemoteCache: function clearRemoteCache() {
                Transport.resetCache();
                return this;
            },
            ttAdapter: function ttAdapter() {
                return this.__ttAdapter();
            }
        });
        return Bloodhound;
    }();
    return Bloodhound;
});

(function(root, factory) {
    if (typeof define === "function" && define.amd) {
        define("typeahead.js", [ "jquery" ], function(a0) {
            return factory(a0);
        });
    } else if (typeof exports === "object") {
        module.exports = factory(require("jquery"));
    } else {
        factory(jQuery);
    }
})(this, function($) {
    var _ = function() {
        "use strict";
        return {
            isMsie: function() {
                return /(msie|trident)/i.test(navigator.userAgent) ? navigator.userAgent.match(/(msie |rv:)(\d+(.\d+)?)/i)[2] : false;
            },
            isBlankString: function(str) {
                return !str || /^\s*$/.test(str);
            },
            escapeRegExChars: function(str) {
                return str.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
            },
            isString: function(obj) {
                return typeof obj === "string";
            },
            isNumber: function(obj) {
                return typeof obj === "number";
            },
            isArray: $.isArray,
            isFunction: $.isFunction,
            isObject: $.isPlainObject,
            isUndefined: function(obj) {
                return typeof obj === "undefined";
            },
            isElement: function(obj) {
                return !!(obj && obj.nodeType === 1);
            },
            isJQuery: function(obj) {
                return obj instanceof $;
            },
            toStr: function toStr(s) {
                return _.isUndefined(s) || s === null ? "" : s + "";
            },
            bind: $.proxy,
            each: function(collection, cb) {
                $.each(collection, reverseArgs);
                function reverseArgs(index, value) {
                    return cb(value, index);
                }
            },
            map: $.map,
            filter: $.grep,
            every: function(obj, test) {
                var result = true;
                if (!obj) {
                    return result;
                }
                $.each(obj, function(key, val) {
                    if (!(result = test.call(null, val, key, obj))) {
                        return false;
                    }
                });
                return !!result;
            },
            some: function(obj, test) {
                var result = false;
                if (!obj) {
                    return result;
                }
                $.each(obj, function(key, val) {
                    if (result = test.call(null, val, key, obj)) {
                        return false;
                    }
                });
                return !!result;
            },
            mixin: $.extend,
            identity: function(x) {
                return x;
            },
            clone: function(obj) {
                return $.extend(true, {}, obj);
            },
            getIdGenerator: function() {
                var counter = 0;
                return function() {
                    return counter++;
                };
            },
            templatify: function templatify(obj) {
                return $.isFunction(obj) ? obj : template;
                function template() {
                    return String(obj);
                }
            },
            defer: function(fn) {
                setTimeout(fn, 0);
            },
            debounce: function(func, wait, immediate) {
                var timeout, result;
                return function() {
                    var context = this, args = arguments, later, callNow;
                    later = function() {
                        timeout = null;
                        if (!immediate) {
                            result = func.apply(context, args);
                        }
                    };
                    callNow = immediate && !timeout;
                    clearTimeout(timeout);
                    timeout = setTimeout(later, wait);
                    if (callNow) {
                        result = func.apply(context, args);
                    }
                    return result;
                };
            },
            throttle: function(func, wait) {
                var context, args, timeout, result, previous, later;
                previous = 0;
                later = function() {
                    previous = new Date();
                    timeout = null;
                    result = func.apply(context, args);
                };
                return function() {
                    var now = new Date(), remaining = wait - (now - previous);
                    context = this;
                    args = arguments;
                    if (remaining <= 0) {
                        clearTimeout(timeout);
                        timeout = null;
                        previous = now;
                        result = func.apply(context, args);
                    } else if (!timeout) {
                        timeout = setTimeout(later, remaining);
                    }
                    return result;
                };
            },
            stringify: function(val) {
                return _.isString(val) ? val : JSON.stringify(val);
            },
            noop: function() {}
        };
    }();
    var WWW = function() {
        "use strict";
        var defaultClassNames = {
            wrapper: "twitter-typeahead",
            input: "tt-input",
            hint: "tt-hint",
            menu: "tt-menu",
            dataset: "tt-dataset",
            suggestion: "tt-suggestion",
            selectable: "tt-selectable",
            empty: "tt-empty",
            open: "tt-open",
            cursor: "tt-cursor",
            highlight: "tt-highlight"
        };
        return build;
        function build(o) {
            var www, classes;
            classes = _.mixin({}, defaultClassNames, o);
            www = {
                css: buildCss(),
                classes: classes,
                html: buildHtml(classes),
                selectors: buildSelectors(classes)
            };
            return {
                css: www.css,
                html: www.html,
                classes: www.classes,
                selectors: www.selectors,
                mixin: function(o) {
                    _.mixin(o, www);
                }
            };
        }
        function buildHtml(c) {
            return {
                wrapper: '<span class="' + c.wrapper + '"></span>',
                menu: '<div class="' + c.menu + '"></div>'
            };
        }
        function buildSelectors(classes) {
            var selectors = {};
            _.each(classes, function(v, k) {
                selectors[k] = "." + v;
            });
            return selectors;
        }
        function buildCss() {
            var css = {
                wrapper: {
                    position: "relative",
                    display: "inline-block"
                },
                hint: {
                    position: "absolute",
                    top: "0",
                    left: "0",
                    borderColor: "transparent",
                    boxShadow: "none",
                    opacity: "1"
                },
                input: {
                    position: "relative",
                    verticalAlign: "top",
                    backgroundColor: "transparent"
                },
                inputWithNoHint: {
                    position: "relative",
                    verticalAlign: "top"
                },
                menu: {
                    position: "absolute",
                    top: "100%",
                    left: "0",
                    zIndex: "100",
                    display: "none"
                },
                ltr: {
                    left: "0",
                    right: "auto"
                },
                rtl: {
                    left: "auto",
                    right: " 0"
                }
            };
            if (_.isMsie()) {
                _.mixin(css.input, {
                    backgroundImage: "url(data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7)"
                });
            }
            return css;
        }
    }();
    var EventBus = function() {
        "use strict";
        var namespace, deprecationMap;
        namespace = "typeahead:";
        deprecationMap = {
            render: "rendered",
            cursorchange: "cursorchanged",
            select: "selected",
            autocomplete: "autocompleted"
        };
        function EventBus(o) {
            if (!o || !o.el) {
                $.error("EventBus initialized without el");
            }
            this.$el = $(o.el);
        }
        _.mixin(EventBus.prototype, {
            _trigger: function(type, args) {
                var $e;
                $e = $.Event(namespace + type);
                (args = args || []).unshift($e);
                this.$el.trigger.apply(this.$el, args);
                return $e;
            },
            before: function(type) {
                var args, $e;
                args = [].slice.call(arguments, 1);
                $e = this._trigger("before" + type, args);
                return $e.isDefaultPrevented();
            },
            trigger: function(type) {
                var deprecatedType;
                this._trigger(type, [].slice.call(arguments, 1));
                if (deprecatedType = deprecationMap[type]) {
                    this._trigger(deprecatedType, [].slice.call(arguments, 1));
                }
            }
        });
        return EventBus;
    }();
    var EventEmitter = function() {
        "use strict";
        var splitter = /\s+/, nextTick = getNextTick();
        return {
            onSync: onSync,
            onAsync: onAsync,
            off: off,
            trigger: trigger
        };
        function on(method, types, cb, context) {
            var type;
            if (!cb) {
                return this;
            }
            types = types.split(splitter);
            cb = context ? bindContext(cb, context) : cb;
            this._callbacks = this._callbacks || {};
            while (type = types.shift()) {
                this._callbacks[type] = this._callbacks[type] || {
                    sync: [],
                    async: []
                };
                this._callbacks[type][method].push(cb);
            }
            return this;
        }
        function onAsync(types, cb, context) {
            return on.call(this, "async", types, cb, context);
        }
        function onSync(types, cb, context) {
            return on.call(this, "sync", types, cb, context);
        }
        function off(types) {
            var type;
            if (!this._callbacks) {
                return this;
            }
            types = types.split(splitter);
            while (type = types.shift()) {
                delete this._callbacks[type];
            }
            return this;
        }
        function trigger(types) {
            var type, callbacks, args, syncFlush, asyncFlush;
            if (!this._callbacks) {
                return this;
            }
            types = types.split(splitter);
            args = [].slice.call(arguments, 1);
            while ((type = types.shift()) && (callbacks = this._callbacks[type])) {
                syncFlush = getFlush(callbacks.sync, this, [ type ].concat(args));
                asyncFlush = getFlush(callbacks.async, this, [ type ].concat(args));
                syncFlush() && nextTick(asyncFlush);
            }
            return this;
        }
        function getFlush(callbacks, context, args) {
            return flush;
            function flush() {
                var cancelled;
                for (var i = 0, len = callbacks.length; !cancelled && i < len; i += 1) {
                    cancelled = callbacks[i].apply(context, args) === false;
                }
                return !cancelled;
            }
        }
        function getNextTick() {
            var nextTickFn;
            if (window.setImmediate) {
                nextTickFn = function nextTickSetImmediate(fn) {
                    setImmediate(function() {
                        fn();
                    });
                };
            } else {
                nextTickFn = function nextTickSetTimeout(fn) {
                    setTimeout(function() {
                        fn();
                    }, 0);
                };
            }
            return nextTickFn;
        }
        function bindContext(fn, context) {
            return fn.bind ? fn.bind(context) : function() {
                fn.apply(context, [].slice.call(arguments, 0));
            };
        }
    }();
    var highlight = function(doc) {
        "use strict";
        var defaults = {
            node: null,
            pattern: null,
            tagName: "strong",
            className: null,
            wordsOnly: false,
            caseSensitive: false
        };
        return function hightlight(o) {
            var regex;
            o = _.mixin({}, defaults, o);
            if (!o.node || !o.pattern) {
                return;
            }
            o.pattern = _.isArray(o.pattern) ? o.pattern : [ o.pattern ];
            regex = getRegex(o.pattern, o.caseSensitive, o.wordsOnly);
            traverse(o.node, hightlightTextNode);
            function hightlightTextNode(textNode) {
                var match, patternNode, wrapperNode;
                if (match = regex.exec(textNode.data)) {
                    wrapperNode = doc.createElement(o.tagName);
                    o.className && (wrapperNode.className = o.className);
                    patternNode = textNode.splitText(match.index);
                    patternNode.splitText(match[0].length);
                    wrapperNode.appendChild(patternNode.cloneNode(true));
                    textNode.parentNode.replaceChild(wrapperNode, patternNode);
                }
                return !!match;
            }
            function traverse(el, hightlightTextNode) {
                var childNode, TEXT_NODE_TYPE = 3;
                for (var i = 0; i < el.childNodes.length; i++) {
                    childNode = el.childNodes[i];
                    if (childNode.nodeType === TEXT_NODE_TYPE) {
                        i += hightlightTextNode(childNode) ? 1 : 0;
                    } else {
                        traverse(childNode, hightlightTextNode);
                    }
                }
            }
        };
        function getRegex(patterns, caseSensitive, wordsOnly) {
            var escapedPatterns = [], regexStr;
            for (var i = 0, len = patterns.length; i < len; i++) {
                escapedPatterns.push(_.escapeRegExChars(patterns[i]));
            }
            regexStr = wordsOnly ? "\\b(" + escapedPatterns.join("|") + ")\\b" : "(" + escapedPatterns.join("|") + ")";
            return caseSensitive ? new RegExp(regexStr) : new RegExp(regexStr, "i");
        }
    }(window.document);
    var Input = function() {
        "use strict";
        var specialKeyCodeMap;
        specialKeyCodeMap = {
            9: "tab",
            27: "esc",
            37: "left",
            39: "right",
            13: "enter",
            38: "up",
            40: "down"
        };
        function Input(o, www) {
            o = o || {};
            if (!o.input) {
                $.error("input is missing");
            }
            www.mixin(this);
            this.$hint = $(o.hint);
            this.$input = $(o.input);
            this.query = this.$input.val();
            this.queryWhenFocused = this.hasFocus() ? this.query : null;
            this.$overflowHelper = buildOverflowHelper(this.$input);
            this._checkLanguageDirection();
            if (this.$hint.length === 0) {
                this.setHint = this.getHint = this.clearHint = this.clearHintIfInvalid = _.noop;
            }
        }
        Input.normalizeQuery = function(str) {
            return _.toStr(str).replace(/^\s*/g, "").replace(/\s{2,}/g, " ");
        };
        _.mixin(Input.prototype, EventEmitter, {
            _onBlur: function onBlur() {
                this.resetInputValue();
                this.trigger("blurred");
            },
            _onFocus: function onFocus() {
                this.queryWhenFocused = this.query;
                this.trigger("focused");
            },
            _onKeydown: function onKeydown($e) {
                var keyName = specialKeyCodeMap[$e.which || $e.keyCode];
                this._managePreventDefault(keyName, $e);
                if (keyName && this._shouldTrigger(keyName, $e)) {
                    this.trigger(keyName + "Keyed", $e);
                }
            },
            _onInput: function onInput() {
                this._setQuery(this.getInputValue());
                this.clearHintIfInvalid();
                this._checkLanguageDirection();
            },
            _managePreventDefault: function managePreventDefault(keyName, $e) {
                var preventDefault;
                switch (keyName) {
                  case "up":
                  case "down":
                    preventDefault = !withModifier($e);
                    break;

                  default:
                    preventDefault = false;
                }
                preventDefault && $e.preventDefault();
            },
            _shouldTrigger: function shouldTrigger(keyName, $e) {
                var trigger;
                switch (keyName) {
                  case "tab":
                    trigger = !withModifier($e);
                    break;

                  default:
                    trigger = true;
                }
                return trigger;
            },
            _checkLanguageDirection: function checkLanguageDirection() {
                var dir = (this.$input.css("direction") || "ltr").toLowerCase();
                if (this.dir !== dir) {
                    this.dir = dir;
                    this.$hint.attr("dir", dir);
                    this.trigger("langDirChanged", dir);
                }
            },
            _setQuery: function setQuery(val, silent) {
                var areEquivalent, hasDifferentWhitespace;
                areEquivalent = areQueriesEquivalent(val, this.query);
                hasDifferentWhitespace = areEquivalent ? this.query.length !== val.length : false;
                this.query = val;
                if (!silent && !areEquivalent) {
                    this.trigger("queryChanged", this.query);
                } else if (!silent && hasDifferentWhitespace) {
                    this.trigger("whitespaceChanged", this.query);
                }
            },
            bind: function() {
                var that = this, onBlur, onFocus, onKeydown, onInput;
                onBlur = _.bind(this._onBlur, this);
                onFocus = _.bind(this._onFocus, this);
                onKeydown = _.bind(this._onKeydown, this);
                onInput = _.bind(this._onInput, this);
                this.$input.on("blur.tt", onBlur).on("focus.tt", onFocus).on("keydown.tt", onKeydown);
                if (!_.isMsie() || _.isMsie() > 9) {
                    this.$input.on("input.tt", onInput);
                } else {
                    this.$input.on("keydown.tt keypress.tt cut.tt paste.tt", function($e) {
                        if (specialKeyCodeMap[$e.which || $e.keyCode]) {
                            return;
                        }
                        _.defer(_.bind(that._onInput, that, $e));
                    });
                }
                return this;
            },
            focus: function focus() {
                this.$input.focus();
            },
            blur: function blur() {
                this.$input.blur();
            },
            getLangDir: function getLangDir() {
                return this.dir;
            },
            getQuery: function getQuery() {
                return this.query || "";
            },
            setQuery: function setQuery(val, silent) {
                this.setInputValue(val);
                this._setQuery(val, silent);
            },
            hasQueryChangedSinceLastFocus: function hasQueryChangedSinceLastFocus() {
                return this.query !== this.queryWhenFocused;
            },
            getInputValue: function getInputValue() {
                return this.$input.val();
            },
            setInputValue: function setInputValue(value) {
                this.$input.val(value);
                this.clearHintIfInvalid();
                this._checkLanguageDirection();
            },
            resetInputValue: function resetInputValue() {
                this.setInputValue(this.query);
            },
            getHint: function getHint() {
                return this.$hint.val();
            },
            setHint: function setHint(value) {
                this.$hint.val(value);
            },
            clearHint: function clearHint() {
                this.setHint("");
            },
            clearHintIfInvalid: function clearHintIfInvalid() {
                var val, hint, valIsPrefixOfHint, isValid;
                val = this.getInputValue();
                hint = this.getHint();
                valIsPrefixOfHint = val !== hint && hint.indexOf(val) === 0;
                isValid = val !== "" && valIsPrefixOfHint && !this.hasOverflow();
                !isValid && this.clearHint();
            },
            hasFocus: function hasFocus() {
                return this.$input.is(":focus");
            },
            hasOverflow: function hasOverflow() {
                var constraint = this.$input.width() - 2;
                this.$overflowHelper.text(this.getInputValue());
                return this.$overflowHelper.width() >= constraint;
            },
            isCursorAtEnd: function() {
                var valueLength, selectionStart, range;
                valueLength = this.$input.val().length;
                selectionStart = this.$input[0].selectionStart;
                if (_.isNumber(selectionStart)) {
                    return selectionStart === valueLength;
                } else if (document.selection) {
                    range = document.selection.createRange();
                    range.moveStart("character", -valueLength);
                    return valueLength === range.text.length;
                }
                return true;
            },
            destroy: function destroy() {
                this.$hint.off(".tt");
                this.$input.off(".tt");
                this.$overflowHelper.remove();
                this.$hint = this.$input = this.$overflowHelper = $("<div>");
            }
        });
        return Input;
        function buildOverflowHelper($input) {
            return $('<pre aria-hidden="true"></pre>').css({
                position: "absolute",
                visibility: "hidden",
                whiteSpace: "pre",
                fontFamily: $input.css("font-family"),
                fontSize: $input.css("font-size"),
                fontStyle: $input.css("font-style"),
                fontVariant: $input.css("font-variant"),
                fontWeight: $input.css("font-weight"),
                wordSpacing: $input.css("word-spacing"),
                letterSpacing: $input.css("letter-spacing"),
                textIndent: $input.css("text-indent"),
                textRendering: $input.css("text-rendering"),
                textTransform: $input.css("text-transform")
            }).insertAfter($input);
        }
        function areQueriesEquivalent(a, b) {
            return Input.normalizeQuery(a) === Input.normalizeQuery(b);
        }
        function withModifier($e) {
            return $e.altKey || $e.ctrlKey || $e.metaKey || $e.shiftKey;
        }
    }();
    var Dataset = function() {
        "use strict";
        var keys, nameGenerator;
        keys = {
            val: "tt-selectable-display",
            obj: "tt-selectable-object"
        };
        nameGenerator = _.getIdGenerator();
        function Dataset(o, www) {
            o = o || {};
            o.templates = o.templates || {};
            o.templates.notFound = o.templates.notFound || o.templates.empty;
            if (!o.source) {
                $.error("missing source");
            }
            if (!o.node) {
                $.error("missing node");
            }
            if (o.name && !isValidName(o.name)) {
                $.error("invalid dataset name: " + o.name);
            }
            www.mixin(this);
            this.highlight = !!o.highlight;
            this.name = o.name || nameGenerator();
            this.limit = o.limit || 5;
            this.displayFn = getDisplayFn(o.display || o.displayKey);
            this.templates = getTemplates(o.templates, this.displayFn);
            this.source = o.source.__ttAdapter ? o.source.__ttAdapter() : o.source;
            this.async = _.isUndefined(o.async) ? this.source.length > 2 : !!o.async;
            this._resetLastSuggestion();
            this.$el = $(o.node).addClass(this.classes.dataset).addClass(this.classes.dataset + "-" + this.name);
        }
        Dataset.extractData = function extractData(el) {
            var $el = $(el);
            if ($el.data(keys.obj)) {
                return {
                    val: $el.data(keys.val) || "",
                    obj: $el.data(keys.obj) || null
                };
            }
            return null;
        };
        _.mixin(Dataset.prototype, EventEmitter, {
            _overwrite: function overwrite(query, suggestions) {
                suggestions = suggestions || [];
                if (suggestions.length) {
                    this._renderSuggestions(query, suggestions);
                } else if (this.async && this.templates.pending) {
                    this._renderPending(query);
                } else if (!this.async && this.templates.notFound) {
                    this._renderNotFound(query);
                } else {
                    this._empty();
                }
                this.trigger("rendered", this.name, suggestions, false);
            },
            _append: function append(query, suggestions) {
                suggestions = suggestions || [];
                if (suggestions.length && this.$lastSuggestion.length) {
                    this._appendSuggestions(query, suggestions);
                } else if (suggestions.length) {
                    this._renderSuggestions(query, suggestions);
                } else if (!this.$lastSuggestion.length && this.templates.notFound) {
                    this._renderNotFound(query);
                }
                this.trigger("rendered", this.name, suggestions, true);
            },
            _renderSuggestions: function renderSuggestions(query, suggestions) {
                var $fragment;
                $fragment = this._getSuggestionsFragment(query, suggestions);
                this.$lastSuggestion = $fragment.children().last();
                this.$el.html($fragment).prepend(this._getHeader(query, suggestions)).append(this._getFooter(query, suggestions));
            },
            _appendSuggestions: function appendSuggestions(query, suggestions) {
                var $fragment, $lastSuggestion;
                $fragment = this._getSuggestionsFragment(query, suggestions);
                $lastSuggestion = $fragment.children().last();
                this.$lastSuggestion.after($fragment);
                this.$lastSuggestion = $lastSuggestion;
            },
            _renderPending: function renderPending(query) {
                var template = this.templates.pending;
                this._resetLastSuggestion();
                template && this.$el.html(template({
                    query: query,
                    dataset: this.name
                }));
            },
            _renderNotFound: function renderNotFound(query) {
                var template = this.templates.notFound;
                this._resetLastSuggestion();
                template && this.$el.html(template({
                    query: query,
                    dataset: this.name
                }));
            },
            _empty: function empty() {
                this.$el.empty();
                this._resetLastSuggestion();
            },
            _getSuggestionsFragment: function getSuggestionsFragment(query, suggestions) {
                var that = this, fragment;
                fragment = document.createDocumentFragment();
                _.each(suggestions, function getSuggestionNode(suggestion) {
                    var $el, context;
                    context = that._injectQuery(query, suggestion);
                    $el = $(that.templates.suggestion(context)).data(keys.obj, suggestion).data(keys.val, that.displayFn(suggestion)).addClass(that.classes.suggestion + " " + that.classes.selectable);
                    fragment.appendChild($el[0]);
                });
                this.highlight && highlight({
                    className: this.classes.highlight,
                    node: fragment,
                    pattern: query
                });
                return $(fragment);
            },
            _getFooter: function getFooter(query, suggestions) {
                return this.templates.footer ? this.templates.footer({
                    query: query,
                    suggestions: suggestions,
                    dataset: this.name
                }) : null;
            },
            _getHeader: function getHeader(query, suggestions) {
                return this.templates.header ? this.templates.header({
                    query: query,
                    suggestions: suggestions,
                    dataset: this.name
                }) : null;
            },
            _resetLastSuggestion: function resetLastSuggestion() {
                this.$lastSuggestion = $();
            },
            _injectQuery: function injectQuery(query, obj) {
                return _.isObject(obj) ? _.mixin({
                    _query: query
                }, obj) : obj;
            },
            update: function update(query) {
                var that = this, canceled = false, syncCalled = false, rendered = 0;
                this.cancel();
                this.cancel = function cancel() {
                    canceled = true;
                    that.cancel = $.noop;
                    that.async && that.trigger("asyncCanceled", query);
                };
                this.source(query, sync, async);
                !syncCalled && sync([]);
                function sync(suggestions) {
                    if (syncCalled) {
                        return;
                    }
                    syncCalled = true;
                    suggestions = (suggestions || []).slice(0, that.limit);
                    rendered = suggestions.length;
                    that._overwrite(query, suggestions);
                    if (rendered < that.limit && that.async) {
                        that.trigger("asyncRequested", query);
                    }
                }
                function async(suggestions) {
                    suggestions = suggestions || [];
                    if (!canceled && rendered < that.limit) {
                        that.cancel = $.noop;
                        rendered += suggestions.length;
                        that._append(query, suggestions.slice(0, that.limit - rendered));
                        that.async && that.trigger("asyncReceived", query);
                    }
                }
            },
            cancel: $.noop,
            clear: function clear() {
                this._empty();
                this.cancel();
                this.trigger("cleared");
            },
            isEmpty: function isEmpty() {
                return this.$el.is(":empty");
            },
            destroy: function destroy() {
                this.$el = $("<div>");
            }
        });
        return Dataset;
        function getDisplayFn(display) {
            display = display || _.stringify;
            return _.isFunction(display) ? display : displayFn;
            function displayFn(obj) {
                return obj[display];
            }
        }
        function getTemplates(templates, displayFn) {
            return {
                notFound: templates.notFound && _.templatify(templates.notFound),
                pending: templates.pending && _.templatify(templates.pending),
                header: templates.header && _.templatify(templates.header),
                footer: templates.footer && _.templatify(templates.footer),
                suggestion: templates.suggestion || suggestionTemplate
            };
            function suggestionTemplate(context) {
                return $("<div>").text(displayFn(context));
            }
        }
        function isValidName(str) {
            return /^[_a-zA-Z0-9-]+$/.test(str);
        }
    }();
    var Menu = function() {
        "use strict";
        function Menu(o, www) {
            var that = this;
            o = o || {};
            if (!o.node) {
                $.error("node is required");
            }
            www.mixin(this);
            this.$node = $(o.node);
            this.query = null;
            this.datasets = _.map(o.datasets, initializeDataset);
            function initializeDataset(oDataset) {
                var node = that.$node.find(oDataset.node).first();
                oDataset.node = node.length ? node : $("<div>").appendTo(that.$node);
                return new Dataset(oDataset, www);
            }
        }
        _.mixin(Menu.prototype, EventEmitter, {
            _onSelectableClick: function onSelectableClick($e) {
                this.trigger("selectableClicked", $($e.currentTarget));
            },
            _onRendered: function onRendered(type, dataset, suggestions, async) {
                this.$node.toggleClass(this.classes.empty, this._allDatasetsEmpty());
                this.trigger("datasetRendered", dataset, suggestions, async);
            },
            _onCleared: function onCleared() {
                this.$node.toggleClass(this.classes.empty, this._allDatasetsEmpty());
                this.trigger("datasetCleared");
            },
            _propagate: function propagate() {
                this.trigger.apply(this, arguments);
            },
            _allDatasetsEmpty: function allDatasetsEmpty() {
                return _.every(this.datasets, isDatasetEmpty);
                function isDatasetEmpty(dataset) {
                    return dataset.isEmpty();
                }
            },
            _getSelectables: function getSelectables() {
                return this.$node.find(this.selectors.selectable);
            },
            _removeCursor: function _removeCursor() {
                var $selectable = this.getActiveSelectable();
                $selectable && $selectable.removeClass(this.classes.cursor);
            },
            _ensureVisible: function ensureVisible($el) {
                var elTop, elBottom, nodeScrollTop, nodeHeight;
                elTop = $el.position().top;
                elBottom = elTop + $el.outerHeight(true);
                nodeScrollTop = this.$node.scrollTop();
                nodeHeight = this.$node.height() + parseInt(this.$node.css("paddingTop"), 10) + parseInt(this.$node.css("paddingBottom"), 10);
                if (elTop < 0) {
                    this.$node.scrollTop(nodeScrollTop + elTop);
                } else if (nodeHeight < elBottom) {
                    this.$node.scrollTop(nodeScrollTop + (elBottom - nodeHeight));
                }
            },
            bind: function() {
                var that = this, onSelectableClick;
                onSelectableClick = _.bind(this._onSelectableClick, this);
                this.$node.on("click.tt", this.selectors.selectable, onSelectableClick);
                _.each(this.datasets, function(dataset) {
                    dataset.onSync("asyncRequested", that._propagate, that).onSync("asyncCanceled", that._propagate, that).onSync("asyncReceived", that._propagate, that).onSync("rendered", that._onRendered, that).onSync("cleared", that._onCleared, that);
                });
                return this;
            },
            isOpen: function isOpen() {
                return this.$node.hasClass(this.classes.open);
            },
            open: function open() {
                this.$node.addClass(this.classes.open);
            },
            close: function close() {
                this.$node.removeClass(this.classes.open);
                this._removeCursor();
            },
            setLanguageDirection: function setLanguageDirection(dir) {
                this.$node.attr("dir", dir);
            },
            selectableRelativeToCursor: function selectableRelativeToCursor(delta) {
                var $selectables, $oldCursor, oldIndex, newIndex;
                $oldCursor = this.getActiveSelectable();
                $selectables = this._getSelectables();
                oldIndex = $oldCursor ? $selectables.index($oldCursor) : -1;
                newIndex = oldIndex + delta;
                newIndex = (newIndex + 1) % ($selectables.length + 1) - 1;
                newIndex = newIndex < -1 ? $selectables.length - 1 : newIndex;
                return newIndex === -1 ? null : $selectables.eq(newIndex);
            },
            setCursor: function setCursor($selectable) {
                this._removeCursor();
                if ($selectable = $selectable && $selectable.first()) {
                    $selectable.addClass(this.classes.cursor);
                    this._ensureVisible($selectable);
                }
            },
            getSelectableData: function getSelectableData($el) {
                return $el && $el.length ? Dataset.extractData($el) : null;
            },
            getActiveSelectable: function getActiveSelectable() {
                var $selectable = this._getSelectables().filter(this.selectors.cursor).first();
                return $selectable.length ? $selectable : null;
            },
            getTopSelectable: function getTopSelectable() {
                var $selectable = this._getSelectables().first();
                return $selectable.length ? $selectable : null;
            },
            update: function update(query) {
                var isValidUpdate = query !== this.query;
                if (isValidUpdate) {
                    this.query = query;
                    _.each(this.datasets, updateDataset);
                }
                return isValidUpdate;
                function updateDataset(dataset) {
                    dataset.update(query);
                }
            },
            empty: function empty() {
                _.each(this.datasets, clearDataset);
                this.query = null;
                this.$node.addClass(this.classes.empty);
                function clearDataset(dataset) {
                    dataset.clear();
                }
            },
            destroy: function destroy() {
                this.$node.off(".tt");
                this.$node = $("<div>");
                _.each(this.datasets, destroyDataset);
                function destroyDataset(dataset) {
                    dataset.destroy();
                }
            }
        });
        return Menu;
    }();
    var DefaultMenu = function() {
        "use strict";
        var s = Menu.prototype;
        function DefaultMenu() {
            Menu.apply(this, [].slice.call(arguments, 0));
        }
        _.mixin(DefaultMenu.prototype, Menu.prototype, {
            open: function open() {
                !this._allDatasetsEmpty() && this._show();
                return s.open.apply(this, [].slice.call(arguments, 0));
            },
            close: function close() {
                this._hide();
                return s.close.apply(this, [].slice.call(arguments, 0));
            },
            _onRendered: function onRendered() {
                if (this._allDatasetsEmpty()) {
                    this._hide();
                } else {
                    this.isOpen() && this._show();
                }
                return s._onRendered.apply(this, [].slice.call(arguments, 0));
            },
            _onCleared: function onCleared() {
                if (this._allDatasetsEmpty()) {
                    this._hide();
                } else {
                    this.isOpen() && this._show();
                }
                return s._onCleared.apply(this, [].slice.call(arguments, 0));
            },
            setLanguageDirection: function setLanguageDirection(dir) {
                this.$node.css(dir === "ltr" ? this.css.ltr : this.css.rtl);
                return s.setLanguageDirection.apply(this, [].slice.call(arguments, 0));
            },
            _hide: function hide() {
                this.$node.hide();
            },
            _show: function show() {
                this.$node.css("display", "block");
            }
        });
        return DefaultMenu;
    }();
    var Typeahead = function() {
        "use strict";
        function Typeahead(o, www) {
            var onFocused, onBlurred, onEnterKeyed, onTabKeyed, onEscKeyed, onUpKeyed, onDownKeyed, onLeftKeyed, onRightKeyed, onQueryChanged, onWhitespaceChanged;
            o = o || {};
            if (!o.input) {
                $.error("missing input");
            }
            if (!o.menu) {
                $.error("missing menu");
            }
            if (!o.eventBus) {
                $.error("missing event bus");
            }
            www.mixin(this);
            this.eventBus = o.eventBus;
            this.minLength = _.isNumber(o.minLength) ? o.minLength : 1;
            this.input = o.input;
            this.menu = o.menu;
            this.enabled = true;
            this.active = false;
            this.input.hasFocus() && this.activate();
            this.dir = this.input.getLangDir();
            this._hacks();
            this.menu.bind().onSync("selectableClicked", this._onSelectableClicked, this).onSync("asyncRequested", this._onAsyncRequested, this).onSync("asyncCanceled", this._onAsyncCanceled, this).onSync("asyncReceived", this._onAsyncReceived, this).onSync("datasetRendered", this._onDatasetRendered, this).onSync("datasetCleared", this._onDatasetCleared, this);
            onFocused = c(this, "activate", "open", "_onFocused");
            onBlurred = c(this, "deactivate", "_onBlurred");
            onEnterKeyed = c(this, "isActive", "isOpen", "_onEnterKeyed");
            onTabKeyed = c(this, "isActive", "isOpen", "_onTabKeyed");
            onEscKeyed = c(this, "isActive", "_onEscKeyed");
            onUpKeyed = c(this, "isActive", "open", "_onUpKeyed");
            onDownKeyed = c(this, "isActive", "open", "_onDownKeyed");
            onLeftKeyed = c(this, "isActive", "isOpen", "_onLeftKeyed");
            onRightKeyed = c(this, "isActive", "isOpen", "_onRightKeyed");
            onQueryChanged = c(this, "_openIfActive", "_onQueryChanged");
            onWhitespaceChanged = c(this, "_openIfActive", "_onWhitespaceChanged");
            this.input.bind().onSync("focused", onFocused, this).onSync("blurred", onBlurred, this).onSync("enterKeyed", onEnterKeyed, this).onSync("tabKeyed", onTabKeyed, this).onSync("escKeyed", onEscKeyed, this).onSync("upKeyed", onUpKeyed, this).onSync("downKeyed", onDownKeyed, this).onSync("leftKeyed", onLeftKeyed, this).onSync("rightKeyed", onRightKeyed, this).onSync("queryChanged", onQueryChanged, this).onSync("whitespaceChanged", onWhitespaceChanged, this).onSync("langDirChanged", this._onLangDirChanged, this);
        }
        _.mixin(Typeahead.prototype, {
            _hacks: function hacks() {
                var $input, $menu;
                $input = this.input.$input || $("<div>");
                $menu = this.menu.$node || $("<div>");
                $input.on("blur.tt", function($e) {
                    var active, isActive, hasActive;
                    active = document.activeElement;
                    isActive = $menu.is(active);
                    hasActive = $menu.has(active).length > 0;
                    if (_.isMsie() && (isActive || hasActive)) {
                        $e.preventDefault();
                        $e.stopImmediatePropagation();
                        _.defer(function() {
                            $input.focus();
                        });
                    }
                });
                $menu.on("mousedown.tt", function($e) {
                    $e.preventDefault();
                });
            },
            _onSelectableClicked: function onSelectableClicked(type, $el) {
                this.select($el);
            },
            _onDatasetCleared: function onDatasetCleared() {
                this._updateHint();
            },
            _onDatasetRendered: function onDatasetRendered(type, dataset, suggestions, async) {
                this._updateHint();
                this.eventBus.trigger("render", suggestions, async, dataset);
            },
            _onAsyncRequested: function onAsyncRequested(type, dataset, query) {
                this.eventBus.trigger("asyncrequest", query, dataset);
            },
            _onAsyncCanceled: function onAsyncCanceled(type, dataset, query) {
                this.eventBus.trigger("asynccancel", query, dataset);
            },
            _onAsyncReceived: function onAsyncReceived(type, dataset, query) {
                this.eventBus.trigger("asyncreceive", query, dataset);
            },
            _onFocused: function onFocused() {
                this._minLengthMet() && this.menu.update(this.input.getQuery());
            },
            _onBlurred: function onBlurred() {
                if (this.input.hasQueryChangedSinceLastFocus()) {
                    this.eventBus.trigger("change", this.input.getQuery());
                }
            },
            _onEnterKeyed: function onEnterKeyed(type, $e) {
                var $selectable;
                if ($selectable = this.menu.getActiveSelectable()) {
                    this.select($selectable) && $e.preventDefault();
                }
            },
            _onTabKeyed: function onTabKeyed(type, $e) {
                var $selectable;
                if ($selectable = this.menu.getActiveSelectable()) {
                    this.select($selectable) && $e.preventDefault();
                } else if ($selectable = this.menu.getTopSelectable()) {
                    this.autocomplete($selectable) && $e.preventDefault();
                }
            },
            _onEscKeyed: function onEscKeyed() {
                this.close();
            },
            _onUpKeyed: function onUpKeyed() {
                this.moveCursor(-1);
            },
            _onDownKeyed: function onDownKeyed() {
                this.moveCursor(+1);
            },
            _onLeftKeyed: function onLeftKeyed() {
                if (this.dir === "rtl" && this.input.isCursorAtEnd()) {
                    this.autocomplete(this.menu.getTopSelectable());
                }
            },
            _onRightKeyed: function onRightKeyed() {
                if (this.dir === "ltr" && this.input.isCursorAtEnd()) {
                    this.autocomplete(this.menu.getTopSelectable());
                }
            },
            _onQueryChanged: function onQueryChanged(e, query) {
                this._minLengthMet(query) ? this.menu.update(query) : this.menu.empty();
            },
            _onWhitespaceChanged: function onWhitespaceChanged() {
                this._updateHint();
            },
            _onLangDirChanged: function onLangDirChanged(e, dir) {
                if (this.dir !== dir) {
                    this.dir = dir;
                    this.menu.setLanguageDirection(dir);
                }
            },
            _openIfActive: function openIfActive() {
                this.isActive() && this.open();
            },
            _minLengthMet: function minLengthMet(query) {
                query = _.isString(query) ? query : this.input.getQuery() || "";
                return query.length >= this.minLength;
            },
            _updateHint: function updateHint() {
                var $selectable, data, val, query, escapedQuery, frontMatchRegEx, match;
                $selectable = this.menu.getTopSelectable();
                data = this.menu.getSelectableData($selectable);
                val = this.input.getInputValue();
                if (data && !_.isBlankString(val) && !this.input.hasOverflow()) {
                    query = Input.normalizeQuery(val);
                    escapedQuery = _.escapeRegExChars(query);
                    frontMatchRegEx = new RegExp("^(?:" + escapedQuery + ")(.+$)", "i");
                    match = frontMatchRegEx.exec(data.val);
                    match && this.input.setHint(val + match[1]);
                } else {
                    this.input.clearHint();
                }
            },
            isEnabled: function isEnabled() {
                return this.enabled;
            },
            enable: function enable() {
                this.enabled = true;
            },
            disable: function disable() {
                this.enabled = false;
            },
            isActive: function isActive() {
                return this.active;
            },
            activate: function activate() {
                if (this.isActive()) {
                    return true;
                } else if (!this.isEnabled() || this.eventBus.before("active")) {
                    return false;
                } else {
                    this.active = true;
                    this.eventBus.trigger("active");
                    return true;
                }
            },
            deactivate: function deactivate() {
                if (!this.isActive()) {
                    return true;
                } else if (this.eventBus.before("idle")) {
                    return false;
                } else {
                    this.active = false;
                    this.close();
                    this.eventBus.trigger("idle");
                    return true;
                }
            },
            isOpen: function isOpen() {
                return this.menu.isOpen();
            },
            open: function open() {
                if (!this.isOpen() && !this.eventBus.before("open")) {
                    this.menu.open();
                    this._updateHint();
                    this.eventBus.trigger("open");
                }
                return this.isOpen();
            },
            close: function close() {
                if (this.isOpen() && !this.eventBus.before("close")) {
                    this.menu.close();
                    this.input.clearHint();
                    this.input.resetInputValue();
                    this.eventBus.trigger("close");
                }
                return !this.isOpen();
            },
            setVal: function setVal(val) {
                this.input.setQuery(_.toStr(val));
            },
            getVal: function getVal() {
                return this.input.getQuery();
            },
            select: function select($selectable) {
                var data = this.menu.getSelectableData($selectable);
                if (data && !this.eventBus.before("select", data.obj)) {
                    this.input.setQuery(data.val, true);
                    this.eventBus.trigger("select", data.obj);
                    this.close();
                    return true;
                }
                return false;
            },
            autocomplete: function autocomplete($selectable) {
                var query, data, isValid;
                query = this.input.getQuery();
                data = this.menu.getSelectableData($selectable);
                isValid = data && query !== data.val;
                if (isValid && !this.eventBus.before("autocomplete", data.obj)) {
                    this.input.setQuery(data.val);
                    this.eventBus.trigger("autocomplete", data.obj);
                    return true;
                }
                return false;
            },
            moveCursor: function moveCursor(delta) {
                var query, $candidate, data, payload, cancelMove;
                query = this.input.getQuery();
                $candidate = this.menu.selectableRelativeToCursor(delta);
                data = this.menu.getSelectableData($candidate);
                payload = data ? data.obj : null;
                cancelMove = this._minLengthMet() && this.menu.update(query);
                if (!cancelMove && !this.eventBus.before("cursorchange", payload)) {
                    this.menu.setCursor($candidate);
                    if (data) {
                        this.input.setInputValue(data.val);
                    } else {
                        this.input.resetInputValue();
                        this._updateHint();
                    }
                    this.eventBus.trigger("cursorchange", payload);
                    return true;
                }
                return false;
            },
            destroy: function destroy() {
                this.input.destroy();
                this.menu.destroy();
            }
        });
        return Typeahead;
        function c(ctx) {
            var methods = [].slice.call(arguments, 1);
            return function() {
                var args = [].slice.call(arguments);
                _.each(methods, function(method) {
                    return ctx[method].apply(ctx, args);
                });
            };
        }
    }();
    (function() {
        "use strict";
        var old, keys, methods;
        old = $.fn.typeahead;
        keys = {
            www: "tt-www",
            attrs: "tt-attrs",
            typeahead: "tt-typeahead"
        };
        methods = {
            initialize: function initialize(o, datasets) {
                var www;
                datasets = _.isArray(datasets) ? datasets : [].slice.call(arguments, 1);
                o = o || {};
                www = WWW(o.classNames);
                return this.each(attach);
                function attach() {
                    var $input, $wrapper, $hint, $menu, defaultHint, defaultMenu, eventBus, input, menu, typeahead, MenuConstructor;
                    _.each(datasets, function(d) {
                        d.highlight = !!o.highlight;
                    });
                    $input = $(this);
                    $wrapper = $(www.html.wrapper);
                    $hint = $elOrNull(o.hint);
                    $menu = $elOrNull(o.menu);
                    defaultHint = o.hint !== false && !$hint;
                    defaultMenu = o.menu !== false && !$menu;
                    defaultHint && ($hint = buildHintFromInput($input, www));
                    defaultMenu && ($menu = $(www.html.menu).css(www.css.menu));
                    $hint && $hint.val("");
                    $input = prepInput($input, www);
                    if (defaultHint || defaultMenu) {
                        $wrapper.css(www.css.wrapper);
                        $input.css(defaultHint ? www.css.input : www.css.inputWithNoHint);
                        $input.wrap($wrapper).parent().prepend(defaultHint ? $hint : null).append(defaultMenu ? $menu : null);
                    }
                    MenuConstructor = defaultMenu ? DefaultMenu : Menu;
                    eventBus = new EventBus({
                        el: $input
                    });
                    input = new Input({
                        hint: $hint,
                        input: $input
                    }, www);
                    menu = new MenuConstructor({
                        node: $menu,
                        datasets: datasets
                    }, www);
                    typeahead = new Typeahead({
                        input: input,
                        menu: menu,
                        eventBus: eventBus,
                        minLength: o.minLength
                    }, www);
                    $input.data(keys.www, www);
                    $input.data(keys.typeahead, typeahead);
                }
            },
            isEnabled: function isEnabled() {
                var enabled;
                ttEach(this.first(), function(t) {
                    enabled = t.isEnabled();
                });
                return enabled;
            },
            enable: function enable() {
                ttEach(this, function(t) {
                    t.enable();
                });
                return this;
            },
            disable: function disable() {
                ttEach(this, function(t) {
                    t.disable();
                });
                return this;
            },
            isActive: function isActive() {
                var active;
                ttEach(this.first(), function(t) {
                    active = t.isActive();
                });
                return active;
            },
            activate: function activate() {
                ttEach(this, function(t) {
                    t.activate();
                });
                return this;
            },
            deactivate: function deactivate() {
                ttEach(this, function(t) {
                    t.deactivate();
                });
                return this;
            },
            isOpen: function isOpen() {
                var open;
                ttEach(this.first(), function(t) {
                    open = t.isOpen();
                });
                return open;
            },
            open: function open() {
                ttEach(this, function(t) {
                    t.open();
                });
                return this;
            },
            close: function close() {
                ttEach(this, function(t) {
                    t.close();
                });
                return this;
            },
            select: function select(el) {
                var success = false, $el = $(el);
                ttEach(this.first(), function(t) {
                    success = t.select($el);
                });
                return success;
            },
            autocomplete: function autocomplete(el) {
                var success = false, $el = $(el);
                ttEach(this.first(), function(t) {
                    success = t.autocomplete($el);
                });
                return success;
            },
            moveCursor: function moveCursoe(delta) {
                var success = false;
                ttEach(this.first(), function(t) {
                    success = t.moveCursor(delta);
                });
                return success;
            },
            val: function val(newVal) {
                var query;
                if (!arguments.length) {
                    ttEach(this.first(), function(t) {
                        query = t.getVal();
                    });
                    return query;
                } else {
                    ttEach(this, function(t) {
                        t.setVal(newVal);
                    });
                    return this;
                }
            },
            destroy: function destroy() {
                ttEach(this, function(typeahead, $input) {
                    revert($input);
                    typeahead.destroy();
                });
                return this;
            }
        };
        $.fn.typeahead = function(method) {
            if (methods[method]) {
                return methods[method].apply(this, [].slice.call(arguments, 1));
            } else {
                return methods.initialize.apply(this, arguments);
            }
        };
        $.fn.typeahead.noConflict = function noConflict() {
            $.fn.typeahead = old;
            return this;
        };
        function ttEach($els, fn) {
            $els.each(function() {
                var $input = $(this), typeahead;
                (typeahead = $input.data(keys.typeahead)) && fn(typeahead, $input);
            });
        }
        function buildHintFromInput($input, www) {
            return $input.clone().addClass(www.classes.hint).removeData().css(www.css.hint).css(getBackgroundStyles($input)).prop("readonly", true).removeAttr("id name placeholder required").attr({
                autocomplete: "off",
                spellcheck: "false",
                tabindex: -1
            });
        }
        function prepInput($input, www) {
            $input.data(keys.attrs, {
                dir: $input.attr("dir"),
                autocomplete: $input.attr("autocomplete"),
                spellcheck: $input.attr("spellcheck"),
                style: $input.attr("style")
            });
            $input.addClass(www.classes.input).attr({
                autocomplete: "off",
                spellcheck: false
            });
            try {
                !$input.attr("dir") && $input.attr("dir", "auto");
            } catch (e) {}
            return $input;
        }
        function getBackgroundStyles($el) {
            return {
                backgroundAttachment: $el.css("background-attachment"),
                backgroundClip: $el.css("background-clip"),
                backgroundColor: $el.css("background-color"),
                backgroundImage: $el.css("background-image"),
                backgroundOrigin: $el.css("background-origin"),
                backgroundPosition: $el.css("background-position"),
                backgroundRepeat: $el.css("background-repeat"),
                backgroundSize: $el.css("background-size")
            };
        }
        function revert($input) {
            var www, $wrapper;
            www = $input.data(keys.www);
            $wrapper = $input.parent().filter(www.selectors.wrapper);
            _.each($input.data(keys.attrs), function(val, key) {
                _.isUndefined(val) ? $input.removeAttr(key) : $input.attr(key, val);
            });
            $input.removeData(keys.typeahead).removeData(keys.www).removeData(keys.attr).removeClass(www.classes.input);
            if ($wrapper.length) {
                $input.detach().insertAfter($wrapper);
                $wrapper.remove();
            }
        }
        function $elOrNull(obj) {
            var isValid, $el;
            isValid = _.isJQuery(obj) || _.isElement(obj);
            $el = isValid ? $(obj).first() : [];
            return $el.length ? $el : null;
        }
    })();
});
/*!
 * Select2 4.0.3
 * https://select2.github.io
 *
 * Released under the MIT license
 * https://github.com/select2/select2/blob/master/LICENSE.md
 */

(function (factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define(['jquery'], factory);
  } else if (typeof exports === 'object') {
    // Node/CommonJS
    factory(require('jquery'));
  } else {
    // Browser globals
    factory(jQuery);
  }
}(function (jQuery) {
  // This is needed so we can catch the AMD loader configuration and use it
  // The inner file should be wrapped (by `banner.start.js`) in a function that
  // returns the AMD loader references.
  var S2 =
(function () {
  // Restore the Select2 AMD loader so it can be used
  // Needed mostly in the language files, where the loader is not inserted
  if (jQuery && jQuery.fn && jQuery.fn.select2 && jQuery.fn.select2.amd) {
    var S2 = jQuery.fn.select2.amd;
  }
var S2;(function () { if (!S2 || !S2.requirejs) {
if (!S2) { S2 = {}; } else { require = S2; }
/**
 * @license almond 0.3.1 Copyright (c) 2011-2014, The Dojo Foundation All Rights Reserved.
 * Available via the MIT or new BSD license.
 * see: http://github.com/jrburke/almond for details
 */
//Going sloppy to avoid 'use strict' string cost, but strict practices should
//be followed.
/*jslint sloppy: true */
/*global setTimeout: false */

var requirejs, require, define;
(function (undef) {
    var main, req, makeMap, handlers,
        defined = {},
        waiting = {},
        config = {},
        defining = {},
        hasOwn = Object.prototype.hasOwnProperty,
        aps = [].slice,
        jsSuffixRegExp = /\.js$/;

    function hasProp(obj, prop) {
        return hasOwn.call(obj, prop);
    }

    /**
     * Given a relative module name, like ./something, normalize it to
     * a real name that can be mapped to a path.
     * @param {String} name the relative name
     * @param {String} baseName a real name that the name arg is relative
     * to.
     * @returns {String} normalized name
     */
    function normalize(name, baseName) {
        var nameParts, nameSegment, mapValue, foundMap, lastIndex,
            foundI, foundStarMap, starI, i, j, part,
            baseParts = baseName && baseName.split("/"),
            map = config.map,
            starMap = (map && map['*']) || {};

        //Adjust any relative paths.
        if (name && name.charAt(0) === ".") {
            //If have a base name, try to normalize against it,
            //otherwise, assume it is a top-level require that will
            //be relative to baseUrl in the end.
            if (baseName) {
                name = name.split('/');
                lastIndex = name.length - 1;

                // Node .js allowance:
                if (config.nodeIdCompat && jsSuffixRegExp.test(name[lastIndex])) {
                    name[lastIndex] = name[lastIndex].replace(jsSuffixRegExp, '');
                }

                //Lop off the last part of baseParts, so that . matches the
                //"directory" and not name of the baseName's module. For instance,
                //baseName of "one/two/three", maps to "one/two/three.js", but we
                //want the directory, "one/two" for this normalization.
                name = baseParts.slice(0, baseParts.length - 1).concat(name);

                //start trimDots
                for (i = 0; i < name.length; i += 1) {
                    part = name[i];
                    if (part === ".") {
                        name.splice(i, 1);
                        i -= 1;
                    } else if (part === "..") {
                        if (i === 1 && (name[2] === '..' || name[0] === '..')) {
                            //End of the line. Keep at least one non-dot
                            //path segment at the front so it can be mapped
                            //correctly to disk. Otherwise, there is likely
                            //no path mapping for a path starting with '..'.
                            //This can still fail, but catches the most reasonable
                            //uses of ..
                            break;
                        } else if (i > 0) {
                            name.splice(i - 1, 2);
                            i -= 2;
                        }
                    }
                }
                //end trimDots

                name = name.join("/");
            } else if (name.indexOf('./') === 0) {
                // No baseName, so this is ID is resolved relative
                // to baseUrl, pull off the leading dot.
                name = name.substring(2);
            }
        }

        //Apply map config if available.
        if ((baseParts || starMap) && map) {
            nameParts = name.split('/');

            for (i = nameParts.length; i > 0; i -= 1) {
                nameSegment = nameParts.slice(0, i).join("/");

                if (baseParts) {
                    //Find the longest baseName segment match in the config.
                    //So, do joins on the biggest to smallest lengths of baseParts.
                    for (j = baseParts.length; j > 0; j -= 1) {
                        mapValue = map[baseParts.slice(0, j).join('/')];

                        //baseName segment has  config, find if it has one for
                        //this name.
                        if (mapValue) {
                            mapValue = mapValue[nameSegment];
                            if (mapValue) {
                                //Match, update name to the new value.
                                foundMap = mapValue;
                                foundI = i;
                                break;
                            }
                        }
                    }
                }

                if (foundMap) {
                    break;
                }

                //Check for a star map match, but just hold on to it,
                //if there is a shorter segment match later in a matching
                //config, then favor over this star map.
                if (!foundStarMap && starMap && starMap[nameSegment]) {
                    foundStarMap = starMap[nameSegment];
                    starI = i;
                }
            }

            if (!foundMap && foundStarMap) {
                foundMap = foundStarMap;
                foundI = starI;
            }

            if (foundMap) {
                nameParts.splice(0, foundI, foundMap);
                name = nameParts.join('/');
            }
        }

        return name;
    }

    function makeRequire(relName, forceSync) {
        return function () {
            //A version of a require function that passes a moduleName
            //value for items that may need to
            //look up paths relative to the moduleName
            var args = aps.call(arguments, 0);

            //If first arg is not require('string'), and there is only
            //one arg, it is the array form without a callback. Insert
            //a null so that the following concat is correct.
            if (typeof args[0] !== 'string' && args.length === 1) {
                args.push(null);
            }
            return req.apply(undef, args.concat([relName, forceSync]));
        };
    }

    function makeNormalize(relName) {
        return function (name) {
            return normalize(name, relName);
        };
    }

    function makeLoad(depName) {
        return function (value) {
            defined[depName] = value;
        };
    }

    function callDep(name) {
        if (hasProp(waiting, name)) {
            var args = waiting[name];
            delete waiting[name];
            defining[name] = true;
            main.apply(undef, args);
        }

        if (!hasProp(defined, name) && !hasProp(defining, name)) {
            throw new Error('No ' + name);
        }
        return defined[name];
    }

    //Turns a plugin!resource to [plugin, resource]
    //with the plugin being undefined if the name
    //did not have a plugin prefix.
    function splitPrefix(name) {
        var prefix,
            index = name ? name.indexOf('!') : -1;
        if (index > -1) {
            prefix = name.substring(0, index);
            name = name.substring(index + 1, name.length);
        }
        return [prefix, name];
    }

    /**
     * Makes a name map, normalizing the name, and using a plugin
     * for normalization if necessary. Grabs a ref to plugin
     * too, as an optimization.
     */
    makeMap = function (name, relName) {
        var plugin,
            parts = splitPrefix(name),
            prefix = parts[0];

        name = parts[1];

        if (prefix) {
            prefix = normalize(prefix, relName);
            plugin = callDep(prefix);
        }

        //Normalize according
        if (prefix) {
            if (plugin && plugin.normalize) {
                name = plugin.normalize(name, makeNormalize(relName));
            } else {
                name = normalize(name, relName);
            }
        } else {
            name = normalize(name, relName);
            parts = splitPrefix(name);
            prefix = parts[0];
            name = parts[1];
            if (prefix) {
                plugin = callDep(prefix);
            }
        }

        //Using ridiculous property names for space reasons
        return {
            f: prefix ? prefix + '!' + name : name, //fullName
            n: name,
            pr: prefix,
            p: plugin
        };
    };

    function makeConfig(name) {
        return function () {
            return (config && config.config && config.config[name]) || {};
        };
    }

    handlers = {
        require: function (name) {
            return makeRequire(name);
        },
        exports: function (name) {
            var e = defined[name];
            if (typeof e !== 'undefined') {
                return e;
            } else {
                return (defined[name] = {});
            }
        },
        module: function (name) {
            return {
                id: name,
                uri: '',
                exports: defined[name],
                config: makeConfig(name)
            };
        }
    };

    main = function (name, deps, callback, relName) {
        var cjsModule, depName, ret, map, i,
            args = [],
            callbackType = typeof callback,
            usingExports;

        //Use name if no relName
        relName = relName || name;

        //Call the callback to define the module, if necessary.
        if (callbackType === 'undefined' || callbackType === 'function') {
            //Pull out the defined dependencies and pass the ordered
            //values to the callback.
            //Default to [require, exports, module] if no deps
            deps = !deps.length && callback.length ? ['require', 'exports', 'module'] : deps;
            for (i = 0; i < deps.length; i += 1) {
                map = makeMap(deps[i], relName);
                depName = map.f;

                //Fast path CommonJS standard dependencies.
                if (depName === "require") {
                    args[i] = handlers.require(name);
                } else if (depName === "exports") {
                    //CommonJS module spec 1.1
                    args[i] = handlers.exports(name);
                    usingExports = true;
                } else if (depName === "module") {
                    //CommonJS module spec 1.1
                    cjsModule = args[i] = handlers.module(name);
                } else if (hasProp(defined, depName) ||
                           hasProp(waiting, depName) ||
                           hasProp(defining, depName)) {
                    args[i] = callDep(depName);
                } else if (map.p) {
                    map.p.load(map.n, makeRequire(relName, true), makeLoad(depName), {});
                    args[i] = defined[depName];
                } else {
                    throw new Error(name + ' missing ' + depName);
                }
            }

            ret = callback ? callback.apply(defined[name], args) : undefined;

            if (name) {
                //If setting exports via "module" is in play,
                //favor that over return value and exports. After that,
                //favor a non-undefined return value over exports use.
                if (cjsModule && cjsModule.exports !== undef &&
                        cjsModule.exports !== defined[name]) {
                    defined[name] = cjsModule.exports;
                } else if (ret !== undef || !usingExports) {
                    //Use the return value from the function.
                    defined[name] = ret;
                }
            }
        } else if (name) {
            //May just be an object definition for the module. Only
            //worry about defining if have a module name.
            defined[name] = callback;
        }
    };

    requirejs = require = req = function (deps, callback, relName, forceSync, alt) {
        if (typeof deps === "string") {
            if (handlers[deps]) {
                //callback in this case is really relName
                return handlers[deps](callback);
            }
            //Just return the module wanted. In this scenario, the
            //deps arg is the module name, and second arg (if passed)
            //is just the relName.
            //Normalize module name, if it contains . or ..
            return callDep(makeMap(deps, callback).f);
        } else if (!deps.splice) {
            //deps is a config object, not an array.
            config = deps;
            if (config.deps) {
                req(config.deps, config.callback);
            }
            if (!callback) {
                return;
            }

            if (callback.splice) {
                //callback is an array, which means it is a dependency list.
                //Adjust args if there are dependencies
                deps = callback;
                callback = relName;
                relName = null;
            } else {
                deps = undef;
            }
        }

        //Support require(['a'])
        callback = callback || function () {};

        //If relName is a function, it is an errback handler,
        //so remove it.
        if (typeof relName === 'function') {
            relName = forceSync;
            forceSync = alt;
        }

        //Simulate async callback;
        if (forceSync) {
            main(undef, deps, callback, relName);
        } else {
            //Using a non-zero value because of concern for what old browsers
            //do, and latest browsers "upgrade" to 4 if lower value is used:
            //http://www.whatwg.org/specs/web-apps/current-work/multipage/timers.html#dom-windowtimers-settimeout:
            //If want a value immediately, use require('id') instead -- something
            //that works in almond on the global level, but not guaranteed and
            //unlikely to work in other AMD implementations.
            setTimeout(function () {
                main(undef, deps, callback, relName);
            }, 4);
        }

        return req;
    };

    /**
     * Just drops the config on the floor, but returns req in case
     * the config return value is used.
     */
    req.config = function (cfg) {
        return req(cfg);
    };

    /**
     * Expose module registry for debugging and tooling
     */
    requirejs._defined = defined;

    define = function (name, deps, callback) {
        if (typeof name !== 'string') {
            throw new Error('See almond README: incorrect module build, no module name');
        }

        //This module may not have dependencies
        if (!deps.splice) {
            //deps is not an array, so probably means
            //an object literal or factory function for
            //the value. Adjust args.
            callback = deps;
            deps = [];
        }

        if (!hasProp(defined, name) && !hasProp(waiting, name)) {
            waiting[name] = [name, deps, callback];
        }
    };

    define.amd = {
        jQuery: true
    };
}());

S2.requirejs = requirejs;S2.require = require;S2.define = define;
}
}());
S2.define("almond", function(){});

/* global jQuery:false, $:false */
S2.define('jquery',[],function () {
  var _$ = jQuery || $;

  if (_$ == null && console && console.error) {
    console.error(
      'Select2: An instance of jQuery or a jQuery-compatible library was not ' +
      'found. Make sure that you are including jQuery before Select2 on your ' +
      'web page.'
    );
  }

  return _$;
});

S2.define('select2/utils',[
  'jquery'
], function ($) {
  var Utils = {};

  Utils.Extend = function (ChildClass, SuperClass) {
    var __hasProp = {}.hasOwnProperty;

    function BaseConstructor () {
      this.constructor = ChildClass;
    }

    for (var key in SuperClass) {
      if (__hasProp.call(SuperClass, key)) {
        ChildClass[key] = SuperClass[key];
      }
    }

    BaseConstructor.prototype = SuperClass.prototype;
    ChildClass.prototype = new BaseConstructor();
    ChildClass.__super__ = SuperClass.prototype;

    return ChildClass;
  };

  function getMethods (theClass) {
    var proto = theClass.prototype;

    var methods = [];

    for (var methodName in proto) {
      var m = proto[methodName];

      if (typeof m !== 'function') {
        continue;
      }

      if (methodName === 'constructor') {
        continue;
      }

      methods.push(methodName);
    }

    return methods;
  }

  Utils.Decorate = function (SuperClass, DecoratorClass) {
    var decoratedMethods = getMethods(DecoratorClass);
    var superMethods = getMethods(SuperClass);

    function DecoratedClass () {
      var unshift = Array.prototype.unshift;

      var argCount = DecoratorClass.prototype.constructor.length;

      var calledConstructor = SuperClass.prototype.constructor;

      if (argCount > 0) {
        unshift.call(arguments, SuperClass.prototype.constructor);

        calledConstructor = DecoratorClass.prototype.constructor;
      }

      calledConstructor.apply(this, arguments);
    }

    DecoratorClass.displayName = SuperClass.displayName;

    function ctr () {
      this.constructor = DecoratedClass;
    }

    DecoratedClass.prototype = new ctr();

    for (var m = 0; m < superMethods.length; m++) {
        var superMethod = superMethods[m];

        DecoratedClass.prototype[superMethod] =
          SuperClass.prototype[superMethod];
    }

    var calledMethod = function (methodName) {
      // Stub out the original method if it's not decorating an actual method
      var originalMethod = function () {};

      if (methodName in DecoratedClass.prototype) {
        originalMethod = DecoratedClass.prototype[methodName];
      }

      var decoratedMethod = DecoratorClass.prototype[methodName];

      return function () {
        var unshift = Array.prototype.unshift;

        unshift.call(arguments, originalMethod);

        return decoratedMethod.apply(this, arguments);
      };
    };

    for (var d = 0; d < decoratedMethods.length; d++) {
      var decoratedMethod = decoratedMethods[d];

      DecoratedClass.prototype[decoratedMethod] = calledMethod(decoratedMethod);
    }

    return DecoratedClass;
  };

  var Observable = function () {
    this.listeners = {};
  };

  Observable.prototype.on = function (event, callback) {
    this.listeners = this.listeners || {};

    if (event in this.listeners) {
      this.listeners[event].push(callback);
    } else {
      this.listeners[event] = [callback];
    }
  };

  Observable.prototype.trigger = function (event) {
    var slice = Array.prototype.slice;
    var params = slice.call(arguments, 1);

    this.listeners = this.listeners || {};

    // Params should always come in as an array
    if (params == null) {
      params = [];
    }

    // If there are no arguments to the event, use a temporary object
    if (params.length === 0) {
      params.push({});
    }

    // Set the `_type` of the first object to the event
    params[0]._type = event;

    if (event in this.listeners) {
      this.invoke(this.listeners[event], slice.call(arguments, 1));
    }

    if ('*' in this.listeners) {
      this.invoke(this.listeners['*'], arguments);
    }
  };

  Observable.prototype.invoke = function (listeners, params) {
    for (var i = 0, len = listeners.length; i < len; i++) {
      listeners[i].apply(this, params);
    }
  };

  Utils.Observable = Observable;

  Utils.generateChars = function (length) {
    var chars = '';

    for (var i = 0; i < length; i++) {
      var randomChar = Math.floor(Math.random() * 36);
      chars += randomChar.toString(36);
    }

    return chars;
  };

  Utils.bind = function (func, context) {
    return function () {
      func.apply(context, arguments);
    };
  };

  Utils._convertData = function (data) {
    for (var originalKey in data) {
      var keys = originalKey.split('-');

      var dataLevel = data;

      if (keys.length === 1) {
        continue;
      }

      for (var k = 0; k < keys.length; k++) {
        var key = keys[k];

        // Lowercase the first letter
        // By default, dash-separated becomes camelCase
        key = key.substring(0, 1).toLowerCase() + key.substring(1);

        if (!(key in dataLevel)) {
          dataLevel[key] = {};
        }

        if (k == keys.length - 1) {
          dataLevel[key] = data[originalKey];
        }

        dataLevel = dataLevel[key];
      }

      delete data[originalKey];
    }

    return data;
  };

  Utils.hasScroll = function (index, el) {
    // Adapted from the function created by @ShadowScripter
    // and adapted by @BillBarry on the Stack Exchange Code Review website.
    // The original code can be found at
    // http://codereview.stackexchange.com/q/13338
    // and was designed to be used with the Sizzle selector engine.

    var $el = $(el);
    var overflowX = el.style.overflowX;
    var overflowY = el.style.overflowY;

    //Check both x and y declarations
    if (overflowX === overflowY &&
        (overflowY === 'hidden' || overflowY === 'visible')) {
      return false;
    }

    if (overflowX === 'scroll' || overflowY === 'scroll') {
      return true;
    }

    return ($el.innerHeight() < el.scrollHeight ||
      $el.innerWidth() < el.scrollWidth);
  };

  Utils.escapeMarkup = function (markup) {
    var replaceMap = {
      '\\': '&#92;',
      '&': '&amp;',
      '<': '&lt;',
      '>': '&gt;',
      '"': '&quot;',
      '\'': '&#39;',
      '/': '&#47;'
    };

    // Do not try to escape the markup if it's not a string
    if (typeof markup !== 'string') {
      return markup;
    }

    return String(markup).replace(/[&<>"'\/\\]/g, function (match) {
      return replaceMap[match];
    });
  };

  // Append an array of jQuery nodes to a given element.
  Utils.appendMany = function ($element, $nodes) {
    // jQuery 1.7.x does not support $.fn.append() with an array
    // Fall back to a jQuery object collection using $.fn.add()
    if ($.fn.jquery.substr(0, 3) === '1.7') {
      var $jqNodes = $();

      $.map($nodes, function (node) {
        $jqNodes = $jqNodes.add(node);
      });

      $nodes = $jqNodes;
    }

    $element.append($nodes);
  };

  return Utils;
});

S2.define('select2/results',[
  'jquery',
  './utils'
], function ($, Utils) {
  function Results ($element, options, dataAdapter) {
    this.$element = $element;
    this.data = dataAdapter;
    this.options = options;

    Results.__super__.constructor.call(this);
  }

  Utils.Extend(Results, Utils.Observable);

  Results.prototype.render = function () {
    var $results = $(
      '<ul class="select2-results__options" role="tree"></ul>'
    );

    if (this.options.get('multiple')) {
      $results.attr('aria-multiselectable', 'true');
    }

    this.$results = $results;

    return $results;
  };

  Results.prototype.clear = function () {
    this.$results.empty();
  };

  Results.prototype.displayMessage = function (params) {
    var escapeMarkup = this.options.get('escapeMarkup');

    this.clear();
    this.hideLoading();

    var $message = $(
      '<li role="treeitem" aria-live="assertive"' +
      ' class="select2-results__option"></li>'
    );

    var message = this.options.get('translations').get(params.message);

    $message.append(
      escapeMarkup(
        message(params.args)
      )
    );

    $message[0].className += ' select2-results__message';

    this.$results.append($message);
  };

  Results.prototype.hideMessages = function () {
    this.$results.find('.select2-results__message').remove();
  };

  Results.prototype.append = function (data) {
    this.hideLoading();

    var $options = [];

    if (data.results == null || data.results.length === 0) {
      if (this.$results.children().length === 0) {
        this.trigger('results:message', {
          message: 'noResults'
        });
      }

      return;
    }

    data.results = this.sort(data.results);

    for (var d = 0; d < data.results.length; d++) {
      var item = data.results[d];

      var $option = this.option(item);

      $options.push($option);
    }

    this.$results.append($options);
  };

  Results.prototype.position = function ($results, $dropdown) {
    var $resultsContainer = $dropdown.find('.select2-results');
    $resultsContainer.append($results);
  };

  Results.prototype.sort = function (data) {
    var sorter = this.options.get('sorter');

    return sorter(data);
  };

  Results.prototype.highlightFirstItem = function () {
    var $options = this.$results
      .find('.select2-results__option[aria-selected]');

    var $selected = $options.filter('[aria-selected=true]');

    // Check if there are any selected options
    if ($selected.length > 0) {
      // If there are selected options, highlight the first
      $selected.first().trigger('mouseenter');
    } else {
      // If there are no selected options, highlight the first option
      // in the dropdown
      $options.first().trigger('mouseenter');
    }

    this.ensureHighlightVisible();
  };

  Results.prototype.setClasses = function () {
    var self = this;

    this.data.current(function (selected) {
      var selectedIds = $.map(selected, function (s) {
        return s.id.toString();
      });

      var $options = self.$results
        .find('.select2-results__option[aria-selected]');

      $options.each(function () {
        var $option = $(this);

        var item = $.data(this, 'data');

        // id needs to be converted to a string when comparing
        var id = '' + item.id;

        if ((item.element != null && item.element.selected) ||
            (item.element == null && $.inArray(id, selectedIds) > -1)) {
          $option.attr('aria-selected', 'true');
        } else {
          $option.attr('aria-selected', 'false');
        }
      });

    });
  };

  Results.prototype.showLoading = function (params) {
    this.hideLoading();

    var loadingMore = this.options.get('translations').get('searching');

    var loading = {
      disabled: true,
      loading: true,
      text: loadingMore(params)
    };
    var $loading = this.option(loading);
    $loading.className += ' loading-results';

    this.$results.prepend($loading);
  };

  Results.prototype.hideLoading = function () {
    this.$results.find('.loading-results').remove();
  };

  Results.prototype.option = function (data) {
    var option = document.createElement('li');
    option.className = 'select2-results__option';

    var attrs = {
      'role': 'treeitem',
      'aria-selected': 'false'
    };

    if (data.disabled) {
      delete attrs['aria-selected'];
      attrs['aria-disabled'] = 'true';
    }

    if (data.id == null) {
      delete attrs['aria-selected'];
    }

    if (data._resultId != null) {
      option.id = data._resultId;
    }

    if (data.title) {
      option.title = data.title;
    }

    if (data.children) {
      attrs.role = 'group';
      attrs['aria-label'] = data.text;
      delete attrs['aria-selected'];
    }

    for (var attr in attrs) {
      var val = attrs[attr];

      option.setAttribute(attr, val);
    }

    if (data.children) {
      var $option = $(option);

      var label = document.createElement('strong');
      label.className = 'select2-results__group';

      var $label = $(label);
      this.template(data, label);

      var $children = [];

      for (var c = 0; c < data.children.length; c++) {
        var child = data.children[c];

        var $child = this.option(child);

        $children.push($child);
      }

      var $childrenContainer = $('<ul></ul>', {
        'class': 'select2-results__options select2-results__options--nested'
      });

      $childrenContainer.append($children);

      $option.append(label);
      $option.append($childrenContainer);
    } else {
      this.template(data, option);
    }

    $.data(option, 'data', data);

    return option;
  };

  Results.prototype.bind = function (container, $container) {
    var self = this;

    var id = container.id + '-results';

    this.$results.attr('id', id);

    container.on('results:all', function (params) {
      self.clear();
      self.append(params.data);

      if (container.isOpen()) {
        self.setClasses();
        self.highlightFirstItem();
      }
    });

    container.on('results:append', function (params) {
      self.append(params.data);

      if (container.isOpen()) {
        self.setClasses();
      }
    });

    container.on('query', function (params) {
      self.hideMessages();
      self.showLoading(params);
    });

    container.on('select', function () {
      if (!container.isOpen()) {
        return;
      }

      self.setClasses();
      self.highlightFirstItem();
    });

    container.on('unselect', function () {
      if (!container.isOpen()) {
        return;
      }

      self.setClasses();
      self.highlightFirstItem();
    });

    container.on('open', function () {
      // When the dropdown is open, aria-expended="true"
      self.$results.attr('aria-expanded', 'true');
      self.$results.attr('aria-hidden', 'false');

      self.setClasses();
      self.ensureHighlightVisible();
    });

    container.on('close', function () {
      // When the dropdown is closed, aria-expended="false"
      self.$results.attr('aria-expanded', 'false');
      self.$results.attr('aria-hidden', 'true');
      self.$results.removeAttr('aria-activedescendant');
    });

    container.on('results:toggle', function () {
      var $highlighted = self.getHighlightedResults();

      if ($highlighted.length === 0) {
        return;
      }

      $highlighted.trigger('mouseup');
    });

    container.on('results:select', function () {
      var $highlighted = self.getHighlightedResults();

      if ($highlighted.length === 0) {
        return;
      }

      var data = $highlighted.data('data');

      if ($highlighted.attr('aria-selected') == 'true') {
        self.trigger('close', {});
      } else {
        self.trigger('select', {
          data: data
        });
      }
    });

    container.on('results:previous', function () {
      var $highlighted = self.getHighlightedResults();

      var $options = self.$results.find('[aria-selected]');

      var currentIndex = $options.index($highlighted);

      // If we are already at te top, don't move further
      if (currentIndex === 0) {
        return;
      }

      var nextIndex = currentIndex - 1;

      // If none are highlighted, highlight the first
      if ($highlighted.length === 0) {
        nextIndex = 0;
      }

      var $next = $options.eq(nextIndex);

      $next.trigger('mouseenter');

      var currentOffset = self.$results.offset().top;
      var nextTop = $next.offset().top;
      var nextOffset = self.$results.scrollTop() + (nextTop - currentOffset);

      if (nextIndex === 0) {
        self.$results.scrollTop(0);
      } else if (nextTop - currentOffset < 0) {
        self.$results.scrollTop(nextOffset);
      }
    });

    container.on('results:next', function () {
      var $highlighted = self.getHighlightedResults();

      var $options = self.$results.find('[aria-selected]');

      var currentIndex = $options.index($highlighted);

      var nextIndex = currentIndex + 1;

      // If we are at the last option, stay there
      if (nextIndex >= $options.length) {
        return;
      }

      var $next = $options.eq(nextIndex);

      $next.trigger('mouseenter');

      var currentOffset = self.$results.offset().top +
        self.$results.outerHeight(false);
      var nextBottom = $next.offset().top + $next.outerHeight(false);
      var nextOffset = self.$results.scrollTop() + nextBottom - currentOffset;

      if (nextIndex === 0) {
        self.$results.scrollTop(0);
      } else if (nextBottom > currentOffset) {
        self.$results.scrollTop(nextOffset);
      }
    });

    container.on('results:focus', function (params) {
      params.element.addClass('select2-results__option--highlighted');
    });

    container.on('results:message', function (params) {
      self.displayMessage(params);
    });

    if ($.fn.mousewheel) {
      this.$results.on('mousewheel', function (e) {
        var top = self.$results.scrollTop();

        var bottom = self.$results.get(0).scrollHeight - top + e.deltaY;

        var isAtTop = e.deltaY > 0 && top - e.deltaY <= 0;
        var isAtBottom = e.deltaY < 0 && bottom <= self.$results.height();

        if (isAtTop) {
          self.$results.scrollTop(0);

          e.preventDefault();
          e.stopPropagation();
        } else if (isAtBottom) {
          self.$results.scrollTop(
            self.$results.get(0).scrollHeight - self.$results.height()
          );

          e.preventDefault();
          e.stopPropagation();
        }
      });
    }

    this.$results.on('mouseup', '.select2-results__option[aria-selected]',
      function (evt) {
      var $this = $(this);

      var data = $this.data('data');

      if ($this.attr('aria-selected') === 'true') {
        if (self.options.get('multiple')) {
          self.trigger('unselect', {
            originalEvent: evt,
            data: data
          });
        } else {
          self.trigger('close', {});
        }

        return;
      }

      self.trigger('select', {
        originalEvent: evt,
        data: data
      });
    });

    this.$results.on('mouseenter', '.select2-results__option[aria-selected]',
      function (evt) {
      var data = $(this).data('data');

      self.getHighlightedResults()
          .removeClass('select2-results__option--highlighted');

      self.trigger('results:focus', {
        data: data,
        element: $(this)
      });
    });
  };

  Results.prototype.getHighlightedResults = function () {
    var $highlighted = this.$results
    .find('.select2-results__option--highlighted');

    return $highlighted;
  };

  Results.prototype.destroy = function () {
    this.$results.remove();
  };

  Results.prototype.ensureHighlightVisible = function () {
    var $highlighted = this.getHighlightedResults();

    if ($highlighted.length === 0) {
      return;
    }

    var $options = this.$results.find('[aria-selected]');

    var currentIndex = $options.index($highlighted);

    var currentOffset = this.$results.offset().top;
    var nextTop = $highlighted.offset().top;
    var nextOffset = this.$results.scrollTop() + (nextTop - currentOffset);

    var offsetDelta = nextTop - currentOffset;
    nextOffset -= $highlighted.outerHeight(false) * 2;

    if (currentIndex <= 2) {
      this.$results.scrollTop(0);
    } else if (offsetDelta > this.$results.outerHeight() || offsetDelta < 0) {
      this.$results.scrollTop(nextOffset);
    }
  };

  Results.prototype.template = function (result, container) {
    var template = this.options.get('templateResult');
    var escapeMarkup = this.options.get('escapeMarkup');

    var content = template(result, container);

    if (content == null) {
      container.style.display = 'none';
    } else if (typeof content === 'string') {
      container.innerHTML = escapeMarkup(content);
    } else {
      $(container).append(content);
    }
  };

  return Results;
});

S2.define('select2/keys',[

], function () {
  var KEYS = {
    BACKSPACE: 8,
    TAB: 9,
    ENTER: 13,
    SHIFT: 16,
    CTRL: 17,
    ALT: 18,
    ESC: 27,
    SPACE: 32,
    PAGE_UP: 33,
    PAGE_DOWN: 34,
    END: 35,
    HOME: 36,
    LEFT: 37,
    UP: 38,
    RIGHT: 39,
    DOWN: 40,
    DELETE: 46
  };

  return KEYS;
});

S2.define('select2/selection/base',[
  'jquery',
  '../utils',
  '../keys'
], function ($, Utils, KEYS) {
  function BaseSelection ($element, options) {
    this.$element = $element;
    this.options = options;

    BaseSelection.__super__.constructor.call(this);
  }

  Utils.Extend(BaseSelection, Utils.Observable);

  BaseSelection.prototype.render = function () {
    var $selection = $(
      '<span class="select2-selection" role="combobox" ' +
      ' aria-haspopup="true" aria-expanded="false">' +
      '</span>'
    );

    this._tabindex = 0;

    if (this.$element.data('old-tabindex') != null) {
      this._tabindex = this.$element.data('old-tabindex');
    } else if (this.$element.attr('tabindex') != null) {
      this._tabindex = this.$element.attr('tabindex');
    }

    $selection.attr('title', this.$element.attr('title'));
    $selection.attr('tabindex', this._tabindex);

    this.$selection = $selection;

    return $selection;
  };

  BaseSelection.prototype.bind = function (container, $container) {
    var self = this;

    var id = container.id + '-container';
    var resultsId = container.id + '-results';

    this.container = container;

    this.$selection.on('focus', function (evt) {
      self.trigger('focus', evt);
    });

    this.$selection.on('blur', function (evt) {
      self._handleBlur(evt);
    });

    this.$selection.on('keydown', function (evt) {
      self.trigger('keypress', evt);

      if (evt.which === KEYS.SPACE) {
        evt.preventDefault();
      }
    });

    container.on('results:focus', function (params) {
      self.$selection.attr('aria-activedescendant', params.data._resultId);
    });

    container.on('selection:update', function (params) {
      self.update(params.data);
    });

    container.on('open', function () {
      // When the dropdown is open, aria-expanded="true"
      self.$selection.attr('aria-expanded', 'true');
      self.$selection.attr('aria-owns', resultsId);

      self._attachCloseHandler(container);
    });

    container.on('close', function () {
      // When the dropdown is closed, aria-expanded="false"
      self.$selection.attr('aria-expanded', 'false');
      self.$selection.removeAttr('aria-activedescendant');
      self.$selection.removeAttr('aria-owns');

      self.$selection.focus();

      self._detachCloseHandler(container);
    });

    container.on('enable', function () {
      self.$selection.attr('tabindex', self._tabindex);
    });

    container.on('disable', function () {
      self.$selection.attr('tabindex', '-1');
    });
  };

  BaseSelection.prototype._handleBlur = function (evt) {
    var self = this;

    // This needs to be delayed as the active element is the body when the tab
    // key is pressed, possibly along with others.
    window.setTimeout(function () {
      // Don't trigger `blur` if the focus is still in the selection
      if (
        (document.activeElement == self.$selection[0]) ||
        ($.contains(self.$selection[0], document.activeElement))
      ) {
        return;
      }

      self.trigger('blur', evt);
    }, 1);
  };

  BaseSelection.prototype._attachCloseHandler = function (container) {
    var self = this;

    $(document.body).on('mousedown.select2.' + container.id, function (e) {
      var $target = $(e.target);

      var $select = $target.closest('.select2');

      var $all = $('.select2.select2-container--open');

      $all.each(function () {
        var $this = $(this);

        if (this == $select[0]) {
          return;
        }

        var $element = $this.data('element');

        $element.select2('close');
      });
    });
  };

  BaseSelection.prototype._detachCloseHandler = function (container) {
    $(document.body).off('mousedown.select2.' + container.id);
  };

  BaseSelection.prototype.position = function ($selection, $container) {
    var $selectionContainer = $container.find('.selection');
    $selectionContainer.append($selection);
  };

  BaseSelection.prototype.destroy = function () {
    this._detachCloseHandler(this.container);
  };

  BaseSelection.prototype.update = function (data) {
    throw new Error('The `update` method must be defined in child classes.');
  };

  return BaseSelection;
});

S2.define('select2/selection/single',[
  'jquery',
  './base',
  '../utils',
  '../keys'
], function ($, BaseSelection, Utils, KEYS) {
  function SingleSelection () {
    SingleSelection.__super__.constructor.apply(this, arguments);
  }

  Utils.Extend(SingleSelection, BaseSelection);

  SingleSelection.prototype.render = function () {
    var $selection = SingleSelection.__super__.render.call(this);

    $selection.addClass('select2-selection--single');

    $selection.html(
      '<span class="select2-selection__rendered"></span>' +
      '<span class="select2-selection__arrow" role="presentation">' +
        '<b role="presentation"></b>' +
      '</span>'
    );

    return $selection;
  };

  SingleSelection.prototype.bind = function (container, $container) {
    var self = this;

    SingleSelection.__super__.bind.apply(this, arguments);

    var id = container.id + '-container';

    this.$selection.find('.select2-selection__rendered').attr('id', id);
    this.$selection.attr('aria-labelledby', id);

    this.$selection.on('mousedown', function (evt) {
      // Only respond to left clicks
      if (evt.which !== 1) {
        return;
      }

      self.trigger('toggle', {
        originalEvent: evt
      });
    });

    this.$selection.on('focus', function (evt) {
      // User focuses on the container
    });

    this.$selection.on('blur', function (evt) {
      // User exits the container
    });

    container.on('focus', function (evt) {
      if (!container.isOpen()) {
        self.$selection.focus();
      }
    });

    container.on('selection:update', function (params) {
      self.update(params.data);
    });
  };

  SingleSelection.prototype.clear = function () {
    this.$selection.find('.select2-selection__rendered').empty();
  };

  SingleSelection.prototype.display = function (data, container) {
    var template = this.options.get('templateSelection');
    var escapeMarkup = this.options.get('escapeMarkup');

    return escapeMarkup(template(data, container));
  };

  SingleSelection.prototype.selectionContainer = function () {
    return $('<span></span>');
  };

  SingleSelection.prototype.update = function (data) {
    if (data.length === 0) {
      this.clear();
      return;
    }

    var selection = data[0];

    var $rendered = this.$selection.find('.select2-selection__rendered');
    var formatted = this.display(selection, $rendered);

    $rendered.empty().append(formatted);
    $rendered.prop('title', selection.title || selection.text);
  };

  return SingleSelection;
});

S2.define('select2/selection/multiple',[
  'jquery',
  './base',
  '../utils'
], function ($, BaseSelection, Utils) {
  function MultipleSelection ($element, options) {
    MultipleSelection.__super__.constructor.apply(this, arguments);
  }

  Utils.Extend(MultipleSelection, BaseSelection);

  MultipleSelection.prototype.render = function () {
    var $selection = MultipleSelection.__super__.render.call(this);

    $selection.addClass('select2-selection--multiple');

    $selection.html(
      '<ul class="select2-selection__rendered"></ul>'
    );

    return $selection;
  };

  MultipleSelection.prototype.bind = function (container, $container) {
    var self = this;

    MultipleSelection.__super__.bind.apply(this, arguments);

    this.$selection.on('click', function (evt) {
      self.trigger('toggle', {
        originalEvent: evt
      });
    });

    this.$selection.on(
      'click',
      '.select2-selection__choice__remove',
      function (evt) {
        // Ignore the event if it is disabled
        if (self.options.get('disabled')) {
          return;
        }

        var $remove = $(this);
        var $selection = $remove.parent();

        var data = $selection.data('data');

        self.trigger('unselect', {
          originalEvent: evt,
          data: data
        });
      }
    );
  };

  MultipleSelection.prototype.clear = function () {
    this.$selection.find('.select2-selection__rendered').empty();
  };

  MultipleSelection.prototype.display = function (data, container) {
    var template = this.options.get('templateSelection');
    var escapeMarkup = this.options.get('escapeMarkup');

    return escapeMarkup(template(data, container));
  };

  MultipleSelection.prototype.selectionContainer = function () {
    var $container = $(
      '<li class="select2-selection__choice">' +
        '<span class="select2-selection__choice__remove" role="presentation">' +
          '&times;' +
        '</span>' +
      '</li>'
    );

    return $container;
  };

  MultipleSelection.prototype.update = function (data) {
    this.clear();

    if (data.length === 0) {
      return;
    }

    var $selections = [];

    for (var d = 0; d < data.length; d++) {
      var selection = data[d];

      var $selection = this.selectionContainer();
      var formatted = this.display(selection, $selection);

      $selection.append(formatted);
      $selection.prop('title', selection.title || selection.text);

      $selection.data('data', selection);

      $selections.push($selection);
    }

    var $rendered = this.$selection.find('.select2-selection__rendered');

    Utils.appendMany($rendered, $selections);
  };

  return MultipleSelection;
});

S2.define('select2/selection/placeholder',[
  '../utils'
], function (Utils) {
  function Placeholder (decorated, $element, options) {
    this.placeholder = this.normalizePlaceholder(options.get('placeholder'));

    decorated.call(this, $element, options);
  }

  Placeholder.prototype.normalizePlaceholder = function (_, placeholder) {
    if (typeof placeholder === 'string') {
      placeholder = {
        id: '',
        text: placeholder
      };
    }

    return placeholder;
  };

  Placeholder.prototype.createPlaceholder = function (decorated, placeholder) {
    var $placeholder = this.selectionContainer();

    $placeholder.html(this.display(placeholder));
    $placeholder.addClass('select2-selection__placeholder')
                .removeClass('select2-selection__choice');

    return $placeholder;
  };

  Placeholder.prototype.update = function (decorated, data) {
    var singlePlaceholder = (
      data.length == 1 && data[0].id != this.placeholder.id
    );
    var multipleSelections = data.length > 1;

    if (multipleSelections || singlePlaceholder) {
      return decorated.call(this, data);
    }

    this.clear();

    var $placeholder = this.createPlaceholder(this.placeholder);

    this.$selection.find('.select2-selection__rendered').append($placeholder);
  };

  return Placeholder;
});

S2.define('select2/selection/allowClear',[
  'jquery',
  '../keys'
], function ($, KEYS) {
  function AllowClear () { }

  AllowClear.prototype.bind = function (decorated, container, $container) {
    var self = this;

    decorated.call(this, container, $container);

    if (this.placeholder == null) {
      if (this.options.get('debug') && window.console && console.error) {
        console.error(
          'Select2: The `allowClear` option should be used in combination ' +
          'with the `placeholder` option.'
        );
      }
    }

    this.$selection.on('mousedown', '.select2-selection__clear',
      function (evt) {
        self._handleClear(evt);
    });

    container.on('keypress', function (evt) {
      self._handleKeyboardClear(evt, container);
    });
  };

  AllowClear.prototype._handleClear = function (_, evt) {
    // Ignore the event if it is disabled
    if (this.options.get('disabled')) {
      return;
    }

    var $clear = this.$selection.find('.select2-selection__clear');

    // Ignore the event if nothing has been selected
    if ($clear.length === 0) {
      return;
    }

    evt.stopPropagation();

    var data = $clear.data('data');

    for (var d = 0; d < data.length; d++) {
      var unselectData = {
        data: data[d]
      };

      // Trigger the `unselect` event, so people can prevent it from being
      // cleared.
      this.trigger('unselect', unselectData);

      // If the event was prevented, don't clear it out.
      if (unselectData.prevented) {
        return;
      }
    }

    this.$element.val(this.placeholder.id).trigger('change');

    this.trigger('toggle', {});
  };

  AllowClear.prototype._handleKeyboardClear = function (_, evt, container) {
    if (container.isOpen()) {
      return;
    }

    if (evt.which == KEYS.DELETE || evt.which == KEYS.BACKSPACE) {
      this._handleClear(evt);
    }
  };

  AllowClear.prototype.update = function (decorated, data) {
    decorated.call(this, data);

    if (this.$selection.find('.select2-selection__placeholder').length > 0 ||
        data.length === 0) {
      return;
    }

    var $remove = $(
      '<span class="select2-selection__clear">' +
        '&times;' +
      '</span>'
    );
    $remove.data('data', data);

    this.$selection.find('.select2-selection__rendered').prepend($remove);
  };

  return AllowClear;
});

S2.define('select2/selection/search',[
  'jquery',
  '../utils',
  '../keys'
], function ($, Utils, KEYS) {
  function Search (decorated, $element, options) {
    decorated.call(this, $element, options);
  }

  Search.prototype.render = function (decorated) {
    var $search = $(
      '<li class="select2-search select2-search--inline">' +
        '<input class="select2-search__field" type="search" tabindex="-1"' +
        ' autocomplete="off" autocorrect="off" autocapitalize="off"' +
        ' spellcheck="false" role="textbox" aria-autocomplete="list" />' +
      '</li>'
    );

    this.$searchContainer = $search;
    this.$search = $search.find('input');

    var $rendered = decorated.call(this);

    this._transferTabIndex();

    return $rendered;
  };

  Search.prototype.bind = function (decorated, container, $container) {
    var self = this;

    decorated.call(this, container, $container);

    container.on('open', function () {
      self.$search.trigger('focus');
    });

    container.on('close', function () {
      self.$search.val('');
      self.$search.removeAttr('aria-activedescendant');
      self.$search.trigger('focus');
    });

    container.on('enable', function () {
      self.$search.prop('disabled', false);

      self._transferTabIndex();
    });

    container.on('disable', function () {
      self.$search.prop('disabled', true);
    });

    container.on('focus', function (evt) {
      self.$search.trigger('focus');
    });

    container.on('results:focus', function (params) {
      self.$search.attr('aria-activedescendant', params.id);
    });

    this.$selection.on('focusin', '.select2-search--inline', function (evt) {
      self.trigger('focus', evt);
    });

    this.$selection.on('focusout', '.select2-search--inline', function (evt) {
      self._handleBlur(evt);
    });

    this.$selection.on('keydown', '.select2-search--inline', function (evt) {
      evt.stopPropagation();

      self.trigger('keypress', evt);

      self._keyUpPrevented = evt.isDefaultPrevented();

      var key = evt.which;

      if (key === KEYS.BACKSPACE && self.$search.val() === '') {
        var $previousChoice = self.$searchContainer
          .prev('.select2-selection__choice');

        if ($previousChoice.length > 0) {
          var item = $previousChoice.data('data');

          self.searchRemoveChoice(item);

          evt.preventDefault();
        }
      }
    });

    // Try to detect the IE version should the `documentMode` property that
    // is stored on the document. This is only implemented in IE and is
    // slightly cleaner than doing a user agent check.
    // This property is not available in Edge, but Edge also doesn't have
    // this bug.
    var msie = document.documentMode;
    var disableInputEvents = msie && msie <= 11;

    // Workaround for browsers which do not support the `input` event
    // This will prevent double-triggering of events for browsers which support
    // both the `keyup` and `input` events.
    this.$selection.on(
      'input.searchcheck',
      '.select2-search--inline',
      function (evt) {
        // IE will trigger the `input` event when a placeholder is used on a
        // search box. To get around this issue, we are forced to ignore all
        // `input` events in IE and keep using `keyup`.
        if (disableInputEvents) {
          self.$selection.off('input.search input.searchcheck');
          return;
        }

        // Unbind the duplicated `keyup` event
        self.$selection.off('keyup.search');
      }
    );

    this.$selection.on(
      'keyup.search input.search',
      '.select2-search--inline',
      function (evt) {
        // IE will trigger the `input` event when a placeholder is used on a
        // search box. To get around this issue, we are forced to ignore all
        // `input` events in IE and keep using `keyup`.
        if (disableInputEvents && evt.type === 'input') {
          self.$selection.off('input.search input.searchcheck');
          return;
        }

        var key = evt.which;

        // We can freely ignore events from modifier keys
        if (key == KEYS.SHIFT || key == KEYS.CTRL || key == KEYS.ALT) {
          return;
        }

        // Tabbing will be handled during the `keydown` phase
        if (key == KEYS.TAB) {
          return;
        }

        self.handleSearch(evt);
      }
    );
  };

  /**
   * This method will transfer the tabindex attribute from the rendered
   * selection to the search box. This allows for the search box to be used as
   * the primary focus instead of the selection container.
   *
   * @private
   */
  Search.prototype._transferTabIndex = function (decorated) {
    this.$search.attr('tabindex', this.$selection.attr('tabindex'));
    this.$selection.attr('tabindex', '-1');
  };

  Search.prototype.createPlaceholder = function (decorated, placeholder) {
    this.$search.attr('placeholder', placeholder.text);
  };

  Search.prototype.update = function (decorated, data) {
    var searchHadFocus = this.$search[0] == document.activeElement;

    this.$search.attr('placeholder', '');

    decorated.call(this, data);

    this.$selection.find('.select2-selection__rendered')
                   .append(this.$searchContainer);

    this.resizeSearch();
    if (searchHadFocus) {
      this.$search.focus();
    }
  };

  Search.prototype.handleSearch = function () {
    this.resizeSearch();

    if (!this._keyUpPrevented) {
      var input = this.$search.val();

      this.trigger('query', {
        term: input
      });
    }

    this._keyUpPrevented = false;
  };

  Search.prototype.searchRemoveChoice = function (decorated, item) {
    this.trigger('unselect', {
      data: item
    });

    this.$search.val(item.text);
    this.handleSearch();
  };

  Search.prototype.resizeSearch = function () {
    this.$search.css('width', '25px');

    var width = '';

    if (this.$search.attr('placeholder') !== '') {
      width = this.$selection.find('.select2-selection__rendered').innerWidth();
    } else {
      var minimumWidth = this.$search.val().length + 1;

      width = (minimumWidth * 0.75) + 'em';
    }

    this.$search.css('width', width);
  };

  return Search;
});

S2.define('select2/selection/eventRelay',[
  'jquery'
], function ($) {
  function EventRelay () { }

  EventRelay.prototype.bind = function (decorated, container, $container) {
    var self = this;
    var relayEvents = [
      'open', 'opening',
      'close', 'closing',
      'select', 'selecting',
      'unselect', 'unselecting'
    ];

    var preventableEvents = ['opening', 'closing', 'selecting', 'unselecting'];

    decorated.call(this, container, $container);

    container.on('*', function (name, params) {
      // Ignore events that should not be relayed
      if ($.inArray(name, relayEvents) === -1) {
        return;
      }

      // The parameters should always be an object
      params = params || {};

      // Generate the jQuery event for the Select2 event
      var evt = $.Event('select2:' + name, {
        params: params
      });

      self.$element.trigger(evt);

      // Only handle preventable events if it was one
      if ($.inArray(name, preventableEvents) === -1) {
        return;
      }

      params.prevented = evt.isDefaultPrevented();
    });
  };

  return EventRelay;
});

S2.define('select2/translation',[
  'jquery',
  'require'
], function ($, require) {
  function Translation (dict) {
    this.dict = dict || {};
  }

  Translation.prototype.all = function () {
    return this.dict;
  };

  Translation.prototype.get = function (key) {
    return this.dict[key];
  };

  Translation.prototype.extend = function (translation) {
    this.dict = $.extend({}, translation.all(), this.dict);
  };

  // Static functions

  Translation._cache = {};

  Translation.loadPath = function (path) {
    if (!(path in Translation._cache)) {
      var translations = require(path);

      Translation._cache[path] = translations;
    }

    return new Translation(Translation._cache[path]);
  };

  return Translation;
});

S2.define('select2/diacritics',[

], function () {
  var diacritics = {
    '\u24B6': 'A',
    '\uFF21': 'A',
    '\u00C0': 'A',
    '\u00C1': 'A',
    '\u00C2': 'A',
    '\u1EA6': 'A',
    '\u1EA4': 'A',
    '\u1EAA': 'A',
    '\u1EA8': 'A',
    '\u00C3': 'A',
    '\u0100': 'A',
    '\u0102': 'A',
    '\u1EB0': 'A',
    '\u1EAE': 'A',
    '\u1EB4': 'A',
    '\u1EB2': 'A',
    '\u0226': 'A',
    '\u01E0': 'A',
    '\u00C4': 'A',
    '\u01DE': 'A',
    '\u1EA2': 'A',
    '\u00C5': 'A',
    '\u01FA': 'A',
    '\u01CD': 'A',
    '\u0200': 'A',
    '\u0202': 'A',
    '\u1EA0': 'A',
    '\u1EAC': 'A',
    '\u1EB6': 'A',
    '\u1E00': 'A',
    '\u0104': 'A',
    '\u023A': 'A',
    '\u2C6F': 'A',
    '\uA732': 'AA',
    '\u00C6': 'AE',
    '\u01FC': 'AE',
    '\u01E2': 'AE',
    '\uA734': 'AO',
    '\uA736': 'AU',
    '\uA738': 'AV',
    '\uA73A': 'AV',
    '\uA73C': 'AY',
    '\u24B7': 'B',
    '\uFF22': 'B',
    '\u1E02': 'B',
    '\u1E04': 'B',
    '\u1E06': 'B',
    '\u0243': 'B',
    '\u0182': 'B',
    '\u0181': 'B',
    '\u24B8': 'C',
    '\uFF23': 'C',
    '\u0106': 'C',
    '\u0108': 'C',
    '\u010A': 'C',
    '\u010C': 'C',
    '\u00C7': 'C',
    '\u1E08': 'C',
    '\u0187': 'C',
    '\u023B': 'C',
    '\uA73E': 'C',
    '\u24B9': 'D',
    '\uFF24': 'D',
    '\u1E0A': 'D',
    '\u010E': 'D',
    '\u1E0C': 'D',
    '\u1E10': 'D',
    '\u1E12': 'D',
    '\u1E0E': 'D',
    '\u0110': 'D',
    '\u018B': 'D',
    '\u018A': 'D',
    '\u0189': 'D',
    '\uA779': 'D',
    '\u01F1': 'DZ',
    '\u01C4': 'DZ',
    '\u01F2': 'Dz',
    '\u01C5': 'Dz',
    '\u24BA': 'E',
    '\uFF25': 'E',
    '\u00C8': 'E',
    '\u00C9': 'E',
    '\u00CA': 'E',
    '\u1EC0': 'E',
    '\u1EBE': 'E',
    '\u1EC4': 'E',
    '\u1EC2': 'E',
    '\u1EBC': 'E',
    '\u0112': 'E',
    '\u1E14': 'E',
    '\u1E16': 'E',
    '\u0114': 'E',
    '\u0116': 'E',
    '\u00CB': 'E',
    '\u1EBA': 'E',
    '\u011A': 'E',
    '\u0204': 'E',
    '\u0206': 'E',
    '\u1EB8': 'E',
    '\u1EC6': 'E',
    '\u0228': 'E',
    '\u1E1C': 'E',
    '\u0118': 'E',
    '\u1E18': 'E',
    '\u1E1A': 'E',
    '\u0190': 'E',
    '\u018E': 'E',
    '\u24BB': 'F',
    '\uFF26': 'F',
    '\u1E1E': 'F',
    '\u0191': 'F',
    '\uA77B': 'F',
    '\u24BC': 'G',
    '\uFF27': 'G',
    '\u01F4': 'G',
    '\u011C': 'G',
    '\u1E20': 'G',
    '\u011E': 'G',
    '\u0120': 'G',
    '\u01E6': 'G',
    '\u0122': 'G',
    '\u01E4': 'G',
    '\u0193': 'G',
    '\uA7A0': 'G',
    '\uA77D': 'G',
    '\uA77E': 'G',
    '\u24BD': 'H',
    '\uFF28': 'H',
    '\u0124': 'H',
    '\u1E22': 'H',
    '\u1E26': 'H',
    '\u021E': 'H',
    '\u1E24': 'H',
    '\u1E28': 'H',
    '\u1E2A': 'H',
    '\u0126': 'H',
    '\u2C67': 'H',
    '\u2C75': 'H',
    '\uA78D': 'H',
    '\u24BE': 'I',
    '\uFF29': 'I',
    '\u00CC': 'I',
    '\u00CD': 'I',
    '\u00CE': 'I',
    '\u0128': 'I',
    '\u012A': 'I',
    '\u012C': 'I',
    '\u0130': 'I',
    '\u00CF': 'I',
    '\u1E2E': 'I',
    '\u1EC8': 'I',
    '\u01CF': 'I',
    '\u0208': 'I',
    '\u020A': 'I',
    '\u1ECA': 'I',
    '\u012E': 'I',
    '\u1E2C': 'I',
    '\u0197': 'I',
    '\u24BF': 'J',
    '\uFF2A': 'J',
    '\u0134': 'J',
    '\u0248': 'J',
    '\u24C0': 'K',
    '\uFF2B': 'K',
    '\u1E30': 'K',
    '\u01E8': 'K',
    '\u1E32': 'K',
    '\u0136': 'K',
    '\u1E34': 'K',
    '\u0198': 'K',
    '\u2C69': 'K',
    '\uA740': 'K',
    '\uA742': 'K',
    '\uA744': 'K',
    '\uA7A2': 'K',
    '\u24C1': 'L',
    '\uFF2C': 'L',
    '\u013F': 'L',
    '\u0139': 'L',
    '\u013D': 'L',
    '\u1E36': 'L',
    '\u1E38': 'L',
    '\u013B': 'L',
    '\u1E3C': 'L',
    '\u1E3A': 'L',
    '\u0141': 'L',
    '\u023D': 'L',
    '\u2C62': 'L',
    '\u2C60': 'L',
    '\uA748': 'L',
    '\uA746': 'L',
    '\uA780': 'L',
    '\u01C7': 'LJ',
    '\u01C8': 'Lj',
    '\u24C2': 'M',
    '\uFF2D': 'M',
    '\u1E3E': 'M',
    '\u1E40': 'M',
    '\u1E42': 'M',
    '\u2C6E': 'M',
    '\u019C': 'M',
    '\u24C3': 'N',
    '\uFF2E': 'N',
    '\u01F8': 'N',
    '\u0143': 'N',
    '\u00D1': 'N',
    '\u1E44': 'N',
    '\u0147': 'N',
    '\u1E46': 'N',
    '\u0145': 'N',
    '\u1E4A': 'N',
    '\u1E48': 'N',
    '\u0220': 'N',
    '\u019D': 'N',
    '\uA790': 'N',
    '\uA7A4': 'N',
    '\u01CA': 'NJ',
    '\u01CB': 'Nj',
    '\u24C4': 'O',
    '\uFF2F': 'O',
    '\u00D2': 'O',
    '\u00D3': 'O',
    '\u00D4': 'O',
    '\u1ED2': 'O',
    '\u1ED0': 'O',
    '\u1ED6': 'O',
    '\u1ED4': 'O',
    '\u00D5': 'O',
    '\u1E4C': 'O',
    '\u022C': 'O',
    '\u1E4E': 'O',
    '\u014C': 'O',
    '\u1E50': 'O',
    '\u1E52': 'O',
    '\u014E': 'O',
    '\u022E': 'O',
    '\u0230': 'O',
    '\u00D6': 'O',
    '\u022A': 'O',
    '\u1ECE': 'O',
    '\u0150': 'O',
    '\u01D1': 'O',
    '\u020C': 'O',
    '\u020E': 'O',
    '\u01A0': 'O',
    '\u1EDC': 'O',
    '\u1EDA': 'O',
    '\u1EE0': 'O',
    '\u1EDE': 'O',
    '\u1EE2': 'O',
    '\u1ECC': 'O',
    '\u1ED8': 'O',
    '\u01EA': 'O',
    '\u01EC': 'O',
    '\u00D8': 'O',
    '\u01FE': 'O',
    '\u0186': 'O',
    '\u019F': 'O',
    '\uA74A': 'O',
    '\uA74C': 'O',
    '\u01A2': 'OI',
    '\uA74E': 'OO',
    '\u0222': 'OU',
    '\u24C5': 'P',
    '\uFF30': 'P',
    '\u1E54': 'P',
    '\u1E56': 'P',
    '\u01A4': 'P',
    '\u2C63': 'P',
    '\uA750': 'P',
    '\uA752': 'P',
    '\uA754': 'P',
    '\u24C6': 'Q',
    '\uFF31': 'Q',
    '\uA756': 'Q',
    '\uA758': 'Q',
    '\u024A': 'Q',
    '\u24C7': 'R',
    '\uFF32': 'R',
    '\u0154': 'R',
    '\u1E58': 'R',
    '\u0158': 'R',
    '\u0210': 'R',
    '\u0212': 'R',
    '\u1E5A': 'R',
    '\u1E5C': 'R',
    '\u0156': 'R',
    '\u1E5E': 'R',
    '\u024C': 'R',
    '\u2C64': 'R',
    '\uA75A': 'R',
    '\uA7A6': 'R',
    '\uA782': 'R',
    '\u24C8': 'S',
    '\uFF33': 'S',
    '\u1E9E': 'S',
    '\u015A': 'S',
    '\u1E64': 'S',
    '\u015C': 'S',
    '\u1E60': 'S',
    '\u0160': 'S',
    '\u1E66': 'S',
    '\u1E62': 'S',
    '\u1E68': 'S',
    '\u0218': 'S',
    '\u015E': 'S',
    '\u2C7E': 'S',
    '\uA7A8': 'S',
    '\uA784': 'S',
    '\u24C9': 'T',
    '\uFF34': 'T',
    '\u1E6A': 'T',
    '\u0164': 'T',
    '\u1E6C': 'T',
    '\u021A': 'T',
    '\u0162': 'T',
    '\u1E70': 'T',
    '\u1E6E': 'T',
    '\u0166': 'T',
    '\u01AC': 'T',
    '\u01AE': 'T',
    '\u023E': 'T',
    '\uA786': 'T',
    '\uA728': 'TZ',
    '\u24CA': 'U',
    '\uFF35': 'U',
    '\u00D9': 'U',
    '\u00DA': 'U',
    '\u00DB': 'U',
    '\u0168': 'U',
    '\u1E78': 'U',
    '\u016A': 'U',
    '\u1E7A': 'U',
    '\u016C': 'U',
    '\u00DC': 'U',
    '\u01DB': 'U',
    '\u01D7': 'U',
    '\u01D5': 'U',
    '\u01D9': 'U',
    '\u1EE6': 'U',
    '\u016E': 'U',
    '\u0170': 'U',
    '\u01D3': 'U',
    '\u0214': 'U',
    '\u0216': 'U',
    '\u01AF': 'U',
    '\u1EEA': 'U',
    '\u1EE8': 'U',
    '\u1EEE': 'U',
    '\u1EEC': 'U',
    '\u1EF0': 'U',
    '\u1EE4': 'U',
    '\u1E72': 'U',
    '\u0172': 'U',
    '\u1E76': 'U',
    '\u1E74': 'U',
    '\u0244': 'U',
    '\u24CB': 'V',
    '\uFF36': 'V',
    '\u1E7C': 'V',
    '\u1E7E': 'V',
    '\u01B2': 'V',
    '\uA75E': 'V',
    '\u0245': 'V',
    '\uA760': 'VY',
    '\u24CC': 'W',
    '\uFF37': 'W',
    '\u1E80': 'W',
    '\u1E82': 'W',
    '\u0174': 'W',
    '\u1E86': 'W',
    '\u1E84': 'W',
    '\u1E88': 'W',
    '\u2C72': 'W',
    '\u24CD': 'X',
    '\uFF38': 'X',
    '\u1E8A': 'X',
    '\u1E8C': 'X',
    '\u24CE': 'Y',
    '\uFF39': 'Y',
    '\u1EF2': 'Y',
    '\u00DD': 'Y',
    '\u0176': 'Y',
    '\u1EF8': 'Y',
    '\u0232': 'Y',
    '\u1E8E': 'Y',
    '\u0178': 'Y',
    '\u1EF6': 'Y',
    '\u1EF4': 'Y',
    '\u01B3': 'Y',
    '\u024E': 'Y',
    '\u1EFE': 'Y',
    '\u24CF': 'Z',
    '\uFF3A': 'Z',
    '\u0179': 'Z',
    '\u1E90': 'Z',
    '\u017B': 'Z',
    '\u017D': 'Z',
    '\u1E92': 'Z',
    '\u1E94': 'Z',
    '\u01B5': 'Z',
    '\u0224': 'Z',
    '\u2C7F': 'Z',
    '\u2C6B': 'Z',
    '\uA762': 'Z',
    '\u24D0': 'a',
    '\uFF41': 'a',
    '\u1E9A': 'a',
    '\u00E0': 'a',
    '\u00E1': 'a',
    '\u00E2': 'a',
    '\u1EA7': 'a',
    '\u1EA5': 'a',
    '\u1EAB': 'a',
    '\u1EA9': 'a',
    '\u00E3': 'a',
    '\u0101': 'a',
    '\u0103': 'a',
    '\u1EB1': 'a',
    '\u1EAF': 'a',
    '\u1EB5': 'a',
    '\u1EB3': 'a',
    '\u0227': 'a',
    '\u01E1': 'a',
    '\u00E4': 'a',
    '\u01DF': 'a',
    '\u1EA3': 'a',
    '\u00E5': 'a',
    '\u01FB': 'a',
    '\u01CE': 'a',
    '\u0201': 'a',
    '\u0203': 'a',
    '\u1EA1': 'a',
    '\u1EAD': 'a',
    '\u1EB7': 'a',
    '\u1E01': 'a',
    '\u0105': 'a',
    '\u2C65': 'a',
    '\u0250': 'a',
    '\uA733': 'aa',
    '\u00E6': 'ae',
    '\u01FD': 'ae',
    '\u01E3': 'ae',
    '\uA735': 'ao',
    '\uA737': 'au',
    '\uA739': 'av',
    '\uA73B': 'av',
    '\uA73D': 'ay',
    '\u24D1': 'b',
    '\uFF42': 'b',
    '\u1E03': 'b',
    '\u1E05': 'b',
    '\u1E07': 'b',
    '\u0180': 'b',
    '\u0183': 'b',
    '\u0253': 'b',
    '\u24D2': 'c',
    '\uFF43': 'c',
    '\u0107': 'c',
    '\u0109': 'c',
    '\u010B': 'c',
    '\u010D': 'c',
    '\u00E7': 'c',
    '\u1E09': 'c',
    '\u0188': 'c',
    '\u023C': 'c',
    '\uA73F': 'c',
    '\u2184': 'c',
    '\u24D3': 'd',
    '\uFF44': 'd',
    '\u1E0B': 'd',
    '\u010F': 'd',
    '\u1E0D': 'd',
    '\u1E11': 'd',
    '\u1E13': 'd',
    '\u1E0F': 'd',
    '\u0111': 'd',
    '\u018C': 'd',
    '\u0256': 'd',
    '\u0257': 'd',
    '\uA77A': 'd',
    '\u01F3': 'dz',
    '\u01C6': 'dz',
    '\u24D4': 'e',
    '\uFF45': 'e',
    '\u00E8': 'e',
    '\u00E9': 'e',
    '\u00EA': 'e',
    '\u1EC1': 'e',
    '\u1EBF': 'e',
    '\u1EC5': 'e',
    '\u1EC3': 'e',
    '\u1EBD': 'e',
    '\u0113': 'e',
    '\u1E15': 'e',
    '\u1E17': 'e',
    '\u0115': 'e',
    '\u0117': 'e',
    '\u00EB': 'e',
    '\u1EBB': 'e',
    '\u011B': 'e',
    '\u0205': 'e',
    '\u0207': 'e',
    '\u1EB9': 'e',
    '\u1EC7': 'e',
    '\u0229': 'e',
    '\u1E1D': 'e',
    '\u0119': 'e',
    '\u1E19': 'e',
    '\u1E1B': 'e',
    '\u0247': 'e',
    '\u025B': 'e',
    '\u01DD': 'e',
    '\u24D5': 'f',
    '\uFF46': 'f',
    '\u1E1F': 'f',
    '\u0192': 'f',
    '\uA77C': 'f',
    '\u24D6': 'g',
    '\uFF47': 'g',
    '\u01F5': 'g',
    '\u011D': 'g',
    '\u1E21': 'g',
    '\u011F': 'g',
    '\u0121': 'g',
    '\u01E7': 'g',
    '\u0123': 'g',
    '\u01E5': 'g',
    '\u0260': 'g',
    '\uA7A1': 'g',
    '\u1D79': 'g',
    '\uA77F': 'g',
    '\u24D7': 'h',
    '\uFF48': 'h',
    '\u0125': 'h',
    '\u1E23': 'h',
    '\u1E27': 'h',
    '\u021F': 'h',
    '\u1E25': 'h',
    '\u1E29': 'h',
    '\u1E2B': 'h',
    '\u1E96': 'h',
    '\u0127': 'h',
    '\u2C68': 'h',
    '\u2C76': 'h',
    '\u0265': 'h',
    '\u0195': 'hv',
    '\u24D8': 'i',
    '\uFF49': 'i',
    '\u00EC': 'i',
    '\u00ED': 'i',
    '\u00EE': 'i',
    '\u0129': 'i',
    '\u012B': 'i',
    '\u012D': 'i',
    '\u00EF': 'i',
    '\u1E2F': 'i',
    '\u1EC9': 'i',
    '\u01D0': 'i',
    '\u0209': 'i',
    '\u020B': 'i',
    '\u1ECB': 'i',
    '\u012F': 'i',
    '\u1E2D': 'i',
    '\u0268': 'i',
    '\u0131': 'i',
    '\u24D9': 'j',
    '\uFF4A': 'j',
    '\u0135': 'j',
    '\u01F0': 'j',
    '\u0249': 'j',
    '\u24DA': 'k',
    '\uFF4B': 'k',
    '\u1E31': 'k',
    '\u01E9': 'k',
    '\u1E33': 'k',
    '\u0137': 'k',
    '\u1E35': 'k',
    '\u0199': 'k',
    '\u2C6A': 'k',
    '\uA741': 'k',
    '\uA743': 'k',
    '\uA745': 'k',
    '\uA7A3': 'k',
    '\u24DB': 'l',
    '\uFF4C': 'l',
    '\u0140': 'l',
    '\u013A': 'l',
    '\u013E': 'l',
    '\u1E37': 'l',
    '\u1E39': 'l',
    '\u013C': 'l',
    '\u1E3D': 'l',
    '\u1E3B': 'l',
    '\u017F': 'l',
    '\u0142': 'l',
    '\u019A': 'l',
    '\u026B': 'l',
    '\u2C61': 'l',
    '\uA749': 'l',
    '\uA781': 'l',
    '\uA747': 'l',
    '\u01C9': 'lj',
    '\u24DC': 'm',
    '\uFF4D': 'm',
    '\u1E3F': 'm',
    '\u1E41': 'm',
    '\u1E43': 'm',
    '\u0271': 'm',
    '\u026F': 'm',
    '\u24DD': 'n',
    '\uFF4E': 'n',
    '\u01F9': 'n',
    '\u0144': 'n',
    '\u00F1': 'n',
    '\u1E45': 'n',
    '\u0148': 'n',
    '\u1E47': 'n',
    '\u0146': 'n',
    '\u1E4B': 'n',
    '\u1E49': 'n',
    '\u019E': 'n',
    '\u0272': 'n',
    '\u0149': 'n',
    '\uA791': 'n',
    '\uA7A5': 'n',
    '\u01CC': 'nj',
    '\u24DE': 'o',
    '\uFF4F': 'o',
    '\u00F2': 'o',
    '\u00F3': 'o',
    '\u00F4': 'o',
    '\u1ED3': 'o',
    '\u1ED1': 'o',
    '\u1ED7': 'o',
    '\u1ED5': 'o',
    '\u00F5': 'o',
    '\u1E4D': 'o',
    '\u022D': 'o',
    '\u1E4F': 'o',
    '\u014D': 'o',
    '\u1E51': 'o',
    '\u1E53': 'o',
    '\u014F': 'o',
    '\u022F': 'o',
    '\u0231': 'o',
    '\u00F6': 'o',
    '\u022B': 'o',
    '\u1ECF': 'o',
    '\u0151': 'o',
    '\u01D2': 'o',
    '\u020D': 'o',
    '\u020F': 'o',
    '\u01A1': 'o',
    '\u1EDD': 'o',
    '\u1EDB': 'o',
    '\u1EE1': 'o',
    '\u1EDF': 'o',
    '\u1EE3': 'o',
    '\u1ECD': 'o',
    '\u1ED9': 'o',
    '\u01EB': 'o',
    '\u01ED': 'o',
    '\u00F8': 'o',
    '\u01FF': 'o',
    '\u0254': 'o',
    '\uA74B': 'o',
    '\uA74D': 'o',
    '\u0275': 'o',
    '\u01A3': 'oi',
    '\u0223': 'ou',
    '\uA74F': 'oo',
    '\u24DF': 'p',
    '\uFF50': 'p',
    '\u1E55': 'p',
    '\u1E57': 'p',
    '\u01A5': 'p',
    '\u1D7D': 'p',
    '\uA751': 'p',
    '\uA753': 'p',
    '\uA755': 'p',
    '\u24E0': 'q',
    '\uFF51': 'q',
    '\u024B': 'q',
    '\uA757': 'q',
    '\uA759': 'q',
    '\u24E1': 'r',
    '\uFF52': 'r',
    '\u0155': 'r',
    '\u1E59': 'r',
    '\u0159': 'r',
    '\u0211': 'r',
    '\u0213': 'r',
    '\u1E5B': 'r',
    '\u1E5D': 'r',
    '\u0157': 'r',
    '\u1E5F': 'r',
    '\u024D': 'r',
    '\u027D': 'r',
    '\uA75B': 'r',
    '\uA7A7': 'r',
    '\uA783': 'r',
    '\u24E2': 's',
    '\uFF53': 's',
    '\u00DF': 's',
    '\u015B': 's',
    '\u1E65': 's',
    '\u015D': 's',
    '\u1E61': 's',
    '\u0161': 's',
    '\u1E67': 's',
    '\u1E63': 's',
    '\u1E69': 's',
    '\u0219': 's',
    '\u015F': 's',
    '\u023F': 's',
    '\uA7A9': 's',
    '\uA785': 's',
    '\u1E9B': 's',
    '\u24E3': 't',
    '\uFF54': 't',
    '\u1E6B': 't',
    '\u1E97': 't',
    '\u0165': 't',
    '\u1E6D': 't',
    '\u021B': 't',
    '\u0163': 't',
    '\u1E71': 't',
    '\u1E6F': 't',
    '\u0167': 't',
    '\u01AD': 't',
    '\u0288': 't',
    '\u2C66': 't',
    '\uA787': 't',
    '\uA729': 'tz',
    '\u24E4': 'u',
    '\uFF55': 'u',
    '\u00F9': 'u',
    '\u00FA': 'u',
    '\u00FB': 'u',
    '\u0169': 'u',
    '\u1E79': 'u',
    '\u016B': 'u',
    '\u1E7B': 'u',
    '\u016D': 'u',
    '\u00FC': 'u',
    '\u01DC': 'u',
    '\u01D8': 'u',
    '\u01D6': 'u',
    '\u01DA': 'u',
    '\u1EE7': 'u',
    '\u016F': 'u',
    '\u0171': 'u',
    '\u01D4': 'u',
    '\u0215': 'u',
    '\u0217': 'u',
    '\u01B0': 'u',
    '\u1EEB': 'u',
    '\u1EE9': 'u',
    '\u1EEF': 'u',
    '\u1EED': 'u',
    '\u1EF1': 'u',
    '\u1EE5': 'u',
    '\u1E73': 'u',
    '\u0173': 'u',
    '\u1E77': 'u',
    '\u1E75': 'u',
    '\u0289': 'u',
    '\u24E5': 'v',
    '\uFF56': 'v',
    '\u1E7D': 'v',
    '\u1E7F': 'v',
    '\u028B': 'v',
    '\uA75F': 'v',
    '\u028C': 'v',
    '\uA761': 'vy',
    '\u24E6': 'w',
    '\uFF57': 'w',
    '\u1E81': 'w',
    '\u1E83': 'w',
    '\u0175': 'w',
    '\u1E87': 'w',
    '\u1E85': 'w',
    '\u1E98': 'w',
    '\u1E89': 'w',
    '\u2C73': 'w',
    '\u24E7': 'x',
    '\uFF58': 'x',
    '\u1E8B': 'x',
    '\u1E8D': 'x',
    '\u24E8': 'y',
    '\uFF59': 'y',
    '\u1EF3': 'y',
    '\u00FD': 'y',
    '\u0177': 'y',
    '\u1EF9': 'y',
    '\u0233': 'y',
    '\u1E8F': 'y',
    '\u00FF': 'y',
    '\u1EF7': 'y',
    '\u1E99': 'y',
    '\u1EF5': 'y',
    '\u01B4': 'y',
    '\u024F': 'y',
    '\u1EFF': 'y',
    '\u24E9': 'z',
    '\uFF5A': 'z',
    '\u017A': 'z',
    '\u1E91': 'z',
    '\u017C': 'z',
    '\u017E': 'z',
    '\u1E93': 'z',
    '\u1E95': 'z',
    '\u01B6': 'z',
    '\u0225': 'z',
    '\u0240': 'z',
    '\u2C6C': 'z',
    '\uA763': 'z',
    '\u0386': '\u0391',
    '\u0388': '\u0395',
    '\u0389': '\u0397',
    '\u038A': '\u0399',
    '\u03AA': '\u0399',
    '\u038C': '\u039F',
    '\u038E': '\u03A5',
    '\u03AB': '\u03A5',
    '\u038F': '\u03A9',
    '\u03AC': '\u03B1',
    '\u03AD': '\u03B5',
    '\u03AE': '\u03B7',
    '\u03AF': '\u03B9',
    '\u03CA': '\u03B9',
    '\u0390': '\u03B9',
    '\u03CC': '\u03BF',
    '\u03CD': '\u03C5',
    '\u03CB': '\u03C5',
    '\u03B0': '\u03C5',
    '\u03C9': '\u03C9',
    '\u03C2': '\u03C3'
  };

  return diacritics;
});

S2.define('select2/data/base',[
  '../utils'
], function (Utils) {
  function BaseAdapter ($element, options) {
    BaseAdapter.__super__.constructor.call(this);
  }

  Utils.Extend(BaseAdapter, Utils.Observable);

  BaseAdapter.prototype.current = function (callback) {
    throw new Error('The `current` method must be defined in child classes.');
  };

  BaseAdapter.prototype.query = function (params, callback) {
    throw new Error('The `query` method must be defined in child classes.');
  };

  BaseAdapter.prototype.bind = function (container, $container) {
    // Can be implemented in subclasses
  };

  BaseAdapter.prototype.destroy = function () {
    // Can be implemented in subclasses
  };

  BaseAdapter.prototype.generateResultId = function (container, data) {
    var id = container.id + '-result-';

    id += Utils.generateChars(4);

    if (data.id != null) {
      id += '-' + data.id.toString();
    } else {
      id += '-' + Utils.generateChars(4);
    }
    return id;
  };

  return BaseAdapter;
});

S2.define('select2/data/select',[
  './base',
  '../utils',
  'jquery'
], function (BaseAdapter, Utils, $) {
  function SelectAdapter ($element, options) {
    this.$element = $element;
    this.options = options;

    SelectAdapter.__super__.constructor.call(this);
  }

  Utils.Extend(SelectAdapter, BaseAdapter);

  SelectAdapter.prototype.current = function (callback) {
    var data = [];
    var self = this;

    this.$element.find(':selected').each(function () {
      var $option = $(this);

      var option = self.item($option);

      data.push(option);
    });

    callback(data);
  };

  SelectAdapter.prototype.select = function (data) {
    var self = this;

    data.selected = true;

    // If data.element is a DOM node, use it instead
    if ($(data.element).is('option')) {
      data.element.selected = true;

      this.$element.trigger('change');

      return;
    }

    if (this.$element.prop('multiple')) {
      this.current(function (currentData) {
        var val = [];

        data = [data];
        data.push.apply(data, currentData);

        for (var d = 0; d < data.length; d++) {
          var id = data[d].id;

          if ($.inArray(id, val) === -1) {
            val.push(id);
          }
        }

        self.$element.val(val);
        self.$element.trigger('change');
      });
    } else {
      var val = data.id;

      this.$element.val(val);
      this.$element.trigger('change');
    }
  };

  SelectAdapter.prototype.unselect = function (data) {
    var self = this;

    if (!this.$element.prop('multiple')) {
      return;
    }

    data.selected = false;

    if ($(data.element).is('option')) {
      data.element.selected = false;

      this.$element.trigger('change');

      return;
    }

    this.current(function (currentData) {
      var val = [];

      for (var d = 0; d < currentData.length; d++) {
        var id = currentData[d].id;

        if (id !== data.id && $.inArray(id, val) === -1) {
          val.push(id);
        }
      }

      self.$element.val(val);

      self.$element.trigger('change');
    });
  };

  SelectAdapter.prototype.bind = function (container, $container) {
    var self = this;

    this.container = container;

    container.on('select', function (params) {
      self.select(params.data);
    });

    container.on('unselect', function (params) {
      self.unselect(params.data);
    });
  };

  SelectAdapter.prototype.destroy = function () {
    // Remove anything added to child elements
    this.$element.find('*').each(function () {
      // Remove any custom data set by Select2
      $.removeData(this, 'data');
    });
  };

  SelectAdapter.prototype.query = function (params, callback) {
    var data = [];
    var self = this;

    var $options = this.$element.children();

    $options.each(function () {
      var $option = $(this);

      if (!$option.is('option') && !$option.is('optgroup')) {
        return;
      }

      var option = self.item($option);

      var matches = self.matches(params, option);

      if (matches !== null) {
        data.push(matches);
      }
    });

    callback({
      results: data
    });
  };

  SelectAdapter.prototype.addOptions = function ($options) {
    Utils.appendMany(this.$element, $options);
  };

  SelectAdapter.prototype.option = function (data) {
    var option;

    if (data.children) {
      option = document.createElement('optgroup');
      option.label = data.text;
    } else {
      option = document.createElement('option');

      if (option.textContent !== undefined) {
        option.textContent = data.text;
      } else {
        option.innerText = data.text;
      }
    }

    if (data.id) {
      option.value = data.id;
    }

    if (data.disabled) {
      option.disabled = true;
    }

    if (data.selected) {
      option.selected = true;
    }

    if (data.title) {
      option.title = data.title;
    }

    var $option = $(option);

    var normalizedData = this._normalizeItem(data);
    normalizedData.element = option;

    // Override the option's data with the combined data
    $.data(option, 'data', normalizedData);

    return $option;
  };

  SelectAdapter.prototype.item = function ($option) {
    var data = {};

    data = $.data($option[0], 'data');

    if (data != null) {
      return data;
    }

    if ($option.is('option')) {
      data = {
        id: $option.val(),
        text: $option.text(),
        disabled: $option.prop('disabled'),
        selected: $option.prop('selected'),
        title: $option.prop('title')
      };
    } else if ($option.is('optgroup')) {
      data = {
        text: $option.prop('label'),
        children: [],
        title: $option.prop('title')
      };

      var $children = $option.children('option');
      var children = [];

      for (var c = 0; c < $children.length; c++) {
        var $child = $($children[c]);

        var child = this.item($child);

        children.push(child);
      }

      data.children = children;
    }

    data = this._normalizeItem(data);
    data.element = $option[0];

    $.data($option[0], 'data', data);

    return data;
  };

  SelectAdapter.prototype._normalizeItem = function (item) {
    if (!$.isPlainObject(item)) {
      item = {
        id: item,
        text: item
      };
    }

    item = $.extend({}, {
      text: ''
    }, item);

    var defaults = {
      selected: false,
      disabled: false
    };

    if (item.id != null) {
      item.id = item.id.toString();
    }

    if (item.text != null) {
      item.text = item.text.toString();
    }

    if (item._resultId == null && item.id && this.container != null) {
      item._resultId = this.generateResultId(this.container, item);
    }

    return $.extend({}, defaults, item);
  };

  SelectAdapter.prototype.matches = function (params, data) {
    var matcher = this.options.get('matcher');

    return matcher(params, data);
  };

  return SelectAdapter;
});

S2.define('select2/data/array',[
  './select',
  '../utils',
  'jquery'
], function (SelectAdapter, Utils, $) {
  function ArrayAdapter ($element, options) {
    var data = options.get('data') || [];

    ArrayAdapter.__super__.constructor.call(this, $element, options);

    this.addOptions(this.convertToOptions(data));
  }

  Utils.Extend(ArrayAdapter, SelectAdapter);

  ArrayAdapter.prototype.select = function (data) {
    var $option = this.$element.find('option').filter(function (i, elm) {
      return elm.value == data.id.toString();
    });

    if ($option.length === 0) {
      $option = this.option(data);

      this.addOptions($option);
    }

    ArrayAdapter.__super__.select.call(this, data);
  };

  ArrayAdapter.prototype.convertToOptions = function (data) {
    var self = this;

    var $existing = this.$element.find('option');
    var existingIds = $existing.map(function () {
      return self.item($(this)).id;
    }).get();

    var $options = [];

    // Filter out all items except for the one passed in the argument
    function onlyItem (item) {
      return function () {
        return $(this).val() == item.id;
      };
    }

    for (var d = 0; d < data.length; d++) {
      var item = this._normalizeItem(data[d]);

      // Skip items which were pre-loaded, only merge the data
      if ($.inArray(item.id, existingIds) >= 0) {
        var $existingOption = $existing.filter(onlyItem(item));

        var existingData = this.item($existingOption);
        var newData = $.extend(true, {}, item, existingData);

        var $newOption = this.option(newData);

        $existingOption.replaceWith($newOption);

        continue;
      }

      var $option = this.option(item);

      if (item.children) {
        var $children = this.convertToOptions(item.children);

        Utils.appendMany($option, $children);
      }

      $options.push($option);
    }

    return $options;
  };

  return ArrayAdapter;
});

S2.define('select2/data/ajax',[
  './array',
  '../utils',
  'jquery'
], function (ArrayAdapter, Utils, $) {
  function AjaxAdapter ($element, options) {
    this.ajaxOptions = this._applyDefaults(options.get('ajax'));

    if (this.ajaxOptions.processResults != null) {
      this.processResults = this.ajaxOptions.processResults;
    }

    AjaxAdapter.__super__.constructor.call(this, $element, options);
  }

  Utils.Extend(AjaxAdapter, ArrayAdapter);

  AjaxAdapter.prototype._applyDefaults = function (options) {
    var defaults = {
      data: function (params) {
        return $.extend({}, params, {
          q: params.term
        });
      },
      transport: function (params, success, failure) {
        var $request = $.ajax(params);

        $request.then(success);
        $request.fail(failure);

        return $request;
      }
    };

    return $.extend({}, defaults, options, true);
  };

  AjaxAdapter.prototype.processResults = function (results) {
    return results;
  };

  AjaxAdapter.prototype.query = function (params, callback) {
    var matches = [];
    var self = this;

    if (this._request != null) {
      // JSONP requests cannot always be aborted
      if ($.isFunction(this._request.abort)) {
        this._request.abort();
      }

      this._request = null;
    }

    var options = $.extend({
      type: 'GET'
    }, this.ajaxOptions);

    if (typeof options.url === 'function') {
      options.url = options.url.call(this.$element, params);
    }

    if (typeof options.data === 'function') {
      options.data = options.data.call(this.$element, params);
    }

    function request () {
      var $request = options.transport(options, function (data) {
        var results = self.processResults(data, params);

        if (self.options.get('debug') && window.console && console.error) {
          // Check to make sure that the response included a `results` key.
          if (!results || !results.results || !$.isArray(results.results)) {
            console.error(
              'Select2: The AJAX results did not return an array in the ' +
              '`results` key of the response.'
            );
          }
        }

        callback(results);
      }, function () {
        // Attempt to detect if a request was aborted
        // Only works if the transport exposes a status property
        if ($request.status && $request.status === '0') {
          return;
        }

        self.trigger('results:message', {
          message: 'errorLoading'
        });
      });

      self._request = $request;
    }

    if (this.ajaxOptions.delay && params.term != null) {
      if (this._queryTimeout) {
        window.clearTimeout(this._queryTimeout);
      }

      this._queryTimeout = window.setTimeout(request, this.ajaxOptions.delay);
    } else {
      request();
    }
  };

  return AjaxAdapter;
});

S2.define('select2/data/tags',[
  'jquery'
], function ($) {
  function Tags (decorated, $element, options) {
    var tags = options.get('tags');

    var createTag = options.get('createTag');

    if (createTag !== undefined) {
      this.createTag = createTag;
    }

    var insertTag = options.get('insertTag');

    if (insertTag !== undefined) {
        this.insertTag = insertTag;
    }

    decorated.call(this, $element, options);

    if ($.isArray(tags)) {
      for (var t = 0; t < tags.length; t++) {
        var tag = tags[t];
        var item = this._normalizeItem(tag);

        var $option = this.option(item);

        this.$element.append($option);
      }
    }
  }

  Tags.prototype.query = function (decorated, params, callback) {
    var self = this;

    this._removeOldTags();

    if (params.term == null || params.page != null) {
      decorated.call(this, params, callback);
      return;
    }

    function wrapper (obj, child) {
      var data = obj.results;

      for (var i = 0; i < data.length; i++) {
        var option = data[i];

        var checkChildren = (
          option.children != null &&
          !wrapper({
            results: option.children
          }, true)
        );

        var checkText = option.text === params.term;

        if (checkText || checkChildren) {
          if (child) {
            return false;
          }

          obj.data = data;
          callback(obj);

          return;
        }
      }

      if (child) {
        return true;
      }

      var tag = self.createTag(params);

      if (tag != null) {
        var $option = self.option(tag);
        $option.attr('data-select2-tag', true);

        self.addOptions([$option]);

        self.insertTag(data, tag);
      }

      obj.results = data;

      callback(obj);
    }

    decorated.call(this, params, wrapper);
  };

  Tags.prototype.createTag = function (decorated, params) {
    var term = $.trim(params.term);

    if (term === '') {
      return null;
    }

    return {
      id: term,
      text: term
    };
  };

  Tags.prototype.insertTag = function (_, data, tag) {
    data.unshift(tag);
  };

  Tags.prototype._removeOldTags = function (_) {
    var tag = this._lastTag;

    var $options = this.$element.find('option[data-select2-tag]');

    $options.each(function () {
      if (this.selected) {
        return;
      }

      $(this).remove();
    });
  };

  return Tags;
});

S2.define('select2/data/tokenizer',[
  'jquery'
], function ($) {
  function Tokenizer (decorated, $element, options) {
    var tokenizer = options.get('tokenizer');

    if (tokenizer !== undefined) {
      this.tokenizer = tokenizer;
    }

    decorated.call(this, $element, options);
  }

  Tokenizer.prototype.bind = function (decorated, container, $container) {
    decorated.call(this, container, $container);

    this.$search =  container.dropdown.$search || container.selection.$search ||
      $container.find('.select2-search__field');
  };

  Tokenizer.prototype.query = function (decorated, params, callback) {
    var self = this;

    function createAndSelect (data) {
      // Normalize the data object so we can use it for checks
      var item = self._normalizeItem(data);

      // Check if the data object already exists as a tag
      // Select it if it doesn't
      var $existingOptions = self.$element.find('option').filter(function () {
        return $(this).val() === item.id;
      });

      // If an existing option wasn't found for it, create the option
      if (!$existingOptions.length) {
        var $option = self.option(item);
        $option.attr('data-select2-tag', true);

        self._removeOldTags();
        self.addOptions([$option]);
      }

      // Select the item, now that we know there is an option for it
      select(item);
    }

    function select (data) {
      self.trigger('select', {
        data: data
      });
    }

    params.term = params.term || '';

    var tokenData = this.tokenizer(params, this.options, createAndSelect);

    if (tokenData.term !== params.term) {
      // Replace the search term if we have the search box
      if (this.$search.length) {
        this.$search.val(tokenData.term);
        this.$search.focus();
      }

      params.term = tokenData.term;
    }

    decorated.call(this, params, callback);
  };

  Tokenizer.prototype.tokenizer = function (_, params, options, callback) {
    var separators = options.get('tokenSeparators') || [];
    var term = params.term;
    var i = 0;

    var createTag = this.createTag || function (params) {
      return {
        id: params.term,
        text: params.term
      };
    };

    while (i < term.length) {
      var termChar = term[i];

      if ($.inArray(termChar, separators) === -1) {
        i++;

        continue;
      }

      var part = term.substr(0, i);
      var partParams = $.extend({}, params, {
        term: part
      });

      var data = createTag(partParams);

      if (data == null) {
        i++;
        continue;
      }

      callback(data);

      // Reset the term to not include the tokenized portion
      term = term.substr(i + 1) || '';
      i = 0;
    }

    return {
      term: term
    };
  };

  return Tokenizer;
});

S2.define('select2/data/minimumInputLength',[

], function () {
  function MinimumInputLength (decorated, $e, options) {
    this.minimumInputLength = options.get('minimumInputLength');

    decorated.call(this, $e, options);
  }

  MinimumInputLength.prototype.query = function (decorated, params, callback) {
    params.term = params.term || '';

    if (params.term.length < this.minimumInputLength) {
      this.trigger('results:message', {
        message: 'inputTooShort',
        args: {
          minimum: this.minimumInputLength,
          input: params.term,
          params: params
        }
      });

      return;
    }

    decorated.call(this, params, callback);
  };

  return MinimumInputLength;
});

S2.define('select2/data/maximumInputLength',[

], function () {
  function MaximumInputLength (decorated, $e, options) {
    this.maximumInputLength = options.get('maximumInputLength');

    decorated.call(this, $e, options);
  }

  MaximumInputLength.prototype.query = function (decorated, params, callback) {
    params.term = params.term || '';

    if (this.maximumInputLength > 0 &&
        params.term.length > this.maximumInputLength) {
      this.trigger('results:message', {
        message: 'inputTooLong',
        args: {
          maximum: this.maximumInputLength,
          input: params.term,
          params: params
        }
      });

      return;
    }

    decorated.call(this, params, callback);
  };

  return MaximumInputLength;
});

S2.define('select2/data/maximumSelectionLength',[

], function (){
  function MaximumSelectionLength (decorated, $e, options) {
    this.maximumSelectionLength = options.get('maximumSelectionLength');

    decorated.call(this, $e, options);
  }

  MaximumSelectionLength.prototype.query =
    function (decorated, params, callback) {
      var self = this;

      this.current(function (currentData) {
        var count = currentData != null ? currentData.length : 0;
        if (self.maximumSelectionLength > 0 &&
          count >= self.maximumSelectionLength) {
          self.trigger('results:message', {
            message: 'maximumSelected',
            args: {
              maximum: self.maximumSelectionLength
            }
          });
          return;
        }
        decorated.call(self, params, callback);
      });
  };

  return MaximumSelectionLength;
});

S2.define('select2/dropdown',[
  'jquery',
  './utils'
], function ($, Utils) {
  function Dropdown ($element, options) {
    this.$element = $element;
    this.options = options;

    Dropdown.__super__.constructor.call(this);
  }

  Utils.Extend(Dropdown, Utils.Observable);

  Dropdown.prototype.render = function () {
    var $dropdown = $(
      '<span class="select2-dropdown">' +
        '<span class="select2-results"></span>' +
      '</span>'
    );

    $dropdown.attr('dir', this.options.get('dir'));

    this.$dropdown = $dropdown;

    return $dropdown;
  };

  Dropdown.prototype.bind = function () {
    // Should be implemented in subclasses
  };

  Dropdown.prototype.position = function ($dropdown, $container) {
    // Should be implmented in subclasses
  };

  Dropdown.prototype.destroy = function () {
    // Remove the dropdown from the DOM
    this.$dropdown.remove();
  };

  return Dropdown;
});

S2.define('select2/dropdown/search',[
  'jquery',
  '../utils'
], function ($, Utils) {
  function Search () { }

  Search.prototype.render = function (decorated) {
    var $rendered = decorated.call(this);

    var $search = $(
      '<span class="select2-search select2-search--dropdown">' +
        '<input class="select2-search__field" type="search" tabindex="-1"' +
        ' autocomplete="off" autocorrect="off" autocapitalize="off"' +
        ' spellcheck="false" role="textbox" />' +
      '</span>'
    );

    this.$searchContainer = $search;
    this.$search = $search.find('input');

    $rendered.prepend($search);

    return $rendered;
  };

  Search.prototype.bind = function (decorated, container, $container) {
    var self = this;

    decorated.call(this, container, $container);

    this.$search.on('keydown', function (evt) {
      self.trigger('keypress', evt);

      self._keyUpPrevented = evt.isDefaultPrevented();
    });

    // Workaround for browsers which do not support the `input` event
    // This will prevent double-triggering of events for browsers which support
    // both the `keyup` and `input` events.
    this.$search.on('input', function (evt) {
      // Unbind the duplicated `keyup` event
      $(this).off('keyup');
    });

    this.$search.on('keyup input', function (evt) {
      self.handleSearch(evt);
    });

    container.on('open', function () {
      self.$search.attr('tabindex', 0);

      self.$search.focus();

      window.setTimeout(function () {
        self.$search.focus();
      }, 0);
    });

    container.on('close', function () {
      self.$search.attr('tabindex', -1);

      self.$search.val('');
    });

    container.on('focus', function () {
      if (container.isOpen()) {
        self.$search.focus();
      }
    });

    container.on('results:all', function (params) {
      if (params.query.term == null || params.query.term === '') {
        var showSearch = self.showSearch(params);

        if (showSearch) {
          self.$searchContainer.removeClass('select2-search--hide');
        } else {
          self.$searchContainer.addClass('select2-search--hide');
        }
      }
    });
  };

  Search.prototype.handleSearch = function (evt) {
    if (!this._keyUpPrevented) {
      var input = this.$search.val();

      this.trigger('query', {
        term: input
      });
    }

    this._keyUpPrevented = false;
  };

  Search.prototype.showSearch = function (_, params) {
    return true;
  };

  return Search;
});

S2.define('select2/dropdown/hidePlaceholder',[

], function () {
  function HidePlaceholder (decorated, $element, options, dataAdapter) {
    this.placeholder = this.normalizePlaceholder(options.get('placeholder'));

    decorated.call(this, $element, options, dataAdapter);
  }

  HidePlaceholder.prototype.append = function (decorated, data) {
    data.results = this.removePlaceholder(data.results);

    decorated.call(this, data);
  };

  HidePlaceholder.prototype.normalizePlaceholder = function (_, placeholder) {
    if (typeof placeholder === 'string') {
      placeholder = {
        id: '',
        text: placeholder
      };
    }

    return placeholder;
  };

  HidePlaceholder.prototype.removePlaceholder = function (_, data) {
    var modifiedData = data.slice(0);

    for (var d = data.length - 1; d >= 0; d--) {
      var item = data[d];

      if (this.placeholder.id === item.id) {
        modifiedData.splice(d, 1);
      }
    }

    return modifiedData;
  };

  return HidePlaceholder;
});

S2.define('select2/dropdown/infiniteScroll',[
  'jquery'
], function ($) {
  function InfiniteScroll (decorated, $element, options, dataAdapter) {
    this.lastParams = {};

    decorated.call(this, $element, options, dataAdapter);

    this.$loadingMore = this.createLoadingMore();
    this.loading = false;
  }

  InfiniteScroll.prototype.append = function (decorated, data) {
    this.$loadingMore.remove();
    this.loading = false;

    decorated.call(this, data);

    if (this.showLoadingMore(data)) {
      this.$results.append(this.$loadingMore);
    }
  };

  InfiniteScroll.prototype.bind = function (decorated, container, $container) {
    var self = this;

    decorated.call(this, container, $container);

    container.on('query', function (params) {
      self.lastParams = params;
      self.loading = true;
    });

    container.on('query:append', function (params) {
      self.lastParams = params;
      self.loading = true;
    });

    this.$results.on('scroll', function () {
      var isLoadMoreVisible = $.contains(
        document.documentElement,
        self.$loadingMore[0]
      );

      if (self.loading || !isLoadMoreVisible) {
        return;
      }

      var currentOffset = self.$results.offset().top +
        self.$results.outerHeight(false);
      var loadingMoreOffset = self.$loadingMore.offset().top +
        self.$loadingMore.outerHeight(false);

      if (currentOffset + 50 >= loadingMoreOffset) {
        self.loadMore();
      }
    });
  };

  InfiniteScroll.prototype.loadMore = function () {
    this.loading = true;

    var params = $.extend({}, {page: 1}, this.lastParams);

    params.page++;

    this.trigger('query:append', params);
  };

  InfiniteScroll.prototype.showLoadingMore = function (_, data) {
    return data.pagination && data.pagination.more;
  };

  InfiniteScroll.prototype.createLoadingMore = function () {
    var $option = $(
      '<li ' +
      'class="select2-results__option select2-results__option--load-more"' +
      'role="treeitem" aria-disabled="true"></li>'
    );

    var message = this.options.get('translations').get('loadingMore');

    $option.html(message(this.lastParams));

    return $option;
  };

  return InfiniteScroll;
});

S2.define('select2/dropdown/attachBody',[
  'jquery',
  '../utils'
], function ($, Utils) {
  function AttachBody (decorated, $element, options) {
    this.$dropdownParent = options.get('dropdownParent') || $(document.body);

    decorated.call(this, $element, options);
  }

  AttachBody.prototype.bind = function (decorated, container, $container) {
    var self = this;

    var setupResultsEvents = false;

    decorated.call(this, container, $container);

    container.on('open', function () {
      self._showDropdown();
      self._attachPositioningHandler(container);

      if (!setupResultsEvents) {
        setupResultsEvents = true;

        container.on('results:all', function () {
          self._positionDropdown();
          self._resizeDropdown();
        });

        container.on('results:append', function () {
          self._positionDropdown();
          self._resizeDropdown();
        });
      }
    });

    container.on('close', function () {
      self._hideDropdown();
      self._detachPositioningHandler(container);
    });

    this.$dropdownContainer.on('mousedown', function (evt) {
      evt.stopPropagation();
    });
  };

  AttachBody.prototype.destroy = function (decorated) {
    decorated.call(this);

    this.$dropdownContainer.remove();
  };

  AttachBody.prototype.position = function (decorated, $dropdown, $container) {
    // Clone all of the container classes
    $dropdown.attr('class', $container.attr('class'));

    $dropdown.removeClass('select2');
    $dropdown.addClass('select2-container--open');

    $dropdown.css({
      position: 'absolute',
      top: -999999
    });

    this.$container = $container;
  };

  AttachBody.prototype.render = function (decorated) {
    var $container = $('<span></span>');

    var $dropdown = decorated.call(this);
    $container.append($dropdown);

    this.$dropdownContainer = $container;

    return $container;
  };

  AttachBody.prototype._hideDropdown = function (decorated) {
    this.$dropdownContainer.detach();
  };

  AttachBody.prototype._attachPositioningHandler =
      function (decorated, container) {
    var self = this;

    var scrollEvent = 'scroll.select2.' + container.id;
    var resizeEvent = 'resize.select2.' + container.id;
    var orientationEvent = 'orientationchange.select2.' + container.id;

    var $watchers = this.$container.parents().filter(Utils.hasScroll);
    $watchers.each(function () {
      $(this).data('select2-scroll-position', {
        x: $(this).scrollLeft(),
        y: $(this).scrollTop()
      });
    });

    $watchers.on(scrollEvent, function (ev) {
      var position = $(this).data('select2-scroll-position');
      $(this).scrollTop(position.y);
    });

    $(window).on(scrollEvent + ' ' + resizeEvent + ' ' + orientationEvent,
      function (e) {
      self._positionDropdown();
      self._resizeDropdown();
    });
  };

  AttachBody.prototype._detachPositioningHandler =
      function (decorated, container) {
    var scrollEvent = 'scroll.select2.' + container.id;
    var resizeEvent = 'resize.select2.' + container.id;
    var orientationEvent = 'orientationchange.select2.' + container.id;

    var $watchers = this.$container.parents().filter(Utils.hasScroll);
    $watchers.off(scrollEvent);

    $(window).off(scrollEvent + ' ' + resizeEvent + ' ' + orientationEvent);
  };

  AttachBody.prototype._positionDropdown = function () {
    var $window = $(window);

    var isCurrentlyAbove = this.$dropdown.hasClass('select2-dropdown--above');
    var isCurrentlyBelow = this.$dropdown.hasClass('select2-dropdown--below');

    var newDirection = null;

    var offset = this.$container.offset();

    offset.bottom = offset.top + this.$container.outerHeight(false);

    var container = {
      height: this.$container.outerHeight(false)
    };

    container.top = offset.top;
    container.bottom = offset.top + container.height;

    var dropdown = {
      height: this.$dropdown.outerHeight(false)
    };

    var viewport = {
      top: $window.scrollTop(),
      bottom: $window.scrollTop() + $window.height()
    };

    var enoughRoomAbove = viewport.top < (offset.top - dropdown.height);
    var enoughRoomBelow = viewport.bottom > (offset.bottom + dropdown.height);

    var css = {
      left: offset.left,
      top: container.bottom
    };

    // Determine what the parent element is to use for calciulating the offset
    var $offsetParent = this.$dropdownParent;

    // For statically positoned elements, we need to get the element
    // that is determining the offset
    if ($offsetParent.css('position') === 'static') {
      $offsetParent = $offsetParent.offsetParent();
    }

    var parentOffset = $offsetParent.offset();

    css.top -= parentOffset.top;
    css.left -= parentOffset.left;

    if (!isCurrentlyAbove && !isCurrentlyBelow) {
      newDirection = 'below';
    }

    if (!enoughRoomBelow && enoughRoomAbove && !isCurrentlyAbove) {
      newDirection = 'above';
    } else if (!enoughRoomAbove && enoughRoomBelow && isCurrentlyAbove) {
      newDirection = 'below';
    }

    if (newDirection == 'above' ||
      (isCurrentlyAbove && newDirection !== 'below')) {
      css.top = container.top - parentOffset.top - dropdown.height;
    }

    if (newDirection != null) {
      this.$dropdown
        .removeClass('select2-dropdown--below select2-dropdown--above')
        .addClass('select2-dropdown--' + newDirection);
      this.$container
        .removeClass('select2-container--below select2-container--above')
        .addClass('select2-container--' + newDirection);
    }

    this.$dropdownContainer.css(css);
  };

  AttachBody.prototype._resizeDropdown = function () {
    var css = {
      width: this.$container.outerWidth(false) + 'px'
    };

    if (this.options.get('dropdownAutoWidth')) {
      css.minWidth = css.width;
      css.position = 'relative';
      css.width = 'auto';
    }

    this.$dropdown.css(css);
  };

  AttachBody.prototype._showDropdown = function (decorated) {
    this.$dropdownContainer.appendTo(this.$dropdownParent);

    this._positionDropdown();
    this._resizeDropdown();
  };

  return AttachBody;
});

S2.define('select2/dropdown/minimumResultsForSearch',[

], function () {
  function countResults (data) {
    var count = 0;

    for (var d = 0; d < data.length; d++) {
      var item = data[d];

      if (item.children) {
        count += countResults(item.children);
      } else {
        count++;
      }
    }

    return count;
  }

  function MinimumResultsForSearch (decorated, $element, options, dataAdapter) {
    this.minimumResultsForSearch = options.get('minimumResultsForSearch');

    if (this.minimumResultsForSearch < 0) {
      this.minimumResultsForSearch = Infinity;
    }

    decorated.call(this, $element, options, dataAdapter);
  }

  MinimumResultsForSearch.prototype.showSearch = function (decorated, params) {
    if (countResults(params.data.results) < this.minimumResultsForSearch) {
      return false;
    }

    return decorated.call(this, params);
  };

  return MinimumResultsForSearch;
});

S2.define('select2/dropdown/selectOnClose',[

], function () {
  function SelectOnClose () { }

  SelectOnClose.prototype.bind = function (decorated, container, $container) {
    var self = this;

    decorated.call(this, container, $container);

    container.on('close', function (params) {
      self._handleSelectOnClose(params);
    });
  };

  SelectOnClose.prototype._handleSelectOnClose = function (_, params) {
    if (params && params.originalSelect2Event != null) {
      var event = params.originalSelect2Event;

      // Don't select an item if the close event was triggered from a select or
      // unselect event
      if (event._type === 'select' || event._type === 'unselect') {
        return;
      }
    }

    var $highlightedResults = this.getHighlightedResults();

    // Only select highlighted results
    if ($highlightedResults.length < 1) {
      return;
    }

    var data = $highlightedResults.data('data');

    // Don't re-select already selected resulte
    if (
      (data.element != null && data.element.selected) ||
      (data.element == null && data.selected)
    ) {
      return;
    }

    this.trigger('select', {
        data: data
    });
  };

  return SelectOnClose;
});

S2.define('select2/dropdown/closeOnSelect',[

], function () {
  function CloseOnSelect () { }

  CloseOnSelect.prototype.bind = function (decorated, container, $container) {
    var self = this;

    decorated.call(this, container, $container);

    container.on('select', function (evt) {
      self._selectTriggered(evt);
    });

    container.on('unselect', function (evt) {
      self._selectTriggered(evt);
    });
  };

  CloseOnSelect.prototype._selectTriggered = function (_, evt) {
    var originalEvent = evt.originalEvent;

    // Don't close if the control key is being held
    if (originalEvent && originalEvent.ctrlKey) {
      return;
    }

    this.trigger('close', {
      originalEvent: originalEvent,
      originalSelect2Event: evt
    });
  };

  return CloseOnSelect;
});

S2.define('select2/i18n/en',[],function () {
  // English
  return {
    errorLoading: function () {
      return 'The results could not be loaded.';
    },
    inputTooLong: function (args) {
      var overChars = args.input.length - args.maximum;

      var message = 'Please delete ' + overChars + ' character';

      if (overChars != 1) {
        message += 's';
      }

      return message;
    },
    inputTooShort: function (args) {
      var remainingChars = args.minimum - args.input.length;

      var message = 'Please enter ' + remainingChars + ' or more characters';

      return message;
    },
    loadingMore: function () {
      return 'Loading more results…';
    },
    maximumSelected: function (args) {
      var message = 'You can only select ' + args.maximum + ' item';

      if (args.maximum != 1) {
        message += 's';
      }

      return message;
    },
    noResults: function () {
      return 'No results found';
    },
    searching: function () {
      return 'Searching…';
    }
  };
});

S2.define('select2/defaults',[
  'jquery',
  'require',

  './results',

  './selection/single',
  './selection/multiple',
  './selection/placeholder',
  './selection/allowClear',
  './selection/search',
  './selection/eventRelay',

  './utils',
  './translation',
  './diacritics',

  './data/select',
  './data/array',
  './data/ajax',
  './data/tags',
  './data/tokenizer',
  './data/minimumInputLength',
  './data/maximumInputLength',
  './data/maximumSelectionLength',

  './dropdown',
  './dropdown/search',
  './dropdown/hidePlaceholder',
  './dropdown/infiniteScroll',
  './dropdown/attachBody',
  './dropdown/minimumResultsForSearch',
  './dropdown/selectOnClose',
  './dropdown/closeOnSelect',

  './i18n/en'
], function ($, require,

             ResultsList,

             SingleSelection, MultipleSelection, Placeholder, AllowClear,
             SelectionSearch, EventRelay,

             Utils, Translation, DIACRITICS,

             SelectData, ArrayData, AjaxData, Tags, Tokenizer,
             MinimumInputLength, MaximumInputLength, MaximumSelectionLength,

             Dropdown, DropdownSearch, HidePlaceholder, InfiniteScroll,
             AttachBody, MinimumResultsForSearch, SelectOnClose, CloseOnSelect,

             EnglishTranslation) {
  function Defaults () {
    this.reset();
  }

  Defaults.prototype.apply = function (options) {
    options = $.extend(true, {}, this.defaults, options);

    if (options.dataAdapter == null) {
      if (options.ajax != null) {
        options.dataAdapter = AjaxData;
      } else if (options.data != null) {
        options.dataAdapter = ArrayData;
      } else {
        options.dataAdapter = SelectData;
      }

      if (options.minimumInputLength > 0) {
        options.dataAdapter = Utils.Decorate(
          options.dataAdapter,
          MinimumInputLength
        );
      }

      if (options.maximumInputLength > 0) {
        options.dataAdapter = Utils.Decorate(
          options.dataAdapter,
          MaximumInputLength
        );
      }

      if (options.maximumSelectionLength > 0) {
        options.dataAdapter = Utils.Decorate(
          options.dataAdapter,
          MaximumSelectionLength
        );
      }

      if (options.tags) {
        options.dataAdapter = Utils.Decorate(options.dataAdapter, Tags);
      }

      if (options.tokenSeparators != null || options.tokenizer != null) {
        options.dataAdapter = Utils.Decorate(
          options.dataAdapter,
          Tokenizer
        );
      }

      if (options.query != null) {
        var Query = require(options.amdBase + 'compat/query');

        options.dataAdapter = Utils.Decorate(
          options.dataAdapter,
          Query
        );
      }

      if (options.initSelection != null) {
        var InitSelection = require(options.amdBase + 'compat/initSelection');

        options.dataAdapter = Utils.Decorate(
          options.dataAdapter,
          InitSelection
        );
      }
    }

    if (options.resultsAdapter == null) {
      options.resultsAdapter = ResultsList;

      if (options.ajax != null) {
        options.resultsAdapter = Utils.Decorate(
          options.resultsAdapter,
          InfiniteScroll
        );
      }

      if (options.placeholder != null) {
        options.resultsAdapter = Utils.Decorate(
          options.resultsAdapter,
          HidePlaceholder
        );
      }

      if (options.selectOnClose) {
        options.resultsAdapter = Utils.Decorate(
          options.resultsAdapter,
          SelectOnClose
        );
      }
    }

    if (options.dropdownAdapter == null) {
      if (options.multiple) {
        options.dropdownAdapter = Dropdown;
      } else {
        var SearchableDropdown = Utils.Decorate(Dropdown, DropdownSearch);

        options.dropdownAdapter = SearchableDropdown;
      }

      if (options.minimumResultsForSearch !== 0) {
        options.dropdownAdapter = Utils.Decorate(
          options.dropdownAdapter,
          MinimumResultsForSearch
        );
      }

      if (options.closeOnSelect) {
        options.dropdownAdapter = Utils.Decorate(
          options.dropdownAdapter,
          CloseOnSelect
        );
      }

      if (
        options.dropdownCssClass != null ||
        options.dropdownCss != null ||
        options.adaptDropdownCssClass != null
      ) {
        var DropdownCSS = require(options.amdBase + 'compat/dropdownCss');

        options.dropdownAdapter = Utils.Decorate(
          options.dropdownAdapter,
          DropdownCSS
        );
      }

      options.dropdownAdapter = Utils.Decorate(
        options.dropdownAdapter,
        AttachBody
      );
    }

    if (options.selectionAdapter == null) {
      if (options.multiple) {
        options.selectionAdapter = MultipleSelection;
      } else {
        options.selectionAdapter = SingleSelection;
      }

      // Add the placeholder mixin if a placeholder was specified
      if (options.placeholder != null) {
        options.selectionAdapter = Utils.Decorate(
          options.selectionAdapter,
          Placeholder
        );
      }

      if (options.allowClear) {
        options.selectionAdapter = Utils.Decorate(
          options.selectionAdapter,
          AllowClear
        );
      }

      if (options.multiple) {
        options.selectionAdapter = Utils.Decorate(
          options.selectionAdapter,
          SelectionSearch
        );
      }

      if (
        options.containerCssClass != null ||
        options.containerCss != null ||
        options.adaptContainerCssClass != null
      ) {
        var ContainerCSS = require(options.amdBase + 'compat/containerCss');

        options.selectionAdapter = Utils.Decorate(
          options.selectionAdapter,
          ContainerCSS
        );
      }

      options.selectionAdapter = Utils.Decorate(
        options.selectionAdapter,
        EventRelay
      );
    }

    if (typeof options.language === 'string') {
      // Check if the language is specified with a region
      if (options.language.indexOf('-') > 0) {
        // Extract the region information if it is included
        var languageParts = options.language.split('-');
        var baseLanguage = languageParts[0];

        options.language = [options.language, baseLanguage];
      } else {
        options.language = [options.language];
      }
    }

    if ($.isArray(options.language)) {
      var languages = new Translation();
      options.language.push('en');

      var languageNames = options.language;

      for (var l = 0; l < languageNames.length; l++) {
        var name = languageNames[l];
        var language = {};

        try {
          // Try to load it with the original name
          language = Translation.loadPath(name);
        } catch (e) {
          try {
            // If we couldn't load it, check if it wasn't the full path
            name = this.defaults.amdLanguageBase + name;
            language = Translation.loadPath(name);
          } catch (ex) {
            // The translation could not be loaded at all. Sometimes this is
            // because of a configuration problem, other times this can be
            // because of how Select2 helps load all possible translation files.
            if (options.debug && window.console && console.warn) {
              console.warn(
                'Select2: The language file for "' + name + '" could not be ' +
                'automatically loaded. A fallback will be used instead.'
              );
            }

            continue;
          }
        }

        languages.extend(language);
      }

      options.translations = languages;
    } else {
      var baseTranslation = Translation.loadPath(
        this.defaults.amdLanguageBase + 'en'
      );
      var customTranslation = new Translation(options.language);

      customTranslation.extend(baseTranslation);

      options.translations = customTranslation;
    }

    return options;
  };

  Defaults.prototype.reset = function () {
    function stripDiacritics (text) {
      // Used 'uni range + named function' from http://jsperf.com/diacritics/18
      function match(a) {
        return DIACRITICS[a] || a;
      }

      return text.replace(/[^\u0000-\u007E]/g, match);
    }

    function matcher (params, data) {
      // Always return the object if there is nothing to compare
      if ($.trim(params.term) === '') {
        return data;
      }

      // Do a recursive check for options with children
      if (data.children && data.children.length > 0) {
        // Clone the data object if there are children
        // This is required as we modify the object to remove any non-matches
        var match = $.extend(true, {}, data);

        // Check each child of the option
        for (var c = data.children.length - 1; c >= 0; c--) {
          var child = data.children[c];

          var matches = matcher(params, child);

          // If there wasn't a match, remove the object in the array
          if (matches == null) {
            match.children.splice(c, 1);
          }
        }

        // If any children matched, return the new object
        if (match.children.length > 0) {
          return match;
        }

        // If there were no matching children, check just the plain object
        return matcher(params, match);
      }

      var original = stripDiacritics(data.text).toUpperCase();
      var term = stripDiacritics(params.term).toUpperCase();

      // Check if the text contains the term
      if (original.indexOf(term) > -1) {
        return data;
      }

      // If it doesn't contain the term, don't return anything
      return null;
    }

    this.defaults = {
      amdBase: './',
      amdLanguageBase: './i18n/',
      closeOnSelect: true,
      debug: false,
      dropdownAutoWidth: false,
      escapeMarkup: Utils.escapeMarkup,
      language: EnglishTranslation,
      matcher: matcher,
      minimumInputLength: 0,
      maximumInputLength: 0,
      maximumSelectionLength: 0,
      minimumResultsForSearch: 0,
      selectOnClose: false,
      sorter: function (data) {
        return data;
      },
      templateResult: function (result) {
        return result.text;
      },
      templateSelection: function (selection) {
        return selection.text;
      },
      theme: 'default',
      width: 'resolve'
    };
  };

  Defaults.prototype.set = function (key, value) {
    var camelKey = $.camelCase(key);

    var data = {};
    data[camelKey] = value;

    var convertedData = Utils._convertData(data);

    $.extend(this.defaults, convertedData);
  };

  var defaults = new Defaults();

  return defaults;
});

S2.define('select2/options',[
  'require',
  'jquery',
  './defaults',
  './utils'
], function (require, $, Defaults, Utils) {
  function Options (options, $element) {
    this.options = options;

    if ($element != null) {
      this.fromElement($element);
    }

    this.options = Defaults.apply(this.options);

    if ($element && $element.is('input')) {
      var InputCompat = require(this.get('amdBase') + 'compat/inputData');

      this.options.dataAdapter = Utils.Decorate(
        this.options.dataAdapter,
        InputCompat
      );
    }
  }

  Options.prototype.fromElement = function ($e) {
    var excludedData = ['select2'];

    if (this.options.multiple == null) {
      this.options.multiple = $e.prop('multiple');
    }

    if (this.options.disabled == null) {
      this.options.disabled = $e.prop('disabled');
    }

    if (this.options.language == null) {
      if ($e.prop('lang')) {
        this.options.language = $e.prop('lang').toLowerCase();
      } else if ($e.closest('[lang]').prop('lang')) {
        this.options.language = $e.closest('[lang]').prop('lang');
      }
    }

    if (this.options.dir == null) {
      if ($e.prop('dir')) {
        this.options.dir = $e.prop('dir');
      } else if ($e.closest('[dir]').prop('dir')) {
        this.options.dir = $e.closest('[dir]').prop('dir');
      } else {
        this.options.dir = 'ltr';
      }
    }

    $e.prop('disabled', this.options.disabled);
    $e.prop('multiple', this.options.multiple);

    if ($e.data('select2Tags')) {
      if (this.options.debug && window.console && console.warn) {
        console.warn(
          'Select2: The `data-select2-tags` attribute has been changed to ' +
          'use the `data-data` and `data-tags="true"` attributes and will be ' +
          'removed in future versions of Select2.'
        );
      }

      $e.data('data', $e.data('select2Tags'));
      $e.data('tags', true);
    }

    if ($e.data('ajaxUrl')) {
      if (this.options.debug && window.console && console.warn) {
        console.warn(
          'Select2: The `data-ajax-url` attribute has been changed to ' +
          '`data-ajax--url` and support for the old attribute will be removed' +
          ' in future versions of Select2.'
        );
      }

      $e.attr('ajax--url', $e.data('ajaxUrl'));
      $e.data('ajax--url', $e.data('ajaxUrl'));
    }

    var dataset = {};

    // Prefer the element's `dataset` attribute if it exists
    // jQuery 1.x does not correctly handle data attributes with multiple dashes
    if ($.fn.jquery && $.fn.jquery.substr(0, 2) == '1.' && $e[0].dataset) {
      dataset = $.extend(true, {}, $e[0].dataset, $e.data());
    } else {
      dataset = $e.data();
    }

    var data = $.extend(true, {}, dataset);

    data = Utils._convertData(data);

    for (var key in data) {
      if ($.inArray(key, excludedData) > -1) {
        continue;
      }

      if ($.isPlainObject(this.options[key])) {
        $.extend(this.options[key], data[key]);
      } else {
        this.options[key] = data[key];
      }
    }

    return this;
  };

  Options.prototype.get = function (key) {
    return this.options[key];
  };

  Options.prototype.set = function (key, val) {
    this.options[key] = val;
  };

  return Options;
});

S2.define('select2/core',[
  'jquery',
  './options',
  './utils',
  './keys'
], function ($, Options, Utils, KEYS) {
  var Select2 = function ($element, options) {
    if ($element.data('select2') != null) {
      $element.data('select2').destroy();
    }

    this.$element = $element;

    this.id = this._generateId($element);

    options = options || {};

    this.options = new Options(options, $element);

    Select2.__super__.constructor.call(this);

    // Set up the tabindex

    var tabindex = $element.attr('tabindex') || 0;
    $element.data('old-tabindex', tabindex);
    $element.attr('tabindex', '-1');

    // Set up containers and adapters

    var DataAdapter = this.options.get('dataAdapter');
    this.dataAdapter = new DataAdapter($element, this.options);

    var $container = this.render();

    this._placeContainer($container);

    var SelectionAdapter = this.options.get('selectionAdapter');
    this.selection = new SelectionAdapter($element, this.options);
    this.$selection = this.selection.render();

    this.selection.position(this.$selection, $container);

    var DropdownAdapter = this.options.get('dropdownAdapter');
    this.dropdown = new DropdownAdapter($element, this.options);
    this.$dropdown = this.dropdown.render();

    this.dropdown.position(this.$dropdown, $container);

    var ResultsAdapter = this.options.get('resultsAdapter');
    this.results = new ResultsAdapter($element, this.options, this.dataAdapter);
    this.$results = this.results.render();

    this.results.position(this.$results, this.$dropdown);

    // Bind events

    var self = this;

    // Bind the container to all of the adapters
    this._bindAdapters();

    // Register any DOM event handlers
    this._registerDomEvents();

    // Register any internal event handlers
    this._registerDataEvents();
    this._registerSelectionEvents();
    this._registerDropdownEvents();
    this._registerResultsEvents();
    this._registerEvents();

    // Set the initial state
    this.dataAdapter.current(function (initialData) {
      self.trigger('selection:update', {
        data: initialData
      });
    });

    // Hide the original select
    $element.addClass('select2-hidden-accessible');
    $element.attr('aria-hidden', 'true');

    // Synchronize any monitored attributes
    this._syncAttributes();

    $element.data('select2', this);
  };

  Utils.Extend(Select2, Utils.Observable);

  Select2.prototype._generateId = function ($element) {
    var id = '';

    if ($element.attr('id') != null) {
      id = $element.attr('id');
    } else if ($element.attr('name') != null) {
      id = $element.attr('name') + '-' + Utils.generateChars(2);
    } else {
      id = Utils.generateChars(4);
    }

    id = id.replace(/(:|\.|\[|\]|,)/g, '');
    id = 'select2-' + id;

    return id;
  };

  Select2.prototype._placeContainer = function ($container) {
    $container.insertAfter(this.$element);

    var width = this._resolveWidth(this.$element, this.options.get('width'));

    if (width != null) {
      $container.css('width', width);
    }
  };

  Select2.prototype._resolveWidth = function ($element, method) {
    var WIDTH = /^width:(([-+]?([0-9]*\.)?[0-9]+)(px|em|ex|%|in|cm|mm|pt|pc))/i;

    if (method == 'resolve') {
      var styleWidth = this._resolveWidth($element, 'style');

      if (styleWidth != null) {
        return styleWidth;
      }

      return this._resolveWidth($element, 'element');
    }

    if (method == 'element') {
      var elementWidth = $element.outerWidth(false);

      if (elementWidth <= 0) {
        return 'auto';
      }

      return elementWidth + 'px';
    }

    if (method == 'style') {
      var style = $element.attr('style');

      if (typeof(style) !== 'string') {
        return null;
      }

      var attrs = style.split(';');

      for (var i = 0, l = attrs.length; i < l; i = i + 1) {
        var attr = attrs[i].replace(/\s/g, '');
        var matches = attr.match(WIDTH);

        if (matches !== null && matches.length >= 1) {
          return matches[1];
        }
      }

      return null;
    }

    return method;
  };

  Select2.prototype._bindAdapters = function () {
    this.dataAdapter.bind(this, this.$container);
    this.selection.bind(this, this.$container);

    this.dropdown.bind(this, this.$container);
    this.results.bind(this, this.$container);
  };

  Select2.prototype._registerDomEvents = function () {
    var self = this;

    this.$element.on('change.select2', function () {
      self.dataAdapter.current(function (data) {
        self.trigger('selection:update', {
          data: data
        });
      });
    });

    this.$element.on('focus.select2', function (evt) {
      self.trigger('focus', evt);
    });

    this._syncA = Utils.bind(this._syncAttributes, this);
    this._syncS = Utils.bind(this._syncSubtree, this);

    if (this.$element[0].attachEvent) {
      this.$element[0].attachEvent('onpropertychange', this._syncA);
    }

    var observer = window.MutationObserver ||
      window.WebKitMutationObserver ||
      window.MozMutationObserver
    ;

    if (observer != null) {
      this._observer = new observer(function (mutations) {
        $.each(mutations, self._syncA);
        $.each(mutations, self._syncS);
      });
      this._observer.observe(this.$element[0], {
        attributes: true,
        childList: true,
        subtree: false
      });
    } else if (this.$element[0].addEventListener) {
      this.$element[0].addEventListener(
        'DOMAttrModified',
        self._syncA,
        false
      );
      this.$element[0].addEventListener(
        'DOMNodeInserted',
        self._syncS,
        false
      );
      this.$element[0].addEventListener(
        'DOMNodeRemoved',
        self._syncS,
        false
      );
    }
  };

  Select2.prototype._registerDataEvents = function () {
    var self = this;

    this.dataAdapter.on('*', function (name, params) {
      self.trigger(name, params);
    });
  };

  Select2.prototype._registerSelectionEvents = function () {
    var self = this;
    var nonRelayEvents = ['toggle', 'focus'];

    this.selection.on('toggle', function () {
      self.toggleDropdown();
    });

    this.selection.on('focus', function (params) {
      self.focus(params);
    });

    this.selection.on('*', function (name, params) {
      if ($.inArray(name, nonRelayEvents) !== -1) {
        return;
      }

      self.trigger(name, params);
    });
  };

  Select2.prototype._registerDropdownEvents = function () {
    var self = this;

    this.dropdown.on('*', function (name, params) {
      self.trigger(name, params);
    });
  };

  Select2.prototype._registerResultsEvents = function () {
    var self = this;

    this.results.on('*', function (name, params) {
      self.trigger(name, params);
    });
  };

  Select2.prototype._registerEvents = function () {
    var self = this;

    this.on('open', function () {
      self.$container.addClass('select2-container--open');
    });

    this.on('close', function () {
      self.$container.removeClass('select2-container--open');
    });

    this.on('enable', function () {
      self.$container.removeClass('select2-container--disabled');
    });

    this.on('disable', function () {
      self.$container.addClass('select2-container--disabled');
    });

    this.on('blur', function () {
      self.$container.removeClass('select2-container--focus');
    });

    this.on('query', function (params) {
      if (!self.isOpen()) {
        self.trigger('open', {});
      }

      this.dataAdapter.query(params, function (data) {
        self.trigger('results:all', {
          data: data,
          query: params
        });
      });
    });

    this.on('query:append', function (params) {
      this.dataAdapter.query(params, function (data) {
        self.trigger('results:append', {
          data: data,
          query: params
        });
      });
    });

    this.on('keypress', function (evt) {
      var key = evt.which;

      if (self.isOpen()) {
        if (key === KEYS.ESC || key === KEYS.TAB ||
            (key === KEYS.UP && evt.altKey)) {
          self.close();

          evt.preventDefault();
        } else if (key === KEYS.ENTER) {
          self.trigger('results:select', {});

          evt.preventDefault();
        } else if ((key === KEYS.SPACE && evt.ctrlKey)) {
          self.trigger('results:toggle', {});

          evt.preventDefault();
        } else if (key === KEYS.UP) {
          self.trigger('results:previous', {});

          evt.preventDefault();
        } else if (key === KEYS.DOWN) {
          self.trigger('results:next', {});

          evt.preventDefault();
        }
      } else {
        if (key === KEYS.ENTER || key === KEYS.SPACE ||
            (key === KEYS.DOWN && evt.altKey)) {
          self.open();

          evt.preventDefault();
        }
      }
    });
  };

  Select2.prototype._syncAttributes = function () {
    this.options.set('disabled', this.$element.prop('disabled'));

    if (this.options.get('disabled')) {
      if (this.isOpen()) {
        this.close();
      }

      this.trigger('disable', {});
    } else {
      this.trigger('enable', {});
    }
  };

  Select2.prototype._syncSubtree = function (evt, mutations) {
    var changed = false;
    var self = this;

    // Ignore any mutation events raised for elements that aren't options or
    // optgroups. This handles the case when the select element is destroyed
    if (
      evt && evt.target && (
        evt.target.nodeName !== 'OPTION' && evt.target.nodeName !== 'OPTGROUP'
      )
    ) {
      return;
    }

    if (!mutations) {
      // If mutation events aren't supported, then we can only assume that the
      // change affected the selections
      changed = true;
    } else if (mutations.addedNodes && mutations.addedNodes.length > 0) {
      for (var n = 0; n < mutations.addedNodes.length; n++) {
        var node = mutations.addedNodes[n];

        if (node.selected) {
          changed = true;
        }
      }
    } else if (mutations.removedNodes && mutations.removedNodes.length > 0) {
      changed = true;
    }

    // Only re-pull the data if we think there is a change
    if (changed) {
      this.dataAdapter.current(function (currentData) {
        self.trigger('selection:update', {
          data: currentData
        });
      });
    }
  };

  /**
   * Override the trigger method to automatically trigger pre-events when
   * there are events that can be prevented.
   */
  Select2.prototype.trigger = function (name, args) {
    var actualTrigger = Select2.__super__.trigger;
    var preTriggerMap = {
      'open': 'opening',
      'close': 'closing',
      'select': 'selecting',
      'unselect': 'unselecting'
    };

    if (args === undefined) {
      args = {};
    }

    if (name in preTriggerMap) {
      var preTriggerName = preTriggerMap[name];
      var preTriggerArgs = {
        prevented: false,
        name: name,
        args: args
      };

      actualTrigger.call(this, preTriggerName, preTriggerArgs);

      if (preTriggerArgs.prevented) {
        args.prevented = true;

        return;
      }
    }

    actualTrigger.call(this, name, args);
  };

  Select2.prototype.toggleDropdown = function () {
    if (this.options.get('disabled')) {
      return;
    }

    if (this.isOpen()) {
      this.close();
    } else {
      this.open();
    }
  };

  Select2.prototype.open = function () {
    if (this.isOpen()) {
      return;
    }

    this.trigger('query', {});
  };

  Select2.prototype.close = function () {
    if (!this.isOpen()) {
      return;
    }

    this.trigger('close', {});
  };

  Select2.prototype.isOpen = function () {
    return this.$container.hasClass('select2-container--open');
  };

  Select2.prototype.hasFocus = function () {
    return this.$container.hasClass('select2-container--focus');
  };

  Select2.prototype.focus = function (data) {
    // No need to re-trigger focus events if we are already focused
    if (this.hasFocus()) {
      return;
    }

    this.$container.addClass('select2-container--focus');
    this.trigger('focus', {});
  };

  Select2.prototype.enable = function (args) {
    if (this.options.get('debug') && window.console && console.warn) {
      console.warn(
        'Select2: The `select2("enable")` method has been deprecated and will' +
        ' be removed in later Select2 versions. Use $element.prop("disabled")' +
        ' instead.'
      );
    }

    if (args == null || args.length === 0) {
      args = [true];
    }

    var disabled = !args[0];

    this.$element.prop('disabled', disabled);
  };

  Select2.prototype.data = function () {
    if (this.options.get('debug') &&
        arguments.length > 0 && window.console && console.warn) {
      console.warn(
        'Select2: Data can no longer be set using `select2("data")`. You ' +
        'should consider setting the value instead using `$element.val()`.'
      );
    }

    var data = [];

    this.dataAdapter.current(function (currentData) {
      data = currentData;
    });

    return data;
  };

  Select2.prototype.val = function (args) {
    if (this.options.get('debug') && window.console && console.warn) {
      console.warn(
        'Select2: The `select2("val")` method has been deprecated and will be' +
        ' removed in later Select2 versions. Use $element.val() instead.'
      );
    }

    if (args == null || args.length === 0) {
      return this.$element.val();
    }

    var newVal = args[0];

    if ($.isArray(newVal)) {
      newVal = $.map(newVal, function (obj) {
        return obj.toString();
      });
    }

    this.$element.val(newVal).trigger('change');
  };

  Select2.prototype.destroy = function () {
    this.$container.remove();

    if (this.$element[0].detachEvent) {
      this.$element[0].detachEvent('onpropertychange', this._syncA);
    }

    if (this._observer != null) {
      this._observer.disconnect();
      this._observer = null;
    } else if (this.$element[0].removeEventListener) {
      this.$element[0]
        .removeEventListener('DOMAttrModified', this._syncA, false);
      this.$element[0]
        .removeEventListener('DOMNodeInserted', this._syncS, false);
      this.$element[0]
        .removeEventListener('DOMNodeRemoved', this._syncS, false);
    }

    this._syncA = null;
    this._syncS = null;

    this.$element.off('.select2');
    this.$element.attr('tabindex', this.$element.data('old-tabindex'));

    this.$element.removeClass('select2-hidden-accessible');
    this.$element.attr('aria-hidden', 'false');
    this.$element.removeData('select2');

    this.dataAdapter.destroy();
    this.selection.destroy();
    this.dropdown.destroy();
    this.results.destroy();

    this.dataAdapter = null;
    this.selection = null;
    this.dropdown = null;
    this.results = null;
  };

  Select2.prototype.render = function () {
    var $container = $(
      '<span class="select2 select2-container">' +
        '<span class="selection"></span>' +
        '<span class="dropdown-wrapper" aria-hidden="true"></span>' +
      '</span>'
    );

    $container.attr('dir', this.options.get('dir'));

    this.$container = $container;

    this.$container.addClass('select2-container--' + this.options.get('theme'));

    $container.data('element', this.$element);

    return $container;
  };

  return Select2;
});

S2.define('jquery-mousewheel',[
  'jquery'
], function ($) {
  // Used to shim jQuery.mousewheel for non-full builds.
  return $;
});

S2.define('jquery.select2',[
  'jquery',
  'jquery-mousewheel',

  './select2/core',
  './select2/defaults'
], function ($, _, Select2, Defaults) {
  if ($.fn.select2 == null) {
    // All methods that should return the element
    var thisMethods = ['open', 'close', 'destroy'];

    $.fn.select2 = function (options) {
      options = options || {};

      if (typeof options === 'object') {
        this.each(function () {
          var instanceOptions = $.extend(true, {}, options);

          var instance = new Select2($(this), instanceOptions);
        });

        return this;
      } else if (typeof options === 'string') {
        var ret;
        var args = Array.prototype.slice.call(arguments, 1);

        this.each(function () {
          var instance = $(this).data('select2');

          if (instance == null && window.console && console.error) {
            console.error(
              'The select2(\'' + options + '\') method was called on an ' +
              'element that is not using Select2.'
            );
          }

          ret = instance[options].apply(instance, args);
        });

        // Check if we should be returning `this`
        if ($.inArray(options, thisMethods) > -1) {
          return this;
        }

        return ret;
      } else {
        throw new Error('Invalid arguments for Select2: ' + options);
      }
    };
  }

  if ($.fn.select2.defaults == null) {
    $.fn.select2.defaults = Defaults;
  }

  return Select2;
});

  // Return the AMD loader configuration so it can be used outside of this file
  return {
    define: S2.define,
    require: S2.require
  };
}());

  // Autoload the jQuery bindings
  // We know that all of the modules exist above this, so we're safe
  var select2 = S2.require('jquery.select2');

  // Hold the AMD module references on the jQuery function that was just loaded
  // This allows Select2 to use the internal loader outside of this file, such
  // as in the language files.
  jQuery.fn.select2.amd = S2;

  // Return the Select2 instance for anyone who is importing it.
  return select2;
}));
/*!
 * Chart.js
 * http://chartjs.org/
 * Version: 2.1.6
 *
 * Copyright 2016 Nick Downie
 * Released under the MIT license
 * https://github.com/chartjs/Chart.js/blob/master/LICENSE.md
 */

(function(f){if(typeof exports==="object"&&typeof module!=="undefined"){module.exports=f()}else if(typeof define==="function"&&define.amd){define([],f)}else{var g;if(typeof window!=="undefined"){g=window}else if(typeof global!=="undefined"){g=global}else if(typeof self!=="undefined"){g=self}else{g=this}g.Chart = f()}})(function(){var define,module,exports;return (function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){

},{}],2:[function(require,module,exports){
/* MIT license */
var colorNames = require(6);

module.exports = {
   getRgba: getRgba,
   getHsla: getHsla,
   getRgb: getRgb,
   getHsl: getHsl,
   getHwb: getHwb,
   getAlpha: getAlpha,

   hexString: hexString,
   rgbString: rgbString,
   rgbaString: rgbaString,
   percentString: percentString,
   percentaString: percentaString,
   hslString: hslString,
   hslaString: hslaString,
   hwbString: hwbString,
   keyword: keyword
}

function getRgba(string) {
   if (!string) {
      return;
   }
   var abbr =  /^#([a-fA-F0-9]{3})$/,
       hex =  /^#([a-fA-F0-9]{6})$/,
       rgba = /^rgba?\(\s*([+-]?\d+)\s*,\s*([+-]?\d+)\s*,\s*([+-]?\d+)\s*(?:,\s*([+-]?[\d\.]+)\s*)?\)$/,
       per = /^rgba?\(\s*([+-]?[\d\.]+)\%\s*,\s*([+-]?[\d\.]+)\%\s*,\s*([+-]?[\d\.]+)\%\s*(?:,\s*([+-]?[\d\.]+)\s*)?\)$/,
       keyword = /(\w+)/;

   var rgb = [0, 0, 0],
       a = 1,
       match = string.match(abbr);
   if (match) {
      match = match[1];
      for (var i = 0; i < rgb.length; i++) {
         rgb[i] = parseInt(match[i] + match[i], 16);
      }
   }
   else if (match = string.match(hex)) {
      match = match[1];
      for (var i = 0; i < rgb.length; i++) {
         rgb[i] = parseInt(match.slice(i * 2, i * 2 + 2), 16);
      }
   }
   else if (match = string.match(rgba)) {
      for (var i = 0; i < rgb.length; i++) {
         rgb[i] = parseInt(match[i + 1]);
      }
      a = parseFloat(match[4]);
   }
   else if (match = string.match(per)) {
      for (var i = 0; i < rgb.length; i++) {
         rgb[i] = Math.round(parseFloat(match[i + 1]) * 2.55);
      }
      a = parseFloat(match[4]);
   }
   else if (match = string.match(keyword)) {
      if (match[1] == "transparent") {
         return [0, 0, 0, 0];
      }
      rgb = colorNames[match[1]];
      if (!rgb) {
         return;
      }
   }

   for (var i = 0; i < rgb.length; i++) {
      rgb[i] = scale(rgb[i], 0, 255);
   }
   if (!a && a != 0) {
      a = 1;
   }
   else {
      a = scale(a, 0, 1);
   }
   rgb[3] = a;
   return rgb;
}

function getHsla(string) {
   if (!string) {
      return;
   }
   var hsl = /^hsla?\(\s*([+-]?\d+)(?:deg)?\s*,\s*([+-]?[\d\.]+)%\s*,\s*([+-]?[\d\.]+)%\s*(?:,\s*([+-]?[\d\.]+)\s*)?\)/;
   var match = string.match(hsl);
   if (match) {
      var alpha = parseFloat(match[4]);
      var h = scale(parseInt(match[1]), 0, 360),
          s = scale(parseFloat(match[2]), 0, 100),
          l = scale(parseFloat(match[3]), 0, 100),
          a = scale(isNaN(alpha) ? 1 : alpha, 0, 1);
      return [h, s, l, a];
   }
}

function getHwb(string) {
   if (!string) {
      return;
   }
   var hwb = /^hwb\(\s*([+-]?\d+)(?:deg)?\s*,\s*([+-]?[\d\.]+)%\s*,\s*([+-]?[\d\.]+)%\s*(?:,\s*([+-]?[\d\.]+)\s*)?\)/;
   var match = string.match(hwb);
   if (match) {
    var alpha = parseFloat(match[4]);
      var h = scale(parseInt(match[1]), 0, 360),
          w = scale(parseFloat(match[2]), 0, 100),
          b = scale(parseFloat(match[3]), 0, 100),
          a = scale(isNaN(alpha) ? 1 : alpha, 0, 1);
      return [h, w, b, a];
   }
}

function getRgb(string) {
   var rgba = getRgba(string);
   return rgba && rgba.slice(0, 3);
}

function getHsl(string) {
  var hsla = getHsla(string);
  return hsla && hsla.slice(0, 3);
}

function getAlpha(string) {
   var vals = getRgba(string);
   if (vals) {
      return vals[3];
   }
   else if (vals = getHsla(string)) {
      return vals[3];
   }
   else if (vals = getHwb(string)) {
      return vals[3];
   }
}

// generators
function hexString(rgb) {
   return "#" + hexDouble(rgb[0]) + hexDouble(rgb[1])
              + hexDouble(rgb[2]);
}

function rgbString(rgba, alpha) {
   if (alpha < 1 || (rgba[3] && rgba[3] < 1)) {
      return rgbaString(rgba, alpha);
   }
   return "rgb(" + rgba[0] + ", " + rgba[1] + ", " + rgba[2] + ")";
}

function rgbaString(rgba, alpha) {
   if (alpha === undefined) {
      alpha = (rgba[3] !== undefined ? rgba[3] : 1);
   }
   return "rgba(" + rgba[0] + ", " + rgba[1] + ", " + rgba[2]
           + ", " + alpha + ")";
}

function percentString(rgba, alpha) {
   if (alpha < 1 || (rgba[3] && rgba[3] < 1)) {
      return percentaString(rgba, alpha);
   }
   var r = Math.round(rgba[0]/255 * 100),
       g = Math.round(rgba[1]/255 * 100),
       b = Math.round(rgba[2]/255 * 100);

   return "rgb(" + r + "%, " + g + "%, " + b + "%)";
}

function percentaString(rgba, alpha) {
   var r = Math.round(rgba[0]/255 * 100),
       g = Math.round(rgba[1]/255 * 100),
       b = Math.round(rgba[2]/255 * 100);
   return "rgba(" + r + "%, " + g + "%, " + b + "%, " + (alpha || rgba[3] || 1) + ")";
}

function hslString(hsla, alpha) {
   if (alpha < 1 || (hsla[3] && hsla[3] < 1)) {
      return hslaString(hsla, alpha);
   }
   return "hsl(" + hsla[0] + ", " + hsla[1] + "%, " + hsla[2] + "%)";
}

function hslaString(hsla, alpha) {
   if (alpha === undefined) {
      alpha = (hsla[3] !== undefined ? hsla[3] : 1);
   }
   return "hsla(" + hsla[0] + ", " + hsla[1] + "%, " + hsla[2] + "%, "
           + alpha + ")";
}

// hwb is a bit different than rgb(a) & hsl(a) since there is no alpha specific syntax
// (hwb have alpha optional & 1 is default value)
function hwbString(hwb, alpha) {
   if (alpha === undefined) {
      alpha = (hwb[3] !== undefined ? hwb[3] : 1);
   }
   return "hwb(" + hwb[0] + ", " + hwb[1] + "%, " + hwb[2] + "%"
           + (alpha !== undefined && alpha !== 1 ? ", " + alpha : "") + ")";
}

function keyword(rgb) {
  return reverseNames[rgb.slice(0, 3)];
}

// helpers
function scale(num, min, max) {
   return Math.min(Math.max(min, num), max);
}

function hexDouble(num) {
  var str = num.toString(16).toUpperCase();
  return (str.length < 2) ? "0" + str : str;
}


//create a list of reverse color names
var reverseNames = {};
for (var name in colorNames) {
   reverseNames[colorNames[name]] = name;
}

},{"6":6}],3:[function(require,module,exports){
/* MIT license */
var convert = require(5);
var string = require(2);

var Color = function (obj) {
	if (obj instanceof Color) {
		return obj;
	}
	if (!(this instanceof Color)) {
		return new Color(obj);
	}

	this.values = {
		rgb: [0, 0, 0],
		hsl: [0, 0, 0],
		hsv: [0, 0, 0],
		hwb: [0, 0, 0],
		cmyk: [0, 0, 0, 0],
		alpha: 1
	};

	// parse Color() argument
	var vals;
	if (typeof obj === 'string') {
		vals = string.getRgba(obj);
		if (vals) {
			this.setValues('rgb', vals);
		} else if (vals = string.getHsla(obj)) {
			this.setValues('hsl', vals);
		} else if (vals = string.getHwb(obj)) {
			this.setValues('hwb', vals);
		} else {
			throw new Error('Unable to parse color from string "' + obj + '"');
		}
	} else if (typeof obj === 'object') {
		vals = obj;
		if (vals.r !== undefined || vals.red !== undefined) {
			this.setValues('rgb', vals);
		} else if (vals.l !== undefined || vals.lightness !== undefined) {
			this.setValues('hsl', vals);
		} else if (vals.v !== undefined || vals.value !== undefined) {
			this.setValues('hsv', vals);
		} else if (vals.w !== undefined || vals.whiteness !== undefined) {
			this.setValues('hwb', vals);
		} else if (vals.c !== undefined || vals.cyan !== undefined) {
			this.setValues('cmyk', vals);
		} else {
			throw new Error('Unable to parse color from object ' + JSON.stringify(obj));
		}
	}
};

Color.prototype = {
	rgb: function () {
		return this.setSpace('rgb', arguments);
	},
	hsl: function () {
		return this.setSpace('hsl', arguments);
	},
	hsv: function () {
		return this.setSpace('hsv', arguments);
	},
	hwb: function () {
		return this.setSpace('hwb', arguments);
	},
	cmyk: function () {
		return this.setSpace('cmyk', arguments);
	},

	rgbArray: function () {
		return this.values.rgb;
	},
	hslArray: function () {
		return this.values.hsl;
	},
	hsvArray: function () {
		return this.values.hsv;
	},
	hwbArray: function () {
		var values = this.values;
		if (values.alpha !== 1) {
			return values.hwb.concat([values.alpha]);
		}
		return values.hwb;
	},
	cmykArray: function () {
		return this.values.cmyk;
	},
	rgbaArray: function () {
		var values = this.values;
		return values.rgb.concat([values.alpha]);
	},
	hslaArray: function () {
		var values = this.values;
		return values.hsl.concat([values.alpha]);
	},
	alpha: function (val) {
		if (val === undefined) {
			return this.values.alpha;
		}
		this.setValues('alpha', val);
		return this;
	},

	red: function (val) {
		return this.setChannel('rgb', 0, val);
	},
	green: function (val) {
		return this.setChannel('rgb', 1, val);
	},
	blue: function (val) {
		return this.setChannel('rgb', 2, val);
	},
	hue: function (val) {
		if (val) {
			val %= 360;
			val = val < 0 ? 360 + val : val;
		}
		return this.setChannel('hsl', 0, val);
	},
	saturation: function (val) {
		return this.setChannel('hsl', 1, val);
	},
	lightness: function (val) {
		return this.setChannel('hsl', 2, val);
	},
	saturationv: function (val) {
		return this.setChannel('hsv', 1, val);
	},
	whiteness: function (val) {
		return this.setChannel('hwb', 1, val);
	},
	blackness: function (val) {
		return this.setChannel('hwb', 2, val);
	},
	value: function (val) {
		return this.setChannel('hsv', 2, val);
	},
	cyan: function (val) {
		return this.setChannel('cmyk', 0, val);
	},
	magenta: function (val) {
		return this.setChannel('cmyk', 1, val);
	},
	yellow: function (val) {
		return this.setChannel('cmyk', 2, val);
	},
	black: function (val) {
		return this.setChannel('cmyk', 3, val);
	},

	hexString: function () {
		return string.hexString(this.values.rgb);
	},
	rgbString: function () {
		return string.rgbString(this.values.rgb, this.values.alpha);
	},
	rgbaString: function () {
		return string.rgbaString(this.values.rgb, this.values.alpha);
	},
	percentString: function () {
		return string.percentString(this.values.rgb, this.values.alpha);
	},
	hslString: function () {
		return string.hslString(this.values.hsl, this.values.alpha);
	},
	hslaString: function () {
		return string.hslaString(this.values.hsl, this.values.alpha);
	},
	hwbString: function () {
		return string.hwbString(this.values.hwb, this.values.alpha);
	},
	keyword: function () {
		return string.keyword(this.values.rgb, this.values.alpha);
	},

	rgbNumber: function () {
		var rgb = this.values.rgb;
		return (rgb[0] << 16) | (rgb[1] << 8) | rgb[2];
	},

	luminosity: function () {
		// http://www.w3.org/TR/WCAG20/#relativeluminancedef
		var rgb = this.values.rgb;
		var lum = [];
		for (var i = 0; i < rgb.length; i++) {
			var chan = rgb[i] / 255;
			lum[i] = (chan <= 0.03928) ? chan / 12.92 : Math.pow(((chan + 0.055) / 1.055), 2.4);
		}
		return 0.2126 * lum[0] + 0.7152 * lum[1] + 0.0722 * lum[2];
	},

	contrast: function (color2) {
		// http://www.w3.org/TR/WCAG20/#contrast-ratiodef
		var lum1 = this.luminosity();
		var lum2 = color2.luminosity();
		if (lum1 > lum2) {
			return (lum1 + 0.05) / (lum2 + 0.05);
		}
		return (lum2 + 0.05) / (lum1 + 0.05);
	},

	level: function (color2) {
		var contrastRatio = this.contrast(color2);
		if (contrastRatio >= 7.1) {
			return 'AAA';
		}

		return (contrastRatio >= 4.5) ? 'AA' : '';
	},

	dark: function () {
		// YIQ equation from http://24ways.org/2010/calculating-color-contrast
		var rgb = this.values.rgb;
		var yiq = (rgb[0] * 299 + rgb[1] * 587 + rgb[2] * 114) / 1000;
		return yiq < 128;
	},

	light: function () {
		return !this.dark();
	},

	negate: function () {
		var rgb = [];
		for (var i = 0; i < 3; i++) {
			rgb[i] = 255 - this.values.rgb[i];
		}
		this.setValues('rgb', rgb);
		return this;
	},

	lighten: function (ratio) {
		var hsl = this.values.hsl;
		hsl[2] += hsl[2] * ratio;
		this.setValues('hsl', hsl);
		return this;
	},

	darken: function (ratio) {
		var hsl = this.values.hsl;
		hsl[2] -= hsl[2] * ratio;
		this.setValues('hsl', hsl);
		return this;
	},

	saturate: function (ratio) {
		var hsl = this.values.hsl;
		hsl[1] += hsl[1] * ratio;
		this.setValues('hsl', hsl);
		return this;
	},

	desaturate: function (ratio) {
		var hsl = this.values.hsl;
		hsl[1] -= hsl[1] * ratio;
		this.setValues('hsl', hsl);
		return this;
	},

	whiten: function (ratio) {
		var hwb = this.values.hwb;
		hwb[1] += hwb[1] * ratio;
		this.setValues('hwb', hwb);
		return this;
	},

	blacken: function (ratio) {
		var hwb = this.values.hwb;
		hwb[2] += hwb[2] * ratio;
		this.setValues('hwb', hwb);
		return this;
	},

	greyscale: function () {
		var rgb = this.values.rgb;
		// http://en.wikipedia.org/wiki/Grayscale#Converting_color_to_grayscale
		var val = rgb[0] * 0.3 + rgb[1] * 0.59 + rgb[2] * 0.11;
		this.setValues('rgb', [val, val, val]);
		return this;
	},

	clearer: function (ratio) {
		var alpha = this.values.alpha;
		this.setValues('alpha', alpha - (alpha * ratio));
		return this;
	},

	opaquer: function (ratio) {
		var alpha = this.values.alpha;
		this.setValues('alpha', alpha + (alpha * ratio));
		return this;
	},

	rotate: function (degrees) {
		var hsl = this.values.hsl;
		var hue = (hsl[0] + degrees) % 360;
		hsl[0] = hue < 0 ? 360 + hue : hue;
		this.setValues('hsl', hsl);
		return this;
	},

	/**
	 * Ported from sass implementation in C
	 * https://github.com/sass/libsass/blob/0e6b4a2850092356aa3ece07c6b249f0221caced/functions.cpp#L209
	 */
	mix: function (mixinColor, weight) {
		var color1 = this;
		var color2 = mixinColor;
		var p = weight === undefined ? 0.5 : weight;

		var w = 2 * p - 1;
		var a = color1.alpha() - color2.alpha();

		var w1 = (((w * a === -1) ? w : (w + a) / (1 + w * a)) + 1) / 2.0;
		var w2 = 1 - w1;

		return this
			.rgb(
				w1 * color1.red() + w2 * color2.red(),
				w1 * color1.green() + w2 * color2.green(),
				w1 * color1.blue() + w2 * color2.blue()
			)
			.alpha(color1.alpha() * p + color2.alpha() * (1 - p));
	},

	toJSON: function () {
		return this.rgb();
	},

	clone: function () {
		// NOTE(SB): using node-clone creates a dependency to Buffer when using browserify,
		// making the final build way to big to embed in Chart.js. So let's do it manually,
		// assuming that values to clone are 1 dimension arrays containing only numbers,
		// except 'alpha' which is a number.
		var result = new Color();
		var source = this.values;
		var target = result.values;
		var value, type;

		for (var prop in source) {
			if (source.hasOwnProperty(prop)) {
				value = source[prop];
				type = ({}).toString.call(value);
				if (type === '[object Array]') {
					target[prop] = value.slice(0);
				} else if (type === '[object Number]') {
					target[prop] = value;
				} else {
					console.error('unexpected color value:', value);
				}
			}
		}

		return result;
	}
};

Color.prototype.spaces = {
	rgb: ['red', 'green', 'blue'],
	hsl: ['hue', 'saturation', 'lightness'],
	hsv: ['hue', 'saturation', 'value'],
	hwb: ['hue', 'whiteness', 'blackness'],
	cmyk: ['cyan', 'magenta', 'yellow', 'black']
};

Color.prototype.maxes = {
	rgb: [255, 255, 255],
	hsl: [360, 100, 100],
	hsv: [360, 100, 100],
	hwb: [360, 100, 100],
	cmyk: [100, 100, 100, 100]
};

Color.prototype.getValues = function (space) {
	var values = this.values;
	var vals = {};

	for (var i = 0; i < space.length; i++) {
		vals[space.charAt(i)] = values[space][i];
	}

	if (values.alpha !== 1) {
		vals.a = values.alpha;
	}

	// {r: 255, g: 255, b: 255, a: 0.4}
	return vals;
};

Color.prototype.setValues = function (space, vals) {
	var values = this.values;
	var spaces = this.spaces;
	var maxes = this.maxes;
	var alpha = 1;
	var i;

	if (space === 'alpha') {
		alpha = vals;
	} else if (vals.length) {
		// [10, 10, 10]
		values[space] = vals.slice(0, space.length);
		alpha = vals[space.length];
	} else if (vals[space.charAt(0)] !== undefined) {
		// {r: 10, g: 10, b: 10}
		for (i = 0; i < space.length; i++) {
			values[space][i] = vals[space.charAt(i)];
		}

		alpha = vals.a;
	} else if (vals[spaces[space][0]] !== undefined) {
		// {red: 10, green: 10, blue: 10}
		var chans = spaces[space];

		for (i = 0; i < space.length; i++) {
			values[space][i] = vals[chans[i]];
		}

		alpha = vals.alpha;
	}

	values.alpha = Math.max(0, Math.min(1, (alpha === undefined ? values.alpha : alpha)));

	if (space === 'alpha') {
		return false;
	}

	var capped;

	// cap values of the space prior converting all values
	for (i = 0; i < space.length; i++) {
		capped = Math.max(0, Math.min(maxes[space][i], values[space][i]));
		values[space][i] = Math.round(capped);
	}

	// convert to all the other color spaces
	for (var sname in spaces) {
		if (sname !== space) {
			values[sname] = convert[space][sname](values[space]);
		}
	}

	return true;
};

Color.prototype.setSpace = function (space, args) {
	var vals = args[0];

	if (vals === undefined) {
		// color.rgb()
		return this.getValues(space);
	}

	// color.rgb(10, 10, 10)
	if (typeof vals === 'number') {
		vals = Array.prototype.slice.call(args);
	}

	this.setValues(space, vals);
	return this;
};

Color.prototype.setChannel = function (space, index, val) {
	var svalues = this.values[space];
	if (val === undefined) {
		// color.red()
		return svalues[index];
	} else if (val === svalues[index]) {
		// color.red(color.red())
		return this;
	}

	// color.red(100)
	svalues[index] = val;
	this.setValues(space, svalues);

	return this;
};

if (typeof window !== 'undefined') {
	window.Color = Color;
}

module.exports = Color;

},{"2":2,"5":5}],4:[function(require,module,exports){
/* MIT license */

module.exports = {
  rgb2hsl: rgb2hsl,
  rgb2hsv: rgb2hsv,
  rgb2hwb: rgb2hwb,
  rgb2cmyk: rgb2cmyk,
  rgb2keyword: rgb2keyword,
  rgb2xyz: rgb2xyz,
  rgb2lab: rgb2lab,
  rgb2lch: rgb2lch,

  hsl2rgb: hsl2rgb,
  hsl2hsv: hsl2hsv,
  hsl2hwb: hsl2hwb,
  hsl2cmyk: hsl2cmyk,
  hsl2keyword: hsl2keyword,

  hsv2rgb: hsv2rgb,
  hsv2hsl: hsv2hsl,
  hsv2hwb: hsv2hwb,
  hsv2cmyk: hsv2cmyk,
  hsv2keyword: hsv2keyword,

  hwb2rgb: hwb2rgb,
  hwb2hsl: hwb2hsl,
  hwb2hsv: hwb2hsv,
  hwb2cmyk: hwb2cmyk,
  hwb2keyword: hwb2keyword,

  cmyk2rgb: cmyk2rgb,
  cmyk2hsl: cmyk2hsl,
  cmyk2hsv: cmyk2hsv,
  cmyk2hwb: cmyk2hwb,
  cmyk2keyword: cmyk2keyword,

  keyword2rgb: keyword2rgb,
  keyword2hsl: keyword2hsl,
  keyword2hsv: keyword2hsv,
  keyword2hwb: keyword2hwb,
  keyword2cmyk: keyword2cmyk,
  keyword2lab: keyword2lab,
  keyword2xyz: keyword2xyz,

  xyz2rgb: xyz2rgb,
  xyz2lab: xyz2lab,
  xyz2lch: xyz2lch,

  lab2xyz: lab2xyz,
  lab2rgb: lab2rgb,
  lab2lch: lab2lch,

  lch2lab: lch2lab,
  lch2xyz: lch2xyz,
  lch2rgb: lch2rgb
}


function rgb2hsl(rgb) {
  var r = rgb[0]/255,
      g = rgb[1]/255,
      b = rgb[2]/255,
      min = Math.min(r, g, b),
      max = Math.max(r, g, b),
      delta = max - min,
      h, s, l;

  if (max == min)
    h = 0;
  else if (r == max)
    h = (g - b) / delta;
  else if (g == max)
    h = 2 + (b - r) / delta;
  else if (b == max)
    h = 4 + (r - g)/ delta;

  h = Math.min(h * 60, 360);

  if (h < 0)
    h += 360;

  l = (min + max) / 2;

  if (max == min)
    s = 0;
  else if (l <= 0.5)
    s = delta / (max + min);
  else
    s = delta / (2 - max - min);

  return [h, s * 100, l * 100];
}

function rgb2hsv(rgb) {
  var r = rgb[0],
      g = rgb[1],
      b = rgb[2],
      min = Math.min(r, g, b),
      max = Math.max(r, g, b),
      delta = max - min,
      h, s, v;

  if (max == 0)
    s = 0;
  else
    s = (delta/max * 1000)/10;

  if (max == min)
    h = 0;
  else if (r == max)
    h = (g - b) / delta;
  else if (g == max)
    h = 2 + (b - r) / delta;
  else if (b == max)
    h = 4 + (r - g) / delta;

  h = Math.min(h * 60, 360);

  if (h < 0)
    h += 360;

  v = ((max / 255) * 1000) / 10;

  return [h, s, v];
}

function rgb2hwb(rgb) {
  var r = rgb[0],
      g = rgb[1],
      b = rgb[2],
      h = rgb2hsl(rgb)[0],
      w = 1/255 * Math.min(r, Math.min(g, b)),
      b = 1 - 1/255 * Math.max(r, Math.max(g, b));

  return [h, w * 100, b * 100];
}

function rgb2cmyk(rgb) {
  var r = rgb[0] / 255,
      g = rgb[1] / 255,
      b = rgb[2] / 255,
      c, m, y, k;

  k = Math.min(1 - r, 1 - g, 1 - b);
  c = (1 - r - k) / (1 - k) || 0;
  m = (1 - g - k) / (1 - k) || 0;
  y = (1 - b - k) / (1 - k) || 0;
  return [c * 100, m * 100, y * 100, k * 100];
}

function rgb2keyword(rgb) {
  return reverseKeywords[JSON.stringify(rgb)];
}

function rgb2xyz(rgb) {
  var r = rgb[0] / 255,
      g = rgb[1] / 255,
      b = rgb[2] / 255;

  // assume sRGB
  r = r > 0.04045 ? Math.pow(((r + 0.055) / 1.055), 2.4) : (r / 12.92);
  g = g > 0.04045 ? Math.pow(((g + 0.055) / 1.055), 2.4) : (g / 12.92);
  b = b > 0.04045 ? Math.pow(((b + 0.055) / 1.055), 2.4) : (b / 12.92);

  var x = (r * 0.4124) + (g * 0.3576) + (b * 0.1805);
  var y = (r * 0.2126) + (g * 0.7152) + (b * 0.0722);
  var z = (r * 0.0193) + (g * 0.1192) + (b * 0.9505);

  return [x * 100, y *100, z * 100];
}

function rgb2lab(rgb) {
  var xyz = rgb2xyz(rgb),
        x = xyz[0],
        y = xyz[1],
        z = xyz[2],
        l, a, b;

  x /= 95.047;
  y /= 100;
  z /= 108.883;

  x = x > 0.008856 ? Math.pow(x, 1/3) : (7.787 * x) + (16 / 116);
  y = y > 0.008856 ? Math.pow(y, 1/3) : (7.787 * y) + (16 / 116);
  z = z > 0.008856 ? Math.pow(z, 1/3) : (7.787 * z) + (16 / 116);

  l = (116 * y) - 16;
  a = 500 * (x - y);
  b = 200 * (y - z);

  return [l, a, b];
}

function rgb2lch(args) {
  return lab2lch(rgb2lab(args));
}

function hsl2rgb(hsl) {
  var h = hsl[0] / 360,
      s = hsl[1] / 100,
      l = hsl[2] / 100,
      t1, t2, t3, rgb, val;

  if (s == 0) {
    val = l * 255;
    return [val, val, val];
  }

  if (l < 0.5)
    t2 = l * (1 + s);
  else
    t2 = l + s - l * s;
  t1 = 2 * l - t2;

  rgb = [0, 0, 0];
  for (var i = 0; i < 3; i++) {
    t3 = h + 1 / 3 * - (i - 1);
    t3 < 0 && t3++;
    t3 > 1 && t3--;

    if (6 * t3 < 1)
      val = t1 + (t2 - t1) * 6 * t3;
    else if (2 * t3 < 1)
      val = t2;
    else if (3 * t3 < 2)
      val = t1 + (t2 - t1) * (2 / 3 - t3) * 6;
    else
      val = t1;

    rgb[i] = val * 255;
  }

  return rgb;
}

function hsl2hsv(hsl) {
  var h = hsl[0],
      s = hsl[1] / 100,
      l = hsl[2] / 100,
      sv, v;

  if(l === 0) {
      // no need to do calc on black
      // also avoids divide by 0 error
      return [0, 0, 0];
  }

  l *= 2;
  s *= (l <= 1) ? l : 2 - l;
  v = (l + s) / 2;
  sv = (2 * s) / (l + s);
  return [h, sv * 100, v * 100];
}

function hsl2hwb(args) {
  return rgb2hwb(hsl2rgb(args));
}

function hsl2cmyk(args) {
  return rgb2cmyk(hsl2rgb(args));
}

function hsl2keyword(args) {
  return rgb2keyword(hsl2rgb(args));
}


function hsv2rgb(hsv) {
  var h = hsv[0] / 60,
      s = hsv[1] / 100,
      v = hsv[2] / 100,
      hi = Math.floor(h) % 6;

  var f = h - Math.floor(h),
      p = 255 * v * (1 - s),
      q = 255 * v * (1 - (s * f)),
      t = 255 * v * (1 - (s * (1 - f))),
      v = 255 * v;

  switch(hi) {
    case 0:
      return [v, t, p];
    case 1:
      return [q, v, p];
    case 2:
      return [p, v, t];
    case 3:
      return [p, q, v];
    case 4:
      return [t, p, v];
    case 5:
      return [v, p, q];
  }
}

function hsv2hsl(hsv) {
  var h = hsv[0],
      s = hsv[1] / 100,
      v = hsv[2] / 100,
      sl, l;

  l = (2 - s) * v;
  sl = s * v;
  sl /= (l <= 1) ? l : 2 - l;
  sl = sl || 0;
  l /= 2;
  return [h, sl * 100, l * 100];
}

function hsv2hwb(args) {
  return rgb2hwb(hsv2rgb(args))
}

function hsv2cmyk(args) {
  return rgb2cmyk(hsv2rgb(args));
}

function hsv2keyword(args) {
  return rgb2keyword(hsv2rgb(args));
}

// http://dev.w3.org/csswg/css-color/#hwb-to-rgb
function hwb2rgb(hwb) {
  var h = hwb[0] / 360,
      wh = hwb[1] / 100,
      bl = hwb[2] / 100,
      ratio = wh + bl,
      i, v, f, n;

  // wh + bl cant be > 1
  if (ratio > 1) {
    wh /= ratio;
    bl /= ratio;
  }

  i = Math.floor(6 * h);
  v = 1 - bl;
  f = 6 * h - i;
  if ((i & 0x01) != 0) {
    f = 1 - f;
  }
  n = wh + f * (v - wh);  // linear interpolation

  switch (i) {
    default:
    case 6:
    case 0: r = v; g = n; b = wh; break;
    case 1: r = n; g = v; b = wh; break;
    case 2: r = wh; g = v; b = n; break;
    case 3: r = wh; g = n; b = v; break;
    case 4: r = n; g = wh; b = v; break;
    case 5: r = v; g = wh; b = n; break;
  }

  return [r * 255, g * 255, b * 255];
}

function hwb2hsl(args) {
  return rgb2hsl(hwb2rgb(args));
}

function hwb2hsv(args) {
  return rgb2hsv(hwb2rgb(args));
}

function hwb2cmyk(args) {
  return rgb2cmyk(hwb2rgb(args));
}

function hwb2keyword(args) {
  return rgb2keyword(hwb2rgb(args));
}

function cmyk2rgb(cmyk) {
  var c = cmyk[0] / 100,
      m = cmyk[1] / 100,
      y = cmyk[2] / 100,
      k = cmyk[3] / 100,
      r, g, b;

  r = 1 - Math.min(1, c * (1 - k) + k);
  g = 1 - Math.min(1, m * (1 - k) + k);
  b = 1 - Math.min(1, y * (1 - k) + k);
  return [r * 255, g * 255, b * 255];
}

function cmyk2hsl(args) {
  return rgb2hsl(cmyk2rgb(args));
}

function cmyk2hsv(args) {
  return rgb2hsv(cmyk2rgb(args));
}

function cmyk2hwb(args) {
  return rgb2hwb(cmyk2rgb(args));
}

function cmyk2keyword(args) {
  return rgb2keyword(cmyk2rgb(args));
}


function xyz2rgb(xyz) {
  var x = xyz[0] / 100,
      y = xyz[1] / 100,
      z = xyz[2] / 100,
      r, g, b;

  r = (x * 3.2406) + (y * -1.5372) + (z * -0.4986);
  g = (x * -0.9689) + (y * 1.8758) + (z * 0.0415);
  b = (x * 0.0557) + (y * -0.2040) + (z * 1.0570);

  // assume sRGB
  r = r > 0.0031308 ? ((1.055 * Math.pow(r, 1.0 / 2.4)) - 0.055)
    : r = (r * 12.92);

  g = g > 0.0031308 ? ((1.055 * Math.pow(g, 1.0 / 2.4)) - 0.055)
    : g = (g * 12.92);

  b = b > 0.0031308 ? ((1.055 * Math.pow(b, 1.0 / 2.4)) - 0.055)
    : b = (b * 12.92);

  r = Math.min(Math.max(0, r), 1);
  g = Math.min(Math.max(0, g), 1);
  b = Math.min(Math.max(0, b), 1);

  return [r * 255, g * 255, b * 255];
}

function xyz2lab(xyz) {
  var x = xyz[0],
      y = xyz[1],
      z = xyz[2],
      l, a, b;

  x /= 95.047;
  y /= 100;
  z /= 108.883;

  x = x > 0.008856 ? Math.pow(x, 1/3) : (7.787 * x) + (16 / 116);
  y = y > 0.008856 ? Math.pow(y, 1/3) : (7.787 * y) + (16 / 116);
  z = z > 0.008856 ? Math.pow(z, 1/3) : (7.787 * z) + (16 / 116);

  l = (116 * y) - 16;
  a = 500 * (x - y);
  b = 200 * (y - z);

  return [l, a, b];
}

function xyz2lch(args) {
  return lab2lch(xyz2lab(args));
}

function lab2xyz(lab) {
  var l = lab[0],
      a = lab[1],
      b = lab[2],
      x, y, z, y2;

  if (l <= 8) {
    y = (l * 100) / 903.3;
    y2 = (7.787 * (y / 100)) + (16 / 116);
  } else {
    y = 100 * Math.pow((l + 16) / 116, 3);
    y2 = Math.pow(y / 100, 1/3);
  }

  x = x / 95.047 <= 0.008856 ? x = (95.047 * ((a / 500) + y2 - (16 / 116))) / 7.787 : 95.047 * Math.pow((a / 500) + y2, 3);

  z = z / 108.883 <= 0.008859 ? z = (108.883 * (y2 - (b / 200) - (16 / 116))) / 7.787 : 108.883 * Math.pow(y2 - (b / 200), 3);

  return [x, y, z];
}

function lab2lch(lab) {
  var l = lab[0],
      a = lab[1],
      b = lab[2],
      hr, h, c;

  hr = Math.atan2(b, a);
  h = hr * 360 / 2 / Math.PI;
  if (h < 0) {
    h += 360;
  }
  c = Math.sqrt(a * a + b * b);
  return [l, c, h];
}

function lab2rgb(args) {
  return xyz2rgb(lab2xyz(args));
}

function lch2lab(lch) {
  var l = lch[0],
      c = lch[1],
      h = lch[2],
      a, b, hr;

  hr = h / 360 * 2 * Math.PI;
  a = c * Math.cos(hr);
  b = c * Math.sin(hr);
  return [l, a, b];
}

function lch2xyz(args) {
  return lab2xyz(lch2lab(args));
}

function lch2rgb(args) {
  return lab2rgb(lch2lab(args));
}

function keyword2rgb(keyword) {
  return cssKeywords[keyword];
}

function keyword2hsl(args) {
  return rgb2hsl(keyword2rgb(args));
}

function keyword2hsv(args) {
  return rgb2hsv(keyword2rgb(args));
}

function keyword2hwb(args) {
  return rgb2hwb(keyword2rgb(args));
}

function keyword2cmyk(args) {
  return rgb2cmyk(keyword2rgb(args));
}

function keyword2lab(args) {
  return rgb2lab(keyword2rgb(args));
}

function keyword2xyz(args) {
  return rgb2xyz(keyword2rgb(args));
}

var cssKeywords = {
  aliceblue:  [240,248,255],
  antiquewhite: [250,235,215],
  aqua: [0,255,255],
  aquamarine: [127,255,212],
  azure:  [240,255,255],
  beige:  [245,245,220],
  bisque: [255,228,196],
  black:  [0,0,0],
  blanchedalmond: [255,235,205],
  blue: [0,0,255],
  blueviolet: [138,43,226],
  brown:  [165,42,42],
  burlywood:  [222,184,135],
  cadetblue:  [95,158,160],
  chartreuse: [127,255,0],
  chocolate:  [210,105,30],
  coral:  [255,127,80],
  cornflowerblue: [100,149,237],
  cornsilk: [255,248,220],
  crimson:  [220,20,60],
  cyan: [0,255,255],
  darkblue: [0,0,139],
  darkcyan: [0,139,139],
  darkgoldenrod:  [184,134,11],
  darkgray: [169,169,169],
  darkgreen:  [0,100,0],
  darkgrey: [169,169,169],
  darkkhaki:  [189,183,107],
  darkmagenta:  [139,0,139],
  darkolivegreen: [85,107,47],
  darkorange: [255,140,0],
  darkorchid: [153,50,204],
  darkred:  [139,0,0],
  darksalmon: [233,150,122],
  darkseagreen: [143,188,143],
  darkslateblue:  [72,61,139],
  darkslategray:  [47,79,79],
  darkslategrey:  [47,79,79],
  darkturquoise:  [0,206,209],
  darkviolet: [148,0,211],
  deeppink: [255,20,147],
  deepskyblue:  [0,191,255],
  dimgray:  [105,105,105],
  dimgrey:  [105,105,105],
  dodgerblue: [30,144,255],
  firebrick:  [178,34,34],
  floralwhite:  [255,250,240],
  forestgreen:  [34,139,34],
  fuchsia:  [255,0,255],
  gainsboro:  [220,220,220],
  ghostwhite: [248,248,255],
  gold: [255,215,0],
  goldenrod:  [218,165,32],
  gray: [128,128,128],
  green:  [0,128,0],
  greenyellow:  [173,255,47],
  grey: [128,128,128],
  honeydew: [240,255,240],
  hotpink:  [255,105,180],
  indianred:  [205,92,92],
  indigo: [75,0,130],
  ivory:  [255,255,240],
  khaki:  [240,230,140],
  lavender: [230,230,250],
  lavenderblush:  [255,240,245],
  lawngreen:  [124,252,0],
  lemonchiffon: [255,250,205],
  lightblue:  [173,216,230],
  lightcoral: [240,128,128],
  lightcyan:  [224,255,255],
  lightgoldenrodyellow: [250,250,210],
  lightgray:  [211,211,211],
  lightgreen: [144,238,144],
  lightgrey:  [211,211,211],
  lightpink:  [255,182,193],
  lightsalmon:  [255,160,122],
  lightseagreen:  [32,178,170],
  lightskyblue: [135,206,250],
  lightslategray: [119,136,153],
  lightslategrey: [119,136,153],
  lightsteelblue: [176,196,222],
  lightyellow:  [255,255,224],
  lime: [0,255,0],
  limegreen:  [50,205,50],
  linen:  [250,240,230],
  magenta:  [255,0,255],
  maroon: [128,0,0],
  mediumaquamarine: [102,205,170],
  mediumblue: [0,0,205],
  mediumorchid: [186,85,211],
  mediumpurple: [147,112,219],
  mediumseagreen: [60,179,113],
  mediumslateblue:  [123,104,238],
  mediumspringgreen:  [0,250,154],
  mediumturquoise:  [72,209,204],
  mediumvioletred:  [199,21,133],
  midnightblue: [25,25,112],
  mintcream:  [245,255,250],
  mistyrose:  [255,228,225],
  moccasin: [255,228,181],
  navajowhite:  [255,222,173],
  navy: [0,0,128],
  oldlace:  [253,245,230],
  olive:  [128,128,0],
  olivedrab:  [107,142,35],
  orange: [255,165,0],
  orangered:  [255,69,0],
  orchid: [218,112,214],
  palegoldenrod:  [238,232,170],
  palegreen:  [152,251,152],
  paleturquoise:  [175,238,238],
  palevioletred:  [219,112,147],
  papayawhip: [255,239,213],
  peachpuff:  [255,218,185],
  peru: [205,133,63],
  pink: [255,192,203],
  plum: [221,160,221],
  powderblue: [176,224,230],
  purple: [128,0,128],
  rebeccapurple: [102, 51, 153],
  red:  [255,0,0],
  rosybrown:  [188,143,143],
  royalblue:  [65,105,225],
  saddlebrown:  [139,69,19],
  salmon: [250,128,114],
  sandybrown: [244,164,96],
  seagreen: [46,139,87],
  seashell: [255,245,238],
  sienna: [160,82,45],
  silver: [192,192,192],
  skyblue:  [135,206,235],
  slateblue:  [106,90,205],
  slategray:  [112,128,144],
  slategrey:  [112,128,144],
  snow: [255,250,250],
  springgreen:  [0,255,127],
  steelblue:  [70,130,180],
  tan:  [210,180,140],
  teal: [0,128,128],
  thistle:  [216,191,216],
  tomato: [255,99,71],
  turquoise:  [64,224,208],
  violet: [238,130,238],
  wheat:  [245,222,179],
  white:  [255,255,255],
  whitesmoke: [245,245,245],
  yellow: [255,255,0],
  yellowgreen:  [154,205,50]
};

var reverseKeywords = {};
for (var key in cssKeywords) {
  reverseKeywords[JSON.stringify(cssKeywords[key])] = key;
}

},{}],5:[function(require,module,exports){
var conversions = require(4);

var convert = function() {
   return new Converter();
}

for (var func in conversions) {
  // export Raw versions
  convert[func + "Raw"] =  (function(func) {
    // accept array or plain args
    return function(arg) {
      if (typeof arg == "number")
        arg = Array.prototype.slice.call(arguments);
      return conversions[func](arg);
    }
  })(func);

  var pair = /(\w+)2(\w+)/.exec(func),
      from = pair[1],
      to = pair[2];

  // export rgb2hsl and ["rgb"]["hsl"]
  convert[from] = convert[from] || {};

  convert[from][to] = convert[func] = (function(func) { 
    return function(arg) {
      if (typeof arg == "number")
        arg = Array.prototype.slice.call(arguments);
      
      var val = conversions[func](arg);
      if (typeof val == "string" || val === undefined)
        return val; // keyword

      for (var i = 0; i < val.length; i++)
        val[i] = Math.round(val[i]);
      return val;
    }
  })(func);
}


/* Converter does lazy conversion and caching */
var Converter = function() {
   this.convs = {};
};

/* Either get the values for a space or
  set the values for a space, depending on args */
Converter.prototype.routeSpace = function(space, args) {
   var values = args[0];
   if (values === undefined) {
      // color.rgb()
      return this.getValues(space);
   }
   // color.rgb(10, 10, 10)
   if (typeof values == "number") {
      values = Array.prototype.slice.call(args);        
   }

   return this.setValues(space, values);
};
  
/* Set the values for a space, invalidating cache */
Converter.prototype.setValues = function(space, values) {
   this.space = space;
   this.convs = {};
   this.convs[space] = values;
   return this;
};

/* Get the values for a space. If there's already
  a conversion for the space, fetch it, otherwise
  compute it */
Converter.prototype.getValues = function(space) {
   var vals = this.convs[space];
   if (!vals) {
      var fspace = this.space,
          from = this.convs[fspace];
      vals = convert[fspace][space](from);

      this.convs[space] = vals;
   }
  return vals;
};

["rgb", "hsl", "hsv", "cmyk", "keyword"].forEach(function(space) {
   Converter.prototype[space] = function(vals) {
      return this.routeSpace(space, arguments);
   }
});

module.exports = convert;
},{"4":4}],6:[function(require,module,exports){
module.exports = {
	"aliceblue": [240, 248, 255],
	"antiquewhite": [250, 235, 215],
	"aqua": [0, 255, 255],
	"aquamarine": [127, 255, 212],
	"azure": [240, 255, 255],
	"beige": [245, 245, 220],
	"bisque": [255, 228, 196],
	"black": [0, 0, 0],
	"blanchedalmond": [255, 235, 205],
	"blue": [0, 0, 255],
	"blueviolet": [138, 43, 226],
	"brown": [165, 42, 42],
	"burlywood": [222, 184, 135],
	"cadetblue": [95, 158, 160],
	"chartreuse": [127, 255, 0],
	"chocolate": [210, 105, 30],
	"coral": [255, 127, 80],
	"cornflowerblue": [100, 149, 237],
	"cornsilk": [255, 248, 220],
	"crimson": [220, 20, 60],
	"cyan": [0, 255, 255],
	"darkblue": [0, 0, 139],
	"darkcyan": [0, 139, 139],
	"darkgoldenrod": [184, 134, 11],
	"darkgray": [169, 169, 169],
	"darkgreen": [0, 100, 0],
	"darkgrey": [169, 169, 169],
	"darkkhaki": [189, 183, 107],
	"darkmagenta": [139, 0, 139],
	"darkolivegreen": [85, 107, 47],
	"darkorange": [255, 140, 0],
	"darkorchid": [153, 50, 204],
	"darkred": [139, 0, 0],
	"darksalmon": [233, 150, 122],
	"darkseagreen": [143, 188, 143],
	"darkslateblue": [72, 61, 139],
	"darkslategray": [47, 79, 79],
	"darkslategrey": [47, 79, 79],
	"darkturquoise": [0, 206, 209],
	"darkviolet": [148, 0, 211],
	"deeppink": [255, 20, 147],
	"deepskyblue": [0, 191, 255],
	"dimgray": [105, 105, 105],
	"dimgrey": [105, 105, 105],
	"dodgerblue": [30, 144, 255],
	"firebrick": [178, 34, 34],
	"floralwhite": [255, 250, 240],
	"forestgreen": [34, 139, 34],
	"fuchsia": [255, 0, 255],
	"gainsboro": [220, 220, 220],
	"ghostwhite": [248, 248, 255],
	"gold": [255, 215, 0],
	"goldenrod": [218, 165, 32],
	"gray": [128, 128, 128],
	"green": [0, 128, 0],
	"greenyellow": [173, 255, 47],
	"grey": [128, 128, 128],
	"honeydew": [240, 255, 240],
	"hotpink": [255, 105, 180],
	"indianred": [205, 92, 92],
	"indigo": [75, 0, 130],
	"ivory": [255, 255, 240],
	"khaki": [240, 230, 140],
	"lavender": [230, 230, 250],
	"lavenderblush": [255, 240, 245],
	"lawngreen": [124, 252, 0],
	"lemonchiffon": [255, 250, 205],
	"lightblue": [173, 216, 230],
	"lightcoral": [240, 128, 128],
	"lightcyan": [224, 255, 255],
	"lightgoldenrodyellow": [250, 250, 210],
	"lightgray": [211, 211, 211],
	"lightgreen": [144, 238, 144],
	"lightgrey": [211, 211, 211],
	"lightpink": [255, 182, 193],
	"lightsalmon": [255, 160, 122],
	"lightseagreen": [32, 178, 170],
	"lightskyblue": [135, 206, 250],
	"lightslategray": [119, 136, 153],
	"lightslategrey": [119, 136, 153],
	"lightsteelblue": [176, 196, 222],
	"lightyellow": [255, 255, 224],
	"lime": [0, 255, 0],
	"limegreen": [50, 205, 50],
	"linen": [250, 240, 230],
	"magenta": [255, 0, 255],
	"maroon": [128, 0, 0],
	"mediumaquamarine": [102, 205, 170],
	"mediumblue": [0, 0, 205],
	"mediumorchid": [186, 85, 211],
	"mediumpurple": [147, 112, 219],
	"mediumseagreen": [60, 179, 113],
	"mediumslateblue": [123, 104, 238],
	"mediumspringgreen": [0, 250, 154],
	"mediumturquoise": [72, 209, 204],
	"mediumvioletred": [199, 21, 133],
	"midnightblue": [25, 25, 112],
	"mintcream": [245, 255, 250],
	"mistyrose": [255, 228, 225],
	"moccasin": [255, 228, 181],
	"navajowhite": [255, 222, 173],
	"navy": [0, 0, 128],
	"oldlace": [253, 245, 230],
	"olive": [128, 128, 0],
	"olivedrab": [107, 142, 35],
	"orange": [255, 165, 0],
	"orangered": [255, 69, 0],
	"orchid": [218, 112, 214],
	"palegoldenrod": [238, 232, 170],
	"palegreen": [152, 251, 152],
	"paleturquoise": [175, 238, 238],
	"palevioletred": [219, 112, 147],
	"papayawhip": [255, 239, 213],
	"peachpuff": [255, 218, 185],
	"peru": [205, 133, 63],
	"pink": [255, 192, 203],
	"plum": [221, 160, 221],
	"powderblue": [176, 224, 230],
	"purple": [128, 0, 128],
	"rebeccapurple": [102, 51, 153],
	"red": [255, 0, 0],
	"rosybrown": [188, 143, 143],
	"royalblue": [65, 105, 225],
	"saddlebrown": [139, 69, 19],
	"salmon": [250, 128, 114],
	"sandybrown": [244, 164, 96],
	"seagreen": [46, 139, 87],
	"seashell": [255, 245, 238],
	"sienna": [160, 82, 45],
	"silver": [192, 192, 192],
	"skyblue": [135, 206, 235],
	"slateblue": [106, 90, 205],
	"slategray": [112, 128, 144],
	"slategrey": [112, 128, 144],
	"snow": [255, 250, 250],
	"springgreen": [0, 255, 127],
	"steelblue": [70, 130, 180],
	"tan": [210, 180, 140],
	"teal": [0, 128, 128],
	"thistle": [216, 191, 216],
	"tomato": [255, 99, 71],
	"turquoise": [64, 224, 208],
	"violet": [238, 130, 238],
	"wheat": [245, 222, 179],
	"white": [255, 255, 255],
	"whitesmoke": [245, 245, 245],
	"yellow": [255, 255, 0],
	"yellowgreen": [154, 205, 50]
};
},{}],7:[function(require,module,exports){
/**
 * @namespace Chart
 */
var Chart = require(26)();

require(25)(Chart);
require(24)(Chart);
require(21)(Chart);
require(22)(Chart);
require(23)(Chart);
require(27)(Chart);
require(31)(Chart);
require(29)(Chart);
require(30)(Chart);
require(32)(Chart);
require(28)(Chart);
require(33)(Chart);

require(34)(Chart);
require(35)(Chart);
require(36)(Chart);
require(37)(Chart);

require(40)(Chart);
require(38)(Chart);
require(39)(Chart);
require(41)(Chart);
require(42)(Chart);
require(43)(Chart);

// Controllers must be loaded after elements
// See Chart.core.datasetController.dataElementType
require(15)(Chart);
require(16)(Chart);
require(17)(Chart);
require(18)(Chart);
require(19)(Chart);
require(20)(Chart);

require(8)(Chart);
require(9)(Chart);
require(10)(Chart);
require(11)(Chart);
require(12)(Chart);
require(13)(Chart);
require(14)(Chart);

window.Chart = module.exports = Chart;

},{"10":10,"11":11,"12":12,"13":13,"14":14,"15":15,"16":16,"17":17,"18":18,"19":19,"20":20,"21":21,"22":22,"23":23,"24":24,"25":25,"26":26,"27":27,"28":28,"29":29,"30":30,"31":31,"32":32,"33":33,"34":34,"35":35,"36":36,"37":37,"38":38,"39":39,"40":40,"41":41,"42":42,"43":43,"8":8,"9":9}],8:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	Chart.Bar = function(context, config) {
		config.type = 'bar';

		return new Chart(context, config);
	};

};
},{}],9:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	Chart.Bubble = function(context, config) {
		config.type = 'bubble';
		return new Chart(context, config);
	};

};
},{}],10:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	Chart.Doughnut = function(context, config) {
		config.type = 'doughnut';

		return new Chart(context, config);
	};

};
},{}],11:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	Chart.Line = function(context, config) {
		config.type = 'line';

		return new Chart(context, config);
	};

};
},{}],12:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	Chart.PolarArea = function(context, config) {
		config.type = 'polarArea';

		return new Chart(context, config);
	};

};
},{}],13:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {
	
	Chart.Radar = function(context, config) {
		config.options = Chart.helpers.configMerge({ aspectRatio: 1 }, config.options);
		config.type = 'radar';

		return new Chart(context, config);
	};

};

},{}],14:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var defaultConfig = {
		hover: {
			mode: 'single'
		},

		scales: {
			xAxes: [{
				type: "linear", // scatter should not use a category axis
				position: "bottom",
				id: "x-axis-1" // need an ID so datasets can reference the scale
			}],
			yAxes: [{
				type: "linear",
				position: "left",
				id: "y-axis-1"
			}]
		},

		tooltips: {
			callbacks: {
				title: function(tooltipItems, data) {
					// Title doesn't make sense for scatter since we format the data as a point
					return '';
				},
				label: function(tooltipItem, data) {
					return '(' + tooltipItem.xLabel + ', ' + tooltipItem.yLabel + ')';
				}
			}
		}
	};

	// Register the default config for this type
	Chart.defaults.scatter = defaultConfig;

	// Scatter charts use line controllers
	Chart.controllers.scatter = Chart.controllers.line;

	Chart.Scatter = function(context, config) {
		config.type = 'scatter';
		return new Chart(context, config);
	};

};
},{}],15:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.bar = {
		hover: {
			mode: "label"
		},

		scales: {
			xAxes: [{
				type: "category",

				// Specific to Bar Controller
				categoryPercentage: 0.8,
				barPercentage: 0.9,

				// grid line settings
				gridLines: {
					offsetGridLines: true
				}
			}],
			yAxes: [{
				type: "linear"
			}]
		}
	};

	Chart.controllers.bar = Chart.DatasetController.extend({

		dataElementType: Chart.elements.Rectangle,

		initialize: function(chart, datasetIndex) {
			Chart.DatasetController.prototype.initialize.call(this, chart, datasetIndex);

			// Use this to indicate that this is a bar dataset.
			this.getMeta().bar = true;
		},

		// Get the number of datasets that display bars. We use this to correctly calculate the bar width
		getBarCount: function getBarCount() {
			var me = this;
			var barCount = 0;
			helpers.each(me.chart.data.datasets, function(dataset, datasetIndex) {
				var meta = me.chart.getDatasetMeta(datasetIndex);
				if (meta.bar && me.chart.isDatasetVisible(datasetIndex)) {
					++barCount;
				}
			}, me);
			return barCount;
		},

		update: function update(reset) {
			var me = this;
			helpers.each(me.getMeta().data, function(rectangle, index) {
				me.updateElement(rectangle, index, reset);
			}, me);
		},

		updateElement: function updateElement(rectangle, index, reset) {
			var me = this;
			var meta = me.getMeta();
			var xScale = me.getScaleForId(meta.xAxisID);
			var yScale = me.getScaleForId(meta.yAxisID);
			var scaleBase = yScale.getBasePixel();
			var rectangleElementOptions = me.chart.options.elements.rectangle;
			var custom = rectangle.custom || {};
			var dataset = me.getDataset();

			helpers.extend(rectangle, {
				// Utility
				_xScale: xScale,
				_yScale: yScale,
				_datasetIndex: me.index,
				_index: index,

				// Desired view properties
				_model: {
					x: me.calculateBarX(index, me.index),
					y: reset ? scaleBase : me.calculateBarY(index, me.index),

					// Tooltip
					label: me.chart.data.labels[index],
					datasetLabel: dataset.label,

					// Appearance
					base: reset ? scaleBase : me.calculateBarBase(me.index, index),
					width: me.calculateBarWidth(index),
					backgroundColor: custom.backgroundColor ? custom.backgroundColor : helpers.getValueAtIndexOrDefault(dataset.backgroundColor, index, rectangleElementOptions.backgroundColor),
					borderSkipped: custom.borderSkipped ? custom.borderSkipped : rectangleElementOptions.borderSkipped,
					borderColor: custom.borderColor ? custom.borderColor : helpers.getValueAtIndexOrDefault(dataset.borderColor, index, rectangleElementOptions.borderColor),
					borderWidth: custom.borderWidth ? custom.borderWidth : helpers.getValueAtIndexOrDefault(dataset.borderWidth, index, rectangleElementOptions.borderWidth)
				}
			});
			rectangle.pivot();
		},

		calculateBarBase: function(datasetIndex, index) {
			var me = this;
			var meta = me.getMeta();
			var yScale = me.getScaleForId(meta.yAxisID);
			var base = 0;

			if (yScale.options.stacked) {
				var chart = me.chart;
				var datasets = chart.data.datasets;
				var value = datasets[datasetIndex].data[index];

				if (value < 0) {
					for (var i = 0; i < datasetIndex; i++) {
						var negDS = datasets[i];
						var negDSMeta = chart.getDatasetMeta(i);
						if (negDSMeta.bar && negDSMeta.yAxisID === yScale.id && chart.isDatasetVisible(i)) {
							base += negDS.data[index] < 0 ? negDS.data[index] : 0;
						}
					}
				} else {
					for (var j = 0; j < datasetIndex; j++) {
						var posDS = datasets[j];
						var posDSMeta = chart.getDatasetMeta(j);
						if (posDSMeta.bar && posDSMeta.yAxisID === yScale.id && chart.isDatasetVisible(j)) {
							base += posDS.data[index] > 0 ? posDS.data[index] : 0;
						}
					}
				}

				return yScale.getPixelForValue(base);
			}

			return yScale.getBasePixel();
		},

		getRuler: function(index) {
			var me = this;
			var meta = me.getMeta();
			var xScale = me.getScaleForId(meta.xAxisID);
			var datasetCount = me.getBarCount();

			var tickWidth;

			if (xScale.options.type === 'category') {
				tickWidth = xScale.getPixelForTick(index + 1) - xScale.getPixelForTick(index);
			} else {
				// Average width
				tickWidth = xScale.width / xScale.ticks.length;
			}
			var categoryWidth = tickWidth * xScale.options.categoryPercentage;
			var categorySpacing = (tickWidth - (tickWidth * xScale.options.categoryPercentage)) / 2;
			var fullBarWidth = categoryWidth / datasetCount;

			if (xScale.ticks.length !== me.chart.data.labels.length) {
			    var perc = xScale.ticks.length / me.chart.data.labels.length;
			    fullBarWidth = fullBarWidth * perc;
			}

			var barWidth = fullBarWidth * xScale.options.barPercentage;
			var barSpacing = fullBarWidth - (fullBarWidth * xScale.options.barPercentage);

			return {
				datasetCount: datasetCount,
				tickWidth: tickWidth,
				categoryWidth: categoryWidth,
				categorySpacing: categorySpacing,
				fullBarWidth: fullBarWidth,
				barWidth: barWidth,
				barSpacing: barSpacing
			};
		},

		calculateBarWidth: function(index) {
			var xScale = this.getScaleForId(this.getMeta().xAxisID);
			var ruler = this.getRuler(index);
			return xScale.options.stacked ? ruler.categoryWidth : ruler.barWidth;
		},

		// Get bar index from the given dataset index accounting for the fact that not all bars are visible
		getBarIndex: function(datasetIndex) {
			var barIndex = 0;
			var meta, j;

			for (j = 0; j < datasetIndex; ++j) {
				meta = this.chart.getDatasetMeta(j);
				if (meta.bar && this.chart.isDatasetVisible(j)) {
					++barIndex;
				}
			}

			return barIndex;
		},

		calculateBarX: function(index, datasetIndex) {
			var me = this;
			var meta = me.getMeta();
			var xScale = me.getScaleForId(meta.xAxisID);
			var barIndex = me.getBarIndex(datasetIndex);

			var ruler = me.getRuler(index);
			var leftTick = xScale.getPixelForValue(null, index, datasetIndex, me.chart.isCombo);
			leftTick -= me.chart.isCombo ? (ruler.tickWidth / 2) : 0;

			if (xScale.options.stacked) {
				return leftTick + (ruler.categoryWidth / 2) + ruler.categorySpacing;
			}

			return leftTick +
				(ruler.barWidth / 2) +
				ruler.categorySpacing +
				(ruler.barWidth * barIndex) +
				(ruler.barSpacing / 2) +
				(ruler.barSpacing * barIndex);
		},

		calculateBarY: function(index, datasetIndex) {
			var me = this;
			var meta = me.getMeta();
			var yScale = me.getScaleForId(meta.yAxisID);
			var value = me.getDataset().data[index];

			if (yScale.options.stacked) {

				var sumPos = 0,
					sumNeg = 0;

				for (var i = 0; i < datasetIndex; i++) {
					var ds = me.chart.data.datasets[i];
					var dsMeta = me.chart.getDatasetMeta(i);
					if (dsMeta.bar && dsMeta.yAxisID === yScale.id && me.chart.isDatasetVisible(i)) {
						if (ds.data[index] < 0) {
							sumNeg += ds.data[index] || 0;
						} else {
							sumPos += ds.data[index] || 0;
						}
					}
				}

				if (value < 0) {
					return yScale.getPixelForValue(sumNeg + value);
				} else {
					return yScale.getPixelForValue(sumPos + value);
				}
			}

			return yScale.getPixelForValue(value);
		},

		draw: function(ease) {
			var me = this;
			var easingDecimal = ease || 1;
			helpers.each(me.getMeta().data, function(rectangle, index) {
				var d = me.getDataset().data[index];
				if (d !== null && d !== undefined && !isNaN(d)) {
					rectangle.transition(easingDecimal).draw();
				}
			}, me);
		},

		setHoverStyle: function(rectangle) {
			var dataset = this.chart.data.datasets[rectangle._datasetIndex];
			var index = rectangle._index;

			var custom = rectangle.custom || {};
			var model = rectangle._model;
			model.backgroundColor = custom.hoverBackgroundColor ? custom.hoverBackgroundColor : helpers.getValueAtIndexOrDefault(dataset.hoverBackgroundColor, index, helpers.getHoverColor(model.backgroundColor));
			model.borderColor = custom.hoverBorderColor ? custom.hoverBorderColor : helpers.getValueAtIndexOrDefault(dataset.hoverBorderColor, index, helpers.getHoverColor(model.borderColor));
			model.borderWidth = custom.hoverBorderWidth ? custom.hoverBorderWidth : helpers.getValueAtIndexOrDefault(dataset.hoverBorderWidth, index, model.borderWidth);
		},

		removeHoverStyle: function(rectangle) {
			var dataset = this.chart.data.datasets[rectangle._datasetIndex];
			var index = rectangle._index;
			var custom = rectangle.custom || {};
			var model = rectangle._model;
			var rectangleElementOptions = this.chart.options.elements.rectangle;

			model.backgroundColor = custom.backgroundColor ? custom.backgroundColor : helpers.getValueAtIndexOrDefault(dataset.backgroundColor, index, rectangleElementOptions.backgroundColor);
			model.borderColor = custom.borderColor ? custom.borderColor : helpers.getValueAtIndexOrDefault(dataset.borderColor, index, rectangleElementOptions.borderColor);
			model.borderWidth = custom.borderWidth ? custom.borderWidth : helpers.getValueAtIndexOrDefault(dataset.borderWidth, index, rectangleElementOptions.borderWidth);
		}

	});


	// including horizontalBar in the bar file, instead of a file of its own
	// it extends bar (like pie extends doughnut)
	Chart.defaults.horizontalBar = {
		hover: {
			mode: "label"
		},

		scales: {
			xAxes: [{
				type: "linear",
				position: "bottom"
			}],
			yAxes: [{
				position: "left",
				type: "category",

				// Specific to Horizontal Bar Controller
				categoryPercentage: 0.8,
				barPercentage: 0.9,

				// grid line settings
				gridLines: {
					offsetGridLines: true
				}
			}]
		},
		elements: {
			rectangle: {
				borderSkipped: 'left'
			}
		},
		tooltips: {
			callbacks: {
				title: function(tooltipItems, data) {
					// Pick first xLabel for now
					var title = '';

					if (tooltipItems.length > 0) {
						if (tooltipItems[0].yLabel) {
							title = tooltipItems[0].yLabel;
						} else if (data.labels.length > 0 && tooltipItems[0].index < data.labels.length) {
							title = data.labels[tooltipItems[0].index];
						}
					}

					return title;
				},
				label: function(tooltipItem, data) {
					var datasetLabel = data.datasets[tooltipItem.datasetIndex].label || '';
				return datasetLabel + ': ' + tooltipItem.xLabel;
				}
			}
		}
	};

	Chart.controllers.horizontalBar = Chart.controllers.bar.extend({
		updateElement: function updateElement(rectangle, index, reset, numBars) {
			var me = this;
			var meta = me.getMeta();
			var xScale = me.getScaleForId(meta.xAxisID);
			var yScale = me.getScaleForId(meta.yAxisID);
			var scaleBase = xScale.getBasePixel();
			var custom = rectangle.custom || {};
			var dataset = me.getDataset();
			var rectangleElementOptions = me.chart.options.elements.rectangle;

			helpers.extend(rectangle, {
				// Utility
				_xScale: xScale,
				_yScale: yScale,
				_datasetIndex: me.index,
				_index: index,

				// Desired view properties
				_model: {
					x: reset ? scaleBase : me.calculateBarX(index, me.index),
					y: me.calculateBarY(index, me.index),

					// Tooltip
					label: me.chart.data.labels[index],
					datasetLabel: dataset.label,

					// Appearance
					base: reset ? scaleBase : me.calculateBarBase(me.index, index),
					height: me.calculateBarHeight(index),
					backgroundColor: custom.backgroundColor ? custom.backgroundColor : helpers.getValueAtIndexOrDefault(dataset.backgroundColor, index, rectangleElementOptions.backgroundColor),
					borderSkipped: custom.borderSkipped ? custom.borderSkipped : rectangleElementOptions.borderSkipped,
					borderColor: custom.borderColor ? custom.borderColor : helpers.getValueAtIndexOrDefault(dataset.borderColor, index, rectangleElementOptions.borderColor),
					borderWidth: custom.borderWidth ? custom.borderWidth : helpers.getValueAtIndexOrDefault(dataset.borderWidth, index, rectangleElementOptions.borderWidth)
				},

				draw: function () {
					var ctx = this._chart.ctx;
					var vm = this._view;

					var halfHeight = vm.height / 2,
						topY = vm.y - halfHeight,
						bottomY = vm.y + halfHeight,
						right = vm.base - (vm.base - vm.x),
						halfStroke = vm.borderWidth / 2;

					// Canvas doesn't allow us to stroke inside the width so we can
					// adjust the sizes to fit if we're setting a stroke on the line
					if (vm.borderWidth) {
						topY += halfStroke;
						bottomY -= halfStroke;
						right += halfStroke;
					}

					ctx.beginPath();

					ctx.fillStyle = vm.backgroundColor;
					ctx.strokeStyle = vm.borderColor;
					ctx.lineWidth = vm.borderWidth;

					// Corner points, from bottom-left to bottom-right clockwise
					// | 1 2 |
					// | 0 3 |
					var corners = [
						[vm.base, bottomY],
						[vm.base, topY],
						[right, topY],
						[right, bottomY]
					];

					// Find first (starting) corner with fallback to 'bottom'
					var borders = ['bottom', 'left', 'top', 'right'];
					var startCorner = borders.indexOf(vm.borderSkipped, 0);
					if (startCorner === -1)
						startCorner = 0;

					function cornerAt(index) {
						return corners[(startCorner + index) % 4];
					}

					// Draw rectangle from 'startCorner'
					ctx.moveTo.apply(ctx, cornerAt(0));
					for (var i = 1; i < 4; i++)
						ctx.lineTo.apply(ctx, cornerAt(i));

					ctx.fill();
					if (vm.borderWidth) {
						ctx.stroke();
					}
				},

				inRange: function (mouseX, mouseY) {
					var vm = this._view;
					var inRange = false;

					if (vm) {
						if (vm.x < vm.base) {
							inRange = (mouseY >= vm.y - vm.height / 2 && mouseY <= vm.y + vm.height / 2) && (mouseX >= vm.x && mouseX <= vm.base);
						} else {
							inRange = (mouseY >= vm.y - vm.height / 2 && mouseY <= vm.y + vm.height / 2) && (mouseX >= vm.base && mouseX <= vm.x);
						}
					}

					return inRange;
				}
			});

			rectangle.pivot();
		},

		calculateBarBase: function (datasetIndex, index) {
			var me = this;
			var meta = me.getMeta();
			var xScale = me.getScaleForId(meta.xAxisID);
			var base = 0;

			if (xScale.options.stacked) {

				var value = me.chart.data.datasets[datasetIndex].data[index];

				if (value < 0) {
					for (var i = 0; i < datasetIndex; i++) {
						var negDS = me.chart.data.datasets[i];
						var negDSMeta = me.chart.getDatasetMeta(i);
						if (negDSMeta.bar && negDSMeta.xAxisID === xScale.id && me.chart.isDatasetVisible(i)) {
							base += negDS.data[index] < 0 ? negDS.data[index] : 0;
						}
					}
				} else {
					for (var j = 0; j < datasetIndex; j++) {
						var posDS = me.chart.data.datasets[j];
						var posDSMeta = me.chart.getDatasetMeta(j);
						if (posDSMeta.bar && posDSMeta.xAxisID === xScale.id && me.chart.isDatasetVisible(j)) {
							base += posDS.data[index] > 0 ? posDS.data[index] : 0;
						}
					}
				}

				return xScale.getPixelForValue(base);
			}

			return xScale.getBasePixel();
		},

		getRuler: function (index) {
			var me = this;
			var meta = me.getMeta();
			var yScale = me.getScaleForId(meta.yAxisID);
			var datasetCount = me.getBarCount();

			var tickHeight;
			if (yScale.options.type === 'category') {
				tickHeight = yScale.getPixelForTick(index + 1) - yScale.getPixelForTick(index);
			} else {
				// Average width
				tickHeight = yScale.width / yScale.ticks.length;
			}
			var categoryHeight = tickHeight * yScale.options.categoryPercentage;
			var categorySpacing = (tickHeight - (tickHeight * yScale.options.categoryPercentage)) / 2;
			var fullBarHeight = categoryHeight / datasetCount;

			if (yScale.ticks.length !== me.chart.data.labels.length) {
				var perc = yScale.ticks.length / me.chart.data.labels.length;
				fullBarHeight = fullBarHeight * perc;
			}

			var barHeight = fullBarHeight * yScale.options.barPercentage;
			var barSpacing = fullBarHeight - (fullBarHeight * yScale.options.barPercentage);

			return {
				datasetCount: datasetCount,
				tickHeight: tickHeight,
				categoryHeight: categoryHeight,
				categorySpacing: categorySpacing,
				fullBarHeight: fullBarHeight,
				barHeight: barHeight,
				barSpacing: barSpacing,
			};
		},

		calculateBarHeight: function (index) {
			var me = this;
			var yScale = me.getScaleForId(me.getMeta().yAxisID);
			var ruler = me.getRuler(index);
			return yScale.options.stacked ? ruler.categoryHeight : ruler.barHeight;
		},

		calculateBarX: function (index, datasetIndex) {
			var me = this;
			var meta = me.getMeta();
			var xScale = me.getScaleForId(meta.xAxisID);
			var value = me.getDataset().data[index];

			if (xScale.options.stacked) {

				var sumPos = 0,
					sumNeg = 0;

				for (var i = 0; i < datasetIndex; i++) {
					var ds = me.chart.data.datasets[i];
					var dsMeta = me.chart.getDatasetMeta(i);
					if (dsMeta.bar && dsMeta.xAxisID === xScale.id && me.chart.isDatasetVisible(i)) {
						if (ds.data[index] < 0) {
							sumNeg += ds.data[index] || 0;
						} else {
							sumPos += ds.data[index] || 0;
						}
					}
				}

				if (value < 0) {
					return xScale.getPixelForValue(sumNeg + value);
				} else {
					return xScale.getPixelForValue(sumPos + value);
				}
			}

			return xScale.getPixelForValue(value);
		},

		calculateBarY: function (index, datasetIndex) {
			var me = this;
			var meta = me.getMeta();
			var yScale = me.getScaleForId(meta.yAxisID);
			var barIndex = me.getBarIndex(datasetIndex);

			var ruler = me.getRuler(index);
			var topTick = yScale.getPixelForValue(null, index, datasetIndex, me.chart.isCombo);
			topTick -= me.chart.isCombo ? (ruler.tickHeight / 2) : 0;

			if (yScale.options.stacked) {
				return topTick + (ruler.categoryHeight / 2) + ruler.categorySpacing;
			}

			return topTick +
				(ruler.barHeight / 2) +
				ruler.categorySpacing +
				(ruler.barHeight * barIndex) +
				(ruler.barSpacing / 2) +
				(ruler.barSpacing * barIndex);
		}
	});
};

},{}],16:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.bubble = {
		hover: {
			mode: "single"
		},

		scales: {
			xAxes: [{
				type: "linear", // bubble should probably use a linear scale by default
				position: "bottom",
				id: "x-axis-0" // need an ID so datasets can reference the scale
			}],
			yAxes: [{
				type: "linear",
				position: "left",
				id: "y-axis-0"
			}]
		},

		tooltips: {
			callbacks: {
				title: function(tooltipItems, data) {
					// Title doesn't make sense for scatter since we format the data as a point
					return '';
				},
				label: function(tooltipItem, data) {
					var datasetLabel = data.datasets[tooltipItem.datasetIndex].label || '';
					var dataPoint = data.datasets[tooltipItem.datasetIndex].data[tooltipItem.index];
					return datasetLabel + ': (' + dataPoint.x + ', ' + dataPoint.y + ', ' + dataPoint.r + ')';
				}
			}
		}
	};

	Chart.controllers.bubble = Chart.DatasetController.extend({

		dataElementType: Chart.elements.Point,

		update: function update(reset) {
			var me = this;
			var meta = me.getMeta();
			var points = meta.data;

			// Update Points
			helpers.each(points, function(point, index) {
				me.updateElement(point, index, reset);
			});
		},

		updateElement: function(point, index, reset) {
			var me = this;
			var meta = me.getMeta();
			var xScale = me.getScaleForId(meta.xAxisID);
			var yScale = me.getScaleForId(meta.yAxisID);

			var custom = point.custom || {};
			var dataset = me.getDataset();
			var data = dataset.data[index];
			var pointElementOptions = me.chart.options.elements.point;
			var dsIndex = me.index;

			helpers.extend(point, {
				// Utility
				_xScale: xScale,
				_yScale: yScale,
				_datasetIndex: dsIndex,
				_index: index,

				// Desired view properties
				_model: {
					x: reset ? xScale.getPixelForDecimal(0.5) : xScale.getPixelForValue(data, index, dsIndex, me.chart.isCombo),
					y: reset ? yScale.getBasePixel() : yScale.getPixelForValue(data, index, dsIndex),
					// Appearance
					radius: reset ? 0 : custom.radius ? custom.radius : me.getRadius(data),

					// Tooltip
					hitRadius: custom.hitRadius ? custom.hitRadius : helpers.getValueAtIndexOrDefault(dataset.hitRadius, index, pointElementOptions.hitRadius)
				}
			});

			// Trick to reset the styles of the point
			Chart.DatasetController.prototype.removeHoverStyle.call(me, point, pointElementOptions);

			var model = point._model;
			model.skip = custom.skip ? custom.skip : (isNaN(model.x) || isNaN(model.y));

			point.pivot();
		},

		getRadius: function(value) {
			return value.r || this.chart.options.elements.point.radius;
		},

		setHoverStyle: function(point) {
			var me = this;
			Chart.DatasetController.prototype.setHoverStyle.call(me, point);

			// Radius
			var dataset = me.chart.data.datasets[point._datasetIndex];
			var index = point._index;
			var custom = point.custom || {};
			var model = point._model;
			model.radius = custom.hoverRadius ? custom.hoverRadius : (helpers.getValueAtIndexOrDefault(dataset.hoverRadius, index, me.chart.options.elements.point.hoverRadius)) + me.getRadius(dataset.data[index]);
		},

		removeHoverStyle: function(point) {
			var me = this;
			Chart.DatasetController.prototype.removeHoverStyle.call(me, point, me.chart.options.elements.point);

			var dataVal = me.chart.data.datasets[point._datasetIndex].data[point._index];
			var custom = point.custom || {};
			var model = point._model;

			model.radius = custom.radius ? custom.radius : me.getRadius(dataVal);
		}
	});
};

},{}],17:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers,
		defaults = Chart.defaults;

	defaults.doughnut = {
		animation: {
			//Boolean - Whether we animate the rotation of the Doughnut
			animateRotate: true,
			//Boolean - Whether we animate scaling the Doughnut from the centre
			animateScale: false
		},
		aspectRatio: 1,
		hover: {
			mode: 'single'
		},
		legendCallback: function(chart) {
			var text = [];
			text.push('<ul class="' + chart.id + '-legend">');

			var data = chart.data;
			var datasets = data.datasets;
			var labels = data.labels;

			if (datasets.length) {
				for (var i = 0; i < datasets[0].data.length; ++i) {
					text.push('<li><span style="background-color:' + datasets[0].backgroundColor[i] + '"></span>');
					if (labels[i]) {
						text.push(labels[i]);
					}
					text.push('</li>');
				}
			}

			text.push('</ul>');
			return text.join("");
		},
		legend: {
			labels: {
				generateLabels: function(chart) {
					var data = chart.data;
					if (data.labels.length && data.datasets.length) {
						return data.labels.map(function(label, i) {
							var meta = chart.getDatasetMeta(0);
							var ds = data.datasets[0];
							var arc = meta.data[i];
							var custom = arc.custom || {};
							var getValueAtIndexOrDefault = helpers.getValueAtIndexOrDefault;
							var arcOpts = chart.options.elements.arc;
							var fill = custom.backgroundColor ? custom.backgroundColor : getValueAtIndexOrDefault(ds.backgroundColor, i, arcOpts.backgroundColor);
							var stroke = custom.borderColor ? custom.borderColor : getValueAtIndexOrDefault(ds.borderColor, i, arcOpts.borderColor);
							var bw = custom.borderWidth ? custom.borderWidth : getValueAtIndexOrDefault(ds.borderWidth, i, arcOpts.borderWidth);

							return {
								text: label,
								fillStyle: fill,
								strokeStyle: stroke,
								lineWidth: bw,
								hidden: isNaN(ds.data[i]) || meta.data[i].hidden,

								// Extra data used for toggling the correct item
								index: i
							};
						});
					} else {
						return [];
					}
				}
			},

			onClick: function(e, legendItem) {
				var index = legendItem.index;
				var chart = this.chart;
				var i, ilen, meta;

				for (i = 0, ilen = (chart.data.datasets || []).length; i < ilen; ++i) {
					meta = chart.getDatasetMeta(i);
					meta.data[index].hidden = !meta.data[index].hidden;
				}

				chart.update();
			}
		},

		//The percentage of the chart that we cut out of the middle.
		cutoutPercentage: 50,

		//The rotation of the chart, where the first data arc begins.
		rotation: Math.PI * -0.5,

		//The total circumference of the chart.
		circumference: Math.PI * 2.0,

		// Need to override these to give a nice default
		tooltips: {
			callbacks: {
				title: function() {
					return '';
				},
				label: function(tooltipItem, data) {
					return data.labels[tooltipItem.index] + ': ' + data.datasets[tooltipItem.datasetIndex].data[tooltipItem.index];
				}
			}
		}
	};

	defaults.pie = helpers.clone(defaults.doughnut);
	helpers.extend(defaults.pie, {
		cutoutPercentage: 0
	});


	Chart.controllers.doughnut = Chart.controllers.pie = Chart.DatasetController.extend({

		dataElementType: Chart.elements.Arc,

		linkScales: helpers.noop,

		// Get index of the dataset in relation to the visible datasets. This allows determining the inner and outer radius correctly
		getRingIndex: function getRingIndex(datasetIndex) {
			var ringIndex = 0;

			for (var j = 0; j < datasetIndex; ++j) {
				if (this.chart.isDatasetVisible(j)) {
					++ringIndex;
				}
			}

			return ringIndex;
		},

		update: function update(reset) {
			var me = this;
			var chart = me.chart,
				chartArea = chart.chartArea,
				opts = chart.options,
				arcOpts = opts.elements.arc,
				availableWidth = chartArea.right - chartArea.left - arcOpts.borderWidth,
				availableHeight = chartArea.bottom - chartArea.top - arcOpts.borderWidth,
				minSize = Math.min(availableWidth, availableHeight),
				offset = {
					x: 0,
					y: 0
				},
				meta = me.getMeta(),
				cutoutPercentage = opts.cutoutPercentage,
				circumference = opts.circumference;

			// If the chart's circumference isn't a full circle, calculate minSize as a ratio of the width/height of the arc
			if (circumference < Math.PI * 2.0) {
				var startAngle = opts.rotation % (Math.PI * 2.0);
				startAngle += Math.PI * 2.0 * (startAngle >= Math.PI ? -1 : startAngle < -Math.PI ? 1 : 0);
				var endAngle = startAngle + circumference;
				var start = {x: Math.cos(startAngle), y: Math.sin(startAngle)};
				var end = {x: Math.cos(endAngle), y: Math.sin(endAngle)};
				var contains0 = (startAngle <= 0 && 0 <= endAngle) || (startAngle <= Math.PI * 2.0 && Math.PI * 2.0 <= endAngle);
				var contains90 = (startAngle <= Math.PI * 0.5 && Math.PI * 0.5 <= endAngle) || (startAngle <= Math.PI * 2.5 && Math.PI * 2.5 <= endAngle);
				var contains180 = (startAngle <= -Math.PI && -Math.PI <= endAngle) || (startAngle <= Math.PI && Math.PI <= endAngle);
				var contains270 = (startAngle <= -Math.PI * 0.5 && -Math.PI * 0.5 <= endAngle) || (startAngle <= Math.PI * 1.5 && Math.PI * 1.5 <= endAngle);
				var cutout = cutoutPercentage / 100.0;
				var min = {x: contains180 ? -1 : Math.min(start.x * (start.x < 0 ? 1 : cutout), end.x * (end.x < 0 ? 1 : cutout)), y: contains270 ? -1 : Math.min(start.y * (start.y < 0 ? 1 : cutout), end.y * (end.y < 0 ? 1 : cutout))};
				var max = {x: contains0 ? 1 : Math.max(start.x * (start.x > 0 ? 1 : cutout), end.x * (end.x > 0 ? 1 : cutout)), y: contains90 ? 1 : Math.max(start.y * (start.y > 0 ? 1 : cutout), end.y * (end.y > 0 ? 1 : cutout))};
				var size = {width: (max.x - min.x) * 0.5, height: (max.y - min.y) * 0.5};
				minSize = Math.min(availableWidth / size.width, availableHeight / size.height);
				offset = {x: (max.x + min.x) * -0.5, y: (max.y + min.y) * -0.5};
			}

			chart.outerRadius = Math.max(minSize / 2, 0);
			chart.innerRadius = Math.max(cutoutPercentage ? (chart.outerRadius / 100) * (cutoutPercentage) : 1, 0);
			chart.radiusLength = (chart.outerRadius - chart.innerRadius) / chart.getVisibleDatasetCount();
			chart.offsetX = offset.x * chart.outerRadius;
			chart.offsetY = offset.y * chart.outerRadius;

			meta.total = me.calculateTotal();

			me.outerRadius = chart.outerRadius - (chart.radiusLength * me.getRingIndex(me.index));
			me.innerRadius = me.outerRadius - chart.radiusLength;

			helpers.each(meta.data, function(arc, index) {
				me.updateElement(arc, index, reset);
			});
		},

		updateElement: function(arc, index, reset) {
			var me = this;
			var chart = me.chart,
				chartArea = chart.chartArea,
				opts = chart.options,
				animationOpts = opts.animation,
				arcOpts = opts.elements.arc,
				centerX = (chartArea.left + chartArea.right) / 2,
				centerY = (chartArea.top + chartArea.bottom) / 2,
				startAngle = opts.rotation, // non reset case handled later
				endAngle = opts.rotation, // non reset case handled later
				dataset = me.getDataset(),
				circumference = reset && animationOpts.animateRotate ? 0 : arc.hidden ? 0 : me.calculateCircumference(dataset.data[index]) * (opts.circumference / (2.0 * Math.PI)),
				innerRadius = reset && animationOpts.animateScale ? 0 : me.innerRadius,
				outerRadius = reset && animationOpts.animateScale ? 0 : me.outerRadius,
				custom = arc.custom || {},
				valueAtIndexOrDefault = helpers.getValueAtIndexOrDefault;

			helpers.extend(arc, {
				// Utility
				_datasetIndex: me.index,
				_index: index,

				// Desired view properties
				_model: {
					x: centerX + chart.offsetX,
					y: centerY + chart.offsetY,
					startAngle: startAngle,
					endAngle: endAngle,
					circumference: circumference,
					outerRadius: outerRadius,
					innerRadius: innerRadius,
					label: valueAtIndexOrDefault(dataset.label, index, chart.data.labels[index])
				}
			});

			var model = arc._model;
			// Resets the visual styles
			this.removeHoverStyle(arc);

			// Set correct angles if not resetting
			if (!reset || !animationOpts.animateRotate) {
				if (index === 0) {
					model.startAngle = opts.rotation;
				} else {
					model.startAngle = me.getMeta().data[index - 1]._model.endAngle;
				}

				model.endAngle = model.startAngle + model.circumference;
			}

			arc.pivot();
		},

		removeHoverStyle: function(arc) {
			Chart.DatasetController.prototype.removeHoverStyle.call(this, arc, this.chart.options.elements.arc);
		},

		calculateTotal: function() {
			var dataset = this.getDataset();
			var meta = this.getMeta();
			var total = 0;
			var value;

			helpers.each(meta.data, function(element, index) {
				value = dataset.data[index];
				if (!isNaN(value) && !element.hidden) {
					total += Math.abs(value);
				}
			});

			return total;
		},

		calculateCircumference: function(value) {
			var total = this.getMeta().total;
			if (total > 0 && !isNaN(value)) {
				return (Math.PI * 2.0) * (value / total);
			} else {
				return 0;
			}
		}
	});
};

},{}],18:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.line = {
		showLines: true,

		hover: {
			mode: "label"
		},

		scales: {
			xAxes: [{
				type: "category",
				id: 'x-axis-0'
			}],
			yAxes: [{
				type: "linear",
				id: 'y-axis-0'
			}]
		}
	};

	function lineEnabled(dataset, options) {
		return helpers.getValueOrDefault(dataset.showLine, options.showLines);
	}

	Chart.controllers.line = Chart.DatasetController.extend({

		datasetElementType: Chart.elements.Line,

		dataElementType: Chart.elements.Point,

		addElementAndReset: function(index) {
			var me = this;
			var options = me.chart.options;
			var meta = me.getMeta();

			Chart.DatasetController.prototype.addElementAndReset.call(me, index);

			// Make sure bezier control points are updated
			if (lineEnabled(me.getDataset(), options) && meta.dataset._model.tension !== 0) {
				me.updateBezierControlPoints();
			}
		},

		update: function update(reset) {
			var me = this;
			var meta = me.getMeta();
			var line = meta.dataset;
			var points = meta.data || [];
			var options = me.chart.options;
			var lineElementOptions = options.elements.line;
			var scale = me.getScaleForId(meta.yAxisID);
			var i, ilen, custom;
			var dataset = me.getDataset();
			var showLine = lineEnabled(dataset, options);

			// Update Line
			if (showLine) {
				custom = line.custom || {};

				// Compatibility: If the properties are defined with only the old name, use those values
				if ((dataset.tension !== undefined) && (dataset.lineTension === undefined)) {
					dataset.lineTension = dataset.tension;
				}

				// Utility
				line._scale = scale;
				line._datasetIndex = me.index;
				// Data
				line._children = points;
				// Model
				line._model = {
					// Appearance
					// The default behavior of lines is to break at null values, according
					// to https://github.com/chartjs/Chart.js/issues/2435#issuecomment-216718158
					// This option gives linse the ability to span gaps
					spanGaps: dataset.spanGaps ? dataset.spanGaps : false,
					tension: custom.tension ? custom.tension : helpers.getValueOrDefault(dataset.lineTension, lineElementOptions.tension),
					backgroundColor: custom.backgroundColor ? custom.backgroundColor : (dataset.backgroundColor || lineElementOptions.backgroundColor),
					borderWidth: custom.borderWidth ? custom.borderWidth : (dataset.borderWidth || lineElementOptions.borderWidth),
					borderColor: custom.borderColor ? custom.borderColor : (dataset.borderColor || lineElementOptions.borderColor),
					borderCapStyle: custom.borderCapStyle ? custom.borderCapStyle : (dataset.borderCapStyle || lineElementOptions.borderCapStyle),
					borderDash: custom.borderDash ? custom.borderDash : (dataset.borderDash || lineElementOptions.borderDash),
					borderDashOffset: custom.borderDashOffset ? custom.borderDashOffset : (dataset.borderDashOffset || lineElementOptions.borderDashOffset),
					borderJoinStyle: custom.borderJoinStyle ? custom.borderJoinStyle : (dataset.borderJoinStyle || lineElementOptions.borderJoinStyle),
					fill: custom.fill ? custom.fill : (dataset.fill !== undefined ? dataset.fill : lineElementOptions.fill),
					// Scale
					scaleTop: scale.top,
					scaleBottom: scale.bottom,
					scaleZero: scale.getBasePixel()
				};

				line.pivot();
			}

			// Update Points
			for (i=0, ilen=points.length; i<ilen; ++i) {
				me.updateElement(points[i], i, reset);
			}

			if (showLine && line._model.tension !== 0) {
				me.updateBezierControlPoints();
			}

			// Now pivot the point for animation
			for (i=0, ilen=points.length; i<ilen; ++i) {
				points[i].pivot();
			}
		},

		getPointBackgroundColor: function(point, index) {
			var backgroundColor = this.chart.options.elements.point.backgroundColor;
			var dataset = this.getDataset();
			var custom = point.custom || {};

			if (custom.backgroundColor) {
				backgroundColor = custom.backgroundColor;
			} else if (dataset.pointBackgroundColor) {
				backgroundColor = helpers.getValueAtIndexOrDefault(dataset.pointBackgroundColor, index, backgroundColor);
			} else if (dataset.backgroundColor) {
				backgroundColor = dataset.backgroundColor;
			}

			return backgroundColor;
		},

		getPointBorderColor: function(point, index) {
			var borderColor = this.chart.options.elements.point.borderColor;
			var dataset = this.getDataset();
			var custom = point.custom || {};

			if (custom.borderColor) {
				borderColor = custom.borderColor;
			} else if (dataset.pointBorderColor) {
				borderColor = helpers.getValueAtIndexOrDefault(dataset.pointBorderColor, index, borderColor);
			} else if (dataset.borderColor) {
				borderColor = dataset.borderColor;
			}

			return borderColor;
		},

		getPointBorderWidth: function(point, index) {
			var borderWidth = this.chart.options.elements.point.borderWidth;
			var dataset = this.getDataset();
			var custom = point.custom || {};

			if (custom.borderWidth) {
				borderWidth = custom.borderWidth;
			} else if (dataset.pointBorderWidth) {
				borderWidth = helpers.getValueAtIndexOrDefault(dataset.pointBorderWidth, index, borderWidth);
			} else if (dataset.borderWidth) {
				borderWidth = dataset.borderWidth;
			}

			return borderWidth;
		},

		updateElement: function(point, index, reset) {
			var me = this;
			var meta = me.getMeta();
			var custom = point.custom || {};
			var dataset = me.getDataset();
			var datasetIndex = me.index;
			var value = dataset.data[index];
			var yScale = me.getScaleForId(meta.yAxisID);
			var xScale = me.getScaleForId(meta.xAxisID);
			var pointOptions = me.chart.options.elements.point;
			var x, y;

			// Compatibility: If the properties are defined with only the old name, use those values
			if ((dataset.radius !== undefined) && (dataset.pointRadius === undefined)) {
				dataset.pointRadius = dataset.radius;
			}
			if ((dataset.hitRadius !== undefined) && (dataset.pointHitRadius === undefined)) {
				dataset.pointHitRadius = dataset.hitRadius;
			}

			x = xScale.getPixelForValue(value, index, datasetIndex, me.chart.isCombo);
			y = reset ? yScale.getBasePixel() : me.calculatePointY(value, index, datasetIndex, me.chart.isCombo);

			// Utility
			point._xScale = xScale;
			point._yScale = yScale;
			point._datasetIndex = datasetIndex;
			point._index = index;

			// Desired view properties
			point._model = {
				x: x,
				y: y,
				skip: custom.skip || isNaN(x) || isNaN(y),
				// Appearance
				radius: custom.radius || helpers.getValueAtIndexOrDefault(dataset.pointRadius, index, pointOptions.radius),
				pointStyle: custom.pointStyle || helpers.getValueAtIndexOrDefault(dataset.pointStyle, index, pointOptions.pointStyle),
				backgroundColor: me.getPointBackgroundColor(point, index),
				borderColor: me.getPointBorderColor(point, index),
				borderWidth: me.getPointBorderWidth(point, index),
				tension: meta.dataset._model ? meta.dataset._model.tension : 0,
				// Tooltip
				hitRadius: custom.hitRadius || helpers.getValueAtIndexOrDefault(dataset.pointHitRadius, index, pointOptions.hitRadius)
			};
		},

		calculatePointY: function(value, index, datasetIndex, isCombo) {
			var me = this;
			var chart = me.chart;
			var meta = me.getMeta();
			var yScale = me.getScaleForId(meta.yAxisID);
			var sumPos = 0;
			var sumNeg = 0;
			var i, ds, dsMeta;

			if (yScale.options.stacked) {
				for (i = 0; i < datasetIndex; i++) {
					ds = chart.data.datasets[i];
					dsMeta = chart.getDatasetMeta(i);
					if (dsMeta.type === 'line' && chart.isDatasetVisible(i)) {
						if (ds.data[index] < 0) {
							sumNeg += ds.data[index] || 0;
						} else {
							sumPos += ds.data[index] || 0;
						}
					}
				}

				if (value < 0) {
					return yScale.getPixelForValue(sumNeg + value);
				} else {
					return yScale.getPixelForValue(sumPos + value);
				}
			}

			return yScale.getPixelForValue(value);
		},

		updateBezierControlPoints: function() {
			var meta = this.getMeta();
			var area = this.chart.chartArea;
			var points = meta.data || [];
			var i, ilen, point, model, controlPoints;

			for (i=0, ilen=points.length; i<ilen; ++i) {
				point = points[i];
				model = point._model;
				controlPoints = helpers.splineCurve(
					helpers.previousItem(points, i)._model,
					model,
					helpers.nextItem(points, i)._model,
					meta.dataset._model.tension
				);

				model.controlPointPreviousX = controlPoints.previous.x;
				model.controlPointPreviousY = controlPoints.previous.y;
				model.controlPointNextX = controlPoints.next.x;
				model.controlPointNextY = controlPoints.next.y;
			}
		},

		draw: function(ease) {
			var me = this;
			var meta = me.getMeta();
			var points = meta.data || [];
			var easingDecimal = ease || 1;
			var i, ilen;

			// Transition Point Locations
			for (i=0, ilen=points.length; i<ilen; ++i) {
				points[i].transition(easingDecimal);
			}

			// Transition and Draw the line
			if (lineEnabled(me.getDataset(), me.chart.options)) {
				meta.dataset.transition(easingDecimal).draw();
			}

			// Draw the points
			for (i=0, ilen=points.length; i<ilen; ++i) {
				points[i].draw();
			}
		},

		setHoverStyle: function(point) {
			// Point
			var dataset = this.chart.data.datasets[point._datasetIndex];
			var index = point._index;
			var custom = point.custom || {};
			var model = point._model;

			model.radius = custom.hoverRadius || helpers.getValueAtIndexOrDefault(dataset.pointHoverRadius, index, this.chart.options.elements.point.hoverRadius);
			model.backgroundColor = custom.hoverBackgroundColor || helpers.getValueAtIndexOrDefault(dataset.pointHoverBackgroundColor, index, helpers.getHoverColor(model.backgroundColor));
			model.borderColor = custom.hoverBorderColor || helpers.getValueAtIndexOrDefault(dataset.pointHoverBorderColor, index, helpers.getHoverColor(model.borderColor));
			model.borderWidth = custom.hoverBorderWidth || helpers.getValueAtIndexOrDefault(dataset.pointHoverBorderWidth, index, model.borderWidth);
		},

		removeHoverStyle: function(point) {
			var me = this;
			var dataset = me.chart.data.datasets[point._datasetIndex];
			var index = point._index;
			var custom = point.custom || {};
			var model = point._model;

			// Compatibility: If the properties are defined with only the old name, use those values
			if ((dataset.radius !== undefined) && (dataset.pointRadius === undefined)) {
				dataset.pointRadius = dataset.radius;
			}

			model.radius = custom.radius || helpers.getValueAtIndexOrDefault(dataset.pointRadius, index, me.chart.options.elements.point.radius);
			model.backgroundColor = me.getPointBackgroundColor(point, index);
			model.borderColor = me.getPointBorderColor(point, index);
			model.borderWidth = me.getPointBorderWidth(point, index);
		}
	});
};

},{}],19:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.polarArea = {

		scale: {
			type: "radialLinear",
			lineArc: true // so that lines are circular
		},

		//Boolean - Whether to animate the rotation of the chart
		animation: {
			animateRotate: true,
			animateScale: true
		},

		aspectRatio: 1,
		legendCallback: function(chart) {
			var text = [];
			text.push('<ul class="' + chart.id + '-legend">');

			var data = chart.data;
			var datasets = data.datasets;
			var labels = data.labels;

			if (datasets.length) {
				for (var i = 0; i < datasets[0].data.length; ++i) {
					text.push('<li><span style="background-color:' + datasets[0].backgroundColor[i] + '">');
					if (labels[i]) {
						text.push(labels[i]);
					}
					text.push('</span></li>');
				}
			}

			text.push('</ul>');
			return text.join("");
		},
		legend: {
			labels: {
				generateLabels: function(chart) {
					var data = chart.data;
					if (data.labels.length && data.datasets.length) {
						return data.labels.map(function(label, i) {
							var meta = chart.getDatasetMeta(0);
							var ds = data.datasets[0];
							var arc = meta.data[i];
							var custom = arc.custom || {};
							var getValueAtIndexOrDefault = helpers.getValueAtIndexOrDefault;
							var arcOpts = chart.options.elements.arc;
							var fill = custom.backgroundColor ? custom.backgroundColor : getValueAtIndexOrDefault(ds.backgroundColor, i, arcOpts.backgroundColor);
							var stroke = custom.borderColor ? custom.borderColor : getValueAtIndexOrDefault(ds.borderColor, i, arcOpts.borderColor);
							var bw = custom.borderWidth ? custom.borderWidth : getValueAtIndexOrDefault(ds.borderWidth, i, arcOpts.borderWidth);

							return {
								text: label,
								fillStyle: fill,
								strokeStyle: stroke,
								lineWidth: bw,
								hidden: isNaN(ds.data[i]) || meta.data[i].hidden,

								// Extra data used for toggling the correct item
								index: i
							};
						});
					} else {
						return [];
					}
				}
			},

			onClick: function(e, legendItem) {
				var index = legendItem.index;
				var chart = this.chart;
				var i, ilen, meta;

				for (i = 0, ilen = (chart.data.datasets || []).length; i < ilen; ++i) {
					meta = chart.getDatasetMeta(i);
					meta.data[index].hidden = !meta.data[index].hidden;
				}

				chart.update();
			}
		},

		// Need to override these to give a nice default
		tooltips: {
			callbacks: {
				title: function() {
					return '';
				},
				label: function(tooltipItem, data) {
					return data.labels[tooltipItem.index] + ': ' + tooltipItem.yLabel;
				}
			}
		}
	};

	Chart.controllers.polarArea = Chart.DatasetController.extend({

		dataElementType: Chart.elements.Arc,

		linkScales: helpers.noop,

		update: function update(reset) {
			var me = this;
			var chart = me.chart;
			var chartArea = chart.chartArea;
			var meta = me.getMeta();
			var opts = chart.options;
			var arcOpts = opts.elements.arc;
			var minSize = Math.min(chartArea.right - chartArea.left, chartArea.bottom - chartArea.top);
			chart.outerRadius = Math.max((minSize - arcOpts.borderWidth / 2) / 2, 0);
			chart.innerRadius = Math.max(opts.cutoutPercentage ? (chart.outerRadius / 100) * (opts.cutoutPercentage) : 1, 0);
			chart.radiusLength = (chart.outerRadius - chart.innerRadius) / chart.getVisibleDatasetCount();

			me.outerRadius = chart.outerRadius - (chart.radiusLength * me.index);
			me.innerRadius = me.outerRadius - chart.radiusLength;

			meta.count = me.countVisibleElements();

			helpers.each(meta.data, function(arc, index) {
				me.updateElement(arc, index, reset);
			});
		},

		updateElement: function(arc, index, reset) {
			var me = this;
			var chart = me.chart;
			var chartArea = chart.chartArea;
			var dataset = me.getDataset();
			var opts = chart.options;
			var animationOpts = opts.animation;
			var arcOpts = opts.elements.arc;
			var custom = arc.custom || {};
			var scale = chart.scale;
			var getValueAtIndexOrDefault = helpers.getValueAtIndexOrDefault;
			var labels = chart.data.labels;

			var circumference = me.calculateCircumference(dataset.data[index]);
			var centerX = (chartArea.left + chartArea.right) / 2;
			var centerY = (chartArea.top + chartArea.bottom) / 2;

			// If there is NaN data before us, we need to calculate the starting angle correctly.
			// We could be way more efficient here, but its unlikely that the polar area chart will have a lot of data
			var visibleCount = 0;
			var meta = me.getMeta();
			for (var i = 0; i < index; ++i) {
				if (!isNaN(dataset.data[i]) && !meta.data[i].hidden) {
					++visibleCount;
				}
			}

			var negHalfPI = -0.5 * Math.PI;
			var distance = arc.hidden ? 0 : scale.getDistanceFromCenterForValue(dataset.data[index]);
			var startAngle = (negHalfPI) + (circumference * visibleCount);
			var endAngle = startAngle + (arc.hidden ? 0 : circumference);

			var resetRadius = animationOpts.animateScale ? 0 : scale.getDistanceFromCenterForValue(dataset.data[index]);

			helpers.extend(arc, {
				// Utility
				_datasetIndex: me.index,
				_index: index,
				_scale: scale,

				// Desired view properties
				_model: {
					x: centerX,
					y: centerY,
					innerRadius: 0,
					outerRadius: reset ? resetRadius : distance,
					startAngle: reset && animationOpts.animateRotate ? negHalfPI : startAngle,
					endAngle: reset && animationOpts.animateRotate ? negHalfPI : endAngle,
					label: getValueAtIndexOrDefault(labels, index, labels[index])
				}
			});

			// Apply border and fill style
			me.removeHoverStyle(arc);

			arc.pivot();
		},

		removeHoverStyle: function(arc) {
			Chart.DatasetController.prototype.removeHoverStyle.call(this, arc, this.chart.options.elements.arc);
		},

		countVisibleElements: function() {
			var dataset = this.getDataset();
			var meta = this.getMeta();
			var count = 0;

			helpers.each(meta.data, function(element, index) {
				if (!isNaN(dataset.data[index]) && !element.hidden) {
					count++;
				}
			});

			return count;
		},

		calculateCircumference: function(value) {
			var count = this.getMeta().count;
			if (count > 0 && !isNaN(value)) {
				return (2 * Math.PI) / count;
			} else {
				return 0;
			}
		}
	});
};

},{}],20:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.radar = {
		scale: {
			type: "radialLinear"
		},
		elements: {
			line: {
				tension: 0 // no bezier in radar
			}
		}
	};

	Chart.controllers.radar = Chart.DatasetController.extend({

		datasetElementType: Chart.elements.Line,

		dataElementType: Chart.elements.Point,

		linkScales: helpers.noop,

		addElementAndReset: function(index) {
			Chart.DatasetController.prototype.addElementAndReset.call(this, index);

			// Make sure bezier control points are updated
			this.updateBezierControlPoints();
		},

		update: function update(reset) {
			var me = this;
			var meta = me.getMeta();
			var line = meta.dataset;
			var points = meta.data;
			var custom = line.custom || {};
			var dataset = me.getDataset();
			var lineElementOptions = me.chart.options.elements.line;
			var scale = me.chart.scale;

			// Compatibility: If the properties are defined with only the old name, use those values
			if ((dataset.tension !== undefined) && (dataset.lineTension === undefined)) {
				dataset.lineTension = dataset.tension;
			}

			helpers.extend(meta.dataset, {
				// Utility
				_datasetIndex: me.index,
				// Data
				_children: points,
				_loop: true,
				// Model
				_model: {
					// Appearance
					tension: custom.tension ? custom.tension : helpers.getValueOrDefault(dataset.lineTension, lineElementOptions.tension),
					backgroundColor: custom.backgroundColor ? custom.backgroundColor : (dataset.backgroundColor || lineElementOptions.backgroundColor),
					borderWidth: custom.borderWidth ? custom.borderWidth : (dataset.borderWidth || lineElementOptions.borderWidth),
					borderColor: custom.borderColor ? custom.borderColor : (dataset.borderColor || lineElementOptions.borderColor),
					fill: custom.fill ? custom.fill : (dataset.fill !== undefined ? dataset.fill : lineElementOptions.fill),
					borderCapStyle: custom.borderCapStyle ? custom.borderCapStyle : (dataset.borderCapStyle || lineElementOptions.borderCapStyle),
					borderDash: custom.borderDash ? custom.borderDash : (dataset.borderDash || lineElementOptions.borderDash),
					borderDashOffset: custom.borderDashOffset ? custom.borderDashOffset : (dataset.borderDashOffset || lineElementOptions.borderDashOffset),
					borderJoinStyle: custom.borderJoinStyle ? custom.borderJoinStyle : (dataset.borderJoinStyle || lineElementOptions.borderJoinStyle),

					// Scale
					scaleTop: scale.top,
					scaleBottom: scale.bottom,
					scaleZero: scale.getBasePosition()
				}
			});

			meta.dataset.pivot();

			// Update Points
			helpers.each(points, function(point, index) {
				me.updateElement(point, index, reset);
			}, me);


			// Update bezier control points
			me.updateBezierControlPoints();
		},
		updateElement: function(point, index, reset) {
			var me = this;
			var custom = point.custom || {};
			var dataset = me.getDataset();
			var scale = me.chart.scale;
			var pointElementOptions = me.chart.options.elements.point;
			var pointPosition = scale.getPointPositionForValue(index, dataset.data[index]);

			helpers.extend(point, {
				// Utility
				_datasetIndex: me.index,
				_index: index,
				_scale: scale,

				// Desired view properties
				_model: {
					x: reset ? scale.xCenter : pointPosition.x, // value not used in dataset scale, but we want a consistent API between scales
					y: reset ? scale.yCenter : pointPosition.y,

					// Appearance
					tension: custom.tension ? custom.tension : helpers.getValueOrDefault(dataset.tension, me.chart.options.elements.line.tension),
					radius: custom.radius ? custom.radius : helpers.getValueAtIndexOrDefault(dataset.pointRadius, index, pointElementOptions.radius),
					backgroundColor: custom.backgroundColor ? custom.backgroundColor : helpers.getValueAtIndexOrDefault(dataset.pointBackgroundColor, index, pointElementOptions.backgroundColor),
					borderColor: custom.borderColor ? custom.borderColor : helpers.getValueAtIndexOrDefault(dataset.pointBorderColor, index, pointElementOptions.borderColor),
					borderWidth: custom.borderWidth ? custom.borderWidth : helpers.getValueAtIndexOrDefault(dataset.pointBorderWidth, index, pointElementOptions.borderWidth),
					pointStyle: custom.pointStyle ? custom.pointStyle : helpers.getValueAtIndexOrDefault(dataset.pointStyle, index, pointElementOptions.pointStyle),

					// Tooltip
					hitRadius: custom.hitRadius ? custom.hitRadius : helpers.getValueAtIndexOrDefault(dataset.hitRadius, index, pointElementOptions.hitRadius)
				}
			});

			point._model.skip = custom.skip ? custom.skip : (isNaN(point._model.x) || isNaN(point._model.y));
		},
		updateBezierControlPoints: function() {
			var chartArea = this.chart.chartArea;
			var meta = this.getMeta();

			helpers.each(meta.data, function(point, index) {
				var model = point._model;
				var controlPoints = helpers.splineCurve(
					helpers.previousItem(meta.data, index, true)._model,
					model,
					helpers.nextItem(meta.data, index, true)._model,
					model.tension
				);

				// Prevent the bezier going outside of the bounds of the graph
				model.controlPointPreviousX = Math.max(Math.min(controlPoints.previous.x, chartArea.right), chartArea.left);
				model.controlPointPreviousY = Math.max(Math.min(controlPoints.previous.y, chartArea.bottom), chartArea.top);

				model.controlPointNextX = Math.max(Math.min(controlPoints.next.x, chartArea.right), chartArea.left);
				model.controlPointNextY = Math.max(Math.min(controlPoints.next.y, chartArea.bottom), chartArea.top);

				// Now pivot the point for animation
				point.pivot();
			});
		},

		draw: function(ease) {
			var meta = this.getMeta();
			var easingDecimal = ease || 1;

			// Transition Point Locations
			helpers.each(meta.data, function(point, index) {
				point.transition(easingDecimal);
			});

			// Transition and Draw the line
			meta.dataset.transition(easingDecimal).draw();

			// Draw the points
			helpers.each(meta.data, function(point) {
				point.draw();
			});
		},

		setHoverStyle: function(point) {
			// Point
			var dataset = this.chart.data.datasets[point._datasetIndex];
			var custom = point.custom || {};
			var index = point._index;
			var model = point._model;

			model.radius = custom.hoverRadius ? custom.hoverRadius : helpers.getValueAtIndexOrDefault(dataset.pointHoverRadius, index, this.chart.options.elements.point.hoverRadius);
			model.backgroundColor = custom.hoverBackgroundColor ? custom.hoverBackgroundColor : helpers.getValueAtIndexOrDefault(dataset.pointHoverBackgroundColor, index, helpers.getHoverColor(model.backgroundColor));
			model.borderColor = custom.hoverBorderColor ? custom.hoverBorderColor : helpers.getValueAtIndexOrDefault(dataset.pointHoverBorderColor, index, helpers.getHoverColor(model.borderColor));
			model.borderWidth = custom.hoverBorderWidth ? custom.hoverBorderWidth : helpers.getValueAtIndexOrDefault(dataset.pointHoverBorderWidth, index, model.borderWidth);
		},

		removeHoverStyle: function(point) {
			var dataset = this.chart.data.datasets[point._datasetIndex];
			var custom = point.custom || {};
			var index = point._index;
			var model = point._model;
			var pointElementOptions = this.chart.options.elements.point;

			model.radius = custom.radius ? custom.radius : helpers.getValueAtIndexOrDefault(dataset.radius, index, pointElementOptions.radius);
			model.backgroundColor = custom.backgroundColor ? custom.backgroundColor : helpers.getValueAtIndexOrDefault(dataset.pointBackgroundColor, index, pointElementOptions.backgroundColor);
			model.borderColor = custom.borderColor ? custom.borderColor : helpers.getValueAtIndexOrDefault(dataset.pointBorderColor, index, pointElementOptions.borderColor);
			model.borderWidth = custom.borderWidth ? custom.borderWidth : helpers.getValueAtIndexOrDefault(dataset.pointBorderWidth, index, pointElementOptions.borderWidth);
		}
	});
};

},{}],21:[function(require,module,exports){
/*global window: false */
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.global.animation = {
		duration: 1000,
		easing: "easeOutQuart",
		onProgress: helpers.noop,
		onComplete: helpers.noop
	};

	Chart.Animation = Chart.Element.extend({
		currentStep: null, // the current animation step
		numSteps: 60, // default number of steps
		easing: "", // the easing to use for this animation
		render: null, // render function used by the animation service

		onAnimationProgress: null, // user specified callback to fire on each step of the animation
		onAnimationComplete: null // user specified callback to fire when the animation finishes
	});

	Chart.animationService = {
		frameDuration: 17,
		animations: [],
		dropFrames: 0,
		request: null,
		addAnimation: function(chartInstance, animationObject, duration, lazy) {
			var me = this;

			if (!lazy) {
				chartInstance.animating = true;
			}

			for (var index = 0; index < me.animations.length; ++index) {
				if (me.animations[index].chartInstance === chartInstance) {
					// replacing an in progress animation
					me.animations[index].animationObject = animationObject;
					return;
				}
			}

			me.animations.push({
				chartInstance: chartInstance,
				animationObject: animationObject
			});

			// If there are no animations queued, manually kickstart a digest, for lack of a better word
			if (me.animations.length === 1) {
				me.requestAnimationFrame();
			}
		},
		// Cancel the animation for a given chart instance
		cancelAnimation: function(chartInstance) {
			var index = helpers.findIndex(this.animations, function(animationWrapper) {
				return animationWrapper.chartInstance === chartInstance;
			});

			if (index !== -1) {
				this.animations.splice(index, 1);
				chartInstance.animating = false;
			}
		},
		requestAnimationFrame: function() {
			var me = this;
			if (me.request === null) {
				// Skip animation frame requests until the active one is executed.
				// This can happen when processing mouse events, e.g. 'mousemove'
				// and 'mouseout' events will trigger multiple renders.
				me.request = helpers.requestAnimFrame.call(window, function() {
					me.request = null;
					me.startDigest();
				});
			}
		},
		startDigest: function() {
			var me = this;

			var startTime = Date.now();
			var framesToDrop = 0;

			if (me.dropFrames > 1) {
				framesToDrop = Math.floor(me.dropFrames);
				me.dropFrames = me.dropFrames % 1;
			}

			var i = 0;
			while (i < me.animations.length) {
				if (me.animations[i].animationObject.currentStep === null) {
					me.animations[i].animationObject.currentStep = 0;
				}

				me.animations[i].animationObject.currentStep += 1 + framesToDrop;

				if (me.animations[i].animationObject.currentStep > me.animations[i].animationObject.numSteps) {
					me.animations[i].animationObject.currentStep = me.animations[i].animationObject.numSteps;
				}

				me.animations[i].animationObject.render(me.animations[i].chartInstance, me.animations[i].animationObject);
				if (me.animations[i].animationObject.onAnimationProgress && me.animations[i].animationObject.onAnimationProgress.call) {
					me.animations[i].animationObject.onAnimationProgress.call(me.animations[i].chartInstance, me.animations[i]);
				}

				if (me.animations[i].animationObject.currentStep === me.animations[i].animationObject.numSteps) {
					if (me.animations[i].animationObject.onAnimationComplete && me.animations[i].animationObject.onAnimationComplete.call) {
						me.animations[i].animationObject.onAnimationComplete.call(me.animations[i].chartInstance, me.animations[i]);
					}

					// executed the last frame. Remove the animation.
					me.animations[i].chartInstance.animating = false;

					me.animations.splice(i, 1);
				} else {
					++i;
				}
			}

			var endTime = Date.now();
			var dropFrames = (endTime - startTime) / me.frameDuration;

			me.dropFrames += dropFrames;

			// Do we have more stuff to animate?
			if (me.animations.length > 0) {
				me.requestAnimationFrame();
			}
		}
	};
};
},{}],22:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;
	//Create a dictionary of chart types, to allow for extension of existing types
	Chart.types = {};

	//Store a reference to each instance - allowing us to globally resize chart instances on window resize.
	//Destroy method on the chart will remove the instance of the chart from this reference.
	Chart.instances = {};

	// Controllers available for dataset visualization eg. bar, line, slice, etc.
	Chart.controllers = {};

	/**
	 * @class Chart.Controller
	 * The main controller of a chart.
	 */
	Chart.Controller = function(instance) {

		this.chart = instance;
		this.config = instance.config;
		this.options = this.config.options = helpers.configMerge(Chart.defaults.global, Chart.defaults[this.config.type], this.config.options || {});
		this.id = helpers.uid();

		Object.defineProperty(this, 'data', {
			get: function() {
				return this.config.data;
			}
		});

		//Add the chart instance to the global namespace
		Chart.instances[this.id] = this;

		if (this.options.responsive) {
			// Silent resize before chart draws
			this.resize(true);
		}

		this.initialize();

		return this;
	};

	helpers.extend(Chart.Controller.prototype, /** @lends Chart.Controller */ {

		initialize: function initialize() {
			var me = this;
			// Before init plugin notification
			Chart.plugins.notify('beforeInit', [me]);

			me.bindEvents();

			// Make sure controllers are built first so that each dataset is bound to an axis before the scales
			// are built
			me.ensureScalesHaveIDs();
			me.buildOrUpdateControllers();
			me.buildScales();
			me.updateLayout();
			me.resetElements();
			me.initToolTip();
			me.update();

			// After init plugin notification
			Chart.plugins.notify('afterInit', [me]);

			return me;
		},

		clear: function clear() {
			helpers.clear(this.chart);
			return this;
		},

		stop: function stop() {
			// Stops any current animation loop occuring
			Chart.animationService.cancelAnimation(this);
			return this;
		},

		resize: function resize(silent) {
			var me = this;
			var chart = me.chart;
			var canvas = chart.canvas;
			var newWidth = helpers.getMaximumWidth(canvas);
			var aspectRatio = chart.aspectRatio;
			var newHeight = (me.options.maintainAspectRatio && isNaN(aspectRatio) === false && isFinite(aspectRatio) && aspectRatio !== 0) ? newWidth / aspectRatio : helpers.getMaximumHeight(canvas);

			var sizeChanged = chart.width !== newWidth || chart.height !== newHeight;

			if (!sizeChanged) {
				return me;
			}

			canvas.width = chart.width = newWidth;
			canvas.height = chart.height = newHeight;

			helpers.retinaScale(chart);

			// Notify any plugins about the resize
			var newSize = { width: newWidth, height: newHeight };
			Chart.plugins.notify('resize', [me, newSize]);

			// Notify of resize
			if (me.options.onResize) {
				me.options.onResize(me, newSize);
			}

			if (!silent) {
				me.stop();
				me.update(me.options.responsiveAnimationDuration);
			}

			return me;
		},

		ensureScalesHaveIDs: function ensureScalesHaveIDs() {
			var options = this.options;
			var scalesOptions = options.scales || {};
			var scaleOptions = options.scale;

			helpers.each(scalesOptions.xAxes, function(xAxisOptions, index) {
				xAxisOptions.id = xAxisOptions.id || ('x-axis-' + index);
			});

			helpers.each(scalesOptions.yAxes, function(yAxisOptions, index) {
				yAxisOptions.id = yAxisOptions.id || ('y-axis-' + index);
			});

			if (scaleOptions) {
				scaleOptions.id = scaleOptions.id || 'scale';
			}
		},

		/**
		 * Builds a map of scale ID to scale object for future lookup.
		 */
		buildScales: function buildScales() {
			var me = this;
			var options = me.options;
			var scales = me.scales = {};
			var items = [];

			if (options.scales) {
				items = items.concat(
					(options.scales.xAxes || []).map(function(xAxisOptions) {
						return { options: xAxisOptions, dtype: 'category' }; }),
					(options.scales.yAxes || []).map(function(yAxisOptions) {
						return { options: yAxisOptions, dtype: 'linear' }; }));
			}

			if (options.scale) {
				items.push({ options: options.scale, dtype: 'radialLinear', isDefault: true });
			}

			helpers.each(items, function(item, index) {
				var scaleOptions = item.options;
				var scaleType = helpers.getValueOrDefault(scaleOptions.type, item.dtype);
				var scaleClass = Chart.scaleService.getScaleConstructor(scaleType);
				if (!scaleClass) {
					return;
				}

				var scale = new scaleClass({
					id: scaleOptions.id,
					options: scaleOptions,
					ctx: me.chart.ctx,
					chart: me
				});

				scales[scale.id] = scale;

				// TODO(SB): I think we should be able to remove this custom case (options.scale)
				// and consider it as a regular scale part of the "scales"" map only! This would
				// make the logic easier and remove some useless? custom code.
				if (item.isDefault) {
					me.scale = scale;
				}
			});

			Chart.scaleService.addScalesToLayout(this);
		},

		updateLayout: function() {
			Chart.layoutService.update(this, this.chart.width, this.chart.height);
		},

		buildOrUpdateControllers: function buildOrUpdateControllers() {
			var me = this;
			var types = [];
			var newControllers = [];

			helpers.each(me.data.datasets, function(dataset, datasetIndex) {
				var meta = me.getDatasetMeta(datasetIndex);
				if (!meta.type) {
					meta.type = dataset.type || me.config.type;
				}

				types.push(meta.type);

				if (meta.controller) {
					meta.controller.updateIndex(datasetIndex);
				} else {
					meta.controller = new Chart.controllers[meta.type](me, datasetIndex);
					newControllers.push(meta.controller);
				}
			}, me);

			if (types.length > 1) {
				for (var i = 1; i < types.length; i++) {
					if (types[i] !== types[i - 1]) {
						me.isCombo = true;
						break;
					}
				}
			}

			return newControllers;
		},

		resetElements: function resetElements() {
			var me = this;
			helpers.each(me.data.datasets, function(dataset, datasetIndex) {
				me.getDatasetMeta(datasetIndex).controller.reset();
			}, me);
		},

		update: function update(animationDuration, lazy) {
			var me = this;
			Chart.plugins.notify('beforeUpdate', [me]);

			// In case the entire data object changed
			me.tooltip._data = me.data;

			// Make sure dataset controllers are updated and new controllers are reset
			var newControllers = me.buildOrUpdateControllers();

			// Make sure all dataset controllers have correct meta data counts
			helpers.each(me.data.datasets, function(dataset, datasetIndex) {
				me.getDatasetMeta(datasetIndex).controller.buildOrUpdateElements();
			}, me);

			Chart.layoutService.update(me, me.chart.width, me.chart.height);

			// Apply changes to the dataets that require the scales to have been calculated i.e BorderColor chages
			Chart.plugins.notify('afterScaleUpdate', [me]);

			// Can only reset the new controllers after the scales have been updated
			helpers.each(newControllers, function(controller) {
				controller.reset();
			});

			me.updateDatasets();

			// Do this before render so that any plugins that need final scale updates can use it
			Chart.plugins.notify('afterUpdate', [me]);

			me.render(animationDuration, lazy);
		},

		/**
		 * @method beforeDatasetsUpdate
		 * @description Called before all datasets are updated. If a plugin returns false,
		 * the datasets update will be cancelled until another chart update is triggered.
		 * @param {Object} instance the chart instance being updated.
		 * @returns {Boolean} false to cancel the datasets update.
		 * @memberof Chart.PluginBase
		 * @since version 2.1.5
		 * @instance
		 */

		/**
		 * @method afterDatasetsUpdate
		 * @description Called after all datasets have been updated. Note that this
		 * extension will not be called if the datasets update has been cancelled.
		 * @param {Object} instance the chart instance being updated.
		 * @memberof Chart.PluginBase
		 * @since version 2.1.5
		 * @instance
		 */

		/**
		 * Updates all datasets unless a plugin returns false to the beforeDatasetsUpdate
		 * extension, in which case no datasets will be updated and the afterDatasetsUpdate
		 * notification will be skipped.
		 * @protected
		 * @instance
		 */
		updateDatasets: function() {
			var me = this;
			var i, ilen;

			if (Chart.plugins.notify('beforeDatasetsUpdate', [ me ])) {
				for (i = 0, ilen = me.data.datasets.length; i < ilen; ++i) {
					me.getDatasetMeta(i).controller.update();
				}

				Chart.plugins.notify('afterDatasetsUpdate', [ me ]);
			}
		},

		render: function render(duration, lazy) {
			var me = this;
			Chart.plugins.notify('beforeRender', [me]);

			var animationOptions = me.options.animation;
			if (animationOptions && ((typeof duration !== 'undefined' && duration !== 0) || (typeof duration === 'undefined' && animationOptions.duration !== 0))) {
				var animation = new Chart.Animation();
				animation.numSteps = (duration || animationOptions.duration) / 16.66; //60 fps
				animation.easing = animationOptions.easing;

				// render function
				animation.render = function(chartInstance, animationObject) {
					var easingFunction = helpers.easingEffects[animationObject.easing];
					var stepDecimal = animationObject.currentStep / animationObject.numSteps;
					var easeDecimal = easingFunction(stepDecimal);

					chartInstance.draw(easeDecimal, stepDecimal, animationObject.currentStep);
				};

				// user events
				animation.onAnimationProgress = animationOptions.onProgress;
				animation.onAnimationComplete = animationOptions.onComplete;

				Chart.animationService.addAnimation(me, animation, duration, lazy);
			} else {
				me.draw();
				if (animationOptions && animationOptions.onComplete && animationOptions.onComplete.call) {
					animationOptions.onComplete.call(me);
				}
			}
			return me;
		},

		draw: function(ease) {
			var me = this;
			var easingDecimal = ease || 1;
			me.clear();

			Chart.plugins.notify('beforeDraw', [me, easingDecimal]);

			// Draw all the scales
			helpers.each(me.boxes, function(box) {
				box.draw(me.chartArea);
			}, me);
			if (me.scale) {
				me.scale.draw();
			}

			Chart.plugins.notify('beforeDatasetsDraw', [me, easingDecimal]);

			// Draw each dataset via its respective controller (reversed to support proper line stacking)
			helpers.each(me.data.datasets, function(dataset, datasetIndex) {
				if (me.isDatasetVisible(datasetIndex)) {
					me.getDatasetMeta(datasetIndex).controller.draw(ease);
				}
			}, me, true);

			Chart.plugins.notify('afterDatasetsDraw', [me, easingDecimal]);

			// Finally draw the tooltip
			me.tooltip.transition(easingDecimal).draw();

			Chart.plugins.notify('afterDraw', [me, easingDecimal]);
		},

		// Get the single element that was clicked on
		// @return : An object containing the dataset index and element index of the matching element. Also contains the rectangle that was draw
		getElementAtEvent: function(e) {
			var me = this;
			var eventPosition = helpers.getRelativePosition(e, me.chart);
			var elementsArray = [];

			helpers.each(me.data.datasets, function(dataset, datasetIndex) {
				if (me.isDatasetVisible(datasetIndex)) {
					var meta = me.getDatasetMeta(datasetIndex);
					helpers.each(meta.data, function(element, index) {
						if (element.inRange(eventPosition.x, eventPosition.y)) {
							elementsArray.push(element);
							return elementsArray;
						}
					});
				}
			});

			return elementsArray;
		},

		getElementsAtEvent: function(e) {
			var me = this;
			var eventPosition = helpers.getRelativePosition(e, me.chart);
			var elementsArray = [];

			var found = (function() {
				if (me.data.datasets) {
					for (var i = 0; i < me.data.datasets.length; i++) {
						var meta = me.getDatasetMeta(i);
						if (me.isDatasetVisible(i)) {
							for (var j = 0; j < meta.data.length; j++) {
								if (meta.data[j].inRange(eventPosition.x, eventPosition.y)) {
									return meta.data[j];
								}
							}
						}
					}
				}
			}).call(me);

			if (!found) {
				return elementsArray;
			}

			helpers.each(me.data.datasets, function(dataset, datasetIndex) {
				if (me.isDatasetVisible(datasetIndex)) {
					var meta = me.getDatasetMeta(datasetIndex);
					elementsArray.push(meta.data[found._index]);
				}
			}, me);

			return elementsArray;
		},

		getElementsAtEventForMode: function(e, mode) {
			var me = this;
			switch (mode) {
			case 'single':
				return me.getElementAtEvent(e);
			case 'label':
				return me.getElementsAtEvent(e);
			case 'dataset':
				return me.getDatasetAtEvent(e);
			default:
				return e;
			}
		},

		getDatasetAtEvent: function(e) {
			var elementsArray = this.getElementAtEvent(e);

			if (elementsArray.length > 0) {
				elementsArray = this.getDatasetMeta(elementsArray[0]._datasetIndex).data;
			}

			return elementsArray;
		},

		getDatasetMeta: function(datasetIndex) {
			var me = this;
			var dataset = me.data.datasets[datasetIndex];
			if (!dataset._meta) {
				dataset._meta = {};
			}

			var meta = dataset._meta[me.id];
			if (!meta) {
				meta = dataset._meta[me.id] = {
				type: null,
				data: [],
				dataset: null,
				controller: null,
				hidden: null,			// See isDatasetVisible() comment
				xAxisID: null,
				yAxisID: null
			};
			}

			return meta;
		},

		getVisibleDatasetCount: function() {
			var count = 0;
			for (var i = 0, ilen = this.data.datasets.length; i<ilen; ++i) {
				 if (this.isDatasetVisible(i)) {
					count++;
				}
			}
			return count;
		},

		isDatasetVisible: function(datasetIndex) {
			var meta = this.getDatasetMeta(datasetIndex);

			// meta.hidden is a per chart dataset hidden flag override with 3 states: if true or false,
			// the dataset.hidden value is ignored, else if null, the dataset hidden state is returned.
			return typeof meta.hidden === 'boolean'? !meta.hidden : !this.data.datasets[datasetIndex].hidden;
		},

		generateLegend: function generateLegend() {
			return this.options.legendCallback(this);
		},

		destroy: function destroy() {
			var me = this;
			me.stop();
			me.clear();
			helpers.unbindEvents(me, me.events);
			helpers.removeResizeListener(me.chart.canvas.parentNode);

			// Reset canvas height/width attributes
			var canvas = me.chart.canvas;
			canvas.width = me.chart.width;
			canvas.height = me.chart.height;

			// if we scaled the canvas in response to a devicePixelRatio !== 1, we need to undo that transform here
			if (me.chart.originalDevicePixelRatio !== undefined) {
				me.chart.ctx.scale(1 / me.chart.originalDevicePixelRatio, 1 / me.chart.originalDevicePixelRatio);
			}

			// Reset to the old style since it may have been changed by the device pixel ratio changes
			canvas.style.width = me.chart.originalCanvasStyleWidth;
			canvas.style.height = me.chart.originalCanvasStyleHeight;

			Chart.plugins.notify('destroy', [me]);

			delete Chart.instances[me.id];
		},

		toBase64Image: function toBase64Image() {
			return this.chart.canvas.toDataURL.apply(this.chart.canvas, arguments);
		},

		initToolTip: function initToolTip() {
			var me = this;
			me.tooltip = new Chart.Tooltip({
				_chart: me.chart,
				_chartInstance: me,
				_data: me.data,
				_options: me.options.tooltips
			}, me);
		},

		bindEvents: function bindEvents() {
			var me = this;
			helpers.bindEvents(me, me.options.events, function(evt) {
				me.eventHandler(evt);
			});
		},

		updateHoverStyle: function(elements, mode, enabled) {
			var method = enabled? 'setHoverStyle' : 'removeHoverStyle';
			var element, i, ilen;

			switch (mode) {
			case 'single':
				elements = [ elements[0] ];
				break;
			case 'label':
			case 'dataset':
				// elements = elements;
				break;
			default:
				// unsupported mode
				return;
			}

			for (i=0, ilen=elements.length; i<ilen; ++i) {
				element = elements[i];
				if (element) {
					this.getDatasetMeta(element._datasetIndex).controller[method](element);
				}
			}
		},

		eventHandler: function eventHandler(e) {
			var me = this;
			var tooltip = me.tooltip;
			var options = me.options || {};
			var hoverOptions = options.hover;
			var tooltipsOptions = options.tooltips;

			me.lastActive = me.lastActive || [];
			me.lastTooltipActive = me.lastTooltipActive || [];

			// Find Active Elements for hover and tooltips
			if (e.type === 'mouseout') {
				me.active = [];
				me.tooltipActive = [];
			} else {
				me.active = me.getElementsAtEventForMode(e, hoverOptions.mode);
				me.tooltipActive =  me.getElementsAtEventForMode(e, tooltipsOptions.mode);
			}

			// On Hover hook
			if (hoverOptions.onHover) {
				hoverOptions.onHover.call(me, me.active);
			}

			if (e.type === 'mouseup' || e.type === 'click') {
				if (options.onClick) {
					options.onClick.call(me, e, me.active);
				}
				if (me.legend && me.legend.handleEvent) {
					me.legend.handleEvent(e);
				}
			}

			// Remove styling for last active (even if it may still be active)
			if (me.lastActive.length) {
				me.updateHoverStyle(me.lastActive, hoverOptions.mode, false);
			}

			// Built in hover styling
			if (me.active.length && hoverOptions.mode) {
				me.updateHoverStyle(me.active, hoverOptions.mode, true);
			}

			// Built in Tooltips
			if (tooltipsOptions.enabled || tooltipsOptions.custom) {
				tooltip.initialize();
				tooltip._active = me.tooltipActive;
				tooltip.update(true);
			}

			// Hover animations
			tooltip.pivot();

			if (!me.animating) {
				// If entering, leaving, or changing elements, animate the change via pivot
				if (!helpers.arrayEquals(me.active, me.lastActive) ||
					!helpers.arrayEquals(me.tooltipActive, me.lastTooltipActive)) {

					me.stop();

					if (tooltipsOptions.enabled || tooltipsOptions.custom) {
						tooltip.update(true);
					}

					// We only need to render at this point. Updating will cause scales to be
					// recomputed generating flicker & using more memory than necessary.
					me.render(hoverOptions.animationDuration, true);
				}
			}

			// Remember Last Actives
			me.lastActive = me.active;
			me.lastTooltipActive = me.tooltipActive;
			return me;
		}
	});
};

},{}],23:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;
	var noop = helpers.noop;

	// Base class for all dataset controllers (line, bar, etc)
	Chart.DatasetController = function(chart, datasetIndex) {
		this.initialize.call(this, chart, datasetIndex);
	};

	helpers.extend(Chart.DatasetController.prototype, {

		/**
		 * Element type used to generate a meta dataset (e.g. Chart.element.Line).
		 * @type {Chart.core.element}
		 */
		datasetElementType: null,

		/**
		 * Element type used to generate a meta data (e.g. Chart.element.Point).
		 * @type {Chart.core.element}
		 */
		dataElementType: null,

		initialize: function(chart, datasetIndex) {
			var me = this;
			me.chart = chart;
			me.index = datasetIndex;
			me.linkScales();
			me.addElements();
		},

		updateIndex: function(datasetIndex) {
			this.index = datasetIndex;
		},

		linkScales: function() {
			var me = this;
			var meta = me.getMeta();
			var dataset = me.getDataset();

			if (meta.xAxisID === null) {
				meta.xAxisID = dataset.xAxisID || me.chart.options.scales.xAxes[0].id;
			}
			if (meta.yAxisID === null) {
				meta.yAxisID = dataset.yAxisID || me.chart.options.scales.yAxes[0].id;
			}
		},

		getDataset: function() {
			return this.chart.data.datasets[this.index];
		},

		getMeta: function() {
			return this.chart.getDatasetMeta(this.index);
		},

		getScaleForId: function(scaleID) {
			return this.chart.scales[scaleID];
		},

		reset: function() {
			this.update(true);
		},

		createMetaDataset: function() {
			var me = this;
			var type = me.datasetElementType;
			return type && new type({
				_chart: me.chart.chart,
				_datasetIndex: me.index
			});
		},

		createMetaData: function(index) {
			var me = this;
			var type = me.dataElementType;
			return type && new type({
				_chart: me.chart.chart,
				_datasetIndex: me.index,
				_index: index
			});
		},

		addElements: function() {
			var me = this;
			var meta = me.getMeta();
			var data = me.getDataset().data || [];
			var metaData = meta.data;
			var i, ilen;

			for (i=0, ilen=data.length; i<ilen; ++i) {
				metaData[i] = metaData[i] || me.createMetaData(meta, i);
			}

			meta.dataset = meta.dataset || me.createMetaDataset();
		},

		addElementAndReset: function(index) {
			var me = this;
			var element = me.createMetaData(index);
			me.getMeta().data.splice(index, 0, element);
			me.updateElement(element, index, true);
		},

		buildOrUpdateElements: function buildOrUpdateElements() {
			// Handle the number of data points changing
			var meta = this.getMeta(),
				md = meta.data,
				numData = this.getDataset().data.length,
				numMetaData = md.length;

			// Make sure that we handle number of datapoints changing
			if (numData < numMetaData) {
				// Remove excess bars for data points that have been removed
				md.splice(numData, numMetaData - numData);
			} else if (numData > numMetaData) {
				// Add new elements
				for (var index = numMetaData; index < numData; ++index) {
					this.addElementAndReset(index);
				}
			}
		},

		update: noop,

		draw: function(ease) {
			var easingDecimal = ease || 1;
			helpers.each(this.getMeta().data, function(element, index) {
				element.transition(easingDecimal).draw();
			});
		},

		removeHoverStyle: function(element, elementOpts) {
			var dataset = this.chart.data.datasets[element._datasetIndex],
				index = element._index,
				custom = element.custom || {},
				valueOrDefault = helpers.getValueAtIndexOrDefault,
				color = helpers.color,
				model = element._model;

			model.backgroundColor = custom.backgroundColor ? custom.backgroundColor : valueOrDefault(dataset.backgroundColor, index, elementOpts.backgroundColor);
			model.borderColor = custom.borderColor ? custom.borderColor : valueOrDefault(dataset.borderColor, index, elementOpts.borderColor);
			model.borderWidth = custom.borderWidth ? custom.borderWidth : valueOrDefault(dataset.borderWidth, index, elementOpts.borderWidth);
		},

		setHoverStyle: function(element) {
			var dataset = this.chart.data.datasets[element._datasetIndex],
				index = element._index,
				custom = element.custom || {},
				valueOrDefault = helpers.getValueAtIndexOrDefault,
				color = helpers.color,
				getHoverColor = helpers.getHoverColor,
				model = element._model;

			model.backgroundColor = custom.hoverBackgroundColor ? custom.hoverBackgroundColor : valueOrDefault(dataset.hoverBackgroundColor, index, getHoverColor(model.backgroundColor));
			model.borderColor = custom.hoverBorderColor ? custom.hoverBorderColor : valueOrDefault(dataset.hoverBorderColor, index, getHoverColor(model.borderColor));
			model.borderWidth = custom.hoverBorderWidth ? custom.hoverBorderWidth : valueOrDefault(dataset.hoverBorderWidth, index, model.borderWidth);
		}
	});

	Chart.DatasetController.extend = helpers.inherits;
};
},{}],24:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

  var helpers = Chart.helpers;

  Chart.elements = {};

  Chart.Element = function(configuration) {
    helpers.extend(this, configuration);
    this.initialize.apply(this, arguments);
  };

  helpers.extend(Chart.Element.prototype, {

    initialize: function() {
      this.hidden = false;
    },

    pivot: function() {
      var me = this;
      if (!me._view) {
        me._view = helpers.clone(me._model);
      }
      me._start = helpers.clone(me._view);
      return me;
    },

    transition: function(ease) {
      var me = this;
      
      if (!me._view) {
        me._view = helpers.clone(me._model);
      }

      // No animation -> No Transition
      if (ease === 1) {
        me._view = me._model;
        me._start = null;
        return me;
      }

      if (!me._start) {
        me.pivot();
      }

      helpers.each(me._model, function(value, key) {

        if (key[0] === '_') {
          // Only non-underscored properties
        }

        // Init if doesn't exist
        else if (!me._view.hasOwnProperty(key)) {
          if (typeof value === 'number' && !isNaN(me._view[key])) {
            me._view[key] = value * ease;
          } else {
            me._view[key] = value;
          }
        }

        // No unnecessary computations
        else if (value === me._view[key]) {
          // It's the same! Woohoo!
        }

        // Color transitions if possible
        else if (typeof value === 'string') {
          try {
            var color = helpers.color(me._model[key]).mix(helpers.color(me._start[key]), ease);
            me._view[key] = color.rgbString();
          } catch (err) {
            me._view[key] = value;
          }
        }
        // Number transitions
        else if (typeof value === 'number') {
          var startVal = me._start[key] !== undefined && isNaN(me._start[key]) === false ? me._start[key] : 0;
          me._view[key] = ((me._model[key] - startVal) * ease) + startVal;
        }
        // Everything else
        else {
          me._view[key] = value;
        }
      }, me);

      return me;
    },

    tooltipPosition: function() {
      return {
        x: this._model.x,
        y: this._model.y
      };
    },

    hasValue: function() {
      return helpers.isNumber(this._model.x) && helpers.isNumber(this._model.y);
    }
  });

  Chart.Element.extend = helpers.inherits;

};

},{}],25:[function(require,module,exports){
/*global window: false */
/*global document: false */
"use strict";

var color = require(3);

module.exports = function(Chart) {
	//Global Chart helpers object for utility methods and classes
	var helpers = Chart.helpers = {};

	//-- Basic js utility methods
	helpers.each = function(loopable, callback, self, reverse) {
		// Check to see if null or undefined firstly.
		var i, len;
		if (helpers.isArray(loopable)) {
			len = loopable.length;
			if (reverse) {
				for (i = len - 1; i >= 0; i--) {
					callback.call(self, loopable[i], i);
				}
			} else {
				for (i = 0; i < len; i++) {
					callback.call(self, loopable[i], i);
				}
			}
		} else if (typeof loopable === 'object') {
			var keys = Object.keys(loopable);
			len = keys.length;
			for (i = 0; i < len; i++) {
				callback.call(self, loopable[keys[i]], keys[i]);
			}
		}
	};
	helpers.clone = function(obj) {
		var objClone = {};
		helpers.each(obj, function(value, key) {
			if (helpers.isArray(value)) {
				objClone[key] = value.slice(0);
			} else if (typeof value === 'object' && value !== null) {
				objClone[key] = helpers.clone(value);
			} else {
				objClone[key] = value;
			}
		});
		return objClone;
	};
	helpers.extend = function(base) {
		var setFn = function(value, key) { base[key] = value; };
		for (var i = 1, ilen = arguments.length; i < ilen; i++) {
			helpers.each(arguments[i], setFn);
		}
		return base;
	};
	// Need a special merge function to chart configs since they are now grouped
	helpers.configMerge = function(_base) {
		var base = helpers.clone(_base);
		helpers.each(Array.prototype.slice.call(arguments, 1), function(extension) {
			helpers.each(extension, function(value, key) {
				if (key === 'scales') {
					// Scale config merging is complex. Add out own function here for that
					base[key] = helpers.scaleMerge(base.hasOwnProperty(key) ? base[key] : {}, value);

				} else if (key === 'scale') {
					// Used in polar area & radar charts since there is only one scale
					base[key] = helpers.configMerge(base.hasOwnProperty(key) ? base[key] : {}, Chart.scaleService.getScaleDefaults(value.type), value);
				} else if (base.hasOwnProperty(key) && helpers.isArray(base[key]) && helpers.isArray(value)) {
					// In this case we have an array of objects replacing another array. Rather than doing a strict replace,
					// merge. This allows easy scale option merging
					var baseArray = base[key];

					helpers.each(value, function(valueObj, index) {

						if (index < baseArray.length) {
							if (typeof baseArray[index] === 'object' && baseArray[index] !== null && typeof valueObj === 'object' && valueObj !== null) {
								// Two objects are coming together. Do a merge of them.
								baseArray[index] = helpers.configMerge(baseArray[index], valueObj);
							} else {
								// Just overwrite in this case since there is nothing to merge
								baseArray[index] = valueObj;
							}
						} else {
							baseArray.push(valueObj); // nothing to merge
						}
					});

				} else if (base.hasOwnProperty(key) && typeof base[key] === "object" && base[key] !== null && typeof value === "object") {
					// If we are overwriting an object with an object, do a merge of the properties.
					base[key] = helpers.configMerge(base[key], value);

				} else {
					// can just overwrite the value in this case
					base[key] = value;
				}
			});
		});

		return base;
	};
	helpers.scaleMerge = function(_base, extension) {
		var base = helpers.clone(_base);

		helpers.each(extension, function(value, key) {
			if (key === 'xAxes' || key === 'yAxes') {
				// These properties are arrays of items
				if (base.hasOwnProperty(key)) {
					helpers.each(value, function(valueObj, index) {
						var axisType = helpers.getValueOrDefault(valueObj.type, key === 'xAxes' ? 'category' : 'linear');
						var axisDefaults = Chart.scaleService.getScaleDefaults(axisType);
						if (index >= base[key].length || !base[key][index].type) {
							base[key].push(helpers.configMerge(axisDefaults, valueObj));
						} else if (valueObj.type && valueObj.type !== base[key][index].type) {
							// Type changed. Bring in the new defaults before we bring in valueObj so that valueObj can override the correct scale defaults
							base[key][index] = helpers.configMerge(base[key][index], axisDefaults, valueObj);
						} else {
							// Type is the same
							base[key][index] = helpers.configMerge(base[key][index], valueObj);
						}
					});
				} else {
					base[key] = [];
					helpers.each(value, function(valueObj) {
						var axisType = helpers.getValueOrDefault(valueObj.type, key === 'xAxes' ? 'category' : 'linear');
						base[key].push(helpers.configMerge(Chart.scaleService.getScaleDefaults(axisType), valueObj));
					});
				}
			} else if (base.hasOwnProperty(key) && typeof base[key] === "object" && base[key] !== null && typeof value === "object") {
				// If we are overwriting an object with an object, do a merge of the properties.
				base[key] = helpers.configMerge(base[key], value);

			} else {
				// can just overwrite the value in this case
				base[key] = value;
			}
		});

		return base;
	};
	helpers.getValueAtIndexOrDefault = function(value, index, defaultValue) {
		if (value === undefined || value === null) {
			return defaultValue;
		}

		if (helpers.isArray(value)) {
			return index < value.length ? value[index] : defaultValue;
		}

		return value;
	};
	helpers.getValueOrDefault = function(value, defaultValue) {
		return value === undefined ? defaultValue : value;
	};
	helpers.indexOf = Array.prototype.indexOf?
		function(array, item) { return array.indexOf(item); } :
		function(array, item) {
			for (var i = 0, ilen = array.length; i < ilen; ++i) {
				if (array[i] === item) {
					return i;
				}
			}
			return -1;
		};
	helpers.where = function(collection, filterCallback) {
		if (helpers.isArray(collection) && Array.prototype.filter) {
			return collection.filter(filterCallback);
		} else {
			var filtered = [];

			helpers.each(collection, function(item) {
				if (filterCallback(item)) {
					filtered.push(item);
				}
			});

			return filtered;
		}
	};
	helpers.findIndex = Array.prototype.findIndex?
		function(array, callback, scope) { return array.findIndex(callback, scope); } :
		function(array, callback, scope) {
			scope = scope === undefined? array : scope;
			for (var i = 0, ilen = array.length; i < ilen; ++i) {
				if (callback.call(scope, array[i], i, array)) {
					return i;
				}
			}
			return -1;
		};
	helpers.findNextWhere = function(arrayToSearch, filterCallback, startIndex) {
		// Default to start of the array
		if (startIndex === undefined || startIndex === null) {
			startIndex = -1;
		}
		for (var i = startIndex + 1; i < arrayToSearch.length; i++) {
			var currentItem = arrayToSearch[i];
			if (filterCallback(currentItem)) {
				return currentItem;
			}
		}
	};
	helpers.findPreviousWhere = function(arrayToSearch, filterCallback, startIndex) {
		// Default to end of the array
		if (startIndex === undefined || startIndex === null) {
			startIndex = arrayToSearch.length;
		}
		for (var i = startIndex - 1; i >= 0; i--) {
			var currentItem = arrayToSearch[i];
			if (filterCallback(currentItem)) {
				return currentItem;
			}
		}
	};
	helpers.inherits = function(extensions) {
		//Basic javascript inheritance based on the model created in Backbone.js
		var parent = this;
		var ChartElement = (extensions && extensions.hasOwnProperty("constructor")) ? extensions.constructor : function() {
			return parent.apply(this, arguments);
		};

		var Surrogate = function() {
			this.constructor = ChartElement;
		};
		Surrogate.prototype = parent.prototype;
		ChartElement.prototype = new Surrogate();

		ChartElement.extend = helpers.inherits;

		if (extensions) {
			helpers.extend(ChartElement.prototype, extensions);
		}

		ChartElement.__super__ = parent.prototype;

		return ChartElement;
	};
	helpers.noop = function() {};
	helpers.uid = (function() {
		var id = 0;
		return function() {
			return id++;
		};
	})();
	//-- Math methods
	helpers.isNumber = function(n) {
		return !isNaN(parseFloat(n)) && isFinite(n);
	};
	helpers.almostEquals = function(x, y, epsilon) {
		return Math.abs(x - y) < epsilon;
	};
	helpers.max = function(array) {
		return array.reduce(function(max, value) {
			if (!isNaN(value)) {
				return Math.max(max, value);
			} else {
				return max;
			}
		}, Number.NEGATIVE_INFINITY);
	};
	helpers.min = function(array) {
		return array.reduce(function(min, value) {
			if (!isNaN(value)) {
				return Math.min(min, value);
			} else {
				return min;
			}
		}, Number.POSITIVE_INFINITY);
	};
	helpers.sign = Math.sign?
		function(x) { return Math.sign(x); } :
		function(x) {
			x = +x; // convert to a number
			if (x === 0 || isNaN(x)) {
				return x;
			}
			return x > 0 ? 1 : -1;
		};
	helpers.log10 = Math.log10?
		function(x) { return Math.log10(x); } :
		function(x) {
			return Math.log(x) / Math.LN10;
		};
	helpers.toRadians = function(degrees) {
		return degrees * (Math.PI / 180);
	};
	helpers.toDegrees = function(radians) {
		return radians * (180 / Math.PI);
	};
	// Gets the angle from vertical upright to the point about a centre.
	helpers.getAngleFromPoint = function(centrePoint, anglePoint) {
		var distanceFromXCenter = anglePoint.x - centrePoint.x,
			distanceFromYCenter = anglePoint.y - centrePoint.y,
			radialDistanceFromCenter = Math.sqrt(distanceFromXCenter * distanceFromXCenter + distanceFromYCenter * distanceFromYCenter);

		var angle = Math.atan2(distanceFromYCenter, distanceFromXCenter);

		if (angle < (-0.5 * Math.PI)) {
			angle += 2.0 * Math.PI; // make sure the returned angle is in the range of (-PI/2, 3PI/2]
		}

		return {
			angle: angle,
			distance: radialDistanceFromCenter
		};
	};
	helpers.aliasPixel = function(pixelWidth) {
		return (pixelWidth % 2 === 0) ? 0 : 0.5;
	};
	helpers.splineCurve = function(firstPoint, middlePoint, afterPoint, t) {
		//Props to Rob Spencer at scaled innovation for his post on splining between points
		//http://scaledinnovation.com/analytics/splines/aboutSplines.html

		// This function must also respect "skipped" points

		var previous = firstPoint.skip ? middlePoint : firstPoint,
			current = middlePoint,
			next = afterPoint.skip ? middlePoint : afterPoint;

		var d01 = Math.sqrt(Math.pow(current.x - previous.x, 2) + Math.pow(current.y - previous.y, 2));
		var d12 = Math.sqrt(Math.pow(next.x - current.x, 2) + Math.pow(next.y - current.y, 2));

		var s01 = d01 / (d01 + d12);
		var s12 = d12 / (d01 + d12);

		// If all points are the same, s01 & s02 will be inf
		s01 = isNaN(s01) ? 0 : s01;
		s12 = isNaN(s12) ? 0 : s12;

		var fa = t * s01; // scaling factor for triangle Ta
		var fb = t * s12;

		return {
			previous: {
				x: current.x - fa * (next.x - previous.x),
				y: current.y - fa * (next.y - previous.y)
			},
			next: {
				x: current.x + fb * (next.x - previous.x),
				y: current.y + fb * (next.y - previous.y)
			}
		};
	};
	helpers.nextItem = function(collection, index, loop) {
		if (loop) {
			return index >= collection.length - 1 ? collection[0] : collection[index + 1];
		}

		return index >= collection.length - 1 ? collection[collection.length - 1] : collection[index + 1];
	};
	helpers.previousItem = function(collection, index, loop) {
		if (loop) {
			return index <= 0 ? collection[collection.length - 1] : collection[index - 1];
		}
		return index <= 0 ? collection[0] : collection[index - 1];
	};
	// Implementation of the nice number algorithm used in determining where axis labels will go
	helpers.niceNum = function(range, round) {
		var exponent = Math.floor(helpers.log10(range));
		var fraction = range / Math.pow(10, exponent);
		var niceFraction;

		if (round) {
			if (fraction < 1.5) {
				niceFraction = 1;
			} else if (fraction < 3) {
				niceFraction = 2;
			} else if (fraction < 7) {
				niceFraction = 5;
			} else {
				niceFraction = 10;
			}
		} else {
			if (fraction <= 1.0) {
				niceFraction = 1;
			} else if (fraction <= 2) {
				niceFraction = 2;
			} else if (fraction <= 5) {
				niceFraction = 5;
			} else {
				niceFraction = 10;
			}
		}

		return niceFraction * Math.pow(10, exponent);
	};
	//Easing functions adapted from Robert Penner's easing equations
	//http://www.robertpenner.com/easing/
	var easingEffects = helpers.easingEffects = {
		linear: function(t) {
			return t;
		},
		easeInQuad: function(t) {
			return t * t;
		},
		easeOutQuad: function(t) {
			return -1 * t * (t - 2);
		},
		easeInOutQuad: function(t) {
			if ((t /= 1 / 2) < 1) {
				return 1 / 2 * t * t;
			}
			return -1 / 2 * ((--t) * (t - 2) - 1);
		},
		easeInCubic: function(t) {
			return t * t * t;
		},
		easeOutCubic: function(t) {
			return 1 * ((t = t / 1 - 1) * t * t + 1);
		},
		easeInOutCubic: function(t) {
			if ((t /= 1 / 2) < 1) {
				return 1 / 2 * t * t * t;
			}
			return 1 / 2 * ((t -= 2) * t * t + 2);
		},
		easeInQuart: function(t) {
			return t * t * t * t;
		},
		easeOutQuart: function(t) {
			return -1 * ((t = t / 1 - 1) * t * t * t - 1);
		},
		easeInOutQuart: function(t) {
			if ((t /= 1 / 2) < 1) {
				return 1 / 2 * t * t * t * t;
			}
			return -1 / 2 * ((t -= 2) * t * t * t - 2);
		},
		easeInQuint: function(t) {
			return 1 * (t /= 1) * t * t * t * t;
		},
		easeOutQuint: function(t) {
			return 1 * ((t = t / 1 - 1) * t * t * t * t + 1);
		},
		easeInOutQuint: function(t) {
			if ((t /= 1 / 2) < 1) {
				return 1 / 2 * t * t * t * t * t;
			}
			return 1 / 2 * ((t -= 2) * t * t * t * t + 2);
		},
		easeInSine: function(t) {
			return -1 * Math.cos(t / 1 * (Math.PI / 2)) + 1;
		},
		easeOutSine: function(t) {
			return 1 * Math.sin(t / 1 * (Math.PI / 2));
		},
		easeInOutSine: function(t) {
			return -1 / 2 * (Math.cos(Math.PI * t / 1) - 1);
		},
		easeInExpo: function(t) {
			return (t === 0) ? 1 : 1 * Math.pow(2, 10 * (t / 1 - 1));
		},
		easeOutExpo: function(t) {
			return (t === 1) ? 1 : 1 * (-Math.pow(2, -10 * t / 1) + 1);
		},
		easeInOutExpo: function(t) {
			if (t === 0) {
				return 0;
			}
			if (t === 1) {
				return 1;
			}
			if ((t /= 1 / 2) < 1) {
				return 1 / 2 * Math.pow(2, 10 * (t - 1));
			}
			return 1 / 2 * (-Math.pow(2, -10 * --t) + 2);
		},
		easeInCirc: function(t) {
			if (t >= 1) {
				return t;
			}
			return -1 * (Math.sqrt(1 - (t /= 1) * t) - 1);
		},
		easeOutCirc: function(t) {
			return 1 * Math.sqrt(1 - (t = t / 1 - 1) * t);
		},
		easeInOutCirc: function(t) {
			if ((t /= 1 / 2) < 1) {
				return -1 / 2 * (Math.sqrt(1 - t * t) - 1);
			}
			return 1 / 2 * (Math.sqrt(1 - (t -= 2) * t) + 1);
		},
		easeInElastic: function(t) {
			var s = 1.70158;
			var p = 0;
			var a = 1;
			if (t === 0) {
				return 0;
			}
			if ((t /= 1) === 1) {
				return 1;
			}
			if (!p) {
				p = 1 * 0.3;
			}
			if (a < Math.abs(1)) {
				a = 1;
				s = p / 4;
			} else {
				s = p / (2 * Math.PI) * Math.asin(1 / a);
			}
			return -(a * Math.pow(2, 10 * (t -= 1)) * Math.sin((t * 1 - s) * (2 * Math.PI) / p));
		},
		easeOutElastic: function(t) {
			var s = 1.70158;
			var p = 0;
			var a = 1;
			if (t === 0) {
				return 0;
			}
			if ((t /= 1) === 1) {
				return 1;
			}
			if (!p) {
				p = 1 * 0.3;
			}
			if (a < Math.abs(1)) {
				a = 1;
				s = p / 4;
			} else {
				s = p / (2 * Math.PI) * Math.asin(1 / a);
			}
			return a * Math.pow(2, -10 * t) * Math.sin((t * 1 - s) * (2 * Math.PI) / p) + 1;
		},
		easeInOutElastic: function(t) {
			var s = 1.70158;
			var p = 0;
			var a = 1;
			if (t === 0) {
				return 0;
			}
			if ((t /= 1 / 2) === 2) {
				return 1;
			}
			if (!p) {
				p = 1 * (0.3 * 1.5);
			}
			if (a < Math.abs(1)) {
				a = 1;
				s = p / 4;
			} else {
				s = p / (2 * Math.PI) * Math.asin(1 / a);
			}
			if (t < 1) {
				return -0.5 * (a * Math.pow(2, 10 * (t -= 1)) * Math.sin((t * 1 - s) * (2 * Math.PI) / p));
			}
			return a * Math.pow(2, -10 * (t -= 1)) * Math.sin((t * 1 - s) * (2 * Math.PI) / p) * 0.5 + 1;
		},
		easeInBack: function(t) {
			var s = 1.70158;
			return 1 * (t /= 1) * t * ((s + 1) * t - s);
		},
		easeOutBack: function(t) {
			var s = 1.70158;
			return 1 * ((t = t / 1 - 1) * t * ((s + 1) * t + s) + 1);
		},
		easeInOutBack: function(t) {
			var s = 1.70158;
			if ((t /= 1 / 2) < 1) {
				return 1 / 2 * (t * t * (((s *= (1.525)) + 1) * t - s));
			}
			return 1 / 2 * ((t -= 2) * t * (((s *= (1.525)) + 1) * t + s) + 2);
		},
		easeInBounce: function(t) {
			return 1 - easingEffects.easeOutBounce(1 - t);
		},
		easeOutBounce: function(t) {
			if ((t /= 1) < (1 / 2.75)) {
				return 1 * (7.5625 * t * t);
			} else if (t < (2 / 2.75)) {
				return 1 * (7.5625 * (t -= (1.5 / 2.75)) * t + 0.75);
			} else if (t < (2.5 / 2.75)) {
				return 1 * (7.5625 * (t -= (2.25 / 2.75)) * t + 0.9375);
			} else {
				return 1 * (7.5625 * (t -= (2.625 / 2.75)) * t + 0.984375);
			}
		},
		easeInOutBounce: function(t) {
			if (t < 1 / 2) {
				return easingEffects.easeInBounce(t * 2) * 0.5;
			}
			return easingEffects.easeOutBounce(t * 2 - 1) * 0.5 + 1 * 0.5;
		}
	};
	//Request animation polyfill - http://www.paulirish.com/2011/requestanimationframe-for-smart-animating/
	helpers.requestAnimFrame = (function() {
		return window.requestAnimationFrame ||
			window.webkitRequestAnimationFrame ||
			window.mozRequestAnimationFrame ||
			window.oRequestAnimationFrame ||
			window.msRequestAnimationFrame ||
			function(callback) {
				return window.setTimeout(callback, 1000 / 60);
			};
	})();
	helpers.cancelAnimFrame = (function() {
		return window.cancelAnimationFrame ||
			window.webkitCancelAnimationFrame ||
			window.mozCancelAnimationFrame ||
			window.oCancelAnimationFrame ||
			window.msCancelAnimationFrame ||
			function(callback) {
				return window.clearTimeout(callback, 1000 / 60);
			};
	})();
	//-- DOM methods
	helpers.getRelativePosition = function(evt, chart) {
		var mouseX, mouseY;
		var e = evt.originalEvent || evt,
			canvas = evt.currentTarget || evt.srcElement,
			boundingRect = canvas.getBoundingClientRect();

		var touches = e.touches;
		if (touches && touches.length > 0) {
			mouseX = touches[0].clientX;
			mouseY = touches[0].clientY;

		} else {
			mouseX = e.clientX;
			mouseY = e.clientY;
		}

		// Scale mouse coordinates into canvas coordinates
		// by following the pattern laid out by 'jerryj' in the comments of
		// http://www.html5canvastutorials.com/advanced/html5-canvas-mouse-coordinates/
		var paddingLeft = parseFloat(helpers.getStyle(canvas, 'padding-left'));
		var paddingTop = parseFloat(helpers.getStyle(canvas, 'padding-top'));
		var paddingRight = parseFloat(helpers.getStyle(canvas, 'padding-right'));
		var paddingBottom = parseFloat(helpers.getStyle(canvas, 'padding-bottom'));
		var width = boundingRect.right - boundingRect.left - paddingLeft - paddingRight;
		var height = boundingRect.bottom - boundingRect.top - paddingTop - paddingBottom;

		// We divide by the current device pixel ratio, because the canvas is scaled up by that amount in each direction. However
		// the backend model is in unscaled coordinates. Since we are going to deal with our model coordinates, we go back here
		mouseX = Math.round((mouseX - boundingRect.left - paddingLeft) / (width) * canvas.width / chart.currentDevicePixelRatio);
		mouseY = Math.round((mouseY - boundingRect.top - paddingTop) / (height) * canvas.height / chart.currentDevicePixelRatio);

		return {
			x: mouseX,
			y: mouseY
		};

	};
	helpers.addEvent = function(node, eventType, method) {
		if (node.addEventListener) {
			node.addEventListener(eventType, method);
		} else if (node.attachEvent) {
			node.attachEvent("on" + eventType, method);
		} else {
			node["on" + eventType] = method;
		}
	};
	helpers.removeEvent = function(node, eventType, handler) {
		if (node.removeEventListener) {
			node.removeEventListener(eventType, handler, false);
		} else if (node.detachEvent) {
			node.detachEvent("on" + eventType, handler);
		} else {
			node["on" + eventType] = helpers.noop;
		}
	};
	helpers.bindEvents = function(chartInstance, arrayOfEvents, handler) {
		// Create the events object if it's not already present
		var events = chartInstance.events = chartInstance.events || {};

		helpers.each(arrayOfEvents, function(eventName) {
			events[eventName] = function() {
				handler.apply(chartInstance, arguments);
			};
			helpers.addEvent(chartInstance.chart.canvas, eventName, events[eventName]);
		});
	};
	helpers.unbindEvents = function(chartInstance, arrayOfEvents) {
		var canvas = chartInstance.chart.canvas;
		helpers.each(arrayOfEvents, function(handler, eventName) {
			helpers.removeEvent(canvas, eventName, handler);
		});
	};

	// Private helper function to convert max-width/max-height values that may be percentages into a number
	function parseMaxStyle(styleValue, node, parentProperty) {
		var valueInPixels;
		if (typeof(styleValue) === 'string') {
			valueInPixels = parseInt(styleValue, 10);

			if (styleValue.indexOf('%') != -1) {
				// percentage * size in dimension
				valueInPixels = valueInPixels / 100 * node.parentNode[parentProperty];
			}
		} else {
			valueInPixels = styleValue;
		}

		return valueInPixels;
	}

	/**
	 * Returns if the given value contains an effective constraint.
	 * @private
	 */
	function isConstrainedValue(value) {
		return value !== undefined &&  value !== null && value !== 'none';
	}

	// Private helper to get a constraint dimension
	// @param domNode : the node to check the constraint on
	// @param maxStyle : the style that defines the maximum for the direction we are using (maxWidth / maxHeight)
	// @param percentageProperty : property of parent to use when calculating width as a percentage
	// @see http://www.nathanaeljones.com/blog/2013/reading-max-width-cross-browser
	function getConstraintDimension(domNode, maxStyle, percentageProperty) {
		var view = document.defaultView;
		var parentNode = domNode.parentNode;
		var constrainedNode = view.getComputedStyle(domNode)[maxStyle];
		var constrainedContainer = view.getComputedStyle(parentNode)[maxStyle];
		var hasCNode = isConstrainedValue(constrainedNode);
		var hasCContainer = isConstrainedValue(constrainedContainer);
		var infinity = Number.POSITIVE_INFINITY;

		if (hasCNode || hasCContainer) {
			return Math.min(
				hasCNode? parseMaxStyle(constrainedNode, domNode, percentageProperty) : infinity,
				hasCContainer? parseMaxStyle(constrainedContainer, parentNode, percentageProperty) : infinity);
		}

		return 'none';
	}
	// returns Number or undefined if no constraint
	helpers.getConstraintWidth = function(domNode) {
		return getConstraintDimension(domNode, 'max-width', 'clientWidth');
	};
	// returns Number or undefined if no constraint
	helpers.getConstraintHeight = function(domNode) {
		return getConstraintDimension(domNode, 'max-height', 'clientHeight');
	};
	helpers.getMaximumWidth = function(domNode) {
		var container = domNode.parentNode;
		var padding = parseInt(helpers.getStyle(container, 'padding-left')) + parseInt(helpers.getStyle(container, 'padding-right'));
		var w = container.clientWidth - padding;
		var cw = helpers.getConstraintWidth(domNode);
		return isNaN(cw)? w : Math.min(w, cw);
	};
	helpers.getMaximumHeight = function(domNode) {
		var container = domNode.parentNode;
		var padding = parseInt(helpers.getStyle(container, 'padding-top')) + parseInt(helpers.getStyle(container, 'padding-bottom'));
		var h = container.clientHeight - padding;
		var ch = helpers.getConstraintHeight(domNode);
		return isNaN(ch)? h : Math.min(h, ch);
	};
	helpers.getStyle = function(el, property) {
		return el.currentStyle ?
			el.currentStyle[property] :
			document.defaultView.getComputedStyle(el, null).getPropertyValue(property);
	};
	helpers.retinaScale = function(chart) {
		var ctx = chart.ctx;
		var canvas = chart.canvas;
		var width = canvas.width;
		var height = canvas.height;
		var pixelRatio = chart.currentDevicePixelRatio = window.devicePixelRatio || 1;

		if (pixelRatio !== 1) {
			canvas.height = height * pixelRatio;
			canvas.width = width * pixelRatio;
			ctx.scale(pixelRatio, pixelRatio);

			// Store the device pixel ratio so that we can go backwards in `destroy`.
			// The devicePixelRatio changes with zoom, so there are no guarantees that it is the same
			// when destroy is called
			chart.originalDevicePixelRatio = chart.originalDevicePixelRatio || pixelRatio;
		}

		canvas.style.width = width + 'px';
		canvas.style.height = height + 'px';
	};
	//-- Canvas methods
	helpers.clear = function(chart) {
		chart.ctx.clearRect(0, 0, chart.width, chart.height);
	};
	helpers.fontString = function(pixelSize, fontStyle, fontFamily) {
		return fontStyle + " " + pixelSize + "px " + fontFamily;
	};
	helpers.longestText = function(ctx, font, arrayOfThings, cache) {
		cache = cache || {};
		var data = cache.data = cache.data || {};
		var gc = cache.garbageCollect = cache.garbageCollect || [];

		if (cache.font !== font) {
			data = cache.data = {};
			gc = cache.garbageCollect = [];
			cache.font = font;
		}

		ctx.font = font;
		var longest = 0;
		helpers.each(arrayOfThings, function(thing) {
			// Undefined strings and arrays should not be measured
			if (thing !== undefined && thing !== null && helpers.isArray(thing) !== true) {
				longest = helpers.measureText(ctx, data, gc, longest, thing);
			} else if (helpers.isArray(thing)) {
				// if it is an array lets measure each element
				// to do maybe simplify this function a bit so we can do this more recursively?
				helpers.each(thing, function(nestedThing) {
					// Undefined strings and arrays should not be measured
					if (nestedThing !== undefined && nestedThing !== null && !helpers.isArray(nestedThing)) {
						longest = helpers.measureText(ctx, data, gc, longest, nestedThing);
					}
				});
			}
		});

		var gcLen = gc.length / 2;
		if (gcLen > arrayOfThings.length) {
			for (var i = 0; i < gcLen; i++) {
				delete data[gc[i]];
			}
			gc.splice(0, gcLen);
		}
		return longest;
	};
	helpers.measureText = function (ctx, data, gc, longest, string) {
		var textWidth = data[string];
		if (!textWidth) {
			textWidth = data[string] = ctx.measureText(string).width;
			gc.push(string);
		}
		if (textWidth > longest) {
			longest = textWidth;
		}
		return longest;
	};
	helpers.numberOfLabelLines = function(arrayOfThings) {
		var numberOfLines = 1;
		helpers.each(arrayOfThings, function(thing) {
			if (helpers.isArray(thing)) {
				if (thing.length > numberOfLines) {
					numberOfLines = thing.length;
				}
			}
		});
		return numberOfLines;
	};
	helpers.drawRoundedRectangle = function(ctx, x, y, width, height, radius) {
		ctx.beginPath();
		ctx.moveTo(x + radius, y);
		ctx.lineTo(x + width - radius, y);
		ctx.quadraticCurveTo(x + width, y, x + width, y + radius);
		ctx.lineTo(x + width, y + height - radius);
		ctx.quadraticCurveTo(x + width, y + height, x + width - radius, y + height);
		ctx.lineTo(x + radius, y + height);
		ctx.quadraticCurveTo(x, y + height, x, y + height - radius);
		ctx.lineTo(x, y + radius);
		ctx.quadraticCurveTo(x, y, x + radius, y);
		ctx.closePath();
	};
	helpers.color = function(c) {
		if (!color) {
			console.log('Color.js not found!');
			return c;
		}

		/* global CanvasGradient */
		if (c instanceof CanvasGradient) {
			return color(Chart.defaults.global.defaultColor);
		}

		return color(c);
	};
	helpers.addResizeListener = function(node, callback) {
		// Hide an iframe before the node
		var hiddenIframe = document.createElement('iframe');
		var hiddenIframeClass = 'chartjs-hidden-iframe';

		if (hiddenIframe.classlist) {
			// can use classlist
			hiddenIframe.classlist.add(hiddenIframeClass);
		} else {
			hiddenIframe.setAttribute('class', hiddenIframeClass);
		}

		// Set the style
		var style = hiddenIframe.style;
		style.width = '100%';
		style.display = 'block';
		style.border = 0;
		style.height = 0;
		style.margin = 0;
		style.position = 'absolute';
		style.left = 0;
		style.right = 0;
		style.top = 0;
		style.bottom = 0;

		// Insert the iframe so that contentWindow is available
		node.insertBefore(hiddenIframe, node.firstChild);

		(hiddenIframe.contentWindow || hiddenIframe).onresize = function() {
			if (callback) {
				callback();
			}
		};
	};
	helpers.removeResizeListener = function(node) {
		var hiddenIframe = node.querySelector('.chartjs-hidden-iframe');

		// Remove the resize detect iframe
		if (hiddenIframe) {
			hiddenIframe.parentNode.removeChild(hiddenIframe);
		}
	};
	helpers.isArray = Array.isArray?
		function(obj) { return Array.isArray(obj); } :
		function(obj) {
			return Object.prototype.toString.call(obj) === '[object Array]';
		};
	//! @see http://stackoverflow.com/a/14853974
	helpers.arrayEquals = function(a0, a1) {
		var i, ilen, v0, v1;

		if (!a0 || !a1 || a0.length != a1.length) {
			return false;
		}

		for (i = 0, ilen=a0.length; i < ilen; ++i) {
			v0 = a0[i];
			v1 = a1[i];

			if (v0 instanceof Array && v1 instanceof Array) {
				if (!helpers.arrayEquals(v0, v1)) {
					return false;
				}
			} else if (v0 != v1) {
				// NOTE: two different object instances will never be equal: {x:20} != {x:20}
				return false;
			}
		}

		return true;
	};
	helpers.callCallback = function(fn, args, _tArg) {
		if (fn && typeof fn.call === 'function') {
			fn.apply(_tArg, args);
		}
	};
	helpers.getHoverColor = function(color) {
		/* global CanvasPattern */
		return (color instanceof CanvasPattern) ?
			color :
			helpers.color(color).saturate(0.5).darken(0.1).rgbString();
	};
};

},{"3":3}],26:[function(require,module,exports){
"use strict";

module.exports = function() {

	//Occupy the global variable of Chart, and create a simple base class
	var Chart = function(context, config) {
		var me = this;
		var helpers = Chart.helpers;
		me.config = config;

		// Support a jQuery'd canvas element
		if (context.length && context[0].getContext) {
			context = context[0];
		}

		// Support a canvas domnode
		if (context.getContext) {
			context = context.getContext("2d");
		}

		me.ctx = context;
		me.canvas = context.canvas;

		context.canvas.style.display = context.canvas.style.display || 'block';

		// Figure out what the size of the chart will be.
		// If the canvas has a specified width and height, we use those else
		// we look to see if the canvas node has a CSS width and height.
		// If there is still no height, fill the parent container
		me.width = context.canvas.width || parseInt(helpers.getStyle(context.canvas, 'width'), 10) || helpers.getMaximumWidth(context.canvas);
		me.height = context.canvas.height || parseInt(helpers.getStyle(context.canvas, 'height'), 10) || helpers.getMaximumHeight(context.canvas);

		me.aspectRatio = me.width / me.height;

		if (isNaN(me.aspectRatio) || isFinite(me.aspectRatio) === false) {
			// If the canvas has no size, try and figure out what the aspect ratio will be.
			// Some charts prefer square canvases (pie, radar, etc). If that is specified, use that
			// else use the canvas default ratio of 2
			me.aspectRatio = config.aspectRatio !== undefined ? config.aspectRatio : 2;
		}

		// Store the original style of the element so we can set it back
		me.originalCanvasStyleWidth = context.canvas.style.width;
		me.originalCanvasStyleHeight = context.canvas.style.height;

		// High pixel density displays - multiply the size of the canvas height/width by the device pixel ratio, then scale.
		helpers.retinaScale(me);

		if (config) {
			me.controller = new Chart.Controller(me);
		}

		// Always bind this so that if the responsive state changes we still work
		helpers.addResizeListener(context.canvas.parentNode, function() {
			if (me.controller && me.controller.config.options.responsive) {
				me.controller.resize();
			}
		});

		return me.controller ? me.controller : me;

	};

	//Globally expose the defaults to allow for user updating/changing
	Chart.defaults = {
		global: {
			responsive: true,
			responsiveAnimationDuration: 0,
			maintainAspectRatio: true,
			events: ["mousemove", "mouseout", "click", "touchstart", "touchmove"],
			hover: {
				onHover: null,
				mode: 'single',
				animationDuration: 400
			},
			onClick: null,
			defaultColor: 'rgba(0,0,0,0.1)',
			defaultFontColor: '#666',
			defaultFontFamily: "'Helvetica Neue', 'Helvetica', 'Arial', sans-serif",
			defaultFontSize: 12,
			defaultFontStyle: 'normal',
			showLines: true,

			// Element defaults defined in element extensions
			elements: {},

			// Legend callback string
			legendCallback: function(chart) {
				var text = [];
				text.push('<ul class="' + chart.id + '-legend">');
				for (var i = 0; i < chart.data.datasets.length; i++) {
					text.push('<li><span style="background-color:' + chart.data.datasets[i].backgroundColor + '"></span>');
					if (chart.data.datasets[i].label) {
						text.push(chart.data.datasets[i].label);
					}
					text.push('</li>');
				}
				text.push('</ul>');

				return text.join("");
			}
		}
	};

	Chart.Chart = Chart;

	return Chart;

};

},{}],27:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	// The layout service is very self explanatory.  It's responsible for the layout within a chart.
	// Scales, Legends and Plugins all rely on the layout service and can easily register to be placed anywhere they need
	// It is this service's responsibility of carrying out that layout.
	Chart.layoutService = {
		defaults: {},

		// Register a box to a chartInstance. A box is simply a reference to an object that requires layout. eg. Scales, Legend, Plugins.
		addBox: function(chartInstance, box) {
			if (!chartInstance.boxes) {
				chartInstance.boxes = [];
			}
			chartInstance.boxes.push(box);
		},

		removeBox: function(chartInstance, box) {
			if (!chartInstance.boxes) {
				return;
			}
			chartInstance.boxes.splice(chartInstance.boxes.indexOf(box), 1);
		},

		// The most important function
		update: function(chartInstance, width, height) {

			if (!chartInstance) {
				return;
			}

			var xPadding = 0;
			var yPadding = 0;

			var leftBoxes = helpers.where(chartInstance.boxes, function(box) {
				return box.options.position === "left";
			});
			var rightBoxes = helpers.where(chartInstance.boxes, function(box) {
				return box.options.position === "right";
			});
			var topBoxes = helpers.where(chartInstance.boxes, function(box) {
				return box.options.position === "top";
			});
			var bottomBoxes = helpers.where(chartInstance.boxes, function(box) {
				return box.options.position === "bottom";
			});

			// Boxes that overlay the chartarea such as the radialLinear scale
			var chartAreaBoxes = helpers.where(chartInstance.boxes, function(box) {
				return box.options.position === "chartArea";
			});

			// Ensure that full width boxes are at the very top / bottom
			topBoxes.sort(function(a, b) {
				return (b.options.fullWidth ? 1 : 0) - (a.options.fullWidth ? 1 : 0);
			});
			bottomBoxes.sort(function(a, b) {
				return (a.options.fullWidth ? 1 : 0) - (b.options.fullWidth ? 1 : 0);
			});

			// Essentially we now have any number of boxes on each of the 4 sides.
			// Our canvas looks like the following.
			// The areas L1 and L2 are the left axes. R1 is the right axis, T1 is the top axis and
			// B1 is the bottom axis
			// There are also 4 quadrant-like locations (left to right instead of clockwise) reserved for chart overlays
			// These locations are single-box locations only, when trying to register a chartArea location that is already taken,
			// an error will be thrown.
			//
			// |----------------------------------------------------|
			// |                  T1 (Full Width)                   |
			// |----------------------------------------------------|
			// |    |    |                 T2                  |    |
			// |    |----|-------------------------------------|----|
			// |    |    | C1 |                           | C2 |    |
			// |    |    |----|                           |----|    |
			// |    |    |                                     |    |
			// | L1 | L2 |           ChartArea (C0)            | R1 |
			// |    |    |                                     |    |
			// |    |    |----|                           |----|    |
			// |    |    | C3 |                           | C4 |    |
			// |    |----|-------------------------------------|----|
			// |    |    |                 B1                  |    |
			// |----------------------------------------------------|
			// |                  B2 (Full Width)                   |
			// |----------------------------------------------------|
			//
			// What we do to find the best sizing, we do the following
			// 1. Determine the minimum size of the chart area.
			// 2. Split the remaining width equally between each vertical axis
			// 3. Split the remaining height equally between each horizontal axis
			// 4. Give each layout the maximum size it can be. The layout will return it's minimum size
			// 5. Adjust the sizes of each axis based on it's minimum reported size.
			// 6. Refit each axis
			// 7. Position each axis in the final location
			// 8. Tell the chart the final location of the chart area
			// 9. Tell any axes that overlay the chart area the positions of the chart area

			// Step 1
			var chartWidth = width - (2 * xPadding);
			var chartHeight = height - (2 * yPadding);
			var chartAreaWidth = chartWidth / 2; // min 50%
			var chartAreaHeight = chartHeight / 2; // min 50%

			// Step 2
			var verticalBoxWidth = (width - chartAreaWidth) / (leftBoxes.length + rightBoxes.length);

			// Step 3
			var horizontalBoxHeight = (height - chartAreaHeight) / (topBoxes.length + bottomBoxes.length);

			// Step 4
			var maxChartAreaWidth = chartWidth;
			var maxChartAreaHeight = chartHeight;
			var minBoxSizes = [];

			helpers.each(leftBoxes.concat(rightBoxes, topBoxes, bottomBoxes), getMinimumBoxSize);

			function getMinimumBoxSize(box) {
				var minSize;
				var isHorizontal = box.isHorizontal();

				if (isHorizontal) {
					minSize = box.update(box.options.fullWidth ? chartWidth : maxChartAreaWidth, horizontalBoxHeight);
					maxChartAreaHeight -= minSize.height;
				} else {
					minSize = box.update(verticalBoxWidth, chartAreaHeight);
					maxChartAreaWidth -= minSize.width;
				}

				minBoxSizes.push({
					horizontal: isHorizontal,
					minSize: minSize,
					box: box
				});
			}

			// At this point, maxChartAreaHeight and maxChartAreaWidth are the size the chart area could
			// be if the axes are drawn at their minimum sizes.

			// Steps 5 & 6
			var totalLeftBoxesWidth = xPadding;
			var totalRightBoxesWidth = xPadding;
			var totalTopBoxesHeight = yPadding;
			var totalBottomBoxesHeight = yPadding;

			// Update, and calculate the left and right margins for the horizontal boxes
			helpers.each(leftBoxes.concat(rightBoxes), fitBox);

			helpers.each(leftBoxes, function(box) {
				totalLeftBoxesWidth += box.width;
			});

			helpers.each(rightBoxes, function(box) {
				totalRightBoxesWidth += box.width;
			});

			// Set the Left and Right margins for the horizontal boxes
			helpers.each(topBoxes.concat(bottomBoxes), fitBox);

			// Function to fit a box
			function fitBox(box) {
				var minBoxSize = helpers.findNextWhere(minBoxSizes, function(minBoxSize) {
					return minBoxSize.box === box;
				});

				if (minBoxSize) {
					if (box.isHorizontal()) {
						var scaleMargin = {
							left: totalLeftBoxesWidth,
							right: totalRightBoxesWidth,
							top: 0,
							bottom: 0
						};

						// Don't use min size here because of label rotation. When the labels are rotated, their rotation highly depends
						// on the margin. Sometimes they need to increase in size slightly
						box.update(box.options.fullWidth ? chartWidth : maxChartAreaWidth, chartHeight / 2, scaleMargin);
					} else {
						box.update(minBoxSize.minSize.width, maxChartAreaHeight);
					}
				}
			}

			// Figure out how much margin is on the top and bottom of the vertical boxes
			helpers.each(topBoxes, function(box) {
				totalTopBoxesHeight += box.height;
			});

			helpers.each(bottomBoxes, function(box) {
				totalBottomBoxesHeight += box.height;
			});

			// Let the left layout know the final margin
			helpers.each(leftBoxes.concat(rightBoxes), finalFitVerticalBox);

			function finalFitVerticalBox(box) {
				var minBoxSize = helpers.findNextWhere(minBoxSizes, function(minBoxSize) {
					return minBoxSize.box === box;
				});

				var scaleMargin = {
					left: 0,
					right: 0,
					top: totalTopBoxesHeight,
					bottom: totalBottomBoxesHeight
				};

				if (minBoxSize) {
					box.update(minBoxSize.minSize.width, maxChartAreaHeight, scaleMargin);
				}
			}

			// Recalculate because the size of each layout might have changed slightly due to the margins (label rotation for instance)
			totalLeftBoxesWidth = xPadding;
			totalRightBoxesWidth = xPadding;
			totalTopBoxesHeight = yPadding;
			totalBottomBoxesHeight = yPadding;

			helpers.each(leftBoxes, function(box) {
				totalLeftBoxesWidth += box.width;
			});

			helpers.each(rightBoxes, function(box) {
				totalRightBoxesWidth += box.width;
			});

			helpers.each(topBoxes, function(box) {
				totalTopBoxesHeight += box.height;
			});
			helpers.each(bottomBoxes, function(box) {
				totalBottomBoxesHeight += box.height;
			});

			// Figure out if our chart area changed. This would occur if the dataset layout label rotation
			// changed due to the application of the margins in step 6. Since we can only get bigger, this is safe to do
			// without calling `fit` again
			var newMaxChartAreaHeight = height - totalTopBoxesHeight - totalBottomBoxesHeight;
			var newMaxChartAreaWidth = width - totalLeftBoxesWidth - totalRightBoxesWidth;

			if (newMaxChartAreaWidth !== maxChartAreaWidth || newMaxChartAreaHeight !== maxChartAreaHeight) {
				helpers.each(leftBoxes, function(box) {
					box.height = newMaxChartAreaHeight;
				});

				helpers.each(rightBoxes, function(box) {
					box.height = newMaxChartAreaHeight;
				});

				helpers.each(topBoxes, function(box) {
					if (!box.options.fullWidth) {
						box.width = newMaxChartAreaWidth;
					}
				});

				helpers.each(bottomBoxes, function(box) {
					if (!box.options.fullWidth) {
						box.width = newMaxChartAreaWidth;
					}
				});

				maxChartAreaHeight = newMaxChartAreaHeight;
				maxChartAreaWidth = newMaxChartAreaWidth;
			}

			// Step 7 - Position the boxes
			var left = xPadding;
			var top = yPadding;
			var right = 0;
			var bottom = 0;

			helpers.each(leftBoxes.concat(topBoxes), placeBox);

			// Account for chart width and height
			left += maxChartAreaWidth;
			top += maxChartAreaHeight;

			helpers.each(rightBoxes, placeBox);
			helpers.each(bottomBoxes, placeBox);

			function placeBox(box) {
				if (box.isHorizontal()) {
					box.left = box.options.fullWidth ? xPadding : totalLeftBoxesWidth;
					box.right = box.options.fullWidth ? width - xPadding : totalLeftBoxesWidth + maxChartAreaWidth;
					box.top = top;
					box.bottom = top + box.height;

					// Move to next point
					top = box.bottom;

				} else {

					box.left = left;
					box.right = left + box.width;
					box.top = totalTopBoxesHeight;
					box.bottom = totalTopBoxesHeight + maxChartAreaHeight;

					// Move to next point
					left = box.right;
				}
			}

			// Step 8
			chartInstance.chartArea = {
				left: totalLeftBoxesWidth,
				top: totalTopBoxesHeight,
				right: totalLeftBoxesWidth + maxChartAreaWidth,
				bottom: totalTopBoxesHeight + maxChartAreaHeight
			};

			// Step 9
			helpers.each(chartAreaBoxes, function(box) {
				box.left = chartInstance.chartArea.left;
				box.top = chartInstance.chartArea.top;
				box.right = chartInstance.chartArea.right;
				box.bottom = chartInstance.chartArea.bottom;

				box.update(maxChartAreaWidth, maxChartAreaHeight);
			});
		}
	};
};

},{}],28:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;
	var noop = helpers.noop;

	Chart.defaults.global.legend = {

		display: true,
		position: 'top',
		fullWidth: true, // marks that this box should take the full width of the canvas (pushing down other boxes)
		reverse: false,

		// a callback that will handle
		onClick: function(e, legendItem) {
			var index = legendItem.datasetIndex;
			var ci = this.chart;
			var meta = ci.getDatasetMeta(index);

			// See controller.isDatasetVisible comment
			meta.hidden = meta.hidden === null? !ci.data.datasets[index].hidden : null;

			// We hid a dataset ... rerender the chart
			ci.update();
		},

		labels: {
			boxWidth: 40,
			padding: 10,
			// Generates labels shown in the legend
			// Valid properties to return:
			// text : text to display
			// fillStyle : fill of coloured box
			// strokeStyle: stroke of coloured box
			// hidden : if this legend item refers to a hidden item
			// lineCap : cap style for line
			// lineDash
			// lineDashOffset :
			// lineJoin :
			// lineWidth :
			generateLabels: function(chart) {
				var data = chart.data;
				return helpers.isArray(data.datasets) ? data.datasets.map(function(dataset, i) {
					return {
						text: dataset.label,
						fillStyle: (!helpers.isArray(dataset.backgroundColor) ? dataset.backgroundColor : dataset.backgroundColor[0]),
						hidden: !chart.isDatasetVisible(i),
						lineCap: dataset.borderCapStyle,
						lineDash: dataset.borderDash,
						lineDashOffset: dataset.borderDashOffset,
						lineJoin: dataset.borderJoinStyle,
						lineWidth: dataset.borderWidth,
						strokeStyle: dataset.borderColor,

						// Below is extra data used for toggling the datasets
						datasetIndex: i
					};
				}, this) : [];
			}
		}
	};

	Chart.Legend = Chart.Element.extend({

		initialize: function(config) {
			helpers.extend(this, config);

			// Contains hit boxes for each dataset (in dataset order)
			this.legendHitBoxes = [];

			// Are we in doughnut mode which has a different data type
			this.doughnutMode = false;
		},

		// These methods are ordered by lifecyle. Utilities then follow.
		// Any function defined here is inherited by all legend types.
		// Any function can be extended by the legend type

		beforeUpdate: noop,
		update: function(maxWidth, maxHeight, margins) {
			var me = this;

			// Update Lifecycle - Probably don't want to ever extend or overwrite this function ;)
			me.beforeUpdate();

			// Absorb the master measurements
			me.maxWidth = maxWidth;
			me.maxHeight = maxHeight;
			me.margins = margins;

			// Dimensions
			me.beforeSetDimensions();
			me.setDimensions();
			me.afterSetDimensions();
			// Labels
			me.beforeBuildLabels();
			me.buildLabels();
			me.afterBuildLabels();

			// Fit
			me.beforeFit();
			me.fit();
			me.afterFit();
			//
			me.afterUpdate();

			return me.minSize;
		},
		afterUpdate: noop,

		//

		beforeSetDimensions: noop,
		setDimensions: function() {
			var me = this;
			// Set the unconstrained dimension before label rotation
			if (me.isHorizontal()) {
				// Reset position before calculating rotation
				me.width = me.maxWidth;
				me.left = 0;
				me.right = me.width;
			} else {
				me.height = me.maxHeight;

				// Reset position before calculating rotation
				me.top = 0;
				me.bottom = me.height;
			}

			// Reset padding
			me.paddingLeft = 0;
			me.paddingTop = 0;
			me.paddingRight = 0;
			me.paddingBottom = 0;

			// Reset minSize
			me.minSize = {
				width: 0,
				height: 0
			};
		},
		afterSetDimensions: noop,

		//

		beforeBuildLabels: noop,
		buildLabels: function() {
			var me = this;
			me.legendItems = me.options.labels.generateLabels.call(me, me.chart);
			if(me.options.reverse){
				me.legendItems.reverse();
			}
		},
		afterBuildLabels: noop,

		//

		beforeFit: noop,
		fit: function() {
			var me = this;
			var opts = me.options;
			var labelOpts = opts.labels;
			var display = opts.display;

			var ctx = me.ctx;

			var globalDefault = Chart.defaults.global,
				itemOrDefault = helpers.getValueOrDefault,
				fontSize = itemOrDefault(labelOpts.fontSize, globalDefault.defaultFontSize),
				fontStyle = itemOrDefault(labelOpts.fontStyle, globalDefault.defaultFontStyle),
				fontFamily = itemOrDefault(labelOpts.fontFamily, globalDefault.defaultFontFamily),
				labelFont = helpers.fontString(fontSize, fontStyle, fontFamily);

			// Reset hit boxes
			var hitboxes = me.legendHitBoxes = [];

			var minSize = me.minSize;
			var isHorizontal = me.isHorizontal();

			if (isHorizontal) {
				minSize.width = me.maxWidth; // fill all the width
				minSize.height = display ? 10 : 0;
			} else {
				minSize.width = display ? 10 : 0;
				minSize.height = me.maxHeight; // fill all the height
			}

			// Increase sizes here
			if (display) {
				ctx.font = labelFont;

				if (isHorizontal) {
					// Labels

					// Width of each line of legend boxes. Labels wrap onto multiple lines when there are too many to fit on one
					var lineWidths = me.lineWidths = [0];
					var totalHeight = me.legendItems.length ? fontSize + (labelOpts.padding) : 0;

					ctx.textAlign = "left";
					ctx.textBaseline = 'top';

					helpers.each(me.legendItems, function(legendItem, i) {
						var width = labelOpts.boxWidth + (fontSize / 2) + ctx.measureText(legendItem.text).width;
						if (lineWidths[lineWidths.length - 1] + width + labelOpts.padding >= me.width) {
							totalHeight += fontSize + (labelOpts.padding);
							lineWidths[lineWidths.length] = me.left;
						}

						// Store the hitbox width and height here. Final position will be updated in `draw`
						hitboxes[i] = {
							left: 0,
							top: 0,
							width: width,
							height: fontSize
						};

						lineWidths[lineWidths.length - 1] += width + labelOpts.padding;
					});

					minSize.height += totalHeight;

				} else {
					var vPadding = labelOpts.padding;
					var columnWidths = me.columnWidths = [];
					var totalWidth = labelOpts.padding;
					var currentColWidth = 0;
					var currentColHeight = 0;
					var itemHeight = fontSize + vPadding;

					helpers.each(me.legendItems, function(legendItem, i) {
						var itemWidth = labelOpts.boxWidth + (fontSize / 2) + ctx.measureText(legendItem.text).width;

						// If too tall, go to new column
						if (currentColHeight + itemHeight > minSize.height) {
							totalWidth += currentColWidth + labelOpts.padding;
							columnWidths.push(currentColWidth); // previous column width

							currentColWidth = 0;
							currentColHeight = 0;
						}

						// Get max width
						currentColWidth = Math.max(currentColWidth, itemWidth);
						currentColHeight += itemHeight;

						// Store the hitbox width and height here. Final position will be updated in `draw`
						hitboxes[i] = {
							left: 0,
							top: 0,
							width: itemWidth,
							height: fontSize
						};
					});

					totalWidth += currentColWidth;
					columnWidths.push(currentColWidth);
					minSize.width += totalWidth;
				}
			}

			me.width = minSize.width;
			me.height = minSize.height;
		},
		afterFit: noop,

		// Shared Methods
		isHorizontal: function() {
			return this.options.position === "top" || this.options.position === "bottom";
		},

		// Actualy draw the legend on the canvas
		draw: function() {
			var me = this;
			var opts = me.options;
			var labelOpts = opts.labels;
			var globalDefault = Chart.defaults.global,
				lineDefault = globalDefault.elements.line,
				legendWidth = me.width,
				legendHeight = me.height,
				lineWidths = me.lineWidths;

			if (opts.display) {
				var ctx = me.ctx,
					cursor,
					itemOrDefault = helpers.getValueOrDefault,
					fontColor = itemOrDefault(labelOpts.fontColor, globalDefault.defaultFontColor),
					fontSize = itemOrDefault(labelOpts.fontSize, globalDefault.defaultFontSize),
					fontStyle = itemOrDefault(labelOpts.fontStyle, globalDefault.defaultFontStyle),
					fontFamily = itemOrDefault(labelOpts.fontFamily, globalDefault.defaultFontFamily),
					labelFont = helpers.fontString(fontSize, fontStyle, fontFamily);

				// Canvas setup
				ctx.textAlign = "left";
				ctx.textBaseline = 'top';
				ctx.lineWidth = 0.5;
				ctx.strokeStyle = fontColor; // for strikethrough effect
				ctx.fillStyle = fontColor; // render in correct colour
				ctx.font = labelFont;

				var boxWidth = labelOpts.boxWidth,
					hitboxes = me.legendHitBoxes;

				// current position
				var drawLegendBox = function(x, y, legendItem) {
					// Set the ctx for the box
					ctx.save();

					ctx.fillStyle = itemOrDefault(legendItem.fillStyle, globalDefault.defaultColor);
					ctx.lineCap = itemOrDefault(legendItem.lineCap, lineDefault.borderCapStyle);
					ctx.lineDashOffset = itemOrDefault(legendItem.lineDashOffset, lineDefault.borderDashOffset);
					ctx.lineJoin = itemOrDefault(legendItem.lineJoin, lineDefault.borderJoinStyle);
					ctx.lineWidth = itemOrDefault(legendItem.lineWidth, lineDefault.borderWidth);
					ctx.strokeStyle = itemOrDefault(legendItem.strokeStyle, globalDefault.defaultColor);

					if (ctx.setLineDash) {
						// IE 9 and 10 do not support line dash
						ctx.setLineDash(itemOrDefault(legendItem.lineDash, lineDefault.borderDash));
					}

					// Draw the box
					ctx.strokeRect(x, y, boxWidth, fontSize);
					ctx.fillRect(x, y, boxWidth, fontSize);

					ctx.restore();
				};
				var fillText = function(x, y, legendItem, textWidth) {
					ctx.fillText(legendItem.text, boxWidth + (fontSize / 2) + x, y);

					if (legendItem.hidden) {
						// Strikethrough the text if hidden
						ctx.beginPath();
						ctx.lineWidth = 2;
						ctx.moveTo(boxWidth + (fontSize / 2) + x, y + (fontSize / 2));
						ctx.lineTo(boxWidth + (fontSize / 2) + x + textWidth, y + (fontSize / 2));
						ctx.stroke();
					}
				};

				// Horizontal
				var isHorizontal = me.isHorizontal();
				if (isHorizontal) {
					cursor = {
						x: me.left + ((legendWidth - lineWidths[0]) / 2),
						y: me.top + labelOpts.padding,
						line: 0
					};
				} else {
					cursor = {
						x: me.left + labelOpts.padding,
						y: me.top,
						line: 0
					};
				}

				var itemHeight = fontSize + labelOpts.padding;
				helpers.each(me.legendItems, function(legendItem, i) {
					var textWidth = ctx.measureText(legendItem.text).width,
						width = boxWidth + (fontSize / 2) + textWidth,
						x = cursor.x,
						y = cursor.y;

					if (isHorizontal) {
						if (x + width >= legendWidth) {
							y = cursor.y += fontSize + (labelOpts.padding);
							cursor.line++;
							x = cursor.x = me.left + ((legendWidth - lineWidths[cursor.line]) / 2);
						}
					} else {
						if (y + itemHeight > me.bottom) {
							x = cursor.x = x + me.columnWidths[cursor.line] + labelOpts.padding;
							y = cursor.y = me.top;
							cursor.line++;
						}
					}
					

					drawLegendBox(x, y, legendItem);

					hitboxes[i].left = x;
					hitboxes[i].top = y;

					// Fill the actual label
					fillText(x, y, legendItem, textWidth);

					if (isHorizontal) {
						cursor.x += width + (labelOpts.padding);
					} else {
						cursor.y += itemHeight;
					}
					
				});
			}
		},

		// Handle an event
		handleEvent: function(e) {
			var me = this;
			var position = helpers.getRelativePosition(e, me.chart.chart),
				x = position.x,
				y = position.y,
				opts = me.options;

			if (x >= me.left && x <= me.right && y >= me.top && y <= me.bottom) {
				// See if we are touching one of the dataset boxes
				var lh = me.legendHitBoxes;
				for (var i = 0; i < lh.length; ++i) {
					var hitBox = lh[i];

					if (x >= hitBox.left && x <= hitBox.left + hitBox.width && y >= hitBox.top && y <= hitBox.top + hitBox.height) {
						// Touching an element
						if (opts.onClick) {
							opts.onClick.call(me, e, me.legendItems[i]);
						}
						break;
					}
				}
			}
		}
	});

	// Register the legend plugin
	Chart.plugins.register({
		beforeInit: function(chartInstance) {
			var opts = chartInstance.options;
			var legendOpts = opts.legend;

			if (legendOpts) {
				chartInstance.legend = new Chart.Legend({
					ctx: chartInstance.chart.ctx,
					options: legendOpts,
					chart: chartInstance
				});

				Chart.layoutService.addBox(chartInstance, chartInstance.legend);
			}
		}
	});
};

},{}],29:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var noop = Chart.helpers.noop;

	/**
	 * The plugin service singleton
	 * @namespace Chart.plugins
	 * @since 2.1.0
	 */
	Chart.plugins = {
		_plugins: [],

		/**
		 * Registers the given plugin(s) if not already registered.
		 * @param {Array|Object} plugins plugin instance(s).
		 */
		register: function(plugins) {
			var p = this._plugins;
			([]).concat(plugins).forEach(function(plugin) {
				if (p.indexOf(plugin) === -1) {
					p.push(plugin);
				}
			});
		},

		/**
		 * Unregisters the given plugin(s) only if registered.
		 * @param {Array|Object} plugins plugin instance(s).
		 */
		unregister: function(plugins) {
			var p = this._plugins;
			([]).concat(plugins).forEach(function(plugin) {
				var idx = p.indexOf(plugin);
				if (idx !== -1) {
					p.splice(idx, 1);
				}
			});
		},

		/**
		 * Remove all registered p^lugins.
		 * @since 2.1.5
		 */
		clear: function() {
			this._plugins = [];
		},

		/**
		 * Returns the number of registered plugins?
		 * @returns {Number}
		 * @since 2.1.5
		 */
		count: function() {
			return this._plugins.length;
		},

		/**
		 * Returns all registered plugin intances.
		 * @returns {Array} array of plugin objects.
		 * @since 2.1.5
		 */
		getAll: function() {
			return this._plugins;
		},

		/**
		 * Calls registered plugins on the specified extension, with the given args. This
		 * method immediately returns as soon as a plugin explicitly returns false. The
		 * returned value can be used, for instance, to interrupt the current action.
		 * @param {String} extension the name of the plugin method to call (e.g. 'beforeUpdate').
		 * @param {Array} [args] extra arguments to apply to the extension call.
		 * @returns {Boolean} false if any of the plugins return false, else returns true.
		 */
		notify: function(extension, args) {
			var plugins = this._plugins;
			var ilen = plugins.length;
			var i, plugin;

			for (i=0; i<ilen; ++i) {
				plugin = plugins[i];
				if (typeof plugin[extension] === 'function') {
					if (plugin[extension].apply(plugin, args || []) === false) {
						return false;
					}
				}
			}

			return true;
		}
	};

	/**
	 * Plugin extension methods.
	 * @interface Chart.PluginBase
	 * @since 2.1.0
	 */
	Chart.PluginBase = Chart.Element.extend({
		// Called at start of chart init
		beforeInit: noop,

		// Called at end of chart init
		afterInit: noop,

		// Called at start of update
		beforeUpdate: noop,

		// Called at end of update
		afterUpdate: noop,

		// Called at start of draw
		beforeDraw: noop,

		// Called at end of draw
		afterDraw: noop,

		// Called during destroy
		destroy: noop
	});

	/**
	 * Provided for backward compatibility, use Chart.plugins instead
	 * @namespace Chart.pluginService
	 * @deprecated since version 2.1.5
	 * @todo remove me at version 3
	 */
	Chart.pluginService = Chart.plugins;
};

},{}],30:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.scale = {
		display: true,
		position: "left",

		// grid line settings
		gridLines: {
			display: true,
			color: "rgba(0, 0, 0, 0.1)",
			lineWidth: 1,
			drawBorder: true,
			drawOnChartArea: true,
			drawTicks: true,
			tickMarkLength: 10,
			zeroLineWidth: 1,
			zeroLineColor: "rgba(0,0,0,0.25)",
			offsetGridLines: false
		},

		// scale label
		scaleLabel: {
			// actual label
			labelString: '',

			// display property
			display: false
		},

		// label settings
		ticks: {
			beginAtZero: false,
			minRotation: 0,
			maxRotation: 50,
			mirror: false,
			padding: 10,
			reverse: false,
			display: true,
			autoSkip: true,
			autoSkipPadding: 0,
			labelOffset: 0,
			// We pass through arrays to be rendered as multiline labels, we convert Others to strings here.
			callback: function(value) {
				return helpers.isArray(value) ? value : '' + value;
			}
		}
	};

	Chart.Scale = Chart.Element.extend({

		// These methods are ordered by lifecyle. Utilities then follow.
		// Any function defined here is inherited by all scale types.
		// Any function can be extended by the scale type

		beforeUpdate: function() {
			helpers.callCallback(this.options.beforeUpdate, [this]);
		},
		update: function(maxWidth, maxHeight, margins) {
			var me = this;

			// Update Lifecycle - Probably don't want to ever extend or overwrite this function ;)
			me.beforeUpdate();

			// Absorb the master measurements
			me.maxWidth = maxWidth;
			me.maxHeight = maxHeight;
			me.margins = helpers.extend({
				left: 0,
				right: 0,
				top: 0,
				bottom: 0
			}, margins);

			// Dimensions
			me.beforeSetDimensions();
			me.setDimensions();
			me.afterSetDimensions();

			// Data min/max
			me.beforeDataLimits();
			me.determineDataLimits();
			me.afterDataLimits();

			// Ticks
			me.beforeBuildTicks();
			me.buildTicks();
			me.afterBuildTicks();

			me.beforeTickToLabelConversion();
			me.convertTicksToLabels();
			me.afterTickToLabelConversion();

			// Tick Rotation
			me.beforeCalculateTickRotation();
			me.calculateTickRotation();
			me.afterCalculateTickRotation();
			// Fit
			me.beforeFit();
			me.fit();
			me.afterFit();
			//
			me.afterUpdate();

			return me.minSize;

		},
		afterUpdate: function() {
			helpers.callCallback(this.options.afterUpdate, [this]);
		},

		//

		beforeSetDimensions: function() {
			helpers.callCallback(this.options.beforeSetDimensions, [this]);
		},
		setDimensions: function() {
			var me = this;
			// Set the unconstrained dimension before label rotation
			if (me.isHorizontal()) {
				// Reset position before calculating rotation
				me.width = me.maxWidth;
				me.left = 0;
				me.right = me.width;
			} else {
				me.height = me.maxHeight;

				// Reset position before calculating rotation
				me.top = 0;
				me.bottom = me.height;
			}

			// Reset padding
			me.paddingLeft = 0;
			me.paddingTop = 0;
			me.paddingRight = 0;
			me.paddingBottom = 0;
		},
		afterSetDimensions: function() {
			helpers.callCallback(this.options.afterSetDimensions, [this]);
		},

		// Data limits
		beforeDataLimits: function() {
			helpers.callCallback(this.options.beforeDataLimits, [this]);
		},
		determineDataLimits: helpers.noop,
		afterDataLimits: function() {
			helpers.callCallback(this.options.afterDataLimits, [this]);
		},

		//
		beforeBuildTicks: function() {
			helpers.callCallback(this.options.beforeBuildTicks, [this]);
		},
		buildTicks: helpers.noop,
		afterBuildTicks: function() {
			helpers.callCallback(this.options.afterBuildTicks, [this]);
		},

		beforeTickToLabelConversion: function() {
			helpers.callCallback(this.options.beforeTickToLabelConversion, [this]);
		},
		convertTicksToLabels: function() {
			var me = this;
			// Convert ticks to strings
			me.ticks = me.ticks.map(function(numericalTick, index, ticks) {
					if (me.options.ticks.userCallback) {
						return me.options.ticks.userCallback(numericalTick, index, ticks);
					}
					return me.options.ticks.callback(numericalTick, index, ticks);
				},
				me);
		},
		afterTickToLabelConversion: function() {
			helpers.callCallback(this.options.afterTickToLabelConversion, [this]);
		},

		//

		beforeCalculateTickRotation: function() {
			helpers.callCallback(this.options.beforeCalculateTickRotation, [this]);
		},
		calculateTickRotation: function() {
			var me = this;
			var context = me.ctx;
			var globalDefaults = Chart.defaults.global;
			var optionTicks = me.options.ticks;

			//Get the width of each grid by calculating the difference
			//between x offsets between 0 and 1.
			var tickFontSize = helpers.getValueOrDefault(optionTicks.fontSize, globalDefaults.defaultFontSize);
			var tickFontStyle = helpers.getValueOrDefault(optionTicks.fontStyle, globalDefaults.defaultFontStyle);
			var tickFontFamily = helpers.getValueOrDefault(optionTicks.fontFamily, globalDefaults.defaultFontFamily);
			var tickLabelFont = helpers.fontString(tickFontSize, tickFontStyle, tickFontFamily);
			context.font = tickLabelFont;

			var firstWidth = context.measureText(me.ticks[0]).width;
			var lastWidth = context.measureText(me.ticks[me.ticks.length - 1]).width;
			var firstRotated;

			me.labelRotation = optionTicks.minRotation || 0;
			me.paddingRight = 0;
			me.paddingLeft = 0;

			if (me.options.display) {
				if (me.isHorizontal()) {
					me.paddingRight = lastWidth / 2 + 3;
					me.paddingLeft = firstWidth / 2 + 3;

					if (!me.longestTextCache) {
						me.longestTextCache = {};
					}
					var originalLabelWidth = helpers.longestText(context, tickLabelFont, me.ticks, me.longestTextCache);
					var labelWidth = originalLabelWidth;
					var cosRotation;
					var sinRotation;

					// Allow 3 pixels x2 padding either side for label readability
					// only the index matters for a dataset scale, but we want a consistent interface between scales
					var tickWidth = me.getPixelForTick(1) - me.getPixelForTick(0) - 6;

					//Max label rotation can be set or default to 90 - also act as a loop counter
					while (labelWidth > tickWidth && me.labelRotation < optionTicks.maxRotation) {
						cosRotation = Math.cos(helpers.toRadians(me.labelRotation));
						sinRotation = Math.sin(helpers.toRadians(me.labelRotation));

						firstRotated = cosRotation * firstWidth;

						// We're right aligning the text now.
						if (firstRotated + tickFontSize / 2 > me.yLabelWidth) {
							me.paddingLeft = firstRotated + tickFontSize / 2;
						}

						me.paddingRight = tickFontSize / 2;

						if (sinRotation * originalLabelWidth > me.maxHeight) {
							// go back one step
							me.labelRotation--;
							break;
						}

						me.labelRotation++;
						labelWidth = cosRotation * originalLabelWidth;
					}
				}
			}

			if (me.margins) {
				me.paddingLeft = Math.max(me.paddingLeft - me.margins.left, 0);
				me.paddingRight = Math.max(me.paddingRight - me.margins.right, 0);
			}
		},
		afterCalculateTickRotation: function() {
			helpers.callCallback(this.options.afterCalculateTickRotation, [this]);
		},

		//

		beforeFit: function() {
			helpers.callCallback(this.options.beforeFit, [this]);
		},
		fit: function() {
			var me = this;
			// Reset
			var minSize = me.minSize = {
				width: 0,
				height: 0
			};

			var opts = me.options;
			var globalDefaults = Chart.defaults.global;
			var tickOpts = opts.ticks;
			var scaleLabelOpts = opts.scaleLabel;
			var display = opts.display;
			var isHorizontal = me.isHorizontal();

			var tickFontSize = helpers.getValueOrDefault(tickOpts.fontSize, globalDefaults.defaultFontSize);
			var tickFontStyle = helpers.getValueOrDefault(tickOpts.fontStyle, globalDefaults.defaultFontStyle);
			var tickFontFamily = helpers.getValueOrDefault(tickOpts.fontFamily, globalDefaults.defaultFontFamily);
			var tickLabelFont = helpers.fontString(tickFontSize, tickFontStyle, tickFontFamily);

			var scaleLabelFontSize = helpers.getValueOrDefault(scaleLabelOpts.fontSize, globalDefaults.defaultFontSize);
			var scaleLabelFontStyle = helpers.getValueOrDefault(scaleLabelOpts.fontStyle, globalDefaults.defaultFontStyle);
			var scaleLabelFontFamily = helpers.getValueOrDefault(scaleLabelOpts.fontFamily, globalDefaults.defaultFontFamily);
			var scaleLabelFont = helpers.fontString(scaleLabelFontSize, scaleLabelFontStyle, scaleLabelFontFamily);

			var tickMarkLength = opts.gridLines.tickMarkLength;

			// Width
			if (isHorizontal) {
				// subtract the margins to line up with the chartArea if we are a full width scale
				minSize.width = me.isFullWidth() ? me.maxWidth - me.margins.left - me.margins.right : me.maxWidth;
			} else {
				minSize.width = display ? tickMarkLength : 0;
			}

			// height
			if (isHorizontal) {
				minSize.height = display ? tickMarkLength : 0;
			} else {
				minSize.height = me.maxHeight; // fill all the height
			}

			// Are we showing a title for the scale?
			if (scaleLabelOpts.display && display) {
				if (isHorizontal) {
					minSize.height += (scaleLabelFontSize * 1.5);
				} else {
					minSize.width += (scaleLabelFontSize * 1.5);
				}
			}

			if (tickOpts.display && display) {
				// Don't bother fitting the ticks if we are not showing them
				if (!me.longestTextCache) {
					me.longestTextCache = {};
				}

				var largestTextWidth = helpers.longestText(me.ctx, tickLabelFont, me.ticks, me.longestTextCache);
				var tallestLabelHeightInLines = helpers.numberOfLabelLines(me.ticks);
				var lineSpace = tickFontSize * 0.5;

				if (isHorizontal) {
					// A horizontal axis is more constrained by the height.
					me.longestLabelWidth = largestTextWidth;

					// TODO - improve this calculation
					var labelHeight = (Math.sin(helpers.toRadians(me.labelRotation)) * me.longestLabelWidth) + (tickFontSize * tallestLabelHeightInLines) + (lineSpace * tallestLabelHeightInLines);

					minSize.height = Math.min(me.maxHeight, minSize.height + labelHeight);
					me.ctx.font = tickLabelFont;

					var firstLabelWidth = me.ctx.measureText(me.ticks[0]).width;
					var lastLabelWidth = me.ctx.measureText(me.ticks[me.ticks.length - 1]).width;

					// Ensure that our ticks are always inside the canvas. When rotated, ticks are right aligned which means that the right padding is dominated
					// by the font height
					var cosRotation = Math.cos(helpers.toRadians(me.labelRotation));
					var sinRotation = Math.sin(helpers.toRadians(me.labelRotation));
					me.paddingLeft = me.labelRotation !== 0 ? (cosRotation * firstLabelWidth) + 3 : firstLabelWidth / 2 + 3; // add 3 px to move away from canvas edges
					me.paddingRight = me.labelRotation !== 0 ? (sinRotation * (tickFontSize / 2)) + 3 : lastLabelWidth / 2 + 3; // when rotated
				} else {
					// A vertical axis is more constrained by the width. Labels are the dominant factor here, so get that length first
					var maxLabelWidth = me.maxWidth - minSize.width;

					// Account for padding
					var mirror = tickOpts.mirror;
					if (!mirror) {
						largestTextWidth += me.options.ticks.padding;
					} else {
						// If mirrored text is on the inside so don't expand
						largestTextWidth = 0;
					}

					if (largestTextWidth < maxLabelWidth) {
						// We don't need all the room
						minSize.width += largestTextWidth;
					} else {
						// Expand to max size
						minSize.width = me.maxWidth;
					}

					me.paddingTop = tickFontSize / 2;
					me.paddingBottom = tickFontSize / 2;
				}
			}

			if (me.margins) {
				me.paddingLeft = Math.max(me.paddingLeft - me.margins.left, 0);
				me.paddingTop = Math.max(me.paddingTop - me.margins.top, 0);
				me.paddingRight = Math.max(me.paddingRight - me.margins.right, 0);
				me.paddingBottom = Math.max(me.paddingBottom - me.margins.bottom, 0);
			}

			me.width = minSize.width;
			me.height = minSize.height;

		},
		afterFit: function() {
			helpers.callCallback(this.options.afterFit, [this]);
		},

		// Shared Methods
		isHorizontal: function() {
			return this.options.position === "top" || this.options.position === "bottom";
		},
		isFullWidth: function() {
			return (this.options.fullWidth);
		},

		// Get the correct value. NaN bad inputs, If the value type is object get the x or y based on whether we are horizontal or not
		getRightValue: function getRightValue(rawValue) {
			// Null and undefined values first
			if (rawValue === null || typeof(rawValue) === 'undefined') {
				return NaN;
			}
			// isNaN(object) returns true, so make sure NaN is checking for a number
			if (typeof(rawValue) === 'number' && isNaN(rawValue)) {
				return NaN;
			}
			// If it is in fact an object, dive in one more level
			if (typeof(rawValue) === "object") {
				if ((rawValue instanceof Date) || (rawValue.isValid)) {
					return rawValue;
				} else {
					return getRightValue(this.isHorizontal() ? rawValue.x : rawValue.y);
				}
			}

			// Value is good, return it
			return rawValue;
		},

		// Used to get the value to display in the tooltip for the data at the given index
		// function getLabelForIndex(index, datasetIndex)
		getLabelForIndex: helpers.noop,

		// Used to get data value locations.  Value can either be an index or a numerical value
		getPixelForValue: helpers.noop,

		// Used to get the data value from a given pixel. This is the inverse of getPixelForValue
		getValueForPixel: helpers.noop,

		// Used for tick location, should
		getPixelForTick: function(index, includeOffset) {
			var me = this;
			if (me.isHorizontal()) {
				var innerWidth = me.width - (me.paddingLeft + me.paddingRight);
				var tickWidth = innerWidth / Math.max((me.ticks.length - ((me.options.gridLines.offsetGridLines) ? 0 : 1)), 1);
				var pixel = (tickWidth * index) + me.paddingLeft;

				if (includeOffset) {
					pixel += tickWidth / 2;
				}

				var finalVal = me.left + Math.round(pixel);
				finalVal += me.isFullWidth() ? me.margins.left : 0;
				return finalVal;
			} else {
				var innerHeight = me.height - (me.paddingTop + me.paddingBottom);
				return me.top + (index * (innerHeight / (me.ticks.length - 1)));
			}
		},

		// Utility for getting the pixel location of a percentage of scale
		getPixelForDecimal: function(decimal /*, includeOffset*/ ) {
			var me = this;
			if (me.isHorizontal()) {
				var innerWidth = me.width - (me.paddingLeft + me.paddingRight);
				var valueOffset = (innerWidth * decimal) + me.paddingLeft;

				var finalVal = me.left + Math.round(valueOffset);
				finalVal += me.isFullWidth() ? me.margins.left : 0;
				return finalVal;
			} else {
				return me.top + (decimal * me.height);
			}
		},

		getBasePixel: function() {
			var me = this;
			var min = me.min;
			var max = me.max;

			return me.getPixelForValue(
				me.beginAtZero? 0:
				min < 0 && max < 0? max :
				min > 0 && max > 0? min :
				0);
		},

		// Actualy draw the scale on the canvas
		// @param {rectangle} chartArea : the area of the chart to draw full grid lines on
		draw: function(chartArea) {
			var me = this;
			var options = me.options;
			if (!options.display) {
				return;
			}

			var context = me.ctx;
			var globalDefaults = Chart.defaults.global;
			var optionTicks = options.ticks;
			var gridLines = options.gridLines;
			var scaleLabel = options.scaleLabel;

			var isRotated = me.labelRotation !== 0;
			var skipRatio;
			var useAutoskipper = optionTicks.autoSkip;
			var isHorizontal = me.isHorizontal();

			// figure out the maximum number of gridlines to show
			var maxTicks;
			if (optionTicks.maxTicksLimit) {
				maxTicks = optionTicks.maxTicksLimit;
			}

			var tickFontColor = helpers.getValueOrDefault(optionTicks.fontColor, globalDefaults.defaultFontColor);
			var tickFontSize = helpers.getValueOrDefault(optionTicks.fontSize, globalDefaults.defaultFontSize);
			var tickFontStyle = helpers.getValueOrDefault(optionTicks.fontStyle, globalDefaults.defaultFontStyle);
			var tickFontFamily = helpers.getValueOrDefault(optionTicks.fontFamily, globalDefaults.defaultFontFamily);
			var tickLabelFont = helpers.fontString(tickFontSize, tickFontStyle, tickFontFamily);
			var tl = gridLines.tickMarkLength;

			var scaleLabelFontColor = helpers.getValueOrDefault(scaleLabel.fontColor, globalDefaults.defaultFontColor);
			var scaleLabelFontSize = helpers.getValueOrDefault(scaleLabel.fontSize, globalDefaults.defaultFontSize);
			var scaleLabelFontStyle = helpers.getValueOrDefault(scaleLabel.fontStyle, globalDefaults.defaultFontStyle);
			var scaleLabelFontFamily = helpers.getValueOrDefault(scaleLabel.fontFamily, globalDefaults.defaultFontFamily);
			var scaleLabelFont = helpers.fontString(scaleLabelFontSize, scaleLabelFontStyle, scaleLabelFontFamily);

			var labelRotationRadians = helpers.toRadians(me.labelRotation);
			var cosRotation = Math.cos(labelRotationRadians);
			var sinRotation = Math.sin(labelRotationRadians);
			var longestRotatedLabel = me.longestLabelWidth * cosRotation;
			var rotatedLabelHeight = tickFontSize * sinRotation;

			// Make sure we draw text in the correct color and font
			context.fillStyle = tickFontColor;

			var itemsToDraw = [];

			if (isHorizontal) {
				skipRatio = false;

                // Only calculate the skip ratio with the half width of longestRotateLabel if we got an actual rotation
                // See #2584
                if (isRotated) {
                    longestRotatedLabel /= 2;
                }

				if ((longestRotatedLabel + optionTicks.autoSkipPadding) * me.ticks.length > (me.width - (me.paddingLeft + me.paddingRight))) {
					skipRatio = 1 + Math.floor(((longestRotatedLabel + optionTicks.autoSkipPadding) * me.ticks.length) / (me.width - (me.paddingLeft + me.paddingRight)));
				}

				// if they defined a max number of optionTicks,
				// increase skipRatio until that number is met
				if (maxTicks && me.ticks.length > maxTicks) {
					while (!skipRatio || me.ticks.length / (skipRatio || 1) > maxTicks) {
						if (!skipRatio) {
							skipRatio = 1;
						}
						skipRatio += 1;
					}
				}

				if (!useAutoskipper) {
					skipRatio = false;
				}
			}


			var xTickStart = options.position === "right" ? me.left : me.right - tl;
			var xTickEnd = options.position === "right" ? me.left + tl : me.right;
			var yTickStart = options.position === "bottom" ? me.top : me.bottom - tl;
			var yTickEnd = options.position === "bottom" ? me.top + tl : me.bottom;

			helpers.each(me.ticks, function(label, index) {
				// If the callback returned a null or undefined value, do not draw this line
				if (label === undefined || label === null) {
					return;
				}

				var isLastTick = me.ticks.length === index + 1;

				// Since we always show the last tick,we need may need to hide the last shown one before
				var shouldSkip = (skipRatio > 1 && index % skipRatio > 0) || (index % skipRatio === 0 && index + skipRatio >= me.ticks.length);
				if (shouldSkip && !isLastTick || (label === undefined || label === null)) {
					return;
				}

				var lineWidth, lineColor;
				if (index === (typeof me.zeroLineIndex !== 'undefined' ? me.zeroLineIndex : 0)) {
					// Draw the first index specially
					lineWidth = gridLines.zeroLineWidth;
					lineColor = gridLines.zeroLineColor;
				} else  {
					lineWidth = helpers.getValueAtIndexOrDefault(gridLines.lineWidth, index);
					lineColor = helpers.getValueAtIndexOrDefault(gridLines.color, index);
				}

				// Common properties
				var tx1, ty1, tx2, ty2, x1, y1, x2, y2, labelX, labelY;
				var textAlign, textBaseline = 'middle';

				if (isHorizontal) {
					if (!isRotated) {
						textBaseline = options.position === 'top' ? 'bottom' : 'top';
					}

					textAlign = isRotated ? 'right' : 'center';

					var xLineValue = me.getPixelForTick(index) + helpers.aliasPixel(lineWidth); // xvalues for grid lines
					labelX = me.getPixelForTick(index, gridLines.offsetGridLines) + optionTicks.labelOffset; // x values for optionTicks (need to consider offsetLabel option)
					labelY = (isRotated) ? me.top + 12 : options.position === 'top' ? me.bottom - tl : me.top + tl;

					tx1 = tx2 = x1 = x2 = xLineValue;
					ty1 = yTickStart;
					ty2 = yTickEnd;
					y1 = chartArea.top;
					y2 = chartArea.bottom;
				} else {
					if (options.position === 'left') {
						if (optionTicks.mirror) {
							labelX = me.right + optionTicks.padding;
							textAlign = 'left';
						} else {
							labelX = me.right - optionTicks.padding;
							textAlign = 'right';
						}
					} else {
						// right side
						if (optionTicks.mirror) {
							labelX = me.left - optionTicks.padding;
							textAlign = 'right';
						} else {
							labelX = me.left + optionTicks.padding;
							textAlign = 'left';
						}
					}

					var yLineValue = me.getPixelForTick(index); // xvalues for grid lines
					yLineValue += helpers.aliasPixel(lineWidth);
					labelY = me.getPixelForTick(index, gridLines.offsetGridLines);

					tx1 = xTickStart;
					tx2 = xTickEnd;
					x1 = chartArea.left;
					x2 = chartArea.right;
					ty1 = ty2 = y1 = y2 = yLineValue;
				}

				itemsToDraw.push({
					tx1: tx1,
					ty1: ty1,
					tx2: tx2,
					ty2: ty2,
					x1: x1,
					y1: y1,
					x2: x2,
					y2: y2,
					labelX: labelX,
					labelY: labelY,
					glWidth: lineWidth,
					glColor: lineColor,
					rotation: -1 * labelRotationRadians,
					label: label,
					textBaseline: textBaseline,
					textAlign: textAlign
				});
			});

			// Draw all of the tick labels, tick marks, and grid lines at the correct places
			helpers.each(itemsToDraw, function(itemToDraw) {
				if (gridLines.display) {
					context.lineWidth = itemToDraw.glWidth;
					context.strokeStyle = itemToDraw.glColor;

					context.beginPath();

					if (gridLines.drawTicks) {
						context.moveTo(itemToDraw.tx1, itemToDraw.ty1);
						context.lineTo(itemToDraw.tx2, itemToDraw.ty2);
					}

					if (gridLines.drawOnChartArea) {
						context.moveTo(itemToDraw.x1, itemToDraw.y1);
						context.lineTo(itemToDraw.x2, itemToDraw.y2);
					}

					context.stroke();
				}

				if (optionTicks.display) {
					context.save();
					context.translate(itemToDraw.labelX, itemToDraw.labelY);
					context.rotate(itemToDraw.rotation);
					context.font = tickLabelFont;
					context.textBaseline = itemToDraw.textBaseline;
					context.textAlign = itemToDraw.textAlign;

					var label = itemToDraw.label;
					if (helpers.isArray(label)) {
						for (var i = 0, y = 0; i < label.length; ++i) {
							// We just make sure the multiline element is a string here..
							context.fillText('' + label[i], 0, y);
							// apply same lineSpacing as calculated @ L#320
							y += (tickFontSize * 1.5);
						}
					} else {
						context.fillText(label, 0, 0);
					}
					context.restore();
				}
			});

			if (scaleLabel.display) {
				// Draw the scale label
				var scaleLabelX;
				var scaleLabelY;
				var rotation = 0;

				if (isHorizontal) {
					scaleLabelX = me.left + ((me.right - me.left) / 2); // midpoint of the width
					scaleLabelY = options.position === 'bottom' ? me.bottom - (scaleLabelFontSize / 2) : me.top + (scaleLabelFontSize / 2);
				} else {
					var isLeft = options.position === 'left';
					scaleLabelX = isLeft ? me.left + (scaleLabelFontSize / 2) : me.right - (scaleLabelFontSize / 2);
					scaleLabelY = me.top + ((me.bottom - me.top) / 2);
					rotation = isLeft ? -0.5 * Math.PI : 0.5 * Math.PI;
				}
				
				context.save();
				context.translate(scaleLabelX, scaleLabelY);
				context.rotate(rotation);
				context.textAlign = 'center';
				context.textBaseline = 'middle';
				context.fillStyle = scaleLabelFontColor; // render in correct colour
				context.font = scaleLabelFont;
				context.fillText(scaleLabel.labelString, 0, 0);
				context.restore();
			}

			if (gridLines.drawBorder) {
				// Draw the line at the edge of the axis
				context.lineWidth = helpers.getValueAtIndexOrDefault(gridLines.lineWidth, 0);
				context.strokeStyle = helpers.getValueAtIndexOrDefault(gridLines.color, 0);
				var x1 = me.left,
					x2 = me.right,
					y1 = me.top,
					y2 = me.bottom;

				var aliasPixel = helpers.aliasPixel(context.lineWidth);
				if (isHorizontal) {
					y1 = y2 = options.position === 'top' ? me.bottom : me.top;
					y1 += aliasPixel;
					y2 += aliasPixel;
				} else {
					x1 = x2 = options.position === 'left' ? me.right : me.left;
					x1 += aliasPixel;
					x2 += aliasPixel;
				}

				context.beginPath();
				context.moveTo(x1, y1);
				context.lineTo(x2, y2);
				context.stroke();
			}
		}
	});
};

},{}],31:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.scaleService = {
		// Scale registration object. Extensions can register new scale types (such as log or DB scales) and then
		// use the new chart options to grab the correct scale
		constructors: {},
		// Use a registration function so that we can move to an ES6 map when we no longer need to support
		// old browsers

		// Scale config defaults
		defaults: {},
		registerScaleType: function(type, scaleConstructor, defaults) {
			this.constructors[type] = scaleConstructor;
			this.defaults[type] = helpers.clone(defaults);
		},
		getScaleConstructor: function(type) {
			return this.constructors.hasOwnProperty(type) ? this.constructors[type] : undefined;
		},
		getScaleDefaults: function(type) {
			// Return the scale defaults merged with the global settings so that we always use the latest ones
			return this.defaults.hasOwnProperty(type) ? helpers.scaleMerge(Chart.defaults.scale, this.defaults[type]) : {};
		},
		updateScaleDefaults: function(type, additions) {
			var defaults = this.defaults;
			if (defaults.hasOwnProperty(type)) {
				defaults[type] = helpers.extend(defaults[type], additions);
			}
		},
		addScalesToLayout: function(chartInstance) {
			// Adds each scale to the chart.boxes array to be sized accordingly
			helpers.each(chartInstance.scales, function(scale) {
				Chart.layoutService.addBox(chartInstance, scale);
			});
		}
	};
};
},{}],32:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.global.title = {
		display: false,
		position: 'top',
		fullWidth: true, // marks that this box should take the full width of the canvas (pushing down other boxes)

		fontStyle: 'bold',
		padding: 10,

		// actual title
		text: ''
	};

	var noop = helpers.noop;
	Chart.Title = Chart.Element.extend({

		initialize: function(config) {
			var me = this;
			helpers.extend(me, config);
			me.options = helpers.configMerge(Chart.defaults.global.title, config.options);

			// Contains hit boxes for each dataset (in dataset order)
			me.legendHitBoxes = [];
		},

		// These methods are ordered by lifecyle. Utilities then follow.

		beforeUpdate: function () {
			var chartOpts = this.chart.options;
			if (chartOpts && chartOpts.title) {
				this.options = helpers.configMerge(Chart.defaults.global.title, chartOpts.title);
			}
		},
		update: function(maxWidth, maxHeight, margins) {
			var me = this;

			// Update Lifecycle - Probably don't want to ever extend or overwrite this function ;)
			me.beforeUpdate();

			// Absorb the master measurements
			me.maxWidth = maxWidth;
			me.maxHeight = maxHeight;
			me.margins = margins;

			// Dimensions
			me.beforeSetDimensions();
			me.setDimensions();
			me.afterSetDimensions();
			// Labels
			me.beforeBuildLabels();
			me.buildLabels();
			me.afterBuildLabels();

			// Fit
			me.beforeFit();
			me.fit();
			me.afterFit();
			//
			me.afterUpdate();

			return me.minSize;

		},
		afterUpdate: noop,

		//

		beforeSetDimensions: noop,
		setDimensions: function() {
			var me = this;
			// Set the unconstrained dimension before label rotation
			if (me.isHorizontal()) {
				// Reset position before calculating rotation
				me.width = me.maxWidth;
				me.left = 0;
				me.right = me.width;
			} else {
				me.height = me.maxHeight;

				// Reset position before calculating rotation
				me.top = 0;
				me.bottom = me.height;
			}

			// Reset padding
			me.paddingLeft = 0;
			me.paddingTop = 0;
			me.paddingRight = 0;
			me.paddingBottom = 0;

			// Reset minSize
			me.minSize = {
				width: 0,
				height: 0
			};
		},
		afterSetDimensions: noop,

		//

		beforeBuildLabels: noop,
		buildLabels: noop,
		afterBuildLabels: noop,

		//

		beforeFit: noop,
		fit: function() {

			var me = this,
				ctx = me.ctx,
				valueOrDefault = helpers.getValueOrDefault,
				opts = me.options,
				globalDefaults = Chart.defaults.global,
				display = opts.display,
				fontSize = valueOrDefault(opts.fontSize, globalDefaults.defaultFontSize),
				minSize = me.minSize;

			if (me.isHorizontal()) {
				minSize.width = me.maxWidth; // fill all the width
				minSize.height = display ? fontSize + (opts.padding * 2) : 0;
			} else {
				minSize.width = display ? fontSize + (opts.padding * 2) : 0;
				minSize.height = me.maxHeight; // fill all the height
			}

			me.width = minSize.width;
			me.height = minSize.height;

		},
		afterFit: noop,

		// Shared Methods
		isHorizontal: function() {
			var pos = this.options.position;
			return pos === "top" || pos === "bottom";
		},

		// Actualy draw the title block on the canvas
		draw: function() {
			var me = this,
				ctx = me.ctx,
				valueOrDefault = helpers.getValueOrDefault,
				opts = me.options,
				globalDefaults = Chart.defaults.global;

			if (opts.display) {
				var fontSize = valueOrDefault(opts.fontSize, globalDefaults.defaultFontSize),
					fontStyle = valueOrDefault(opts.fontStyle, globalDefaults.defaultFontStyle),
					fontFamily = valueOrDefault(opts.fontFamily, globalDefaults.defaultFontFamily),
					titleFont = helpers.fontString(fontSize, fontStyle, fontFamily),
					rotation = 0,
					titleX,
					titleY,
					top = me.top,
					left = me.left,
					bottom = me.bottom,
					right = me.right;

				ctx.fillStyle = valueOrDefault(opts.fontColor, globalDefaults.defaultFontColor); // render in correct colour
				ctx.font = titleFont;

				// Horizontal
				if (me.isHorizontal()) {
					titleX = left + ((right - left) / 2); // midpoint of the width
					titleY = top + ((bottom - top) / 2); // midpoint of the height
				} else {
					titleX = opts.position === 'left' ? left + (fontSize / 2) : right - (fontSize / 2);
					titleY = top + ((bottom - top) / 2);
					rotation = Math.PI * (opts.position === 'left' ? -0.5 : 0.5);
				}

				ctx.save();
				ctx.translate(titleX, titleY);
				ctx.rotate(rotation);
				ctx.textAlign = 'center';
				ctx.textBaseline = 'middle';
				ctx.fillText(opts.text, 0, 0);
				ctx.restore();
			}
		}
	});

	// Register the title plugin
	Chart.plugins.register({
		beforeInit: function(chartInstance) {
			var opts = chartInstance.options;
			var titleOpts = opts.title;

			if (titleOpts) {
				chartInstance.titleBlock = new Chart.Title({
					ctx: chartInstance.chart.ctx,
					options: titleOpts,
					chart: chartInstance
				});

				Chart.layoutService.addBox(chartInstance, chartInstance.titleBlock);
			}
		}
	});
};

},{}],33:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	Chart.defaults.global.tooltips = {
		enabled: true,
		custom: null,
		mode: 'single',
		backgroundColor: "rgba(0,0,0,0.8)",
		titleFontStyle: "bold",
		titleSpacing: 2,
		titleMarginBottom: 6,
		titleFontColor: "#fff",
		titleAlign: "left",
		bodySpacing: 2,
		bodyFontColor: "#fff",
		bodyAlign: "left",
		footerFontStyle: "bold",
		footerSpacing: 2,
		footerMarginTop: 6,
		footerFontColor: "#fff",
		footerAlign: "left",
		yPadding: 6,
		xPadding: 6,
		yAlign : 'center',
		xAlign : 'center',
		caretSize: 5,
		cornerRadius: 6,
		multiKeyBackground: '#fff',
		callbacks: {
			// Args are: (tooltipItems, data)
			beforeTitle: helpers.noop,
			title: function(tooltipItems, data) {
				// Pick first xLabel for now
				var title = '';
				var labels = data.labels;
				var labelCount = labels ? labels.length : 0;

				if (tooltipItems.length > 0) {
					var item = tooltipItems[0];

					if (item.xLabel) {
						title = item.xLabel;
					} else if (labelCount > 0 && item.index < labelCount) {
						title = labels[item.index];
					}
				}

				return title;
			},
			afterTitle: helpers.noop,

			// Args are: (tooltipItems, data)
			beforeBody: helpers.noop,

			// Args are: (tooltipItem, data)
			beforeLabel: helpers.noop,
			label: function(tooltipItem, data) {
				var datasetLabel = data.datasets[tooltipItem.datasetIndex].label || '';
				return datasetLabel + ': ' + tooltipItem.yLabel;
			},
			labelColor: function(tooltipItem, chartInstance) {
				var meta = chartInstance.getDatasetMeta(tooltipItem.datasetIndex);
				var activeElement = meta.data[tooltipItem.index];
				var view = activeElement._view;
				return {
					borderColor: view.borderColor,
					backgroundColor: view.backgroundColor
				};
			},
			afterLabel: helpers.noop,

			// Args are: (tooltipItems, data)
			afterBody: helpers.noop,

			// Args are: (tooltipItems, data)
			beforeFooter: helpers.noop,
			footer: helpers.noop,
			afterFooter: helpers.noop
		}
	};

	// Helper to push or concat based on if the 2nd parameter is an array or not
	function pushOrConcat(base, toPush) {
		if (toPush) {
			if (helpers.isArray(toPush)) {
				//base = base.concat(toPush);
				Array.prototype.push.apply(base, toPush);
			} else {
				base.push(toPush);
			}
		}

		return base;
	}

	function getAveragePosition(elements) {
		if (!elements.length) {
			return false;
		}

		var i, len;
		var xPositions = [];
		var yPositions = [];

		for (i = 0, len = elements.length; i < len; ++i) {
			var el = elements[i];
			if (el && el.hasValue()){
				var pos = el.tooltipPosition();
				xPositions.push(pos.x);
				yPositions.push(pos.y);
			}
		}

		var x = 0,
			y = 0;
		for (i = 0, len - xPositions.length; i < len; ++i) {
			x += xPositions[i];
			y += yPositions[i];
		}

		return {
			x: Math.round(x / xPositions.length),
			y: Math.round(y / xPositions.length)
		};
	}

	// Private helper to create a tooltip iteam model
	// @param element : the chart element (point, arc, bar) to create the tooltip item for
	// @return : new tooltip item
	function createTooltipItem(element) {
		var xScale = element._xScale;
		var yScale = element._yScale || element._scale; // handle radar || polarArea charts
		var index = element._index,
			datasetIndex = element._datasetIndex;

		return {
			xLabel: xScale ? xScale.getLabelForIndex(index, datasetIndex) : '',
			yLabel: yScale ? yScale.getLabelForIndex(index, datasetIndex) : '',
			index: index,
			datasetIndex: datasetIndex
		};
	}

	Chart.Tooltip = Chart.Element.extend({
		initialize: function() {
			var me = this;
			var globalDefaults = Chart.defaults.global;
			var tooltipOpts = me._options;
			var getValueOrDefault = helpers.getValueOrDefault;

			helpers.extend(me, {
				_model: {
					// Positioning
					xPadding: tooltipOpts.xPadding,
					yPadding: tooltipOpts.yPadding,
					xAlign : tooltipOpts.yAlign,
					yAlign : tooltipOpts.xAlign,

					// Body
					bodyFontColor: tooltipOpts.bodyFontColor,
					_bodyFontFamily: getValueOrDefault(tooltipOpts.bodyFontFamily, globalDefaults.defaultFontFamily),
					_bodyFontStyle: getValueOrDefault(tooltipOpts.bodyFontStyle, globalDefaults.defaultFontStyle),
					_bodyAlign: tooltipOpts.bodyAlign,
					bodyFontSize: getValueOrDefault(tooltipOpts.bodyFontSize, globalDefaults.defaultFontSize),
					bodySpacing: tooltipOpts.bodySpacing,

					// Title
					titleFontColor: tooltipOpts.titleFontColor,
					_titleFontFamily: getValueOrDefault(tooltipOpts.titleFontFamily, globalDefaults.defaultFontFamily),
					_titleFontStyle: getValueOrDefault(tooltipOpts.titleFontStyle, globalDefaults.defaultFontStyle),
					titleFontSize: getValueOrDefault(tooltipOpts.titleFontSize, globalDefaults.defaultFontSize),
					_titleAlign: tooltipOpts.titleAlign,
					titleSpacing: tooltipOpts.titleSpacing,
					titleMarginBottom: tooltipOpts.titleMarginBottom,

					// Footer
					footerFontColor: tooltipOpts.footerFontColor,
					_footerFontFamily: getValueOrDefault(tooltipOpts.footerFontFamily, globalDefaults.defaultFontFamily),
					_footerFontStyle: getValueOrDefault(tooltipOpts.footerFontStyle, globalDefaults.defaultFontStyle),
					footerFontSize: getValueOrDefault(tooltipOpts.footerFontSize, globalDefaults.defaultFontSize),
					_footerAlign: tooltipOpts.footerAlign,
					footerSpacing: tooltipOpts.footerSpacing,
					footerMarginTop: tooltipOpts.footerMarginTop,

					// Appearance
					caretSize: tooltipOpts.caretSize,
					cornerRadius: tooltipOpts.cornerRadius,
					backgroundColor: tooltipOpts.backgroundColor,
					opacity: 0,
					legendColorBackground: tooltipOpts.multiKeyBackground
				}
			});
		},

		// Get the title
		// Args are: (tooltipItem, data)
		getTitle: function() {
			var me = this;
			var opts = me._options;
			var callbacks = opts.callbacks;

			var beforeTitle = callbacks.beforeTitle.apply(me, arguments),
				title = callbacks.title.apply(me, arguments),
				afterTitle = callbacks.afterTitle.apply(me, arguments);

			var lines = [];
			lines = pushOrConcat(lines, beforeTitle);
			lines = pushOrConcat(lines, title);
			lines = pushOrConcat(lines, afterTitle);

			return lines;
		},

		// Args are: (tooltipItem, data)
		getBeforeBody: function() {
			var lines = this._options.callbacks.beforeBody.apply(this, arguments);
			return helpers.isArray(lines) ? lines : lines !== undefined ? [lines] : [];
		},

		// Args are: (tooltipItem, data)
		getBody: function(tooltipItems, data) {
			var me = this;
			var callbacks = me._options.callbacks;
			var bodyItems = [];

			helpers.each(tooltipItems, function(tooltipItem) {
				var bodyItem = {
					before: [],
					lines: [],
					after: []
				};
				pushOrConcat(bodyItem.before, callbacks.beforeLabel.call(me, tooltipItem, data));
				pushOrConcat(bodyItem.lines, callbacks.label.call(me, tooltipItem, data));
				pushOrConcat(bodyItem.after, callbacks.afterLabel.call(me, tooltipItem, data));

				bodyItems.push(bodyItem);
			});

			return bodyItems;
		},

		// Args are: (tooltipItem, data)
		getAfterBody: function() {
			var lines = this._options.callbacks.afterBody.apply(this, arguments);
			return helpers.isArray(lines) ? lines : lines !== undefined ? [lines] : [];
		},

		// Get the footer and beforeFooter and afterFooter lines
		// Args are: (tooltipItem, data)
		getFooter: function() {
			var me = this;
			var callbacks = me._options.callbacks;

			var beforeFooter = callbacks.beforeFooter.apply(me, arguments);
			var footer = callbacks.footer.apply(me, arguments);
			var afterFooter = callbacks.afterFooter.apply(me, arguments);

			var lines = [];
			lines = pushOrConcat(lines, beforeFooter);
			lines = pushOrConcat(lines, footer);
			lines = pushOrConcat(lines, afterFooter);

			return lines;
		},

		update: function(changed) {
			var me = this;
			var opts = me._options;
			var model = me._model;
			var active = me._active;

			var data = me._data;
			var chartInstance = me._chartInstance;

			var i, len;

			if (active.length) {
				model.opacity = 1;

				var labelColors = [],
					tooltipPosition = getAveragePosition(active);

				var tooltipItems = [];
				for (i = 0, len = active.length; i < len; ++i) {
					tooltipItems.push(createTooltipItem(active[i]));
				}

				// If the user provided a sorting function, use it to modify the tooltip items
				if (opts.itemSort) {
					tooltipItems = tooltipItems.sort(opts.itemSort);
				}

				// If there is more than one item, show color items
				if (active.length > 1) {
					helpers.each(tooltipItems, function(tooltipItem) {
						labelColors.push(opts.callbacks.labelColor.call(me, tooltipItem, chartInstance));
					});
				}

				// Build the Text Lines
				helpers.extend(model, {
					title: me.getTitle(tooltipItems, data),
					beforeBody: me.getBeforeBody(tooltipItems, data),
					body: me.getBody(tooltipItems, data),
					afterBody: me.getAfterBody(tooltipItems, data),
					footer: me.getFooter(tooltipItems, data),
					x: Math.round(tooltipPosition.x),
					y: Math.round(tooltipPosition.y),
					caretPadding: helpers.getValueOrDefault(tooltipPosition.padding, 2),
					labelColors: labelColors
				});

				// We need to determine alignment of
				var tooltipSize = me.getTooltipSize(model);
				me.determineAlignment(tooltipSize); // Smart Tooltip placement to stay on the canvas

				helpers.extend(model, me.getBackgroundPoint(model, tooltipSize));
			} else {
				me._model.opacity = 0;
			}

			if (changed && opts.custom) {
				opts.custom.call(me, model);
			}

			return me;
		},
		getTooltipSize: function getTooltipSize(vm) {
			var ctx = this._chart.ctx;

			var size = {
				height: vm.yPadding * 2, // Tooltip Padding
				width: 0
			};

			// Count of all lines in the body
			var body = vm.body;
			var combinedBodyLength = body.reduce(function(count, bodyItem) {
				return count + bodyItem.before.length + bodyItem.lines.length + bodyItem.after.length;
			}, 0);
			combinedBodyLength += vm.beforeBody.length + vm.afterBody.length;

			var titleLineCount = vm.title.length;
			var footerLineCount = vm.footer.length;
			var titleFontSize = vm.titleFontSize,
				bodyFontSize = vm.bodyFontSize,
				footerFontSize = vm.footerFontSize;

			size.height += titleLineCount * titleFontSize; // Title Lines
			size.height += (titleLineCount - 1) * vm.titleSpacing; // Title Line Spacing
			size.height += titleLineCount ? vm.titleMarginBottom : 0; // Title's bottom Margin
			size.height += combinedBodyLength * bodyFontSize; // Body Lines
			size.height += combinedBodyLength ? (combinedBodyLength - 1) * vm.bodySpacing : 0; // Body Line Spacing
			size.height += footerLineCount ? vm.footerMarginTop : 0; // Footer Margin
			size.height += footerLineCount * (footerFontSize); // Footer Lines
			size.height += footerLineCount ? (footerLineCount - 1) * vm.footerSpacing : 0; // Footer Line Spacing

			// Title width
			var widthPadding = 0;
			var maxLineWidth = function(line) {
				size.width = Math.max(size.width, ctx.measureText(line).width + widthPadding);
			};

			ctx.font = helpers.fontString(titleFontSize, vm._titleFontStyle, vm._titleFontFamily);
			helpers.each(vm.title, maxLineWidth);

			// Body width
			ctx.font = helpers.fontString(bodyFontSize, vm._bodyFontStyle, vm._bodyFontFamily);
			helpers.each(vm.beforeBody.concat(vm.afterBody), maxLineWidth);

			// Body lines may include some extra width due to the color box
			widthPadding = body.length > 1 ? (bodyFontSize + 2) : 0;
			helpers.each(body, function(bodyItem) {
				helpers.each(bodyItem.before, maxLineWidth);
				helpers.each(bodyItem.lines, maxLineWidth);
				helpers.each(bodyItem.after, maxLineWidth);
			});

			// Reset back to 0
			widthPadding = 0;

			// Footer width
			ctx.font = helpers.fontString(footerFontSize, vm._footerFontStyle, vm._footerFontFamily);
			helpers.each(vm.footer, maxLineWidth);

			// Add padding
			size.width += 2 * vm.xPadding;

			return size;
		},
		determineAlignment: function determineAlignment(size) {
			var me = this;
			var model = me._model;
			var chart = me._chart;
			var chartArea = me._chartInstance.chartArea;

			if (model.y < size.height) {
				model.yAlign = 'top';
			} else if (model.y > (chart.height - size.height)) {
				model.yAlign = 'bottom';
			}

			var lf, rf; // functions to determine left, right alignment
			var olf, orf; // functions to determine if left/right alignment causes tooltip to go outside chart
			var yf; // function to get the y alignment if the tooltip goes outside of the left or right edges
			var midX = (chartArea.left + chartArea.right) / 2;
			var midY = (chartArea.top + chartArea.bottom) / 2;

			if (model.yAlign === 'center') {
				lf = function(x) {
					return x <= midX;
				};
				rf = function(x) {
					return x > midX;
				};
			} else {
				lf = function(x) {
					return x <= (size.width / 2);
				};
				rf = function(x) {
					return x >= (chart.width - (size.width / 2));
				};
			}

			olf = function(x) {
				return x + size.width > chart.width;
			};
			orf = function(x) {
				return x - size.width < 0;
			};
			yf = function(y) {
				return y <= midY ? 'top' : 'bottom';
			};

			if (lf(model.x)) {
				model.xAlign = 'left';

				// Is tooltip too wide and goes over the right side of the chart.?
				if (olf(model.x)) {
					model.xAlign = 'center';
					model.yAlign = yf(model.y);
				}
			} else if (rf(model.x)) {
				model.xAlign = 'right';

				// Is tooltip too wide and goes outside left edge of canvas?
				if (orf(model.x)) {
					model.xAlign = 'center';
					model.yAlign = yf(model.y);
				}
			}
		},
		getBackgroundPoint: function getBackgroundPoint(vm, size) {
			// Background Position
			var pt = {
				x: vm.x,
				y: vm.y
			};

			var caretSize = vm.caretSize,
				caretPadding = vm.caretPadding,
				cornerRadius = vm.cornerRadius,
				xAlign = vm.xAlign,
				yAlign = vm.yAlign,
				paddingAndSize = caretSize + caretPadding,
				radiusAndPadding = cornerRadius + caretPadding;

			if (xAlign === 'right') {
				pt.x -= size.width;
			} else if (xAlign === 'center') {
				pt.x -= (size.width / 2);
			}

			if (yAlign === 'top') {
				pt.y += paddingAndSize;
			} else if (yAlign === 'bottom') {
				pt.y -= size.height + paddingAndSize;
			} else {
				pt.y -= (size.height / 2);
			}

			if (yAlign === 'center') {
				if (xAlign === 'left') {
					pt.x += paddingAndSize;
				} else if (xAlign === 'right') {
					pt.x -= paddingAndSize;
				}
			} else {
				if (xAlign === 'left') {
					pt.x -= radiusAndPadding;
				} else if (xAlign === 'right') {
					pt.x += radiusAndPadding;
				}
			}

			return pt;
		},
		drawCaret: function drawCaret(tooltipPoint, size, opacity, caretPadding) {
			var vm = this._view;
			var ctx = this._chart.ctx;
			var x1, x2, x3;
			var y1, y2, y3;
			var caretSize = vm.caretSize;
			var cornerRadius = vm.cornerRadius;
			var xAlign = vm.xAlign,
				yAlign = vm.yAlign;
			var ptX = tooltipPoint.x,
				ptY = tooltipPoint.y;
			var width = size.width,
				height = size.height;

			if (yAlign === 'center') {
				// Left or right side
				if (xAlign === 'left') {
					x1 = ptX;
					x2 = x1 - caretSize;
					x3 = x1;
				} else {
					x1 = ptX + width;
					x2 = x1 + caretSize;
					x3 = x1;
				}

				y2 = ptY + (height / 2);
				y1 = y2 - caretSize;
				y3 = y2 + caretSize;
			} else {
				if (xAlign === 'left') {
					x1 = ptX + cornerRadius;
					x2 = x1 + caretSize;
					x3 = x2 + caretSize;
				} else if (xAlign === 'right') {
					x1 = ptX + width - cornerRadius;
					x2 = x1 - caretSize;
					x3 = x2 - caretSize;
				} else {
					x2 = ptX + (width / 2);
					x1 = x2 - caretSize;
					x3 = x2 + caretSize;
				}

				if (yAlign === 'top') {
					y1 = ptY;
					y2 = y1 - caretSize;
					y3 = y1;
				} else {
					y1 = ptY + height;
					y2 = y1 + caretSize;
					y3 = y1;
				}
			}

			var bgColor = helpers.color(vm.backgroundColor);
			ctx.fillStyle = bgColor.alpha(opacity * bgColor.alpha()).rgbString();
			ctx.beginPath();
			ctx.moveTo(x1, y1);
			ctx.lineTo(x2, y2);
			ctx.lineTo(x3, y3);
			ctx.closePath();
			ctx.fill();
		},
		drawTitle: function drawTitle(pt, vm, ctx, opacity) {
			var title = vm.title;

			if (title.length) {
				ctx.textAlign = vm._titleAlign;
				ctx.textBaseline = "top";

				var titleFontSize = vm.titleFontSize,
					titleSpacing = vm.titleSpacing;

				var titleFontColor = helpers.color(vm.titleFontColor);
				ctx.fillStyle = titleFontColor.alpha(opacity * titleFontColor.alpha()).rgbString();
				ctx.font = helpers.fontString(titleFontSize, vm._titleFontStyle, vm._titleFontFamily);

				var i, len;
				for (i = 0, len = title.length; i < len; ++i) {
					ctx.fillText(title[i], pt.x, pt.y);
					pt.y += titleFontSize + titleSpacing; // Line Height and spacing

					if (i + 1 === title.length) {
						pt.y += vm.titleMarginBottom - titleSpacing; // If Last, add margin, remove spacing
					}
				}
			}
		},
		drawBody: function drawBody(pt, vm, ctx, opacity) {
			var bodyFontSize = vm.bodyFontSize;
			var bodySpacing = vm.bodySpacing;
			var body = vm.body;

			ctx.textAlign = vm._bodyAlign;
			ctx.textBaseline = "top";

			var bodyFontColor = helpers.color(vm.bodyFontColor);
			var textColor = bodyFontColor.alpha(opacity * bodyFontColor.alpha()).rgbString();
			ctx.fillStyle = textColor;
			ctx.font = helpers.fontString(bodyFontSize, vm._bodyFontStyle, vm._bodyFontFamily);

			// Before Body
			var xLinePadding = 0;
			var fillLineOfText = function(line) {
				ctx.fillText(line, pt.x + xLinePadding, pt.y);
				pt.y += bodyFontSize + bodySpacing;
			};

			// Before body lines
			helpers.each(vm.beforeBody, fillLineOfText);

			var drawColorBoxes = body.length > 1;
			xLinePadding = drawColorBoxes ? (bodyFontSize + 2) : 0;

			// Draw body lines now
			helpers.each(body, function(bodyItem, i) {
				helpers.each(bodyItem.before, fillLineOfText);

				helpers.each(bodyItem.lines, function(line) {
					// Draw Legend-like boxes if needed
					if (drawColorBoxes) {
						// Fill a white rect so that colours merge nicely if the opacity is < 1
						ctx.fillStyle = helpers.color(vm.legendColorBackground).alpha(opacity).rgbaString();
						ctx.fillRect(pt.x, pt.y, bodyFontSize, bodyFontSize);

						// Border
						ctx.strokeStyle = helpers.color(vm.labelColors[i].borderColor).alpha(opacity).rgbaString();
						ctx.strokeRect(pt.x, pt.y, bodyFontSize, bodyFontSize);

						// Inner square
						ctx.fillStyle = helpers.color(vm.labelColors[i].backgroundColor).alpha(opacity).rgbaString();
						ctx.fillRect(pt.x + 1, pt.y + 1, bodyFontSize - 2, bodyFontSize - 2);

						ctx.fillStyle = textColor;
					}

					fillLineOfText(line);
				});

				helpers.each(bodyItem.after, fillLineOfText);
			});

			// Reset back to 0 for after body
			xLinePadding = 0;

			// After body lines
			helpers.each(vm.afterBody, fillLineOfText);
			pt.y -= bodySpacing; // Remove last body spacing
		},
		drawFooter: function drawFooter(pt, vm, ctx, opacity) {
			var footer = vm.footer;

			if (footer.length) {
				pt.y += vm.footerMarginTop;

				ctx.textAlign = vm._footerAlign;
				ctx.textBaseline = "top";

				var footerFontColor = helpers.color(vm.footerFontColor);
				ctx.fillStyle = footerFontColor.alpha(opacity * footerFontColor.alpha()).rgbString();
				ctx.font = helpers.fontString(vm.footerFontSize, vm._footerFontStyle, vm._footerFontFamily);

				helpers.each(footer, function(line) {
					ctx.fillText(line, pt.x, pt.y);
					pt.y += vm.footerFontSize + vm.footerSpacing;
				});
			}
		},
		draw: function draw() {
			var ctx = this._chart.ctx;
			var vm = this._view;

			if (vm.opacity === 0) {
				return;
			}

			var tooltipSize = this.getTooltipSize(vm);
			var pt = {
				x: vm.x,
				y: vm.y
			};

			// IE11/Edge does not like very small opacities, so snap to 0
			var opacity = Math.abs(vm.opacity < 1e-3) ? 0 : vm.opacity;

			if (this._options.enabled) {
				// Draw Background
				var bgColor = helpers.color(vm.backgroundColor);
				ctx.fillStyle = bgColor.alpha(opacity * bgColor.alpha()).rgbString();
				helpers.drawRoundedRectangle(ctx, pt.x, pt.y, tooltipSize.width, tooltipSize.height, vm.cornerRadius);
				ctx.fill();

				// Draw Caret
				this.drawCaret(pt, tooltipSize, opacity, vm.caretPadding);

				// Draw Title, Body, and Footer
				pt.x += vm.xPadding;
				pt.y += vm.yPadding;

				// Titles
				this.drawTitle(pt, vm, ctx, opacity);

				// Body
				this.drawBody(pt, vm, ctx, opacity);

				// Footer
				this.drawFooter(pt, vm, ctx, opacity);
			}
		}
	});
};

},{}],34:[function(require,module,exports){
"use strict";

module.exports = function(Chart, moment) {

  var helpers = Chart.helpers,
    globalOpts = Chart.defaults.global;

  globalOpts.elements.arc = {
    backgroundColor: globalOpts.defaultColor,
    borderColor: "#fff",
    borderWidth: 2
  };

  Chart.elements.Arc = Chart.Element.extend({
    inLabelRange: function(mouseX) {
      var vm = this._view;

      if (vm) {
        return (Math.pow(mouseX - vm.x, 2) < Math.pow(vm.radius + vm.hoverRadius, 2));
      } else {
        return false;
      }
    },
    inRange: function(chartX, chartY) {
      var vm = this._view;

      if (vm) {
        var pointRelativePosition = helpers.getAngleFromPoint(vm, {
            x: chartX,
            y: chartY
          }),
          angle = pointRelativePosition.angle,
          distance = pointRelativePosition.distance;

        //Sanitise angle range
        var startAngle = vm.startAngle;
        var endAngle = vm.endAngle;
        while (endAngle < startAngle) {
          endAngle += 2.0 * Math.PI;
        }
        while (angle > endAngle) {
          angle -= 2.0 * Math.PI;
        }
        while (angle < startAngle) {
          angle += 2.0 * Math.PI;
        }

        //Check if within the range of the open/close angle
        var betweenAngles = (angle >= startAngle && angle <= endAngle),
          withinRadius = (distance >= vm.innerRadius && distance <= vm.outerRadius);

        return (betweenAngles && withinRadius);
      } else {
        return false;
      }
    },
    tooltipPosition: function() {
      var vm = this._view;

      var centreAngle = vm.startAngle + ((vm.endAngle - vm.startAngle) / 2),
        rangeFromCentre = (vm.outerRadius - vm.innerRadius) / 2 + vm.innerRadius;
      return {
        x: vm.x + (Math.cos(centreAngle) * rangeFromCentre),
        y: vm.y + (Math.sin(centreAngle) * rangeFromCentre)
      };
    },
    draw: function() {

      var ctx = this._chart.ctx,
        vm = this._view,
        sA = vm.startAngle,
        eA = vm.endAngle;

      ctx.beginPath();

      ctx.arc(vm.x, vm.y, vm.outerRadius, sA, eA);
      ctx.arc(vm.x, vm.y, vm.innerRadius, eA, sA, true);

      ctx.closePath();
      ctx.strokeStyle = vm.borderColor;
      ctx.lineWidth = vm.borderWidth;

      ctx.fillStyle = vm.backgroundColor;

      ctx.fill();
      ctx.lineJoin = 'bevel';

      if (vm.borderWidth) {
        ctx.stroke();
      }
    }
  });
};

},{}],35:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;
	var globalDefaults = Chart.defaults.global;

	Chart.defaults.global.elements.line = {
		tension: 0.4,
		backgroundColor: globalDefaults.defaultColor,
		borderWidth: 3,
		borderColor: globalDefaults.defaultColor,
		borderCapStyle: 'butt',
		borderDash: [],
		borderDashOffset: 0.0,
		borderJoinStyle: 'miter',
		fill: true // do we fill in the area between the line and its base axis
	};

	Chart.elements.Line = Chart.Element.extend({
		lineToNextPoint: function(previousPoint, point, nextPoint, skipHandler, previousSkipHandler) {
			var me = this;
			var ctx = me._chart.ctx;
			var spanGaps = me._view ? me._view.spanGaps : false;

			if (point._view.skip && !spanGaps) {
				skipHandler.call(me, previousPoint, point, nextPoint);
			} else if (previousPoint._view.skip && !spanGaps) {
				previousSkipHandler.call(me, previousPoint, point, nextPoint);
			} else if (point._view.tension === 0) {
				ctx.lineTo(point._view.x, point._view.y);
			} else {
				// Line between points
				ctx.bezierCurveTo(
					previousPoint._view.controlPointNextX,
					previousPoint._view.controlPointNextY,
					point._view.controlPointPreviousX,
					point._view.controlPointPreviousY,
					point._view.x,
					point._view.y
				);
			}
		},

		draw: function() {
			var me = this;

			var vm = me._view;
			var ctx = me._chart.ctx;
			var first = me._children[0];
			var last = me._children[me._children.length - 1];

			function loopBackToStart(drawLineToCenter) {
				if (!first._view.skip && !last._view.skip) {
					// Draw a bezier line from last to first
					ctx.bezierCurveTo(
						last._view.controlPointNextX,
						last._view.controlPointNextY,
						first._view.controlPointPreviousX,
						first._view.controlPointPreviousY,
						first._view.x,
						first._view.y
					);
				} else if (drawLineToCenter) {
					// Go to center
					ctx.lineTo(me._view.scaleZero.x, me._view.scaleZero.y);
				}
			}

			ctx.save();

			// If we had points and want to fill this line, do so.
			if (me._children.length > 0 && vm.fill) {
				// Draw the background first (so the border is always on top)
				ctx.beginPath();

				helpers.each(me._children, function(point, index) {
					var previous = helpers.previousItem(me._children, index);
					var next = helpers.nextItem(me._children, index);

					// First point moves to it's starting position no matter what
					if (index === 0) {
						if (me._loop) {
							ctx.moveTo(vm.scaleZero.x, vm.scaleZero.y);
						} else {
							ctx.moveTo(point._view.x, vm.scaleZero);
						}

						if (point._view.skip) {
							if (!me._loop) {
								ctx.moveTo(next._view.x, me._view.scaleZero);
							}
						} else {
							ctx.lineTo(point._view.x, point._view.y);
						}
					} else {
						me.lineToNextPoint(previous, point, next, function(previousPoint, point, nextPoint) {
							if (me._loop) {
								// Go to center
								ctx.lineTo(me._view.scaleZero.x, me._view.scaleZero.y);
							} else {
								ctx.lineTo(previousPoint._view.x, me._view.scaleZero);
								ctx.moveTo(nextPoint._view.x, me._view.scaleZero);
							}
						}, function(previousPoint, point) {
							// If we skipped the last point, draw a line to ourselves so that the fill is nice
							ctx.lineTo(point._view.x, point._view.y);
						});
					}
				}, me);

				// For radial scales, loop back around to the first point
				if (me._loop) {
					loopBackToStart(true);
				} else {
					//Round off the line by going to the base of the chart, back to the start, then fill.
					ctx.lineTo(me._children[me._children.length - 1]._view.x, vm.scaleZero);
					ctx.lineTo(me._children[0]._view.x, vm.scaleZero);
				}

				ctx.fillStyle = vm.backgroundColor || globalDefaults.defaultColor;
				ctx.closePath();
				ctx.fill();
			}

			var globalOptionLineElements = globalDefaults.elements.line;
			// Now draw the line between all the points with any borders
			ctx.lineCap = vm.borderCapStyle || globalOptionLineElements.borderCapStyle;

			// IE 9 and 10 do not support line dash
			if (ctx.setLineDash) {
				ctx.setLineDash(vm.borderDash || globalOptionLineElements.borderDash);
			}

			ctx.lineDashOffset = vm.borderDashOffset || globalOptionLineElements.borderDashOffset;
			ctx.lineJoin = vm.borderJoinStyle || globalOptionLineElements.borderJoinStyle;
			ctx.lineWidth = vm.borderWidth || globalOptionLineElements.borderWidth;
			ctx.strokeStyle = vm.borderColor || globalDefaults.defaultColor;
			ctx.beginPath();

			helpers.each(me._children, function(point, index) {
				var previous = helpers.previousItem(me._children, index);
				var next = helpers.nextItem(me._children, index);

				if (index === 0) {
					ctx.moveTo(point._view.x, point._view.y);
				} else {
					me.lineToNextPoint(previous, point, next, function(previousPoint, point, nextPoint) {
						ctx.moveTo(nextPoint._view.x, nextPoint._view.y);
					}, function(previousPoint, point) {
						// If we skipped the last point, move up to our point preventing a line from being drawn
						ctx.moveTo(point._view.x, point._view.y);
					});
				}
			}, me);

			if (me._loop && me._children.length > 0) {
				loopBackToStart();
			}

			ctx.stroke();
			ctx.restore();
		}
	});
};
},{}],36:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers,
		globalOpts = Chart.defaults.global,
		defaultColor = globalOpts.defaultColor;

	globalOpts.elements.point = {
		radius: 3,
		pointStyle: 'circle',
		backgroundColor: defaultColor,
		borderWidth: 1,
		borderColor: defaultColor,
		// Hover
		hitRadius: 1,
		hoverRadius: 4,
		hoverBorderWidth: 1
	};

	Chart.elements.Point = Chart.Element.extend({
		inRange: function(mouseX, mouseY) {
			var vm = this._view;
			return vm ? ((Math.pow(mouseX - vm.x, 2) + Math.pow(mouseY - vm.y, 2)) < Math.pow(vm.hitRadius + vm.radius, 2)) : false;
		},
		inLabelRange: function(mouseX) {
			var vm = this._view;
			return vm ? (Math.pow(mouseX - vm.x, 2) < Math.pow(vm.radius + vm.hitRadius, 2)) : false;
		},
		tooltipPosition: function() {
			var vm = this._view;
			return {
				x: vm.x,
				y: vm.y,
				padding: vm.radius + vm.borderWidth
			};
		},
		draw: function() {
			var vm = this._view;
			var ctx = this._chart.ctx;
			var pointStyle = vm.pointStyle;
			var radius = vm.radius;
			var x = vm.x;
			var y = vm.y;
			var type, edgeLength, xOffset, yOffset, height, size;

			if (vm.skip) {
				return;
			}

			if (typeof pointStyle === 'object') {
				type = pointStyle.toString();
				if (type === '[object HTMLImageElement]' || type === '[object HTMLCanvasElement]') {
					ctx.drawImage(pointStyle, x - pointStyle.width / 2, y - pointStyle.height / 2);
					return;
				}
			}

			if (isNaN(radius) || radius <= 0) {
				return;
			}

			ctx.strokeStyle = vm.borderColor || defaultColor;
			ctx.lineWidth = helpers.getValueOrDefault(vm.borderWidth, globalOpts.elements.point.borderWidth);
			ctx.fillStyle = vm.backgroundColor || defaultColor;

			switch (pointStyle) {
			// Default includes circle
			default:
				ctx.beginPath();
				ctx.arc(x, y, radius, 0, Math.PI * 2);
				ctx.closePath();
				ctx.fill();
				break;
			case 'triangle':
				ctx.beginPath();
				edgeLength = 3 * radius / Math.sqrt(3);
				height = edgeLength * Math.sqrt(3) / 2;
				ctx.moveTo(x - edgeLength / 2, y + height / 3);
				ctx.lineTo(x + edgeLength / 2, y + height / 3);
				ctx.lineTo(x, y - 2 * height / 3);
				ctx.closePath();
				ctx.fill();
				break;
			case 'rect':
				size = 1 / Math.SQRT2 * radius;
				ctx.fillRect(x - size, y - size, 2 * size,  2 * size);
				ctx.strokeRect(x - size, y - size, 2 * size, 2 * size);
				break;
			case 'rectRot':
				size = 1 / Math.SQRT2 * radius;
				ctx.beginPath();
				ctx.moveTo(x - size, y);
				ctx.lineTo(x, y + size);
				ctx.lineTo(x + size, y);
				ctx.lineTo(x, y - size);
				ctx.closePath();
				ctx.fill();
				break;
			case 'cross':
				ctx.beginPath();
				ctx.moveTo(x, y + radius);
				ctx.lineTo(x, y - radius);
				ctx.moveTo(x - radius, y);
				ctx.lineTo(x + radius, y);
				ctx.closePath();
				break;
			case 'crossRot':
				ctx.beginPath();
				xOffset = Math.cos(Math.PI / 4) * radius;
				yOffset = Math.sin(Math.PI / 4) * radius;
				ctx.moveTo(x - xOffset, y - yOffset);
				ctx.lineTo(x + xOffset, y + yOffset);
				ctx.moveTo(x - xOffset, y + yOffset);
				ctx.lineTo(x + xOffset, y - yOffset);
				ctx.closePath();
				break;
			case 'star':
				ctx.beginPath();
				ctx.moveTo(x, y + radius);
				ctx.lineTo(x, y - radius);
				ctx.moveTo(x - radius, y);
				ctx.lineTo(x + radius, y);
				xOffset = Math.cos(Math.PI / 4) * radius;
				yOffset = Math.sin(Math.PI / 4) * radius;
				ctx.moveTo(x - xOffset, y - yOffset);
				ctx.lineTo(x + xOffset, y + yOffset);
				ctx.moveTo(x - xOffset, y + yOffset);
				ctx.lineTo(x + xOffset, y - yOffset);
				ctx.closePath();
				break;
			case 'line':
				ctx.beginPath();
				ctx.moveTo(x - radius, y);
				ctx.lineTo(x + radius, y);
				ctx.closePath();
				break;
			case 'dash':
				ctx.beginPath();
				ctx.moveTo(x, y);
				ctx.lineTo(x + radius, y);
				ctx.closePath();
				break;
			}

			ctx.stroke();
		}
	});
};

},{}],37:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers,
		globalOpts = Chart.defaults.global;

	globalOpts.elements.rectangle = {
		backgroundColor: globalOpts.defaultColor,
		borderWidth: 0,
		borderColor: globalOpts.defaultColor,
		borderSkipped: 'bottom'
	};

	Chart.elements.Rectangle = Chart.Element.extend({
		draw: function() {
			var ctx = this._chart.ctx;
			var vm = this._view;

			var halfWidth = vm.width / 2,
				leftX = vm.x - halfWidth,
				rightX = vm.x + halfWidth,
				top = vm.base - (vm.base - vm.y),
				halfStroke = vm.borderWidth / 2;

			// Canvas doesn't allow us to stroke inside the width so we can
			// adjust the sizes to fit if we're setting a stroke on the line
			if (vm.borderWidth) {
				leftX += halfStroke;
				rightX -= halfStroke;
				top += halfStroke;
			}

			ctx.beginPath();
			ctx.fillStyle = vm.backgroundColor;
			ctx.strokeStyle = vm.borderColor;
			ctx.lineWidth = vm.borderWidth;

			// Corner points, from bottom-left to bottom-right clockwise
			// | 1 2 |
			// | 0 3 |
			var corners = [
				[leftX, vm.base],
				[leftX, top],
				[rightX, top],
				[rightX, vm.base]
			];

			// Find first (starting) corner with fallback to 'bottom' 
			var borders = ['bottom', 'left', 'top', 'right'];
			var startCorner = borders.indexOf(vm.borderSkipped, 0);
			if (startCorner === -1)
				startCorner = 0;

			function cornerAt(index) {
				return corners[(startCorner + index) % 4];
			}

			// Draw rectangle from 'startCorner'
			ctx.moveTo.apply(ctx, cornerAt(0));
			for (var i = 1; i < 4; i++)
				ctx.lineTo.apply(ctx, cornerAt(i));

			ctx.fill();
			if (vm.borderWidth) {
				ctx.stroke();
			}
		},
		height: function() {
			var vm = this._view;
			return vm.base - vm.y;
		},
		inRange: function(mouseX, mouseY) {
			var vm = this._view;
			return vm ? 
					(vm.y < vm.base ? 
						(mouseX >= vm.x - vm.width / 2 && mouseX <= vm.x + vm.width / 2) && (mouseY >= vm.y && mouseY <= vm.base) :
						(mouseX >= vm.x - vm.width / 2 && mouseX <= vm.x + vm.width / 2) && (mouseY >= vm.base && mouseY <= vm.y)) :
					false;
		},
		inLabelRange: function(mouseX) {
			var vm = this._view;
			return vm ? (mouseX >= vm.x - vm.width / 2 && mouseX <= vm.x + vm.width / 2) : false;
		},
		tooltipPosition: function() {
			var vm = this._view;
			return {
				x: vm.x,
				y: vm.y
			};
		}
	});

};
},{}],38:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;
	// Default config for a category scale
	var defaultConfig = {
		position: "bottom"
	};

	var DatasetScale = Chart.Scale.extend({
		// Implement this so that 
		determineDataLimits: function() {
			var me = this;
			me.minIndex = 0;
			me.maxIndex = me.chart.data.labels.length - 1;
			var findIndex;

			if (me.options.ticks.min !== undefined) {
				// user specified min value
				findIndex = helpers.indexOf(me.chart.data.labels, me.options.ticks.min);
				me.minIndex = findIndex !== -1 ? findIndex : me.minIndex;
			}

			if (me.options.ticks.max !== undefined) {
				// user specified max value
				findIndex = helpers.indexOf(me.chart.data.labels, me.options.ticks.max);
				me.maxIndex = findIndex !== -1 ? findIndex : me.maxIndex;
			}

			me.min = me.chart.data.labels[me.minIndex];
			me.max = me.chart.data.labels[me.maxIndex];
		},

		buildTicks: function(index) {
			var me = this;
			// If we are viewing some subset of labels, slice the original array
			me.ticks = (me.minIndex === 0 && me.maxIndex === me.chart.data.labels.length - 1) ? me.chart.data.labels : me.chart.data.labels.slice(me.minIndex, me.maxIndex + 1);
		},

		getLabelForIndex: function(index, datasetIndex) {
			return this.ticks[index];
		},

		// Used to get data value locations.  Value can either be an index or a numerical value
		getPixelForValue: function(value, index, datasetIndex, includeOffset) {
			var me = this;
			// 1 is added because we need the length but we have the indexes
			var offsetAmt = Math.max((me.maxIndex + 1 - me.minIndex - ((me.options.gridLines.offsetGridLines) ? 0 : 1)), 1);

			if (me.isHorizontal()) {
				var innerWidth = me.width - (me.paddingLeft + me.paddingRight);
				var valueWidth = innerWidth / offsetAmt;
				var widthOffset = (valueWidth * (index - me.minIndex)) + me.paddingLeft;

				if (me.options.gridLines.offsetGridLines && includeOffset) {
					widthOffset += (valueWidth / 2);
				}

				return me.left + Math.round(widthOffset);
			} else {
				var innerHeight = me.height - (me.paddingTop + me.paddingBottom);
				var valueHeight = innerHeight / offsetAmt;
				var heightOffset = (valueHeight * (index - me.minIndex)) + me.paddingTop;

				if (me.options.gridLines.offsetGridLines && includeOffset) {
					heightOffset += (valueHeight / 2);
				}

				return me.top + Math.round(heightOffset);
			}
		},
		getPixelForTick: function(index, includeOffset) {
			return this.getPixelForValue(this.ticks[index], index + this.minIndex, null, includeOffset);
		},
		getValueForPixel: function(pixel) {
			var me = this;
			var value;
			var offsetAmt = Math.max((me.ticks.length - ((me.options.gridLines.offsetGridLines) ? 0 : 1)), 1);
			var horz = me.isHorizontal();
			var innerDimension = horz ? me.width - (me.paddingLeft + me.paddingRight) : me.height - (me.paddingTop + me.paddingBottom);
			var valueDimension = innerDimension / offsetAmt;

			if (me.options.gridLines.offsetGridLines) {
				pixel -= (valueDimension / 2);
			}
			pixel -= horz ? me.paddingLeft : me.paddingTop;

			if (pixel <= 0) {
				value = 0;
			} else {
				value = Math.round(pixel / valueDimension);
			}

			return value;
		}
	});

	Chart.scaleService.registerScaleType("category", DatasetScale, defaultConfig);

};
},{}],39:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	var defaultConfig = {
		position: "left",
		ticks: {
			callback: function(tickValue, index, ticks) {
				// If we have lots of ticks, don't use the ones
				var delta = ticks.length > 3 ? ticks[2] - ticks[1] : ticks[1] - ticks[0];

				// If we have a number like 2.5 as the delta, figure out how many decimal places we need
				if (Math.abs(delta) > 1) {
					if (tickValue !== Math.floor(tickValue)) {
						// not an integer
						delta = tickValue - Math.floor(tickValue);
					}
				}

				var logDelta = helpers.log10(Math.abs(delta));
				var tickString = '';

				if (tickValue !== 0) {
					var numDecimal = -1 * Math.floor(logDelta);
					numDecimal = Math.max(Math.min(numDecimal, 20), 0); // toFixed has a max of 20 decimal places
					tickString = tickValue.toFixed(numDecimal);
				} else {
					tickString = '0'; // never show decimal places for 0
				}

				return tickString;
			}
		}
	};

	var LinearScale = Chart.LinearScaleBase.extend({
		determineDataLimits: function() {
			var me = this;
			var opts = me.options;
			var tickOpts = opts.ticks;
			var chart = me.chart;
			var data = chart.data;
			var datasets = data.datasets;
			var isHorizontal = me.isHorizontal();

			function IDMatches(meta) {
				return isHorizontal ? meta.xAxisID === me.id : meta.yAxisID === me.id;
			}

			// First Calculate the range
			me.min = null;
			me.max = null;

			if (opts.stacked) {
				var valuesPerType = {};
				var hasPositiveValues = false;
				var hasNegativeValues = false;

				helpers.each(datasets, function(dataset, datasetIndex) {
					var meta = chart.getDatasetMeta(datasetIndex);
					if (valuesPerType[meta.type] === undefined) {
						valuesPerType[meta.type] = {
							positiveValues: [],
							negativeValues: []
						};
					}

					// Store these per type
					var positiveValues = valuesPerType[meta.type].positiveValues;
					var negativeValues = valuesPerType[meta.type].negativeValues;

					if (chart.isDatasetVisible(datasetIndex) && IDMatches(meta)) {
						helpers.each(dataset.data, function(rawValue, index) {
							var value = +me.getRightValue(rawValue);
							if (isNaN(value) || meta.data[index].hidden) {
								return;
							}

							positiveValues[index] = positiveValues[index] || 0;
							negativeValues[index] = negativeValues[index] || 0;

							if (opts.relativePoints) {
								positiveValues[index] = 100;
							} else {
								if (value < 0) {
									hasNegativeValues = true;
									negativeValues[index] += value;
								} else {
									hasPositiveValues = true;
									positiveValues[index] += value;
								}
							}
						});
					}
				});

				helpers.each(valuesPerType, function(valuesForType) {
					var values = valuesForType.positiveValues.concat(valuesForType.negativeValues);
					var minVal = helpers.min(values);
					var maxVal = helpers.max(values);
					me.min = me.min === null ? minVal : Math.min(me.min, minVal);
					me.max = me.max === null ? maxVal : Math.max(me.max, maxVal);
				});

			} else {
				helpers.each(datasets, function(dataset, datasetIndex) {
					var meta = chart.getDatasetMeta(datasetIndex);
					if (chart.isDatasetVisible(datasetIndex) && IDMatches(meta)) {
						helpers.each(dataset.data, function(rawValue, index) {
							var value = +me.getRightValue(rawValue);
							if (isNaN(value) || meta.data[index].hidden) {
								return;
							}

							if (me.min === null) {
								me.min = value;
							} else if (value < me.min) {
								me.min = value;
							}

							if (me.max === null) {
								me.max = value;
							} else if (value > me.max) {
								me.max = value;
							}
						});
					}
				});
			}

			// Common base implementation to handle ticks.min, ticks.max, ticks.beginAtZero
			this.handleTickRangeOptions();
		},
		getTickLimit: function() {
			var maxTicks;
			var me = this;
			var tickOpts = me.options.ticks;

			if (me.isHorizontal()) {
				maxTicks = Math.min(tickOpts.maxTicksLimit ? tickOpts.maxTicksLimit : 11, Math.ceil(me.width / 50));
			} else {
				// The factor of 2 used to scale the font size has been experimentally determined.
				var tickFontSize = helpers.getValueOrDefault(tickOpts.fontSize, Chart.defaults.global.defaultFontSize);
				maxTicks = Math.min(tickOpts.maxTicksLimit ? tickOpts.maxTicksLimit : 11, Math.ceil(me.height / (2 * tickFontSize)));
			}

			return maxTicks;
		},
		// Called after the ticks are built. We need 
		handleDirectionalChanges: function() {
			if (!this.isHorizontal()) {
				// We are in a vertical orientation. The top value is the highest. So reverse the array
				this.ticks.reverse();
			}
		},
		getLabelForIndex: function(index, datasetIndex) {
			return +this.getRightValue(this.chart.data.datasets[datasetIndex].data[index]);
		},
		// Utils
		getPixelForValue: function(value, index, datasetIndex, includeOffset) {
			// This must be called after fit has been run so that
			//      this.left, this.top, this.right, and this.bottom have been defined
			var me = this;
			var paddingLeft = me.paddingLeft;
			var paddingBottom = me.paddingBottom;
			var start = me.start;

			var rightValue = +me.getRightValue(value);
			var pixel;
			var innerDimension;
			var range = me.end - start;

			if (me.isHorizontal()) {
				innerDimension = me.width - (paddingLeft + me.paddingRight);
				pixel = me.left + (innerDimension / range * (rightValue - start));
				return Math.round(pixel + paddingLeft);
			} else {
				innerDimension = me.height - (me.paddingTop + paddingBottom);
				pixel = (me.bottom - paddingBottom) - (innerDimension / range * (rightValue - start));
				return Math.round(pixel);
			}
		},
		getValueForPixel: function(pixel) {
			var me = this;
			var isHorizontal = me.isHorizontal();
			var paddingLeft = me.paddingLeft;
			var paddingBottom = me.paddingBottom;
			var innerDimension = isHorizontal ? me.width - (paddingLeft + me.paddingRight) : me.height - (me.paddingTop + paddingBottom);
			var offset = (isHorizontal ? pixel - me.left - paddingLeft : me.bottom - paddingBottom - pixel) / innerDimension;
			return me.start + ((me.end - me.start) * offset);
		},
		getPixelForTick: function(index, includeOffset) {
			return this.getPixelForValue(this.ticksAsNumbers[index], null, null, includeOffset);
		}
	});
	Chart.scaleService.registerScaleType("linear", LinearScale, defaultConfig);

};
},{}],40:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers,
		noop = helpers.noop;

	Chart.LinearScaleBase = Chart.Scale.extend({
		handleTickRangeOptions: function() {
			var me = this;
			var opts = me.options;
			var tickOpts = opts.ticks;

			// If we are forcing it to begin at 0, but 0 will already be rendered on the chart,
			// do nothing since that would make the chart weird. If the user really wants a weird chart
			// axis, they can manually override it
			if (tickOpts.beginAtZero) {
				var minSign = helpers.sign(me.min);
				var maxSign = helpers.sign(me.max);

				if (minSign < 0 && maxSign < 0) {
					// move the top up to 0
					me.max = 0;
				} else if (minSign > 0 && maxSign > 0) {
					// move the botttom down to 0
					me.min = 0;
				}
			}

			if (tickOpts.min !== undefined) {
				me.min = tickOpts.min;
			} else if (tickOpts.suggestedMin !== undefined) {
				me.min = Math.min(me.min, tickOpts.suggestedMin);
			}

			if (tickOpts.max !== undefined) {
				me.max = tickOpts.max;
			} else if (tickOpts.suggestedMax !== undefined) {
				me.max = Math.max(me.max, tickOpts.suggestedMax);
			}

			if (me.min === me.max) {
				me.max++;

				if (!tickOpts.beginAtZero) {
					me.min--;
				}
			}
		},
		getTickLimit: noop,
		handleDirectionalChanges: noop,

		buildTicks: function() {
			var me = this;
			var opts = me.options;
			var tickOpts = opts.ticks;
			var getValueOrDefault = helpers.getValueOrDefault;
			var isHorizontal = me.isHorizontal();

			var ticks = me.ticks = [];

			// Figure out what the max number of ticks we can support it is based on the size of
			// the axis area. For now, we say that the minimum tick spacing in pixels must be 50
			// We also limit the maximum number of ticks to 11 which gives a nice 10 squares on
			// the graph

			var maxTicks = me.getTickLimit();

			// Make sure we always have at least 2 ticks
			maxTicks = Math.max(2, maxTicks);

			// To get a "nice" value for the tick spacing, we will use the appropriately named
			// "nice number" algorithm. See http://stackoverflow.com/questions/8506881/nice-label-algorithm-for-charts-with-minimum-ticks
			// for details.

			var spacing;
			var fixedStepSizeSet = (tickOpts.fixedStepSize && tickOpts.fixedStepSize > 0) || (tickOpts.stepSize && tickOpts.stepSize > 0);
			if (fixedStepSizeSet) {
				spacing = getValueOrDefault(tickOpts.fixedStepSize, tickOpts.stepSize);
			} else {
				var niceRange = helpers.niceNum(me.max - me.min, false);
				spacing = helpers.niceNum(niceRange / (maxTicks - 1), true);
			}
			var niceMin = Math.floor(me.min / spacing) * spacing;
			var niceMax = Math.ceil(me.max / spacing) * spacing;
			var numSpaces = (niceMax - niceMin) / spacing;

			// If very close to our rounded value, use it.
			if (helpers.almostEquals(numSpaces, Math.round(numSpaces), spacing / 1000)) {
				numSpaces = Math.round(numSpaces);
			} else {
				numSpaces = Math.ceil(numSpaces);
			}

			// Put the values into the ticks array
			ticks.push(tickOpts.min !== undefined ? tickOpts.min : niceMin);
			for (var j = 1; j < numSpaces; ++j) {
				ticks.push(niceMin + (j * spacing));
			}
			ticks.push(tickOpts.max !== undefined ? tickOpts.max : niceMax);

			me.handleDirectionalChanges();

			// At this point, we need to update our max and min given the tick values since we have expanded the
			// range of the scale
			me.max = helpers.max(ticks);
			me.min = helpers.min(ticks);

			if (tickOpts.reverse) {
				ticks.reverse();

				me.start = me.max;
				me.end = me.min;
			} else {
				me.start = me.min;
				me.end = me.max;
			}
		},
		convertTicksToLabels: function() {
			var me = this;
			me.ticksAsNumbers = me.ticks.slice();
			me.zeroLineIndex = me.ticks.indexOf(0);

			Chart.Scale.prototype.convertTicksToLabels.call(me);
		},
	});
};
},{}],41:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;

	var defaultConfig = {
		position: "left",

		// label settings
		ticks: {
			callback: function(value, index, arr) {
				var remain = value / (Math.pow(10, Math.floor(helpers.log10(value))));

				if (remain === 1 || remain === 2 || remain === 5 || index === 0 || index === arr.length - 1) {
					return value.toExponential();
				} else {
					return '';
				}
			}
		}
	};

	var LogarithmicScale = Chart.Scale.extend({
		determineDataLimits: function() {
			var me = this;
			var opts = me.options;
			var tickOpts = opts.ticks;
			var chart = me.chart;
			var data = chart.data;
			var datasets = data.datasets;
			var getValueOrDefault = helpers.getValueOrDefault;
			var isHorizontal = me.isHorizontal();
			function IDMatches(meta) {
				return isHorizontal ? meta.xAxisID === me.id : meta.yAxisID === me.id;
			}

			// Calculate Range
			me.min = null;
			me.max = null;

			if (opts.stacked) {
				var valuesPerType = {};

				helpers.each(datasets, function(dataset, datasetIndex) {
					var meta = chart.getDatasetMeta(datasetIndex);
					if (chart.isDatasetVisible(datasetIndex) && IDMatches(meta)) {
						if (valuesPerType[meta.type] === undefined) {
							valuesPerType[meta.type] = [];
						}

						helpers.each(dataset.data, function(rawValue, index) {
							var values = valuesPerType[meta.type];
							var value = +me.getRightValue(rawValue);
							if (isNaN(value) || meta.data[index].hidden) {
								return;
							}

							values[index] = values[index] || 0;

							if (opts.relativePoints) {
								values[index] = 100;
							} else {
								// Don't need to split positive and negative since the log scale can't handle a 0 crossing
								values[index] += value;
							}
						});
					}
				});

				helpers.each(valuesPerType, function(valuesForType) {
					var minVal = helpers.min(valuesForType);
					var maxVal = helpers.max(valuesForType);
					me.min = me.min === null ? minVal : Math.min(me.min, minVal);
					me.max = me.max === null ? maxVal : Math.max(me.max, maxVal);
				});

			} else {
				helpers.each(datasets, function(dataset, datasetIndex) {
					var meta = chart.getDatasetMeta(datasetIndex);
					if (chart.isDatasetVisible(datasetIndex) && IDMatches(meta)) {
						helpers.each(dataset.data, function(rawValue, index) {
							var value = +me.getRightValue(rawValue);
							if (isNaN(value) || meta.data[index].hidden) {
								return;
							}

							if (me.min === null) {
								me.min = value;
							} else if (value < me.min) {
								me.min = value;
							}

							if (me.max === null) {
								me.max = value;
							} else if (value > me.max) {
								me.max = value;
							}
						});
					}
				});
			}

			me.min = getValueOrDefault(tickOpts.min, me.min);
			me.max = getValueOrDefault(tickOpts.max, me.max);

			if (me.min === me.max) {
				if (me.min !== 0 && me.min !== null) {
					me.min = Math.pow(10, Math.floor(helpers.log10(me.min)) - 1);
					me.max = Math.pow(10, Math.floor(helpers.log10(me.max)) + 1);
				} else {
					me.min = 1;
					me.max = 10;
				}
			}
		},
		buildTicks: function() {
			var me = this;
			var opts = me.options;
			var tickOpts = opts.ticks;
			var getValueOrDefault = helpers.getValueOrDefault;

			// Reset the ticks array. Later on, we will draw a grid line at these positions
			// The array simply contains the numerical value of the spots where ticks will be
			var ticks = me.ticks = [];

			// Figure out what the max number of ticks we can support it is based on the size of
			// the axis area. For now, we say that the minimum tick spacing in pixels must be 50
			// We also limit the maximum number of ticks to 11 which gives a nice 10 squares on
			// the graph

			var tickVal = getValueOrDefault(tickOpts.min, Math.pow(10, Math.floor(helpers.log10(me.min))));

			while (tickVal < me.max) {
				ticks.push(tickVal);

				var exp = Math.floor(helpers.log10(tickVal));
				var significand = Math.floor(tickVal / Math.pow(10, exp)) + 1;

				if (significand === 10) {
					significand = 1;
					++exp;
				}

				tickVal = significand * Math.pow(10, exp);
			}

			var lastTick = getValueOrDefault(tickOpts.max, tickVal);
			ticks.push(lastTick);

			if (!me.isHorizontal()) {
				// We are in a vertical orientation. The top value is the highest. So reverse the array
				ticks.reverse();
			}

			// At this point, we need to update our max and min given the tick values since we have expanded the
			// range of the scale
			me.max = helpers.max(ticks);
			me.min = helpers.min(ticks);

			if (tickOpts.reverse) {
				ticks.reverse();

				me.start = me.max;
				me.end = me.min;
			} else {
				me.start = me.min;
				me.end = me.max;
			}
		},
		convertTicksToLabels: function() {
			this.tickValues = this.ticks.slice();

			Chart.Scale.prototype.convertTicksToLabels.call(this);
		},
		// Get the correct tooltip label
		getLabelForIndex: function(index, datasetIndex) {
			return +this.getRightValue(this.chart.data.datasets[datasetIndex].data[index]);
		},
		getPixelForTick: function(index, includeOffset) {
			return this.getPixelForValue(this.tickValues[index], null, null, includeOffset);
		},
		getPixelForValue: function(value, index, datasetIndex, includeOffset) {
			var me = this;
			var innerDimension;
			var pixel;

			var start = me.start;
			var newVal = +me.getRightValue(value);
			var range = helpers.log10(me.end) - helpers.log10(start);
			var paddingTop = me.paddingTop;
			var paddingBottom = me.paddingBottom;
			var paddingLeft = me.paddingLeft;

			if (me.isHorizontal()) {

				if (newVal === 0) {
					pixel = me.left + paddingLeft;
				} else {
					innerDimension = me.width - (paddingLeft + me.paddingRight);
					pixel = me.left + (innerDimension / range * (helpers.log10(newVal) - helpers.log10(start)));
					pixel += paddingLeft;
				}
			} else {
				// Bottom - top since pixels increase downard on a screen
				if (newVal === 0) {
					pixel = me.top + paddingTop;
				} else {
					innerDimension = me.height - (paddingTop + paddingBottom);
					pixel = (me.bottom - paddingBottom) - (innerDimension / range * (helpers.log10(newVal) - helpers.log10(start)));
				}
			}

			return pixel;
		},
		getValueForPixel: function(pixel) {
			var me = this;
			var offset;
			var range = helpers.log10(me.end) - helpers.log10(me.start);
			var value;
			var innerDimension;

			if (me.isHorizontal()) {
				innerDimension = me.width - (me.paddingLeft + me.paddingRight);
				value = me.start * Math.pow(10, (pixel - me.left - me.paddingLeft) * range / innerDimension);
			} else {
				innerDimension = me.height - (me.paddingTop + me.paddingBottom);
				value = Math.pow(10, (me.bottom - me.paddingBottom - pixel) * range / innerDimension) / me.start;
			}

			return value;
		}
	});
	Chart.scaleService.registerScaleType("logarithmic", LogarithmicScale, defaultConfig);

};
},{}],42:[function(require,module,exports){
"use strict";

module.exports = function(Chart) {

	var helpers = Chart.helpers;
	var globalDefaults = Chart.defaults.global;

	var defaultConfig = {
		display: true,

		//Boolean - Whether to animate scaling the chart from the centre
		animate: true,
		lineArc: false,
		position: "chartArea",

		angleLines: {
			display: true,
			color: "rgba(0, 0, 0, 0.1)",
			lineWidth: 1
		},

		// label settings
		ticks: {
			//Boolean - Show a backdrop to the scale label
			showLabelBackdrop: true,

			//String - The colour of the label backdrop
			backdropColor: "rgba(255,255,255,0.75)",

			//Number - The backdrop padding above & below the label in pixels
			backdropPaddingY: 2,

			//Number - The backdrop padding to the side of the label in pixels
			backdropPaddingX: 2
		},

		pointLabels: {
			//Number - Point label font size in pixels
			fontSize: 10,

			//Function - Used to convert point labels
			callback: function(label) {
				return label;
			}
		}
	};

	var LinearRadialScale = Chart.LinearScaleBase.extend({
		getValueCount: function() {
			return this.chart.data.labels.length;
		},
		setDimensions: function() {
			var me = this;
			var opts = me.options;
			var tickOpts = opts.ticks;
			// Set the unconstrained dimension before label rotation
			me.width = me.maxWidth;
			me.height = me.maxHeight;
			me.xCenter = Math.round(me.width / 2);
			me.yCenter = Math.round(me.height / 2);

			var minSize = helpers.min([me.height, me.width]);
			var tickFontSize = helpers.getValueOrDefault(tickOpts.fontSize, globalDefaults.defaultFontSize);
			me.drawingArea = opts.display ? (minSize / 2) - (tickFontSize / 2 + tickOpts.backdropPaddingY) : (minSize / 2);
		},
		determineDataLimits: function() {
			var me = this;
			var chart = me.chart;
			me.min = null;
			me.max = null;


			helpers.each(chart.data.datasets, function(dataset, datasetIndex) {
				if (chart.isDatasetVisible(datasetIndex)) {
					var meta = chart.getDatasetMeta(datasetIndex);

					helpers.each(dataset.data, function(rawValue, index) {
						var value = +me.getRightValue(rawValue);
						if (isNaN(value) || meta.data[index].hidden) {
							return;
						}

						if (me.min === null) {
							me.min = value;
						} else if (value < me.min) {
							me.min = value;
						}

						if (me.max === null) {
							me.max = value;
						} else if (value > me.max) {
							me.max = value;
						}
					});
				}
			});

			// Common base implementation to handle ticks.min, ticks.max, ticks.beginAtZero
			me.handleTickRangeOptions();
		},
		getTickLimit: function() {
			var tickOpts = this.options.ticks;
			var tickFontSize = helpers.getValueOrDefault(tickOpts.fontSize, globalDefaults.defaultFontSize);
			return Math.min(tickOpts.maxTicksLimit ? tickOpts.maxTicksLimit : 11, Math.ceil(this.drawingArea / (1.5 * tickFontSize)));
		},
		convertTicksToLabels: function() {
			var me = this;
			Chart.LinearScaleBase.prototype.convertTicksToLabels.call(me);

			// Point labels
			me.pointLabels = me.chart.data.labels.map(me.options.pointLabels.callback, me);
		},
		getLabelForIndex: function(index, datasetIndex) {
			return +this.getRightValue(this.chart.data.datasets[datasetIndex].data[index]);
		},
		fit: function() {
			/*
			 * Right, this is really confusing and there is a lot of maths going on here
			 * The gist of the problem is here: https://gist.github.com/nnnick/696cc9c55f4b0beb8fe9
			 *
			 * Reaction: https://dl.dropboxusercontent.com/u/34601363/toomuchscience.gif
			 *
			 * Solution:
			 *
			 * We assume the radius of the polygon is half the size of the canvas at first
			 * at each index we check if the text overlaps.
			 *
			 * Where it does, we store that angle and that index.
			 *
			 * After finding the largest index and angle we calculate how much we need to remove
			 * from the shape radius to move the point inwards by that x.
			 *
			 * We average the left and right distances to get the maximum shape radius that can fit in the box
			 * along with labels.
			 *
			 * Once we have that, we can find the centre point for the chart, by taking the x text protrusion
			 * on each side, removing that from the size, halving it and adding the left x protrusion width.
			 *
			 * This will mean we have a shape fitted to the canvas, as large as it can be with the labels
			 * and position it in the most space efficient manner
			 *
			 * https://dl.dropboxusercontent.com/u/34601363/yeahscience.gif
			 */

			var pointLabels = this.options.pointLabels;
			var pointLabelFontSize = helpers.getValueOrDefault(pointLabels.fontSize, globalDefaults.defaultFontSize);
			var pointLabeFontStyle = helpers.getValueOrDefault(pointLabels.fontStyle, globalDefaults.defaultFontStyle);
			var pointLabeFontFamily = helpers.getValueOrDefault(pointLabels.fontFamily, globalDefaults.defaultFontFamily);
			var pointLabeFont = helpers.fontString(pointLabelFontSize, pointLabeFontStyle, pointLabeFontFamily);

			// Get maximum radius of the polygon. Either half the height (minus the text width) or half the width.
			// Use this to calculate the offset + change. - Make sure L/R protrusion is at least 0 to stop issues with centre points
			var largestPossibleRadius = helpers.min([(this.height / 2 - pointLabelFontSize - 5), this.width / 2]),
				pointPosition,
				i,
				textWidth,
				halfTextWidth,
				furthestRight = this.width,
				furthestRightIndex,
				furthestRightAngle,
				furthestLeft = 0,
				furthestLeftIndex,
				furthestLeftAngle,
				xProtrusionLeft,
				xProtrusionRight,
				radiusReductionRight,
				radiusReductionLeft,
				maxWidthRadius;
			this.ctx.font = pointLabeFont;

			for (i = 0; i < this.getValueCount(); i++) {
				// 5px to space the text slightly out - similar to what we do in the draw function.
				pointPosition = this.getPointPosition(i, largestPossibleRadius);
				textWidth = this.ctx.measureText(this.pointLabels[i] ? this.pointLabels[i] : '').width + 5;
				if (i === 0 || i === this.getValueCount() / 2) {
					// If we're at index zero, or exactly the middle, we're at exactly the top/bottom
					// of the radar chart, so text will be aligned centrally, so we'll half it and compare
					// w/left and right text sizes
					halfTextWidth = textWidth / 2;
					if (pointPosition.x + halfTextWidth > furthestRight) {
						furthestRight = pointPosition.x + halfTextWidth;
						furthestRightIndex = i;
					}
					if (pointPosition.x - halfTextWidth < furthestLeft) {
						furthestLeft = pointPosition.x - halfTextWidth;
						furthestLeftIndex = i;
					}
				} else if (i < this.getValueCount() / 2) {
					// Less than half the values means we'll left align the text
					if (pointPosition.x + textWidth > furthestRight) {
						furthestRight = pointPosition.x + textWidth;
						furthestRightIndex = i;
					}
				} else if (i > this.getValueCount() / 2) {
					// More than half the values means we'll right align the text
					if (pointPosition.x - textWidth < furthestLeft) {
						furthestLeft = pointPosition.x - textWidth;
						furthestLeftIndex = i;
					}
				}
			}

			xProtrusionLeft = furthestLeft;
			xProtrusionRight = Math.ceil(furthestRight - this.width);

			furthestRightAngle = this.getIndexAngle(furthestRightIndex);
			furthestLeftAngle = this.getIndexAngle(furthestLeftIndex);

			radiusReductionRight = xProtrusionRight / Math.sin(furthestRightAngle + Math.PI / 2);
			radiusReductionLeft = xProtrusionLeft / Math.sin(furthestLeftAngle + Math.PI / 2);

			// Ensure we actually need to reduce the size of the chart
			radiusReductionRight = (helpers.isNumber(radiusReductionRight)) ? radiusReductionRight : 0;
			radiusReductionLeft = (helpers.isNumber(radiusReductionLeft)) ? radiusReductionLeft : 0;

			this.drawingArea = Math.round(largestPossibleRadius - (radiusReductionLeft + radiusReductionRight) / 2);
			this.setCenterPoint(radiusReductionLeft, radiusReductionRight);
		},
		setCenterPoint: function(leftMovement, rightMovement) {
			var me = this;
			var maxRight = me.width - rightMovement - me.drawingArea,
				maxLeft = leftMovement + me.drawingArea;

			me.xCenter = Math.round(((maxLeft + maxRight) / 2) + me.left);
			// Always vertically in the centre as the text height doesn't change
			me.yCenter = Math.round((me.height / 2) + me.top);
		},

		getIndexAngle: function(index) {
			var angleMultiplier = (Math.PI * 2) / this.getValueCount();
			// Start from the top instead of right, so remove a quarter of the circle

			return index * angleMultiplier - (Math.PI / 2);
		},
		getDistanceFromCenterForValue: function(value) {
			var me = this;

			if (value === null) {
				return 0; // null always in center
			}

			// Take into account half font size + the yPadding of the top value
			var scalingFactor = me.drawingArea / (me.max - me.min);
			if (me.options.reverse) {
				return (me.max - value) * scalingFactor;
			} else {
				return (value - me.min) * scalingFactor;
			}
		},
		getPointPosition: function(index, distanceFromCenter) {
			var me = this;
			var thisAngle = me.getIndexAngle(index);
			return {
				x: Math.round(Math.cos(thisAngle) * distanceFromCenter) + me.xCenter,
				y: Math.round(Math.sin(thisAngle) * distanceFromCenter) + me.yCenter
			};
		},
		getPointPositionForValue: function(index, value) {
			return this.getPointPosition(index, this.getDistanceFromCenterForValue(value));
		},

		getBasePosition: function() {
			var me = this;
			var min = me.min;
			var max = me.max;

			return me.getPointPositionForValue(0,
				me.beginAtZero? 0:
				min < 0 && max < 0? max :
				min > 0 && max > 0? min :
				0);
		},

		draw: function() {
			var me = this;
			var opts = me.options;
			var gridLineOpts = opts.gridLines;
			var tickOpts = opts.ticks;
			var angleLineOpts = opts.angleLines;
			var pointLabelOpts = opts.pointLabels;
			var getValueOrDefault = helpers.getValueOrDefault;

			if (opts.display) {
				var ctx = me.ctx;

				// Tick Font
				var tickFontSize = getValueOrDefault(tickOpts.fontSize, globalDefaults.defaultFontSize);
				var tickFontStyle = getValueOrDefault(tickOpts.fontStyle, globalDefaults.defaultFontStyle);
				var tickFontFamily = getValueOrDefault(tickOpts.fontFamily, globalDefaults.defaultFontFamily);
				var tickLabelFont = helpers.fontString(tickFontSize, tickFontStyle, tickFontFamily);

				helpers.each(me.ticks, function(label, index) {
					// Don't draw a centre value (if it is minimum)
					if (index > 0 || opts.reverse) {
						var yCenterOffset = me.getDistanceFromCenterForValue(me.ticksAsNumbers[index]);
						var yHeight = me.yCenter - yCenterOffset;

						// Draw circular lines around the scale
						if (gridLineOpts.display && index !== 0) {
							ctx.strokeStyle = helpers.getValueAtIndexOrDefault(gridLineOpts.color, index - 1);
							ctx.lineWidth = helpers.getValueAtIndexOrDefault(gridLineOpts.lineWidth, index - 1);

							if (opts.lineArc) {
								// Draw circular arcs between the points
								ctx.beginPath();
								ctx.arc(me.xCenter, me.yCenter, yCenterOffset, 0, Math.PI * 2);
								ctx.closePath();
								ctx.stroke();
							} else {
								// Draw straight lines connecting each index
								ctx.beginPath();
								for (var i = 0; i < me.getValueCount(); i++) {
									var pointPosition = me.getPointPosition(i, yCenterOffset);
									if (i === 0) {
										ctx.moveTo(pointPosition.x, pointPosition.y);
									} else {
										ctx.lineTo(pointPosition.x, pointPosition.y);
									}
								}
								ctx.closePath();
								ctx.stroke();
							}
						}

						if (tickOpts.display) {
							var tickFontColor = getValueOrDefault(tickOpts.fontColor, globalDefaults.defaultFontColor);
							ctx.font = tickLabelFont;

							if (tickOpts.showLabelBackdrop) {
								var labelWidth = ctx.measureText(label).width;
								ctx.fillStyle = tickOpts.backdropColor;
								ctx.fillRect(
									me.xCenter - labelWidth / 2 - tickOpts.backdropPaddingX,
									yHeight - tickFontSize / 2 - tickOpts.backdropPaddingY,
									labelWidth + tickOpts.backdropPaddingX * 2,
									tickFontSize + tickOpts.backdropPaddingY * 2
								);
							}

							ctx.textAlign = 'center';
							ctx.textBaseline = "middle";
							ctx.fillStyle = tickFontColor;
							ctx.fillText(label, me.xCenter, yHeight);
						}
					}
				});

				if (!opts.lineArc) {
					ctx.lineWidth = angleLineOpts.lineWidth;
					ctx.strokeStyle = angleLineOpts.color;

					var outerDistance = me.getDistanceFromCenterForValue(opts.reverse ? me.min : me.max);

					// Point Label Font
					var pointLabelFontSize = getValueOrDefault(pointLabelOpts.fontSize, globalDefaults.defaultFontSize);
					var pointLabeFontStyle = getValueOrDefault(pointLabelOpts.fontStyle, globalDefaults.defaultFontStyle);
					var pointLabeFontFamily = getValueOrDefault(pointLabelOpts.fontFamily, globalDefaults.defaultFontFamily);
					var pointLabeFont = helpers.fontString(pointLabelFontSize, pointLabeFontStyle, pointLabeFontFamily);

					for (var i = me.getValueCount() - 1; i >= 0; i--) {
						if (angleLineOpts.display) {
							var outerPosition = me.getPointPosition(i, outerDistance);
							ctx.beginPath();
							ctx.moveTo(me.xCenter, me.yCenter);
							ctx.lineTo(outerPosition.x, outerPosition.y);
							ctx.stroke();
							ctx.closePath();
						}
						// Extra 3px out for some label spacing
						var pointLabelPosition = me.getPointPosition(i, outerDistance + 5);

						// Keep this in loop since we may support array properties here
						var pointLabelFontColor = getValueOrDefault(pointLabelOpts.fontColor, globalDefaults.defaultFontColor);
						ctx.font = pointLabeFont;
						ctx.fillStyle = pointLabelFontColor;

						var pointLabels = me.pointLabels,
							labelsCount = pointLabels.length,
							halfLabelsCount = pointLabels.length / 2,
							quarterLabelsCount = halfLabelsCount / 2,
							upperHalf = (i < quarterLabelsCount || i > labelsCount - quarterLabelsCount),
							exactQuarter = (i === quarterLabelsCount || i === labelsCount - quarterLabelsCount);
						if (i === 0) {
							ctx.textAlign = 'center';
						} else if (i === halfLabelsCount) {
							ctx.textAlign = 'center';
						} else if (i < halfLabelsCount) {
							ctx.textAlign = 'left';
						} else {
							ctx.textAlign = 'right';
						}

						// Set the correct text baseline based on outer positioning
						if (exactQuarter) {
							ctx.textBaseline = 'middle';
						} else if (upperHalf) {
							ctx.textBaseline = 'bottom';
						} else {
							ctx.textBaseline = 'top';
						}

						ctx.fillText(pointLabels[i] ? pointLabels[i] : '', pointLabelPosition.x, pointLabelPosition.y);
					}
				}
			}
		}
	});
	Chart.scaleService.registerScaleType("radialLinear", LinearRadialScale, defaultConfig);

};

},{}],43:[function(require,module,exports){
/*global window: false */
"use strict";

var moment = require(1);
moment = typeof(moment) === 'function' ? moment : window.moment;

module.exports = function(Chart) {

	var helpers = Chart.helpers;
	var time = {
		units: [{
			name: 'millisecond',
			steps: [1, 2, 5, 10, 20, 50, 100, 250, 500]
		}, {
			name: 'second',
			steps: [1, 2, 5, 10, 30]
		}, {
			name: 'minute',
			steps: [1, 2, 5, 10, 30]
		}, {
			name: 'hour',
			steps: [1, 2, 3, 6, 12]
		}, {
			name: 'day',
			steps: [1, 2, 5]
		}, {
			name: 'week',
			maxStep: 4
		}, {
			name: 'month',
			maxStep: 3
		}, {
			name: 'quarter',
			maxStep: 4
		}, {
			name: 'year',
			maxStep: false
		}]
	};

	var defaultConfig = {
		position: "bottom",

		time: {
			parser: false, // false == a pattern string from http://momentjs.com/docs/#/parsing/string-format/ or a custom callback that converts its argument to a moment
			format: false, // DEPRECATED false == date objects, moment object, callback or a pattern string from http://momentjs.com/docs/#/parsing/string-format/
			unit: false, // false == automatic or override with week, month, year, etc.
			round: false, // none, or override with week, month, year, etc.
			displayFormat: false, // DEPRECATED
			isoWeekday: false, // override week start day - see http://momentjs.com/docs/#/get-set/iso-weekday/

			// defaults to unit's corresponding unitFormat below or override using pattern string from http://momentjs.com/docs/#/displaying/format/
			displayFormats: {
				'millisecond': 'h:mm:ss.SSS a', // 11:20:01.123 AM,
				'second': 'h:mm:ss a', // 11:20:01 AM
				'minute': 'h:mm:ss a', // 11:20:01 AM
				'hour': 'MMM D, hA', // Sept 4, 5PM
				'day': 'll', // Sep 4 2015
				'week': 'll', // Week 46, or maybe "[W]WW - YYYY" ?
				'month': 'MMM YYYY', // Sept 2015
				'quarter': '[Q]Q - YYYY', // Q3
				'year': 'YYYY' // 2015
			}
		},
		ticks: {
			autoSkip: false
		}
	};

	var TimeScale = Chart.Scale.extend({
		initialize: function() {
			if (!moment) {
				throw new Error('Chart.js - Moment.js could not be found! You must include it before Chart.js to use the time scale. Download at https://momentjs.com');
			}

			Chart.Scale.prototype.initialize.call(this);
		},
		getLabelMoment: function(datasetIndex, index) {
			return this.labelMoments[datasetIndex][index];
		},
		getMomentStartOf: function(tick) {
			var me = this;
			if (me.options.time.unit === 'week' && me.options.time.isoWeekday !== false) {
				return tick.clone().startOf('isoWeek').isoWeekday(me.options.time.isoWeekday);
			} else {
				return tick.clone().startOf(me.tickUnit);
			}
		},
		determineDataLimits: function() {
			var me = this;
			me.labelMoments = [];

			// Only parse these once. If the dataset does not have data as x,y pairs, we will use
			// these
			var scaleLabelMoments = [];
			if (me.chart.data.labels && me.chart.data.labels.length > 0) {
				helpers.each(me.chart.data.labels, function(label, index) {
					var labelMoment = me.parseTime(label);

					if (labelMoment.isValid()) {
						if (me.options.time.round) {
							labelMoment.startOf(me.options.time.round);
						}
						scaleLabelMoments.push(labelMoment);
					}
				}, me);

				me.firstTick = moment.min.call(me, scaleLabelMoments);
				me.lastTick = moment.max.call(me, scaleLabelMoments);
			} else {
				me.firstTick = null;
				me.lastTick = null;
			}

			helpers.each(me.chart.data.datasets, function(dataset, datasetIndex) {
				var momentsForDataset = [];
				var datasetVisible = me.chart.isDatasetVisible(datasetIndex);

				if (typeof dataset.data[0] === 'object' && dataset.data[0] !== null) {
					helpers.each(dataset.data, function(value, index) {
						var labelMoment = me.parseTime(me.getRightValue(value));

						if (labelMoment.isValid()) {
							if (me.options.time.round) {
								labelMoment.startOf(me.options.time.round);
							}
							momentsForDataset.push(labelMoment);

							if (datasetVisible) {
								// May have gone outside the scale ranges, make sure we keep the first and last ticks updated
								me.firstTick = me.firstTick !== null ? moment.min(me.firstTick, labelMoment) : labelMoment;
								me.lastTick = me.lastTick !== null ? moment.max(me.lastTick, labelMoment) : labelMoment;
							}
						}
					}, me);
				} else {
					// We have no labels. Use the ones from the scale
					momentsForDataset = scaleLabelMoments;
				}

				me.labelMoments.push(momentsForDataset);
			}, me);

			// Set these after we've done all the data
			if (me.options.time.min) {
				me.firstTick = me.parseTime(me.options.time.min);
			}

			if (me.options.time.max) {
				me.lastTick = me.parseTime(me.options.time.max);
			}

			// We will modify these, so clone for later
			me.firstTick = (me.firstTick || moment()).clone();
			me.lastTick = (me.lastTick || moment()).clone();
		},
		buildTicks: function(index) {
			var me = this;

			me.ctx.save();
			var tickFontSize = helpers.getValueOrDefault(me.options.ticks.fontSize, Chart.defaults.global.defaultFontSize);
			var tickFontStyle = helpers.getValueOrDefault(me.options.ticks.fontStyle, Chart.defaults.global.defaultFontStyle);
			var tickFontFamily = helpers.getValueOrDefault(me.options.ticks.fontFamily, Chart.defaults.global.defaultFontFamily);
			var tickLabelFont = helpers.fontString(tickFontSize, tickFontStyle, tickFontFamily);
			me.ctx.font = tickLabelFont;

			me.ticks = [];
			me.unitScale = 1; // How much we scale the unit by, ie 2 means 2x unit per step
			me.scaleSizeInUnits = 0; // How large the scale is in the base unit (seconds, minutes, etc)

			// Set unit override if applicable
			if (me.options.time.unit) {
				me.tickUnit = me.options.time.unit || 'day';
				me.displayFormat = me.options.time.displayFormats[me.tickUnit];
				me.scaleSizeInUnits = me.lastTick.diff(me.firstTick, me.tickUnit, true);
				me.unitScale = helpers.getValueOrDefault(me.options.time.unitStepSize, 1);
			} else {
				// Determine the smallest needed unit of the time
				var innerWidth = me.isHorizontal() ? me.width - (me.paddingLeft + me.paddingRight) : me.height - (me.paddingTop + me.paddingBottom);

				// Crude approximation of what the label length might be
				var tempFirstLabel = me.tickFormatFunction(me.firstTick, 0, []);
				var tickLabelWidth = me.ctx.measureText(tempFirstLabel).width;
				var cosRotation = Math.cos(helpers.toRadians(me.options.ticks.maxRotation));
				var sinRotation = Math.sin(helpers.toRadians(me.options.ticks.maxRotation));
				tickLabelWidth = (tickLabelWidth * cosRotation) + (tickFontSize * sinRotation);
				var labelCapacity = innerWidth / (tickLabelWidth);

				// Start as small as possible
				me.tickUnit = 'millisecond';
				me.scaleSizeInUnits = me.lastTick.diff(me.firstTick, me.tickUnit, true);
				me.displayFormat = me.options.time.displayFormats[me.tickUnit];

				var unitDefinitionIndex = 0;
				var unitDefinition = time.units[unitDefinitionIndex];

				// While we aren't ideal and we don't have units left
				while (unitDefinitionIndex < time.units.length) {
					// Can we scale this unit. If `false` we can scale infinitely
					me.unitScale = 1;

					if (helpers.isArray(unitDefinition.steps) && Math.ceil(me.scaleSizeInUnits / labelCapacity) < helpers.max(unitDefinition.steps)) {
						// Use one of the prefedined steps
						for (var idx = 0; idx < unitDefinition.steps.length; ++idx) {
							if (unitDefinition.steps[idx] >= Math.ceil(me.scaleSizeInUnits / labelCapacity)) {
								me.unitScale = helpers.getValueOrDefault(me.options.time.unitStepSize, unitDefinition.steps[idx]);
								break;
							}
						}

						break;
					} else if ((unitDefinition.maxStep === false) || (Math.ceil(me.scaleSizeInUnits / labelCapacity) < unitDefinition.maxStep)) {
						// We have a max step. Scale this unit
						me.unitScale = helpers.getValueOrDefault(me.options.time.unitStepSize, Math.ceil(me.scaleSizeInUnits / labelCapacity));
						break;
					} else {
						// Move to the next unit up
						++unitDefinitionIndex;
						unitDefinition = time.units[unitDefinitionIndex];

						me.tickUnit = unitDefinition.name;
						var leadingUnitBuffer = me.firstTick.diff(me.getMomentStartOf(me.firstTick), me.tickUnit, true);
						var trailingUnitBuffer = me.getMomentStartOf(me.lastTick.clone().add(1, me.tickUnit)).diff(me.lastTick, me.tickUnit, true);
						me.scaleSizeInUnits = me.lastTick.diff(me.firstTick, me.tickUnit, true) + leadingUnitBuffer + trailingUnitBuffer;
						me.displayFormat = me.options.time.displayFormats[unitDefinition.name];
					}
				}
			}

			var roundedStart;

			// Only round the first tick if we have no hard minimum
			if (!me.options.time.min) {
				me.firstTick = me.getMomentStartOf(me.firstTick);
				roundedStart = me.firstTick;
			} else {
				roundedStart = me.getMomentStartOf(me.firstTick);
			}

			// Only round the last tick if we have no hard maximum
			if (!me.options.time.max) {
				var roundedEnd = me.getMomentStartOf(me.lastTick);
				if (roundedEnd.diff(me.lastTick, me.tickUnit, true) !== 0) {
					// Do not use end of because we need me to be in the next time unit
					me.lastTick = me.getMomentStartOf(me.lastTick.add(1, me.tickUnit));
				}
			}

			me.smallestLabelSeparation = me.width;

			helpers.each(me.chart.data.datasets, function(dataset, datasetIndex) {
				for (var i = 1; i < me.labelMoments[datasetIndex].length; i++) {
					me.smallestLabelSeparation = Math.min(me.smallestLabelSeparation, me.labelMoments[datasetIndex][i].diff(me.labelMoments[datasetIndex][i - 1], me.tickUnit, true));
				}
			}, me);

			// Tick displayFormat override
			if (me.options.time.displayFormat) {
				me.displayFormat = me.options.time.displayFormat;
			}

			// first tick. will have been rounded correctly if options.time.min is not specified
			me.ticks.push(me.firstTick.clone());

			// For every unit in between the first and last moment, create a moment and add it to the ticks tick
			for (var i = 1; i <= me.scaleSizeInUnits; ++i) {
				var newTick = roundedStart.clone().add(i, me.tickUnit);

				// Are we greater than the max time
				if (me.options.time.max && newTick.diff(me.lastTick, me.tickUnit, true) >= 0) {
					break;
				}

				if (i % me.unitScale === 0) {
					me.ticks.push(newTick);
				}
			}

			// Always show the right tick
			var diff = me.ticks[me.ticks.length - 1].diff(me.lastTick, me.tickUnit);
			if (diff !== 0 || me.scaleSizeInUnits === 0) {
				// this is a weird case. If the <max> option is the same as the end option, we can't just diff the times because the tick was created from the roundedStart
				// but the last tick was not rounded.
				if (me.options.time.max) {
					me.ticks.push(me.lastTick.clone());
					me.scaleSizeInUnits = me.lastTick.diff(me.ticks[0], me.tickUnit, true);
				} else {
					me.ticks.push(me.lastTick.clone());
					me.scaleSizeInUnits = me.lastTick.diff(me.firstTick, me.tickUnit, true);
				}
			}

			me.ctx.restore();
		},
		// Get tooltip label
		getLabelForIndex: function(index, datasetIndex) {
			var me = this;
			var label = me.chart.data.labels && index < me.chart.data.labels.length ? me.chart.data.labels[index] : '';

			if (typeof me.chart.data.datasets[datasetIndex].data[0] === 'object') {
				label = me.getRightValue(me.chart.data.datasets[datasetIndex].data[index]);
			}

			// Format nicely
			if (me.options.time.tooltipFormat) {
				label = me.parseTime(label).format(me.options.time.tooltipFormat);
			}

			return label;
		},
		// Function to format an individual tick mark
		tickFormatFunction: function tickFormatFunction(tick, index, ticks) {
			var formattedTick = tick.format(this.displayFormat);
			var tickOpts = this.options.ticks;
			var callback = helpers.getValueOrDefault(tickOpts.callback, tickOpts.userCallback);

			if (callback) {
				return callback(formattedTick, index, ticks);
			} else {
				return formattedTick;
			}
		},
		convertTicksToLabels: function() {
			var me = this;
			me.tickMoments = me.ticks;
			me.ticks = me.ticks.map(me.tickFormatFunction, me);
		},
		getPixelForValue: function(value, index, datasetIndex, includeOffset) {
			var me = this;
			var labelMoment = value && value.isValid && value.isValid() ? value : me.getLabelMoment(datasetIndex, index);

			if (labelMoment) {
				var offset = labelMoment.diff(me.firstTick, me.tickUnit, true);

				var decimal = offset / me.scaleSizeInUnits;

				if (me.isHorizontal()) {
					var innerWidth = me.width - (me.paddingLeft + me.paddingRight);
					var valueWidth = innerWidth / Math.max(me.ticks.length - 1, 1);
					var valueOffset = (innerWidth * decimal) + me.paddingLeft;

					return me.left + Math.round(valueOffset);
				} else {
					var innerHeight = me.height - (me.paddingTop + me.paddingBottom);
					var valueHeight = innerHeight / Math.max(me.ticks.length - 1, 1);
					var heightOffset = (innerHeight * decimal) + me.paddingTop;

					return me.top + Math.round(heightOffset);
				}
			}
		},
		getPixelForTick: function(index, includeOffset) {
			return this.getPixelForValue(this.tickMoments[index], null, null, includeOffset);
		},
		getValueForPixel: function(pixel) {
			var me = this;
			var innerDimension = me.isHorizontal() ? me.width - (me.paddingLeft + me.paddingRight) : me.height - (me.paddingTop + me.paddingBottom);
			var offset = (pixel - (me.isHorizontal() ? me.left + me.paddingLeft : me.top + me.paddingTop)) / innerDimension;
			offset *= me.scaleSizeInUnits;
			return me.firstTick.clone().add(moment.duration(offset, me.tickUnit).asSeconds(), 'seconds');
		},
		parseTime: function(label) {
			var me = this;
			if (typeof me.options.time.parser === 'string') {
				return moment(label, me.options.time.parser);
			}
			if (typeof me.options.time.parser === 'function') {
				return me.options.time.parser(label);
			}
			// Date objects
			if (typeof label.getMonth === 'function' || typeof label === 'number') {
				return moment(label);
			}
			// Moment support
			if (label.isValid && label.isValid()) {
				return label;
			}
			// Custom parsing (return an instance of moment)
			if (typeof me.options.time.format !== 'string' && me.options.time.format.call) {
				console.warn("options.time.format is deprecated and replaced by options.time.parser. See http://nnnick.github.io/Chart.js/docs-v2/#scales-time-scale");
				return me.options.time.format(label);
			}
			// Moment format parsing
			return moment(label, me.options.time.format);
		}
	});
	Chart.scaleService.registerScaleType("time", TimeScale, defaultConfig);

};

},{"1":1}]},{},[7])(7)
});
/* NProgress, (c) 2013, 2014 Rico Sta. Cruz - http://ricostacruz.com/nprogress
 * @license MIT */


;(function(root, factory) {

  if (typeof define === 'function' && define.amd) {
    define(factory);
  } else if (typeof exports === 'object') {
    module.exports = factory();
  } else {
    root.NProgress = factory();
  }

})(this, function() {
  var NProgress = {};

  NProgress.version = '0.2.0';

  var Settings = NProgress.settings = {
    minimum: 0.08,
    easing: 'ease',
    positionUsing: '',
    speed: 200,
    trickle: true,
    trickleRate: 0.02,
    trickleSpeed: 800,
    showSpinner: true,
    barSelector: '[role="bar"]',
    spinnerSelector: '[role="spinner"]',
    parent: 'body',
    template: '<div class="bar" role="bar"><div class="peg"></div></div><div class="spinner" role="spinner"><div class="spinner-icon"></div></div>'
  };

  /**
   * Updates configuration.
   *
   *     NProgress.configure({
   *       minimum: 0.1
   *     });
   */
  NProgress.configure = function(options) {
    var key, value;
    for (key in options) {
      value = options[key];
      if (value !== undefined && options.hasOwnProperty(key)) Settings[key] = value;
    }

    return this;
  };

  /**
   * Last number.
   */

  NProgress.status = null;

  /**
   * Sets the progress bar status, where `n` is a number from `0.0` to `1.0`.
   *
   *     NProgress.set(0.4);
   *     NProgress.set(1.0);
   */

  NProgress.set = function(n) {
    var started = NProgress.isStarted();

    n = clamp(n, Settings.minimum, 1);
    NProgress.status = (n === 1 ? null : n);

    var progress = NProgress.render(!started),
        bar      = progress.querySelector(Settings.barSelector),
        speed    = Settings.speed,
        ease     = Settings.easing;

    progress.offsetWidth; /* Repaint */

    queue(function(next) {
      // Set positionUsing if it hasn't already been set
      if (Settings.positionUsing === '') Settings.positionUsing = NProgress.getPositioningCSS();

      // Add transition
      css(bar, barPositionCSS(n, speed, ease));

      if (n === 1) {
        // Fade out
        css(progress, {
          transition: 'none',
          opacity: 1
        });
        progress.offsetWidth; /* Repaint */

        setTimeout(function() {
          css(progress, {
            transition: 'all ' + speed + 'ms linear',
            opacity: 0
          });
          setTimeout(function() {
            NProgress.remove();
            next();
          }, speed);
        }, speed);
      } else {
        setTimeout(next, speed);
      }
    });

    return this;
  };

  NProgress.isStarted = function() {
    return typeof NProgress.status === 'number';
  };

  /**
   * Shows the progress bar.
   * This is the same as setting the status to 0%, except that it doesn't go backwards.
   *
   *     NProgress.start();
   *
   */
  NProgress.start = function() {
    if (!NProgress.status) NProgress.set(0);

    var work = function() {
      setTimeout(function() {
        if (!NProgress.status) return;
        NProgress.trickle();
        work();
      }, Settings.trickleSpeed);
    };

    if (Settings.trickle) work();

    return this;
  };

  /**
   * Hides the progress bar.
   * This is the *sort of* the same as setting the status to 100%, with the
   * difference being `done()` makes some placebo effect of some realistic motion.
   *
   *     NProgress.done();
   *
   * If `true` is passed, it will show the progress bar even if its hidden.
   *
   *     NProgress.done(true);
   */

  NProgress.done = function(force) {
    if (!force && !NProgress.status) return this;

    return NProgress.inc(0.3 + 0.5 * Math.random()).set(1);
  };

  /**
   * Increments by a random amount.
   */

  NProgress.inc = function(amount) {
    var n = NProgress.status;

    if (!n) {
      return NProgress.start();
    } else {
      if (typeof amount !== 'number') {
        amount = (1 - n) * clamp(Math.random() * n, 0.1, 0.95);
      }

      n = clamp(n + amount, 0, 0.994);
      return NProgress.set(n);
    }
  };

  NProgress.trickle = function() {
    return NProgress.inc(Math.random() * Settings.trickleRate);
  };

  /**
   * Waits for all supplied jQuery promises and
   * increases the progress as the promises resolve.
   *
   * @param $promise jQUery Promise
   */
  (function() {
    var initial = 0, current = 0;

    NProgress.promise = function($promise) {
      if (!$promise || $promise.state() === "resolved") {
        return this;
      }

      if (current === 0) {
        NProgress.start();
      }

      initial++;
      current++;

      $promise.always(function() {
        current--;
        if (current === 0) {
            initial = 0;
            NProgress.done();
        } else {
            NProgress.set((initial - current) / initial);
        }
      });

      return this;
    };

  })();

  /**
   * (Internal) renders the progress bar markup based on the `template`
   * setting.
   */

  NProgress.render = function(fromStart) {
    if (NProgress.isRendered()) return document.getElementById('nprogress');

    addClass(document.documentElement, 'nprogress-busy');

    var progress = document.createElement('div');
    progress.id = 'nprogress';
    progress.innerHTML = Settings.template;

    var bar      = progress.querySelector(Settings.barSelector),
        perc     = fromStart ? '-100' : toBarPerc(NProgress.status || 0),
        parent   = document.querySelector(Settings.parent),
        spinner;

    css(bar, {
      transition: 'all 0 linear',
      transform: 'translate3d(' + perc + '%,0,0)'
    });

    if (!Settings.showSpinner) {
      spinner = progress.querySelector(Settings.spinnerSelector);
      spinner && removeElement(spinner);
    }

    if (parent != document.body) {
      addClass(parent, 'nprogress-custom-parent');
    }

    parent.appendChild(progress);
    return progress;
  };

  /**
   * Removes the element. Opposite of render().
   */

  NProgress.remove = function() {
    removeClass(document.documentElement, 'nprogress-busy');
    removeClass(document.querySelector(Settings.parent), 'nprogress-custom-parent');
    var progress = document.getElementById('nprogress');
    progress && removeElement(progress);
  };

  /**
   * Checks if the progress bar is rendered.
   */

  NProgress.isRendered = function() {
    return !!document.getElementById('nprogress');
  };

  /**
   * Determine which positioning CSS rule to use.
   */

  NProgress.getPositioningCSS = function() {
    // Sniff on document.body.style
    var bodyStyle = document.body.style;

    // Sniff prefixes
    var vendorPrefix = ('WebkitTransform' in bodyStyle) ? 'Webkit' :
                       ('MozTransform' in bodyStyle) ? 'Moz' :
                       ('msTransform' in bodyStyle) ? 'ms' :
                       ('OTransform' in bodyStyle) ? 'O' : '';

    if (vendorPrefix + 'Perspective' in bodyStyle) {
      // Modern browsers with 3D support, e.g. Webkit, IE10
      return 'translate3d';
    } else if (vendorPrefix + 'Transform' in bodyStyle) {
      // Browsers without 3D support, e.g. IE9
      return 'translate';
    } else {
      // Browsers without translate() support, e.g. IE7-8
      return 'margin';
    }
  };

  /**
   * Helpers
   */

  function clamp(n, min, max) {
    if (n < min) return min;
    if (n > max) return max;
    return n;
  }

  /**
   * (Internal) converts a percentage (`0..1`) to a bar translateX
   * percentage (`-100%..0%`).
   */

  function toBarPerc(n) {
    return (-1 + n) * 100;
  }


  /**
   * (Internal) returns the correct CSS for changing the bar's
   * position given an n percentage, and speed and ease from Settings
   */

  function barPositionCSS(n, speed, ease) {
    var barCSS;

    if (Settings.positionUsing === 'translate3d') {
      barCSS = { transform: 'translate3d('+toBarPerc(n)+'%,0,0)' };
    } else if (Settings.positionUsing === 'translate') {
      barCSS = { transform: 'translate('+toBarPerc(n)+'%,0)' };
    } else {
      barCSS = { 'margin-left': toBarPerc(n)+'%' };
    }

    barCSS.transition = 'all '+speed+'ms '+ease;

    return barCSS;
  }

  /**
   * (Internal) Queues a function to be executed.
   */

  var queue = (function() {
    var pending = [];

    function next() {
      var fn = pending.shift();
      if (fn) {
        fn(next);
      }
    }

    return function(fn) {
      pending.push(fn);
      if (pending.length == 1) next();
    };
  })();

  /**
   * (Internal) Applies css properties to an element, similar to the jQuery
   * css method.
   *
   * While this helper does assist with vendor prefixed property names, it
   * does not perform any manipulation of values prior to setting styles.
   */

  var css = (function() {
    var cssPrefixes = [ 'Webkit', 'O', 'Moz', 'ms' ],
        cssProps    = {};

    function camelCase(string) {
      return string.replace(/^-ms-/, 'ms-').replace(/-([\da-z])/gi, function(match, letter) {
        return letter.toUpperCase();
      });
    }

    function getVendorProp(name) {
      var style = document.body.style;
      if (name in style) return name;

      var i = cssPrefixes.length,
          capName = name.charAt(0).toUpperCase() + name.slice(1),
          vendorName;
      while (i--) {
        vendorName = cssPrefixes[i] + capName;
        if (vendorName in style) return vendorName;
      }

      return name;
    }

    function getStyleProp(name) {
      name = camelCase(name);
      return cssProps[name] || (cssProps[name] = getVendorProp(name));
    }

    function applyCss(element, prop, value) {
      prop = getStyleProp(prop);
      element.style[prop] = value;
    }

    return function(element, properties) {
      var args = arguments,
          prop,
          value;

      if (args.length == 2) {
        for (prop in properties) {
          value = properties[prop];
          if (value !== undefined && properties.hasOwnProperty(prop)) applyCss(element, prop, value);
        }
      } else {
        applyCss(element, args[1], args[2]);
      }
    }
  })();

  /**
   * (Internal) Determines if an element or space separated list of class names contains a class name.
   */

  function hasClass(element, name) {
    var list = typeof element == 'string' ? element : classList(element);
    return list.indexOf(' ' + name + ' ') >= 0;
  }

  /**
   * (Internal) Adds a class to an element.
   */

  function addClass(element, name) {
    var oldList = classList(element),
        newList = oldList + name;

    if (hasClass(oldList, name)) return;

    // Trim the opening space.
    element.className = newList.substring(1);
  }

  /**
   * (Internal) Removes a class from an element.
   */

  function removeClass(element, name) {
    var oldList = classList(element),
        newList;

    if (!hasClass(element, name)) return;

    // Replace the class name.
    newList = oldList.replace(' ' + name + ' ', ' ');

    // Trim the opening and closing spaces.
    element.className = newList.substring(1, newList.length - 1);
  }

  /**
   * (Internal) Gets a space separated list of the class names on the element.
   * The list is wrapped with a single space on each end to facilitate finding
   * matches within the list.
   */

  function classList(element) {
    return (' ' + (element.className || '') + ' ').replace(/\s+/gi, ' ');
  }

  /**
   * (Internal) Removes an element from the DOM.
   */

  function removeElement(element) {
    element && element.parentNode && element.parentNode.removeChild(element);
  }

  return NProgress;
});
(function() {


}).call(this);
(function() {


}).call(this);
(function() {


}).call(this);
$(document).ready(function(){ 
    $(window).scroll(function(){ 
        if ($(this).scrollTop() > 100) { 
            $('#scroll').fadeIn(); 
            $('.header').addClass('shrink');
            $('.header .logo').addClass('shrink');
            $('.openbtn').addClass('shrink');
        } else { 
            $('#scroll').fadeOut(); 
            $('.header').removeClass('shrink');
            $('.header .logo').removeClass('shrink');
            $('.openbtn').removeClass('shrink');
        } 
    }); 
    $('#scroll').click(function(){ 
        $("html, body").animate({ scrollTop: 0 }, 600); 
        return false; 
    }); 
});

/* Set the width of the side navigation to 250px */

function openNav() {
    
if (window.matchMedia('screen and (max-width:970px) , (max-height:465px').matches) {
// do smth
document.getElementById("mySidenav").style.width = "100%";
} else {
// do smth.else 
document.getElementById("mySidenav").style.width = "25%";
}



}

/* Set the width of the side navigation to 0 */
function closeNav() {
    document.getElementById("mySidenav").style.width = "0";
}

;

$(document).ready(function(){
  
  $('#list_all').click(function() {
   
        if (this.checked) {
            $(':checkbox').each(function() {
                this.checked = true;
            });
        } else {
            $(':checkbox').each(function() {
                this.checked = false;
            });
        }
    });
  $('.e11, .product_type_dropdown, .brand_dropdown').on('select2:unselecting', function() {
    var opts = $(this).data('select2').options;
    opts.set('disabled', true);
    setTimeout(function() {
        opts.set('disabled', false);
    }, 1);
});

  $(".e11").select2({
    minimumInputLength: 3,
    width: "100%",
    allowClear: true,
    placeholder: "Click Here",
    data: 
    [
    {id:"",text:""},
        {id:"1",text:"Avalon Organics Lavender Luminosity Renewal Facial Serum"},
{id:"2",text:"Aveda Tourmaline Charged Radiant Skin Refiner"},
{id:"3",text:"Algenist Repairing Tint & Radiance Moisturizer Broad Spectrum SPF 30"},
{id:"4",text:"Aveeno Mens After Shave Lotion"},
{id:"5",text:"BeautiControl BC Spa Resurface Multi-Acid Resurfacing Peel"},
{id:"6",text:"Andalou Naturals Purple Carrot + C Luminous Night Cream"},
{id:"7",text:"BeautiControl Regeneration Tight, Firm & Fill Firming Neck Serum"},
{id:"8",text:"Arbonne Aromassentials Awaken Bath and Shower Gel"},
{id:"9",text:"Alba Botanica Un-petroleum Vanilla SPF 18 Lip Balm"},
{id:"10",text:"Aubrey Organics Natural Lips"},
{id:"11",text:"AcneFree Therapeutic Sulfur Mask"},
{id:"12",text:"Arbonne Aromassentials Awaken Massage Oil"},
{id:"13",text:"Aubrey Organics Pure Aloe Vera"},
{id:"14",text:"BeautiControl BC Spa Facial Regenerating Tonic with AHAs"},
{id:"15",text:"Aubrey Organics Green Tea & Ginkgo Facial Cleansing Lotion, for Normal Skin"},
{id:"16",text:"BeautiControl Regeneration Tight, Firm & Fill Extreme Lip Treatment"},
{id:"17",text:"Alba Botanica Very Emollient Sunscreen Kids SPF 45"},
{id:"18",text:"Anthony Logistics for Men Facial Moisturizer SPF 15"},
{id:"19",text:"Aubrey Organics Natural Sun SPF 30+ Active Lifestyles Tropical Scent"},
{id:"20",text:"Aveeno Hydrosport Wet Skin Spray Sunscreen with Broad Spectrum SPF 30"},
{id:"21",text:"BeautiControl BC Spa Facial Exfoliating Polish"},
{id:"22",text:"Beautycounter Hydrate Everyday Body Lotion"},
{id:"23",text:"Avon Be Blushed Cheek Color"},
{id:"24",text:"Aveeno Skin Relief Moisturizing Lotion with Soothing Oat Essence, Fragrance-Free"},
{id:"25",text:"Aesop Parsley Seed Cleansing Masque"},
{id:"26",text:"Algenist GENIUS Ultimate Anti-Aging Melting Cleanser"},
{id:"27",text:"Aesop Fabulous Face Oil"},
{id:"28",text:"Acure Organics Coconut Argan Oil"},
{id:"29",text:"Almay Oil-Free Makeup Remover Towelettes"},
{id:"30",text:"Aveeno Baby Calming Comfort Lotion-Lavender & Vanilla"},
{id:"31",text:"Avalon Organics Lavender Luminosity Revitalizing Eye Gel"},
{id:"32",text:"Aveeno Baby Natural Protection Lotion Sunscreen with Broad Spectrum SPF 50"},
{id:"33",text:"Avon Glimmersticks Waterproof Eye Liner"},
{id:"34",text:"Aveeno Moisturizing Bar"},
{id:"35",text:"Aveeno Positively Radiant Skin Brightening Daily Scrub"},
{id:"36",text:"Avalon Organics CoQ10 Repair Perfecting Facial Toner"},
{id:"37",text:"Avon Clearskin Blemish Clearing 2-in-1 Treatment & Hydrator"},
{id:"38",text:"Alba Botanica Good & Clean Daily Detox Foaming Cleanser"},
{id:"39",text:"Avon Banishing Cream Skin Discoloration Improver"},
{id:"40",text:"Ahava Extreme Firming Eye Cream"},
{id:"41",text:"Alba Botanica Good & Healthy Tinted Perfector Lotion"},
{id:"42",text:"Aveda Lip Saver"},
{id:"43",text:"Anthony Logistics for Men Advanced Formula Lip Balm SPF 25 Mint and White Tea"},
{id:"44",text:"Acure Organics Eye Cream Chlorella + Edelweiss Stem Cell"},
{id:"45",text:"Alba Botanica Hawaiian Hand & Body Lotion Replenishing Cocoa Butter"},
{id:"46",text:"Anastasia of Beverly Hills Brow Wiz"},
{id:"47",text:"Anthony Logistics for Men Instant Fix Oil Control"},
{id:"48",text:"BeautiControl Skinlogics Tonic"},
{id:"49",text:"Avon Foot Works Therapeutic Cracked Heel Relief Cream"},
{id:"50",text:"Aveda Petal Essence Single Eye Color"},
{id:"51",text:"Beautycounter Touchup Skin Concealer Pen"},
{id:"52",text:"Anastasia of Beverly Hills Dipbrow Pomade"},
{id:"53",text:"Acure Organics Argan Oil 100% Certified Organic"},
{id:"54",text:"BeautiControl BC Spa Facial Defend & Restore Moisture Creme SPF 20"},
{id:"55",text:"Avon Anew Platinum Serum"},
{id:"56",text:"Becca Shimmering Skin Perfector"},
{id:"57",text:"Ahava Deadsea Plants Dry Oil Body Mist"},
{id:"58",text:"Almay Clear Complexion Makeup BlemisHeal Technology"},
{id:"59",text:"Bare Escentuals (Bare Minerals) bareMinerals Flawless Definition Curl & Lengthen Mascara"},
{id:"60",text:"Ahava Time To Smooth Age Control All Day Moisturizer SPF 15"},
{id:"61",text:"Aveeno Skin Relief Shower & Bath Oil"},
{id:"62",text:"BeautiControl Skinlogics Essentials Facial Scrub"},
{id:"63",text:"Avon Mark I-Mark Wet/Dry Eyeshadow"},
{id:"64",text:"Bare Escentuals (Bare Minerals) BareSkin Sheer Sun Serum Bronzer"},
{id:"65",text:"Ahava Purifying Mud Mask, for Oily Skin"},
{id:"66",text:"Avon Mark All Butter Now Lip Treat"},
{id:"67",text:"Avon Clearskin Professional Deep Pore Cleansing Scrub"},
{id:"68",text:"Andalou Naturals Fruit Stem Cell Revitalize Serum"},
{id:"69",text:"Banana Boat Protect & Hydrate Sunscreen Lotion SPF 30"},
{id:"70",text:"Almay One Coat Multi-Benefit Mascara"},
{id:"71",text:"Andalou Naturals Blossom + Leaf Toning Refresher"},
{id:"72",text:"Becca Shimmering Skin Perfector Poured"},
{id:"73",text:"Arbonne ABC Baby Care Body Lotion"},
{id:"74",text:"Bare Escentuals (Bare Minerals) bareMinerals SPF 20 Concealer"},
{id:"75",text:"AmorePacific Moisture Bound Skin Energy Hydration Delivery System"},
{id:"76",text:"Beautycounter Routine Exfoliator Polishing Cream"},
{id:"77",text:"Avon Mark Touch & Glow Shimmer Cream Cubes All Over Face Palette"},
{id:"78",text:"Aveda Green Science Perfecting Cleanser"},
{id:"79",text:"Aubrey Organics Rosa Mosqueta Night Creme with Alpha Lipoic Acid"},
{id:"80",text:"Avon Extra Lasting Eyeshadow Ink"},
{id:"81",text:"Beautycounter Countertime Restorative Night Cream"},
{id:"82",text:"Aubrey Organics Blue Green Algae with Grape Seed Extract Facial Cleansing Lotion, for Combination/Oily Skin"},
{id:"83",text:"Bare Escentuals (Bare Minerals) Stick Around Eye Primer"},
{id:"84",text:"BeautiControl Skinlogics Clear Purifying Scrub/Masque"},
{id:"85",text:"Avon Ideal Luminous Blush"},
{id:"86",text:"Ahava Time To Smooth Age Control Eye Cream"},
{id:"87",text:"Acure Organics Lemongrass + Moroccan Argan Oil Firming Body Lotion"},
{id:"88",text:"Avon Ideal Flawless Invisible Coverage Liquid Foundation SPF 15"},
{id:"89",text:"BeautiControl Skinlogics Clear Deep Cleansing Gel"},
{id:"90",text:"Bare Escentuals (Bare Minerals) bareMinerals READY Bronzer"},
{id:"91",text:"Avon Anew Ultimate 7S Cleanser"},
{id:"92",text:"Almay Line Smoothing Makeup SPF 15"},
{id:"93",text:"Beautycounter Countertime Uplifting Day Cream"},
{id:"94",text:"Bare Escentuals (Bare Minerals) bareMinerals Blemish Therapy"},
{id:"95",text:"Beautycounter Tint Skin Complexion Coverage"},
{id:"96",text:"AmorePacific Treatment Cleansing Oil Face & Eyes"},
{id:"97",text:"AcneFree Sensitive Skin 24 Hour Acne Clearing System"},
{id:"98",text:"Alba Botanica Very Emollient Sunscreen Mineral Protection, Kids SPF 30"},
{id:"99",text:"Banana Boat Triple Defense Spray Sunscreen for Men SPF 30"},
{id:"100",text:"Bare Escentuals (Bare Minerals) bareMinerals High Shine Eyecolor"},
{id:"101",text:"Avon Anew Rejuvenate Revitalizing 2-in-1 Gel Cleanser"},
{id:"102",text:"Alpha Skin Care Revitalizing Body Lotion"},
{id:"103",text:"Aveeno Ultra-Calming Foaming Cleanser"},
{id:"104",text:"Avon Elements Moisture Boost Daily Moisture Cream Broad Spectrum SPF 15 Oil-Free"},
{id:"105",text:"Aubrey Organics Blue Green Algae with Grape Seed Extract Soothing Mask, for Combination/Oily Skin"},
{id:"106",text:"Arbonne FC5 Moisturizing Night Creme"},
{id:"107",text:"Aveda Nourish-Mint Lip Definer"},
{id:"108",text:"Becca Line and Pore Corrector"},
{id:"109",text:"Aesop Oil-Free Facial Hydrating Serum"},
{id:"110",text:"Bare Escentuals (Bare Minerals) bareMinerals Flawless Definition Volumizing Mascara"},
{id:"111",text:"Avon Ideal Flawless Pressed Powder"},
{id:"112",text:"Alba Botanica Good & Healthy Broad Spectrum SPF 15 Moisturizer"},
{id:"113",text:"Avon Anew Clinical Resurfacing Expert Smoothing Fluid"},
{id:"114",text:"Arbonne RE9 Advanced for Men Shave Gel"},
{id:"115",text:"Beautycounter Countertime Radiance Firming Complex"},
{id:"116",text:"Arbonne FC5 Nurturing Day Lotion with SPF 20"},
{id:"117",text:"Avalon Organics Essential Lift Smoothing Cleanser"},
{id:"118",text:"Algenist Genius Ultimate Anti-Aging Cream"},
{id:"119",text:"Ahava Hand Cream for Men"},
{id:"120",text:"Almay Liquid Eyeliner"},
{id:"121",text:"Aesop B Triple C Facial Balancing Gel"},
{id:"122",text:"AmorePacific Color Control Cushion Compact SPF 50+"},
{id:"123",text:"Benefit Stay Dont Stray"},
{id:"124",text:"Almay Clear Complexion Concealer"},
{id:"125",text:"Aveeno Clear Complexion Daily Moisturizer"},
{id:"126",text:"Avon Dramatic Firming Cream"},
{id:"127",text:"Avon Mark Lipclick Full Color Lipstick"},
{id:"128",text:"Aveda Outer Peace Acne Spot Relief"},
{id:"129",text:"Aveda Uruku Bronzer"},
{id:"130",text:"Acne.org Treatment"},
{id:"131",text:"Avalon Organics CoQ10 Repair Facial Cleansing Milk"},
{id:"132",text:"Almay Line Smoothing Pressed Powder"},
{id:"133",text:"Avon Anew Clinical Advanced Wrinkle Corrector"},
{id:"134",text:"Aveeno Wash & Shampoo"},
{id:"135",text:"Aubrey Organics Natural Herbal Facial Astringent, for Oily Skin"},
{id:"136",text:"Avon Anew Ultimate 7S Night Cream"},
{id:"137",text:"Almay Intense i-Color Eyeliner"},
{id:"138",text:"Algenist Regenerative Anti-Aging Moisturizer"},
{id:"139",text:"Arbonne RE9 Advanced Night Repair Creme"},
{id:"140",text:"Aubrey Organics Sparkling Mineral Water Herbal Complexion Mist"},
{id:"141",text:"Aveeno Protect + Hydrate Lotion Sunscreen with Broad Spectrum SPF 50"},
{id:"142",text:"Aubrey Organics Lumessence Lift Firming Renewal Cream with CoQ10 Liposomes"},
{id:"143",text:"Algenist Concentrated Reconstructing Serum"},
{id:"144",text:"BeautiControl Platinum Regeneration Age-Defying Concealer"},
{id:"145",text:"Aveeno Positively Radiant Daily Moisturizer SPF 15"},
{id:"146",text:"Aveeno Smart Essentials Daily Detoxifying Scrub"},
{id:"147",text:"Avon Makeup Remover Wipes with Mineral Complex"},
{id:"148",text:"Arbonne Clear Advantage Acne Spot Treatment Salicylic Acid 2%"},
{id:"149",text:"Algenist Algae Brightening Mask"},
{id:"150",text:"Beautycounter Countertime Vibrant Eye Perfector"},
{id:"151",text:"Anastasia of Beverly Hills Clear Brow Gel"},
{id:"152",text:"Aveeno Positively Nourishing Body WashCalming"},
{id:"153",text:"BeautiControl Spa Manicure Extreme Repair Hand Creme"},
{id:"154",text:"Arbonne Calm Gentle Daily Cleanser"},
{id:"155",text:"Avon Mark Thats Brilliant! Face & Eye Luminizer"},
{id:"156",text:"Aubrey Organics Natural Sun SPF 15 Saving Face Unscented"},
{id:"157",text:"Algenist Targeted Age Correcting Serum"},
{id:"158",text:"Beautycounter Wash Everyday Body Wash"},
{id:"159",text:"BeautiControl Skinlogics Clear Spot Treatment Duo"},
{id:"160",text:"Aubrey Organics Natural Herbal Maintenance Oil Balancing Moisturizer, for Oily Skin"},
{id:"161",text:"Bare Escentuals (Bare Minerals) Hydrating Mineral Veil"},
{id:"162",text:"Avon Clearskin Professional Invisible Blemish Treatment"},
{id:"163",text:"Arbonne RE9 Advanced Intensive Renewal Serum"},
{id:"164",text:"Avon Foot Works Overnight Renewing Foot Cream"},
{id:"165",text:"Amazing Cosmetics VelvetMineral Liquid Foundation with AmazingConcealer Technology SPF 15"},
{id:"166",text:"Bare Escentuals (Bare Minerals) Makeup Remover Wipes"},
{id:"167",text:"Algenist Hydrating Essence Toner"},
{id:"168",text:"Anastasia of Beverly Hills Stencils"},
{id:"169",text:"Ahava Time To Clear Refreshing Cleansing Gel"},
{id:"170",text:"Almay Smart Shade Smart Balance Pressed Powder"},
{id:"171",text:"Arbonne RE9 Advanced For Men Post Shave Balm"},
{id:"172",text:"Bare Escentuals (Bare Minerals) bareMinerals Prime Time Foundation Primer"},
{id:"173",text:"Becca Beach Tint"},
{id:"174",text:"Amazing Cosmetics Velvet Mineral Pressed Powder Foundation"},
{id:"175",text:"Algenist Complete Eye Renewal Balm"},
{id:"176",text:"Beautycounter Any Time Eye Cream"},
{id:"177",text:"Avon SuperSHOCK Max Mascara Waterproof"},
{id:"178",text:"AmorePacific Treatment Cleansing Tissue"},
{id:"179",text:"Algenist Genius Ultimate Anti-Aging Bi-Phase Peel"},
{id:"180",text:"Almay Smart Shade Anti-Aging Makeup SPF 20"},
{id:"181",text:"Bare Escentuals (Bare Minerals) bareMinerals SPF 30 Natural Sunscreen"},
{id:"182",text:"Aveeno Daily Moisturizing Lotion SPF 15"},
{id:"183",text:"Avon Anew Clinical Absolute Even Dark Circle Corrector"},
{id:"184",text:"Avon Mark Please Hold Eye Primer"},
{id:"185",text:"Avon Elements Youth Restoring Anti-Wrinkle Moisture Cream Broad Spectrum SPF 20"},
{id:"186",text:"Avalon Organics Intense Defense with Vitamin C Facial Serum"},
{id:"187",text:"Aveeno Positively Ageless Daily Exfoliating Cleanser"},
{id:"188",text:"Algenist Multi-Perfecting Detoxifying Exfoliator"},
{id:"189",text:"Almay Shadow Softies by Intense i-Color"},
{id:"190",text:"Bare Escentuals (Bare Minerals) bareMinerals Intensive Glow Pads Brightening Treatment"},
{id:"191",text:"Avon Anew Clinical Absolute Even Spot Correcting Hand Cream SPF 15"},
{id:"192",text:"Avon Glimmersticks Lipliner"},
{id:"193",text:"Aesop Immediate Moisture Facial Hydrosol"},
{id:"194",text:"Beautycounter Glow Sugar Scrub"},
{id:"195",text:"Arbonne SeaSource Detox Spa Sea Mud Face & Body Mask"},
{id:"196",text:"Aveeno Continuous Protection Sunblock Lotion SPF 55, for Baby"},
{id:"197",text:"Anthony Logistics for Men Vitamin C Facial Serum"},
{id:"198",text:"Aveda Nourish-Mint Renewing Lip Treatment"},
{id:"199",text:"Avon Moisture Therapy Intensive Moisturizing Lip Treatment SPF 15"},
{id:"200",text:"Aveda Intensive Hydrating Masque"},
{id:"201",text:"Avon Clearskin Blackhead Eliminating Daily Astringent"},
{id:"202",text:"Almay Smart Shade Perfect & Correct Primer"},
{id:"203",text:"Alba Botanica Very Emollient Sunscreen Sport Broad Spectrum SPF 45"},
{id:"204",text:"Avalon Organics Lavender Luminosity Moisture Plus Lotion Broadspectrum SPF 15"},
{id:"205",text:"BeautiControl Regeneration Tight, Firm & Fill Eye Firming Serum"},
{id:"206",text:"beautyblender beautyblender original"},
{id:"207",text:"Aveeno Aveeno Baby Calming Comfort Bath- Lavendar & Vanilla"},
{id:"208",text:"Arbonne Shea Butter Hand & Body Wash"},
{id:"209",text:"Aubrey Organics Blue Green Algae with Grape Seed Extract Moisturizer SPF 15, for Combination/Oily Skin"},
{id:"210",text:"Andalou Naturals 1000 Roses Color + Correct Sheer Tan with SPF 30"},
{id:"211",text:"Bare Escentuals (Bare Minerals) bareMinerals Blush"},
{id:"212",text:"Benefit Magic Ink Liquid Jet-Black Eyeliner"},
{id:"213",text:"Bare Escentuals (Bare Minerals) bareMinerals Brow Color"},
{id:"214",text:"Aveeno Positively Radiant Daily Cleansing Pads"},
{id:"215",text:"Avon Smooth Minerals Foundation"},
{id:"216",text:"AmorePacific Moisture Bound Lip Treatment"},
{id:"217",text:"Ahava Time To Hydrate Essential Day Moisturizer, Combination skin"},
{id:"218",text:"Avalon Organics Lavender Luminosity Ultimate Night Cream"},
{id:"219",text:"Avon Anew Ultimate 7S Cream Broad Spectrum SPF 25"},
{id:"220",text:"Becca Under Eye Brightening Corrector"},
{id:"221",text:"BeautiControl Regeneration Overnight Retinol Recovery Serum"},
{id:"222",text:"Avon Anew Rejuvenate Day Revitalizing Cream SPF 25"},
{id:"223",text:"Arbonne ABC Herbal Diaper Rash Cream"},
{id:"224",text:"Aubrey Organics Rosa Mosqueta Luxurious Moisturizing Lotion"},
{id:"225",text:"Avalon Organics Lavender Luminosity Daily Moisturizer"},
{id:"226",text:"Algenist Multi-Perfecting Pore Corrector Gel Moisturizer"},
{id:"227",text:"Avon Anew Genics Treatment Cream"},
{id:"228",text:"Avalon Organics Lavender Exfoliating Enzyme Scrub"},
{id:"229",text:"Acure Organics Argan Cell Stimulating Body Wash"},
{id:"230",text:"Anastasia of Beverly Hills Brow Pen"},
{id:"231",text:"Arbonne FC5 Hydrating Eye Creme"},
{id:"232",text:"Arbonne RE9 Advanced Age-Defying Neck Cream"},
{id:"233",text:"Algenist Firming & Lifting Eye Gel"},
{id:"234",text:"Avon Elements Youth Restoring Anti-Wrinkle Eye Roller"},
{id:"235",text:"Arbonne FC5 Deep Cleansing Mask"},
{id:"236",text:"Arbonne FC5 Exfoliating New Cell Scrub"},
{id:"237",text:"Ahava Time To Hydrate Essential Day Moisturizer, Normal to Dry Skin"},
{id:"238",text:"Aveeno Clear Complexion Daily Cleansing Pads"},
{id:"239",text:"Bare Escentuals (Bare Minerals) Wrinkle Concentrate"},
{id:"240",text:"Algenist Overnight Restorative Cream"},
{id:"241",text:"BeautiControl BC Color Lip Perfecting Pencil"},
{id:"242",text:"Andalou Naturals BioActive 8 Berry Fruit Enzyme Mask"},
{id:"243",text:"Avon SuperShock Eye Liner"},
{id:"244",text:"Ahava Firming Body Cream"},
{id:"245",text:"Acure Organics Argan Cleansing Towelettes"},
{id:"246",text:"Alba Botanica Even Advanced Sea Kelp Facial Toner"},
{id:"247",text:"Aveda Pure-Formance Shave Cream"},
{id:"248",text:"Aubrey Organics Lumessence Rejuvenating Eye Creme with Liposomes"},
{id:"249",text:"Arbonne Revelage Age Spot Brightening Hand Therapy Broad Spectrum SPF 30 Sunscreen"},
{id:"250",text:"Ahava After Sun Body Lotion"},
{id:"251",text:"Aveeno Positively Nourishing Energizing Body Lotion"},
{id:"252",text:"Banana Boat Triple Defense Sunscreen Lotion for Men SPF 30"},
{id:"253",text:"Aveeno Stress Relief Foaming Bath"},
{id:"254",text:"Algenist Retinol Firming and Lifting Serum"},
{id:"255",text:"Bare Escentuals (Bare Minerals) Bareskin Pure Brightening Serum Foundation SPF 20"},
{id:"256",text:"Aveeno Protect + Hydrate Lotion Sunscreen with Broad Spectrum SPF 70"},
{id:"257",text:"Algenist Firming & Lifting Cream"},
{id:"258",text:"Avon Anew Vitale Night Cream"},
{id:"259",text:"Arbonne Prolief Natural Balancing Cream"},
{id:"260",text:"Aesop Moroccan Neroli Post-Shave Lotion"},
{id:"261",text:"Andalou Naturals Revitalizing Lash + Lid Make-Up Remover"},
{id:"262",text:"Beautycounter Kidscounter Squeaky Clean Body Wash"},
{id:"263",text:"Avon Clearskin Professional Acne Treatment System"},
{id:"264",text:"BeautiControl BC Spa Solutions Under Eye Dark Circle & Puffiness Solution"},
{id:"265",text:"Bare Escentuals (Bare Minerals) Prime Time Oil Control Foundation Primer"},
{id:"266",text:"Andalou Naturals 1000 Roses Color + Correct Sheer Nude with SPF 30"},
{id:"267",text:"Bare Escentuals (Bare Minerals) bareMinerals Liner Shadow"},
{id:"268",text:"Bare Escentuals (Bare Minerals) Buxom Stay-There Eye Shadow"},
{id:"269",text:"BeautiControl Regeneration Tight, Firm & Fill Extreme Eye Complex"},
{id:"270",text:"BeautiControl Skinlogics Lip Balm SPF 20"},
{id:"271",text:"AmorePacific Moisture Bound Rejuvenating Eye Treatment Gel"},
{id:"272",text:"Avon Anew Ultimate 7S Elixir"},
{id:"273",text:"Bare Escentuals (Bare Minerals) Clay Chameleon Transforming Purifying Cleanser"},
{id:"274",text:"Banana Boat Triple Defense Spray Sunscreen for Men SPF 50+"},
{id:"275",text:"Avalon Organics Vitamin C Renewal Rejuvenating Oil-Free Moisturizer"},
{id:"276",text:"Alba Botanica Even Advanced Sea Algae Enzyme Facial Scrub"},
{id:"277",text:"Avon Smooth Minerals Blush"},
{id:"278",text:"Andalou Naturals Citrus Kombucha Cleansing Gel"},
{id:"279",text:"Acure Organics Sensitive Facial Cleanser Argan Oil + Probiotic"},
{id:"280",text:"Ahava Skin Replenisher (Night), for Normal to Dry Skin"},
{id:"281",text:"Andalou Naturals Clementine Ginger Energizing Body Lotion"},
{id:"282",text:"Ahava Comforting Cream"},
{id:"283",text:"Bare Escentuals (Bare Minerals) Marvelous Moxie Lipliner"},
{id:"284",text:"Becca Glossy Lip Tint"},
{id:"285",text:"Alba Botanica Hawaiian Eye Gel Revitalizing Green Tea"},
{id:"286",text:"Almay Color + Care Liquid Lip Balm"},
{id:"287",text:"Andalou Naturals Ultra Sheer Daily Defense Facial Lotion with SPF 18"},
{id:"288",text:"Bare Escentuals (Bare Minerals) Marvelous Moxie Lipgloss"},
{id:"289",text:"Ahava Dermud Intensive Foot Cream"},
{id:"290",text:"Avon Elements Refreshing Cleansing Wipes"},
{id:"291",text:"Avon SuperExtend Liquid Eye Liner"},
{id:"292",text:"Anthony Logistics for Men Body Cleansing Gel Eucalyptus-Mint"},
{id:"293",text:"Anthony Logistics for Men Ingrown Hair Treatment"},
{id:"294",text:"Avon Kohl Eye Liner"},
{id:"295",text:"Ahava Sun Protection Anti-Aging Moisturizer SPF 30"},
{id:"296",text:"Aveeno Natural Protection Lotion Sunscreen with Broad Spectrum SPF 50"},
{id:"297",text:"Aesop Purifying Facial Exfoliant Paste"},
{id:"298",text:"Bare Escentuals (Bare Minerals) Advanced Protection SPF 20 Moisturizer with ActiveSoil Complex"},
{id:"299",text:"Alba Botanica Clear Lip Gloss"},
{id:"300",text:"BeautiControl Skinlogics Sensitive Hydra-Calm Moisturizer"},
{id:"301",text:"Aesop Parsley Seed Anti-Oxidant Eye Cream"},
{id:"302",text:"AmLactin Moisturizing Body Lotion"},
{id:"303",text:"Anastasia of Beverly Hills Waterproof Crme Color"},
{id:"304",text:"Avalon Organics Essential Lift Lifting Serum"},
{id:"305",text:"Ahava Sun Protection Anti-Aging Facial Moisturizer SPF 50"},
{id:"306",text:"Becca Mineral Blush"},
{id:"307",text:"BeautiControl BC Spa Facial Purifying Cleansing Lotion"},
{id:"308",text:"Aesop Mandarin Facial Hydrating Cream"},
{id:"309",text:"Acure Organics Seriously Firming Facial Serum"},
{id:"310",text:"Almay Smart Shade Makeup SPF 15"},
{id:"311",text:"Andalou Naturals Super Goji Peptide Perfecting Cream"},
{id:"312",text:"Bare Escentuals (Bare Minerals) Faux Tan Body Moisturizer"},
{id:"313",text:"Aubrey Organics Vegecell Nighttime Hydrator with Green Tea"},
{id:"314",text:"Aesop Parsley Seed Anti-Oxidant Facial Toner"},
{id:"315",text:"BeautiControl Regeneration Tight, Firm & Fill Face Cream"},
{id:"316",text:"Beautycounter Lustro Face Oil 3- Ylang Ylang & Chamomile"},
{id:"317",text:"Banana Boat Triple Defense Sunscreen Lotion for Men SPF 50"},
{id:"318",text:"Aveda Nourish-Mint Rehydrating Lip Glaze"},
{id:"319",text:"Ahava Time To Hydrate Essential Reviving Serum"},
{id:"320",text:"Bare Escentuals (Bare Minerals) Buxom Sculpted Lash Mascara"},
{id:"321",text:"Andalou Naturals 1000 Roses Daily Shade Facial Lotion with SPF 18"},
{id:"322",text:"Anthony Logistics for Men All Purpose Facial Moisturizer"},
{id:"323",text:"Bare Escentuals (Bare Minerals) Show Some Skin Weightless Foundation Broad Spectrum SPF 30"},
{id:"324",text:"Aveda Color Options Eye Shadow Transformer"},
{id:"325",text:"Aubrey Organics Natural Translucent Base"},
{id:"326",text:"AmorePacific Moisture Bound Rejuvenating Serum"},
{id:"327",text:"Aveeno Absolutely Ageless Daily Moisturizer with Sunscreen Broad Spectrum SPF 30"},
{id:"328",text:"Almay Smart Shade Concealer SPF 15"},
{id:"329",text:"Aubrey Organics Silken Earth Powder Blush"},
{id:"330",text:"Almay Smart Shade Powder Blush"},
{id:"331",text:"Algenist Regenerative Anti-Aging Moisturizer SPF 20"},
{id:"332",text:"Bare Escentuals (Bare Minerals) i.d. Shine Control Sheets"},
{id:"333",text:"Aveda Petal Essence Eye Color Trio"},
{id:"334",text:"Becca Shimmering Skin Perfector (Pressed)"},
{id:"335",text:"Aubrey Organics Vegecol with Aloe Alcohol-Free Facial Toner, for Sensitive Skin"},
{id:"336",text:"Avon Ideal Flawless Invisible Coverage Cream-to-Powder Foundation SPF 15"},
{id:"337",text:"Avon Anew Clinical Pro+ Line Eraser Treatment"},
{id:"338",text:"Bare Escentuals (Bare Minerals) 5-in-1 BB Cream Shadow SPF 15"},
{id:"339",text:"Aveeno Natural Protection Mineral Block Face Stick SPF 50"},
{id:"340",text:"Aveeno Positively Nourishing Body WashSmoothing"},
{id:"341",text:"Aveda Enbrightenment Brightening Treatment Toner"},
{id:"342",text:"Aveeno Smart Essentials Nighttime Moisture Infusion"},
{id:"343",text:"Beautycounter Countertime Nourishing Cleansing Balm"},
{id:"344",text:"Aveeno Clear Complexion Cream Cleanser"},
{id:"345",text:"Avon Anew Vitale Day Cream Broad Spectrum SPF 25"},
{id:"346",text:"Aveda Green Science Firming Face Creme"},
{id:"347",text:"Alpha Skin Care Dual Action Skin Lightener"},
{id:"348",text:"Avon Mark Glow Baby Glow Luxe Hook Up Lip Gloss/Glow Baby Glow Hook Up Lip Gloss"},
{id:"349",text:"Aveeno Absolutely Ageless Eye Cream"},
{id:"350",text:"Avon Super Drama Waterproof Mascara"},
{id:"351",text:"BeautiControl BC Color Brow Perfecting Pencil"},
{id:"352",text:"Andalou Naturals Argan + Mint Lip Remedy"},
{id:"353",text:"Aesop Rosehip Lip Seed Oil"},
{id:"354",text:"Aveda Balancing Infusion for Dry Skin"},
{id:"355",text:"Ahava Sun Protection Anti-Aging Moisturizing Spray SPF 30"},
{id:"356",text:"Avon Anew Platinum Day Cream SPF 25"},
{id:"357",text:"Almay Smart Shade CC Luminous Primer SPF 15"},
{id:"358",text:"Avon Moisture Effective Eye Makeup Remover Lotion"},
{id:"359",text:"AmLactin Foot Cream Therapy"},
{id:"360",text:"Avon SuperExtend Infinitize Mascara"},
{id:"361",text:"Bare Escentuals (Bare Minerals) bareVitamins Eye Rev-er Upper"},
{id:"362",text:"Anthony Logistics for Men Alcohol Free Toner"},
{id:"363",text:"Aveda Botanical Kinetics Energizing Eye Creme"},
{id:"364",text:"Avon Anew Genics Eye Treatment"},
{id:"365",text:"BeautiControl Extra Help Concealer"},
{id:"366",text:"Bare Escentuals (Bare Minerals) Purely Nourishing Cream with RareMinerals ActiveSoil Complex: Dry Skin"},
{id:"367",text:"Aveda Tourmaline Charged Hydrating Creme"},
{id:"368",text:"Aubrey Organics Natural Sun SPF 30+ Unscented Sensitive Skin/Children"},
{id:"369",text:"Bare Escentuals (Bare Minerals) bareMinerals Multi-Wrinkle Repair"},
{id:"370",text:"Bare Escentuals (Bare Minerals) bareMinerals Natural Light Face Lifting Duo"},
{id:"371",text:"Avon Anew Reversalist Complete Renewal Express Wrinkle Smoother"},
{id:"372",text:"Aesop Parsley Seed Anti-Oxidant Facial Treatment"},
{id:"373",text:"Arbonne FC5 Hydrating Cleanser + Freshener"},
{id:"374",text:"Becca Backlighting Primer Filter"},
{id:"375",text:"Aveeno Smart Essentials Daily Nourishing Moisturizer SPF 30"},
{id:"376",text:"Aveeno Daily Moisturizing Body Wash"},
{id:"377",text:"Alba Botanica Acnedote Oil Control Lotion"},
{id:"378",text:"AcneFree Acne Astringent Toner Pads 2-in-1 Dual Action"},
{id:"379",text:"Bare Escentuals (Bare Minerals) Faux Tan"},
{id:"380",text:"Avon Clearskin Professional Clear Pore Thermal Mask"},
{id:"381",text:"Arbonne RE9 Advanced Smoothing Facial Cleanser"},
{id:"382",text:"BeautiControl BC Color Tinted Lip Balm SPF 20"},
{id:"383",text:"BeautiControl Secret AGEnt Color Primer"},
{id:"384",text:"Aveda Inner Light Foundation Brush"},
{id:"385",text:"Ahava Time To Smooth Age Control Night Nourishment"},
{id:"386",text:"BeautiControl Skinlogics Essentials Moisturizer"},
{id:"387",text:"Alba Botanica Acnedote Maximum Strength Clean 'n Treat Daily Cleansing Towelettes"},
{id:"388",text:"Ahava Extreme Radiance Lifting Mask"},
{id:"389",text:"Avon Clearskin Professional Daily Correcting Lotion"},
{id:"390",text:"Anastasia of Beverly Hills Brow Enhancing Serum"},
{id:"391",text:"Avon Elements Moisturizing Cream Cleanser"},
{id:"392",text:"Aveda Green Science Lifting Serum"},
{id:"393",text:"Almay Brow Defining Pencil"},
{id:"394",text:"Andalou Naturals Aloe Mint Cooling Shower Gel"},
{id:"395",text:"Almay One Coat Multi-Benefit Mascara Waterproof"},
{id:"396",text:"Aveda Outer Peace Cooling Masque"},
{id:"397",text:"Almay Wake Up Undereye Concealer"},
{id:"398",text:"Alba Botanica Even Advanced Sea Plus Renewal Night Cream"},
{id:"399",text:"Avon Clearskin Blemish Clearing Acne Pads"},
{id:"400",text:"Avon ANEW CLINICAL Pro Line Eraser Eye Treatment"},
{id:"401",text:"Aveeno Clear Complexion Foaming Cleanser"},
{id:"402",text:"Alba Botanica Hawaiian Facial Cleanser Pore Purifying Pineapple Enzyme"},
{id:"403",text:"Avon Anew Reversalist Day Renewal Cream SPF 25"},
{id:"404",text:"Arbonne Shea Butter Hand & Body Lotion"},
{id:"405",text:"Aubrey Organics Rosa Mosqueta Rose Hip Moisturizing Cream, for Dry Skin"},
{id:"406",text:"Aveeno Positively Radiant Cleanser"},
{id:"407",text:"Aveeno Protect + Hydrate Lotion Sunscreen with Broad Spectrum SPF 30"},
{id:"408",text:"Bare Escentuals (Bare Minerals) bareMinerals READY SPF 15 Touch Up Veil"},
{id:"409",text:"Benefit Fakeup Hydrating Crease-Control Concealer"},
{id:"410",text:"Avon Nurtura Replenishing Cream"},
{id:"411",text:"Avon Anew Clinical ThermaFirm Face Lifting Cream"},
{id:"412",text:"Almay One Coat Get Up & Grow Mascara"},
{id:"413",text:"BeautiControl BC Color Intense Mineral Shadow Trio"},
{id:"414",text:"Alba Botanica Hawaiian Renewing Passion Fruit Nectar Lip Balm"},
{id:"415",text:"Acure Organics Facial Cleansing Crme Argan Oil + Mint"},
{id:"416",text:"Andalou Naturals Reservatrol Q10 Night Repair Cream"},
{id:"417",text:"Aveeno Creamy Moisturizing Oil"},
{id:"418",text:"Anthony Logistics for Men Facial Scrub"},
{id:"419",text:"Aesop Parsley Seed Anti-Oxidant Serum"},
{id:"420",text:"Aveda Pure-Formance Dual Action Aftershave"},
{id:"421",text:"Arbonne ABC Hair & Body Wash"},
{id:"422",text:"Aubrey Organics Sea Buckthorn & Cucumber with Ester-C Facial Toner, for Combination/Dry Skin"},
{id:"423",text:"Acure Organics Day Cream: Gotu Kola Stem Cell + 1% Chlorella Growth Factor"},
{id:"424",text:"Aesop Chamomile Concentrate Anti-Blemish Masque"},
{id:"425",text:"Andalou Naturals Lemon Sugar Facial Scrub"},
{id:"426",text:"BeautiControl Regeneration Tight, Firm & Fill Concealer"},
{id:"427",text:"Alba Botanica Very Emollient Body Lotion Original"},
{id:"428",text:"Aubrey Organics Herbessence Makeup Remover"},
{id:"429",text:"Anastasia of Beverly Hills 5-Element Brow Kit"},
{id:"430",text:"Avon Solutions a.m. Ageless Results Day Cream SPF 15"},
{id:"431",text:"Ahava Eye Makeup Remover"},
{id:"432",text:"Avon Wash-Off Waterproof Mascara"},
{id:"433",text:"Alba Botanica Hawaiian Lip Balm Refreshing Pineapple Quench"},
{id:"434",text:"Avon Anew Platinum Night Cream"},
{id:"435",text:"Aubrey Organics Mens Stock North Woods Shave Cream"},
{id:"436",text:"Aveeno Daily Moisture Lotion, Fragrance Free"},
{id:"437",text:"AcneFree 24 Hour Severe Acne Clearing System"},
{id:"438",text:"Arbonne RE9 Advanced For Men Facial Moisturizer SPF 20"},
{id:"439",text:"Avon Slick Tint for Lips"},
{id:"440",text:"Aveda Enbrightenment Brightening Correcting Serum"},
{id:"441",text:"Alba Botanica Hawaiian Body Cream Deep Moisturizing Kukui Nut"},
{id:"442",text:"Arbonne Made in the Shade Self-Tanner Broad Spectrum SPF 15"},
{id:"443",text:"Beautycounter Flat Complexion Brush"},
{id:"444",text:"Almay Clear Complexion Pressed Powder"},
{id:"445",text:"BeautiControl BC Color Eye Perfecting Pencil"},
{id:"446",text:"Aveda Tourmaline Charged Exfoliating Cleanser"},
{id:"447",text:"Avon Ultra Luxury Lipliner"},
{id:"448",text:"Avon Elements Skin Revitalize Night Radiance Cream"},
{id:"449",text:"Bare Escentuals (Bare Minerals) Buxom Full-On Lip Cream"},
{id:"450",text:"Bare Escentuals (Bare Minerals) Well-Cared For Brush Conditioning Shampoo"},
{id:"451",text:"Aveeno Positively Nourishing 24-Hour Ultra Hydrating Whipped Souffle"},
{id:"452",text:"Avon Totally Kissable Lip Gloss"},
{id:"453",text:"Bare Escentuals (Bare Minerals) Marvelous Moxie Lipstick"},
{id:"454",text:"Amazing Cosmetics Anti-Aging Face Primer"},
{id:"455",text:"Avon Anew Beauty Age-Transforming Concealer SPF 15"},
{id:"456",text:"Aesop Damascan Rose Facial Treatment"},
{id:"457",text:"Aveeno Positively Ageless Lifting & Firming Eye Cream"},
{id:"458",text:"Aveeno Positively Ageless Skin Strengthening Body Cream"},
{id:"459",text:"Avon Shine Attract Lipstick"},
{id:"460",text:"Avon Moisture Therapy Intensive Extra Strength Cream"},
{id:"461",text:"AcneFree Oatmeal & Jojoba Gentle Acne Scrub"},
{id:"462",text:"Bare Escentuals (Bare Minerals) Buxom Full-Bodied Lipstick"},
{id:"463",text:"Anthony Logistics for Men Oil Free Facial Lotion SPF 15"},
{id:"464",text:"BeautiControl Skinlogics Sensitive Rinse & Restore Tonic"},
{id:"465",text:"Bare Escentuals (Bare Minerals) bareSkin Complete Coverage Serum Concealer"},
{id:"466",text:"Bare Escentuals (Bare Minerals) bareMinerals Flawless Definition Mascara"},
{id:"467",text:"Avon Clearskin Professional Clarifying Toner Pads"},
{id:"468",text:"Algenist Sublime Defense Anti-Aging Blurring Moisturizer SPF 30"},
{id:"469",text:"Arbonne RE9 Advanced Restorative Day Creme SPF 20"},
{id:"470",text:"Avalon Organics Vitamin C Renewal Refreshing Cleansing Gel"},
{id:"471",text:"Anthony Logistics for Men Mud Scrub Exfoliating Bar"},
{id:"472",text:"BeautiControl Makeup Erase Pencil"},
{id:"473",text:"BeautiControl BC Spa Facial Defend & Restore Night Creme"},
{id:"474",text:"Avon SuperShock Max Mascara"},
{id:"475",text:"Alba Botanica Good & Clean Toxin Release Scrub"},
{id:"476",text:"Alba Botanica Hawaiian Body Cream Nourishing Coconut Milk"},
{id:"477",text:"Alba Botanica Acnedote Deep Pore Wash"},
{id:"478",text:"Arbonne Intelligence Genius"},
{id:"479",text:"Andalou Naturals Lavender Shea Body Butter"},
{id:"480",text:"AmorePacific Moisture Bound Rejuvenating Creme"},
{id:"481",text:"Ahava Time To Hydrate Night Replenisher, Normal to Dry Skin"},
{id:"482",text:"Aesop Parsley Seed Facial Cleanser"},
{id:"483",text:"Andalou Naturals Clementine Ginger Energizing Shower Gel"},
{id:"484",text:"Becca Ever-Matte Poreless Priming Perfector"},
{id:"485",text:"Arbonne SeaSource Detox Spa Foaming Sea Salt Scrub"},
{id:"486",text:"Becca Blotting Powder Perfector"},
{id:"487",text:"Ahava Dead Sea Mud Gentle Body Exfoliator"},
{id:"488",text:"Aveda Lip Shine"},
{id:"489",text:"BeautiControl Skinlogics Clear Oil Control Moisturizer"},
{id:"490",text:"Banana Boat Sport Performance Faces Clear Zinc Sunscreen SPF 50"},
{id:"491",text:"BeautiControl Liquid Eye Liner"},
{id:"492",text:"BeautiControl Skinlogics Lash Enhancing Eye Makeup Remover"},
{id:"493",text:"Beautycounter Lustro Body Oil- Rosemary + Citrus"},
{id:"494",text:"AmorePacific Moisture Bound Intensive Vitalizing Eye Complex"},
{id:"495",text:"Alba Botanica Very Emollient Body Lotion Original Unscented"},
{id:"496",text:"BeautiControl SpectacuLash Waterproof Mascara"},
{id:"497",text:"Aveda Tourmaline Charged Radiance Masque"},
{id:"498",text:"Avon Clearskin Clear Emergency Instant Spot Treatment"},
{id:"499",text:"Acure Organics Facial Cleanser Gel Superfruit + Chlorella Growth Factor"},
{id:"500",text:"Avon Mark Gloss Gorgeous Stay On Lip Stain"},
{id:"501",text:"Alba Botanica Very Emollient Sunscreen Facial Broad Spectrum SPF 30"},
{id:"502",text:"Andalou Naturals Turmeric + C Enlighten Serum"},
{id:"503",text:"Becca Lowlight/Highlight Perfecting Palette Pressed"},
{id:"504",text:"Alba Botanica Hawaiian Sunscreen Soothing Aloe Vera SPF 30"},
{id:"505",text:"Arbonne Clear Advantage Clarifying Wash Acne Medication"},
{id:"506",text:"Anthony Logistics for Men Invigorating Rush Hair + Body Wash"},
{id:"507",text:"AcneFree Acne Spot Treatment + Redness Control Original Strength Terminator"},
{id:"508",text:"Bare Escentuals (Bare Minerals) SPF 20 Correcting Concealer"},
{id:"509",text:"Avon Elements Skin Revitalize 2-in-1 Brightening Eye Roller"},
{id:"510",text:"Alba Botanica Un-Petroleum Multi-Purpose Jelly"},
{id:"511",text:"BeautiControl Regeneration Tight, Firm & Fill Extreme Wrinkle Concentrate"},
{id:"512",text:"Arbonne BefoRE Sun Damage Control Water Resistant Sunscreen SPF 30"},
{id:"513",text:"AmLactin AmLactin Ultra Hydrating Body Cream"},
{id:"514",text:"Avon Anew Clinical Skinvincible Multi-Shield Lotion Broad Spectrum SPF 50 Sunscreen"},
{id:"515",text:"Aesop Bitter Orange Astringent Toner"},
{id:"516",text:"Algenist Regenerative Anti-Aging Lotion"},
{id:"517",text:"Bare Escentuals (Bare Minerals) Buxom Big & Healthy Lip Cream Lip Polish"},
{id:"518",text:"Bare Escentuals (Bare Minerals) Active Cell Renewal Night Serum"},
{id:"519",text:"Arbonne SeaSource Detox Spa Detoxifying Rescue Wash"},
{id:"520",text:"Avon Anew Platinum Cleanser"},
{id:"521",text:"Avon Glow Bronzing Powder"},
{id:"522",text:"Avon Clearskin Blemish Clearing Foaming Cleanser"},
{id:"523",text:"Beautycounter Routine Clean Cream Cleanser"},
{id:"524",text:"Avon Clearskin Clear Emergency Intensive Cream Cleanser"},
{id:"525",text:"Avon Clearskin Blackhead Eliminating Daily Cleanser"},
{id:"526",text:"Avon Elements Moisture Boost Eye Cream"},
{id:"527",text:"Acure Organics Facial Marula Oil"},
{id:"528",text:"Arbonne Glow With It After Sun Lotion"},
{id:"529",text:"Avon Elements Moisture Boost Daily Moisture Lotion Broad Spectrum SPF 15"},
{id:"530",text:"Andalou Naturals Probiotic + C Renewal Cream"},
{id:"531",text:"Andalou Naturals All-in-One Beauty Balm Sheer Tint with SPF 30"},
{id:"532",text:"Andalou Naturals Kombucha Enzyme Exfoliating Peel"},
{id:"533",text:"Anthony Logistics for Men Shave Cream"},
{id:"534",text:"Arbonne Eye Makeup Remover"},
{id:"535",text:"Aveeno Skin Relief Healing Ointment"},
{id:"536",text:"Anthony Logistics for Men Self Tanner With Anti-Aging Complex"},
{id:"537",text:"Aveda Inner Light Mineral Pressed Powder"},
{id:"538",text:"Andalou Naturals Pumpkin Honey Glycolic Mask"},
{id:"539",text:"Bare Escentuals (Bare Minerals) Buxom Big & Healthy Lip Stick"},
{id:"540",text:"Aveda Outer Peace Foaming Cleanser"},
{id:"541",text:"Bare Escentuals (Bare Minerals) bareMinerals Flawless Definition Waterproof Mascara"},
{id:"542",text:"Alba Botanica Good & Healthy Anti-Oil Weightless Moisture Lotion"},
{id:"543",text:"Arbonne Calm Gentle Daily Moisturizer"},
{id:"544",text:"Aubrey Organics Rosa Mosqueta Rose Hip Seed Oil"},
{id:"545",text:"BeautiControl Perfecting Creme to Powder Finish"},
{id:"546",text:"Avalon Organics CoQ10 Repair Wrinkle Defense Serum"},
{id:"547",text:"Banana Boat Ultramist Protect & Hydrate Clear Spray SPF 50+"},
{id:"548",text:"Aveeno Ultra Calming Nourishing Night Cream"},
{id:"549",text:"Avon Anew Vitale Gel Eye Cream"},
{id:"550",text:"Becca Matte Skin Shine Proof Foundation"},
{id:"551",text:"Aveda Nourish-Mint Sheer Mineral Lip Color"},
{id:"552",text:"Bare Escentuals (Bare Minerals) bareVitamins Skin Rev-er Upper"},
{id:"553",text:"Arbonne Calm Soothing Facial Serum"},
{id:"554",text:"Aveeno Daily Moisturizing Lotion, Fragrance-Free"},
{id:"555",text:"Aubrey Organics Sea Buckthorn & Cucumber with Ester-C Moisturizing Mask, for Combination/Dry Skin"},
{id:"556",text:"Almay Oil-Free Eye Makeup Remover Liquid"},
{id:"557",text:"Aubrey Organics Natural Sun SPF 30+ Green Tea"},
{id:"558",text:"Anthony Logistics for Men Glycerin Hand & Body Lotion"},
{id:"559",text:"Algenist Pore Corrector Anti-Aging Primer"},
{id:"560",text:"Aveeno Ultra-Calming Daily Moisturizer SPF 15"},
{id:"561",text:"Aubrey Organics Rosa Mosqueta Luxurious Bath Bar"},
{id:"562",text:"Bare Escentuals (Bare Minerals) bareMinerals 100% Natural Lipgloss"},
{id:"563",text:"Aveda Nourish-Mint Smoothing Lip Color"},
{id:"564",text:"Alba Botanica Even Advanced Sea Moss Moisturizer SPF 15"},
{id:"565",text:"Alba Botanica Very Emollient Sunscreen Kids Spray SPF 40"},
{id:"566",text:"Ahava Rich Cleansing Cream"},
{id:"567",text:"Andalou Naturals Clementine + C Illuminating Toner"},
{id:"568",text:"Acne.org AHA +"},
{id:"569",text:"Bare Escentuals (Bare Minerals) bareMinerals Prime Time Eyelid Primer"},
{id:"570",text:"Becca Shadow & Light Bronze/Contour Perfector"},
{id:"571",text:"Anthony Logistics for Men Acne Cleanser"},
{id:"572",text:"Alba Botanica Natural Very Emollient Sunscreen Natural Protection Lip Care SPF 25"},
{id:"573",text:"Aveeno Skin Relief Hand Cream"},
{id:"574",text:"Algenist Gentle Rejuvenating Cleanser"},
{id:"575",text:"Alba Botanica Even Advanced Sea Lettuce Cleansing Milk"},
{id:"576",text:"Arbonne Revelage Concentrated Age Spot Minimizer"},
{id:"577",text:"Bare Escentuals (Bare Minerals) Instant Waterproof Eye Makeup Remover"},
{id:"578",text:"Aubrey Organics Collagen TCM Therapeutic Cream Moisturizer"},
{id:"579",text:"Alba Botanica Good & Healthy Oil-Free Radiance Cream"},
{id:"580",text:"Anthony Logistics for Men Glycolic Facial Cleanser"},
{id:"581",text:"Bare Escentuals (Bare Minerals) Renew & Hydrate Eye Cream"},
{id:"582",text:"BeautiControl SpectacuLash Thickening Primer and Maximum Length Mascara"},
{id:"583",text:"Bare Escentuals (Bare Minerals) bareMinerals READY Eyeshadow"},
{id:"584",text:"Aesop Moroccan Neroli Shaving Serum"},
{id:"585",text:"Algenist Targeted Deep Wrinkle Minimizer"},
{id:"586",text:"Avalon Organics Vitamin C Renewal Facial Cream"},
{id:"587",text:"Becca The Ultimate Mascara"},
{id:"588",text:"Aveda Botanical Kinetics Exfoliant"},
{id:"589",text:"Ahava Active Deadsea Minerals Deadsea Plants Caressing Body Sorbet"},
{id:"590",text:"Bare Escentuals (Bare Minerals) bareMinerals Magic Wand Brushless Mascara"},
{id:"591",text:"Beautycounter Countertime Soothing Face Wash"},
{id:"592",text:"Anthony Logistics for Men Continuous Moisture Eye Cream"},
{id:"593",text:"Ahava Hydration Cream Mask"},
{id:"594",text:"Andalou Naturals Luminous Eye Serum"},
{id:"595",text:"Avon Extralasting Lipstick"},
{id:"596",text:"Acure Organics Argan Stem Cell + Chlorella Growth Factor Brightening Facial Scrub"},
{id:"597",text:"Beautycounter Protect All Over Sunscreen Broad Spectrum SPF 30"},
{id:"598",text:"Becca Aqua Luminous Perfecting Foundation"},
{id:"599",text:"Aveeno Skin Relief Overnight Cream"},
{id:"600",text:"Aubrey Organics Sea Buckthorn with Ester-C Antioxidant Serum"},
{id:"601",text:"Bare Escentuals (Bare Minerals) bareMinerals Firming Eye Treatment"},
{id:"602",text:"Aveeno Mens Face Wash Fragrance Free"},
{id:"603",text:"BeautiControl Regeneration Tight Firm & Fill Extreme Tri-Peptide Complex"},
{id:"604",text:"Aveeno Positively Radiant Intensive Night Cream"},
{id:"605",text:"Avon Clearskin Clear Emergency Intensive Treatment"},
{id:"606",text:"Banana Boat Ultramist Protect & Hydrate Clear Spray SPF 30"},
{id:"607",text:"Alba Botanica Acnedote Invisible Treatment Gel"},
{id:"608",text:"Avon Foot Works Intensive Callus Cream"},
{id:"609",text:"Aveeno Positively Ageless Youth Perfecting Moisturizer SPF 30"},
{id:"610",text:"Alpha Skin Care Enhanced Wrinkle Repair Cream"},
{id:"611",text:"Aubrey Organics Rosa Mosqueta & English Lavender Facial Toner, for Dry Skin"},
{id:"612",text:"Avon Clearskin Pore Penetrating Black Mineral Mask"},
{id:"613",text:"BeautiControl Skinlogics Clear Blemish Control Tonic"},
{id:"614",text:"BeautiControl Skinlogics Sensitive Gentle Cleansing Lotion"},
{id:"615",text:"Avon Anew Clinical Advanced Retexturizing Peel"},
{id:"616",text:"Aveda Inner Light Mineral Tinted Moisture SPF 15"},
{id:"617",text:"Aubrey Organics Green Tea & Ginkgo Facial Toner, for Normal Skin"},
{id:"618",text:"Bare Escentuals (Bare Minerals) Buxom Waterproof Smokey Eye Sticks"},
{id:"619",text:"Almay Eyeliner"},
{id:"620",text:"Amazing Cosmetics Suede Collection"},
{id:"621",text:"Bare Escentuals (Bare Minerals) bareMinerals Deep Cleansing Foam with RareMinerals ActiveSoil Complex"},
{id:"622",text:"Acure Organics Night Cream: Argan Stem Cell + 2% Chlorella Growth Factor"},
{id:"623",text:"Aveda Botanical Kinetics Purifying Creme Cleanser"},
{id:"624",text:"Almay Smart Shade Butter Kiss Lipstick"},
{id:"625",text:"Aveda Enbrightenment Brightening Intensive Massage Masque"},
{id:"626",text:"Avalon Organics Vitamin C Renewal Moisture Plus Lotion Broad Spectrum SPF 15"},
{id:"627",text:"Avon Mark What a Line Felt Tip Eyeliner"},
{id:"628",text:"Andalou Naturals Argan + Omega Natural Glow 3 in 1 Treatment"},
{id:"629",text:"Aveda Pure Comfort Eye Makeup Remover"},
{id:"630",text:"BeautiControl BC Spa Detoxifying Clay Masque"},
{id:"631",text:"Avon Super Extend Mascara"},
{id:"632",text:"AcneFree 24 Hour Acne Clearing System"},
{id:"633",text:"Aveeno Cleansing Therapy Moisturizing Wash"},
{id:"634",text:"Bare Escentuals (Bare Minerals) bareMinerals Exfoliating Treatment Cleanser with RareMinerals ActiveSoil Complex"},
{id:"635",text:"Aveda Botanical Kinetics Skin Firming/Toning Agent"},
{id:"636",text:"Aveda Inner Light Mineral Dual Foundation"},
{id:"637",text:"Avon Anew Clinical Eye Lift Pro Dual Eye Lift System"},
{id:"638",text:"Aveeno Mens Shave Gel"},
{id:"639",text:"Bare Escentuals (Bare Minerals) bareMinerals Extra Firming Neck Cream"},
{id:"640",text:"Aveeno Positively Radiant Tinted Moisturizer SPF 30"},
{id:"641",text:"Becca Lip & Cheek Creme"},
{id:"642",text:"Almay Intense i-Color Liquid Liner for Eyes"},
{id:"643",text:"Arbonne Revelage Intensive Pro-Brightening Night Serum"},
{id:"644",text:"BeautiControl BC Spa Solutions Radical Hydration Concentrate"},
{id:"645",text:"Alba Botanica Even Advanced Sea Mineral Cleansing Gel"},
{id:"646",text:"BeautiControl BC Spa Bright Daily Illuminating Serum"},
{id:"647",text:"Aubrey Organics Blue Green Algae with Grape Seed Extract Facial Toner, for Combination/Oily Skin"},
{id:"648",text:"Bare Escentuals (Bare Minerals) bareMinerals Foundation SPF 15"},
{id:"649",text:"Amazing Cosmetics AmazingCosmetics Bronzer"},
{id:"650",text:"Aesop Primrose Facial Cleansing Masque"},
{id:"651",text:"Aveda Green Science Firming Eye Creme"},
{id:"652",text:"Avon Beyond Color Plumping Lip Color SPF 15"},
{id:"653",text:"Avon Clearskin Pore Penetrating Invigorating Scrub"},
{id:"654",text:"Alba Botanica Hawaiian Facial Mask Pore-fecting Papaya Enzyme"},
{id:"655",text:"Alba Botanica Good & Clean Pore Purifying Mini Peel"},
{id:"656",text:"Alba Botanica Very Emollient Sunscreen Pure Lavender SPF 45"},
{id:"657",text:"Avon Mark No Place to Run Longwear Eyeliner"},
{id:"658",text:"BeautiControl BC Spa Facial Purifying Cleansing Gel"},
{id:"659",text:"Bare Escentuals (Bare Minerals) bareMinerals 100% Natural Lipliner"},
{id:"660",text:"Aveeno Positively Radiant Targeted Tone Corrector"},
{id:"661",text:"Bare Escentuals (Bare Minerals) bareMinerals Matte Foundation SPF 15"},
{id:"662",text:"Aveda Uruku Color Gloss"},
{id:"663",text:"Avon Ultra Color Rich Lipstick"},
{id:"664",text:"Aubrey Organics Natural Herbal Seaclay with Goa Herb Oil Balancing Mask, for Oily Skin"},
{id:"665",text:"BeautiControl SpectacuLash Mascara"},
{id:"666",text:"Bare Escentuals (Bare Minerals) Purely Nourishing Moisturizer with RareMinerals ActiveSoil Complex: Combination Skin"},
{id:"667",text:"Alba Botanica Hawaiian Dry-Oil Sunscreen Nourishing Coconut Oil SPF 15"},
{id:"668",text:"Ahava Time To Smooth Age Control Intensive Serum"},
{id:"669",text:"Beautycounter Lustro Face Oil 1- Calendula"},
{id:"670",text:"Almay Smart Shade CC Cream Complexion Corrector SPF 35 Broad Spectrum"},
{id:"671",text:"Anthony Logistics for Men Sea Salt Body Scrub"},
{id:"672",text:"Becca Line and Illuminate Pencil"},
{id:"673",text:"Bare Escentuals (Bare Minerals) bareMinerals READY Blush"},
{id:"674",text:"Almay One Coat Get Up & Grow Waterproof Mascara"},
{id:"675",text:"Aveeno Positively Ageless Firming Body Lotion"},
{id:"676",text:"Alba Botanica Hawaiian Lip Balm Nourishing Coconut Cream"},
{id:"677",text:"Avon Ideal Flawless Concealer Stick"},
{id:"678",text:"Aveda Inner Light Concealer"},
{id:"679",text:"Benefit Ooh La Lift"},
{id:"680",text:"Anastasia of Beverly Hills Brow Fix"},
{id:"681",text:"Arbonne FC5 Intense Hydration Mask"},
{id:"682",text:"Bare Escentuals (Bare Minerals) SkinLongevity Vital Power Infusion"},
{id:"683",text:"AmorePacific Time Response Eye Renewal Creme"},
{id:"684",text:"Arbonne FC5 Mattifying Powder"},
{id:"685",text:"Aveeno Soothing Relief Creamy Wash, Fragrance Free"},
{id:"686",text:"Beautycounter Powder Brush"},
{id:"687",text:"Bare Escentuals (Bare Minerals) Buxom Pen & Ink Long-Last Eyeliner"},
{id:"688",text:"Amazing Cosmetics Powder Set"},
{id:"689",text:"Aveeno Skin Relief Moisture Repair Cream"},
{id:"690",text:"Acne.org Moisturizer"},
{id:"691",text:"Andalou Naturals Oil Control Beauty Balm Un-Tinted with SPF 30"},
{id:"692",text:"Alba Botanica Very Emollient Sunless Golden Tanning Without The Sun"},
{id:"693",text:"Aveda Enbrightenment Brightening Cleanser"},
{id:"694",text:"Andalou Naturals Hyaluronic DMAE Lift & Firm Cream"},
{id:"695",text:"Avalon Organics Nourishing Cherry Aloe Lip Balm"},
{id:"696",text:"Aveda All-Sensitive Cleanser"},
{id:"697",text:"Aubrey Organics Silken Earth"},
{id:"698",text:"Andalou Naturals Chia + Omega Radiant Skin Polish"},
{id:"699",text:"Anthony Logistics for Men Algae Facial Cleanser"},
{id:"700",text:"Andalou Naturals Avo Cocoa Skin Food Mask"},
{id:"701",text:"Avalon Organics Lavender Luminosity Facial Cleansing Milk"},
{id:"702",text:"BeautiControl Lip Apeel"},
{id:"703",text:"Aesop Fabulous Face Cleanser"},
{id:"704",text:"Aubrey Organics Amino Derm Gel Clear Skin Complex"},
{id:"705",text:"Avon Mega Effects Mascara"},
{id:"706",text:"Bare Escentuals (Bare Minerals) bareMinerals Pretty Amazing Lipcolor"},
{id:"707",text:"Aveeno Positively Ageless Skin Strengthening Hand Cream"},
{id:"708",text:"Avon Ideal Flawless BB Beauty Balm Cream Broad Spectrum SPF 15 Sunscreen"},
{id:"709",text:"Aveda Uruku Lip Pigment"},
{id:"710",text:"Aubrey Organics After Sun Body & Face Maintenance Moisturizer"},
{id:"711",text:"Acne.org Cleanser"},
{id:"712",text:"Benefit Moisture Prep Toning Lotion"},
{id:"713",text:"Ahava Time To Hydrate Gentle Eye Cream"},
{id:"714",text:"Bare Escentuals (Bare Minerals) bareMinerals Mineral Veil SPF 25"},
{id:"715",text:"Aveeno Ultra-Calming Makeup Removing Wipes"},
{id:"716",text:"Avon Solutions Cleanse Total Radiance Day and Night Cleanser"},
{id:"717",text:"Beautycounter Lustro Face Oil 2- Jasmine"},
{id:"718",text:"Alba Botanica Hawaiian After-Sun Lotion Replenishing Kona Coffee"},
{id:"719",text:"Algenist Firming & Lifting Intensive Mask"},
{id:"720",text:"Almay TLC Truly Lasting Color 16 Hour Makeup"},
{id:"721",text:"BeautiControl BC Spa Facial Defend & Restore Moisture Lotion SPF 20"},
{id:"722",text:"Andalou Naturals Acai + White Tea Oil-Free Moisturizer"},
{id:"723",text:"Avalon Organics Lavender Luminosity Facial Cleansing Gel"},
{id:"724",text:"Becca Pressed Bronzing Powder"},
{id:"725",text:"Avon Foot Works Thermal Exfoliating Scrub"},
{id:"726",text:"Avon Elements Youth Restoring Anti-Wrinkle Night Cream"},
{id:"727",text:"BeautiControl Skinlogics Sensitive Protective Services Calming Fluid"},
{id:"728",text:"Arbonne Aromassentials Awaken Body Lotion"},
{id:"729",text:"Avon Anew Clinical Skinvincible Deep Recovery Cream"},
{id:"730",text:"Alba Botanica Hawaiian Facial Scrub Pore Purifying Pineapple Enzyme"},
{id:"731",text:"Aubrey Organics Natural Herbal Facial Cleanser, for Oily Skin"},
{id:"732",text:"Avon Elements Skin Revitalize Instant Radiance Lotion Broad Spectrum SPF 20"},
{id:"733",text:"Bare Escentuals (Bare Minerals) bareMinerals Prime Time Neutralizing Foundation Primer"},
{id:"734",text:"Aveeno Stress Relief Body Wash"},
{id:"735",text:"Aveda Deep Cleansing Herbal Clay Masque"},
{id:"736",text:"Aveda Tourmaline Charged Radiance Fluid"},
{id:"737",text:"Bare Escentuals (Bare Minerals) bareMinerals Eyecolor"},
{id:"738",text:"Andalou Naturals Apricot Probiotic Cleansing Milk"},
{id:"739",text:"Avalon Organics Soothing Vanilla Rosemary Lip Balm"},
{id:"740",text:"AcneFree 2-in-1 Acne Wipes Oil-Free"},
{id:"741",text:"Almay Oil Free Makeup Eraser Sticks"},
{id:"742",text:"Anthony Logistics for Men After Shave Balm"},
{id:"743",text:"Alba Botanica Very Emollient Sunscreen Mineral Protection, Facial SPF 20"},
{id:"744",text:"AmorePacific Green Tea Seed Treatment Oil"},
{id:"745",text:"Avon Mark Glowdacious Illuminating Powder"},
{id:"746",text:"Bare Escentuals (Bare Minerals) Well-Rested Face and Eye Brightener"},
{id:"747",text:"Anthony Logistics for Men Pre-Shave Oil"},
{id:"748",text:"Alba Botanica Very Emollient Sunscreen Sport Spray SPF 40"},
{id:"749",text:"Avon Solutions Cleanse Ageless Day and Night Cleanser"},
{id:"750",text:"BeautiControl Platinum Regeneration Rejuvenating Eye Treatment"},
{id:"751",text:"Beautycounter Crease Eye Brush"},
{id:"752",text:"Bare Escentuals (Bare Minerals) 7-Day Skin Detox Mineral Brightening Peel"},
{id:"753",text:"Algenist Triple-Action Micropolish and Peel"},
{id:"754",text:"Bare Escentuals (Bare Minerals) Complexion Rescue Tinted Hydrating Gel Cream Broad Spectrum SPF 30"},
{id:"755",text:"AmorePacific Treatment Cleansing Foam"},
{id:"756",text:"Aveda Enbrightenment Brightening Correcting Lotion"},
{id:"757",text:"Bare Escentuals (Bare Minerals) bareMinerals Redness Remedy"},
{id:"758",text:"Bare Escentuals (Bare Minerals) Buxom Amplified Lash Mascara"},
{id:"759",text:"Avalon Organics Essential Lift Firming Moisturizer Broad Spectrum SPF 15"},
{id:"760",text:"Anthony Logistics for Men Astringent Toner Pads"},
{id:"761",text:"Bare Escentuals (Bare Minerals) Mineralilixirs Five Oil Blend"},
{id:"762",text:"Avon Foot Works Deep Moisture Cream"},
{id:"763",text:"Arbonne Lip Saver SPF 30"},
{id:"764",text:"Aveda Green Science Line Minimizer"},
{id:"765",text:"Avon Anew Genics Treatment Concentrate"},
{id:"766",text:"Anastasia of Beverly Hills Perfect Brow Pencil"},
{id:"767",text:"Aesop Geranium Leaf Body Scrub"},
{id:"768",text:"Bare Escentuals (Bare Minerals) Hot Escapes Bronzer"},
{id:"769",text:"Bare Escentuals (Bare Minerals) Prime Time Primer Shadow"},
{id:"770",text:"Ahava Dermud Nourishing Body Cream"},
{id:"771",text:"Arbonne ABC Body Oil"},
{id:"772",text:"Beautycounter Every Day AM Hydrating Cream"},
{id:"773",text:"Bare Escentuals (Bare Minerals) Faux Tan Matte"},
{id:"774",text:"Aveeno Baby Natural Protection Face Stick with Broad Spectrum SPF 50"},
{id:"775",text:"Bare Escentuals (Bare Minerals) bareMinerals Maximum Coverage Concealer Brush"},
{id:"776",text:"Bare Escentuals (Bare Minerals) Smart Combination Smoothing Lightweight Emulsion"},
{id:"777",text:"Avon Clearskin Pore Penetrating Cooling Toner"},
{id:"778",text:"Alba Botanica Un-petroleum Cherry SPF 18 Lip Balm"},
{id:"779",text:"Alpha Skin Care Moisturizing Facial Sunscreen Broad Spectrum SPF 30"},
{id:"780",text:"Benefit High Beam"},
{id:"781",text:"Alba Botanica Hawaiian Oil-Free Moisturizer Refining Aloe & Green Tea"},
{id:"782",text:"Avon Super Drama Mascara"},
{id:"783",text:"Aveda Outer Peace Acne Relief Lotion"},
{id:"784",text:"Becca Creme Blush"},
{id:"785",text:"Arbonne Intelligence Rejuvenating Cream"},
{id:"786",text:"Avalon Organics CoQ10 Repair Wrinkle Defense Night Creme"},
{id:"787",text:"BeautiControl Platinum Regeneration Age-Defying Lip Color"},
{id:"788",text:"Avon Ultra Luxury Eye Liner"},
{id:"789",text:"Avon Clearskin Blackhead Eliminating Deep Treatment Mask"},
{id:"790",text:"Bare Escentuals (Bare Minerals) Divine Goddess Luminizer"},
{id:"791",text:"Aubrey Organics Green Tea & Green Clay Rejuvenating Mask, for Normal Skin"},
{id:"792",text:"Alba Botanica Hawaiian Revitalizing Green Tea SPF 45"},
{id:"793",text:"Avalon Organics Essential Lift Contouring Eye Creme"},
{id:"794",text:"Bare Escentuals (Bare Minerals) Blemish Remedy Mattifying Prep Gel"},
{id:"795",text:"Aveda Petal Essence Face Accents"},
{id:"796",text:"Arbonne Calm Soothing Eye Gel"},
{id:"797",text:"Aubrey Organics White Camellia Oil"},
{id:"798",text:"Avalon Organics Essential Lift Fortifying Toner"},
{id:"799",text:"AcneFree Severe Acne Spot Treatment + Redness Control Maximum Strength Terminator 10"},
{id:"800",text:"Arbonne RE9 Advanced Instant Lift Gel"},
{id:"801",text:"Arbonne Aromassentials Awaken Sea Salt Scrub"},
{id:"802",text:"Alba Botanica Good & Clean Dual Textured Exfoliating Towelettes"},
{id:"803",text:"Arbonne FC5 Oil-Absorbing Day Lotion SPF 20"},
{id:"804",text:"Avon ANEW CLINICAL Absolute Even Multi-Tone Skin Corrector"},
{id:"805",text:"Anthony Logistics for Men Oil-Free Facial Lotion"},
{id:"806",text:"Alba Botanica Even Advanced Sea Elements Eye Makeup Remover"},
{id:"807",text:"BeautiControl BC Spa Resurface Microderm Abrasion for Face"},
{id:"808",text:"Almay Smart Shade Mousse Makeup"},
{id:"809",text:"Avon Anew Power Serum"},
{id:"810",text:"Aubrey Organics Sea Buckthorn & Cucumber with Ester-C Moisturizing Cream, for Combination/Dry Skin"},
{id:"811",text:"Bare Escentuals (Bare Minerals) Buxom Lashliner"},
{id:"812",text:"Aubrey Organics Vegecol with Aloe & Oatmeal Soothing Mask, for Sensitive Skin"},
{id:"813",text:"Avalon Organics CoQ10 Repair Facial Cleansing Gel"},
{id:"814",text:"Avalon Organics Lavender Luminosity Hydrating Toner"},
{id:"815",text:"Aesop Parsley Seed Anti-Oxidant Hydrator"},
{id:"816",text:"BeautiControl Margarita Tangy Lip Balm"},
{id:"817",text:"Ahava Time To Clear All In One Toning Cleanser"},
{id:"818",text:"Avon Anew Beauty Age-Transforming Foundation SPF 15"},
{id:"819",text:"Bare Escentuals (Bare Minerals) Buxom Lash Mascara"},
{id:"820",text:"Amazing Cosmetics Amazing Concealer"},
{id:"821",text:"Anthony Logistics for Men Shave Gel"},
{id:"822",text:"Bare Escentuals (Bare Minerals) Pure Transformation Night Treatment"},
{id:"823",text:"Ahava Sun Protection Anti-Aging Moisturizer SPF 15"},
{id:"824",text:"BeautiControl Secret AGEnt Undercover Makeup"},
{id:"825",text:"Aveeno Positively Ageless Reconditioning Night Cream"},
{id:"826",text:"Aesop Tea Tree Leaf Facial Exfoliant"},
{id:"827",text:"Avalon Organics Essential Lift Restructuring Night Creme"},
{id:"828",text:"Aveda Green Science Replenishing Toner"},
{id:"829",text:"Arbonne SeaSource Detox Spa 5-in-1 Essential Massage Oil"},
{id:"830",text:"Ahava Foam Free Silk Shave"},
{id:"831",text:"Anthony Logistics for Men Vitamin A Facial Treatment"},
{id:"832",text:"Arbonne FC5 Skin Conditioning Oil"},
{id:"833",text:"Alba Botanica Even Advanced Sea Lipids Daily Cream"},
{id:"834",text:"Bare Escentuals (Bare Minerals) Lash Domination Volumizing Mascara"},
{id:"835",text:"Alba Botanica Good & Clean Gentle Acne Wash"},
{id:"836",text:"Avon Solutions Plus+ Total Radiance Thermal Cleanser"},
{id:"837",text:"Benefit You Rebel SPF 15"},
{id:"838",text:"Benefit Posietint"},
{id:"839",text:"Bare Escentuals (Bare Minerals) Prime Time Brightening Foundation Primer"},
{id:"840",text:"Acure Organics Facial Toner Balancing Rose + Red Tea"},
{id:"841",text:"Almay One Coat  Lengthening Mascara"},
{id:"842",text:"Aveeno Positively Radiant Daily Moisturizer SPF 30"},
{id:"843",text:"Aveeno Positively Radiant Brightening Cleanser"},
{id:"844",text:"Almay Oil-Free Eye Makeup Remover Pads"},
{id:"845",text:"Alba Botanica Very Emollient Body Lotion Maximum"},
{id:"846",text:"Algenist Genius Ultimate Anti-Aging Vitamin C+ Serum"},
{id:"847",text:"Andalou Naturals Blemish Vanishing Gel"},
{id:"848",text:"Bare Escentuals (Bare Minerals) Oil Obsessed Total Cleansing Oil"},
{id:"849",text:"BeautiControl BC Spa Resurface Daily Resurfacing Serum"},
{id:"850",text:"Alba Botanica Acnedote Face & Body Scrub"},
{id:"851",text:"Alba Botanica Very Emollient Sunscreen Mineral Protection, Fragrance Free SPF 30"},
{id:"852",text:"Arbonne RE9 Advanced Regenerating Toner"},
{id:"853",text:"Almay Intense i-Color Volumizing Mascara"},
{id:"854",text:"BeautiControl Shadow Control Crme"},
{id:"855",text:"Avalon Organics Vitamin C Renewal Revitalizing Eye Cream"},
{id:"856",text:"Becca Backlight Targeted Colour Corrector"},
{id:"857",text:"Aubrey Organics Vegecol Facial Cleansing Lotion, for Sensitive Skin"},
{id:"858",text:"Bare Escentuals (Bare Minerals) Prime Time BB Primer Cream Daily Defense Broad Spectrum SPF 30"},
{id:"859",text:"Avon Mark Lash Act Build & Define Mascara"},
{id:"860",text:"Anastasia of Beverly Hills Tinted Brow Gel"},
{id:"861",text:"Avalon Organics Refreshing Peppermint Green Tea Lip Balm"},
{id:"862",text:"Algenist Advanced Anti-Aging Repairing Oil"},
{id:"863",text:"Aubrey Organics Silken Earth Body Shimmer"},
{id:"864",text:"BeautiControl BC Spa Solutions Scar, Stretch Mark & Spider Vein Minimizing Cream"},
{id:"865",text:"Andalou Naturals Kukui Cocoa Body Butter"},
{id:"866",text:"Aveda Botanical Kinetics Toning Mist"},
{id:"867",text:"Aveeno Skin Relief Body Wash, Fragrance-Free"},
{id:"868",text:"Aveeno Clear Complexion Cleansing Bar"},
{id:"869",text:"Almay Intense i-Color Liquid Shadow + Color Primer"},
{id:"870",text:"Aubrey Organics Green Tea & Ginkgo Moisturizer"},
{id:"871",text:"Beautycounter Angled Blush Brush"},
{id:"872",text:"Arbonne RE9 Advanced Cellular Renewal Masque"},
{id:"873",text:"Avon Eyeshadow Primer"},
{id:"874",text:"Aubrey Organics Collagen & Almond Enriching Moisturizing Lotion"},
{id:"875",text:"Arbonne Clear Advantage Clarifying Toner Acne Medication"},
{id:"876",text:"Andalou Naturals Meyer Lemon Creamy Cleanser"},
{id:"877",text:"Avalon Organics Vitamin C Renewal Soothing Lip Balm"},
{id:"878",text:"Ahava All Night Nourishment"},
{id:"879",text:"Algenist POWER Advanced Wrinkle Fighter Serum"},
{id:"880",text:"BeautiControl BC Spa Facial Restructuring Eye Creme"},
{id:"881",text:"Aveda Inner Light Mineral Loose Powder"},
{id:"882",text:"Avon Foot Works All Day Deodorant Foot Cream"},
{id:"883",text:"Avon Clearskin Professional Acne Mark Treatment"},
{id:"884",text:"Aesop Perfect Facial Hydrating Cream"},
{id:"885",text:"Aveeno Positively Nourishing Calming Body Lotion"},
{id:"886",text:"Aesop Camellia Nut Facial Hydrating Cream"},
{id:"887",text:"Anthony Logistics for Men Glycerin Cleansing Bar"},
{id:"888",text:"Algenist Multi-Perfecting Pore Corrector Concentrate"},
{id:"889",text:"Bare Escentuals (Bare Minerals) Buxom Big & Healthy Lip Tarnish"},
{id:"890",text:"BeautiControl Skinlogics Corticure Comfort Lotion With Chamomile"},
{id:"891",text:"Aveda Mosscara"},
{id:"892",text:"Avon Extralasting Lip Gloss"},
{id:"893",text:"Beautycounter Rosewater Uplifting Spray"},
{id:"894",text:"Bare Escentuals (Bare Minerals) On The Spot Eye Makeup Remover"},
{id:"895",text:"Bare Escentuals (Bare Minerals) bareMinerals Refillable Buffing Brush"},
{id:"896",text:"Anastasia of Beverly Hills Brow Powder Duo"},
{id:"897",text:"Arbonne RE9 Advanced for Men Exfoliating Wash"},
{id:"898",text:"Bare Escentuals (Bare Minerals) Stroke of Light Eye Brightener"},
{id:"899",text:"Avon Anew Vitale Gel Cleanser"},
{id:"900",text:"Acne.org Jojoba Oil"},
{id:"901",text:"Bare Escentuals (Bare Minerals) Round The Clock Waterproof Eyeliner"},
{id:"902",text:"BeautiControl Secret Agent Tri-color Bronzing Powder"},
{id:"903",text:"Aveda Botanical Kinetics Hydrating Lotion"},
{id:"904",text:"Arbonne ABC Sunscreen SPF 30"},
{id:"905",text:"Aesop Control"},
{id:"906",text:"Aveeno Positively Nourishing Body WashHydrating"},
{id:"907",text:"BeautiControl Skinlogics Thermal Facial Scrub"},
{id:"908",text:"Beautycounter All Over Eye Brush"},
{id:"909",text:"Becca Eye Tint"},
{id:"910",text:"Bare Escentuals (Bare Minerals) Buxom Insider Eyeliner"},
{id:"911",text:"Beautycounter Every Day PM Hydrating Cream"},
{id:"912",text:"Beautycounter Shave Shaving Cream"},
{id:"913",text:"Bare Escentuals (Bare Minerals) bareMinerals 100% Natural Lipcolor"},
{id:"914",text:"Avalon Organics CoQ10 Repair Wrinkle Defense Creme Broad Spectrum SPF 15"},
{id:"915",text:"Bare Escentuals (Bare Minerals) bareMinerals All Over Face Color"},
{id:"916",text:"Aveda Outer Peace Acne Relief Pads"},
{id:"917",text:"Bare Escentuals (Bare Minerals) Well-Rested SPF 20 Eye Brightener"},
{id:"918",text:"Alpha Skin Care Intensive Rejuvenating Serum"},
{id:"919",text:"Banana Boat Protect & Hydrate Sunscreen Lotion SPF 50"},
{id:"920",text:"Aveeno Stress Relief Moisturizing Lotion"},
{id:"921",text:"Alba Botanica Very Emollient Sunscreen Fragrance Free Broad Spectrum SPF 30"},
{id:"922",text:"Ahava Sun Protection Anti-Aging Moisturizer SPF 50"},
{id:"923",text:"Avalon Organics Lavender Facial Cleansing Milk"},
{id:"924",text:"Bare Escentuals (Bare Minerals) Quick Change Brush Cleaner"},
{id:"925",text:"Bare Escentuals (Bare Minerals) READY SPF 20 Foundation"},
{id:"926",text:"Bare Escentuals (Bare Minerals) Mineral Veil"},
{id:"927",text:"Avon Elements Purifying Gel Cleanser"},
{id:"928",text:"AmorePacific Moisture Bound Refreshing Hydra-Gel Oil Free"},
{id:"929",text:"Beautycounter Countertime Enlightening Treatment Pads"},
{id:"930",text:"Avon Glimmersticks Eye Liner"},
{id:"931",text:"Andalou Naturals Aloe Mint Cooling Body Lotion"},
{id:"932",text:"BeautiControl Crme Sheer Protection"},
{id:"933",text:"Avon Clearskin Pore Penetrating Gel Cleanser"},
{id:"934",text:"BeautiControl BC Spa Facial Regenerating Tonic with PHAs"},
{id:"935",text:"Aveeno Soothing Relief Diaper Rash Cream, Fragrance Free"},
{id:"936",text:"Almay One Coat Thickening Mascara"},
{id:"937",text:"Alba Botanica Un-petroleum Tangerine SPF 18 Lip Balm"},
{id:"938",text:"Avon Ideal Flawless Tinted Moisturizer Lotion SPF 20 Sunscreen"},
{id:"939",text:"Aveeno Eczema Therapy Moisturizing Cream"},
{id:"940",text:"Bare Escentuals (Bare Minerals) bareMinerals Brow Finishing Gel"},
{id:"941",text:"Avon ExtraLasting Liquid Foundation SPF 12"},
{id:"942",text:"Arbonne Clear Advantage Clarifying Lotion Acne Medication Salicylic Acid 1%"},
{id:"943",text:"Aveeno Positively Ageless Restructuring Treatment Cream"},
{id:"944",text:"BeautiControl Lip Gloss"},
{id:"945",text:"BeautiControl Skinlogics Herbal Hydrating Mist"},
{id:"946",text:"Avon Anew Ultimate 7S Eye System"},
{id:"947",text:"Avalon Organics Vitamin C  Renewal Hydrating Cleansing Milk"},
{id:"948",text:"Aubrey Organics Seaware with Rosa Mosqueta Facial Cleansing Cream, for Dry Skin"},
{id:"949",text:"Aveeno Clear Complexion BB Cream SPF 30"},
{id:"950",text:"Avalon Organics Lavender Luminosity Exfoliating Enzyme Scrub"},
{id:"951",text:"Aesop Sage and Zinc Facial Hydrating Cream SPF 15"},
{id:"952",text:"Ahava Dermud Intensive Hand Cream"},
{id:"953",text:"Avon Anew Platinum Eye & Lip Cream"},
{id:"954",text:"BeautiControl Spa Warming Trend Green Tea Masque"},
{id:"955",text:"BeautiControl Regeneration Overnight Retinol Recovery Eye Capsules"},
{id:"956",text:"Acure Organics Citrus Ginger Argan Oil"},
{id:"957",text:"Aveda All-Sensitive Moisturizer"},
{id:"958",text:"Avon MagiX Cashmere Finish Liquid Foundation SPF 10"},
{id:"959",text:"Ahava Dead Sea Water Mineral Foot Cream"},
{id:"960",text:"AmLactin Cerapeutic Alpha-Hydroxy Ceramide Therapy Restoring Body Lotion, Fragrance Free"},
{id:"961",text:"Aubrey Organics Vegecol with Aloe Moisturizing Cream, for Sensitive Skin"},
{id:"962",text:"Beautycounter Lustro Body Oil- Jasmine"},
{id:"963",text:"BeautiControl BC Color Mineral Shadow Trio"},
{id:"964",text:"Aveda Petal Essence Eye Definer"},
{id:"965",text:"BeautiControl SpectacuLash Maximum Volume Mascara"},
{id:"966",text:"Arbonne FC5 Purifying Cleanser + Toner"},
{id:"967",text:"Bare Escentuals (Bare Minerals) Buxom Full-On Lip Polish"},
{id:"968",text:"Aesop Purifying Facial Cream Cleanser"},
{id:"969",text:"BeautiControl Perfecting Wet/Dry Finish Foundation"},
{id:"970",text:"Avalon Organics Vitamin C Renewal Balancing Toner"},
{id:"971",text:"Avon Ultra Luxury Brow Liner"},
{id:"972",text:"Bare Escentuals (Bare Minerals) bareMinerals Radiance"},
{id:"973",text:"Avon MagiX Face Perfector SPF 20"},
{id:"974",text:"Algenist Ultra Lightweight UV Defense Fluid SPF 50"},
{id:"975",text:"Avon True Color Eyeshadow Quad"},
{id:"976",text:"Aveeno Soothing Relief Moisture Cream, Fragrance Free"},
{id:"977",text:"Aveda Enbrightenment Brightening Correcting Creme"},
{id:"978",text:"brand product"},
{id:"979",text:"Clarins Extra-Firming Body Lotion"},
{id:"980",text:"Cargo TexasLiner Liquid Eye Liner"},
{id:"981",text:"Boots Expert Anti-Redness Serum"},
{id:"982",text:"Charlotte Tilbury Lip Cheat Lip Liner"},
{id:"983",text:"Burt's Bees Rosewater & Glycerin Toner"},
{id:"984",text:"Bite Beauty Matte Crme Lip Crayon"},
{id:"985",text:"Boots Complexion Refining Deep Clean Mousse"},
{id:"986",text:"Bioderma Atoderm Cream"},
{id:"987",text:"Burt's Bees Intense Hydration Nourishing Facial Water"},
{id:"988",text:"Clarisonic Refreshing Gel Cleanser"},
{id:"989",text:"Bioderma Hydrabio Rich Cream"},
{id:"990",text:"Biore Blemish Fighting Ice Cleanser"},
{id:"991",text:"Boots Expert Anti-Blemish 2-in-1 Scrub and Mask"},
{id:"992",text:"Chantecaille Rose Eye Makeup Remover"},
{id:"993",text:"Chanel Hydramax + Active Lip Care"},
{id:"994",text:"Burt's Bees Nourishing Lip Balm with Mango Butter"},
{id:"995",text:"Bobbi Brown SPF 15 Tinted Moisturizer"},
{id:"996",text:"Clarins Face Treatment Oil, Lotus, for Combination Skin"},
{id:"997",text:"Bobbi Brown Extra Balm Rinse"},
{id:"998",text:"Bioderma Sensibio H2O"},
{id:"999",text:"Bobbi Brown Perfectly Defined Long-Wear Brow Pencil"},
{id:"1000",text:"Cetaphil Daily Facial Moisturizer SPF 15"},
{id:"1001",text:"Clarins Gentle Eye Make-Up Remover Lotion"},
{id:"1002",text:"Bioderma Sensibio AR"},
{id:"1003",text:"Chantecaille Detox Clay Mask with Rosemary and Honey"},
{id:"1004",text:"Clarins Super Restorative Foundation SPF 15"},
{id:"1005",text:"Bobbi Brown Long-Wear Cream Shadow Stick"},
{id:"1006",text:"Boscia Clear Complexion Treatment with Botanical Blast"},
{id:"1007",text:"Burt's Bees Aloe & Linden Flower After Sun Soother"},
{id:"1008",text:"Clarins Super Restorative Tinted Cream SPF 20"},
{id:"1009",text:"Bioderma Hydrabio Tonique"},
{id:"1010",text:"Chanel Demaquillant Yeux Intense Gentle Biphase Eye Makeup Remover"},
{id:"1011",text:"Chanel Glossimer Levres Scintillantes"},
{id:"1012",text:"Clarins Cleansing Milk with Gentian, for Combination/Oily Skin"},
{id:"1013",text:"Bobbi Brown Buffing Grains for Face"},
{id:"1014",text:"Boots Quick Cover Blemish Stick"},
{id:"1015",text:"Boots Botanics Tinted Lip Balm"},
{id:"1016",text:"Chanel Ultra Correction Lift Lifting Firming Day Cream SPF 15"},
{id:"1017",text:"Clarins Radiance-Plus Self-Tanning Body Lotion"},
{id:"1018",text:"CeraVe Baby Moisturizing Lotion"},
{id:"1019",text:"Clarins After Sun Moisturizer Ultra-Hydrating"},
{id:"1020",text:"Bliss The Youth As We Know It"},
{id:"1021",text:"Chanel Lift Lumiere Firming and Smoothing Fluid Makeup SPF 15"},
{id:"1022",text:"Burberry Beauty Sheer Concealer"},
{id:"1023",text:"Cargo Lip Gloss"},
{id:"1024",text:"Caudalie Paris Premieres Vendanges Moisturizing Cream"},
{id:"1025",text:"Boots Stay Perfect Eye Shadow Palette"},
{id:"1026",text:"Bobbi Brown Bronzing Powder"},
{id:"1027",text:"Boots Sheer Temptation Lipstick"},
{id:"1028",text:"Bobbi Brown High Shimmer Lip Gloss"},
{id:"1029",text:"Caudalie Paris Vinexpert Night Infusion Cream"},
{id:"1030",text:"Boots Face Renewal Cream SPF 15"},
{id:"1031",text:"Chanel Illusion DOmbre Long Wear Luminous Eyeshadow"},
{id:"1032",text:"Charlotte Tilbury Beach Stick Lip to Cheek Dewy Colour Pop"},
{id:"1033",text:"Benefit Benetint Lip Balm SPF 15"},
{id:"1034",text:"Bobbi Brown Instant Long-Wear Makeup Remover"},
{id:"1035",text:"Bobbi Brown Metallic Long-Wear Cream Shadow"},
{id:"1036",text:"Chanel Le Blanc Brightening Concentrate Double Action TXC"},
{id:"1037",text:"Boots Lift & Luminate Day Cream No7 SPF 15"},
{id:"1038",text:"Benefit Ultra Shines Lip Shine"},
{id:"1039",text:"Clarins Multi-Active Night Youth Recovery Comfort Cream for Normal to Dry Skin"},
{id:"1040",text:"Chanel Hydra Beauty Gel Creme"},
{id:"1041",text:"Clarins Eye Contour Gel"},
{id:"1042",text:"Boots Botanics Corrective Concealer Stick"},
{id:"1043",text:"Boots No7 Intensively Moisturizing Body Serum"},
{id:"1044",text:"CeraVe Baby Wash & Shampoo"},
{id:"1045",text:"Clarins Multi-Active Day Early Wrinkle Correction Cream, All Skin Types"},
{id:"1046",text:"Chanel Ecriture De Chanel Automatic Liquid Eyeliner"},
{id:"1047",text:"Bobbi Brown Luminous Moisturizing Foundation"},
{id:"1048",text:"Boots Expert Sensitive Hydrating Eye Cream"},
{id:"1049",text:"Chantecaille Vital Essence with Arbutin"},
{id:"1050",text:"Bobbi Brown Hydrating Gel Cream"},
{id:"1051",text:"Caudalie Paris Pulpe Vitaminee Eye and Lip Cream"},
{id:"1052",text:"Bobbi Brown Intensive Skin Serum Foundation SPF 40"},
{id:"1053",text:"CeraVe Moisturizing Cream"},
{id:"1054",text:"Clarins Super Restorative Replenishing Comfort Mask"},
{id:"1055",text:"Benefit Bigger Than BB Big Easy Multi-Balancing Skin Perfector SPF 35"},
{id:"1056",text:"Burt's Bees Natural Acne Solutions Maximum Strength Spot Treatment Cream"},
{id:"1057",text:"Benefit Gimme Brow"},
{id:"1058",text:"Bioderma Atoderm Nutritive Cream"},
{id:"1059",text:"Bobbi Brown Sheer Color Lip Gloss"},
{id:"1060",text:"Bliss Thinny Thin Chin"},
{id:"1061",text:"Bobbi Brown Intensive Skin Supplement"},
{id:"1062",text:"Chantecaille Super Lip Sheer"},
{id:"1063",text:"Benefit Get Even Pressed Powder"},
{id:"1064",text:"Clarins HydraQuench Intensive Serum Bi-Phase, for Dehydrated Skin"},
{id:"1065",text:"Bobbi Brown Extreme Party Mascara"},
{id:"1066",text:"Clarins Super Restorative Day Cream SPF 20"},
{id:"1067",text:"Chanel Lait Confort Creamy Cleansing Milk"},
{id:"1068",text:"Boots Radiant Glow Concealer"},
{id:"1069",text:"Boots Exceptional Definition Nutrient Enriched Mascara"},
{id:"1070",text:"Chanel Hydra Beauty Creme"},
{id:"1071",text:"Benefit Benetint"},
{id:"1072",text:"Chantecaille Shine Eye Shade"},
{id:"1073",text:"Clarins Blush Prodige Illuminating Cheek Color"},
{id:"1074",text:"Boots Natural Blush Cheek Colour"},
{id:"1075",text:"Clarins Instant Liner"},
{id:"1076",text:"Bobbi Brown Long-Wear Even Finish Foundation SPF 15"},
{id:"1077",text:"Boots Mineral Perfection Lipstick"},
{id:"1078",text:"Boots Botanics Radiance Concentrate Serum All Bright"},
{id:"1079",text:"Clarins Bronzing Duo Compact"},
{id:"1080",text:"Clarins Bright Plus HP Intensive Brightening Smoothing Serum"},
{id:"1081",text:"Bobbi Brown Face Highlighter"},
{id:"1082",text:"Boots Complexion Refining Light Night Cream"},
{id:"1083",text:"Burberry Beauty Fresh Glow BB Cream SPF 20"},
{id:"1084",text:"Bliss Steep Clean Mattifying Toner Pads"},
{id:"1085",text:"Bobbi Brown Shimmer Lip Gloss"},
{id:"1086",text:"Clarins Wonder Length Mascara"},
{id:"1087",text:"Cetaphil Gentle Cleansing Antibacterial Bar"},
{id:"1088",text:"Chantecaille Lip Matte"},
{id:"1089",text:"Chantecaille Poudre Delicate"},
{id:"1090",text:"Benefit Hello Flawless! Custom Powder Cover-Up"},
{id:"1091",text:"Bobbi Brown Protective Face Lotion SPF 15"},
{id:"1092",text:"Benefit California Kissin"},
{id:"1093",text:"Boots No. 7 Youthful Replenishing Facial Oil"},
{id:"1094",text:"Benefit Hoola"},
{id:"1095",text:"Chantecaille Real Skin Foundation SPF 30"},
{id:"1096",text:"Chanel Gel Purete Rinse-Off Foaming Gel Cleanser"},
{id:"1097",text:"Chantecaille Brow Definer"},
{id:"1098",text:"Chantecaille Nano Gold Energizing Cream"},
{id:"1099",text:"Chanel Sublimage Essential Regenerating Cream (Texture Supreme)"},
{id:"1100",text:"Clarins Instant Light Eye Perfecting Base"},
{id:"1101",text:"Benefit Roller Lash"},
{id:"1102",text:"Carol's Daughter Ecstasy Sea Salt Scrub"},
{id:"1103",text:"Clarins Beauty Flash Balm"},
{id:"1104",text:"Boots Expert Sensitive Gentle Refreshing Toner"},
{id:"1105",text:"Bioderma Hydrabio Mask"},
{id:"1106",text:"Chanel Ultra Correction Lift Sculpting Firming Concentrate"},
{id:"1107",text:"Boots Facial Oil"},
{id:"1108",text:"Clarins Water Comfort One-Step Cleanser, for Normal to Dry Skin"},
{id:"1109",text:"Clarins Shaping Facial Lift Wrap"},
{id:"1110",text:"Chantecaille Real Skin Foundation"},
{id:"1111",text:"Boots No. 7 Lip Glace"},
{id:"1112",text:"Clarins Sunscreen Wrinkle Control Cream High Protection SPF 30, for Sun-Sensitive Skin"},
{id:"1113",text:"Clarins Pure Melt Cleansing Gel, for All Skin Types"},
{id:"1114",text:"Clarins Self Tanning Instant Gel"},
{id:"1115",text:"Chanel Les 4 Ombres De Chanel Quadra Eyeshadow"},
{id:"1116",text:"Benefit Brow Zings"},
{id:"1117",text:"Caudalie Paris Pulpe Vitaminee Anti-Wrinkle Serum"},
{id:"1118",text:"Chanel Soleil Tan De Chanel Sheer Illuminating Fluid"},
{id:"1119",text:"Chanel Ultra Correction Lift Lifting Firming Day Fluid SPF 15"},
{id:"1120",text:"Clarins Extra-Firming Foundation SPF 15"},
{id:"1121",text:"Benefit Play Sticks"},
{id:"1122",text:"Bioderma White Objective H2O"},
{id:"1123",text:"Clarins Multi-Active Day Early Wrinkle Correction Comfort Cream, Dry Skin"},
{id:"1124",text:"Cargo 3 Triple Action Mascara"},
{id:"1125",text:"Burt's Bees Radiance Eye Creme"},
{id:"1126",text:"Bite Beauty Agave Lip Mask"},
{id:"1127",text:"Burt's Bees Carrot Nutritive Night Creme"},
{id:"1128",text:"Bliss No Zit Sherlock Purifying Cleanser + Toner"},
{id:"1129",text:"Bite Beauty BB For Lips SPF 15"},
{id:"1130",text:"Clarisonic Smart Profile Sonic Brush"},
{id:"1131",text:"Charlotte Tilbury Light Wonder Youth-Boosting Perfect Skin Foundation"},
{id:"1132",text:"Clarins ClarinsMen After Shave Soother"},
{id:"1133",text:"Burt's Bees Garden Tomato Toner"},
{id:"1134",text:"Chanel Mousse Confort Rinse-Off Foaming Cleanser"},
{id:"1135",text:"Burt's Bees Baby Bee Cream-to-Powder"},
{id:"1136",text:"Clarins ClarinsMen Line-Control Cream"},
{id:"1137",text:"Clarins Bright Plus HP Repairing Brightening Night Cream"},
{id:"1138",text:"Bobbi Brown Skin Foundation SPF 15"},
{id:"1139",text:"Carol's Daughter Lip Butter"},
{id:"1140",text:"Cetaphil DailyAdvance Lotion"},
{id:"1141",text:"Carol's Daughter Almond Cookie Body Cleansing Gel"},
{id:"1142",text:"Boots Ultimate Lift Eye Gel"},
{id:"1143",text:"Chanel Eclat Lumiere Highlighter Face Pen"},
{id:"1144",text:"Clarins Gentle Day Cream, for Sensitive Skin"},
{id:"1145",text:"Benefit Theyre Real! Mascara"},
{id:"1146",text:"Boscia Clear Complexion Cleanser with Botanical Blast"},
{id:"1147",text:"Bioderma White Objective Serum"},
{id:"1148",text:"Clarins Renew-Plus Body Serum Age-Defying Concentrate"},
{id:"1149",text:"Boots Botanics Mineral Blusher"},
{id:"1150",text:"Bioderma Photoderm SENSITIVE SPF 50+"},
{id:"1151",text:"Boots Quick Fix Cleansing Wipes"},
{id:"1152",text:"Blue Lizard Blue Lizard Australian Sunscreen SPF 30+ Sport"},
{id:"1153",text:"Boots Radiance Boosting Hot Cloth Cleanser"},
{id:"1154",text:"Burt's Bees Rejuvenating Lip Balm with Aa Berry"},
{id:"1155",text:"Benefit Total Moisture Facial Cream"},
{id:"1156",text:"CeraVe Intensive Stretch Marks Cream"},
{id:"1157",text:"Bioderma Sebium Exfoliating Gel"},
{id:"1158",text:"Cetaphil RESTORADERM Eczema Calming Body Moisturizer"},
{id:"1159",text:"Bobbi Brown Lip Gloss"},
{id:"1160",text:"Biore Deep Cleansing Pore Strips"},
{id:"1161",text:"Burberry Beauty Light Glow"},
{id:"1162",text:"Clarins Water Purify One-Step Cleanser, for Combination or Oily Skin"},
{id:"1163",text:"Clarins Super Restorative Decollete and Neck Concentrate"},
{id:"1164",text:"Cetaphil Dermacontrol Oil Control Foam Wash"},
{id:"1165",text:"Caudalie Paris Premier Cru The Eye Cream"},
{id:"1166",text:"Cargo Eye Shadow Single"},
{id:"1167",text:"Burberry Beauty Lip Mist"},
{id:"1168",text:"Caudalie Paris Vinosource Intense Moisture Rescue Cream"},
{id:"1169",text:"Burt's Bees Ultra Conditioning Lip Balm with Kokum Butter"},
{id:"1170",text:"Boots Expert Sensitive Anti-Blemish Serum"},
{id:"1171",text:"Clarins Super Restorative Serum"},
{id:"1172",text:"Caudalie Paris Premier Cru The Elixir"},
{id:"1173",text:"Chantecaille Protection Naturelle SPF 46 PA+++"},
{id:"1174",text:"Chantecaille Flower Infused Cleansing Milk"},
{id:"1175",text:"Clarins HydraQuench Cream, for Normal to Dry Skin"},
{id:"1176",text:"Chanel Hydra Beauty Micro Serum"},
{id:"1177",text:"Benefit Fine One One Sheer Brightening Color for Cheeks & Lips"},
{id:"1178",text:"Chanel Beaute Initiale Energizing Multi-Protection Eye Cream SPF 15"},
{id:"1179",text:"Burt's Bees Beeswax Lip Balm"},
{id:"1180",text:"Caudalie Paris Moisturizing Cream Mask"},
{id:"1181",text:"Benefit BADgal Waterproof Mascara"},
{id:"1182",text:"Benefit Sun Beam"},
{id:"1183",text:"Caudalie Paris Gentle Cleanser Face And Eyes, For Normal To Dry Skin"},
{id:"1184",text:"Chanel Base Ombre A Paupieres Professional Eye Shadow Base"},
{id:"1185",text:"Bite Beauty Line & Define Lip Primer"},
{id:"1186",text:"Burberry Beauty Fresh Glow Highlighting Powder"},
{id:"1187",text:"Carol's Daughter Acai Hydrating Foam Cleanser"},
{id:"1188",text:"Boots Liquid Eye Liner"},
{id:"1189",text:"Boots Complexion Refining Foundation"},
{id:"1190",text:"Benefit Longwear Powder Shadow"},
{id:"1191",text:"Boscia B.B. Cream SPF 27 PA++"},
{id:"1192",text:"Chanel La Ligne de Chanel Professional Eyeliner Duo"},
{id:"1193",text:"Cetaphil Men Daily Face Wash"},
{id:"1194",text:"Cetaphil Daily Facial Cleanser for Normal to Oily Skin"},
{id:"1195",text:"Bobbi Brown No Smudge Mascara"},
{id:"1196",text:"Benefit Matterial Girl Matte Finish Setting Powder"},
{id:"1197",text:"Carol's Daughter Lemon and Jojoba Cuticle Oil"},
{id:"1198",text:"Bioderma Photoderm MAX Spray SPF 50+"},
{id:"1199",text:"Charlotte Tilbury The Retoucher Conceal & Treat Stick"},
{id:"1200",text:"Chanel Poudre Universelle Compact Natural Finish Pressed Powder"},
{id:"1201",text:"Bobbi Brown Hydrating Intense Night Cream"},
{id:"1202",text:"Boots Expert Sensitive Gentle Cleansing Wash"},
{id:"1203",text:"Boots Expert Sensitive Light Moisturizing Lotion"},
{id:"1204",text:"Boots Botanics Lash Defining Mascara"},
{id:"1205",text:"Charlotte Tilbury The Feline Flick Quick Fine Line Shodo Pen"},
{id:"1206",text:"Benefit Creaseless Cream Eyeshadow/Liner"},
{id:"1207",text:"Burt's Bees Natural Acne Solutions Clarifying Toner"},
{id:"1208",text:"Clarins Double Serum Complete Age Control Concentrate"},
{id:"1209",text:"Burt's Bees Beeswax Moisturizing Day Creme"},
{id:"1210",text:"Boots Protect & Perfect Lip Care"},
{id:"1211",text:"Boots Daily Hand Therapy SPF 10"},
{id:"1212",text:"Chanel Lotion Douceur Gentle Hydrating Toner"},
{id:"1213",text:"Boots Mattifying Makeup Base"},
{id:"1214",text:"Boots Organic Face Nourishing Cleanser"},
{id:"1215",text:"Carol's Daughter Scar Butter"},
{id:"1216",text:"Bobbi Brown Brightening Lip Gloss"},
{id:"1217",text:"Burberry Beauty Effortless Mascara"},
{id:"1218",text:"Bioderma Sensibio Cleansing Milk"},
{id:"1219",text:"Bioderma Photoderm After Sun"},
{id:"1220",text:"Clarins Eyebrow Pencil"},
{id:"1221",text:"Cargo EyeLighter"},
{id:"1222",text:"Clarins Eye Contour Balm, for All Skin Types"},
{id:"1223",text:"Benefit Boo-Boo Zap"},
{id:"1224",text:"Chantecaille Loose Powder"},
{id:"1225",text:"Charlotte Tilbury Goddess Skin Clay Mask"},
{id:"1226",text:"Burt's Bees Baby Bee Kissable Cheeks Balm"},
{id:"1227",text:"Burt's Bees Baby Bee Buttermilk Soap"},
{id:"1228",text:"Boots Lip Glace"},
{id:"1229",text:"Burt's Bees Brightening Daily Facial Cleanser"},
{id:"1230",text:"Burt's Bees Radiance SPF 7 Day Lotion"},
{id:"1231",text:"Chantecaille Retinol Body Treatment with Rose and Edelweiss"},
{id:"1232",text:"Clarins Moisture-Rich Body Lotion For Dry Skin"},
{id:"1233",text:"Boots Advanced Renewal Anti-Ageing Glycolic Peel Kit"},
{id:"1234",text:"Benefit Pocket Pal"},
{id:"1235",text:"Chantecaille Future Skin Foundation"},
{id:"1236",text:"Boots Triple Action Day Moisture Cream SPF 15"},
{id:"1237",text:"Bobbi Brown Shimmer Brick Compact"},
{id:"1238",text:"Clarins Ombre Matte Eyeshadow Cream-To-Powder Smoothing & Long-Lasting"},
{id:"1239",text:"Bioderma Cicabio Cream"},
{id:"1240",text:"Biore Daily Cleansing Cloths"},
{id:"1241",text:"Boots Protect & Perfect Beauty Serum"},
{id:"1242",text:"Burt's Bees Natural Acne Solutions Pore Refining Scrub"},
{id:"1243",text:"Chanel Lotion Confort Silky Soothing Toner"},
{id:"1244",text:"Clarins Truly Matte Pure & Radiant Mask"},
{id:"1245",text:"Chanel Lift Lumiere Concealer Smoothing and Rejuvenating Eye Contour Concealer"},
{id:"1246",text:"Charlotte Tilbury Rock 'N' Kohl Iconic Liquid Eye Pencil"},
{id:"1247",text:"Boscia Tsubaki Cleansing Oil Gel"},
{id:"1248",text:"Burberry Beauty Warm Glow"},
{id:"1249",text:"Bobbi Brown Blush"},
{id:"1250",text:"Burt's Bees Herbal Blemish Stick"},
{id:"1251",text:"Boots Expert Anti-Blemish Night Moisturizer"},
{id:"1252",text:"Boscia Fresh Blotting Linens"},
{id:"1253",text:"Carol's Daughter Acai Clarifying Face Lotion"},
{id:"1254",text:"Boots Botanics Lighter Than Air Loose Powder"},
{id:"1255",text:"Caudalie Paris Instant Foaming Cleanser"},
{id:"1256",text:"Benefit Smokin Eyes"},
{id:"1257",text:"Chanel Body Excellence Nourishing and Rejuvenating Hand Cream"},
{id:"1258",text:"Caudalie Paris Resveratrol Lift Firming Serum"},
{id:"1259",text:"Clarins Mono Eyeshadow"},
{id:"1260",text:"Chantecaille Rice and Geranium Foaming Cleanser"},
{id:"1261",text:"Caudalie Paris Vinoperfect Complexion Correcting Radiance Serum"},
{id:"1262",text:"Boots Eye and Lip Correction Serum"},
{id:"1263",text:"Benefit Ultra Plush Lip Gloss"},
{id:"1264",text:"Biore Deep Pore Charcoal Cleanser"},
{id:"1265",text:"Bite Beauty Cashmere Lip Cream"},
{id:"1266",text:"Chantecaille Faux Cils Longest Lash Mascara"},
{id:"1267",text:"Boots Radiance Renewal Night Serum"},
{id:"1268",text:"Burt's Bees Naturally Ageless Line Diminishing Day Lotion"},
{id:"1269",text:"Boots Protect & Perfect Intense Beauty Serum"},
{id:"1270",text:"Biore Combination Skin Balancing Cleanser"},
{id:"1271",text:"Chantecaille Hydra Chic Lipstick"},
{id:"1272",text:"Clarins Gentle Foaming Cleanser With Shea Butter"},
{id:"1273",text:"Carol's Daughter Almond Cookie Sea Salt Scrub"},
{id:"1274",text:"Clarins Extra-Firming Body Cream"},
{id:"1275",text:"Clarins Face Treatment Oil, Blue Orchid, for Dehydrated Skin"},
{id:"1276",text:"Clarins Multi-Active Night Youth Recovery Cream for Normal to Combination Skin"},
{id:"1277",text:"Burt's Bees Facial Cleansing Towelettes Pink Grapefruit"},
{id:"1278",text:"Bioderma Photoderm MAX Milk SPF 50"},
{id:"1279",text:"Benefit Theyre Real! Push-Up Liner"},
{id:"1280",text:"Cargo Color Eyeshadow Palette"},
{id:"1281",text:"Chantecaille Flower Harmonizing Cream"},
{id:"1282",text:"Boots No. 7 Dual Action Tinted Moisturizer SPF 15"},
{id:"1283",text:"Charlotte Tilbury Brow Lift Three-Way Shape, Lift & Shade Tool"},
{id:"1284",text:"Bliss Baggage Handler"},
{id:"1285",text:"Burt's Bees Brightening Eye Treatment"},
{id:"1286",text:"Bliss Lid + Lash Wash"},
{id:"1287",text:"Boscia Oil-Free Daily Hydration SPF 15"},
{id:"1288",text:"Boots Lash & Brow Perfector"},
{id:"1289",text:"Chanel Le Jour De Chanel"},
{id:"1290",text:"Chantecaille Nano Gold Energizing Eye Cream"},
{id:"1291",text:"Chanel Le Blanc de Chanel Sheer Illuminating Base"},
{id:"1292",text:"Chanel La Nuit De Chanel"},
{id:"1293",text:"Bioderma Hydrabio Serum Moisturizing Concentrate"},
{id:"1294",text:"Burt's Bees Brightening Dark Spot Corrector"},
{id:"1295",text:"Bobbi Brown CC Cream Broad Spectrum SPF 35"},
{id:"1296",text:"Bliss Pore Perfecting Facial Polish"},
{id:"1297",text:"Chanel Sublimage La Creme Texture Supreme"},
{id:"1298",text:"Boots Expert Anti-Blemish Concealer Duo"},
{id:"1299",text:"Cargo Bronzer"},
{id:"1300",text:"Boots Organic Face Rosewater Toner"},
{id:"1301",text:"Boots Protect & Perfect Night Cream"},
{id:"1302",text:"Burt's Bees Facial Towelettes with White Tea Extract"},
{id:"1303",text:"Chanel Le Blanc Brightening Moisture Lotion"},
{id:"1304",text:"Boots Perfectly Bronzed Mosaic Bronzer"},
{id:"1305",text:"Caudalie Paris Contouring Concentrate"},
{id:"1306",text:"Bobbi Brown Intensive Skin Serum Concealer"},
{id:"1307",text:"Burt's Bees Sensitive Daily Moisturizing Cream"},
{id:"1308",text:"Caudalie Paris Vinexpert Anti-Ageing Serum Eyes And Lips, For Skin Lacking Vitality"},
{id:"1309",text:"Chantecaille Vital Essence"},
{id:"1310",text:"Benefit Lip Plump"},
{id:"1311",text:"Burt's Bees Facial Cleansing Oil"},
{id:"1312",text:"Burt's Bees Peach & Willowbark Deep Pore Scrub"},
{id:"1313",text:"Boots Botanics Lip Colour"},
{id:"1314",text:"Boots Wild Volume Lipstick"},
{id:"1315",text:"Charlotte Tilbury Filmstar Bronze & Glow Face Sculpt & Highlight (Light to Medium)"},
{id:"1316",text:"Boots Botanics Lip Gloss"},
{id:"1317",text:"Clarins Joli Rouge"},
{id:"1318",text:"Boots Complexion Refining Clay Mask"},
{id:"1319",text:"Clarins HydraQuench Cream SPF 15"},
{id:"1320",text:"Benefit Brows A-Go-Go"},
{id:"1321",text:"Bliss The Youth As We Know It Night Cream"},
{id:"1322",text:"Chanel Hydramax + Active Teinte Active Moisture Tinted Lotion SPF 15"},
{id:"1323",text:"Burt's Bees Soap Bark & Chamomile Deep Cleansing Cream"},
{id:"1324",text:"Bobbi Brown Extra Eye Repair Cream"},
{id:"1325",text:"Boscia Tsubaki Beauty Oil"},
{id:"1326",text:"CeraVe Hydrating Cleanser"},
{id:"1327",text:"Boots Stay Perfect Lipstick"},
{id:"1328",text:"Bliss The Youth As We Know It Anti-Aging Moisture Lotion SPF 30"},
{id:"1329",text:"Boots Botanics Eye Colour"},
{id:"1330",text:"Charlotte Tilbury Lip Magic"},
{id:"1331",text:"Caudalie Paris Hand and Nail Cream"},
{id:"1332",text:"Clarins HydraQuench Cream-Mask"},
{id:"1333",text:"Clarins Lip Definer"},
{id:"1334",text:"CeraVe Sunscreen Stick Broad Spectrum SPF 50"},
{id:"1335",text:"Benefit Erase Paste"},
{id:"1336",text:"Burt's Bees Island Lip Balm With Passion Fruit"},
{id:"1337",text:"Burt's Bees Ultra Moisturizing Lip Treatment"},
{id:"1338",text:"Bobbi Brown Pot Rouge for Lips and Cheeks"},
{id:"1339",text:"Biore Make-Up Removing Towelettes"},
{id:"1340",text:"Burt's Bees Baby Bee Skin Creme"},
{id:"1341",text:"Benefit Hydrating Tinted Lip Balm"},
{id:"1342",text:"Boots Stay Perfect Eye Shadow Duo"},
{id:"1343",text:"Clarins Everlasting Foundation SPF 15"},
{id:"1344",text:"Burt's Bees Soothing Lip Balm with Eucalyptus and Menthol"},
{id:"1345",text:"Benefit One Hot Minute"},
{id:"1346",text:"Clarins Intense Bronze Self Tanning Tint for Face and Decollete"},
{id:"1347",text:"Bobbi Brown Face Touch Up Stick"},
{id:"1348",text:"Clarins Vital Light Day Illuminating Anti-Ageing Comfort Cream"},
{id:"1349",text:"Bobbi Brown Sheer Lip Color"},
{id:"1350",text:"Boots Beautifully Matte Foundation SPF 15"},
{id:"1351",text:"Chanel Le Blanc Fresh Brightening Foam Cleanser"},
{id:"1352",text:"Bobbi Brown Hydrating Face Cream"},
{id:"1353",text:"Charlotte Tilbury Cheek to Chic Swish and Pop Blush"},
{id:"1354",text:"Clarins Eye Pencil"},
{id:"1355",text:"Chantecaille Radiance Gel Bronzer"},
{id:"1356",text:"Bioderma Sensibio Light Cream"},
{id:"1357",text:"Bioderma Sebium H2O"},
{id:"1358",text:"Boots Perfect Light Pressed Powder"},
{id:"1359",text:"Bioderma Hydrabio Lait"},
{id:"1360",text:"Clarins Bright Plus HP Brightening Hydrating Day Lotion SPF 20"},
{id:"1361",text:"Bioderma Photoderm Anti-Age SPF 30"},
{id:"1362",text:"Caudalie Paris Polyphenol C15 Overnight Detox Oil"},
{id:"1363",text:"Burt's Bees Naturally Ageless Intensive Repairing Serum"},
{id:"1364",text:"Burt's Bees Repair Serum"},
{id:"1365",text:"Chantecaille Retinol Hand Cream with Rose and Vitamin C"},
{id:"1366",text:"Clarins Vital Light Day SPF 15 Illuminating Anti-Ageing Cream-All Skin Types"},
{id:"1367",text:"Clarins Gentle Foaming Cleanser for Normal or Combination Skin"},
{id:"1368",text:"Chanel Les Tissages de Chanel Blush Duo Tweed Effect"},
{id:"1369",text:"Bioderma White Objective Pen"},
{id:"1370",text:"Benefit The POREfessional: Matte Rescue Invisible-Finish Mattifying Gel"},
{id:"1371",text:"Chanel Rouge Allure Luminous Satin Lip Color"},
{id:"1372",text:"Charlotte Tilbury Eyes to Mesmerise Long-Lasting Easy Cream Eyeshadow"},
{id:"1373",text:"Boots Radiance Beauty Balm"},
{id:"1374",text:"Chantecaille Brilliant Gloss"},
{id:"1375",text:"Cargo High Pigment Eye Pencil"},
{id:"1376",text:"Boots Skin Illuminator Radiance Boosting Beauty Fluid"},
{id:"1377",text:"Chantecaille White Tiger Poudre Delicate"},
{id:"1378",text:"CeraVe Sunscreen for Face SPF 30"},
{id:"1379",text:"Clarins Eyebrow Kit Pro Palette"},
{id:"1380",text:"Biore Steam Activated Cleanser"},
{id:"1381",text:"Carol's Daughter Lemon and Rose Hand Cream"},
{id:"1382",text:"Benefit Hello Flawless Oxygen Wow Broad Spectrum SPF 25 Brightening Sunscreen Makeup"},
{id:"1383",text:"Caudalie Paris Vinosource Moisture Recovery Cream"},
{id:"1384",text:"Clarins Instant Concealer"},
{id:"1385",text:"Carol's Daughter Almond Cookie Body Butter"},
{id:"1386",text:"Benefit Thrrrob"},
{id:"1387",text:"Bliss The Youth As We Know It Eye Cream"},
{id:"1388",text:"Bioderma Sebium Cream"},
{id:"1389",text:"CeraVe Sunscreen for Body SPF 30"},
{id:"1390",text:"Chanel Ombres Contraste Duo, Eyeshadow Duo"},
{id:"1391",text:"Boscia Restorative Eye Treatment for Under-Eye Bags"},
{id:"1392",text:"Boots In Shower Facial Polish"},
{id:"1393",text:"Charlotte Tilbury Charlotte's Magic Cream"},
{id:"1394",text:"Clarins ClarinsMen Active Face Wash"},
{id:"1395",text:"Bioderma Sebium AKN"},
{id:"1396",text:"Bobbi Brown Extra Treatment Lotion"},
{id:"1397",text:"Chanel Hydra Beauty Serum"},
{id:"1398",text:"Chanel Masque Destressant Purete Purifying Cream Mask"},
{id:"1399",text:"Bioderma Sensibio DS Cream"},
{id:"1400",text:"Boots Expert Anti-Blemish Serum"},
{id:"1401",text:"Chanel Mousse Douceur Rinse-Off Foaming Mousse Cleanser"},
{id:"1402",text:"Cargo One Base Concealer + Foundation in One"},
{id:"1403",text:"Boots Moisturising Eye Cream"},
{id:"1404",text:"Chanel Rouge Coco Hydrating Crme Lip Colour"},
{id:"1405",text:"Clarins Ever Matte SPF 15 Oil-Free Skin Balancing Foundation"},
{id:"1406",text:"Bobbi Brown Shimmer Blush"},
{id:"1407",text:"Boscia Tsubaki Oil-Infused Exfoliating Powder"},
{id:"1408",text:"Clarins Instant Smooth Golden Glow"},
{id:"1409",text:"Benefit Speed Brow"},
{id:"1410",text:"Boots Perfectly Bronzed Pearls"},
{id:"1411",text:"Clarins Sun Wrinkle Control Cream for Face SPF 50+, Very High Protection"},
{id:"1412",text:"Burberry Beauty Complete Eye Palette"},
{id:"1413",text:"Boots Organic Lip Balm"},
{id:"1414",text:"Boots Botanics Eye Definer"},
{id:"1415",text:"Boots Expert Sensitive Cleansing & Toning Wipes"},
{id:"1416",text:"Benefit Instant Brow Pencil"},
{id:"1417",text:"Boots Beautiful Brows Pencil"},
{id:"1418",text:"Bioderma Sensibio Eye Contour Gel"},
{id:"1419",text:"Chantecaille Mascara Noir"},
{id:"1420",text:"Cargo Lash Activator"},
{id:"1421",text:"Clarins ClarinsMen Exfoliating Cleanser"},
{id:"1422",text:"Chanel Correcteur Perfection Long Lasting Concealer"},
{id:"1423",text:"Boscia Detoxifying Black Cleanser"},
{id:"1424",text:"Caudalie Paris Moisturizing Toner"},
{id:"1425",text:"Clarins Eye Quartet Mineral Palette"},
{id:"1426",text:"Bobbi Brown Rich Color Lip Gloss"},
{id:"1427",text:"Burt's Bees Radiance Night Cream"},
{id:"1428",text:"Chantecaille Compact Soleil"},
{id:"1429",text:"Burt's Bees Natural Skin Care For Men Aftershave"},
{id:"1430",text:"CeraVe Makeup Removing Cleanser Cloths"},
{id:"1431",text:"CeraVe Sunscreen for Body SPF 50"},
{id:"1432",text:"Clarins Face Treatment Oil, Santal, for Dry or Extra Dry Skin"},
{id:"1433",text:"Cetaphil Gentle Cleansing Bar"},
{id:"1434",text:"CeraVe Eye Repair Cream"},
{id:"1435",text:"Bio-Oil Bio-Oil"},
{id:"1436",text:"Burt's Bees Facial Cleansing Towelettes Cucumber and Sage"},
{id:"1437",text:"Bobbi Brown Intensive Skin Serum Corrector"},
{id:"1438",text:"Chantecaille Lip Definer"},
{id:"1439",text:"Chanel Le Lift Firming Anti-Wrinkle Crme Riche"},
{id:"1440",text:"Clarins One-Step Facial Cleanser with Orange Extract"},
{id:"1441",text:"Bobbi Brown BB Cream Broad Spectrum SPF 35"},
{id:"1442",text:"Chanel Rouge Coco Shine Hydrating Sheer Lipshine"},
{id:"1443",text:"Bobbi Brown Brow Kit"},
{id:"1444",text:"CeraVe SA Renewing Lotion"},
{id:"1445",text:"Bobbi Brown Extra Repair Serum"},
{id:"1446",text:"Benefit Bathina Take a Picture...It Lasts Longer..."},
{id:"1447",text:"Burt's Bees Res-Q Ointment"},
{id:"1448",text:"Bobbi Brown Lash Glamour Extreme Lengthening Mascara"},
{id:"1449",text:"Burt's Bees Intense Hydration Eye Cream with Clary Sage"},
{id:"1450",text:"Chanel Le Blanc Brightening Moisturizing Cream"},
{id:"1451",text:"CeraVe Therapeutic Hand Cream"},
{id:"1452",text:"Benefit The POREfessional: Agent Zero Shine"},
{id:"1453",text:"Caudalie Paris Firming Concentrate"},
{id:"1454",text:"Burt's Bees Hydrating Lip Balm with Coconut & Pear"},
{id:"1455",text:"Bliss Peeling Groovy Facial Serum"},
{id:"1456",text:"Boscia Clear Complexion Moisturizer with Botanical Blast"},
{id:"1457",text:"Bioderma Photoderm AKN Mat SPF 30"},
{id:"1458",text:"Burt's Bees Intense Hydration Night Cream with Clary Sage"},
{id:"1459",text:"Caudalie Paris Vinexpert Firming Serum"},
{id:"1460",text:"Clarins ClarinsMen Revitalizing Gel"},
{id:"1461",text:"Burberry Beauty Lip Definer"},
{id:"1462",text:"Clarins Sunscreen Care Cream SPF 30, High Protection"},
{id:"1463",text:"Caudalie Paris Vinoperfect Enzymatic Peel Mask"},
{id:"1464",text:"Burberry Beauty Lip Velvet"},
{id:"1465",text:"Chantecaille Faux Cils Mascara"},
{id:"1466",text:"Caudalie Paris Lip Conditioner"},
{id:"1467",text:"Bioderma Sensibio Forte"},
{id:"1468",text:"Boots Calm Skin Redness-Relief Gel"},
{id:"1469",text:"Bobbi Brown Lip Liner"},
{id:"1470",text:"Burt's Bees Naturally Nourishing Milk & Shea Butter Body Wash"},
{id:"1471",text:"Bioderma Sebium Pore Refiner Corrective Concentrate"},
{id:"1472",text:"Clarins Extra-Comfort Toning Lotion, for Dry or Sensitized Skin"},
{id:"1473",text:"CeraVe Hydrating Body Wash"},
{id:"1474",text:"Clarins Instant Smooth Perfecting Touch"},
{id:"1475",text:"Cargo Magic Brush"},
{id:"1476",text:"Chanel Gommage Microperle Eclat Maximum Radiance Exfoliating Gel"},
{id:"1477",text:"Chantecaille Bio Lift Concealer"},
{id:"1478",text:"Chanel Inimitable Intense Mascara Multi-Dimensionel Sophistique Volume Length Curl Separation"},
{id:"1479",text:"Clarins Extra-Firming Day Wrinkle Lifting Cream, Special for Dry Skin"},
{id:"1480",text:"Clarins Bust Beauty Extra-Lift Gel"},
{id:"1481",text:"Benefit The Rockateur"},
{id:"1482",text:"Chanel Le Blush Creme"},
{id:"1483",text:"Chanel Vitalumiere Aqua Ultra-Light Skin Perfecting Makeup SPF 15"},
{id:"1484",text:"Clarins Instant Eye Make-Up Remover"},
{id:"1485",text:"Burt's Bees Natural Acne Solutions Daily Moisturizing Lotion"},
{id:"1486",text:"Clarins Extra-Firming Day Wrinkle Lifting Cream , for All Skin Types"},
{id:"1487",text:"Benefit Some Kind-A-Gorgeous"},
{id:"1488",text:"Caudalie Paris Premier Cru The Creme"},
{id:"1489",text:"Benefit The POREfessional"},
{id:"1490",text:"Clarins ClarinsMen Super Moisture Balm"},
{id:"1491",text:"Caudalie Paris Pulpe Vitaminee Anti-Wrinkle Fluid"},
{id:"1492",text:"Benefit Dandelion Shy Beam Matte Liquid Highlighter"},
{id:"1493",text:"Chanel Ultra Correction Lift Intensive Lifting Concentrate"},
{id:"1494",text:"Bobbi Brown Perfectly Defined Gel Eyeliner"},
{id:"1495",text:"Bobbi Brown Hydrating Eye Cream"},
{id:"1496",text:"Carol's Daughter Ecstasy Body Butter"},
{id:"1497",text:"Benefit BADgal Liner Waterproof"},
{id:"1498",text:"Boots Moisture Drench Lipstick"},
{id:"1499",text:"CeraVe Itch Relief Moisturizing Lotion"},
{id:"1500",text:"Bobbi Brown Extra Repair Moisturizing Balm"},
{id:"1501",text:"Boots Extravagant Lashes Mascara"},
{id:"1502",text:"Clarins Double Fix Mascara Waterproofing Seal"},
{id:"1503",text:"Benefit Lemon Aid"},
{id:"1504",text:"Bobbi Brown Extra Repair Moisturizing Balm SPF 25"},
{id:"1505",text:"CeraVe Skin Renewing Night Cream"},
{id:"1506",text:"Boots No. 7 Lift & Luminate Foundation SPF 15"},
{id:"1507",text:"Benefit CORALista"},
{id:"1508",text:"Clarins Sunscreen Care Milk-Lotion Spray SPF 50+"},
{id:"1509",text:"Benefit Remove It Makeup Remover"},
{id:"1510",text:"Boots No7 Lift & Luminate Night Cream"},
{id:"1511",text:"Bobbi Brown Retouching Face Pencil"},
{id:"1512",text:"Burt's Bees Naturally Ageless Line Smoothing Eye Creme"},
{id:"1513",text:"Chantecaille Lasting Eye Shade"},
{id:"1514",text:"Benefit Boi-ing"},
{id:"1515",text:"Clarins Liquid Bronze Self Tanning, for Face and Decollete"},
{id:"1516",text:"Chanel Ombre Essentielle Soft Touch Eyeshadow"},
{id:"1517",text:"Burberry Beauty Sheer Foundation"},
{id:"1518",text:"Boscia Smoothing Facial Polish"},
{id:"1519",text:"Bobbi Brown Sequin Eye Shadow"},
{id:"1520",text:"Clarins Mission Perfection Serum"},
{id:"1521",text:"Charlotte Tilbury Airbrush Flawless Finish Powder"},
{id:"1522",text:"Clarins Extra-Firming Lip & Contour Balm"},
{id:"1523",text:"Burt's Bees Thoroughly Therapeutic Honey & Grapeseed Oil Hand Creme"},
{id:"1524",text:"Clarins Instant Smooth Line Correcting Concentrate"},
{id:"1525",text:"Clarins Skin Illusion Loose Powder Foundation"},
{id:"1526",text:"Boots Botanics Age Defense Foundation"},
{id:"1527",text:"Boots Expert Scar Care Serum"},
{id:"1528",text:"Boots Stay Perfect Eyeliner"},
{id:"1529",text:"Bobbi Brown Smokey Eye Mascara"},
{id:"1530",text:"Clarins Bright Plus HP On-The-Spot Brightening Corrector"},
{id:"1531",text:"Bobbi Brown Eye Shadow"},
{id:"1532",text:"Chanel Le Lift Firming Anti-Wrinkle Crme Fine"},
{id:"1533",text:"Benefit Dandelion"},
{id:"1534",text:"Boots Instant Radiance Beauty Balm"},
{id:"1535",text:"Bobbi Brown Double Lining Eyeshadow"},
{id:"1536",text:"Clarins Extra-Firming Night Cream, Special for Dry Skin"},
{id:"1537",text:"Chanel Stylo Yeux Waterproof Long-Lasting Eyeliner Waterproof"},
{id:"1538",text:"Bliss Triple Oxygen Instant Energizing Cleansing Foam"},
{id:"1539",text:"Biore Deep Cleansing Pore Strips Ultra"},
{id:"1540",text:"Boots Botanics Volumising Mascara"},
{id:"1541",text:"Chanel CC Cream Complete Correction Sunscreen Broad Spectrum SPF 30"},
{id:"1542",text:"Clarins Radiance Plus Golden Glow Booster"},
{id:"1543",text:"Bioderma Hydrabio Exfoliating Cream"},
{id:"1544",text:"Boots Mineral Perfection Eye Shadow Palette"},
{id:"1545",text:"Bobbi Brown Shimmer Wash Eye Shadow"},
{id:"1546",text:"Clarins Delectable Self-Tanning Mousse with Mirabelle Oil, SPF 15"},
{id:"1547",text:"Bobbi Brown Creamy Concealer Kit"},
{id:"1548",text:"Benefit Air Patrol BB Cream Eyelid Primer Broad Spectrum SPF 20"},
{id:"1549",text:"Benefit Triple Performing Facial Emulsion SPF 15"},
{id:"1550",text:"Clarins Stretch Mark Minimizer"},
{id:"1551",text:"CeraVe Eczema Soothing Body Wash"},
{id:"1552",text:"Benefit Girl Meets Pearl"},
{id:"1553",text:"Boscia Tsubaki Deep Hydration Sleeping Mask"},
{id:"1554",text:"Chantecaille Total Concealer"},
{id:"1555",text:"Burt's Bees Super Glossy Lip Shine"},
{id:"1556",text:"Burt's Bees Lemon Butter Cuticle Cream"},
{id:"1557",text:"Clarins Multi-Active Day Correction Cream-Gel Normal to Combination Skin"},
{id:"1558",text:"Bite Beauty Luminous Creme Lipstick"},
{id:"1559",text:"Chantecaille Biodynamic Lifting Oil Free Fluid"},
{id:"1560",text:"Chanel Joues Contraste Powder Blush"},
{id:"1561",text:"Chanel Rouge Allure Luminous Intense Lip Color"},
{id:"1562",text:"Boots Total Renewal Micro-Dermabrasion Exfoliator"},
{id:"1563",text:"Chantecaille La Baleine Bleue Eyes"},
{id:"1564",text:"Benefit Silky-Finish Lipstick"},
{id:"1565",text:"Chanel Mat Lumiere Long Lasting Luminous Matte Fluid Makeup SPF 15"},
{id:"1566",text:"Chantecaille Lip Sheer"},
{id:"1567",text:"Cargo BeachBlush"},
{id:"1568",text:"Cetaphil Moisturizing Lotion"},
{id:"1569",text:"Boscia Pore Purifying Black Strips"},
{id:"1570",text:"Blue Lizard Blue Lizard Australian Sunscreen SPF 30+ Regular"},
{id:"1571",text:"Benefit Dream Screen Invisible Silky-Matte Broad Spectrum SPF 45 for Face"},
{id:"1572",text:"Burberry Beauty Eye Definer"},
{id:"1573",text:"Bliss Triple Oxygen Instant Energizing Eye Mask"},
{id:"1574",text:"Cetaphil Intensive Moisturizing Cream"},
{id:"1575",text:"Bioderma Photoderm MAX Cream SPF 100"},
{id:"1576",text:"Burt's Bees Beeswax Moisturizing Night Creme"},
{id:"1577",text:"Burt's Bees Natural Skin Care For Men Shave Cream"},
{id:"1578",text:"Boots Cream Eye Liner"},
{id:"1579",text:"Bobbi Brown Natural Brow Shaper"},
{id:"1580",text:"Chanel Ultra Correction Lift Total Eye Lift"},
{id:"1581",text:"Chantecaille Iridescent Eye Shade"},
{id:"1582",text:"Bobbi Brown Eye Makeup Remover"},
{id:"1583",text:"Chanel Les 4 Ombres Multi-Effect Quadra Eyeshadow"},
{id:"1584",text:"Clarins Joli Rouge Perfect Shine Sheer Lipstick"},
{id:"1585",text:"Chanel Poudre Douce Soft Pressed Powder"},
{id:"1586",text:"Boots High Shine Lip Gloss"},
{id:"1587",text:"Boots Stay Precise Felt Tip Eye Liner"},
{id:"1588",text:"Boots Quick Thinking 4 in 1 Wipes"},
{id:"1589",text:"Boscia Clarifying Detox Mask"},
{id:"1590",text:"Boots Skin Brightening Cleanser"},
{id:"1591",text:"Boots Expert Anti-Blemish Toner"},
{id:"1592",text:"Caudalie Paris Vinexpert Broad Spectrum SPF 15 Radiance Day Cream"},
{id:"1593",text:"Boots Stay Perfect Smoothing & Brightening Eye Base"},
{id:"1594",text:"Benefit You Rebel Lite SPF 15"},
{id:"1595",text:"Clarins Defining Eye Lift"},
{id:"1596",text:"Biore 4-in-1 Detoxifying Cleanser"},
{id:"1597",text:"Bioderma Photoderm BRONZ Spray SPF 50+"},
{id:"1598",text:"Boots Organic Face Super Balm"},
{id:"1599",text:"Caudalie Paris Organic Grape Water, for All Skin Types"},
{id:"1600",text:"Clarins Body Lift Cellulite Control"},
{id:"1601",text:"Boots Expert Sensitive Hydrating Moisturizer"},
{id:"1602",text:"Boots Expert Sensitive Hydrating Serum"},
{id:"1603",text:"Boots Mineral Perfection Foundation"},
{id:"1604",text:"Cetaphil Daily Facial Moisturizer with SPF 50+"},
{id:"1605",text:"Chanel Sublimage Essential Revitalizing Concentrate"},
{id:"1606",text:"Boots Perfect Light Loose Powder"},
{id:"1607",text:"Chantecaille Cheek Creme"},
{id:"1608",text:"Chantecaille Just Skin Anti-Smog Tinted Moisturizer SPF 15"},
{id:"1609",text:"Clarins ClarinsMen Anti-Fatigue Eye Serum"},
{id:"1610",text:"Clarins Eye Revive Beauty Flash"},
{id:"1611",text:"Chanel Ultra Correction Lift Plumping Anti Wrinkle Lips and Contour"},
{id:"1612",text:"Bioderma Sensibio Mask"},
{id:"1613",text:"CeraVe AM Facial Moisturizing Lotion Broad Spectrum SPF 30"},
{id:"1614",text:"Clarins Smoothing Body Scrub For a New Skin, With Bamboo Powders"},
{id:"1615",text:"Boots Intensive Nourishing Serum"},
{id:"1616",text:"Charlotte Tilbury Lip Lustre"},
{id:"1617",text:"Clarins Instant Definition Mascara"},
{id:"1618",text:"CeraVe Renewing SA Cream"},
{id:"1619",text:"Benefit Majorette"},
{id:"1620",text:"Caudalie Paris Crushed Cabernet Scrub"},
{id:"1621",text:"Bobbi Brown Creamy Lip Color"},
{id:"1622",text:"Benefit Sugarbomb"},
{id:"1623",text:"Boots Expert Shine Control Papers"},
{id:"1624",text:"Bobbi Brown Creamy Matte Lip Color"},
{id:"1625",text:"Clarins Toning Lotion with Iris, for Combination or Oily Skin"},
{id:"1626",text:"Cargo Liquid Foundation"},
{id:"1627",text:"Burt's Bees Lip Shimmers"},
{id:"1628",text:"Chanel Mousse Exfoliante Purete Rinse Off Exfoliating Cleansing Foam Purity + Anti-Pollution"},
{id:"1629",text:"Clarins Extra-Firming Tightening Lift Botanical Serum"},
{id:"1630",text:"Cetaphil Gentle Skin Cleanser"},
{id:"1631",text:"Carol's Daughter Body Jelly"},
{id:"1632",text:"Bioderma Hydrabio H2O"},
{id:"1633",text:"Boots Expert Shine Control Lotion"},
{id:"1634",text:"Chanel Lotion Purete Fresh Mattifying Toner"},
{id:"1635",text:"Bioderma Sensibio Rich Cream"},
{id:"1636",text:"Chanel Sublimage Essential Regenerating Fluid"},
{id:"1637",text:"Bobbi Brown Long-Wear Eye Pencil"},
{id:"1638",text:"Boscia Clear Complexion Willow Bark Spot Treatment"},
{id:"1639",text:"Cargo Tinted Moisturizer SPF 20"},
{id:"1640",text:"Boots Stay Perfect Eye Shadow Single"},
{id:"1641",text:"Clarins Super Restorative Day Cream"},
{id:"1642",text:"Burt's Bees Radiance Daily Cleanser"},
{id:"1643",text:"Boscia Oil-Free Nightly Hydration"},
{id:"1644",text:"Clarins Body Treatment Oil-Tonic"},
{id:"1645",text:"Benefit Full-Finish Lipstick"},
{id:"1646",text:"CeraVe Renewing SA Cleanser"},
{id:"1647",text:"Charlotte Tilbury Filmstars On The Go Easy Look In A Palette"},
{id:"1648",text:"Burberry Beauty Sheer Compact Foundation"},
{id:"1649",text:"Burt's Bees Tinted Lip Balm"},
{id:"1650",text:"Burt's Bees Natural Acne Solutions Purifying Gel Cleanser"},
{id:"1651",text:"Burt's Bees Beeswax & Royal Jelly Eye Creme"},
{id:"1652",text:"Bobbi Brown Blotting Papers"},
{id:"1653",text:"Chanel Ultra Correction Line Repair Anti-Wrinkle Comfort Day Cream SPF 15"},
{id:"1654",text:"Burt's Bees Poison Ivy Soap"},
{id:"1655",text:"Chanel Vitalumiere Moisture-Rich Radiance Fluid Makeup SPF 15"},
{id:"1656",text:"Bobbi Brown Extra SPF 25 Tinted Moisturizing Balm"},
{id:"1657",text:"Clarins Truly Matte Ultra-Matte Rebalancing Lotion"},
{id:"1658",text:"Chanel Ultra Correction Lift Ultra Lifting Firming Night Cream"},
{id:"1659",text:"Carol's Daughter Acai Hydrating Face Lotion"},
{id:"1660",text:"Bobbi Brown Extra Face Oil"},
{id:"1661",text:"Clarins Radiance-Plus Self Tanning Cream-Gel"},
{id:"1662",text:"Chantecaille Biodynamic Lifting Cream"},
{id:"1663",text:"Clarins Eye Contour Balm, Special for Dry Skin"},
{id:"1664",text:"Bobbi Brown Rich Lip Color"},
{id:"1665",text:"Bobbi Brown Lip Balm"},
{id:"1666",text:"Bobbi Brown Brightening Finishing Powder"},
{id:"1667",text:"Carol's Daughter Sage and Shea Foot Butter"},
{id:"1668",text:"Caudalie Paris Vinoperfect Day Perfecting Fluid SPF 15 PA++"},
{id:"1669",text:"Bobbi Brown Brightening Brick"},
{id:"1670",text:"Chanel Rouge Double Intensite Ultra Wear Lip Colour"},
{id:"1671",text:"Burberry Beauty Fresh Glow Fluid"},
{id:"1672",text:"Chanel Rouge Allure Gloss"},
{id:"1673",text:"Boscia Enlivening Amino-AG Eye Treatment"},
{id:"1674",text:"Bobbi Brown Metallic Eye Shadow"},
{id:"1675",text:"Caudalie Paris Vinoperfect Cell Renewal Night Cream"},
{id:"1676",text:"Bobbi Brown Brow Pencil"},
{id:"1677",text:"Bobbi Brown Vitamin Enriched Face Base"},
{id:"1678",text:"Benefit Chacha Tint"},
{id:"1679",text:"Bite Beauty Whipped Cherry Lip Scrub"},
{id:"1680",text:"Burt's Bees Sensitive Eye Cream"},
{id:"1681",text:"Clarins Concealer Stick"},
{id:"1682",text:"Charlotte Tilbury Colour Chameleon Colour Morphing Eye Shadow Pencil"},
{id:"1683",text:"Burt's Bees Carrot Nutritive Day Creme"},
{id:"1684",text:"Bite Beauty Lush Lip Tint"},
{id:"1685",text:"Carol's Daughter Ecstasy Body and Bath Oil"},
{id:"1686",text:"Bobbi Brown SPF 15 Tinted Moisturizer Oil-Free"},
{id:"1687",text:"Cargo Liquid Liner"},
{id:"1688",text:"Bioderma Photoderm MAX Milk SPF 100"},
{id:"1689",text:"Clarins Self Tanning Milk SPF 6"},
{id:"1690",text:"Benefit Watts Up! Soft Focus Highlighter For Face"},
{id:"1691",text:"Bioderma Photoderm MAX Stick SPF 50+"},
{id:"1692",text:"Chantecaille Compact Makeup"},
{id:"1693",text:"Chantecaille Nano Gold Firming Treatment"},
{id:"1694",text:"Burt's Bees Natural Acne Solutions Targeted Spot Treatment"},
{id:"1695",text:"CeraVe Healing Ointment"},
{id:"1696",text:"Cetaphil RESTORADERM Eczema Calming Body Wash"},
{id:"1697",text:"Carol's Daughter Ocean Body Cleansing Gel"},
{id:"1698",text:"Biore Pore Penetrating Charcoal Bar"},
{id:"1699",text:"Charlotte Tilbury Magic Foundation Broad Spectrum SPF 15"},
{id:"1700",text:"Boscia Super-Cool De-Puffing Eye Balm"},
{id:"1701",text:"Chanel Ultra Correction Line Repair Intensive Anti-Wrinkle Concentrate"},
{id:"1702",text:"Bobbi Brown Skin Foundation Stick"},
{id:"1703",text:"Burt's Bees Honey Lip Balm"},
{id:"1704",text:"Boots True Identity Foundation"},
{id:"1705",text:"Caudalie Paris Divine Legs Tinted Body Lotion"},
{id:"1706",text:"Boots Botanics Colour Adjusting Foundation"},
{id:"1707",text:"Clarins ClarinsMen Line-Control Balm"},
{id:"1708",text:"Boscia Daily Defense Sunscreen Broad Spectrum SPF 50"},
{id:"1709",text:"Caudalie Paris Beauty Elixir, for All Skin Types"},
{id:"1710",text:"Bobbi Brown Long-Wear Gel Eyeliner"},
{id:"1711",text:"Chantecaille Water Flower Fluid"},
{id:"1712",text:"Cetaphil Moisturizing Cream"},
{id:"1713",text:"Benefit Dr. Feelgood"},
{id:"1714",text:"Clarins Super Restorative Wake-Up Lotion"},
{id:"1715",text:"Bobbi Brown SPF 50 Protective Face Base"},
{id:"1716",text:"Clarins HydraQuench Tinted Moisturizer SPF 6"},
{id:"1717",text:"Bobbi Brown Long-Wear Even Finish Compact Foundation"},
{id:"1718",text:"Clarins Skin Beauty Repair Concentrate for Sensitive Skin"},
{id:"1719",text:"Chantecaille HD Perfecting Powder"},
{id:"1720",text:"Chantecaille Nano Gold Energizing Eye Serum"},
{id:"1721",text:"Boscia Soothing Cleansing Cream"},
{id:"1722",text:"Clarins Bust Beauty Firming Lotion"},
{id:"1723",text:"Biore Blemish Treating Astringent"},
{id:"1724",text:"Burt's Bees Replenishing Lip Balm With Pomegranate Oil"},
{id:"1725",text:"Burt's Bees Brightening Even-Tone Moisturizing Cream"},
{id:"1726",text:"Boots Expert Dry Skin Balm"},
{id:"1727",text:"Bobbi Brown Moisture Rich Foundation SPF 15"},
{id:"1728",text:"Blue Lizard Blue Lizard Australian Sunscreen SPF 30+ Chemical Free Fragrance Free Sensitive"},
{id:"1729",text:"Boots Botanics Shine Away Ionic Clay Mask"},
{id:"1730",text:"Caudalie Paris Vinosource S.O.S. Thirst-Quenching Serum"},
{id:"1731",text:"Clarins ClarinsMen After Shave Energizer"},
{id:"1732",text:"Bioderma Sensibio DS Foaming Gel"},
{id:"1733",text:"Bobbi Brown Sheer Finish Pressed Powder"},
{id:"1734",text:"Boscia Clear Complexion Tonic with Botanical Blast"},
{id:"1735",text:"Chanel Inimitable Waterproof Mascara"},
{id:"1736",text:"Bioderma Sebium AI"},
{id:"1737",text:"Chantecaille Luminous Gloss"},
{id:"1738",text:"Burt's Bees Citrus Facial Scrub"},
{id:"1739",text:"Chantecaille Stress Repair Concentrate"},
{id:"1740",text:"Boscia No Pores No Shine T-Zone Treatment"},
{id:"1741",text:"Chanel Le Weekend De Chanel"},
{id:"1742",text:"Charlotte Tilbury K.I.S.S.I.N.G. Fallen From The Lipstick Tree"},
{id:"1743",text:"Bobbi Brown Hydrating Rich Cream Cleanser"},
{id:"1744",text:"Chanel Soleil Tan de Chanel Bronzing Makeup Base"},
{id:"1745",text:"Chanel Aqua Crayon Lip Colour Stick"},
{id:"1746",text:"Clarins Truly Matte Pore Minimizing Serum"},
{id:"1747",text:"Clarins Instant Light Brush-On Perfector"},
{id:"1748",text:"Burberry Beauty Sheer Eye Shadow"},
{id:"1749",text:"Caudalie Paris Pulpe Vitaminee Regenerating Concentrate"},
{id:"1750",text:"Chanel Le Crayon Levres Precision Lip Definer"},
{id:"1751",text:"Chanel Le Crayon Khol Intense Eye Pencil"},
{id:"1752",text:"Bobbi Brown Extra Soothing Balm"},
{id:"1753",text:"Clarins Hand and Nail Treatment Cream"},
{id:"1754",text:"Clarins One-Step Gentle Exfoliating Cleanser with Orange Extract"},
{id:"1755",text:"Cargo Reverse Lipliner"},
{id:"1756",text:"Chanel Ultra Correction Line Repair Anti-Wrinkle Eye Cream"},
{id:"1757",text:"Clarins Waterproof Eye Pencil"},
{id:"1758",text:"Benefit Dew the Hoola Soft Matte Liquid Bronzer for Face"},
{id:"1759",text:"Boots Soothing Eye Make Up Remover ALL BRIGHT"},
{id:"1760",text:"Burt's Bees Lip Crayon"},
{id:"1761",text:"Chanel Ultra Correction Line Repair Anti-Wrinkle Day Fluid SPF 15"},
{id:"1762",text:"Chantecaille Retinol Intense"},
{id:"1763",text:"Chanel Hydramax+Active Active Moisture Mask"},
{id:"1764",text:"Benefit High Brow"},
{id:"1765",text:"Clarins After Sun Replenishing Moisture Care for Face and Decollete"},
{id:"1766",text:"Boots Responsive Moisture Lotion"},
{id:"1767",text:"Boots Extreme Length Mascara"},
{id:"1768",text:"Benefit Velvet Eyeshadow"},
{id:"1769",text:"Charlotte Tilbury Full Fat Lashes 5 Star Mascara"},
{id:"1770",text:"Boots No. 7 Restore & Renew Day & Night Serum"},
{id:"1771",text:"Burberry Beauty Lip Glow"},
{id:"1772",text:"Clarins Super Restorative Total Eye Concentrate"},
{id:"1773",text:"Bioderma Atoderm Milk"},
{id:"1774",text:"Clarins Gentle Beauty Soap"},
{id:"1775",text:"Bite Beauty High Pigment Pencil"},
{id:"1776",text:"Boots Protect & Perfect Hand Cream SPF 15"},
{id:"1777",text:"Chanel Sublimage Essential Regenerating Cream (Texture Universelle)"},
{id:"1778",text:"Burt's Bees Orange Essence Facial Cleanser"},
{id:"1779",text:"Boots Protect & Perfect Day Cream SPF 15"},
{id:"1780",text:"Chantecaille Le Stylo"},
{id:"1781",text:"Bioderma Photoderm MAX Tinted Cream SPF 100"},
{id:"1782",text:"Clarins Vital Light Serum"},
{id:"1783",text:"Blue Lizard Baby Lizard Australian Sunscreen SPF 30+ Chemical Free Fragrance Free"},
{id:"1784",text:"Boscia Makeup-Breakup Cool Cleansing Oil"},
{id:"1785",text:"Bioderma Photoderm BRONZ Fluid SPF 50+"},
{id:"1786",text:"Boots Mineral Perfection Blush"},
{id:"1787",text:"Carol's Daughter Almond Cookie Shea Souffle"},
{id:"1788",text:"Burt's Bees Brightening Refining Tonic"},
{id:"1789",text:"Burt's Bees Radiance Serum with Royal Jelly"},
{id:"1790",text:"Clarins Extra-Firming Night Rejuvenating Cream, For All Skin Types"},
{id:"1791",text:"Clarins Gentle Foaming Cleanser for Combination or Oily Skin"},
{id:"1792",text:"Chantecaille Lip Gloss SPF 15"},
{id:"1793",text:"Bobbi Brown Creamy Concealer"},
{id:"1794",text:"Bite Beauty Honey Lip Lacquer"},
{id:"1795",text:"Boscia Bright White Mask"},
{id:"1796",text:"Clarisonic Clarisonic Cleansing Devices"},
{id:"1797",text:"Clarins Instant Light Natural Lip Perfector"},
{id:"1798",text:"Benefit Hervana"},
{id:"1799",text:"Benefit BADgal Lash Mascara"},
{id:"1800",text:"Bioderma Photoderm MAX Cream SPF 50+"},
{id:"1801",text:"Bobbi Brown Intensifying Long Wear Mascara"},
{id:"1802",text:"Boscia Restorative Night Treatment"},
{id:"1803",text:"Boots Colour Calming Makeup Primer"},
{id:"1804",text:"Caudalie Paris Divine Oil"},
{id:"1805",text:"Caudalie Paris Vinosource Nourishing Concentrate"},
{id:"1806",text:"Boots Botanics Lighter than Air Pressed Powder"},
{id:"1807",text:"Carol's Daughter Gelee de Soleil Browning Oil SPF 15"},
{id:"1808",text:"Boots Organic Face Smoothing Face Polish"},
{id:"1809",text:"Cargo HydraBronze"},
{id:"1810",text:"Benefit Bella Bamba"},
{id:"1811",text:"Benefit Hydra-Smooth Lip Color"},
{id:"1812",text:"Chantecaille Pure Rosewater"},
{id:"1813",text:"Benefit Eye Bright"},
{id:"1814",text:"Caudalie Paris Gentle Buffing Cream"},
{id:"1815",text:"Clarins BB Skin Perfecting Cream SPF 25"},
{id:"1816",text:"Bobbi Brown Face Mist"},
{id:"1817",text:"Charlotte Tilbury Matte Revolution"},
{id:"1818",text:"Cargo The Big Bronzer"},
{id:"1819",text:"CeraVe Moisturizing Lotion"},
{id:"1820",text:"Burt's Bees Refreshing Lip Balm with Pink Grapefruit"},
{id:"1821",text:"Cetaphil Gentle Skin Cleansing Cloths"},
{id:"1822",text:"Cargo Blush"},
{id:"1823",text:"Boscia Balancing Facial Tonic"},
{id:"1824",text:"Caudalie Paris Vinosource Moisturizing Sorbet"},
{id:"1825",text:"Boscia Luminizing Black Mask"},
{id:"1826",text:"Bioderma Photoderm BRONZ Spray SPF 15"},
{id:"1827",text:"Clarins UV PLUS Anti-Pollution Broad Spectrum SPF 50 Sunscreen Multi-Protection Tint"},
{id:"1828",text:"Bobbi Brown Long-Wear Cream Shadow"},
{id:"1829",text:"Boots Expert Sensitive Gentle Cleansing Lotion"},
{id:"1830",text:"Cargo TexasLash Mascara"},
{id:"1831",text:"Bioderma Atoderm PO Zinc"},
{id:"1832",text:"Burt's Bees Radiance Day Creme"},
{id:"1833",text:"Carol's Daughter Love Butter"},
{id:"1834",text:"Burt's Bees Intense Hydration Treatment Mask with Clary Sage"},
{id:"1835",text:"Clarins The Brush"},
{id:"1836",text:"Clarins Colour Quartet for Eyes"},
{id:"1837",text:"Cetaphil Dermacontrol Oil Control Moisturizer SPF 30"},
{id:"1838",text:"Boots Expert Sensitive Gentle Smoothing Scrub"},
{id:"1839",text:"Clarins Skin-Smoothing Eye Mask"},
{id:"1840",text:"Clarins Toning Lotion with Camomile, for Dry or Normal Skin"},
{id:"1841",text:"Clarins ClarinsMen Super Moisture Gel"},
{id:"1842",text:"Bobbi Brown Hydrating Face Tonic"},
{id:"1843",text:"Clarins ClarinsMen Shave Ease Oil"},
{id:"1844",text:"Charlotte Tilbury The Classic Eyeliner Powder Pencil"},
{id:"1845",text:"CeraVe Sunscreen for Face SPF 50"},
{id:"1846",text:"Clarins ClarinsMen Smooth Shave"},
{id:"1847",text:"Bobbi Brown Extra Illuminating Moisture Balm"},
{id:"1848",text:"Chantecaille Biodynamic Lifting Mask"},
{id:"1849",text:"Clarins Skin Illusion SPF 10 Natural Radiance Foundation"},
{id:"1850",text:"Clarins Sun Control Stick Ultra Protection SPF 30, for Sun-Sensitive Areas"},
{id:"1851",text:"Clarisonic Deep Pore Daily Cleanser"},
{id:"1852",text:"Chanel Sublimage Eye Essential Regenerating Eye Cream"},
{id:"1853",text:"Bobbi Brown Soothing Cleansing Oil"},
{id:"1854",text:"Chantecaille Ultra Sun Protection SPF 50 PA+++ Anti-Glycation Primer"},
{id:"1855",text:"Boscia Purifying Cleansing Gel"},
{id:"1856",text:"Bioderma Photoderm AR SPF 50+"},
{id:"1857",text:"Bioderma Photoderm MAX Tinted Cream SPF 50"},
{id:"1858",text:"Burberry Beauty Lip Cover"},
{id:"1859",text:"Chanel Ultra Correction Line Repair Anti-Wrinkle Night Cream"},
{id:"1860",text:"Biore Warming Anti-Blackhead Cream Cleanser"},
{id:"1861",text:"Biore Pore Unclogging Scrub"},
{id:"1862",text:"Caudalie Paris Vinexpert Broad Spectrum SPF 15 Radiance Day Fluid"},
{id:"1863",text:"Blue Lizard Blue Lizard Australian Sunscreen Daily Moisturizer Face SPF 30+"},
{id:"1864",text:"Burt's Bees Sensitive Facial Cleanser"},
{id:"1865",text:"Clarins Gentle Night Cream"},
{id:"1866",text:"Chanel Le Lift Firming Anti-Wrinkle Crme"},
{id:"1867",text:"Boots Nourishing Night Cream"},
{id:"1868",text:"Burt's Bees Lipstick"},
{id:"1869",text:"Chantecaille Cheek Shade"},
{id:"1870",text:"Burberry Beauty Velvet Foundation"},
{id:"1871",text:"Chanel Purete Ideale Serum Intense Refining Skin Complex"},
{id:"1872",text:"Charlotte Tilbury Wonderglow Instant Soft Focus Beauty Flash"},
{id:"1873",text:"Boots Micellar Cleansing Solution 3 in 1 All Bright"},
{id:"1874",text:"Boots Intensive Moisture Face Mask"},
{id:"1875",text:"Boscia Green Tea Blotting Linens"},
{id:"1876",text:"Benefit Puff Off!"},
{id:"1877",text:"Chanel UV Essentiel Multi Protection Daily UV Care SPF 50"},
{id:"1878",text:"Clarins Bright Plus HP Gentle Brightening Exfoliator"},
{id:"1879",text:"Boots No 7. Fanomenal Lashes Mascara"},
{id:"1880",text:"Chantecaille New Stick Foundation"},
{id:"1881",text:"Benefit That Gal Brightening Face Primer"},
{id:"1882",text:"Caudalie Paris Merlot Friction Scrub"},
{id:"1883",text:"Carol's Daughter Acai Hydrating Face Butter"},
{id:"1884",text:"Clarins Delicious Self Tanning Cream"},
{id:"1885",text:"Clarins Multi-Active Day Early Wrinkle Correcting Lotion SPF 15"},
{id:"1886",text:"Bliss Clog Dissolving Cleansing Milk"},
{id:"1887",text:"Bioderma Photoderm MAX Fluid SPF 100"},
{id:"1888",text:"Burt's Bees Intense Hydration Day Lotion with Clary Sage"},
{id:"1889",text:"Chanel Perfection Lumiere Velvet Smooth Effect Makeup SPF 15"},
{id:"1890",text:"Bioderma Photoderm BRONZ Spray SPF 30"},
{id:"1891",text:"Clarins Truly Matte Hydra-Matte Lotion, for Combination Skin"},
{id:"1892",text:"Bobbi Brown Sheer Finish Loose Powder"},
{id:"1893",text:"Benefit Refined Finish Facial Polish"},
{id:"1894",text:"Chanel Soleil Tan de Chanel Moisturizing Bronzing Powder"},
{id:"1895",text:"Chanel Double Perfection Lumiere Long-Wear Flawless Sunscreen Powder Makeup Broad Spectrum SPF 15"},
{id:"1896",text:"Bobbi Brown Illuminating Bronzing Powder"},
{id:"1897",text:"Cargo Be-you-tiful Water Resistant Bronzer"},
{id:"1898",text:"CeraVe Foaming Facial Cleanser"},
{id:"1899",text:"Boots Protect & Perfect Eye Cream"},
{id:"1900",text:"Bobbi Brown Crystal Lip Gloss"},
{id:"1901",text:"Chanel Vitalumiere Aqua Fresh and Hydrating Cream Compact Makeup SPF 15"},
{id:"1902",text:"Chanel Crayon Sourcils Sculpting Eyebrow Pencil"},
{id:"1903",text:"Clarins UV Plus HP Day Screen High Protection SPF 40"},
{id:"1904",text:"Clarins Super Restorative Redefining Body Care for Abdomen and Waist"},
{id:"1905",text:"Carol's Daughter Almond Cookie Body and Bath Oil"},
{id:"1906",text:"Benefit Lollitint"},
{id:"1907",text:"Chanel Les Beiges Healthy Glow Sheer Powder SPF 15"},
{id:"1908",text:"Benefit Dallas"},
{id:"1909",text:"Clarins Gentle Refiner Exfoliating Cream with Microbeads"},
{id:"1910",text:"Charlotte Tilbury Luxury Palette Colour-Coded Eyeshadow"},
{id:"1911",text:"Burt's Bees Naturally Ageless Skin Hydrating Night Creme"},
{id:"1912",text:"Bobbi Brown Lathering Tube Soap"},
{id:"1913",text:"CeraVe Facial Moisturizing Lotion PM"},
{id:"1914",text:"Bobbi Brown Skin Foundation Mineral Makeup SPF 15"},
{id:"1915",text:"Chanel Le Crayon Yeux Precision Eye Definer"},
{id:"1916",text:"Boscia All-in-One B.B. Eye Brightener Broad Spectrum SPF 20"},
{id:"1917",text:"CeraVe Skin Renewing Cream Serum"},
{id:"1918",text:"Bliss A Tan For All Seasons"},
{id:"1919",text:"Chantecaille Jasmine and Lily Healing Mask"},
{id:"1920",text:"Chantecaille Fruit and Flower Acids Mask"},
{id:"1921",text:"Burt's Bees Sensitive Night Cream"},
{id:"1922",text:"Boscia Self-Defense Vital Antioxidant Moisture SPF 30"},
{id:"1923",text:"Clarins Wonder Perfect Mascara"},
{id:"1924",text:"Boots Expert Anti-Blemish Cleansing Foam"},
{id:"1925",text:"Boots Expert Sensitive Gentle Eye Makeup Removal Lotion"},
{id:"1926",text:"Carol's Daughter Ecstasy Body Cleansing Gel"},
{id:"1927",text:"Chanel Perfection Lumiere Long-Wear Flawless Fluid Makeup SPF 15"},
{id:"1928",text:"Bioderma Sebium Mat Anti-Shine Fluid"},
{id:"1929",text:"Bobbi Brown Extra Repair Foundation SPF 25"},
{id:"1930",text:"Boots Purifying Face Scrub ALL BRIGHT"},
{id:"1931",text:"Boots No7 Photo Fix Wrinkle Filler and Primer"},
{id:"1932",text:"Chanel Poudre Universelle Libre Natural Finish Loose Powder"},
{id:"1933",text:"Benefit Foamingly Clean Facial Wash"},
{id:"1934",text:"Bioderma Atoderm PP Anti-Recurrence Emollient Balm"},
{id:"1935",text:"Bioderma Photoderm MAX Fluid SPF 50"},
{id:"1936",text:"Bite Beauty Lush Fruit Lip Gloss"},
{id:"1937",text:"Boots Maximum Volume Mascara"},
{id:"1938",text:"Burt's Bees Beeswax Lip Balm Tin"},
{id:"1939",text:"Boscia Revitalizing Black Hydration Gel"},
{id:"1940",text:"Chanel Inimitable Mascara Multi-Dimensionnel"},
{id:"1941",text:"Chanel Base Lumiere Illuminating Makeup Base"},
{id:"1942",text:"Carol's Daughter Acai Clarifying Foam Cleanser"},
{id:"1943",text:"Clarins Multi-Active Skin Renewal Serum"},
{id:"1944",text:"Benefit Bluff Dust"},
{id:"1945",text:"Bobbi Brown Corrector"},
{id:"1946",text:"Chantecaille Luminous Eye Liner"},
{id:"1947",text:"Bobbi Brown Skin Smoothing Pore Perfector"},
{id:"1948",text:"Chanel Ultra Correction Lift Express Lifting Firming Mask"},
{id:"1949",text:"Burt's Bees Baby Bee SPF 30 Sunscreen Stick"},
{id:"1950",text:"Boots Intensive Moisture Mask"},
{id:"1951",text:"Clarins Cleansing Milk with Alpine Herbs, for Dry or Normal Skin"},
{id:"1952",text:"Bioderma Hydrabio Light Cream"},
{id:"1953",text:"Bliss Triple Oxygen Instant Energizing Mask"},
{id:"1954",text:"Bobbi Brown Luminous Moisturizing Treatment Foundation"},
{id:"1955",text:"Bobbi Brown Everything Mascara"},
{id:"1956",text:"Benefit Moon Beam"},
{id:"1957",text:"Bioderma Sebium Foaming Gel"},
{id:"1958",text:"Cargo Essential Eyeshadow Palette"},
{id:"1959",text:"Carol's Daughter Ecstasy Shea Souffle"},
{id:"1960",text:"Clarins Super Restorative Night Cream"},
{id:"1961",text:"CeraVe Diaper Rash Cream"},
{id:"1962",text:"Bobbi Brown Lip Color"},
{id:"1963",text:"Boots Expert Sensitive Restoring Night Treatment"},
{id:"1964",text:"Boots Skin Brightening Deep Clean Gel"},
{id:"1965",text:"Bite Beauty Cinnamon Plumping Lip Oil"},
{id:"1966",text:"Clarins Fix' Make-Up"},
{id:"1967",text:"Clarins ClarinsMen Line-Control Eye Balm"},
{id:"1968",text:"Bioderma White Objective Active Cream"},
{id:"1969",text:"Cargo ColorStick"},
{id:"1970",text:"Boots No. 7 Exquisite Curl Mascara"},
{id:"1971",text:"Cetaphil Men Daily Face Lotion with Broad Spectrum SPF 15"},
{id:"1972",text:"Bioderma Photoderm SPOT SPF 50+"},
{id:"1973",text:"Benefit Its Potent! Eye Cream"},
{id:"1974",text:"Boots No. 7 Lash Adapt Mascara"},
{id:"1975",text:"Benefit Ultra Radiance Facial Re-Hydrating Mist"},
{id:"1976",text:"Chanel Ombre D-Eau Fluid Iridescent Eyeshadow"},
{id:"1977",text:"Clarins Everlasting Compact Foundation SPF 15"},
{id:"1978",text:"Bliss Steep Clean, Professional-Strength Facial Mask, for All Skin Types"},
{id:"1979",text:"brand product"},
{id:"1980",text:"Kiehl's Midnight Recovery Eye Cream"},
{id:"1981",text:"KORA Organics Luxurious Rosehip Oil Revitalise and Smooth"},
{id:"1982",text:"Kate Somerville RetAsphere 2-in-1 Retinol Night Cream"},
{id:"1983",text:"Hada Labo Tokyo Ultimate Anti-Aging Facial Mask"},
{id:"1984",text:"L'Oreal Paris Revitalift Triple Power Intensive Overnight Mask"},
{id:"1985",text:"L'Oreal Paris Voluminous Original Volume Building Mascara"},
{id:"1986",text:"Jergens Natural Glow Foaming Moisturizer, Fair to Medium and Medium to Tan"},
{id:"1987",text:"L'Oreal Paris The One Sweep Sculpting Blush Duo"},
{id:"1988",text:"L'Oreal Paris Revitalift Double Eye Lift"},
{id:"1989",text:"Indeed Laboratories Retinol Reface"},
{id:"1990",text:"L'Oreal Paris True Match Naturale Gentle Mineral Concealer SPF 25"},
{id:"1991",text:"L'Oreal Paris Revitalift Complete SPF 30 Day Lotion with Photo-Aging Complex"},
{id:"1992",text:"iS Clinical Active Serum"},
{id:"1993",text:"L'Oreal Paris Youth Code Day/Night Cream Moisturizer"},
{id:"1994",text:"La Roche-Posay Effaclar MAT Daily Moisturizer for Oily Skin"},
{id:"1995",text:"La Roche-Posay Anthelios AOX Daily Antioxidant Serum Broad Spectrum SPF 50"},
{id:"1996",text:"Kate Somerville DermalQuench Liquid Lift Advanced Wrinkle Treatment"},
{id:"1997",text:"Kat Von D Everlasting Liquid Lipstick"},
{id:"1998",text:"L'Oreal Paris Sublime Bronze Tinted Self Tanning Golden Mousse"},
{id:"1999",text:"Kiehl's Facial Fuel Energizing Face Wash"},
{id:"2000",text:"Kiehl's Calendula Herbal-Extract Toner, Alcohol-Free, for a Normal to Oily Skin Type"},
{id:"2001",text:"Kiss My Face Olive & Chamomile Soap"},
{id:"2002",text:"L'Occitane Immortelle Precious Night Cream"},
{id:"2003",text:"iS Clinical Body Complex"},
{id:"2004",text:"Jergens Natural Glow & Protect Daily Moisturizer SPF 20 for Fair to Medium Skin Tones"},
{id:"2005",text:"La Roche-Posay Active C Corrective Dermatological Care for Wrinkles"},
{id:"2006",text:"L'Oreal Paris Double Extend Eye Illuminator Eyeliner"},
{id:"2007",text:"Kate Somerville Clinic-To-Go Resurfacing Peel Pads"},
{id:"2008",text:"Kate Somerville Kateceuticals Multi-Active Repair Lifting and Lineless Eye Cream"},
{id:"2009",text:"Kiehl's Rosa Arctica Lightweight Cream"},
{id:"2010",text:"La Prairie Essential Exfoliator"},
{id:"2011",text:"Jane Iredale Quad Bronzer"},
{id:"2012",text:"Jason Natural Moisturizing Herbs Pure Natural Hand Soap"},
{id:"2013",text:"L'Oreal Paris Studio Secrets Professional Eye Shadow Duos"},
{id:"2014",text:"L'Oreal Paris Advanced Suncare Silky Sheer Face Lotion SPF 30"},
{id:"2015",text:"Jane Iredale PurePressed Blush"},
{id:"2016",text:"L'Oreal Paris Infallible Never Fail The Super Slim 12H Liquid Eyeliner"},
{id:"2017",text:"Jan Marini Skin Research, Inc. Benzoyl Peroxide 5%"},
{id:"2018",text:"Jane Iredale Magic Mitt"},
{id:"2019",text:"IT Cosmetics CC+ Lip Serum Hydrating Anti-Aging Color Correcting Creme Gloss"},
{id:"2020",text:"L'Oreal Paris True Match Super-Blendable Makeup SPF 17 Sunscreen"},
{id:"2021",text:"Hydroxatone Instant Wrinkle Filler"},
{id:"2022",text:"Korres Wild Rose Advanced Brightening Sleeping Facial"},
{id:"2023",text:"Kate Somerville CytoCell Dermal Energizing Treatment"},
{id:"2024",text:"La Prairie Anti-Aging Emulsion SPF 30"},
{id:"2025",text:"Guerlain Orchidee Imperiale Toner"},
{id:"2026",text:"La Roche-Posay Effaclar Serum"},
{id:"2027",text:"La Prairie Cellular Radiance Emulsion SPF 30"},
{id:"2028",text:"L'Oreal Paris Advanced Suncare Quick Dry Sheer Finish Spray SPF 50"},
{id:"2029",text:"L'Oreal Paris Sublime Sun Advanced Sunscreen Crystal Clear Mist, SPF 50"},
{id:"2030",text:"L'Oreal Paris HiP Studio Secrets Professional Shocking Shadow Pigments"},
{id:"2031",text:"La Mer The Cleansing Gel"},
{id:"2032",text:"La Prairie White Caviar Illuminating Eye Serum"},
{id:"2033",text:"Jan Marini Skin Research, Inc. Recover-E"},
{id:"2034",text:"La Mer The Powder"},
{id:"2035",text:"Jane Iredale Brush-Me Bronze"},
{id:"2036",text:"La Roche-Posay Toleriane Dermo-Cleanser"},
{id:"2037",text:"IT Cosmetics Vitality Lip Flush Butter"},
{id:"2038",text:"L'Oreal Paris Colour Riche Anti-Aging Serum Lipstick"},
{id:"2039",text:"L'Oreal Paris Magic Smooth Souffle Makeup"},
{id:"2040",text:"Kiehl's Ultra Facial Toner, For All Skin Types"},
{id:"2041",text:"Kiss My Face Cell Mate Facial Creme and Sunscreen SPF 15 Normal to Oily Skin"},
{id:"2042",text:"La Roche-Posay Effaclar K Acne Treatment Fluid"},
{id:"2043",text:"L'Oreal Paris Sublime Bronze Self-Tanning Towelettes, for Face, Light-Medium"},
{id:"2044",text:"L'Oreal Paris Collagen Moisture Filler Day Lotion SPF 15"},
{id:"2045",text:"Hourglass Cosmetics Extreme Sheen High Shine Lip Gloss"},
{id:"2046",text:"Jan Marini Skin Research, Inc. Age Intervention Face Cream"},
{id:"2047",text:"La Prairie Cellular Power Infusion"},
{id:"2048",text:"L'Oreal Paris Revitalift Complete Anti-Wrinkle and Firming Moisturizer SPF 30 Day Lotion"},
{id:"2049",text:"Kiss My Face Kids Natural Mineral Sunscreen Broad Spectrum SPF 30"},
{id:"2050",text:"Kate Somerville 24 Hour Pimple Punisher"},
{id:"2051",text:"La Roche-Posay Redermic + Eyes Intensive Daily Anti-Wrinkle/Firming Fill-In Care"},
{id:"2052",text:"Jane Iredale Pommisst"},
{id:"2053",text:"L'Oreal Paris True Match Mineral Gentle Mineral Powder"},
{id:"2054",text:"IT Cosmetics Bye Bye Pores Airbrush Illuminizer"},
{id:"2055",text:"La Roche-Posay Ceralip Lip Repair Cream"},
{id:"2056",text:"L'Oreal Paris Age Perfect Hydra-Nutrition Glow Renewal Facial Oil"},
{id:"2057",text:"Kate Somerville Neck Tissue Repair Cream with Peptide K8"},
{id:"2058",text:"KORA Organics Cream Cleanser Replenish and Rejuvenate, for Normal, Dry or Sensitive Skin"},
{id:"2059",text:"KORA Organics Exfoliating Cream Purify and Renew for Face and Body"},
{id:"2060",text:"Kate Somerville True Lash Lash Enhancing Eye Makeup Remover"},
{id:"2061",text:"Jan Marini Skin Research, Inc. Regeneration Booster"},
{id:"2062",text:"Jane Iredale Zap&Hide Blemish Concealer"},
{id:"2063",text:"Kate Somerville Total Vitamin Antioxidant Face Serum"},
{id:"2064",text:"Jason Natural Revitalizing Vitamin E 5,000 I.U. Pure Natural Moisturizing Creme"},
{id:"2065",text:"Korres Wild Rose Face and Eye Serum"},
{id:"2066",text:"Jergens Natural Glow Healthy Complexion Daily Facial Moisturizer SPF 20 Fair to Medium and Medium to Tan"},
{id:"2067",text:"Kiehl's Facial Fuel"},
{id:"2068",text:"IT Cosmetics Celebration Foundation"},
{id:"2069",text:"Jason Natural Soothing 98% Aloe Vera Pure Natural Moisturizing Gel"},
{id:"2070",text:"Hourglass Cosmetics Veil Fluid Makeup Oil Free SPF 15"},
{id:"2071",text:"La Prairie Anti-Aging Foundation SPF 15"},
{id:"2072",text:"Kiehl's Acne Blemish Control Daily Skin-Clearing Treatment"},
{id:"2073",text:"L'Oreal Paris go 360 Clean Deep Exfoliating Scrub"},
{id:"2074",text:"Hawaiian Tropic Sheer Touch Lotion Sunscreen Broad Spectrum SPF 50"},
{id:"2075",text:"L'Oreal Paris Visible Lift Serum Absolute Advanced Age-Reversing Makeup SPF 17"},
{id:"2076",text:"Jergens Daily Moisture Fragrance-Free Dry Skin Moisturizer"},
{id:"2077",text:"L'Oreal Paris Lineur Intense Felt Tip Liquid Eyeliner"},
{id:"2078",text:"Kevyn Aucoin The Matte Lip Color"},
{id:"2079",text:"Guerlain Blanc de Perle Cleansing Foam"},
{id:"2080",text:"Kiss My Face Oat Protein Complex Sun Screen SPF 18"},
{id:"2081",text:"Hourglass Cosmetics No. 28 Lip Treatment Oil"},
{id:"2082",text:"Kiehl's Clearly Corrective Dark Circle Perfector Broad Spectrum SPF 30"},
{id:"2083",text:"Kiehl's Ultra Facial Cream Intense Hydration"},
{id:"2084",text:"L'Occitane Shea Butter Ultra Rich Face Soap"},
{id:"2085",text:"Hourglass Cosmetics Hidden Corrective Concealer"},
{id:"2086",text:"La Prairie Skin Caviar Luxe Eye Lift Cream"},
{id:"2087",text:"IT Cosmetics Hello Light Anti-Aging Creme Illuminator"},
{id:"2088",text:"Guerlain Secret de Purete Cleansing Creme"},
{id:"2089",text:"Jason Natural Soothing 84% Aloe Vera Pure Natural Moisturizing Creme"},
{id:"2090",text:"Kiehl's Ultra Facial Oil-Free Gel Cream"},
{id:"2091",text:"Jason Natural Family Natural Sunscreen Broad Spectrum SPF 45"},
{id:"2092",text:"Josie Maran Cosmetics Argan Lip Treatment"},
{id:"2093",text:"IT Cosmetics Vitality Lip Flush"},
{id:"2094",text:"La Roche-Posay Derm AOX Intense Anti-Wrinkle Radiance Serum"},
{id:"2095",text:"La Roche-Posay Lipikar Lipid Replenishing Body Milk"},
{id:"2096",text:"Kiehl's Midnight Recovery Concentrate"},
{id:"2097",text:"Kate Somerville Goat Milk Body Lotion"},
{id:"2098",text:"Hawaiian Tropic Silk Hydration Lotion Sunscreen Broad Spectrum SPF 30"},
{id:"2099",text:"Josie Maran Cosmetics 100% Pure Argan Oil Organic Moisturizing Oil"},
{id:"2100",text:"Kate Somerville Nourish Daily Moisturizer"},
{id:"2101",text:"La Mer The Radiant Infusion"},
{id:"2102",text:"L'Oreal Paris True Match Concealer"},
{id:"2103",text:"L'Oreal Paris Voluminous Miss Manga Mascara"},
{id:"2104",text:"La Prairie Cellular Treatment Mascara Instant Build"},
{id:"2105",text:"L'Oreal Paris Hydra-Renewal Continuous Moisture Cream, for Dry/Sensitive Skin"},
{id:"2106",text:"Kiehl's Panthenol Protein Moisturizing Face Cream, for Normal to Dry and Dry Skin Types"},
{id:"2107",text:"L'Oreal Paris Age Perfect Cell Renewal Golden Serum Treatment"},
{id:"2108",text:"Jan Marini Skin Research, Inc. Transformation Face Cream"},
{id:"2109",text:"La Mer The Hydrating Infusion"},
{id:"2110",text:"La Prairie Purifying Cream Cleanser"},
{id:"2111",text:"Jan Marini Skin Research, Inc. Transformation Eye Cream"},
{id:"2112",text:"Kat Von D Tattoo Brow"},
{id:"2113",text:"Jergens Shea Butter Deep Conditioning Moisturizer"},
{id:"2114",text:"Hawaiian Tropic Silk Hydration Lotion Sunscreen Broad Spectrum SPF 12"},
{id:"2115",text:"La Roche-Posay Effaclar BB Blur Instant Oil-Absorbing Coverage Cream Mousse"},
{id:"2116",text:"Kiehl's Rare Earth Pore Refining Tonic"},
{id:"2117",text:"La Roche-Posay Effaclar Duo Dual Action Acne Treatment"},
{id:"2118",text:"L'Oreal Paris Double Extend Eye Illuminator"},
{id:"2119",text:"Hydroxatone Instant Effect"},
{id:"2120",text:"La Roche-Posay Thermal Spring Water"},
{id:"2121",text:"Guerlain Abeille Royale Day Cream-Normal to Dry Skin"},
{id:"2122",text:"L'Oreal Paris Double Extend Lash Fortifier & Extender Mascara"},
{id:"2123",text:"Jane Iredale Disappear"},
{id:"2124",text:"Jergens Natural Glow Firming Daily Moisturizer Fair to Medium"},
{id:"2125",text:"KORA Organics Soothing Day and Night Cream Nurture and Protect for Sensitive/Normal Skin"},
{id:"2126",text:"L'Oreal Paris Voluminous Million Lashes Defining Volume Mascara"},
{id:"2127",text:"iS Clinical C-15 Serum Advance +"},
{id:"2128",text:"Hourglass Cosmetics Opaque Rouge Liquid Lipstick"},
{id:"2129",text:"L'Occitane Immortelle Brightening Cleansing Foam"},
{id:"2130",text:"Jan Marini Skin Research, Inc. Age Intervention Gentle Cleanser"},
{id:"2131",text:"L'Oreal Paris Visible Lift Blur Blush"},
{id:"2132",text:"Jason Natural Vitamin E 14,000 I.U. Pure Natural Skin Oil"},
{id:"2133",text:"Jason Natural C-Effects Day Lotion SPF 30"},
{id:"2134",text:"L'Oreal Paris Voluminous Butterfly Mascara"},
{id:"2135",text:"Jane Iredale Powder-Me SPF Dry Sunscreen SPF 30"},
{id:"2136",text:"Kate Somerville Dilo Oil Restorative Treatment"},
{id:"2137",text:"Kiss My Face Vanilla Honey Organic Lip Balm SPF 15"},
{id:"2138",text:"La Roche-Posay Rosaliac Skin Perfecting Anti-Redness Moisturizer"},
{id:"2139",text:"Kate Somerville Daily Deflector Waterlight Broad Spectrum 50+ PA+++ Anti-Aging Sunscreen"},
{id:"2140",text:"Guerlain Les Voilettes Loose Powder for the Face"},
{id:"2141",text:"La Roche-Posay Effaclar Clarifying Solution"},
{id:"2142",text:"L'Oreal Paris HiP Studio Secrets Professional Color Chrome Eyeliner"},
{id:"2143",text:"L'Oreal Paris True Match Super-Blendable Compact Makeup SPF 17 Sunscreen, Oil-Free"},
{id:"2144",text:"La Prairie Cellular Hydrating Serum"},
{id:"2145",text:"KORA Organics Blemish Gel for Oily/Combination Skin"},
{id:"2146",text:"Kiss My Face Strawberry Organic Lip Balm SPF 15"},
{id:"2147",text:"L'Oreal Paris Revitalift Deep-Set Wrinkle Repair SPF 15 Day Lotion"},
{id:"2148",text:"L'Oreal Paris Infallible Le Gloss 8HR"},
{id:"2149",text:"Jergens Daily Moisture Dry Skin Moisturizer"},
{id:"2150",text:"iS Clinical Super Serum Advance +"},
{id:"2151",text:"L'Oreal Paris Colour Riche Mono"},
{id:"2152",text:"Korres Wild Rose 24-Hour Moisturising & Brightening Cream"},
{id:"2153",text:"Kiehl's Intensive Treatment and Moisturizer for Dry or Callused Areas"},
{id:"2154",text:"iS Clinical Youth Eye Complex"},
{id:"2155",text:"Kiehl's Rosa Artica Eye Youth Regenerating Eye Balm"},
{id:"2156",text:"L'Oreal Paris Pencil Perfect Self-Advancing Eyeliner"},
{id:"2157",text:"L'Oreal Paris Infallible Never Fail 16HR Eyeliner"},
{id:"2158",text:"L'Oreal Paris Voluminous False Fiber Lashes Waterproof"},
{id:"2159",text:"Hada Labo Tokyo Anti-Aging Hydrator"},
{id:"2160",text:"Kiehl's Precision Lifting & Pore-Tightening Concentrate"},
{id:"2161",text:"Josie Maran Cosmetics Pure Argan Milk Intensive Hydrating Treatment"},
{id:"2162",text:"Jergens Natural Glow 3 Days to Glow Moisturizer Medium to Tan Skin Tones"},
{id:"2163",text:"L'Oreal Paris Voluminous Superstar Red Carpet Black Mascara"},
{id:"2164",text:"L'Oreal Paris Voluminous Butterfly Intenza Mascara"},
{id:"2165",text:"L'Oreal Paris True Match Lumi Powder Glow Illuminator"},
{id:"2166",text:"L'Oreal Paris Colour Caresse by Colour Riche"},
{id:"2167",text:"L'Oreal Paris Voluminous Million Lashes Excess"},
{id:"2168",text:"La Roche-Posay Rosaliac CC Cream Daily Complete Tone-Correcting Cream Broad Spectrum SPF 30"},
{id:"2169",text:"L'Oreal Paris Voluminous Superstar Mascara"},
{id:"2170",text:"Kate Somerville Serum Sunscreen Broad Spectrum SPF 50+"},
{id:"2171",text:"Kevyn Aucoin The Sensual Skin Enhancer"},
{id:"2172",text:"Kiehl's Ultra Facial Overnight Hydrating Masque"},
{id:"2173",text:"iS Clinical Moisturizing Complex"},
{id:"2174",text:"Jergens Ultra Healing Extra Dry Skin Moisturizer"},
{id:"2175",text:"La Prairie Skin Caviar Luxe Sleep Mask"},
{id:"2176",text:"L'Oreal Paris Voluminous False Fiber Lashes"},
{id:"2177",text:"Hawaiian Tropic Shimmer Effect Lotion Sunscreen with Mica Minerals Broad Spectrum SPF 40"},
{id:"2178",text:"Jergens Skin Renewing Daily Moisturizer"},
{id:"2179",text:"Jane Iredale Longest Lash Thickening and Lengthening Mascara"},
{id:"2180",text:"Kate Somerville Deep Tissue Repair Cream With Peptide K8"},
{id:"2181",text:"Kiehl's Sun-Free Self-Tanning Formula"},
{id:"2182",text:"Korres Greek Yoghurt Advanced Nourishing Sleeping Facial"},
{id:"2183",text:"Kiehl's Superbly Restorative Argan Body Lotion"},
{id:"2184",text:"Jason Natural Shaving Lotion"},
{id:"2185",text:"L'Oreal Paris HiP Studio Secrets Professional Color Presso"},
{id:"2186",text:"L'Oreal Paris Voluminous Waterproof Mascara"},
{id:"2187",text:"Kat Von D Lock-it Tattoo Foundation"},
{id:"2188",text:"Jane Iredale PureMatte Finish Powder"},
{id:"2189",text:"L'Oreal Paris HydraFresh Toner"},
{id:"2190",text:"La Roche-Posay Lipikar Surgras Lipid-Enriched Cleansing Bar, for Severely Dry Skin"},
{id:"2191",text:"L'Occitane Immortelle Brightening Water"},
{id:"2192",text:"L'Oreal Paris Sublime Sun Advanced Sunscreen Oil Spray SPF 50+"},
{id:"2193",text:"L'Oreal Paris Infallible 24 HR Eye Shadow"},
{id:"2194",text:"Kate Somerville CytoCell P299 Oil Free Anti-Wrinkle Serum"},
{id:"2195",text:"L'Oreal Paris Visible Lift CC Eye Concealer"},
{id:"2196",text:"La Prairie Age Management Balancer"},
{id:"2197",text:"Kiehl's Line-Reducing Eye-Brightening Concentrate"},
{id:"2198",text:"L'Oreal Paris Colour Riche Lip Gloss"},
{id:"2199",text:"Jane Iredale PureMoist LipColours SPF 18"},
{id:"2200",text:"La Mer The Oil Absorbing Lotion"},
{id:"2201",text:"IT Cosmetics Hello Light Powder Illuminizer"},
{id:"2202",text:"La Roche-Posay Anthelios 50 Mineral Ultra Light Sunscreen Fluid, SPF 50 Face"},
{id:"2203",text:"Guerlain Meteorites Illuminating Perfecting Pressed Powder"},
{id:"2204",text:"Kate Somerville Detox Daily Cleanser"},
{id:"2205",text:"L'Oreal Paris True Match Crayon Concealer"},
{id:"2206",text:"Jason Natural Balancing Wild Yam Pure Natural Moisturizing Creme"},
{id:"2207",text:"Kiss My Face Start Up Exfoliating Face Wash"},
{id:"2208",text:"L'Oreal Paris True Match Naturale Mineral-Enriched Mascara"},
{id:"2209",text:"Hada Labo Tokyo Skin Plumping Gel Cream"},
{id:"2210",text:"Kate Somerville Age Arrest Hydrating Firming Mask"},
{id:"2211",text:"Kate Somerville Daily Deflector Moisturizer Broad Spectrum SPF 20 Anti-Aging Sunscreen"},
{id:"2212",text:"Josie Maran Cosmetics Argan Daily Moisturizer SPF 47 Protect + Perfect Tinted Warm Glow"},
{id:"2213",text:"Hawaiian Tropic Tanning Creme Lotion SPF 2"},
{id:"2214",text:"Jane Iredale Liquid Minerals"},
{id:"2215",text:"Jan Marini Skin Research, Inc. Marini Physical Protectant SPF 45"},
{id:"2216",text:"L'Oreal Paris Age Perfect Hydra-Nutrition Golden Balm Face, Neck & Chest"},
{id:"2217",text:"L'Oreal Paris Youth Code  Day Lotion SPF 30"},
{id:"2218",text:"La Roche-Posay Redermic [C] Anti-Wrinkle Firming Moisturizing Filler for Normal to Combination Skin"},
{id:"2219",text:"Kiehl's Crme DElegance Repairateur, Superb Tissue Repairateur Creme"},
{id:"2220",text:"Jan Marini Skin Research, Inc. Daily Body Protectant SPF 34"},
{id:"2221",text:"La Mer The Reparative Body Lotion"},
{id:"2222",text:"L'Oreal Paris The Infallible Blackbuster Liquid Eyeliner"},
{id:"2223",text:"L'Oreal Paris Telescopic Shocking Extensions Extreme Length + Intensity Mascara"},
{id:"2224",text:"Jurlique International Soothing Foaming Cleanser"},
{id:"2225",text:"L'Oreal Paris Telescopic Precision Liquid Eyeliner Waterproof"},
{id:"2226",text:"L'Occitane Divine Extract"},
{id:"2227",text:"Jan Marini Skin Research, Inc. Age Intervention Transitions Face Lotion"},
{id:"2228",text:"Jason Natural Softening Mango Pure Natural Hand Soap"},
{id:"2229",text:"Jane Iredale Enlighten Concealer"},
{id:"2230",text:"Kevyn Aucoin The Expert Lip Color"},
{id:"2231",text:"IT Cosmetics Bye Bye Lid Lines"},
{id:"2232",text:"L'Oreal Paris Youth Code Pore Vanisher"},
{id:"2233",text:"Korres Wild Rose Instant Brightening Eye Treatment"},
{id:"2234",text:"La Mer Creme De La Mer"},
{id:"2235",text:"Jane Iredale PurePressed Eye Shadows"},
{id:"2236",text:"L'Occitane Angelica UV Shield SPF 40"},
{id:"2237",text:"L'Oreal Paris Revitalift Miracle Blur Instant Skin Smoother Oil-Free"},
{id:"2238",text:"La Mer The Cleansing Lotion"},
{id:"2239",text:"Hawaiian Tropic Vanilla Mint Lip Balm Stick Sunscreen SPF 45+"},
{id:"2240",text:"IT Cosmetics Your Skin But Better CC+ with SPF 50+"},
{id:"2241",text:"La Roche-Posay Lipikar Baume AP Body Balm"},
{id:"2242",text:"La Mer The Regenerating Serum"},
{id:"2243",text:"Kiehl's Ultra Facial Oil-Free Cleanser"},
{id:"2244",text:"L'Oreal Paris Revitalift Complete Night Cream"},
{id:"2245",text:"L'Oreal Paris Ideal Clean All Skin Types Foaming Gel Cleanser"},
{id:"2246",text:"Jan Marini Skin Research, Inc. Factor-A Plus Lotion"},
{id:"2247",text:"Kiss My Face Olive & Aloe Soap"},
{id:"2248",text:"La Prairie Cellular Luxe Lip Enhancer"},
{id:"2249",text:"Jane Iredale 24-Karat Gold Dust"},
{id:"2250",text:"Jan Marini Skin Research, Inc. C-ESTA Serum"},
{id:"2251",text:"Jason Natural Soothing Aloe Vera Pure Natural Body Wash"},
{id:"2252",text:"L'Occitane Angelica Instant Hydration Mask"},
{id:"2253",text:"Korres Greek Yoghurt Nourishing Moisturiser"},
{id:"2254",text:"Hawaiian Tropic Silk Hydration After Sun Ultra Hydrating Lotion and Soothing Aloe Gel Coconut Papaya"},
{id:"2255",text:"Jan Marini Skin Research, Inc. Age Intervention Eye Cream"},
{id:"2256",text:"Kiss My Face Face Factor Face & Neck SPF 50"},
{id:"2257",text:"Jane Iredale Highlighter Pencil"},
{id:"2258",text:"Kate Somerville LumiWhite Dark Spot Corrector"},
{id:"2259",text:"IT Cosmetics Hello Lashes 5-in-1 Treatment Mascara"},
{id:"2260",text:"La Prairie Skin Caviar Liquid Lift"},
{id:"2261",text:"Jane Iredale Facial Blotting Papers"},
{id:"2262",text:"Jane Iredale Balance Antioxidant Hydration Spray"},
{id:"2263",text:"Hada Labo Tokyo Replenishing Hydrator"},
{id:"2264",text:"Jane Iredale Brush-Me Matte"},
{id:"2265",text:"La Prairie Skin Caviar Luxe Cream"},
{id:"2266",text:"Hawaiian Tropic After Sun Cooling Gel Cool Aloe"},
{id:"2267",text:"L'Oreal Paris Colour Riche La Lacque Lip Pen"},
{id:"2268",text:"Kiss My Face Everyday SPF 15 Moisturizer"},
{id:"2269",text:"Jergens Skin Smoothing Cellular Renewal Moisturizer"},
{id:"2270",text:"IT Cosmetics Airbrush Blush StainSweet Cheeks"},
{id:"2271",text:"L'Oreal Paris Extra-Volume Collagen Waterproof Mascara"},
{id:"2272",text:"La Mer The Hand Treatment"},
{id:"2273",text:"L'Oreal Paris Double Extend Lash Fortifier & Extender Mascara Waterproof"},
{id:"2274",text:"Kiehl's Super Multi-Corrective Cream"},
{id:"2275",text:"Kiss My Face C the Change Ester C Serum for All Skin Types"},
{id:"2276",text:"KORA Organics Essential Body Wash Cleanse and Invigorate"},
{id:"2277",text:"L'Oreal Paris Mens Expert Vita Lift SPF 15 Anti-Wrinkle & Firming Moisturizer"},
{id:"2278",text:"Jergens Natural Glow & Protect Daily Moisturizer SPF 20 for Medium to Tan Skin Tones"},
{id:"2279",text:"Korres Pomegranate Toner"},
{id:"2280",text:"La Prairie Cellular Night Repair Cream"},
{id:"2281",text:"Jan Marini Skin Research, Inc. Transformation Face Serum"},
{id:"2282",text:"Jan Marini Skin Research, Inc. C-ESTA Cream"},
{id:"2283",text:"Kiehl's Ultra Facial Cleanser, For All Skin Types"},
{id:"2284",text:"La Mer The Eye Balm Intense"},
{id:"2285",text:"Kevyn Aucoin The Pure Powder Glow"},
{id:"2286",text:"La Roche-Posay Anthelios 40 Sunscreen Cream SPF 40"},
{id:"2287",text:"La Prairie Luxe Eye Liner Automatique"},
{id:"2288",text:"L'Oreal Paris Revitalift Deep-Set Wrinkle Repair Night Creme"},
{id:"2289",text:"KINeSYS Kids SPF 30 Alcohol Free Sunscreen Spray"},
{id:"2290",text:"Hawaiian Tropic Sheer Touch Lotion Sunscreen Broad Spectrum SPF 15"},
{id:"2291",text:"L'Occitane Immortelle Precious Cream"},
{id:"2292",text:"Jan Marini Skin Research, Inc. Factor-A Cream"},
{id:"2293",text:"L'Oreal Paris Ideal Moisture Normal Skin Day Lotion SPF 25"},
{id:"2294",text:"Jan Marini Skin Research, Inc. Antioxidant Recover-E"},
{id:"2295",text:"La Prairie Anti-Aging Longevity Serum"},
{id:"2296",text:"L'Occitane Ultra Gentle Shea Cleansing Milk"},
{id:"2297",text:"L'Oreal Paris Infallible Pro Spray & Set Makeup Extender Setting Spray"},
{id:"2298",text:"L'Oreal Paris Revitalift Radiant Smoothing Cream Cleanser"},
{id:"2299",text:"Guerlain Terracotta Loose Powder Kohl"},
{id:"2300",text:"L'Oreal Paris Sublime Bronze Self-Tanning Gelee, Medium-Deep, for Body"},
{id:"2301",text:"L'Oreal Paris Mens Expert Vita Lift Anti-Wrinkle & Firming Moisturizer"},
{id:"2302",text:"Josie Maran Cosmetics Argan Oil Moisturizing Stick"},
{id:"2303",text:"Hourglass Cosmetics Modernist Eyeshadow Palette"},
{id:"2304",text:"La Prairie Cellular Eye Cream Platinum Rare"},
{id:"2305",text:"Jan Marini Skin Research, Inc. Bioglycolic Bioclear Cream"},
{id:"2306",text:"La Prairie Cellular Treatment Bronzing Powder"},
{id:"2307",text:"Kate Somerville KateCeuticals Moisture Milk Cleanser"},
{id:"2308",text:"La Roche-Posay Anthelios 45 Ultra Light Sunscreen Fluid for Face"},
{id:"2309",text:"La Roche-Posay Hydraphase UV SPF 20"},
{id:"2310",text:"L'Oreal Paris HiP Studio Secrets Professional The One Sweep Eye Shadow"},
{id:"2311",text:"Korres Black Pine Firming, Lifting & Antiwrinkle Serum"},
{id:"2312",text:"Kiehl's Blue Herbal Moisturizer"},
{id:"2313",text:"L'Oreal Paris RevitaLift Triple Power Concentrated Serum Treatment"},
{id:"2314",text:"L'Oreal Paris Youth Code SPF 30 Day Cream"},
{id:"2315",text:"La Roche-Posay Substiane (+)"},
{id:"2316",text:"Jan Marini Skin Research, Inc. Age Intervention Duality Anti-Acne Cream & Anti-Aging Face Cream"},
{id:"2317",text:"Kiehl's Supremely Gentle Eye Make-Up Remover"},
{id:"2318",text:"Jan Marini Skin Research, Inc. Antioxidant Hydro Silk Protecting Hydrator"},
{id:"2319",text:"L'Oreal Paris Revitalift Double Lifting"},
{id:"2320",text:"Kate Somerville Gentle Daily Wash"},
{id:"2321",text:"L'Oreal Paris Youth Code Texture Perfector Serum Concentrate"},
{id:"2322",text:"Josie Maran Cosmetics Argan Cleansing Oil"},
{id:"2323",text:"Korres Pomegranate Moisturising & Balancing Cream-Gel"},
{id:"2324",text:"Jan Marini Skin Research, Inc. Bioglycolic Bioclear Lotion"},
{id:"2325",text:"L'Oreal Paris Lash Boosting Serum"},
{id:"2326",text:"Jergens Total Nourishment Daily Moisturizer"},
{id:"2327",text:"L'Oreal Paris Infallible Pro-Matte 24HR Foundation"},
{id:"2328",text:"L'Oreal Paris Ideal Moisture Eventone Day/Night Cream"},
{id:"2329",text:"Hourglass Cosmetics Ambient Lighting Bronzer"},
{id:"2330",text:"L'Oreal Paris Telescopic Explosion Waterproof Mascara"},
{id:"2331",text:"Jason Natural C Effects Pure Natural Creme"},
{id:"2332",text:"La Prairie Cellular Swiss Ice Crystal Cream"},
{id:"2333",text:"Kat Von D High Voltage Skin"},
{id:"2334",text:"Kiehl's Facial Fuel Transformer Age Correcting Moisture Gel for Men"},
{id:"2335",text:"IT Cosmetics Brow Power Universal Brow Pencil"},
{id:"2336",text:"Kiss My Face Pure Olive Oil Soap"},
{id:"2337",text:"La Prairie Skin Caviar Crystalline Concentre"},
{id:"2338",text:"Kiehl's Rare Earth Deep Pore Daily Cleanser"},
{id:"2339",text:"L'Oreal Paris go 360 Clean Deep Cream Cleanser"},
{id:"2340",text:"La Prairie Cellular Softening and Balancing Lotion"},
{id:"2341",text:"iS Clinical Cream Cleanser"},
{id:"2342",text:"L'Oreal Paris Telescopic Mascara"},
{id:"2343",text:"Jurlique International Purely Bright Radiance Serum"},
{id:"2344",text:"IT Cosmetics Bye Bye Under Eye Full Coverage Waterproof Concealer"},
{id:"2345",text:"Jan Marini Skin Research, Inc. Age Intervention Face Serum"},
{id:"2346",text:"Jan Marini Skin Research, Inc. Age Intervention Enlighten Plus"},
{id:"2347",text:"Kiehl's Blue Astringent Herbal Lotion"},
{id:"2348",text:"L'Occitane Shea Butter Fabulous Serum"},
{id:"2349",text:"Hourglass Cosmetics Ambient Lighting Blush"},
{id:"2350",text:"L'Oreal Paris Infallible Advanced Never Fail Makeup SPF 20"},
{id:"2351",text:"Kiehl's Actively Correcting & Beautifying BB Cream Sunscreen Broad Spectrum SPF 50 PA +++"},
{id:"2352",text:"L'Occitane Immortelle Cream Mask"},
{id:"2353",text:"KORA Organics Hydrating Mask"},
{id:"2354",text:"La Roche-Posay Redermic + Intensive Daily Anti-Wrinkle/Firming Fill-In Care Normal to Combination Skin"},
{id:"2355",text:"La Mer The Lifting Intensifier"},
{id:"2356",text:"Kate Somerville Overnight Discoloration Perfector Corrective Treatment"},
{id:"2357",text:"La Mer The Moisturizing Lotion"},
{id:"2358",text:"iS Clinical C Eye Advance +"},
{id:"2359",text:"Hydroxatone Advanced Under Eye Formula"},
{id:"2360",text:"La Mer The Brightening Essence Intense"},
{id:"2361",text:"Guerlain Rouge G LExtrait Creme De Rouge Matte Colour"},
{id:"2362",text:"Jan Marini Skin Research, Inc. Antioxidant Daily Face Protectant, SPF 30, Waterproof"},
{id:"2363",text:"L'Oreal Paris Magic Skin Beautifier BB Cream Anti-Redness"},
{id:"2364",text:"KORA Organics Enriched Body Lotion Nourish and Rehydrate"},
{id:"2365",text:"KINeSYS SPF 30 Alcohol-Free Spray Sunscreen with Mango"},
{id:"2366",text:"La Roche-Posay Anthelios Clear Skin Dry Touch Sunscreen Broad Spectrum SPF 60"},
{id:"2367",text:"KORA Organics Tinted Day Cream"},
{id:"2368",text:"Kiss My Face Olive & Aloe Ultra Moisturizer Fragrance-Free, for Extra Sensitive Skin"},
{id:"2369",text:"Hawaiian Tropic Silk Hydration Clear Mist Spray Broad Spectrum SPF 15"},
{id:"2370",text:"La Roche-Posay Rosaliac AR Intense Localized Redness Intensive Serum"},
{id:"2371",text:"Korres Quercetin & Oak Antiageing & Antiwrinkle Eye Cream"},
{id:"2372",text:"Hawaiian Tropic Shimmer Effect After Sun Moisturizer with Mica Minerals Coconut Papaya"},
{id:"2373",text:"La Roche-Posay Redermic [R] Anti-Aging Dermatological Treatment, Intensive"},
{id:"2374",text:"Kevyn Aucoin The Eye Pencil Primatif"},
{id:"2375",text:"Kevyn Aucoin The Eye Pigment Primatif"},
{id:"2376",text:"La Mer The Refining Facial"},
{id:"2377",text:"Guerlain SOS Serum, for Sensitive and Intolerant Skins"},
{id:"2378",text:"Hada Labo Tokyo Hydrating Facial Cleanser"},
{id:"2379",text:"Kiss My Face Sliced Peach Organic Lip Balm SPF 15"},
{id:"2380",text:"Jane Iredale Corrective Colors"},
{id:"2381",text:"Jason Natural Calming Lavender Pure Natural Hand Soap"},
{id:"2382",text:"Jane Iredale Eye Pencils"},
{id:"2383",text:"L'Oreal Paris Infallible Le Rouge"},
{id:"2384",text:"La Roche-Posay Toleriane Purifying Foaming Cream"},
{id:"2385",text:"La Prairie Cellular Cream Platinum Rare"},
{id:"2386",text:"La Prairie Skin Caviar Concealer/Foundation SPF 15"},
{id:"2387",text:"Jane Iredale Sugar & Butter Lip Exfoliator/Plumper"},
{id:"2388",text:"L'Oreal Paris Visible Lift Blur Concealer"},
{id:"2389",text:"Jason Natural Sunblock Sport  SPF 45"},
{id:"2390",text:"La Mer The Lifting Contour Serum"},
{id:"2391",text:"Hada Labo Tokyo Protecting Day Lotion SPF 30"},
{id:"2392",text:"Guerlain Abeille Royal Night Cream"},
{id:"2393",text:"La Roche-Posay Anthelios 60 Ultra Light Sunscreen Fluid"},
{id:"2394",text:"L'Oreal Paris Revitalift Volume Filler Serum"},
{id:"2395",text:"La Roche-Posay Effaclar Astringent Lotion Micro-Exfoliant"},
{id:"2396",text:"La Prairie Sun Protection Emulsion Face SPF 30"},
{id:"2397",text:"Kiehl's Facial Fuel Anti Wrinkle Cream"},
{id:"2398",text:"Hawaiian Tropic Dark Tanning Oil Original"},
{id:"2399",text:"Jane Iredale Eye Gloss"},
{id:"2400",text:"Josie Maran Cosmetics Argan Infinity Cream Intensive Creamy Oil"},
{id:"2401",text:"La Prairie Cellular Luxe Lip Colour"},
{id:"2402",text:"L'Oreal Paris Age Perfect Cell Renewal Day Cream Broad Spectrum SPF 15"},
{id:"2403",text:"Hawaiian Tropic Tanning Creme Lotion SPF 10"},
{id:"2404",text:"L'Oreal Paris Sublime Sun Advanced Sunscreen, SPF 50"},
{id:"2405",text:"Kiehl's Blue Herbal Spot Treatment"},
{id:"2406",text:"La Roche-Posay Rosaliac UV Fortifying Anti-Redness Moisturizer SPF 15"},
{id:"2407",text:"Hourglass Cosmetics Femme Nude Lip Stylo"},
{id:"2408",text:"L'Oreal Paris Age Perfect Cell Renewal Night Cream"},
{id:"2409",text:"Jan Marini Skin Research, Inc. C-ESTA Cleansing Gel"},
{id:"2410",text:"Jason Natural Revitalizing Citrus Pure Natural Body Wash"},
{id:"2411",text:"L'Occitane Angelica Protective Lotion SPF 15"},
{id:"2412",text:"L'Oreal Paris Hydra Perfecte Perfecting Loose Powder"},
{id:"2413",text:"La Prairie Light Fantastic Cellular Concealing Brightening Eye Treatment"},
{id:"2414",text:"Jane Iredale PurePressed Eye Shadow Triple"},
{id:"2415",text:"Kat Von D Shade + Light Eye Contour Palette"},
{id:"2416",text:"Kiehl's Facial Fuel Energizing Tonic for Men"},
{id:"2417",text:"Jergens Ultra Hydrating Daily Moisturizer"},
{id:"2418",text:"L'Oreal Paris Voluminous Original Volume Building Mascara, Curved Brush"},
{id:"2419",text:"L'Oreal Paris Colour Riche Le Matte Lip Pen"},
{id:"2420",text:"La Prairie Advanced Marine Biology Cream SPF 20"},
{id:"2421",text:"Jane Iredale Liquid Eye Liner"},
{id:"2422",text:"La Prairie Advanced Marine Biology Tonic"},
{id:"2423",text:"L'Oreal Paris Age Perfect Glow Renewal Day Lotion Moisturizer SPF 30"},
{id:"2424",text:"L'Occitane Immortelle Essential Water for the Face"},
{id:"2425",text:"L'Oreal Paris Visible Lift Serum Inside Line Minimizing Makeup SPF 17 Normal to Dry Skin"},
{id:"2426",text:"La Prairie Cellular Radiance Eye Cream"},
{id:"2427",text:"La Mer The Moisturizing Soft Cream"},
{id:"2428",text:"KORA Organics Energising Citrus Mist Revitalise and Uplift"},
{id:"2429",text:"L'Oreal Paris go 360 Clean Anti-Breakout Facial Cleanser"},
{id:"2430",text:"L'Oreal Paris Silky Sheer BB Face Lotion 50+ Sunscreen Broad Spectrum SPF 50+"},
{id:"2431",text:"Kiehl's Blue Herbal Gel Cleanser"},
{id:"2432",text:"La Roche-Posay Toleriane Riche, Soothing Protective Cream"},
{id:"2433",text:"Kate Somerville Clarifying Treatment Toner"},
{id:"2434",text:"Jason Natural Soothing Aloe Vera Organic Oil"},
{id:"2435",text:"IT Cosmetics Bye Bye Redness"},
{id:"2436",text:"L'Oreal Paris Sublime Bronze Clear Self-Tanning Gel Medium Natural Tan"},
{id:"2437",text:"Kate Somerville IllumiKate CC Cream Broad Spectrum SPF 50+ PA+++"},
{id:"2438",text:"L'Oreal Paris HiP Studio Secrets Professional Concentrated Shadow Duo"},
{id:"2439",text:"Kiss My Face Lavender & Shea Butter Ultra Moisturizer"},
{id:"2440",text:"Hawaiian Tropic Silk Hydration Lotion Sunscreen Broad Spectrum SPF 50"},
{id:"2441",text:"Kiehl's Facial Fuel No-Shine Hydrator Oil-Free 24 Hour Moisture Gel for Men"},
{id:"2442",text:"Kiehl's Facial Fuel SPF 15"},
{id:"2443",text:"KORA Organics Clay Purifying Mask Refine and Purify"},
{id:"2444",text:"Kiehl's Creme de Corps Light-Weight Body Lotion"},
{id:"2445",text:"Kate Somerville Oil Free Moisturizer"},
{id:"2446",text:"Josie Maran Cosmetics Argan Gentle Exfoliating Powder"},
{id:"2447",text:"Indeed Laboratories Pepta-Bright"},
{id:"2448",text:"Kiss My Face Sport Spray SPF 50"},
{id:"2449",text:"La Prairie Cellular Cleansing Water Face/Eyes"},
{id:"2450",text:"L'Oreal Paris Ideal Moisture Sensitive Skin Day Lotion SPF 25"},
{id:"2451",text:"Kate Somerville LumiWhite Skin Tone Perfector"},
{id:"2452",text:"L'Oreal Paris Infallible Pro-Matte 16HR Powder"},
{id:"2453",text:"Hydroxatone Anti-Acne CC Cream"},
{id:"2454",text:"Jane Iredale Lip Drink SPF 15"},
{id:"2455",text:"L'Oreal Paris Voluminous Million Lashes Waterproof"},
{id:"2456",text:"Jurlique International Purely Bright Day Moisturizer"},
{id:"2457",text:"La Mer The Brightening Lotion Intense"},
{id:"2458",text:"L'Oreal Paris Visible Lift Color Lift Blush"},
{id:"2459",text:"Jane Iredale PureBrow Brow Gel"},
{id:"2460",text:"La Prairie Cellular Swiss Ice Crystal Dry Oil"},
{id:"2461",text:"Jan Marini Skin Research, Inc. C-ESTA Eye Repair Concentrate"},
{id:"2462",text:"La Roche-Posay Anthelios 30 Cooling Water-Lotion Sunscreen"},
{id:"2463",text:"iS Clinical Hydra-Cool Serum"},
{id:"2464",text:"Jason Natural Maximum Strength Vitamin E 45,000 I.U. Pure Natural Skin Oil"},
{id:"2465",text:"Jane Iredale Lip Definer"},
{id:"2466",text:"La Mer The Treatment Fluid Foundation SPF 15"},
{id:"2467",text:"Kate Somerville EradiKate Acne Treatment"},
{id:"2468",text:"L'Oreal Paris Revitalift Face & Neck Day Cream"},
{id:"2469",text:"Jan Marini Skin Research, Inc. C-ESTA Eye Contour Cream"},
{id:"2470",text:"Kevyn Aucoin The Eyeshadow Single"},
{id:"2471",text:"La Roche-Posay Toleraine Ultra Intense Soothing Care"},
{id:"2472",text:"Hydroxatone Luminique"},
{id:"2473",text:"KORA Organics Vitamin Enhanced Lip Balm"},
{id:"2474",text:"L'Occitane Immortelle Brightening UV Shield SPF 40"},
{id:"2475",text:"Jason Natural Pure Natural Sun Facial Sunscreen Broad Spectrum SPF 20"},
{id:"2476",text:"Jan Marini Skin Research, Inc. Marini Instant Refining Exfoliator"},
{id:"2477",text:"Jane Iredale PurePressed Base Mineral Foundation SPF 20"},
{id:"2478",text:"Hawaiian Tropic Silk Hydration Clear Mist Spray Broad Spectrum SPF 30"},
{id:"2479",text:"Kevyn Aucoin The Precision Brow Pencil"},
{id:"2480",text:"Kate Somerville Daily Deflector Moisturizer SPF 50+ PA+++"},
{id:"2481",text:"Jurlique International Replenishing Cleansing Lotion"},
{id:"2482",text:"Jason Natural Pure Natural Sun Mineral Sunscreen Broad Spectrum SPF 30"},
{id:"2483",text:"Jason Natural Anti-Aging Tea Time Pure Natural Moisturizing Creme"},
{id:"2484",text:"Jason Natural Pure Natural Sun Kids Natural Sunscreen Broad Spectrum SPF 45"},
{id:"2485",text:"L'Oreal Paris Ideal Clean All Skin Types Makeup Removing Towelettes"},
{id:"2486",text:"L'Oreal Paris Studio Secrets Professional Color-Correcting Primers"},
{id:"2487",text:"Kiehl's Epidermal Re-Texturizing Micro-Dermabrasion"},
{id:"2488",text:"L'Oreal Paris Extra-Intense Liquid Pencil Eyeliner"},
{id:"2489",text:"Jan Marini Skin Research, Inc. Marini Mascara Performance Mascara"},
{id:"2490",text:"L'Oreal Paris Revitalift Complete Day Cream SPF 18"},
{id:"2491",text:"L'Oreal Paris Voluminous Smoldering Liner"},
{id:"2492",text:"L'Oreal Paris Age Perfect Anti-Sagging + Anti-Age Spot Hydrating Moisturizer Day Cream SPF 15"},
{id:"2493",text:"Jergens Overnight Repair Nightly Restoring Moisturizer"},
{id:"2494",text:"L'Occitane Divine Eyes"},
{id:"2495",text:"L'Oreal Paris Revitalift Complete Eye Cream"},
{id:"2496",text:"La Prairie Cellular Treatment Powder Blush"},
{id:"2497",text:"Jan Marini Skin Research, Inc. Benzoyl Peroxide 10%"},
{id:"2498",text:"Hourglass Cosmetics No. 7 Finishing Brush"},
{id:"2499",text:"L'Oreal Paris True Match Naturale All-Over Mineral Glow"},
{id:"2500",text:"L'Oreal Paris Infallible Smokissime Powder Pen Eyeliner"},
{id:"2501",text:"La Prairie Cellular Eye Contour Cream"},
{id:"2502",text:"La Prairie Cellular Refining Lotion"},
{id:"2503",text:"Kiss My Face Balancing Act Facial Toner"},
{id:"2504",text:"L'Oreal Paris Magic Nude Liquid Powder SPF 18"},
{id:"2505",text:"Jane Iredale Tantasia Self Tanner"},
{id:"2506",text:"L'Oreal Paris Magic Lumi Highlighter"},
{id:"2507",text:"Hourglass Cosmetics Femme Rouge Velvet Creme Lipstick"},
{id:"2508",text:"Jason Natural Purifying Tea Tree Pure Natural Body Wash"},
{id:"2509",text:"Kiehl's Superbly Restorative Argan Dry Oil"},
{id:"2510",text:"Jane Iredale In Touch Cream Blush"},
{id:"2511",text:"Korres Wild Rose Brightening & Refinishing Buff Cleanser"},
{id:"2512",text:"Kiss My Face Coconut Pineapple Lip Balm SPF 15"},
{id:"2513",text:"La Prairie Cellular Treatment Mascara Instant Curl"},
{id:"2514",text:"L'Oreal Paris Voluminous Mistake-Proof Marker Eyeliner"},
{id:"2515",text:"L'Occitane Immortelle Precious Protection SPF 20"},
{id:"2516",text:"L'Oreal Paris Age Perfect Hydra-Nutrition Day/Night Cream for Mature, Very Dry Skin"},
{id:"2517",text:"Jergens Naturals Ultra Hydrating Daily Moisturizer"},
{id:"2518",text:"Jane Iredale Circle/Delete"},
{id:"2519",text:"La Prairie Anti-Aging Complex"},
{id:"2520",text:"Kiehl's Ultra Facial Cream"},
{id:"2521",text:"Jan Marini Skin Research, Inc. Age Intervention Retinol Plus"},
{id:"2522",text:"Hydroxatone Age-Defying Toner"},
{id:"2523",text:"L'Oreal Paris go 360 Clean Deep Facial Cleanser for Sensitive Skin"},
{id:"2524",text:"Indeed Laboratories Snoxin Facial Line Fighter"},
{id:"2525",text:"Kate Somerville Goat Milk Cream"},
{id:"2526",text:"La Mer The Body Creme"},
{id:"2527",text:"KORA Organics Daily Hand Cream Replenish and Protect"},
{id:"2528",text:"Kiss My Face Pore Shrink Deep Cleansing Mask"},
{id:"2529",text:"Jane Iredale Amazing Matte Loose Powder"},
{id:"2530",text:"iS Clinical Pro-Heal Serum Advance Plus"},
{id:"2531",text:"Indeed Laboratories Hydraluron Moisture Serum"},
{id:"2532",text:"Kiehl's Lip Balm #1"},
{id:"2533",text:"L'Oreal Paris Brow Stylist Plumper"},
{id:"2534",text:"L'Occitane Ultra Rich Face Scrub with Shea Butter"},
{id:"2535",text:"IT Cosmetics No. 50 Serum Collagen Veil Anti-Aging Primer"},
{id:"2536",text:"Kat Von D Tattoo Liner"},
{id:"2537",text:"Kiss My Face Sport Lip Balm SPF 30"},
{id:"2538",text:"Kiss My Face Eyewitness Eye Repair Creme for All Skin Types"},
{id:"2539",text:"L'Occitane Angelica Face Toner"},
{id:"2540",text:"Josie Maran Cosmetics Argan Infinity Lip and Cheek Creamy Oil"},
{id:"2541",text:"Jason Natural Invigorating Rosewater Pure Natural Hand Soap"},
{id:"2542",text:"La Roche-Posay Pigmentclar Serum Intensive Dark Spot Correcting Serum"},
{id:"2543",text:"Kiehl's Micro-Blur Skin Perfector"},
{id:"2544",text:"La Prairie Luxe Brow Liner Automatique"},
{id:"2545",text:"Guerlain Abeille Royale Day Cream-Normal to Combination Skin"},
{id:"2546",text:"La Roche-Posay Effaclar K Daily Oil-Free Clarifying Moisturizer"},
{id:"2547",text:"Kevyn Aucoin The Creamy Glow"},
{id:"2548",text:"IT Cosmetics Feel The Moment Anti-Aging Ultra Hydrating Sensory Awakening Primer Serum"},
{id:"2549",text:"La Mer The Eye Concentrate"},
{id:"2550",text:"L'Oreal Paris Visible Lift Serum Absolute Advanced Age-Reversing Powder"},
{id:"2551",text:"Kiehl's Clearly Corrective Hydrating Moisture Emulsion"},
{id:"2552",text:"Jan Marini Skin Research, Inc. Age Intervention Prime Face Primer"},
{id:"2553",text:"La Roche-Posay Anthelios 60 Ultra Light Sunscreen Lotion Spray"},
{id:"2554",text:"La Prairie Cellular Treatment Eye Colour Ensemble"},
{id:"2555",text:"L'Oreal Paris Visible Lift CC Cream"},
{id:"2556",text:"Jurlique International Purely Bright Mist"},
{id:"2557",text:"Jane Iredale PureLash Lengthening Mascara"},
{id:"2558",text:"Kat Von D Lock-It Featherweight Primer"},
{id:"2559",text:"KORA Organics Hydrating Day and Night Cream Replenish and Rejuvenate for Normal to Dry Skin"},
{id:"2560",text:"Josie Maran Cosmetics Argan Color Stick"},
{id:"2561",text:"L'Oreal Paris Revitalift Radiant Smoothing Wet Cleansing Towelettes"},
{id:"2562",text:"Jergens Natural Glow 3 Days to Glow Moisturizer Fair to Medium Skin Tones"},
{id:"2563",text:"L'Oreal Paris Youth Code BB Cream Illuminator SPF 15"},
{id:"2564",text:"Kiehl's Clearly Corrective Deep Moisture Clarifying Cream"},
{id:"2565",text:"Jurlique International Purely Bright Cleanser"},
{id:"2566",text:"L'Oreal Paris Age Perfect BB Cream Instant Radiance SPF 20"},
{id:"2567",text:"Kiss My Face Olive & Green Tea Soap"},
{id:"2568",text:"Jergens Skin Firming Daily Toning Moisturizer"},
{id:"2569",text:"L'Oreal Paris HiP Studio Secrets Professional Color Truth Eyeliner"},
{id:"2570",text:"Hourglass Cosmetics Ambient Lighting Powder"},
{id:"2571",text:"Jan Marini Skin Research, Inc. C-ESTA Facial Mask"},
{id:"2572",text:"iS Clinical White Lightening Serum"},
{id:"2573",text:"Jergens Fragrance Free Sensitive Skin Daily Moisturizer"},
{id:"2574",text:"L'Oreal Paris Pop Balm"},
{id:"2575",text:"Hourglass Cosmetics Retractable Foundation Brush"},
{id:"2576",text:"La Prairie Foam Cleanser"},
{id:"2577",text:"Kat Von D Studded Kiss Lipstick"},
{id:"2578",text:"L'Oreal Paris Brow Stylist Sculptor"},
{id:"2579",text:"Kat Von D Immortal Lash 24 Hour Mascara"},
{id:"2580",text:"L'Oreal Paris Colour Riche Lipcolour"},
{id:"2581",text:"L'Oreal Paris Youth Code Texture Perfector Day/Night Cream"},
{id:"2582",text:"Hawaiian Tropic Tropical Lip Balm Stick Sunscreen SPF 45+"},
{id:"2583",text:"Korres Lip Butter"},
{id:"2584",text:"Guerlain Orchide Impriale Exceptional Complete Care Eye and Lip Cream"},
{id:"2585",text:"La Prairie Cellular Treatment Foundation Powder Finish"},
{id:"2586",text:"Guerlain Secret De Purete Cleansing Foaming Cream"},
{id:"2587",text:"Kate Somerville Eczema Therapy Cream"},
{id:"2588",text:"L'Oreal Paris Advanced Suncare Silky Sheer Lotion SPF 30"},
{id:"2589",text:"La Prairie Sun Protection Lotion SPF 30 Body"},
{id:"2590",text:"Jurlique International Replenishing Foaming Cleanser"},
{id:"2591",text:"La Mer The Hydrating Facial"},
{id:"2592",text:"L'Oreal Paris Infallible Never Fail Silkissime Pencil Eyeliner"},
{id:"2593",text:"La Mer The Cleansing Foam"},
{id:"2594",text:"Guerlain Meteorites Compact Pressed Powder"},
{id:"2595",text:"Jason Natural Softening Mango Pure Natural Body Wash"},
{id:"2596",text:"La Prairie Cellular Radiance Concentrate Pure Gold"},
{id:"2597",text:"La Roche-Posay Anthelios 45 Ultra Light Sunscreen Fluid for Body"},
{id:"2598",text:"La Prairie White Caviar Spot Treatment"},
{id:"2599",text:"L'Oreal Paris Power Volume Collagen 24H Mascara"},
{id:"2600",text:"IT Cosmetics Bye Bye Pores Airbrush Bronzer"},
{id:"2601",text:"L'Oreal Paris Infallible Pro-Last Lipcolor"},
{id:"2602",text:"Jergens Shea Beauty Oil"},
{id:"2603",text:"La Mer The Brightening Infusion Intense"},
{id:"2604",text:"Hydroxatone Gentle Milky Cleanser"},
{id:"2605",text:"Hourglass Cosmetics Illusion Hyaluronic Skin Tint Broad Spectrum SPF 15"},
{id:"2606",text:"KORA Organics Calming Lavender Mist"},
{id:"2607",text:"La Roche-Posay Pigmentclar Eyes Dark Circle Skin-Evening Corrector"},
{id:"2608",text:"L'Occitane Bonne Mere Gentle Cream for Face, Body & Hands  Honey"},
{id:"2609",text:"Jane Iredale D2O Hydration Spritz"},
{id:"2610",text:"Kiehl's Facial Fuel UV Guard Fast-Absorbing Sunscreen for Men SPF 50+"},
{id:"2611",text:"L'Oreal Paris Studio Secrets Professional Secret No. 1 Magic Perfecting Base"},
{id:"2612",text:"Kiss My Face Break Out Botanical Acne Gel"},
{id:"2613",text:"La Mer The Body Refiner"},
{id:"2614",text:"L'Occitane Immortelle Precious Fluid"},
{id:"2615",text:"Indeed Laboratories Eysilix Total Eye Rescuer"},
{id:"2616",text:"La Prairie Cellular Hydralift Firming Mask"},
{id:"2617",text:"L'Oreal Paris Age Perfect Eye Renewal Eye Cream"},
{id:"2618",text:"Jergens Age Defying Multi-Vitamin Moisturizer"},
{id:"2619",text:"Korres Pomegranate Mattifying Primer"},
{id:"2620",text:"L'Oreal Paris Collagen Moisture Filler Day/Night Cream"},
{id:"2621",text:"L'Oreal Paris Double Extend Beauty Tubes Lash Extension Effect Mascara"},
{id:"2622",text:"Hawaiian Tropic Sensitive Skin Face Lotion Sunscreen Broad Spectrum SPF 30"},
{id:"2623",text:"L'Oreal Paris RevitaLift Triple Power Eye Treatment"},
{id:"2624",text:"La Prairie Anti-Aging Hydra Tint SPF 20"},
{id:"2625",text:"Kevyn Aucoin The Lip Gloss"},
{id:"2626",text:"La Prairie Cellular Radiance Cream"},
{id:"2627",text:"La Prairie White Caviar Illuminating Cream"},
{id:"2628",text:"L'Oreal Paris Revitalift Miracle Blur Eye"},
{id:"2629",text:"La Roche-Posay Anthelios 50 Daily Anti-Aging Primer with Sunscreen"},
{id:"2630",text:"L'Oreal Paris HiP Studio Secrets Professional Bright Eye Shadow Duo"},
{id:"2631",text:"L'Oreal Paris Sublime Bronze Tinted Self-Tanning Lotion, Medium Natural Tan"},
{id:"2632",text:"L'Oreal Paris Sublime Sun Advanced Sunscreen Oil Spray SPF 15"},
{id:"2633",text:"La Roche-Posay Hydraphase Eyes"},
{id:"2634",text:"La Prairie Cellular Treatment Illuminating Face Powder"},
{id:"2635",text:"L'Oreal Paris Advanced Suncare Silky Sheer Lotion SPF 50+"},
{id:"2636",text:"Jane Iredale Dot the i"},
{id:"2637",text:"Kat Von D Metal Crush Eyeshadow"},
{id:"2638",text:"Kiss My Face Baby's First Kiss Air Powered Spray Lotion Broad Spectrum SPF 30"},
{id:"2639",text:"Kat Von D Monarch Eyeshadow Palette"},
{id:"2640",text:"Hydroxatone AM/PM Wrinkle Complex for Sensitive Skin"},
{id:"2641",text:"Kiehl's Powerful Strength Line Reducing Concentrate"},
{id:"2642",text:"Kevyn Aucoin The Sensual Skin Fluid Foundation"},
{id:"2643",text:"Kate Somerville ExfoliKate Gentle Exfoliating Treatment"},
{id:"2644",text:"L'Oreal Paris True Match Lumi Healthy Luminous Makeup SPF 20"},
{id:"2645",text:"L'Oreal Paris Revitalift Clinical Repair 10 Day/Night"},
{id:"2646",text:"Hawaiian Tropic Shimmer Effect Lotion Sunscreen with Mica Minerals Broad Spectrum SPF 8"},
{id:"2647",text:"IT Cosmetics IT-O2 Ultra Repair Liquid Oxygen Foundation"},
{id:"2648",text:"Jane Iredale Amazing Base Loose Minerals SPF 20"},
{id:"2649",text:"L'Oreal Paris Youth Code Dark Spot Correcting & Illuminating Serum Corrector"},
{id:"2650",text:"L'Oreal Paris Sublime Glow Daily Moisturizer, for Medium Skin Tones"},
{id:"2651",text:"L'Occitane Angelica Eye Roll-On"},
{id:"2652",text:"L'Oreal Paris Sublime Sheer Protect Sunscreen Oil Spray SPF 50+"},
{id:"2653",text:"Jan Marini Skin Research, Inc. Bioglycolic Facial Cleanser"},
{id:"2654",text:"L'Oreal Paris Sublime Bronze Self-Tanning Gelee, Deep, for Body"},
{id:"2655",text:"Jan Marini Skin Research, Inc. Age Intervention Peptide Extreme Facial Serum"},
{id:"2656",text:"Kiehl's Facial Fuel Eye De-Puffer"},
{id:"2657",text:"Kiss My Face Ginger Mango Organic Lip Balm SPF 15"},
{id:"2658",text:"L'Oreal Paris Visible Lift Blur Foundation SPF 18"},
{id:"2659",text:"Kiss My Face Hot Spots with SPF 30"},
{id:"2660",text:"Josie Maran Cosmetics Argan Daily Moisturizer SPF 47 with Sunboost ATB"},
{id:"2661",text:"Kat Von D Shade & Light Contour Palette"},
{id:"2662",text:"iS Clinical Eye Complex"},
{id:"2663",text:"Jason Natural Soothing Aloe Vera Pure Natural Hand Soap"},
{id:"2664",text:"La Prairie Cellular Radiance Night Cream"},
{id:"2665",text:"La Prairie Cellular 3-Minute Peel"},
{id:"2666",text:"L'Oreal Paris Visible Lift Repair Absolute Foundation SPF 16"},
{id:"2667",text:"La Roche-Posay Mela-D Serum Anti-Dark Spot Concentrate"},
{id:"2668",text:"Korres Pomegranate Deep Cleansing Scrub"},
{id:"2669",text:"KORA Organics Purifying Day and Night Cream Refine and Clarify for Normal/Oily Skin"},
{id:"2670",text:"Kiss My Face Clean for a Day Creamy Face Cleanser"},
{id:"2671",text:"Jan Marini Skin Research, Inc. C-ESTA Serum, Oil Control"},
{id:"2672",text:"Guerlain Kiss Kiss Precious Colours Lipstick"},
{id:"2673",text:"Jergens Soothing Aloe Refreshing Moisturizer"},
{id:"2674",text:"La Mer The Concentrate"},
{id:"2675",text:"Jan Marini Skin Research, Inc. Age Intervention Dark Circle Eye Defense"},
{id:"2676",text:"L'Oreal Paris De-Crease Eye Shadow Base"},
{id:"2677",text:"L'Oreal Paris Mens Expert Comfort Max Anti-Irritation After Shave Balm with SPF 15 Sunscreen"},
{id:"2678",text:"L'Oreal Paris Revitalift Daily Re-Volumizing Moisturizer"},
{id:"2679",text:"Kiehl's Pineapple Papaya Facial Scrub, Made with Real Fruit"},
{id:"2680",text:"Kiss My Face Olive & Aloe Moisturizer, for Sensitive Skin"},
{id:"2681",text:"L'Oreal Paris Studio Secrets Professional Eye Shadow Quad"},
{id:"2682",text:"Jason Natural Tea Tree Oil Pure Oil"},
{id:"2683",text:"Kiehl's Double Strength Visible Wrinkle Filler"},
{id:"2684",text:"L'Oreal Paris Colour Riche Le Gloss"},
{id:"2685",text:"L'Oreal Paris Sublime Bronze Luminous Bronzer Instant Action Self-Tanning Lotion"},
{id:"2686",text:"Kate Somerville Age Arrest Eye Cream"},
{id:"2687",text:"Kiss My Face Peaceful Patchouli Moisturizer"},
{id:"2688",text:"Jane Iredale Cream to Powder Eyeliner"},
{id:"2689",text:"KINeSYS Performance Sunscreen Spray SPF 30, Fragrance Free"},
{id:"2690",text:"La Prairie Cellular Radiance Cream Blush"},
{id:"2691",text:"La Prairie Cellular Treatment Eye Colour"},
{id:"2692",text:"L'Oreal Paris Infallible Never Fail Lipliner"},
{id:"2693",text:"KINeSYS SPF 15 Alcohol-Free Sunscreen Spray"},
{id:"2694",text:"Kate Somerville Exfolikate Body Intensive Exfoliating Treatment"},
{id:"2695",text:"Kiehl's Centella Recovery Skin Salve"},
{id:"2696",text:"L'Oreal Paris Magic Lumi Light Infusing Primer"},
{id:"2697",text:"Kiehl's Mens Oil Eliminator 24 Hour Anti-Shine Moisturizer"},
{id:"2698",text:"Jane Iredale Just Kissed Lip Plumper"},
{id:"2699",text:"KORA Organics Balancing Rose Mist Enrich and Nourish"},
{id:"2700",text:"L'Oreal Paris Visible Lift Serum Absolute  Concealer"},
{id:"2701",text:"Kate Somerville Nourish Hydrating Firming Mist"},
{id:"2702",text:"L'Oreal Paris Voluminous Naturale Natural-Looking Volume & Definition Mascara"},
{id:"2703",text:"Hourglass Cosmetics N 28 Primer Serum Hydrating Anti-Aging"},
{id:"2704",text:"Jane Iredale Active Light Under-Eye Concealer"},
{id:"2705",text:"Kate Somerville Quench Oil-Free Hydrating Face Serum"},
{id:"2706",text:"Kiss My Face So Refined Jojoba & Mint Facial Scrub"},
{id:"2707",text:"L'Oreal Paris Voluminous Superstar Mascara Waterproof"},
{id:"2708",text:"Kate Somerville Line Release Under Eye Repair Cream"},
{id:"2709",text:"L'Oreal Paris Youth Code Foaming Gel Cleanser"},
{id:"2710",text:"L'Oreal Paris Double Extend with Lash Boosting Serum Mascara"},
{id:"2711",text:"Kate Somerville Anti Bac Clearing Lotion"},
{id:"2712",text:"Hydroxatone Anti-Aging BB Cream Broad Spectrum SPF 40 Sunscreen"},
{id:"2713",text:"Jane Iredale Smooth Affair Facial Primer & Brightener"},
{id:"2714",text:"Jason Natural C Effects Pure Natural Hyper-C Serum"},
{id:"2715",text:"Kate Somerville Quench Hydrating Mask"},
{id:"2716",text:"L'Oreal Paris Colour Riche Extraordinaire Lip Color"},
{id:"2717",text:"Jan Marini Skin Research, Inc. Clean Zyme"},
{id:"2718",text:"IT Cosmetics Vitality Cheek Flush"},
{id:"2719",text:"KORA Organics Foaming Cleanser Refine and Clarify, For Oily/Combination Skin"},
{id:"2720",text:"Guerlain Super Aqua-Mask Mask, Optimum Hydration Revitalizer"},
{id:"2721",text:"L'Oreal Paris Sublime Bronze Pro Perfect Salon Airbrush Self-Tanning Mist, Medium Natural Tan"},
{id:"2722",text:"KINeSYS SPF 30 Ointment with Pure Zinc Oxide"},
{id:"2723",text:"Kate Somerville Goat Milk De-Puffing Eye Balm"},
{id:"2724",text:"L'Oreal Paris Extra-Volume Collagen Mascara"},
{id:"2725",text:"L'Oreal Paris Colour Riche Balm SPF 15"},
{id:"2726",text:"L'Oreal Paris Youth Code Eye Cream"},
{id:"2727",text:"La Roche-Posay Effaclar Purifying Foaming Gel"},
{id:"2728",text:"Guerlain L'Or Radiance Concentrate with Pure Gold"},
{id:"2729",text:"Jason Natural Invigorating Rosewater Pure Natural Body Wash"},
{id:"2730",text:"L'Occitane Angelica Hydration Cream"},
{id:"2731",text:"L'Oreal Paris Active Daily Moisture Lotion SPF 15, for All Skin Types"},
{id:"2732",text:"Jan Marini Skin Research, Inc. Factor-A Plus Face Cream"},
{id:"2733",text:"La Mer The Lip Balm"},
{id:"2734",text:"Hada Labo Tokyo Age Correcting Eye Cream"},
{id:"2735",text:"La Prairie Anti-Aging Day Cream SPF 30"},
{id:"2736",text:"IT Cosmetics Hello Lashes Extensions 24 HR Lash Extending Fiber Mascara"},
{id:"2737",text:"Kiehl's Clearly Corrective Dark Spot Solution"},
{id:"2738",text:"L'Occitane Shea Ultra Gentle Moisturizer"},
{id:"2739",text:"Kate Somerville Quench Hydrating Face Serum"},
{id:"2740",text:"Josie Maran Cosmetics Argan Love Your Lips Hydrating Lipstick"},
{id:"2741",text:"L'Oreal Paris True Match Super-Blendable Blush"},
{id:"2742",text:"La Roche-Posay Effaclar Deep Cleansing Foaming Cream"},
{id:"2743",text:"Kate Somerville Clearing Mask"},
{id:"2744",text:"Hawaiian Tropic Shimmer Effect Lotion Sunscreen with Mica Minerals Broad Spectrum SPF 20"},
{id:"2745",text:"La Roche-Posay Redermic + UV SPF 25"},
{id:"2746",text:"L'Oreal Paris Colour Riche Dual Effects Eyeshadow"},
{id:"2747",text:"Jason Natural Lightening Vitamin K Creme Plus"},
{id:"2748",text:"Jane Iredale Glow Time Full Coverage Mineral BB Cream SPF 25"},
{id:"2749",text:"Hawaiian Tropic Island Radiance Creme Self Tanner Medium"},
{id:"2750",text:"La Prairie Cellular Treatment Loose Powder"},
{id:"2751",text:"Guerlain Secret De Purete Cleansing Radiance Gel"},
{id:"2752",text:"L'Oreal Paris Sublime Bronze Self-Tanning Towelettes, for Body, Medium"},
{id:"2753",text:"Guerlain Secret De Purete Gentle Polish Exfoliator"},
{id:"2754",text:"iS Clinical Youth Complex"},
{id:"2755",text:"Josie Maran Cosmetics Coconut Watercolor Eye Shadow"},
{id:"2756",text:"La Prairie Cellular Serum Platinum Rare"},
{id:"2757",text:"La Prairie Essence of Skin Caviar Eye Complex"},
{id:"2758",text:"Jergens All-Purpose Face Cream"},
{id:"2759",text:"La Roche-Posay Mela-D Pigment Control Concentrated Dark-Spot Correcting Serum"},
{id:"2760",text:"Guerlain Super Aqua-Mask Intensive Mask, Optimum Hydration Revitalizer"},
{id:"2761",text:"L'Oreal Paris True Match Mineral Gentle Mineral Makeup SPF 19"},
{id:"2762",text:"Jurlique International Balancing Cleansing Lotion"},
{id:"2763",text:"Kiehl's Rare Earth Deep Pore Cleansing Masque"},
{id:"2764",text:"Kate Somerville CytoCell Dark Circle Corrective Eye Cream"},
{id:"2765",text:"Kiss My Face Face + Neck Face Factor 30 Broad Spectrum SPF 30"},
{id:"2766",text:"L'Oreal Paris Colour Riche Eye Shadow"},
{id:"2767",text:"Kate Somerville Mega-C Dual Radiance Serum"},
{id:"2768",text:"La Prairie Cellular Time Release Moisturizer, Intensive"},
{id:"2769",text:"La Roche-Posay Redermic + Intensive Daily Anti-Wrinkle/Firming Fill-In Care Dry Skin"},
{id:"2770",text:"La Mer The Radiant Serum"},
{id:"2771",text:"Kiss My Face Oat Protein Complex 30 Broad Spectrum SPF 30"},
{id:"2772",text:"Kiss My Face Honey Calendula Natural Ultra Moisturizer"},
{id:"2773",text:"Kate Somerville KateCeuticals Multi-Active Repair Lifting and Lineless Cream"},
{id:"2774",text:"L'Oreal Paris Youth Code Skin Illuminator Tinted All-in-1 Moisturizer SPF 20"},
{id:"2775",text:"Kate Somerville Age Arrest Anti-Wrinkle Cream"},
{id:"2776",text:"La Prairie Anti-Aging Stress Cream"},
{id:"2777",text:"Jergens Original Scent Dry Skin Moisturizer"},
{id:"2778",text:"Kate Somerville Daily Discoloration Perfector Corrective Moisturizer"},
{id:"2779",text:"IT Cosmetics Brow Power Perfector 5-in-1 Powder Gel Pencil"},
{id:"2780",text:"L'Oreal Paris RevitaLift Triple Power Deep Acting Moisturizer"},
{id:"2781",text:"Kate Somerville HydraClear Hydrating Acne Gel"},
{id:"2782",text:"Kiehl's Powerful Wrinkle Reducing Eye Cream"},
{id:"2783",text:"La Prairie Luxe Lip Liner Automatique"},
{id:"2784",text:"Kate Somerville Somerville360 Tan Maximizer Tan Enhancing Moisturizer"},
{id:"2785",text:"L'Oreal Paris HiP Studio Secrets Professional Metallic Shadow Duo"},
{id:"2786",text:"La Prairie White Caviar Illuminating Eye Cream"},
{id:"2787",text:"Kiss My Face Treat Mint Organic Lip Balm SPF 15"},
{id:"2788",text:"Jason Natural C Effects Pure Natural C-Lite Skin Tone Balancer"},
{id:"2789",text:"Jurlique International Purifying Foaming Cleanser"},
{id:"2790",text:"Kiehl's Powerful Wrinkle Reducing Cream"},
{id:"2791",text:"Jane Iredale PureGloss for Lips"},
{id:"2792",text:"Kiehl's Ultra Facial Moisturizer SPF 30"},
{id:"2793",text:"Jurlique International Purely Bright Night Moisturizer"},
{id:"2794",text:"L'Oreal Paris Youth Code Serum Intense Daily Treatment"},
{id:"2795",text:"Hourglass Cosmetics Ambient Strobe Lighting Powder"},
{id:"2796",text:"Hourglass Cosmetics Film Noir Full Spectrum Mascara"},
{id:"2797",text:"La Prairie Advanced Marine Biology Revitalizing Emulsion"},
{id:"2798",text:"Jason Natural Relaxing Chamomile Pure Natural Hand Soap"},
{id:"2799",text:"Jergens Natural Glow Firming Daily Moisturizer Medium to Tan"},
{id:"2800",text:"Jan Marini Skin Research, Inc. Benzoyl Peroxide 2.5% Wash"},
{id:"2801",text:"La Mer The Mist"},
{id:"2802",text:"La Mer The SPF 50 UV Protecting Fluid"},
{id:"2803",text:"Jane Iredale Dream Tint Moisture Tint SPF 15"},
{id:"2804",text:"IT Cosmetics Hello Light Anti-Aging Luminizing Crme Stick"},
{id:"2805",text:"L'Oreal Paris RevitaLift Triple Power Day Lotion Moisturizer SPF 20"},
{id:"2806",text:"La Mer The Radiant Concealer SPF 25"},
{id:"2807",text:"Jason Natural Fragrance Free Pure Natural Body Wash"},
{id:"2808",text:"iS Clinical C & E Serum Advance +"},
{id:"2809",text:"Jason Natural Glowing Apricot Pure Natural Body Wash"},
{id:"2810",text:"L'Oreal Paris Age Perfect Anti-Sagging + Anti-Age Spot Hydrating Moisturizer Night Cream"},
{id:"2811",text:"Kiehl's Rosa Arctica Youth Regenerating Cream with Rare Resurrection Flower"},
{id:"2812",text:"Hawaiian Tropic Dark Tanning Oil Pump Spray SPF 6"},
{id:"2813",text:"Kiehl's Calendula Deep Cleansing Foaming Face Wash"},
{id:"2814",text:"Guerlain Orchide Impriale Cream"},
{id:"2815",text:"L'Occitane Divine Cream"},
{id:"2816",text:"L'Oreal Paris True Match Lumi Cushion Foundation"},
{id:"2817",text:"La Roche-Posay Anthelios SX Daily Moisturizing Cream with Sunscreen SPF 15"},
{id:"2818",text:"L'Oreal Paris Sublime Sun Advanced Sunscreen Oil Spray SPF 30"},
{id:"2819",text:"L'Oreal Paris Clean Artiste Waterproof & Long Wearing Eye Makeup Remover"},
{id:"2820",text:"La Prairie Anti-Aging Neck Cream"},
{id:"2821",text:"L'Oreal Paris Endless Comfortable 8-Hour LipColour"},
{id:"2822",text:"Kate Somerville HydraKate Lifting Serum"},
{id:"2823",text:"Kiss My Face Sun Spray Oil SPF 30"},
{id:"2824",text:"Guerlain Super Aqua-Eye"},
{id:"2825",text:"L'Oreal Paris Sublime Sun Advanced Sunscreen Crystal Clear Mist, SPF 30"},
{id:"2826",text:"Kiehl's Ultra Facial Oil-Free Lotion"},
{id:"2827",text:"La Prairie Cellular Lip Colour Effects Luminous Transparent Glaze"},
{id:"2828",text:"La Prairie Cellular Eye Make-Up Remover"},
{id:"2829",text:"La Mer The Moisturizing Gel Cream"},
{id:"2830",text:"Hawaiian Tropic Sheer Touch Lotion Sunscreen Broad Spectrum SPF 30"},
{id:"2831",text:"Josie Maran Cosmetics Argan Black Oil Mascara"},
{id:"2832",text:"Kate Somerville Kateceuticals Restor8 Replenishing Serum"},
{id:"2833",text:"Kiehl's Rare Earth Pore Minimizing Lotion"},
{id:"2834",text:"Guerlain Blanc de Perle Emulsion"},
{id:"2835",text:"L'Oreal Paris Age Perfect for Mature Skin Hydra-Nutrition Advanced Skin Repair Daily Serum"},
{id:"2836",text:"Kevyn Aucoin The Celestial Skin Liquid Lighting"},
{id:"2837",text:"L'Oreal Paris Brow Stylist Definer"},
{id:"2838",text:"Kate Somerville Body Glow Sunscreen Broad Spectrum SPF 20"},
{id:"2839",text:"Jason Natural Brightening Apricot Scrubble"},
{id:"2840",text:"L'Occitane Immortelle Brightening Essence"},
{id:"2841",text:"Kiss My Face Sun Spray Lotion SPF 30"},
{id:"2842",text:"Hourglass Cosmetics Immaculate Liquid Powder Foundation"},
{id:"2843",text:"Kate Somerville Exfolikate Intensive Exfoliating Treatment"},
{id:"2844",text:"Kiss My Face Olive & Lavender Soap"},
{id:"2845",text:"Kiss My Face Olive & Honey Bar Soap"},
{id:"2846",text:"La Roche-Posay Toleriane Soothing Protective Skincare"},
{id:"2847",text:"L'Oreal Paris Color Riche Caresse Wet Shine Stain"},
{id:"2848",text:"Jane Iredale So-Bronze"},
{id:"2849",text:"Jan Marini Skin Research, Inc. Retinol Plus Mask"},
{id:"2850",text:"L'Oreal Paris Le Kohl Smooth Defining Eyeliner"},
{id:"2851",text:"L'Occitane Angelica Cleansing Gel"},
{id:"2852",text:"iS Clinical Poly-Vitamin Serum"},
{id:"2853",text:"KINeSYS SPF 30 Sun Protection Stick"},
{id:"2854",text:"Hawaiian Tropic Tanning Dry Oil Clear Spray Sunscreen SPF 6"},
{id:"2855",text:"IT Cosmetics Vitality Glow Anti-Aging Matte Bronzer"},
{id:"2856",text:"Jason Natural C Effects Pure Natural Super-C Toner"},
{id:"2857",text:"La Roche-Posay Toleriane Fluide, Soothing Protective Non-Oily Emulsion"},
{id:"2858",text:"Josie Maran Cosmetics Argan Matchmaker Serum Foundation"},
{id:"2859",text:"L'Oreal Paris Telescopic Precision Liquid Eyeliner"},
{id:"2860",text:"Kiehl's Cucumber Herbal Alcohol-Free Toner, for Dry or Sensitive Skin"},
{id:"2861",text:"iS Clinical Firming Complex"},
{id:"2862",text:"Kiehl's Ultra Facial Moisturizer"},
{id:"2863",text:"L'Oreal Paris Ideal Moisture Dry Skin Day Lotion SPF 25"},
{id:"2864",text:"Jason Natural C Effects Pure Natural Lotion"},
{id:"2865",text:"Kevyn Aucoin The Sensual Lip Satin"},
{id:"2866",text:"Kiehl's Creme de Corps"},
{id:"2867",text:"Guerlain Abeille Royale Up-Lifting Eye Care"},
{id:"2868",text:"L'Occitane Shea Butter Ultra Rich Face Cream"},
{id:"2869",text:"Kate Somerville Purify Clarifying Cleanser"},
{id:"2870",text:"La Roche-Posay Hydraphase Intense Riche"},
{id:"2871",text:"L'Oreal Paris Studio Secrets Professional Color Smokes Eye Shadow"},
{id:"2872",text:"Jason Natural C Effects Pure Natural Ultra-C Eye Lift"},
{id:"2873",text:"Kevyn Aucoin The Matte Bronzing Veil"},
{id:"2874",text:"La Prairie Advanced Marine Biology Foaming Mousse Cleanser"},
{id:"2875",text:"L'Oreal Paris HiP Studio Secrets Professional Crystal Shadow Duo"},
{id:"2876",text:"L'Oreal Paris Voluminous Superstar Liquid Eyeliner"},
{id:"2877",text:"Hydroxatone 90 Second Wrinkle Reducer"},
{id:"2878",text:"Indeed Laboratories Nanoblur"},
{id:"2879",text:"Jason Natural Nourishing Cocoa Butter Pure Natural Moisturizing Creme"},
{id:"2880",text:"L'Oreal Paris Ideal Moisture Eventone Tinted Day Lotion SPF 25"},
{id:"2881",text:"Kat Von D Lock-It Concealer"},
{id:"2882",text:"Kate Somerville RetAsphere Micro Peel Retinol Glycolic Treatment"},
{id:"2883",text:"Kiehl's Clearly Corrective Purifying Foaming Cleanser"},
{id:"2884",text:"L'Occitane Immortelle Brightening Moisture Cream"},
{id:"2885",text:"Jason Natural Age Renewal Vitamin E 25,000 I.U. Pure Natural Moisturizing Crme"},
{id:"2886",text:"La Roche-Posay Anthelios 60 Melt-In Sunscreen Milk"},
{id:"2887",text:"Kiehl's Ultra Facial Oil-Free Toner"},
{id:"2888",text:"Hawaiian Tropic Royal Tanning Clear Spray"},
{id:"2889",text:"L'Oreal Paris True Match Naturale Soft-Focus Mineral Finish"},
{id:"2890",text:"L'Oreal Paris Age Perfect Anti-Sagging & Ultra-Hydrating Eye Cream"},
{id:"2891",text:"L'Oreal Paris Sublime Bronze Self-Tanning Lotion SPF 15, Medium Natural Tan"},
{id:"2892",text:"Jan Marini Skin Research, Inc. C-ESTA Face Cream"},
{id:"2893",text:"Jason Natural Quick Clean Pure Natural Makeup Remover Pads"},
{id:"2894",text:"Jason Natural 32,000 I.U. Vitamin E Pure Beauty Oil"},
{id:"2895",text:"Kiss My Face Vitamin A & E Moisturizer"},
{id:"2896",text:"L'Oreal Paris Magic Smooth Souffle Blush"},
{id:"2897",text:"L'Oreal Paris Studio Secrets Professional Eye Shadow Singles"},
{id:"2898",text:"La Roche-Posay Nutritic Lips"},
{id:"2899",text:"Korres Pomegranate Cleansing & Make Up Removing Wipes"},
{id:"2900",text:"La Roche-Posay Physiological Ultra-Fine Scrub"},
{id:"2901",text:"Jane Iredale PureBrow Fix & Mascara"},
{id:"2902",text:"La Roche-Posay Active C Eyes"},
{id:"2903",text:"La Prairie Anti-Aging Concealer SPF 20 Face/Eyes"},
{id:"2904",text:"Jergens BB Body Perfecting Skin Cream"},
{id:"2905",text:"Jason Natural Glowing Apricot Pure Natural Hand Soap"},
{id:"2906",text:"La Roche-Posay Physiological Micellar Solution"},
{id:"2907",text:"Jergens Natural Glow Revitalizing Daily Moisturizer Fair to Medium"},
{id:"2908",text:"Kate Somerville Somerville 360 Tanning Towelette"},
{id:"2909",text:"Jason Natural C Effects Pure Natural Super-C Cleanser"},
{id:"2910",text:"Josie Maran Cosmetics Bear Naked Wipes"},
{id:"2911",text:"Kiehl's Super Fluid UV Defense SPF 50+"},
{id:"2912",text:"Kiehl's Creamy Eye Treatment with Avocado"},
{id:"2913",text:"Kiehl's Skin Rescuer"},
{id:"2914",text:"L'Occitane Angelica Exfoliating Gel"},
{id:"2915",text:"Jan Marini Skin Research, Inc. Age Intervention Regeneration Facial Mask"},
{id:"2916",text:"La Prairie Extrait of Skin Caviar Firming Complex"},
{id:"2917",text:"Kate Somerville ExfoliKate Acne Clearing Exfoliating Acne Treatment"},
{id:"2918",text:"La Roche-Posay Mela-D Dark Spots SPF 15"},
{id:"2919",text:"L'Oreal Paris Magic Skin Beautifier B.B. Cream"},
{id:"2920",text:"Jane Iredale Lid Primer"},
{id:"2921",text:"Jergens Natural Glow Revitalizing Daily Moisturizer Medium to Tan"},
{id:"2922",text:"Jane Iredale PureLash Conditioner"},
{id:"2923",text:"L'Oreal Paris Age Perfect Glow Renewal Day/Night Cream"},
{id:"2924",text:"L'Oreal Paris Revitalift Miracle Blur Broad Spectrum SPF 30 Sunscreen"},
{id:"2925",text:"IT Cosmetics Luxe High Performance Eyeshadow Trio"},
{id:"2926",text:"Hourglass Cosmetics No. 2 Foundation/Blush Brush"},
{id:"2927",text:"L'Oreal Paris Infallible Never Fail Lacquer Liner 24H"},
{id:"2928",text:"Kiss My Face Cranberry Orange Organic Lip Balm SPF 15"},
{id:"2929",text:"La Prairie Anti-Aging Eye Cream SPF 15"},
{id:"2930",text:"Jane Iredale Absence Oil Control Primer"},
{id:"2931",text:"Jan Marini Skin Research, Inc. Bioglycolic Oily Skin Cleansing Gel"},
{id:"2932",text:"Hourglass Cosmetics Veil Mineral Primer Oil Free SPF 15"},
{id:"2933",text:"L'Oreal Paris Advanced Suncare Silky Sheer Face Lotion SPF 50+"},
{id:"2934",text:"L'Oreal Paris Eye Defense"},
{id:"2935",text:"La Prairie Cellular Anti-Wrinkle Sun Cream SPF 30"},
{id:"2936",text:"La Prairie Skin Caviar"},
{id:"2937",text:"Jason Natural Calming Lavender Pure Natural Body Wash"},
{id:"2938",text:"L'Oreal Paris Telescopic Shocking Extensions Extreme Length + Intensity Mascara Waterproof"},
{id:"2939",text:"Guerlain Super Aqua Lotion Replumping Toner"},
{id:"2940",text:"Kiss My Face Under Age Ultra Hydrating Moisturizer"},
{id:"2941",text:"L'Oreal Paris go 360 Clean Deep Facial Cleanser"},
{id:"2942",text:"Kat Von D Lock-It Powder Foundation"},
{id:"2943",text:"Jason Natural Relaxing Chamomile Body Wash"},
{id:"2944",text:"L'Oreal Paris True Match Lumi Liquid Glow Illuminator"},
{id:"2945",text:"L'Oreal Paris Sublime Glow Daily Moisturizer, for Fair Skin Tones"},
{id:"2946",text:"Kiss My Face Peaches & Crme Ultra Moisturizer 4% Alpha Hydroxy"},
{id:"2947",text:"La Prairie Anti-Aging Night Cream"},
{id:"2948",text:"KORA Organics Luxurious Rosehip Body Oil"},
{id:"2949",text:"Josie Maran Cosmetics Nirvana Hydrating Treatment Mist"},
{id:"2950",text:"Jurlique International Balancing Foaming Cleanser"},
{id:"2951",text:"Kiehl's Ultimate Man Razor Bump Relief"},
{id:"2952",text:"Jane Iredale PurePressed Eye Shadow Duo"},
{id:"2953",text:"La Roche-Posay Physiological Cleansing Gel"},
{id:"2954",text:"L'Oreal Paris True Match Super-Blendable Powder"},
{id:"2955",text:"Guerlain Meteorites Perles Illuminating Powder Pure Radiance"},
{id:"2956",text:"KORA Organics Rejuvenating Foot Balm"},
{id:"2957",text:"KORA Organics Recovery Eye Gel Cream"},
{id:"2958",text:"iS Clinical Cleansing Complex"},
{id:"2959",text:"Jane Iredale PureLash Mascara"},
{id:"2960",text:"La Mer The Lifting and Firming Mask"},
{id:"2961",text:"Hawaiian Tropic Protective Dry Oil Broad Spectrum SPF 15"},
{id:"2962",text:"Jergens Crema Deep-Conditioning Oatmeal"},
{id:"2963",text:"Kate Somerville KateCeuticals Replenishing Toner"},
{id:"2964",text:"Josie Maran Cosmetics Coconut Watercolor Cheek Gelee"},
{id:"2965",text:"Jane Iredale Lip Fixation"},
{id:"2966",text:"Hydroxatone AM/PM Anti Wrinkle Complex SPF 15"},
{id:"2967",text:"La Roche-Posay Anthelios 60 Melt-In Sunscreen Lotion"},
{id:"2968",text:"KINeSYS SPF 30 Girl Sunscreen Spray"},
{id:"2969",text:"L'Occitane Shea Butter Lip Balm Stick"},
{id:"2970",text:"Jason Natural Purifying Tea Tree Pure Natural Hand Soap"},
{id:"2971",text:"Jergens Crema Hydrating Coconut Milk"},
{id:"2972",text:"iS Clinical White Lightening Complex"},
{id:"2973",text:"Kiehl's Eye Alert"},
{id:"2974",text:"Jan Marini Skin Research, Inc. Marini Lash Eyelash Conditioner"},
{id:"2975",text:"L'Oreal Paris Lineur Intense Brush Tip Liquid Eyeliner"},
{id:"2976",text:"L'Oreal Paris HiP Studio Secrets Professional Matte Shadow Duo"},
{id:"2977",text:"L'Oreal Paris Infallible Pro Matte Gloss"},
{id:"2978",text:"Kate Somerville Micro Glycolic Polisher"},
{id:"2979",text:"brand product"},
{id:"2980",text:"Estee Lauder Lucidity Translucent Pressed Powder"},
{id:"2981",text:"Givenchy Makeup Rouge Interdit Magic Lipstick"},
{id:"2982",text:"Dr. Denese New York Lash Faker Eyelash & Brow Enhancer"},
{id:"2983",text:"Dr. Hauschka Eyeliner"},
{id:"2984",text:"Estee Lauder Pure Color Blush"},
{id:"2985",text:"Garnier Nutritioniste Clean + Shine Control Cleansing Gel for Oily Skin, Oil-Free"},
{id:"2986",text:"fresh Twilight Freshface Glow"},
{id:"2987",text:"e.l.f. Cosmetics Mineral Blush"},
{id:"2988",text:"Giorgio Armani Crema Nera Extrema Obsidian Mineral High Recovery Elixir"},
{id:"2989",text:"Dr. Jart Every Sun Day UV Illuminating Sunscreen SPF 36"},
{id:"2990",text:"First Aid Beauty Vitamin Hydrating Mist"},
{id:"2991",text:"Dr. Brandt Time Arrest Creme Cleanser"},
{id:"2992",text:"Epicuren Discovery Evening Emulsion Enzyme Moisturizer"},
{id:"2993",text:"Dr. Brandt r3p Cream"},
{id:"2994",text:"Estee Lauder Advanced Time Zone Age Reversing Line/Wrinkle Creme SPF 15 Oil Free"},
{id:"2995",text:"Dr. Denese New York Wrinkle Rx Moisture Surge Day Cream"},
{id:"2996",text:"Dr. Denese New York RestorEyes Eye Cream"},
{id:"2997",text:"Epicuren Discovery Glycolic Skin Peel, Glyco-Treat Exfoliating Solution 10%"},
{id:"2998",text:"e.l.f. Cosmetics Studio Blush"},
{id:"2999",text:"Dr. Jart Water Fuse Smart Gel BB Ultra Hydrating + Memory Activated Formula Broad Spectrum SPF 30"},
{id:"3000",text:"Giorgio Armani Shine Lipstick"},
{id:"3001",text:"Giorgio Armani Face Fabric Second Skin Nude Makeup SPF 12"},
{id:"3002",text:"Giorgio Armani Eyes to Kill Stretch Lengthening Mascara"},
{id:"3003",text:"Eucerin Daily Protection Face Lotion SPF 30"},
{id:"3004",text:"Estee Lauder Verite LightLotion Cleanser"},
{id:"3005",text:"Dr. Dennis Gross Skincare Extra Strength Alpha Beta Peel Two-Step System"},
{id:"3006",text:"Dr. Hauschka Intensive Treatment 01"},
{id:"3007",text:"fresh Lotus Eye Gel"},
{id:"3008",text:"Erno Laszlo Morning Beauty Rescue Brightening Gel Primer"},
{id:"3009",text:"Flower Beauty Lighten Up! Illuminating Concealer"},
{id:"3010",text:"Giorgio Armani Sun Fabric Powder Bronzer"},
{id:"3011",text:"Givenchy Makeup Parad'Eyes Fluid Eye Liner"},
{id:"3012",text:"Dr. Jart Every Sun Day UV Sunscreen Broad Spectrum SPF 50"},
{id:"3013",text:"Estee Lauder New Dimension Shape + Fill Expert Serum"},
{id:"3014",text:"Estee Lauder Sumptuous Waterproof Bold Volume Lifting Mascara"},
{id:"3015",text:"Flower Beauty Lash Knockout Volumizing Mascara"},
{id:"3016",text:"Erno Laszlo Anti Blemish Control Treatment"},
{id:"3017",text:"Dr. Brandt Time Arrest Laser Tight"},
{id:"3018",text:"e.l.f. Cosmetics Studio SPF 45 Sunscreen UVA/UVB Protection"},
{id:"3019",text:"Estee Lauder Double Wear All Day Glow BB Moisture Makeup Broad Spectrum SPF 30"},
{id:"3020",text:"Giorgio Armani Eye Brow Defining Pencil"},
{id:"3021",text:"Estee Lauder Double Wear Stay-in-Place Gel Eyeliner"},
{id:"3022",text:"Estee Lauder Hydrationist Maximum Moisture Creme SPF 15, Normal/Combination Skin"},
{id:"3023",text:"Estee Lauder Signature Hydra Lustre Lipstick"},
{id:"3024",text:"Exuviance by NeoStrata Soothing Toning Lotion"},
{id:"3025",text:"Estee Lauder Idealist Pore Minimizing Skin Refinisher"},
{id:"3026",text:"fresh Black Tea Age-Delay Cream"},
{id:"3027",text:"Estee Lauder Lash Primer Plus"},
{id:"3028",text:"Exuviance by NeoStrata Age Reverse BioActiv Wash"},
{id:"3029",text:"e.l.f. Cosmetics Soothing Serum"},
{id:"3030",text:"Estee Lauder Bronze Goddess Powder Bronzer"},
{id:"3031",text:"Elizabeth Arden Beautiful Color Smoky Eyes Pencil"},
{id:"3032",text:"Epicuren Discovery Medicated Acne Gel"},
{id:"3033",text:"Estee Lauder Advanced Night Repair Concentrated Recovery PowerFoil Mask"},
{id:"3034",text:"Elizabeth Arden Hydra-Gentle Cream Cleanser, for Dry/Sensitive Skin"},
{id:"3035",text:"Giorgio Armani Eyes to Kill Intense Silk Eyeshadow"},
{id:"3036",text:"e.l.f. Cosmetics Eyeshadow Duo"},
{id:"3037",text:"e.l.f. Cosmetics Studio Lip Exfoliator"},
{id:"3038",text:"Elizabeth Arden Color Intrigue Eyeliner"},
{id:"3039",text:"Estee Lauder Perfectly Clean Multi-Action Foam Cleanser/Purifying Mask"},
{id:"3040",text:"Gold Bond Ultimate Healing Foot Therapy Cream"},
{id:"3041",text:"Dr. Brandt Glow by Dr. Brandt Revitalizing Retinol Eye Cream"},
{id:"3042",text:"Flower Beauty Powder Trip Powder Foundation"},
{id:"3043",text:"Flower Beauty Secret Service Cover-Up"},
{id:"3044",text:"e.l.f. Cosmetics Natural Lash Kit/Dramatic Lash Kit"},
{id:"3045",text:"fresh High Noon Freshface Glow"},
{id:"3046",text:"Elizabeth Arden Ceramide Premiere Intense Moisture and Renewal Overnight Regeneration Cream"},
{id:"3047",text:"Dr. Denese New York DermaClean Gentle BHA Cleanser"},
{id:"3048",text:"e.l.f. Cosmetics Essential Eyelid Primer"},
{id:"3049",text:"First Aid Beauty Ultra Repair Cream Intense Therapy Skin Protectant w/FAB Antioxidant Booster"},
{id:"3050",text:"Drunk Elephant JuJu Bar"},
{id:"3051",text:"Embryolisse Soin Lissant Contour des Yeux (Eye Contour Treatment)"},
{id:"3052",text:"Estee Lauder Perfectionist Youth-Infusing Makeup Broad Spectrum SPF 25"},
{id:"3053",text:"First Aid Beauty Ultra Repair Wash"},
{id:"3054",text:"Dr. Jart Black Label Detox BB Beauty Balm SPF 25"},
{id:"3055",text:"EltaMD Moisturizer"},
{id:"3056",text:"e.l.f. Cosmetics Pressed Mineral Eyeshadow"},
{id:"3057",text:"Dr. Hauschka Lemon Lemongrass Body Moisturizer"},
{id:"3058",text:"Elizabeth Arden Flawless Future Moisture Cream"},
{id:"3059",text:"Guerlain Terracotta Loose Powder Kohl"},
{id:"3060",text:"fresh Rose Floral Toner"},
{id:"3061",text:"Flower Beauty Glisten Up! Highlighter Chubby"},
{id:"3062",text:"e.l.f. Cosmetics Essential Duo Eye Shadow Cream"},
{id:"3063",text:"First Aid Beauty Skin Rescue Oil-Free Mattifying Gel"},
{id:"3064",text:"Estee Lauder Soft Clean Silky Hydrating Lotion"},
{id:"3065",text:"Estee Lauder Hydrationist Maximum Moisture Creme, Normal/Combination Skin"},
{id:"3066",text:"Dr. Brandt Light Years Away Whitening Renewal Solution"},
{id:"3067",text:"EltaMD UV Daily SPF 40"},
{id:"3068",text:"Giorgio Armani Lasting Silk UV Foundation SPF 20"},
{id:"3069",text:"Estee Lauder Re-Nutriv Ultimate Lift Age Correcting Mask"},
{id:"3070",text:"Dr. Denese New York Wrinkle Rx 76% Peptide Solution Concentrate"},
{id:"3071",text:"Epicuren Discovery X-Treme Cream Sunscreen SPF 45"},
{id:"3072",text:"Dove MEN + CARE Body and Face Wash, Sensitive Skin"},
{id:"3073",text:"Dr. Denese New York Contouring Serum with Stem Bio-Technology"},
{id:"3074",text:"e.l.f. Cosmetics Hydrating Cleanser"},
{id:"3075",text:"Estee Lauder DayWear Advanced Multi-Protection Anti-Oxidant Creme Oil-Free SPF 25"},
{id:"3076",text:"Giorgio Armani Crema Nera Obsidian Mineral Restoring Serum"},
{id:"3077",text:"e.l.f. Cosmetics Studio Complete Coverage Concealer"},
{id:"3078",text:"Guerlain Secret De Purete Cleansing Foaming Cream"},
{id:"3079",text:"Estee Lauder Revitalizing Supreme Global Anti-Aging Eye Balm"},
{id:"3080",text:"GlamGlow Tingling and Exfoliating Mud Mask"},
{id:"3081",text:"Elizabeth Arden Ceramide Ultra Lipstick"},
{id:"3082",text:"Estee Lauder Hydrationist Maximum Moisture Lotion"},
{id:"3083",text:"Garnier Nutritioniste Skin Renew Anti-Dark-Circle Eye Roller"},
{id:"3084",text:"Dr. Brandt Blemishes No More Oil-Free Hydrator"},
{id:"3085",text:"Guerlain Super Aqua-Eye Serum"},
{id:"3086",text:"fresh Umbrian Clay Face Treatment"},
{id:"3087",text:"e.l.f. Cosmetics Mineral Eyeshadow"},
{id:"3088",text:"Elizabeth Arden Visible Difference Skin Balancing Lotion SPF 15"},
{id:"3089",text:"Dr. Brandt Blemishes No More Redness Relief"},
{id:"3090",text:"EltaMD PM Therapy Facial Moisturizer"},
{id:"3091",text:"Givenchy Makeup Lip Liner"},
{id:"3092",text:"Flower Beauty Extreme Measures Lengthening Mascara"},
{id:"3093",text:"Dr. Denese New York HydroShield Eye Serum"},
{id:"3094",text:"Gold Bond Ultimate Soothing Skin Therapy Lotion, Chamomile"},
{id:"3095",text:"Gold Bond Original Strength Medicated Body Lotion"},
{id:"3096",text:"Epicuren Discovery Colostrum Hydrating Mist"},
{id:"3097",text:"e.l.f. Cosmetics Studio Long-Lasting Lustrous Eyeshadow"},
{id:"3098",text:"Dr. Hauschka Rose Body Moisturizer"},
{id:"3099",text:"Giorgio Armani Light Master Primer"},
{id:"3100",text:"Eucerin Q10 Anti-Wrinkle Sensitive Skin Lotion SPF 15"},
{id:"3101",text:"Garnier Nutritioniste The Radiance Renewer Cleansing Gelee"},
{id:"3102",text:"EltaMD UV Clear SPF 46"},
{id:"3103",text:"e.l.f. Cosmetics Eye Makeup Remover Pads"},
{id:"3104",text:"Dr. Denese New York Resurface & Glow Treatment Face Wash"},
{id:"3105",text:"Eucerin Aquaphor Baby Healing Ointment"},
{id:"3106",text:"Erno Laszlo Oil Control Cleansing Bar"},
{id:"3107",text:"Exuviance by NeoStrata Gentle Cleansing Creme"},
{id:"3108",text:"Erno Laszlo Laszlo Blue Firmarine Eye Serum"},
{id:"3109",text:"Dr. Hauschka Face Powder Brush"},
{id:"3110",text:"Dr. Denese New York Pro C Illuminating Cell Defense Pads"},
{id:"3111",text:"Erno Laszlo Laszlo Blue Firmarine Treatment Bar"},
{id:"3112",text:"Estee Lauder Sumptuous Extreme Lash Multiplying Volume Mascara"},
{id:"3113",text:"Epicuren Discovery Fine Herbal Facial Scrub"},
{id:"3114",text:"Guerlain Abeille Royale Lotion"},
{id:"3115",text:"Dr. Hauschka Liquid Concealer"},
{id:"3116",text:"Epicuren Discovery Gelee Cleanser"},
{id:"3117",text:"First Aid Beauty 5 in 1 Face Cream SPF 30 Age Delay"},
{id:"3118",text:"Gold Bond Ultimate Softening Skin Therapy Lotion, Shea Butter"},
{id:"3119",text:"Givenchy Makeup Phenomen'Eyes Mascara"},
{id:"3120",text:"Elizabeth Arden Prevage Anti-Aging Triple Defense Shield Broad Spectrum Sunscreen SPF 50"},
{id:"3121",text:"Gold Bond Diabetics Dry Skin Relief Skin Protectant Foot Cream"},
{id:"3122",text:"Flower Beauty Line & Shine Lip Liner"},
{id:"3123",text:"Giorgio Armani Maestro Eraser"},
{id:"3124",text:"e.l.f. Cosmetics Studio Lash Extending Mascara"},
{id:"3125",text:"Dr. Hauschka Cleansing Clay Mask, for All Skin Conditions"},
{id:"3126",text:"Dr. Hauschka Eye Solace, for All Skin Conditions"},
{id:"3127",text:"Erno Laszlo Laszlo Blue Firmarine Face Serum"},
{id:"3128",text:"Dr. Dennis Gross Skincare Clinical Concentrate Purifying Booster"},
{id:"3129",text:"Dr. Jart BB Bounce Beauty Balm SPF 30"},
{id:"3130",text:"fresh Creme Ancienne"},
{id:"3131",text:"Epicuren Discovery Hydro Plus Moisturizer"},
{id:"3132",text:"Estee Lauder Pure Color Instant Intense EyeShadow Trio"},
{id:"3133",text:"Estee Lauder Advanced Night Repair Eye Serum Synchronized Complex II"},
{id:"3134",text:"Dr. Denese New York Wrinkle Rx Extreme Retinol Eye Gel"},
{id:"3135",text:"Dr. Dennis Gross Skincare Ferulic & Retinol Eye Cream"},
{id:"3136",text:"e.l.f. Cosmetics Regular & Waterproof Mascara Duo"},
{id:"3137",text:"Epicuren Discovery Bulgarian Rose Otto"},
{id:"3138",text:"Estee Lauder Advanced Time Zone Age Reversing Line/Wrinkle Hydrating Gel Oil-Free"},
{id:"3139",text:"Dr. Hauschka Toned Day Cream, for Normal, Dry or Sensitive Skin"},
{id:"3140",text:"Epicuren Discovery Microderm Exfoliating Cream"},
{id:"3141",text:"Dr. Hauschka Cleansing Milk, for All Skin Conditions"},
{id:"3142",text:"Dr. Hauschka Liquid Eyeliner"},
{id:"3143",text:"Epicuren Discovery Colostrum Cream Serum"},
{id:"3144",text:"Dr. Brandt dr.brandt lineless lines no more for lips"},
{id:"3145",text:"Dr. Hauschka Regenerating Eye Cream"},
{id:"3146",text:"Elizabeth Arden Flawless Future Caplet Serum"},
{id:"3147",text:"Elizabeth Arden Flawless Finish Maximum Coverage Concealer"},
{id:"3148",text:"Estee Lauder Re Nutriv Ultimate Lift Age Correcting Creme"},
{id:"3149",text:"Dove Deep Moisture Nourishing Body Wash"},
{id:"3150",text:"fresh Soy Moisturizing Cream SPF 20"},
{id:"3151",text:"Garnier Nutritioniste BB Eye Miracle Skin Perfector Daily Eye Roller"},
{id:"3152",text:"Giorgio Armani Crema Nera Reviving Eye Compact"},
{id:"3153",text:"fresh Lotus Youth Preserve Face Cream with Super 7 Complex"},
{id:"3154",text:"Dr. Hauschka Mascara"},
{id:"3155",text:"Eucerin Redness Relief Soothing Night Creme"},
{id:"3156",text:"Estee Lauder Re-Nutriv Intensive Soothing Hand Crme"},
{id:"3157",text:"Estee Lauder Hydrationist Maximum Moisture Creme, Dry Skin"},
{id:"3158",text:"Estee Lauder Double Wear BB Brush-On Glow BB Highlighter"},
{id:"3159",text:"EltaMD AM Therapy Facial Moisturizer"},
{id:"3160",text:"Drunk Elephant Peekee Bar"},
{id:"3161",text:"First Aid Beauty Ultra Repair Intensive Lip Balm"},
{id:"3162",text:"Giorgio Armani Rouge Ecstasy"},
{id:"3163",text:"e.l.f. Cosmetics Plumping Lip Glaze"},
{id:"3164",text:"e.l.f. Cosmetics Concealer Pencil & Brush"},
{id:"3165",text:"Dr. Brandt Crease Release, with GABA"},
{id:"3166",text:"e.l.f. Cosmetics Studio Pigment Eyeshadow"},
{id:"3167",text:"Garnier Nutritioniste Ultra-Lift Anti-Wrinkle Eye Roller"},
{id:"3168",text:"Guerlain Secret de Purete Cleansing Creme"},
{id:"3169",text:"EltaMD UV Shield SPF 45"},
{id:"3170",text:"Eucerin Aquaphor Lip Repair + Protect Broad Spectrum SPF 30"},
{id:"3171",text:"Elizabeth Arden Flawless Finish Mousse Makeup"},
{id:"3172",text:"e.l.f. Cosmetics Studio Lip Primer & Plumper"},
{id:"3173",text:"e.l.f. Cosmetics Super Glossy Lip Shine SPF 15"},
{id:"3174",text:"Dr. Jart Most Moist Water-Full Hydrogel Mask Hydrating Act Gel Facial Patch"},
{id:"3175",text:"Estee Lauder Double Wear Zero-Smudge Liquid Eyeliner"},
{id:"3176",text:"Giorgio Armani Maestro Fusion Blush"},
{id:"3177",text:"Dove Visible Care Softening Creme Body Wash"},
{id:"3178",text:"Erno Laszlo Instant Eye Repair"},
{id:"3179",text:"Epicuren Discovery Citrus Herbal Cleanser"},
{id:"3180",text:"Dr. Hauschka Clarifying Toner, for Oily or Blemished Skin"},
{id:"3181",text:"Exuviance by NeoStrata Essential Daily Defense Fluid SPF 15"},
{id:"3182",text:"Epicuren Discovery Noni-Herbal Elixir"},
{id:"3183",text:"Dr. Dennis Gross Skincare Alpha-Beta Body Peel"},
{id:"3184",text:"Dove MEN+CARE Extra Fresh Body and Face Bar"},
{id:"3185",text:"Drunk Elephant Lippe"},
{id:"3186",text:"Dr. Denese New York Hydroshield Face Emulsion with Acquacell Technology"},
{id:"3187",text:"e.l.f. Cosmetics Studio Eyebrow Treat & Tame"},
{id:"3188",text:"e.l.f. Cosmetics Essential Lip Stain"},
{id:"3189",text:"Epicuren Discovery Clarifying Exfoliating Astringent"},
{id:"3190",text:"fresh Rose Face Mask"},
{id:"3191",text:"Dr. Jart Pore Medic Pore Clear Mask Pore Tightening & Sebum Control"},
{id:"3192",text:"Garnier Nutritioniste Miracle Skin Perfector Daily Anti-Acne BB Cream Salicylic Acid Acne Treatment"},
{id:"3193",text:"Elizabeth Arden Ceramide Skin Smoothing Loose Powder"},
{id:"3194",text:"Dr. Hauschka Lipliner"},
{id:"3195",text:"e.l.f. Cosmetics Studio Mascara Primer"},
{id:"3196",text:"e.l.f. Cosmetics Studio Cream Eyeshadow"},
{id:"3197",text:"Dr. Hauschka Eyeshadow Palette"},
{id:"3198",text:"Guerlain Blanc de Perle Essence"},
{id:"3199",text:"Elizabeth Arden Ceramide Ultra Lift and Firm Makeup SPF 15"},
{id:"3200",text:"Drunk Elephant B-Hydra Intensive Hydration Gel"},
{id:"3201",text:"Dr. Brandt Light Years Away Whitening Cream"},
{id:"3202",text:"Gold Bond Ultimate Restoring Skin Therapy Lotion, CoQ10"},
{id:"3203",text:"Epicuren Discovery Botanical Elixir Herbal Eukaryotic Facial Serum"},
{id:"3204",text:"Dove Purely Pampering Nourishing Body Wash, Warm Vanilla & Brown Sugar"},
{id:"3205",text:"Dr. Denese New York CellRenew Rx Cell Recharging Gold Serum"},
{id:"3206",text:"Guerlain Orchide Impriale Exceptional Complete Care Fluid"},
{id:"3207",text:"e.l.f. Cosmetics Studio Tinted Moisturizer SPF 20"},
{id:"3208",text:"Guerlain Orchidee Imperiale White Age Defying Brightening Serum"},
{id:"3209",text:"Giorgio Armani Maestro Liquid Eye Liner"},
{id:"3210",text:"Dr. Denese New York Triple Strength Eye Wrinkle Smoother"},
{id:"3211",text:"Epicuren Discovery Aloe Vera Calming Gel"},
{id:"3212",text:"fresh Soy Face Cleanser"},
{id:"3213",text:"Estee Lauder Re-Nutriv Ultimate Diamond Sculpting/Refinishing Dual Infusion"},
{id:"3214",text:"Elizabeth Arden Lip Definer"},
{id:"3215",text:"Elizabeth Arden Ceramide Lift and Firm Day Cream Broad Spectrum Sunscreen SPF 30"},
{id:"3216",text:"Exuviance by NeoStrata Performance Peel AP 25"},
{id:"3217",text:"Eucerin Daily Replenishing Moisturizing Lotion"},
{id:"3218",text:"Dr. Dennis Gross Skincare All-Over Blemish Solution"},
{id:"3219",text:"Dr. Brandt Time Arrest Creme"},
{id:"3220",text:"e.l.f. Cosmetics Studio Mineral Infused Mascara"},
{id:"3221",text:"Dove Purely Pampering Nourishing Body Wash, Coconut Milk with Jasmine"},
{id:"3222",text:"Estee Lauder Pure Color Envy Defining EyeShadow Wet/Dry"},
{id:"3223",text:"Estee Lauder Nutritious Vita-Mineral Moisture Creme"},
{id:"3224",text:"e.l.f. Cosmetics Wrinkle Refiner"},
{id:"3225",text:"Elizabeth Arden Color Intrigue Bronzing Powder Duo"},
{id:"3226",text:"Dove MEN+CARE Deep Clean Body and Face Bar"},
{id:"3227",text:"Eucerin Redness Relief Soothing Cleanser"},
{id:"3228",text:"Elizabeth Arden Beautiful Color Smooth Line Lip Pencil"},
{id:"3229",text:"Dr. Denese New York Firming Facial Age Corrector Eye Cream"},
{id:"3230",text:"Elizabeth Arden Visible Difference Skin Balancing Lotion"},
{id:"3231",text:"First Aid Beauty Facial Radiance Serum"},
{id:"3232",text:"Estee Lauder Pure Color Gelee Powder Eyeshadow"},
{id:"3233",text:"Guerlain Blanc de Perle Cleansing Foam"},
{id:"3234",text:"Flower Beauty Win Some, Rouge Some Creme Blush"},
{id:"3235",text:"Estee Lauder Double Wear Stay-in-Place Flawless Wear Concealer"},
{id:"3236",text:"Epicuren Discovery CXc Vitamin C Complex"},
{id:"3237",text:"GlamGlow Supercleanse Daily Clearing Cleanser"},
{id:"3238",text:"Estee Lauder Resilience Lift Firming/Sculpting Face and Neck Lotion SPF 15"},
{id:"3239",text:"e.l.f. Cosmetics Mineral Booster"},
{id:"3240",text:"GlamGlow Thirstymud Hydrating Treatment"},
{id:"3241",text:"Estee Lauder Re-Nutriv Intensive Concealing Duo"},
{id:"3242",text:"Gold Bond Ultimate Eczema Relief Skin Protectant Cream"},
{id:"3243",text:"Estee Lauder Bronze Goddess Self Tanning Milk for Body"},
{id:"3244",text:"Dr. Denese New York Vitamin C Line Filling Radiance Cream"},
{id:"3245",text:"Dr. Brandt Time Arrest V-Zone Neck Cream"},
{id:"3246",text:"Estee Lauder Re-Nutriv Intensive Smoothing Body Creme"},
{id:"3247",text:"Giorgio Armani Flash Lacquer"},
{id:"3248",text:"e.l.f. Cosmetics Studio Tone Correcting Powder"},
{id:"3249",text:"Drunk Elephant C-Firma Day Serum"},
{id:"3250",text:"e.l.f. Cosmetics Studio Translucent Mattifying Powder"},
{id:"3251",text:"fresh Rose Hydrating Gel Cream"},
{id:"3252",text:"Garnier Nutritioniste Ultra-Lift Anti-Wrinkle Firming Moisturizer, SPF 15"},
{id:"3253",text:"Giorgio Armani Maestro Eye Shadow"},
{id:"3254",text:"e.l.f. Cosmetics Studio Maximum Coverage Concealer  Oil Free"},
{id:"3255",text:"Epicuren Discovery Bio Peptide Complex Serum"},
{id:"3256",text:"Giorgio Armani Eyes to Kill Mascara"},
{id:"3257",text:"Epicuren Discovery Lip Balm SPF 15"},
{id:"3258",text:"Garnier Nutritioniste Clean + Smoothing Cream Cleanser"},
{id:"3259",text:"Elizabeth Arden Pure Finish Mineral Tinted Moisturizer SPF 15"},
{id:"3260",text:"First Aid Beauty Ultra Repair Hydrating Serum"},
{id:"3261",text:"Embryolisse mulsion Lacte Demaquillante (Purifying Milk Wash)"},
{id:"3262",text:"Exuviance by NeoStrata Rejuvenating Complex"},
{id:"3263",text:"Garnier Nutritioniste Skin Renew Anti-Sun Damage SPF 28"},
{id:"3264",text:"e.l.f. Cosmetics Studio Flawless Finish Foundation SPF 15"},
{id:"3265",text:"Elizabeth Arden Prevage Clinical Lash + Brow Enhancing Serum"},
{id:"3266",text:"Dr. Brandt BB Matte With Signature SHINERASE Broad Spectrum SPF 30"},
{id:"3267",text:"e.l.f. Cosmetics Mineral Lip Tint SPF 8"},
{id:"3268",text:"Elizabeth Arden Ceramide Capsules Daily Youth Restoring Serum"},
{id:"3269",text:"Gold Bond Ultimate Mens Essentials Everyday Hydrating Lotion"},
{id:"3270",text:"Estee Lauder Re-Nutriv Intensive Hydrating Crme Cleanser"},
{id:"3271",text:"Elizabeth Arden Intervene Makeup SPF 15"},
{id:"3272",text:"Dr. Hauschka Translucent Bronze Concentrate, for All Skin Conditions"},
{id:"3273",text:"Drunk Elephant Shaba Complex Eye Serum"},
{id:"3274",text:"fresh Sugar Lip Polish"},
{id:"3275",text:"elure Advanced Brightening Lotion"},
{id:"3276",text:"Dr. Brandt Do Not Age Transforming Pearl Serum"},
{id:"3277",text:"e.l.f. Cosmetics Mineral Eye Brightener"},
{id:"3278",text:"Elizabeth Arden Crystal Clear Lip Gloss"},
{id:"3279",text:"Guerlain Lingerie De Peau Compact Foundation & Concealer SPF 20"},
{id:"3280",text:"Estee Lauder Idealist Cooling Eye Illuminator"},
{id:"3281",text:"Dr. Denese New York Triple Strength Neck Wrinkle Smoother"},
{id:"3282",text:"fresh Sugar Lip Gloss"},
{id:"3283",text:"Dr. Denese New York Omega-3 Eye Treatment with Cooling Wand"},
{id:"3284",text:"Estee Lauder Resilience Lift Firming/Sculpting Face and Neck Creme SPF 15"},
{id:"3285",text:"Epicuren Discovery Lip Balm SPF 15 (Jar)"},
{id:"3286",text:"Drunk Elephant T.L.C. Framboos Glycolic Night Serum"},
{id:"3287",text:"Estee Lauder Pure Color Five Color EyeShadow Palette"},
{id:"3288",text:"Estee Lauder Pure Color High Intensity Lip Lacquer"},
{id:"3289",text:"fresh Umbrian Clay Mattifying Serum"},
{id:"3290",text:"Estee Lauder Enlighten Even Effect Skintone Corrector SPF 30"},
{id:"3291",text:"Epicuren Discovery Acidophilus Probiotic Facial Cream"},
{id:"3292",text:"e.l.f. Cosmetics Mineral Glow Kit"},
{id:"3293",text:"Erno Laszlo Laszlo Blue Firmarine Night Cream"},
{id:"3294",text:"Dr. Dennis Gross Skincare Ferulic Acid + Retinol Brightening Solution"},
{id:"3295",text:"Flower Beauty Eyes On The Prize Eyeshadow Chubby"},
{id:"3296",text:"Giorgio Armani Fluid Master Primer"},
{id:"3297",text:"Estee Lauder Pure Color EyeShadow"},
{id:"3298",text:"Dr. Hauschka Translucent Make-Up"},
{id:"3299",text:"Giorgio Armani Maestro Instant Volume Mascara"},
{id:"3300",text:"GlamGlow GravityMud Firming Treatment"},
{id:"3301",text:"e.l.f. Cosmetics Tone Correcting Concealer"},
{id:"3302",text:"Estee Lauder Re-Nutriv Softening Lotion"},
{id:"3303",text:"Elizabeth Arden Flawless Finish Sponge-On Cream Makeup"},
{id:"3304",text:"Dove Men+Care Sensitive Fresh Awake Body and Face Wash"},
{id:"3305",text:"Garnier Nutritioniste Clean + Blackhead Eliminating Scrub for Oily Skin, Oil-Free"},
{id:"3306",text:"Guerlain Orchidee Imperiale Toner"},
{id:"3307",text:"Exuviance by NeoStrata Matte Perfection"},
{id:"3308",text:"EltaMD Facial Cleanser"},
{id:"3309",text:"e.l.f. Cosmetics Mineral Concealer SPF 15"},
{id:"3310",text:"Dr. Hauschka Lip Care Stick"},
{id:"3311",text:"e.l.f. Cosmetics Eye Primer & Liner Sealer"},
{id:"3312",text:"fresh Seaberry Restorative Body Cream"},
{id:"3313",text:"Dr. Denese New York Hydroshield Cleansing Cream"},
{id:"3314",text:"Guerlain Secret de Purete Cleansing Milk"},
{id:"3315",text:"Guerlain Orchidee Imperiale Neck and Decollete Cream"},
{id:"3316",text:"Dr. Denese New York Firming Facial Age Corrector Cream"},
{id:"3317",text:"Dr. Dennis Gross Skincare All-In-One Cleansing Foam"},
{id:"3318",text:"e.l.f. Cosmetics Studio Pout Perfecter"},
{id:"3319",text:"Dr. Denese New York HydroShield Hydrating Dream Cream"},
{id:"3320",text:"Dr. Hauschka Regenerating Day Cream"},
{id:"3321",text:"Guerlain SOS Serum, for Sensitive and Intolerant Skins"},
{id:"3322",text:"Dr. Brandt Light Years Away Whitening Essence"},
{id:"3323",text:"Dr. Denese New York Pore Refining Toner"},
{id:"3324",text:"Epicuren Discovery Skin Conditioner Enzyme Toner"},
{id:"3325",text:"Garnier Nutritioniste Clean + Invigorating Daily Scrub"},
{id:"3326",text:"Exuviance by NeoStrata Moisture Balance Toner"},
{id:"3327",text:"Embryolisse Lait-Creme Concentre (24-Hour Miracle Cream)"},
{id:"3328",text:"Elizabeth Arden Visible Difference Moisturizing Eye Cream"},
{id:"3329",text:"Epicuren Discovery Active Sport Treat UVA & UVB Sunscreen SPF 30+"},
{id:"3330",text:"First Aid Beauty Anti-Redness Serum Intense Therapy w/FAB Antioxidant Booster"},
{id:"3331",text:"First Aid Beauty Ultra Repair Lip Therapy"},
{id:"3332",text:"Exuviance by NeoStrata Blemish Treatment Gel"},
{id:"3333",text:"Erno Laszlo Timeless Skin Serum"},
{id:"3334",text:"Estee Lauder Matte Perfecting Primer"},
{id:"3335",text:"e.l.f. Cosmetics Feather Proof Moisturizing Lip Liner"},
{id:"3336",text:"First Aid Beauty Ultra Repair Lotion"},
{id:"3337",text:"Elizabeth Arden Ceramide Plump Perfect Targeted Line Concentrate"},
{id:"3338",text:"Dr. Denese New York Body Perfect Concealer"},
{id:"3339",text:"Elizabeth Arden Ceramide Lift and Firm Eye Cream Sunscreen SPF 15"},
{id:"3340",text:"Estee Lauder Revitalizing Supreme Global Anti-Aging Creme"},
{id:"3341",text:"Guerlain Les Voilettes Loose Powder for the Face"},
{id:"3342",text:"Dr. Brandt Lineless Eye Cream, for All Skin Types"},
{id:"3343",text:"EltaMD UV Facial SPF 30+"},
{id:"3344",text:"e.l.f. Cosmetics Studio HD Blush"},
{id:"3345",text:"e.l.f. Cosmetics Studio Eyeshadow"},
{id:"3346",text:"Flower Beauty BB Cream"},
{id:"3347",text:"e.l.f. Cosmetics 11 Piece Studio Brush Collection"},
{id:"3348",text:"Elizabeth Arden Ceramide Purifying Cream Cleanser"},
{id:"3349",text:"Gold Bond Intensive Relief Medicated Anti-Itch Lotion"},
{id:"3350",text:"Dr. Denese New York Age Corrector Firming & Retexturizing Foundation"},
{id:"3351",text:"Exuviance by NeoStrata Intense Lightening Complex"},
{id:"3352",text:"EltaMD Foaming Facial Cleanser"},
{id:"3353",text:"Estee Lauder Pure Color Intense Kajal EyeLiner"},
{id:"3354",text:"e.l.f. Cosmetics Mineral Lip Liner"},
{id:"3355",text:"e.l.f. Cosmetics Studio Contouring Blush & Bronzing Powder"},
{id:"3356",text:"Estee Lauder Sumptuous Infinite Daring Length + Volume Mascara"},
{id:"3357",text:"Elizabeth Arden Prevage Anti-Aging Targeted Skin Tone Corrector"},
{id:"3358",text:"e.l.f. Cosmetics Brightening Eye Color"},
{id:"3359",text:"Elizabeth Arden Visible Difference Multi-Targeted BB Cream Broad Spectrum SPF 30"},
{id:"3360",text:"e.l.f. Cosmetics Essential Lengthening & Defining Mascara"},
{id:"3361",text:"Erno Laszlo Luminous Intensive Cream"},
{id:"3362",text:"fresh Sugar Aa Age-Delay Body Cream"},
{id:"3363",text:"e.l.f. Cosmetics Hydrating BB Cream"},
{id:"3364",text:"Estee Lauder Re-Nutriv Replenishing Comfort Creme"},
{id:"3365",text:"Erno Laszlo Oil Control Day Lotion SPF 15"},
{id:"3366",text:"Estee Lauder Double Wear Mineral Rich Loose Powder Makeup"},
{id:"3367",text:"Elizabeth Arden Color Intrigue Cheekcolor"},
{id:"3368",text:"Elizabeth Arden Prevage Anti-Aging Moisture Cream Broad Spectrum Sunscreen SPF 30"},
{id:"3369",text:"Garnier Nutritioniste Ultra Lift Intensive Deep Wrinkle Night Cream"},
{id:"3370",text:"EltaMD Renew Eye Gel"},
{id:"3371",text:"Elizabeth Arden Ceramide Time Complex Moisture Cream SPF 15"},
{id:"3372",text:"Dr. Hauschka Play of Light Lipstick"},
{id:"3373",text:"Elizabeth Arden Prevage Anti-aging + Intensive Repair Daily Serum"},
{id:"3374",text:"Flower Beauty Easy On The Eyes Long-Lasting Eye Pencil"},
{id:"3375",text:"e.l.f. Cosmetics Studio HD Mattifying Cream Foundation"},
{id:"3376",text:"e.l.f. Cosmetics Studio Lip Stain"},
{id:"3377",text:"Dove MEN+CARE Clean Comfort Body and Face Wash"},
{id:"3378",text:"Guerlain Abeille Royale Day Cream-Normal to Dry Skin"},
{id:"3379",text:"Estee Lauder Swiss Performing Extract"},
{id:"3380",text:"e.l.f. Cosmetics Studio Eyeliner & Shadow Stick"},
{id:"3381",text:"Guerlain Super Aqua-Mask Mask, Optimum Hydration Revitalizer"},
{id:"3382",text:"Elizabeth Arden Ceramide Lift and Firm Makeup Broad Spectrum Sunscreen SPF 30"},
{id:"3383",text:"Gold Bond Therapeutic Foot Cream"},
{id:"3384",text:"Estee Lauder Nutritious Radiant Vitality Energy Lotion"},
{id:"3385",text:"Givenchy Makeup Mister Lash Booster"},
{id:"3386",text:"Estee Lauder Take It Away LongWear Makeup Remover Towelettes"},
{id:"3387",text:"Exuviance by NeoStrata SkinRise Bionic Tonic"},
{id:"3388",text:"First Aid Beauty Detox Eye Roller Intense Therapy w/FAB Antioxidant Booster"},
{id:"3389",text:"Eucerin Intensive Repair Extra-Enriched Foot Creme"},
{id:"3390",text:"Dr. Denese New York Smart Concealer Duo Compact for Eyes & Face"},
{id:"3391",text:"Elizabeth Arden Eight Hour Cream Skin Protectant"},
{id:"3392",text:"e.l.f. Cosmetics Studio Lip Balm SPF 15"},
{id:"3393",text:"Dr. Dennis Gross Skincare Triple C Peptide Firming Oil"},
{id:"3394",text:"GlamGlow Flashmud Brightening Treatment"},
{id:"3395",text:"Givenchy Makeup Le Prisme Blush Powder Blush"},
{id:"3396",text:"Estee Lauder Bronze Goddess Sun Indulgence Lotion for Face SPF 30"},
{id:"3397",text:"Exuviance by NeoStrata Age Reverse Eye Contour"},
{id:"3398",text:"Dr. Denese New York HydroShield Lip Intensive Balm"},
{id:"3399",text:"e.l.f. Cosmetics Lip Liner & Blending Brush"},
{id:"3400",text:"Estee Lauder Sumptuous Bold Volume Lifting Mascara"},
{id:"3401",text:"Givenchy Makeup Mister Light Instant Light Corrective Pen SPF 10"},
{id:"3402",text:"Giorgio Armani Designer Lift Smoothing Firming Foundation SPF 20"},
{id:"3403",text:"e.l.f. Cosmetics Jumbo Lip Gloss Stick"},
{id:"3404",text:"e.l.f. Cosmetics Studio Matte Eyeshadow"},
{id:"3405",text:"Garnier Nutritioniste Clean + Purifying Foam Cleanser"},
{id:"3406",text:"First Aid Beauty Eye Duty Triple Remedy"},
{id:"3407",text:"Gold Bond Diabetics Dry Skin Relief Lotion"},
{id:"3408",text:"e.l.f. Cosmetics Luscious Liquid Lipstick"},
{id:"3409",text:"Dr. Hauschka Regenerating Neck & Decollete Cream"},
{id:"3410",text:"Dr. Hauschka Rhythmic Conditioner, Sensitive, for Sensitive Skin"},
{id:"3411",text:"Dr. Denese New York Med MD 33 Clinical Reserve Cream"},
{id:"3412",text:"Elizabeth Arden Beautiful Color Eye Shadow Duo"},
{id:"3413",text:"Estee Lauder Clear Difference Oil Control/Mattifying Hydrating Gel"},
{id:"3414",text:"Estee Lauder Re-Nutriv Ultimate Lift Age Correcting Crme for Throat and Decolletage"},
{id:"3415",text:"Dr. Hauschka Almond Body Moisturizer"},
{id:"3416",text:"e.l.f. Cosmetics Studio Conditioning Lip Balm"},
{id:"3417",text:"Epicuren Discovery Alpha Lipoic Omega Facial Serum"},
{id:"3418",text:"EltaMD UV Sport SPF 50"},
{id:"3419",text:"Garnier Nutritioniste Miracle Anti-Fatigue Wake-Up Hydra-Gel Moisturizer"},
{id:"3420",text:"Dr. Brandt Do Not Age Firming Neck Cream"},
{id:"3421",text:"e.l.f. Cosmetics Essential Soothing Lip Balm"},
{id:"3422",text:"Estee Lauder Re-Nutriv Ultimate Lift Age Correcting Eye Creme"},
{id:"3423",text:"Gold Bond Maximum Strength Medicated Anti-Itch Cream"},
{id:"3424",text:"Dr. Hauschka Normalizing Day Oil, for Oily, Blemished and Mature Skin"},
{id:"3425",text:"Estee Lauder DayWear BB Anti-Oxidant Beauty Benefit Crme SPF 35"},
{id:"3426",text:"Exuviance by NeoStrata Vespera Bionic Serum"},
{id:"3427",text:"Dr. Denese New York FirmaTone RetinolMax Neurotrophic Night Cream"},
{id:"3428",text:"e.l.f. Cosmetics Studio Lengthening & Volumizing Mascara"},
{id:"3429",text:"e.l.f. Cosmetics Studio High Definition Powder"},
{id:"3430",text:"Dr. Dennis Gross Skincare Hydra-Pure Vitamin C Brightening Serum"},
{id:"3431",text:"Elizabeth Arden SUPERSTART Skin Renewal Booster"},
{id:"3432",text:"Dr. Denese New York Triple Strength Wrinkle Smoother"},
{id:"3433",text:"fresh Soy Face Serum"},
{id:"3434",text:"Givenchy Makeup Magic Kohl Eye Liner Pencil"},
{id:"3435",text:"Garnier Nutritioniste Ultra-Lift Anti-Wrinkle Firming Eye Cream"},
{id:"3436",text:"Dr. Denese New York Damage Reversal Pads"},
{id:"3437",text:"Elizabeth Arden Ceramide Ultra Lift and Firm Concealer"},
{id:"3438",text:"Estee Lauder Take It Away Total Makeup Remover"},
{id:"3439",text:"Dr. Denese New York HydroShield Hydrating Treatment Powder"},
{id:"3440",text:"Estee Lauder Resilience Lift Restorative Radiance Oil"},
{id:"3441",text:"Estee Lauder Re-Nutriv Intensive Smoothing Powder"},
{id:"3442",text:"Elizabeth Arden Prevage Anti-Aging Eye Serum"},
{id:"3443",text:"Erno Laszlo Luminous C10 Night Treatment"},
{id:"3444",text:"Estee Lauder Nutritious Vitality8 Night Radiant Overnight Detox Concentrate"},
{id:"3445",text:"Estee Lauder New Dimension Expert Liquid Tape"},
{id:"3446",text:"Dr. Brandt Glow by Dr. Brandt Overnight Resurfacing Serum"},
{id:"3447",text:"Dr. Denese New York Hydroshield Body Serum"},
{id:"3448",text:"Elizabeth Arden Advanced Lip-Fix Cream"},
{id:"3449",text:"Elizabeth Arden Ceramide Time Complex Moisture Cream"},
{id:"3450",text:"Estee Lauder Perfectionist [CP + R] Wrinkle Lifting/Firming Serum"},
{id:"3451",text:"Gold Bond Extra Strength Medicated Body Lotion"},
{id:"3452",text:"Epicuren Discovery Instantlift Facial Cream"},
{id:"3453",text:"Dove MEN+CARE Deep Clean Body and Face Wash"},
{id:"3454",text:"Givenchy Makeup Mister EyebrowFixing Pencil"},
{id:"3455",text:"Drunk Elephant Lala Retro Whipped Cream"},
{id:"3456",text:"e.l.f. Cosmetics Eye Widener"},
{id:"3457",text:"Dr. Brandt Xtend Your Youth Face Cream"},
{id:"3458",text:"Eucerin Intensive Repair Very Dry Skin Lotion"},
{id:"3459",text:"Estee Lauder Pure Color Sensuous Rouge Lip Color"},
{id:"3460",text:"Dr. Brandt Dark Circles Away Collagen Eye Serum"},
{id:"3461",text:"Dove Gentle Exfoliating Nourishing Body Wash"},
{id:"3462",text:"Gold Bond Rough & Bumpy Skin Daily Therapy Cream"},
{id:"3463",text:"Dove Go Fresh Cool Moisture Body Wash"},
{id:"3464",text:"Giorgio Armani Cheek Fabric Powder Blush"},
{id:"3465",text:"fresh Seaberry Skin Nutrition Cleansing Oil"},
{id:"3466",text:"Giorgio Armani High Precision Retouch"},
{id:"3467",text:"Embryolisse Emulsion Hydra-Mat (Hydra-Mat Emulsion)"},
{id:"3468",text:"Exuviance by NeoStrata CoverBlend Anti-Aging Finishing Powder"},
{id:"3469",text:"Garnier Nutritioniste Clean + Nourishing Cleansing Oil"},
{id:"3470",text:"Dr. Hauschka Rouge Powder"},
{id:"3471",text:"GlamGlow Plumprageous Gloss Lip Treatment"},
{id:"3472",text:"Estee Lauder DayWear Multi-Protection Anti-Oxidant Sheer Tint Release Moisturizer Broad Spectrum SPF 15"},
{id:"3473",text:"Elizabeth Arden Ceramide Premier Intense Moisture Renewal Regeneration Eye Cream"},
{id:"3474",text:"fresh Rose FreshFace Primer"},
{id:"3475",text:"Giorgio Armani Rouge DArmani Lipstick"},
{id:"3476",text:"Flower Beauty On Your Mark Liquid Eyeliner"},
{id:"3477",text:"fresh Crme Ancienne Eye Cream"},
{id:"3478",text:"Estee Lauder Perfectly Clean Multi-Action Cleansing Gelee/Refiner"},
{id:"3479",text:"Estee Lauder Pure Color Envy Liquid Lip Potion"},
{id:"3480",text:"Embryolisse Hydra-Masque"},
{id:"3481",text:"Estee Lauder Revitalizing Supreme Global Anti-Aging Mask Boost"},
{id:"3482",text:"e.l.f. Cosmetics Mineral Foundation SPF 15"},
{id:"3483",text:"Estee Lauder Double Wear Stay-in-Place Lipstick"},
{id:"3484",text:"Estee Lauder Crescent White Full Cycle Brightening BB Creme and Brightening Balm"},
{id:"3485",text:"Giorgio Armani Eye Tint"},
{id:"3486",text:"Giorgio Armani Sheer Blush"},
{id:"3487",text:"fresh Brown Sugar Body Polish"},
{id:"3488",text:"Elizabeth Arden Prevage Face Advanced Anti-Aging Serum"},
{id:"3489",text:"e.l.f. Cosmetics Brightening Eye Liner"},
{id:"3490",text:"Dr. Brandt Lineless Lines No More"},
{id:"3491",text:"Elizabeth Arden Eight Hour Cream Lip Protectant Stick SPF 15"},
{id:"3492",text:"Dove Go Fresh Rebalance Body Wash"},
{id:"3493",text:"e.l.f. Cosmetics Mineral Lipstick"},
{id:"3494",text:"e.l.f. Cosmetics Studio Waterproof Lengthening & Volumizing Mascara"},
{id:"3495",text:"Givenchy Makeup Magic Kajal Eye Pencil"},
{id:"3496",text:"Epicuren Discovery PhytO2 Hydration Serum"},
{id:"3497",text:"Dove Cream Oil Ultra Rich Velvet Body Wash"},
{id:"3498",text:"e.l.f. Cosmetics Mineral Glow"},
{id:"3499",text:"EltaMD UV Lotion SPF 30+"},
{id:"3500",text:"Eucerin Aquaphor Baby Gentle Wash & Shampoo"},
{id:"3501",text:"Dr. Hauschka Bronzing Powder"},
{id:"3502",text:"Guerlain Orchidee Imperiale Mask"},
{id:"3503",text:"Giorgio Armani Sheer Lipstick"},
{id:"3504",text:"Estee Lauder Nutritious Radiant Vitality Essence Oil"},
{id:"3505",text:"Estee Lauder Resilience Lift Firming/Sculpting Eye Creme"},
{id:"3506",text:"Epicuren Discovery Eye Cream"},
{id:"3507",text:"Dr. Hauschka Rose Day Cream Light"},
{id:"3508",text:"fresh Black Tea Age-Delay Face Serum"},
{id:"3509",text:"Dr. Denese New York Wrinkle Rx Extreme Pro-Peptide Gel"},
{id:"3510",text:"e.l.f. Cosmetics Illuminating Eye Cream"},
{id:"3511",text:"Estee Lauder Nutritious Rosy Prism Radiant Essence"},
{id:"3512",text:"Elizabeth Arden All Gone Eye and Lip Makeup Remover"},
{id:"3513",text:"e.l.f. Cosmetics All Over Color Stick"},
{id:"3514",text:"GlamGlow Powermud Dualcleanse Treatment"},
{id:"3515",text:"Dr. Denese New York SPF 25 Hand Defense Cream UVA/UVB"},
{id:"3516",text:"Drunk Elephant Umbra Sheer Physical Defense SPF 30"},
{id:"3517",text:"fresh Sugar Lip Treatment Advanced Therapy"},
{id:"3518",text:"Giorgio Armani Luminous Silk Foundation"},
{id:"3519",text:"Epicuren Discovery Milk Cleanser"},
{id:"3520",text:"fresh Umbrian Clay Absolute Concealer"},
{id:"3521",text:"Givenchy Makeup Gloss Interdit Ultra-Shiny Color Plumping Effect"},
{id:"3522",text:"Dr. Brandt Pore Thing with EVERMAT T-Zone Pore Tightener"},
{id:"3523",text:"Dr. Jart V7 Beauty Balm SPF 30"},
{id:"3524",text:"Elizabeth Arden Prevage Anti-Aging Overnight Cream"},
{id:"3525",text:"Garnier Nutritioniste SkinActive Clearly Brighter Dark Spot Corrector"},
{id:"3526",text:"Dr. Dennis Gross Skincare Antioxidant Cleansing Cloths with AHAs"},
{id:"3527",text:"Epicuren Discovery Extra Fine Citrus Facial Scrub"},
{id:"3528",text:"Garnier Nutritioniste Moisture Rescue Refreshing Gel-Cream Dry Skin"},
{id:"3529",text:"Flower Beauty Powder Up Loose Powder"},
{id:"3530",text:"Estee Lauder Double Wear Stay in Place Eyeshadow Base"},
{id:"3531",text:"Flower Beauty Shadow Play Eyeshadow Quad"},
{id:"3532",text:"Elizabeth Arden Visible Difference Brightening Eye Gel"},
{id:"3533",text:"Dr. Dennis Gross Skincare EZ4U Facial Towelette, all skin types"},
{id:"3534",text:"Guerlain Midnight Secret Late Night Recovery Treatment"},
{id:"3535",text:"Givenchy Makeup Eyebrow Show Powdery Eyebrow Pencil"},
{id:"3536",text:"First Aid Beauty Ultra Repair Instant Oatmeal Mask"},
{id:"3537",text:"Estee Lauder Fresh Air Makeup Base"},
{id:"3538",text:"Flower Beauty Lip Service Lip Butter"},
{id:"3539",text:"Flower Beauty Eye 2 Eye Eye Marker and Mascara"},
{id:"3540",text:"e.l.f. Cosmetics Studio Exact Lash Mascara"},
{id:"3541",text:"EltaMD UV Pure Broad-Spectrum SPF 47"},
{id:"3542",text:"Estee Lauder Perfectly Clean Multi-Action Toning Lotion/Refiner"},
{id:"3543",text:"Eucerin Intensive Repair Extra-Enriched Hand Creme"},
{id:"3544",text:"Dr. Hauschka Firming Mask, for All Skin Conditions"},
{id:"3545",text:"Epicuren Discovery Glycolic Skin Peel, Glyco-Treat Exfoliating Solution 5%"},
{id:"3546",text:"Epicuren Discovery Crystal Clear Makeup Remover"},
{id:"3547",text:"Estee Lauder Automatic Eye Pencil Duo"},
{id:"3548",text:"Dr. Hauschka Intensive Treatment 05"},
{id:"3549",text:"Egyptian Magic Egyptian Magic All Purpose Skin Cream"},
{id:"3550",text:"e.l.f. Cosmetics Liquid Eye Liner"},
{id:"3551",text:"Dr. Brandt Needles No More 3-D Filler Mask"},
{id:"3552",text:"Estee Lauder Re-Nutriv Intensive Lifting Makeup SPF 15"},
{id:"3553",text:"Dr. Hauschka Intensive Treatment 02"},
{id:"3554",text:"Dr. Hauschka Daily Revitalizing Eye Cream, for All Skin Conditions"},
{id:"3555",text:"Eucerin Original Healing Soothing Repair Lotion"},
{id:"3556",text:"Elizabeth Arden Ceramide Cream Blush"},
{id:"3557",text:"Epicuren Discovery Apricot Cream Cleanser"},
{id:"3558",text:"e.l.f. Cosmetics Studio Eye Refresh"},
{id:"3559",text:"Flower Beauty Kiss Stick High-Shine Lip Color"},
{id:"3560",text:"Garnier Nutritioniste SkinActive Micellar Cleansing Water, Regular"},
{id:"3561",text:"First Aid Beauty Ultra Repair Concentrate"},
{id:"3562",text:"Guerlain Rouge Automatique Lipstick"},
{id:"3563",text:"Dr. Dennis Gross Skincare All-in-One Facial Cleanser with Toner"},
{id:"3564",text:"e.l.f. Cosmetics Custom Eyes"},
{id:"3565",text:"Eucerin Gentle Hydrating Cleanser"},
{id:"3566",text:"Erno Laszlo Hydraphel Intensive Night Cream"},
{id:"3567",text:"Dr. Denese New York Baggage Lost Puff Reducing Eye Gel"},
{id:"3568",text:"Giorgio Armani Lip Wax"},
{id:"3569",text:"Estee Lauder Double Wear Zero-Smudge Lengthening Mascara"},
{id:"3570",text:"fresh Sugar Lip Serum Advanced Therapy"},
{id:"3571",text:"Giorgio Armani Eye & Brow Maestro"},
{id:"3572",text:"Dr. Denese New York Damage Reversal Treatment Stick"},
{id:"3573",text:"Eucerin Redness Relief Daily Perfecting Lotion SPF 15"},
{id:"3574",text:"e.l.f. Cosmetics Hydrating Revitalizing Mist"},
{id:"3575",text:"Drunk Elephant Virgin Marula Luxury Facial Oil"},
{id:"3576",text:"Eucerin Eczema Relief Instant Therapy Creme"},
{id:"3577",text:"Giorgio Armani Regenessence 3.R High Lift Eyes Multi-Firming Rejuvenating Eye Balm"},
{id:"3578",text:"e.l.f. Cosmetics Ultimate Eyeshadow Palette"},
{id:"3579",text:"Epicuren Discovery Clarify Cleanser"},
{id:"3580",text:"Dr. Denese New York Neck Saver Neck Firming Pads"},
{id:"3581",text:"Dr. Denese New York SkinPerfect Rx Glycolic Transforming Peel"},
{id:"3582",text:"Erno Laszlo Hydra Therapy Skin Revitalizer"},
{id:"3583",text:"Dr. Brandt r3p Eye"},
{id:"3584",text:"Estee Lauder Nutritious Rosy Prism Radiant Gel Emulsion"},
{id:"3585",text:"fresh Seaberry Moisturizing Face Oil"},
{id:"3586",text:"Givenchy Makeup Mister Radiant Made-to-Measure Glow"},
{id:"3587",text:"Dr. Denese New York Pro C Clinical PM Brightening Serum"},
{id:"3588",text:"Estee Lauder Micro Essence Skin Activating Treatment Lotion"},
{id:"3589",text:"e.l.f. Cosmetics Cover Everything Concealer"},
{id:"3590",text:"Estee Lauder Courrges Super Gloss"},
{id:"3591",text:"Elizabeth Arden Beautiful Color Maximum Volume Mascara"},
{id:"3592",text:"e.l.f. Cosmetics Studio Glossy Gloss"},
{id:"3593",text:"Elizabeth Arden Ceramide Line Smoothing Exfoliator"},
{id:"3594",text:"Dr. Brandt Blemishes No More Intensolution"},
{id:"3595",text:"Dr. Dennis Gross Skincare Alpha Beta Medi-Spa Peel"},
{id:"3596",text:"Estee Lauder Invisible Fluid Makeup"},
{id:"3597",text:"Elizabeth Arden Visible Difference Refining Moisture Cream Complex"},
{id:"3598",text:"e.l.f. Cosmetics Shine Eraser"},
{id:"3599",text:"Exuviance by NeoStrata Night Renewal HydraGel"},
{id:"3600",text:"Flower Beauty Color Play Creme Eyeshadow"},
{id:"3601",text:"Garnier Nutritioniste Expert Exfoliator Daily Exfoliating Gel, Oil-free"},
{id:"3602",text:"Epicuren Discovery Pro Collagen III Serum Amplifier"},
{id:"3603",text:"Exuviance by NeoStrata Exuviance Collagen Triple Boost Serum"},
{id:"3604",text:"Guerlain Kiss Kiss Precious Colours Lipstick"},
{id:"3605",text:"Dr. Denese New York HydroShield Ultra Moisturizing Face Serum"},
{id:"3606",text:"Dr. Dennis Gross Skincare Ferulic + Retinol Triple Correction Eye Serum"},
{id:"3607",text:"Giorgio Armani Master Corrector"},
{id:"3608",text:"e.l.f. Cosmetics Studio Acne Fighting Foundation"},
{id:"3609",text:"Elizabeth Arden Visible Difference Optimizing Skin Serum"},
{id:"3610",text:"Eucerin Aquaphor Lip Repair"},
{id:"3611",text:"Elizabeth Arden Visible Difference Peel and Reveal Revitalizing Mask"},
{id:"3612",text:"e.l.f. Cosmetics Healthy Glow Bronzing Powder"},
{id:"3613",text:"Estee Lauder Verite Moisture Relief Creme"},
{id:"3614",text:"Estee Lauder Double Wear Stay-in-Place Makeup"},
{id:"3615",text:"Dr. Dennis Gross Skincare Alpha Beta Glow Pad"},
{id:"3616",text:"fresh Black Tea Age-Delay Eye Concentrate"},
{id:"3617",text:"fresh Black Tea Age-Delay Lotion Broad Spectrum Sunscreen SPF 20"},
{id:"3618",text:"Epicuren Discovery Hydrating Mineral Mask"},
{id:"3619",text:"Estee Lauder Gentle Eye Makeup Remover"},
{id:"3620",text:"Givenchy Makeup Noir Couture 4-in-1 Mascara Volume, Length, Curl & Care"},
{id:"3621",text:"Estee Lauder New Dimension Plump + Fill Expert Lip Treatment"},
{id:"3622",text:"e.l.f. Cosmetics Clarifying Pressed Powder"},
{id:"3623",text:"Dr. Hauschka Rosemary Foot Balm"},
{id:"3624",text:"Dr. Hauschka Rose Day Cream"},
{id:"3625",text:"Guerlain L'Or Radiance Concentrate with Pure Gold"},
{id:"3626",text:"Exuviance by NeoStrata Daily Antioxidant Peel CA10"},
{id:"3627",text:"Flower Beauty Shine On Lip Gloss Wand"},
{id:"3628",text:"e.l.f. Cosmetics Mineral Eye Liner"},
{id:"3629",text:"Dove Nourishing Care Shea Butter Beauty Bar"},
{id:"3630",text:"Dr. Denese New York HydroShield Eye Fix Duo"},
{id:"3631",text:"Elizabeth Arden Ceramide Purifying Toner"},
{id:"3632",text:"Elizabeth Arden Pure Finish Mineral Powder Foundation SPF 20"},
{id:"3633",text:"Dr. Hauschka Eyeliner Duo"},
{id:"3634",text:"Giorgio Armani Blending Blush Duo"},
{id:"3635",text:"Elizabeth Arden Visible Difference Gentle Hydrating Cream"},
{id:"3636",text:"Dr. Dennis Gross Skincare Dark Spot Sun Defense Broad Spectrum SPF 50"},
{id:"3637",text:"Gold Bond Ultimate Mens Essentials Everyday Hydrating Cream"},
{id:"3638",text:"Estee Lauder Illuminating Perfecting Primer"},
{id:"3639",text:"Estee Lauder NightWear Plus 3-Minute Detox Mask"},
{id:"3640",text:"Dr. Denese New York Cellular Firming Serum"},
{id:"3641",text:"Guerlain Orchidee Imperiale The Cure 3rd Generation Cure Treatment"},
{id:"3642",text:"e.l.f. Cosmetics Studio Volume Plumping Mascara"},
{id:"3643",text:"fresh Supernova Mascara"},
{id:"3644",text:"Dr. Denese New York Wrinkle RX Deep Wrinkle Treatment with Retinol"},
{id:"3645",text:"Giorgio Armani Lip Brush"},
{id:"3646",text:"Exuviance by NeoStrata Skin Brightening Gel"},
{id:"3647",text:"Dr. Dennis Gross Skincare Hydra-Pure Intense Moisture Cream"},
{id:"3648",text:"Estee Lauder Crescent White Full Cycle Brightening UV Protector SPF 50"},
{id:"3649",text:"GlamGlow Poutmud Wet Lip Balm Treatment"},
{id:"3650",text:"Giorgio Armani Luminous Silk Compact"},
{id:"3651",text:"Estee Lauder Perfectly Clean Multi-Action Crme Cleanser/Moisture Mask"},
{id:"3652",text:"Dr. Brandt Microdermabrasion Exfoliating Face Cream"},
{id:"3653",text:"Gold Bond Dark Spot Minimizing Body Cream"},
{id:"3654",text:"Estee Lauder Re-Nutriv Ultimate Lifting Creme Makeup SPF 15"},
{id:"3655",text:"Guerlain Meteorites Compact Pressed Powder"},
{id:"3656",text:"Gold Bond Ultimate Hand Sanitizer Moisturizer"},
{id:"3657",text:"e.l.f. Cosmetics Studio BB Cream SPF 20"},
{id:"3658",text:"Dr. Hauschka Regenerating Body Moisturizer"},
{id:"3659",text:"e.l.f. Cosmetics Studio 3-in-1 Mascara"},
{id:"3660",text:"Free & Clear Free & Clear Liquid Cleanser for Sensitive Skin"},
{id:"3661",text:"Dr. Dennis Gross Skincare Hydra-Pure Firming Eye Cream"},
{id:"3662",text:"Giorgio Armani Lasting Silk UV Compact SPF 34"},
{id:"3663",text:"e.l.f. Cosmetics Studio Lip Lock Pencil"},
{id:"3664",text:"Givenchy Makeup Le Prisme Eyeshadow Quartet"},
{id:"3665",text:"Estee Lauder Sumptuous Two Tone Eye Opening Mascara"},
{id:"3666",text:"Dove Go Fresh Burst Beauty Bar"},
{id:"3667",text:"Guerlain Super Aqua-Mask Intensive Mask, Optimum Hydration Revitalizer"},
{id:"3668",text:"Garnier Nutritioniste Ultra-Lift Miracle Sleeping Cream Anti-Age + Anti-Fatigue Night Cream"},
{id:"3669",text:"Exuviance by NeoStrata AntiRedness Calming Serum"},
{id:"3670",text:"Giorgio Armani Lip Maestro"},
{id:"3671",text:"Eucerin Calming Dry Skin Body Wash"},
{id:"3672",text:"Dr. Dennis Gross Skincare Alpha Beta Daily Glow Moisture"},
{id:"3673",text:"Dr. Brandt UV SPF 30"},
{id:"3674",text:"Flower Beauty Skincognito Stick Foundation"},
{id:"3675",text:"Giorgio Armani Blushing Fabric Second Skin Blush"},
{id:"3676",text:"Exuviance by NeoStrata Rejuvenating Treatment Masque"},
{id:"3677",text:"Flower Beauty Zoom-In Ultimate Mascara"},
{id:"3678",text:"e.l.f. Cosmetics Studio Pressed Powder"},
{id:"3679",text:"e.l.f. Cosmetics Daily Hydration Moisturizer"},
{id:"3680",text:"Garnier Nutritioniste The Ultimate Cleanser 3-Way Clean, Oil-free"},
{id:"3681",text:"Givenchy Makeup Gelee D'Interdit Smoothing Gloss Balm Crystal Shine"},
{id:"3682",text:"Dr. Hauschka Soothing Mask"},
{id:"3683",text:"Garnier Nutritioniste Skin Renew Anti-Puff Eye Roller"},
{id:"3684",text:"Flower Beauty Glow Baby, Glow Blush/Bronzer Duo"},
{id:"3685",text:"e.l.f. Cosmetics Lip Definer & Shaper"},
{id:"3686",text:"Giorgio Armani Gloss DArmani The Cinema Lacquer"},
{id:"3687",text:"Dr. Jart Renewalist BB Beauty Balm Multi-Action Skincare + Makeup SPF 40"},
{id:"3688",text:"Giorgio Armani Sheer Bronzer"},
{id:"3689",text:"Elizabeth Arden Flawless Future Moisture Cream Broad Spectrum Sunscreen SPF 30"},
{id:"3690",text:"Exuviance by NeoStrata Purifying Clay Masque"},
{id:"3691",text:"Embryolisse Redensifiant Contours Yeux-Levres (Re-Densifying Eye and Lip Contour Cream)"},
{id:"3692",text:"e.l.f. Cosmetics Studio Baked Eyeshadow"},
{id:"3693",text:"Dr. Brandt Light Years Away Whitening Cream Cleanser"},
{id:"3694",text:"e.l.f. Cosmetics Studio HD Lifting Concealer"},
{id:"3695",text:"Giorgio Armani Regenessence [3.r] Multi-Corrective Rejuvenating Cream"},
{id:"3696",text:"Dr. Denese New York Doctors Night Recovery Cream"},
{id:"3697",text:"Estee Lauder Pure Color Crystal Lipstick"},
{id:"3698",text:"Dr. Denese New York SunShield Anti-Aging Bronzer"},
{id:"3699",text:"Garnier Nutritioniste SkinActive Micellar Cleansing Water, Waterproof"},
{id:"3700",text:"Dr. Denese New York Advanced Firming Facial Pads"},
{id:"3701",text:"Garnier Nutritioniste Skin Renew Dark Spot Hand Treatment Broad Spectrum SPF 30"},
{id:"3702",text:"Elizabeth Arden Advanced Eye-Fix Primer"},
{id:"3703",text:"Estee Lauder Lip Conditioner"},
{id:"3704",text:"GlamGlow Super-Mud Clearing Treatment"},
{id:"3705",text:"Dr. Hauschka St. Johns Wort Foot Cream"},
{id:"3706",text:"Dove Beauty Bar, Pink"},
{id:"3707",text:"e.l.f. Cosmetics Personal Blend Foundation SPF 15"},
{id:"3708",text:"Estee Lauder Smoothing Creme Concealer"},
{id:"3709",text:"Epicuren Discovery Zinc Oxide Perfecting Sunscreen SPF 27"},
{id:"3710",text:"Epicuren Discovery Glycolic Skin Peel, Clear Glycolic Exfoliating Solution 10%"},
{id:"3711",text:"EltaMD UV Physical SPF 41"},
{id:"3712",text:"Estee Lauder Re-Nutriv Intensive Age Renewal Crme"},
{id:"3713",text:"Epicuren Discovery Retinol Anti-Wrinkle Complex"},
{id:"3714",text:"Dr. Dennis Gross Skincare All-In-One Tinted Moisturizer Sunscreen SPF 15"},
{id:"3715",text:"Estee Lauder Pure Color Cheek Rush"},
{id:"3716",text:"Dr. Hauschka Facial Toner, for Normal, Dry or Sensitive Skin"},
{id:"3717",text:"Elizabeth Arden Prevage Body Total Transforming Anti-Aging Moisturizer"},
{id:"3718",text:"Dr. Hauschka Hand Cream"},
{id:"3719",text:"Dr. Hauschka Cleansing Cream, For All Skin Conditions"},
{id:"3720",text:"Guerlain SOS Crme"},
{id:"3721",text:"Guerlain Meteorites Travel Touch Voyage Powder"},
{id:"3722",text:"Estee Lauder Lucidity Light-Diffusing Makeup"},
{id:"3723",text:"Elizabeth Arden Ceramide Lash Extending Treatment Mascara"},
{id:"3724",text:"Eucerin Daily Defense Moisturizing Hand Creme"},
{id:"3725",text:"e.l.f. Cosmetics Studio High Definition Undereye Setting Powder"},
{id:"3726",text:"e.l.f. Cosmetics Studio Corrective Concealer"},
{id:"3727",text:"Dove Go Fresh Cool Moisture Beauty Bar"},
{id:"3728",text:"Dr. Hauschka Volume Mascara"},
{id:"3729",text:"e.l.f. Cosmetics Shimmering Facial Whip"},
{id:"3730",text:"Givenchy Makeup PhotoPerfection Fluid Foundation SPF 20 PA+++"},
{id:"3731",text:"Elizabeth Arden Prevage Anti-aging + Intensive Repair Eye Serum"},
{id:"3732",text:"Dr. Dennis Gross Skincare Botanical Cleansing Bar with Tea Tree & Aloe"},
{id:"3733",text:"e.l.f. Cosmetics Essential Blush"},
{id:"3734",text:"Eucerin Calming Itch-Relief Treatment"},
{id:"3735",text:"Giorgio Armani Maestro Eye Shadow Quads"},
{id:"3736",text:"e.l.f. Cosmetics Studio Eye Enhancing Mascara"},
{id:"3737",text:"Garnier Nutritioniste Skin Renew Radiance Moisture Cream"},
{id:"3738",text:"Estee Lauder Pure Color Long Lasting Lipstick"},
{id:"3739",text:"Estee Lauder Futurist Age-Resisting Makeup SPF 15"},
{id:"3740",text:"Estee Lauder Resilience Lift Extreme Ultra Firming Creme Compact Makeup SPF 15"},
{id:"3741",text:"EltaMD UV Aero SPF 45"},
{id:"3742",text:"First Aid Beauty Daily Face Cream Daily Treatment w/FAB Antioxidant Booster"},
{id:"3743",text:"Dr. Brandt Pores No More Anti-Aging Mattifying Lotion"},
{id:"3744",text:"Garnier Nutritioniste Skin Renew Miracle Skin Perfector BB Cream SPF 15 Normal to Dry Skin"},
{id:"3745",text:"e.l.f. Cosmetics Eye Transformer"},
{id:"3746",text:"Epicuren Discovery Moisture Surge Hyaluronic Acid Gel"},
{id:"3747",text:"Exuviance by NeoStrata CoverBlend Concealing Treatment Makeup SPF 20"},
{id:"3748",text:"Estee Lauder Pure Color Envy Sculpting Lipstick"},
{id:"3749",text:"e.l.f. Cosmetics Hydrating Cleansing Cloths"},
{id:"3750",text:"Flower Beauty Face the World Tinted Moisturizer"},
{id:"3751",text:"Dr. Dennis Gross Skincare Correct & Perfect Spot Treatment"},
{id:"3752",text:"Dr. Brandt Needles No More"},
{id:"3753",text:"Guerlain Super Aqua-Night"},
{id:"3754",text:"e.l.f. Cosmetics Makeup Remover Cleansing Cloths"},
{id:"3755",text:"Dr. Dennis Gross Skincare Active Vitamin D Serum-Oil"},
{id:"3756",text:"Dr. Hauschka Eyeshadow Solo"},
{id:"3757",text:"Dove Purely Pampering Nourishing Body Wash, Pistachio Cream with Magnolia"},
{id:"3758",text:"Estee Lauder Resilience Lift Firming/Sculpting Face and Neck Creme Oil Free Broad Spectrum SPF 15"},
{id:"3759",text:"Estee Lauder Clear Difference Targeted Blemish Treatment"},
{id:"3760",text:"Estee Lauder Enlighten Even Skintone Correcting Crme"},
{id:"3761",text:"GlamGlow Brightmud Eye Treatment"},
{id:"3762",text:"Garnier Nutritioniste Ultra-Lift Anti-Wrinkle Firming Night Cream"},
{id:"3763",text:"Givenchy Makeup Rouge Interdit Satin Lipstick"},
{id:"3764",text:"Givenchy Makeup Le Prisme Visage Mat Soft Compact Face Powder"},
{id:"3765",text:"Dr. Dennis Gross Skincare Lift & Lighten Eye Cream"},
{id:"3766",text:"Estee Lauder Re-Nutriv Re-Creation Day Crme SPF 15 and Re-Creation Night Crme"},
{id:"3767",text:"EltaMD Lotion"},
{id:"3768",text:"e.l.f. Cosmetics Studio Waterproof Eyeliner Crayon"},
{id:"3769",text:"Eucerin Professional Repair Extremely Dry Skin Lotion"},
{id:"3770",text:"Dove Summer Care Beauty Bar"},
{id:"3771",text:"Dove Go Fresh Burst Body Wash"},
{id:"3772",text:"Dr. Dennis Gross Skincare Clarifying Colloidal Sulfur Mask"},
{id:"3773",text:"Dr. Dennis Gross Skincare Age Erase Moisture for Eyes With Mega 10 Plus"},
{id:"3774",text:"Givenchy Makeup Le Prisme Mono Eyeshadow"},
{id:"3775",text:"Estee Lauder Enlighten Dark Spot Correcting Night Serum"},
{id:"3776",text:"Dr. Jart Water Fuse BB Beauty Balm SPF 25 PA++"},
{id:"3777",text:"Dr. Denese New York Sheer Glow Face Brightener with Pro-Peptide Factor"},
{id:"3778",text:"Epicuren Discovery Gel Plus Enzyme Protein Gel"},
{id:"3779",text:"e.l.f. Cosmetics Mineral Blemish Kit"},
{id:"3780",text:"e.l.f. Cosmetics Essential Shimmer Lip Gloss"},
{id:"3781",text:"Dove Go Fresh ReVive Body Wash"},
{id:"3782",text:"Dr. Hauschka Translucent Face Powder, Compact"},
{id:"3783",text:"Dr. Brandt Pores No More Pore Effect"},
{id:"3784",text:"Estee Lauder Re-Nutriv Radiant UV Base Broad Spectrum SPF 50"},
{id:"3785",text:"Dr. Brandt Pores No More Vacuum Cleaner"},
{id:"3786",text:"Givenchy Makeup Eye Fly Lash Extender Mascara"},
{id:"3787",text:"Estee Lauder DayWear Advanced Multi-Protection Anti-Oxidant Creme SPF 15 Normal/Combination Skin"},
{id:"3788",text:"e.l.f. Cosmetics Tinted Moisturizer SPF 15"},
{id:"3789",text:"Exuviance by NeoStrata Brightening Bionic Eye Cream"},
{id:"3790",text:"Flower Beauty Kiss Me Twice Lip and Cheek Chubby"},
{id:"3791",text:"Garnier Nutritioniste Skin Renew 5 Second Blur Instant Smoother"},
{id:"3792",text:"Dr. Hauschka Lavender Sandalwood Body Moisturizer"},
{id:"3793",text:"Giorgio Armani Maestro Fusion Makeup Compact Broad Spectrum SPF 29"},
{id:"3794",text:"e.l.f. Cosmetics Hypershine Lip Gloss"},
{id:"3795",text:"Epicuren Discovery Glycolic Skin Peel, Clear Glycolic Exfoliating Solution 5%"},
{id:"3796",text:"fresh Peony Whitening Essence"},
{id:"3797",text:"Dr. Denese New York High Potency Dark Circle Rx Eye Formula"},
{id:"3798",text:"Estee Lauder Double Wear Stay-in-Place Powder Makeup"},
{id:"3799",text:"Flower Beauty Raise Some Brows Defining Brow Pencil"},
{id:"3800",text:"Epicuren Discovery Enzyme Concentrate Vitamin Protein Complex"},
{id:"3801",text:"Exuviance by NeoStrata Intensive Eye Treatment Pads"},
{id:"3802",text:"Dr. Jart Premium BB Beauty Balm SPF 45 PA+++"},
{id:"3803",text:"fresh Sugar Face Polish"},
{id:"3804",text:"Elizabeth Arden Beautiful Color Eye Shadow"},
{id:"3805",text:"Givenchy Makeup Teint Couture Long-Wearing Fluid Foundation Broad Spectrum SPF 20"},
{id:"3806",text:"Elizabeth Arden Beautiful Color Luminous Lip Gloss"},
{id:"3807",text:"Estee Lauder Resilience Lift Night Firming/Sculpting Face and Neck Creme"},
{id:"3808",text:"Estee Lauder Re-Nutriv Lightweight Crme"},
{id:"3809",text:"Giorgio Armani Smooth Silk Lip Pencil"},
{id:"3810",text:"e.l.f. Cosmetics Essential Waterproof Eyeliner Pen"},
{id:"3811",text:"Estee Lauder More Than Mascara"},
{id:"3812",text:"Epicuren Discovery Citrus Herbal Polisher"},
{id:"3813",text:"e.l.f. Cosmetics Studio Radiance Enhancer"},
{id:"3814",text:"Eucerin Calming Creme Daily Moisturizer"},
{id:"3815",text:"Giorgio Armani Fluid Sheer"},
{id:"3816",text:"e.l.f. Cosmetics Zit Zapper"},
{id:"3817",text:"Epicuren Discovery Ultra Rose Treat Enzyme Moisturizer"},
{id:"3818",text:"Estee Lauder Pure Color Stay-On Shadow Paint"},
{id:"3819",text:"e.l.f. Cosmetics Wet Gloss Lash & Brow Clear Mascara"},
{id:"3820",text:"Dr. Denese New York FirmaTone Rx Retinol Maximum Chin and Neck Firming Serum"},
{id:"3821",text:"e.l.f. Cosmetics Flawless Eyeshadow"},
{id:"3822",text:"Garnier Nutritioniste Skin Renew Miracle Skin Perfector Anti-Aging BB Cream Broad Spectrum SPF 15 Sunscreen"},
{id:"3823",text:"Erno Laszlo Oil-Control Night Treatment"},
{id:"3824",text:"e.l.f. Cosmetics Essential Lipstick"},
{id:"3825",text:"Guerlain Super Aqua Day Triple Protection Shield SPF 30"},
{id:"3826",text:"e.l.f. Cosmetics Studio Concealer"},
{id:"3827",text:"Givenchy Makeup Mister Bright Touch of Light Pen"},
{id:"3828",text:"Garnier Nutritioniste Skin Renew Miracle Skin Perfector Combination to Oily Skin SPF 20"},
{id:"3829",text:"e.l.f. Cosmetics Studio Eyebrow Kit"},
{id:"3830",text:"Estee Lauder Advanced Time Zone Age Reversing Line/Wrinkle Crme Broad Spectrum SPF 15  Normal/Combination Skin"},
{id:"3831",text:"Estee Lauder Re-Nutriv Crme"},
{id:"3832",text:"Dr. Hauschka Rhythmic Night Conditioner, for All Skin Conditions"},
{id:"3833",text:"Eucerin Daily Defense Body Lotion"},
{id:"3834",text:"Garnier Nutritioniste Refreshing Remover Cleansing Towelettes, Oil-Free"},
{id:"3835",text:"e.l.f. Cosmetics Moisturizing Lipstick"},
{id:"3836",text:"Dr. Dennis Gross Skincare Alpha Beta Daily Face Peel Two-Step System"},
{id:"3837",text:"e.l.f. Cosmetics Studio Contouring Blush & Bronzing Cream"},
{id:"3838",text:"Dr. Denese New York HydroShield Micro 20"},
{id:"3839",text:"e.l.f. Cosmetics Studio Under Eye Concealer & Highlighter"},
{id:"3840",text:"Dr. Brandt Pores No More Pore Refiner"},
{id:"3841",text:"Estee Lauder All-Day Lipstick"},
{id:"3842",text:"Givenchy Makeup Phenomen'Eyes Effet Extension"},
{id:"3843",text:"e.l.f. Cosmetics All Over Cover Stick"},
{id:"3844",text:"Giorgio Armani Luminesscence CC Cream SPF 35"},
{id:"3845",text:"e.l.f. Cosmetics Shimmer Pencil"},
{id:"3846",text:"Flower Beauty Perfect Canvas Creme Foundation"},
{id:"3847",text:"e.l.f. Cosmetics Studio Baked Highlighter"},
{id:"3848",text:"Estee Lauder Clear Difference Advanced Blemish Serum"},
{id:"3849",text:"Estee Lauder Re-Nutriv Ultimate Lift Age Correcting Serum"},
{id:"3850",text:"Estee Lauder Re Nutriv Re-Creation Eye Balm and Re-Creation Night Serum for Eyes"},
{id:"3851",text:"Giorgio Armani Smooth Silk Eye Pencil"},
{id:"3852",text:"Exuviance by NeoStrata Targeted Filler T5"},
{id:"3853",text:"e.l.f. Cosmetics Exfoliating Scrub"},
{id:"3854",text:"Dr. Denese New York Firming Facial Light Resurfacing Peel"},
{id:"3855",text:"Estee Lauder DayWear Advanced Multi-Protection Anti-Oxidant Lotion SPF 15"},
{id:"3856",text:"Estee Lauder TurboLash All Effects Motion Mascara"},
{id:"3857",text:"Estee Lauder Automatic Brow Pencil Duo"},
{id:"3858",text:"Estee Lauder DayWear Advanced Multi-Protection Anti-Oxidant & UV Defense Broad Spectrum SPF 50"},
{id:"3859",text:"First Aid Beauty Facial Radiance Pads"},
{id:"3860",text:"Gold Bond Ultimate Healing Skin Therapy Lotion, Aloe"},
{id:"3861",text:"Estee Lauder Clear Difference Complexion Perfecting BB Creme SPF 35"},
{id:"3862",text:"Dr. Hauschka Lipstick Novum"},
{id:"3863",text:"Elizabeth Arden Flawless Future Powered by Ceramide Eye Gel"},
{id:"3864",text:"Epicuren Discovery Protein Mist Enzyme Activated Toner"},
{id:"3865",text:"Eucerin Smoothing Repair"},
{id:"3866",text:"Dr. Brandt Lineless Infinite Moisture, for All Skin Types"},
{id:"3867",text:"Eucerin Eczema Relief Body Creme"},
{id:"3868",text:"First Aid Beauty Facial Radiance Overnight Mask"},
{id:"3869",text:"e.l.f. Cosmetics Zit Zapping Concealer"},
{id:"3870",text:"Elizabeth Arden Ceramide Plump Perfect Lip Moisture Cream SPF 30"},
{id:"3871",text:"Dr. Hauschka Intensive Treatment 04"},
{id:"3872",text:"Epicuren Discovery Silk Mist Sunscreen SPF 15"},
{id:"3873",text:"Garnier Nutritioniste Ultra-Lift Miracle Sleeping Cream Anti-Age Anti-Fatigue Eye Cream"},
{id:"3874",text:"Epicuren Discovery Facial Emulsion Enzyme Moisturizer"},
{id:"3875",text:"First Aid Beauty Face Cleanser Daily Treatment with FAB Antioxidant Booster"},
{id:"3876",text:"Eucerin Original Healing Soothing Repair Creme"},
{id:"3877",text:"Erno Laszlo Transphuse Age-Defying Mask"},
{id:"3878",text:"Dr. Dennis Gross Skincare Hydration Super Serum Clinical Concentrate Booster"},
{id:"3879",text:"Garnier Nutritioniste Ultra-Lift Targeted Line Smoother Tightener+Cream"},
{id:"3880",text:"Flower Beauty Sheer Up Lip Tint"},
{id:"3881",text:"e.l.f. Cosmetics Studio Hydrating Face Primer"},
{id:"3882",text:"Givenchy Makeup Phenomen'Eyes Waterproof Mascara"},
{id:"3883",text:"Giorgio Armani Crema Nera Obsidian Mineral Regenerating Cream"},
{id:"3884",text:"Dr. Denese New York Peptide Pads Triple Strength Wrinkle Smoother Peptide Pads with Collagen"},
{id:"3885",text:"Estee Lauder Advanced Night Repair Eye Synchronized Complex II"},
{id:"3886",text:"Dr. Denese New York SPF 30 Defense Day Cream"},
{id:"3887",text:"Elizabeth Arden Ceramide Lift and Firm Night Cream"},
{id:"3888",text:"Dr. Denese New York FirmaTone Rx Retinol Maximum Firming Serum"},
{id:"3889",text:"Exuviance by NeoStrata Super Retinol Concentrate"},
{id:"3890",text:"Dr. Hauschka Neem Nail Oil Pen"},
{id:"3891",text:"EltaMD UV Spray SPF 46"},
{id:"3892",text:"First Aid Beauty 5 in 1 Eye Cream"},
{id:"3893",text:"Estee Lauder Double Wear Maximum Cover Camouflage Makeup for Face & Body SPF 15"},
{id:"3894",text:"Guerlain Super Aqua Lotion Replumping Toner"},
{id:"3895",text:"Dr. Dennis Gross Skincare Hydra-Pure Smart Spray"},
{id:"3896",text:"Estee Lauder Advanced Time Zone Age Reversing Line/Wrinkle Crme Broad Spectrum SPF 15  Dry Skin"},
{id:"3897",text:"e.l.f. Cosmetics Volumizing Mascara"},
{id:"3898",text:"e.l.f. Cosmetics Mineral Eyeshadow Primer"},
{id:"3899",text:"GlamGlow Plumprageous Matte Lip Treatment"},
{id:"3900",text:"Eucerin Daily Protection Body Lotion SPF 15"},
{id:"3901",text:"Estee Lauder Advanced Time Zone Night Age Reversing Line/Wrinkle Crme"},
{id:"3902",text:"e.l.f. Cosmetics Studio Shimmering Palette (aka Shimmer Palette)"},
{id:"3903",text:"Dr. Dennis Gross Skincare Alpha Beta Ultra Gentle Daily Peel for Sensitive Skin"},
{id:"3904",text:"Eucerin Q10 Anti-Wrinkle Sensitive Skin Creme"},
{id:"3905",text:"Dr. Denese New York Hydrating Cleanser"},
{id:"3906",text:"e.l.f. Cosmetics Studio Defining Mascara"},
{id:"3907",text:"Erno Laszlo Active Phelityl Intensive Cream"},
{id:"3908",text:"Dr. Jart BB Dis-A-Pore Beauty Balm SPF 30"},
{id:"3909",text:"fresh Sugar Shine Tinted Lip Treatment"},
{id:"3910",text:"Garnier Nutritioniste Skin Renew Dark Spot Overnight Peel"},
{id:"3911",text:"Dr. Hauschka Translucent Face Powder, Loose"},
{id:"3912",text:"Estee Lauder Automatic Lip Pencil Duo"},
{id:"3913",text:"e.l.f. Cosmetics Studio Baked Bronzer"},
{id:"3914",text:"Exuviance by NeoStrata Hydrating Eye Complex"},
{id:"3915",text:"Elizabeth Arden Ceramide Capsules Daily Youth Restoring Eye Serum"},
{id:"3916",text:"Dr. Hauschka Moisturizing Mask, for Dry, Sensitive and Mature Skin"},
{id:"3917",text:"e.l.f. Cosmetics Studio Makeup Remover Pen"},
{id:"3918",text:"Guerlain Blanc de Perle Lotion"},
{id:"3919",text:"Estee Lauder Re-Nutriv Replenishing Comfort Eye Creme"},
{id:"3920",text:"Garnier Nutritioniste Ultra-Lift Transformer Anti-Age Skin Corrector Broad Spectrum SPF 20 Sunscreen"},
{id:"3921",text:"Estee Lauder Double Wear Stay-in-Place High Cover Concealer SPF 35"},
{id:"3922",text:"Dr. Denese New York Glow Younger Self Tanning Glove"},
{id:"3923",text:"Dr. Hauschka Eye Contour Day Balm, for All Skin Conditions"},
{id:"3924",text:"Estee Lauder Resilience Lift Extreme Radiant Lifting Makeup SPF 15"},
{id:"3925",text:"Dr. Brandt Lineless Cream, Age-Inhibitor Complex, for All Skin Types"},
{id:"3926",text:"Estee Lauder Double Wear Stay-in-Place Brow Lift Duo"},
{id:"3927",text:"Dr. Denese New York SPF 30 Neck Defense Day Cream UVA/UVB"},
{id:"3928",text:"Elizabeth Arden Prevage Day Intensive Anti-Aging Moisture Cream SPF 30"},
{id:"3929",text:"Garnier Nutritioniste Moisture Rescue Refreshing Gel-Cream"},
{id:"3930",text:"Epicuren Discovery Skin Lightener Balancing Cream"},
{id:"3931",text:"Epicuren Discovery Lancer Gel"},
{id:"3932",text:"Estee Lauder Double Wear Stay-in-Place Eye Pencil"},
{id:"3933",text:"Epicuren Discovery Chai Soy Mud Mask"},
{id:"3934",text:"Estee Lauder Pure Color Gloss"},
{id:"3935",text:"Exuviance by NeoStrata Ultra Restorative Creme"},
{id:"3936",text:"Giorgio Armani Maestro Fusion Makeup SPF 15"},
{id:"3937",text:"Estee Lauder Double Wear Stay-in-Place Lip Pencil"},
{id:"3938",text:"Dr. Hauschka Rejuvenating Mask, for All Skin Conditions"},
{id:"3939",text:"Dr. Denese New York Pro C Eye Illuminating Treatment"},
{id:"3940",text:"Elizabeth Arden Flawless Finish Bare Perfection Makeup SPF 8"},
{id:"3941",text:"Estee Lauder Double Matte Oil-Control Pressed Powder"},
{id:"3942",text:"fresh Black Tea Age-Delay Instant Infusion Treatment Toner"},
{id:"3943",text:"Estee Lauder Courrges Lip Visor"},
{id:"3944",text:"Flower Beauty About Face Liquid Foundation"},
{id:"3945",text:"Dr. Denese New York Firming & Resurfacing Eye Pads"},
{id:"3946",text:"Dr. Hauschka Lip Balm, for All Skin Conditions"},
{id:"3947",text:"Givenchy Makeup Le Prisme Sun Visage Mat Soft Compact Face Powder"},
{id:"3948",text:"Exuviance by NeoStrata Sheer Daily Protector Sunscreen Broad Spectrum SPF 50 PA++++"},
{id:"3949",text:"Exuviance by NeoStrata Skin Healthy Home Resurfacing Peel System"},
{id:"3950",text:"Exuviance by NeoStrata Evening Restorative Complex"},
{id:"3951",text:"Dr. Brandt Flexitone BB Cream SPF 30"},
{id:"3952",text:"Eucerin Aquaphor Healing Ointment"},
{id:"3953",text:"e.l.f. Cosmetics Nourishing Night Cream"},
{id:"3954",text:"Dr. Hauschka Melissa Day Cream"},
{id:"3955",text:"Estee Lauder DayWear Advanced Multi-Protection Anti-Oxidant Creme SPF 15 Dry Skin"},
{id:"3956",text:"Dr. Dennis Gross Skincare Trifix Acne Clearing Lotion"},
{id:"3957",text:"Dr. Hauschka Moisturizing Day Cream, for Normal, Dry or Mature Skin"},
{id:"3958",text:"Estee Lauder Advanced Night Repair Synchronized Recovery Complex II"},
{id:"3959",text:"Estee Lauder Lucidity Translucent Loose Powder"},
{id:"3960",text:"Flower Beauty Kiss Stick Velvet Lip Color"},
{id:"3961",text:"Elizabeth Arden Beautiful Color Brow Shaper & Eyeliner"},
{id:"3962",text:"fresh Umbrian Clay Face Lotion, Oil-Free"},
{id:"3963",text:"Givenchy Makeup Rouge Interdit Shine Lipstick"},
{id:"3964",text:"e.l.f. Cosmetics Eyebrow Lifter & Filler"},
{id:"3965",text:"e.l.f. Cosmetics Studio Cream Eyeliner"},
{id:"3966",text:"Gold Bond Ultimate Firming Neck & Chest Cream"},
{id:"3967",text:"fresh Sugar Lip Treatment SPF 15"},
{id:"3968",text:"Dr. Dennis Gross Skincare Ferulic + Retinol Fibroblast Anti-Aging Moisturizer"},
{id:"3969",text:"Estee Lauder Re-Nutriv Ultimate Radiance Makeup SPF 15"},
{id:"3970",text:"Flower Beauty Double Take Eye Marker and Creme Shadow"},
{id:"3971",text:"Estee Lauder Double Wear Light Stay-in-Place Makeup"},
{id:"3972",text:"Dr. Hauschka Intensive Treatment 03"},
{id:"3973",text:"Flower Beauty Lip Suede Velvet Lip Chubby"},
{id:"3974",text:"e.l.f. Cosmetics Studio Matte Lip Color"},
{id:"3975",text:"Dove Purely Pampering Nourishing Body Wash, Almond Cream with Hibiscus"},
{id:"3976",text:"Dr. Dennis Gross Skincare Clinical Concentrate Radiance Booster"},
{id:"3977",text:"Garnier Nutritioniste Ultra-Lift 2-in-1 Wrinkle Reducer Serum + Moisturizer"},
{id:"3978",text:"Estee Lauder Advanced Time Zone Age Reversing Line/Wrinkle Eye Crme"},
{id:"3979",text:"First Aid Beauty Milk Oil Conditioning Cleanser"},
{id:"3980",text:"brand product"},
{id:"3981",text:"Lancome Modele Sourcils Brow Groomer"},
{id:"3982",text:"Make Up For Ever Super Lip Gloss"},
{id:"3983",text:"Lorac POREfection Foundation SPF 20"},
{id:"3984",text:"M.A.C. Studio Tech Foundation"},
{id:"3985",text:"Lancome Renergie Eye, Anti-Wrinkle and Firming Eye Creme"},
{id:"3986",text:"Make Up For Ever So Divine, Moisturizing Cleansing Cream"},
{id:"3987",text:"Lab Series Skincare for Men Age Rescue Eye Therapy"},
{id:"3988",text:"Mary Kay Brow Gel"},
{id:"3989",text:"Lancome Bienfait Teinte BB Cream Sunscreen Broad Spectrum SPF 30"},
{id:"3990",text:"Mario Badescu Whitening Mask"},
{id:"3991",text:"Laura Geller Creme Couture Soft Touch Matte Lipstick"},
{id:"3992",text:"Lorac Front of the Line PRO Waterproof Eyeliner"},
{id:"3993",text:"Lancer Skincare The Method: Polish"},
{id:"3994",text:"Mary Kay Botanical Effects Freshen 3"},
{id:"3995",text:"Laura Mercier Illuminating Tinted Moisturizer SPF 20"},
{id:"3996",text:"Mary Kay Eye Primer"},
{id:"3997",text:"Mally Beauty Lip Defender Clear Liner and Magnifier"},
{id:"3998",text:"Lancome Renergie Lift Volumetry Night Volumetric Lifting and Reshaping Night Cream"},
{id:"3999",text:"Laura Mercier Pressed Setting Powder"},
{id:"4000",text:"M.A.C. Lightful Moisture Creme"},
{id:"4001",text:"Mary Kay Timewise Repair Day Cream Sunscreen Broad Spectrum SPF 30-Volu-Firm"},
{id:"4002",text:"Lancome High Resolution Eye Refill 3X"},
{id:"4003",text:"M.A.C. Eye Shadows Matte"},
{id:"4004",text:"Marcelle Waterproof Lip Definition Crayon"},
{id:"4005",text:"Lancome Le Crayon Lip Contour"},
{id:"4006",text:"Laura Mercier Lip Balm"},
{id:"4007",text:"M.A.C. Lipsticks Mattes"},
{id:"4008",text:"Maybelline New York The Colossal Volum Express Waterproof Mascara"},
{id:"4009",text:"Laura Geller Lip Spackle"},
{id:"4010",text:"Lancome LExtreme Waterproof Instant Extensions Lengthening Mascara"},
{id:"4011",text:"Mario Badescu Glycolic Eye Cream"},
{id:"4012",text:"Lancome Juicy Tubes Ultra Shiny Lip Gloss"},
{id:"4013",text:"Maybelline New York Eye Studio Color Molten Duo"},
{id:"4014",text:"Make Up For Ever HD Elixir"},
{id:"4015",text:"Mario Badescu Honey Moisturizer"},
{id:"4016",text:"M.A.C. Eye Shadows  Veluxe Pearl"},
{id:"4017",text:"Make Up For Ever Aqua Rouge Waterproof Liquid Lip Color"},
{id:"4018",text:"Marc Jacobs Beauty Style Eye-Con No. 3 Plush Shadow"},
{id:"4019",text:"Lancer Skincare The Method: Nourish Blemish Control"},
{id:"4020",text:"Lancome Absolue LExtrait Ultimate Eye Contour Collection"},
{id:"4021",text:"Lorac Behind the Scenes Eye Primer"},
{id:"4022",text:"Make Up For Ever Eyelashes Individual"},
{id:"4023",text:"M.A.C. Studio Fix Powder Plus Foundation"},
{id:"4024",text:"Maybelline New York The Mega Plush Volum Express Mascara"},
{id:"4025",text:"Maybelline New York Vivid Matte Liquid Lipstick by ColorSensational"},
{id:"4026",text:"Laura Mercier Waterproof Mascara"},
{id:"4027",text:"Mally Beauty Dual Action Makeup Remover"},
{id:"4028",text:"Laura Mercier Lip Plumper"},
{id:"4029",text:"Mally Beauty Perfect Prep Poreless Primer"},
{id:"4030",text:"Make Up For Ever HD Microfinish Blush"},
{id:"4031",text:"Laura Mercier Bronzing Pressed Powder"},
{id:"4032",text:"Marcelle Revival Gold Beauty Elixir Night Illuminator"},
{id:"4033",text:"Laura Mercier Creme Eye Liner"},
{id:"4034",text:"Maybelline New York Coverstick Corrector Concealer"},
{id:"4035",text:"Lorac MakeupPREP Gentle Skin Resurfacer"},
{id:"4036",text:"Laura Geller Eye Spackle"},
{id:"4037",text:"Make Up For Ever HD Blush"},
{id:"4038",text:"Mary Kay Sheer Mineral Pressed Powder"},
{id:"4039",text:"Lorac Multiplex 3-D Lashes Volumizing & Thickening Mascara"},
{id:"4040",text:"Lancome Teint Visionnaire Skin Correcting Makeup Duo Dark Spots Pores Fine Lines SPF 20"},
{id:"4041",text:"M.A.C. Lightful C Vibrancy Eye Cream"},
{id:"4042",text:"Laura Geller Brow Marker Long-Lasting Brow Color"},
{id:"4043",text:"Mary Kay Timewise Body Hand and Dcollet Cream Sunscreen SPF 15"},
{id:"4044",text:"Liz Earle Naturally Active Skin Care Skin Repair Moisturiser Light  Combination/Oily"},
{id:"4045",text:"Lancome Effacil, Gentle Eye Makeup Remover"},
{id:"4046",text:"M.A.C. Mineralize Concealer"},
{id:"4047",text:"Lancome Color Design Eye Brightening All-in-One 5 Shadow & Liner Palette"},
{id:"4048",text:"Lancome Renergie Eye Multiple Action Ultimate Eye Care Duo"},
{id:"4049",text:"Lancome Eau Fraiche Douceur"},
{id:"4050",text:"Liz Earle Naturally Active Skin Care Brightening Treatment Mask"},
{id:"4051",text:"Lubriderm Advanced Therapy Lotion SPF 30"},
{id:"4052",text:"Maybelline New York Lash Discovery Mini Brush Mascara"},
{id:"4053",text:"Make Up For Ever Full Cover Concealer"},
{id:"4054",text:"Mary Kay Botanical Effects Hydrate 3"},
{id:"4055",text:"Lancome Blush Subtil Delicate Oil-Free Powder Blush"},
{id:"4056",text:"Make Up For Ever Matte Lip"},
{id:"4057",text:"M.A.C. Mineralize Charged Water Face And Body Lotion"},
{id:"4058",text:"Laura Mercier Flawless Skin Repair Oil-Free Day Lotion SPF 15"},
{id:"4059",text:"Mary Kay Bronzing Powder"},
{id:"4060",text:"Marc Jacobs Beauty Remedy Concealer Pen"},
{id:"4061",text:"Mary Kay TimeWise Day Solution Sunscreen Broad Spectrum SPF 35"},
{id:"4062",text:"M.A.C. Cremestick Liner"},
{id:"4063",text:"Mary Kay Timewise Repair Lifting Serum-Volu-Firm"},
{id:"4064",text:"Mario Badescu Summer Shine Body Lotion"},
{id:"4065",text:"Mario Badescu Olive Eye Cream"},
{id:"4066",text:"Marcelle New*Age Precision Anti-Wrinkle + Firming Day Cream"},
{id:"4067",text:"Lubriderm Soothing Relief Daily Lotion"},
{id:"4068",text:"Lancome Flash Bronzer Oil-Free Tinted Self-Tanning Face Lotion with Pure Vitamin E"},
{id:"4069",text:"Lorac Visual Effects Curling, Separating, and Lengthening Mascara"},
{id:"4070",text:"Laura Mercier Brow Powder Duo"},
{id:"4071",text:"Laneige Bright Renew Original Serum"},
{id:"4072",text:"M.A.C. Studio Sculpt SPF 15 Foundation"},
{id:"4073",text:"Lancome Translucence Mattifying Silky Pressed Powder"},
{id:"4074",text:"Lab Series Skincare for Men Power Protector Broad Spectrum SPF 50"},
{id:"4075",text:"Marc Jacobs Beauty Perfection Powder Featherweight Foundation"},
{id:"4076",text:"Lancome Genifique Eye Youth Activating Eye Concentrate"},
{id:"4077",text:"Make Up For Ever Lab Shine Lip Gloss"},
{id:"4078",text:"Lancome Definicils Waterproof High Definition Mascara"},
{id:"4079",text:"Mary Kay Concealer"},
{id:"4080",text:"M.A.C. Zoom Lash Mascara"},
{id:"4081",text:"Mary Kay Clearproof Oil-Free Moisturizer for Acne-Prone Skin"},
{id:"4082",text:"M.A.C. Pro Longwear Lipcolour"},
{id:"4083",text:"Laura Geller Sugar Free Matte Baked Eyeshadow Duo"},
{id:"4084",text:"Lancome Renergie French Lift"},
{id:"4085",text:"Lancome Nude Miracle SPF 15 Liquid-To-Powder Makeup"},
{id:"4086",text:"Mario Badescu Control Moisturizer for Oily Skin"},
{id:"4087",text:"Marc Jacobs Beauty Shameless Bold Blush"},
{id:"4088",text:"Mario Badescu Ceramide Herbal Eye Cream"},
{id:"4089",text:"M.A.C. Select Sheer Pressed Powder"},
{id:"4090",text:"M.A.C. Blot Powder Pressed"},
{id:"4091",text:"Maybelline New York The Falsies Volum Express Mascara"},
{id:"4092",text:"Mary Kay TimeWise Microdermabrasion Set"},
{id:"4093",text:"Make Up For Ever Star Powder"},
{id:"4094",text:"Make Up For Ever Powder Blush"},
{id:"4095",text:"Maybelline New York Color Tattoo Pure Pigments"},
{id:"4096",text:"M.A.C. Mineralize Charged Water Moisture Eye Creme"},
{id:"4097",text:"M.A.C. Prep + Prime CC Colour Correcting Loose"},
{id:"4098",text:"Mary Kay CC Cream Broad Spectrum SPF 15"},
{id:"4099",text:"Mario Badescu Kera Moist Cream"},
{id:"4100",text:"Lab Series Skincare for Men Multi-Action Face Wash"},
{id:"4101",text:"Make Up For Ever Super Matte Loose Powder"},
{id:"4102",text:"Make Up For Ever Cake Eyeliner"},
{id:"4103",text:"Make Up For Ever Pro Finish Multi-Use Powder Foundation"},
{id:"4104",text:"Lorac Special Effects Conditioning Primer and Defining & Lengthening Mascara"},
{id:"4105",text:"Maybelline New York Superstay Concealer 24HR"},
{id:"4106",text:"Marcelle Hydra-C Ultra-Light Matifying Fluid"},
{id:"4107",text:"Lorac Creamy Brow Pencil"},
{id:"4108",text:"M.A.C. Studio Fix Boldblack Lash"},
{id:"4109",text:"Laura Mercier Tinted Moisturizer Broad Spectrum SPF 20 Sunscreen"},
{id:"4110",text:"Laneige Water Bank Gel Cream"},
{id:"4111",text:"Mary Kay TimeWise Moisture Renewing Gel Mask"},
{id:"4112",text:"Laura Mercier Flawless Skin Repair Eye Crme"},
{id:"4113",text:"Laneige Multiberry Yogurt Peeling Gel"},
{id:"4114",text:"Mary Kay Eyeliner"},
{id:"4115",text:"Marcelle I-Bronze Self-Tanning Cloths"},
{id:"4116",text:"Make Up For Ever Sculpting Kit"},
{id:"4117",text:"M.A.C. In Extreme Dimensions 3D Black Lash"},
{id:"4118",text:"Make Up For Ever HD High Definition Pressed Powder"},
{id:"4119",text:"Mario Badescu Aloe Lotion"},
{id:"4120",text:"Mary Kay Satin Hands Peach Hand Cream"},
{id:"4121",text:"M.A.C. In Extreme Dimension Lash Mascara"},
{id:"4122",text:"Laura Mercier Rouge Nouveau Weightless Lip Colour"},
{id:"4123",text:"Mally Beauty Ultimate Performance Professional Foundation"},
{id:"4124",text:"Laura Geller Waterproof Eye Spackle"},
{id:"4125",text:"Make Up For Ever Face and Body Liquid Makeup"},
{id:"4126",text:"Laneige Perfect Renew Cream"},
{id:"4127",text:"Marcelle Revival Gold Beauty Elixir Day Illuminator"},
{id:"4128",text:"Lancome Hypnose Custom Volume Mascara"},
{id:"4129",text:"Laura Geller I-Care Waterproof Eyeliner"},
{id:"4130",text:"M.A.C. Pro Longwear Lip Pencil"},
{id:"4131",text:"Lancome Renergie Night, Night Treatment"},
{id:"4132",text:"Marcelle Power Volume Mascara"},
{id:"4133",text:"Mally Beauty Volumizing Mascara"},
{id:"4134",text:"Mary Kay Lip Liner"},
{id:"4135",text:"Mary Kay MKMen Face Bar"},
{id:"4136",text:"M.A.C. Huggable Lipcolour"},
{id:"4137",text:"Laura Mercier Matte Eye Colour"},
{id:"4138",text:"Make Up For Ever Rouge Artist Intense"},
{id:"4139",text:"Lancome Blush Subtil Palette"},
{id:"4140",text:"M.A.C. Sheen Supreme Lipglass"},
{id:"4141",text:"Lorac Vitamin E Stick"},
{id:"4142",text:"Laura Mercier Loose Setting Powder"},
{id:"4143",text:"Laura Mercier Foundation PrimerMineral"},
{id:"4144",text:"Lancome Hypnose Doll Lashes Mascara"},
{id:"4145",text:"Marc Jacobs Beauty Brow Tamer Grooming Gel"},
{id:"4146",text:"Marcelle Waterproof Eyeliner"},
{id:"4147",text:"Lorac 3-in-1 Waterproof Eyeliner Pencil"},
{id:"4148",text:"Lab Series Skincare for Men Maximum Comfort Shave Cream Jar"},
{id:"4149",text:"Lancome Rnergie Lift Multi-Action Night"},
{id:"4150",text:"Lancome Grandiose Mascara"},
{id:"4151",text:"M.A.C. Liquid Eye Liner"},
{id:"4152",text:"Mally Beauty Evercolor Poreless Face Defender"},
{id:"4153",text:"Lab Series Skincare for Men Skin Revitalizer Lotion"},
{id:"4154",text:"Mary Kay Botanical Effects Cleanse 2"},
{id:"4155",text:"Make Up For Ever Diamond Powder"},
{id:"4156",text:"Mario Badescu Hyaluronic Eye Cream"},
{id:"4157",text:"Marcelle Revival Pro-Sculpt Day Care"},
{id:"4158",text:"Lorac Natural Performance Foundation"},
{id:"4159",text:"M.A.C. Lightful C Marine-Bright Formula Essence/Serum"},
{id:"4160",text:"Laneige Deep Pore Cleansing Foam"},
{id:"4161",text:"M.A.C. Matchmaster Concealer"},
{id:"4162",text:"Mary Kay Lash Love Mascara"},
{id:"4163",text:"Laura Geller Spackle Under Makeup Primer"},
{id:"4164",text:"Lab Series Skincare for Men Razor Burn Relief Ultra"},
{id:"4165",text:"Laura Geller Spackle Trio"},
{id:"4166",text:"Lancome Flash Bronzer Tinted Self-Tanning Body Gel"},
{id:"4167",text:"M.A.C. Mineralize Moisture SPF 15 Foundation"},
{id:"4168",text:"Laura Geller Sugared Baked Pearl Eyeshadow"},
{id:"4169",text:"Laura Mercier Eye Basics"},
{id:"4170",text:"Mario Badescu Cucumber Make-Up Remover Cream"},
{id:"4171",text:"M.A.C. Complete Comfort Creme"},
{id:"4172",text:"Laura Mercier Flawless Skin Repair Day Creme SPF 15"},
{id:"4173",text:"Laura Geller Spackle Tinted Under Make-Up Primer"},
{id:"4174",text:"Mario Badescu Keratoplast Cream Soap"},
{id:"4175",text:"Lab Series Skincare for Men Pro LS All-In-One Face Treatment"},
{id:"4176",text:"Lancome Creme Mousse Confort, Comforting Creamy Foaming Cleanser"},
{id:"4177",text:"Lorac TANtalizer Baked Matte Satin Bronzer SPF 15"},
{id:"4178",text:"Laura Mercier Creme Smooth Foundation"},
{id:"4179",text:"Make Up For Ever Aqua Brow"},
{id:"4180",text:"Make Up For Ever Diamond Shadow"},
{id:"4181",text:"Maybelline New York Master Hi-Light by Face Studio"},
{id:"4182",text:"Maybelline New York Illegal Length Fiber Extensions Mascara"},
{id:"4183",text:"Laura Mercier Paint Wash Liquid Lip Color"},
{id:"4184",text:"Make Up For Ever Lip Palettes"},
{id:"4185",text:"Mario Badescu Facial Spray with Aloe, Herbs, and Rosewater"},
{id:"4186",text:"M.A.C. Lip Pencils"},
{id:"4187",text:"Maybelline New York Baby Skin Instant Pore Eraser Primer"},
{id:"4188",text:"Mario Badescu Kiwi Face Scrub"},
{id:"4189",text:"Laura Geller Baked Cake Eyeliner Duo"},
{id:"4190",text:"M.A.C. Lightful C Marine-Bright Formula SPF 30 Moisturizer Broad Spectrum SPF 30"},
{id:"4191",text:"Laura Mercier Eye Brow Pencil"},
{id:"4192",text:"Maybelline New York Eyestudio by Maybelline Color Plush Silk Eyeshadow"},
{id:"4193",text:"Lubriderm Advanced Therapy Lotion"},
{id:"4194",text:"Marc Jacobs Beauty Highliner Gel Eye Crayon"},
{id:"4195",text:"Liz Earle Naturally Active Skin Care After-Shaving Moisturiser"},
{id:"4196",text:"Laura Mercier Bronzing Gel"},
{id:"4197",text:"Lancome Color Design Sensational Effects Lipcolor"},
{id:"4198",text:"Lancome Bienfait Multi-Vital SPF 30 Cream"},
{id:"4199",text:"Mally Beauty Ultimate Performance Dream Brow with Brush"},
{id:"4200",text:"Mario Badescu Seaweed Cleansing Soap"},
{id:"4201",text:"Marcelle New Age Precision 8-in-1 Power Serum"},
{id:"4202",text:"M.A.C. Fast Response Eye Cream"},
{id:"4203",text:"Lab Series Skincare for Men MAX LS Daily Renewing Cleanser"},
{id:"4204",text:"Mary Kay MKMen Advanced Eye Cream"},
{id:"4205",text:"Laura Mercier Baked Eye Colour"},
{id:"4206",text:"M.A.C. Mineralize Skinfinish/Natural"},
{id:"4207",text:"Maybelline New York Instant Age Rewind Eraser Dark Circles Treatment Concealer"},
{id:"4208",text:"Mario Badescu Protein Night Cream"},
{id:"4209",text:"Lab Series Skincare for Men Max LS Age-Less Face Cream"},
{id:"4210",text:"Lancome Star Bronzer Long Lasting Bronzing Powder Natural Glow"},
{id:"4211",text:"Liz Earle Naturally Active Skin Care Superbalm"},
{id:"4212",text:"Lancome Color Design Sensational Effects Eye Shadow Smooth Hold"},
{id:"4213",text:"Laura Mercier Lip Pencil"},
{id:"4214",text:"Mario Badescu Calma Mask"},
{id:"4215",text:"Mary Kay Mineral Powder Foundation"},
{id:"4216",text:"Laura Mercier Secret Brightener"},
{id:"4217",text:"Maybelline New York Instant Age Rewind The Perfector Primer Inside Skin-Smoothing Powder"},
{id:"4218",text:"Mally Beauty Perfect Prep Poreless Illuminating Primer"},
{id:"4219",text:"Mario Badescu Eye Make-Up Remover Gel (Non Oily)"},
{id:"4220",text:"M.A.C. Lipmix"},
{id:"4221",text:"Make Up For Ever Brow Seal Transparent Gel"},
{id:"4222",text:"M.A.C. Prep + Prime Lash"},
{id:"4223",text:"M.A.C. Mineralize Skinfinish"},
{id:"4224",text:"Mary Kay Clearproof Pore Purifying Serum"},
{id:"4225",text:"Make Up For Ever Step 1 Skin Equalizer Smoothing Primer"},
{id:"4226",text:"Mario Badescu Acne Facial Cleanser"},
{id:"4227",text:"Mary Kay Botanical Effects Mask 3"},
{id:"4228",text:"Mary Kay TimeWise Repair Volu-Fill Deep Wrinkle Filler"},
{id:"4229",text:"Lorac Pro Brow Pencil"},
{id:"4230",text:"Make Up For Ever Mat Velvet + Mattifying Foundation"},
{id:"4231",text:"Lorac Oil-free Luminizer"},
{id:"4232",text:"M.A.C. M.A.C. Pro Chromaline"},
{id:"4233",text:"Make Up For Ever Aqua Cream Waterproof Cream Color"},
{id:"4234",text:"M.A.C. Powder Blush Sheertone/Sheertone Shimmer"},
{id:"4235",text:"Laneige Time Freeze Wrinkle Filler"},
{id:"4236",text:"Marc Jacobs Beauty O!Mega Bronze  Perfect Tan"},
{id:"4237",text:"Mario Badescu Special Hand Cream with Vitamin E"},
{id:"4238",text:"M.A.C. Eye Shadows Veluxe"},
{id:"4239",text:"Laura Geller BB Cream All-in-One Skin Perfecting Beauty Balm SPF 21"},
{id:"4240",text:"Lorac Multiplex 3D Lip Gloss"},
{id:"4241",text:"Laura Mercier Oil-Free Supreme Foundation"},
{id:"4242",text:"Mary Kay Mechanical Brow Liner"},
{id:"4243",text:"Mario Badescu Glycolic Skin Renewal Complex"},
{id:"4244",text:"Lancome Teint Idole Fresh Wear 18 Hour Shine-Free Makeup SPF 15 Sunscreen"},
{id:"4245",text:"Laura Mercier Eye Brow Gel"},
{id:"4246",text:"Laura Mercier Silk Crme Moisturizing Photo Edition Foundation"},
{id:"4247",text:"Laura Geller Liquid Candlelight Face & Body Glow"},
{id:"4248",text:"Make Up For Ever Shine-On Powder"},
{id:"4249",text:"Lancome Absolue Premium Bx Advanced Replenishing Cream Cleanser"},
{id:"4250",text:"Make Up For Ever Step 1 Skin Equalizer Hydrating Primer"},
{id:"4251",text:"Make Up For Ever Kohl Eye Pencil"},
{id:"4252",text:"Liz Earle Naturally Active Skin Care Face Scrub"},
{id:"4253",text:"Laura Mercier Flawless Skin Purifying Cleansing Oil"},
{id:"4254",text:"Mary Kay TimeWise Luminous-Wear Liquid Foundation"},
{id:"4255",text:"Lancome Renergie Lift Makeup SPF 20"},
{id:"4256",text:"Mary Kay Creme-to-Powder Foundation"},
{id:"4257",text:"M.A.C. Select Cover-Up"},
{id:"4258",text:"Lorac Pro Cream Eyeliner"},
{id:"4259",text:"Laura Mercier Faux Lash Mascara"},
{id:"4260",text:"Laura Mercier Metallic Creme Eye Colour"},
{id:"4261",text:"Lancome Renergie Lift Multi-Action Reviva-Concentrate"},
{id:"4262",text:"Lancome Absolue Ultimate Bx Replenishing and Restructuring Serum"},
{id:"4263",text:"Lancome Effacernes Waterproof Long-Lasting Undereye Concealer"},
{id:"4264",text:"Maybelline New York The Rocket Volum Express Waterproof Mascara"},
{id:"4265",text:"Mario Badescu Special Mask For Oily Skin"},
{id:"4266",text:"Mary Kay MKMen Shave Foam"},
{id:"4267",text:"Lancome Absolue Powder Radiant Smoothing Powder"},
{id:"4268",text:"Lancome Absolue Premium Bx Advanced Replenishing Toner"},
{id:"4269",text:"Lancome Ageless Minerale Skin-Transforming Mineral Powder Foundation with White Sapphire Complex SPF 21"},
{id:"4270",text:"Mary Kay TimeWise Age-Fighting Moisturizer, for Combination/Oily Skin"},
{id:"4271",text:"Mary Kay Clearproof Blemish Control Toner"},
{id:"4272",text:"Mary Kay Classic Blonde Brow Definer Pencil (wood)"},
{id:"4273",text:"Laura Geller Crease-Less Concealer"},
{id:"4274",text:"Lab Series Skincare for Men Instant Moisture Gel"},
{id:"4275",text:"Lancome Le Lipstique LipColouring Stick with Brush"},
{id:"4276",text:"M.A.C. Mineralize Timecheck Lotion"},
{id:"4277",text:"Marcelle Hydra-C Gentle Self-Foaming Cleanser"},
{id:"4278",text:"M.A.C. Studio Moisture Cream"},
{id:"4279",text:"Liz Earle Naturally Active Skin Care Energising Body Scrub"},
{id:"4280",text:"Maybelline New York Great Lash Waterproof Mascara"},
{id:"4281",text:"Laura Mercier Lip Glace"},
{id:"4282",text:"Lancome Absolue Precious Cells Advanced Regenerating and Reconstructing Cream SPF 15"},
{id:"4283",text:"Lorac ProtecTINT SPF 30 Oil-Free Tinted Moisturizer"},
{id:"4284",text:"Make Up For Ever Aqua Black Waterproof Cream Eye Shadow"},
{id:"4285",text:"Maybelline New York Dream Pure BB 8-in-1 Beauty Balm Skin Clearing Perfector 2% Salicylic Acid Acne Treatment"},
{id:"4286",text:"Mary Kay Sunscreen Broad Spectrum SPF 30"},
{id:"4287",text:"M.A.C. Prep + Prime Fix +"},
{id:"4288",text:"Maybelline New York Fit Me! Blush"},
{id:"4289",text:"Mario Badescu Alpha Grapefruit Cleansing Lotion"},
{id:"4290",text:"Mary Kay TimeWise Matte-Wear Liquid Foundation"},
{id:"4291",text:"Maybelline New York Mineral Power Bronzer Shimmer Loose Powder"},
{id:"4292",text:"Laura Mercier Flawless Skin Perfecting Water Moisture Mist"},
{id:"4293",text:"Make Up For Ever Ultra HD Invisible Cover Stick Foundation"},
{id:"4294",text:"Maybelline New York Expertwear Defining Liner"},
{id:"4295",text:"M.A.C. Eye Kohl"},
{id:"4296",text:"Maybelline New York Dream Liquid Mousse Airbrush Finish"},
{id:"4297",text:"M.A.C. Studio Moisture Fix SPF 15"},
{id:"4298",text:"Laura Mercier Mineral Finishing Powder"},
{id:"4299",text:"M.A.C. M.A.C. Pro Lip Erase"},
{id:"4300",text:"Lorac Starry-Eyed Baked Eyeshadow Trios"},
{id:"4301",text:"Mally Beauty Mally Ultimate Performance Ink Liner"},
{id:"4302",text:"Mario Badescu Strawberry Face Scrub"},
{id:"4303",text:"Lancome Bienfait Multi-Vital Daily Replenishing Oil"},
{id:"4304",text:"Laura Mercier Foundation Primer"},
{id:"4305",text:"M.A.C. Lipsticks Lustre"},
{id:"4306",text:"Lancome Flash Bronzer Anti-Age Tinted Anti-Age Self-Tanning Face Lotion SPF 15 Sunscreen"},
{id:"4307",text:"Mario Badescu Special Eye Cream V"},
{id:"4308",text:"Lancome Tropiques Minerale All Over Magic Bronzing Brush"},
{id:"4309",text:"M.A.C. Retro Matte Lipstick"},
{id:"4310",text:"Lancome Renergie Lift Multi-Action Eye"},
{id:"4311",text:"Maybelline New York SuperStay 24 Color"},
{id:"4312",text:"Laura Mercier Kohl Eye Pencil"},
{id:"4313",text:"Mary Kay Medium Coverage Foundation"},
{id:"4314",text:"Mary Kay Lash Primer"},
{id:"4315",text:"M.A.C. Set Powder"},
{id:"4316",text:"Mary Kay Nourishine Plus Lip Gloss"},
{id:"4317",text:"Maybelline New York Volum Express The Falsies Mascara Black Drama"},
{id:"4318",text:"Lancome Hypnose Waterproof Custom Volume Mascara"},
{id:"4319",text:"M.A.C. Cleanse Off Oil"},
{id:"4320",text:"Mally Beauty Waterproof Volumizing Mascara"},
{id:"4321",text:"Maybelline New York Expert Eyes Moisturizing Eye Makeup Remover for Waterproof Makeup"},
{id:"4322",text:"Laura Mercier Smooth Finish Flawless Fluide"},
{id:"4323",text:"Make Up For Ever Aqua Liner"},
{id:"4324",text:"Mary Kay Skinvigorate Cleansing Brush"},
{id:"4325",text:"Lancome Shine Lover Vibrant Shine Lipstick"},
{id:"4326",text:"Lancome Definicils High Definition Mascara"},
{id:"4327",text:"Lorac Couture Shine Liquid Lipstick"},
{id:"4328",text:"Marcelle Ultra Gentle Cleansing Gel"},
{id:"4329",text:"Make Up For Ever Lash Fibers Volume and Length Lash Primer"},
{id:"4330",text:"Mary Kay Mineral Cheek Color"},
{id:"4331",text:"M.A.C. Pro Longwear Nourishing Waterproof Foundation"},
{id:"4332",text:"Laura Mercier Mineral Pressed Powder SPF 15"},
{id:"4333",text:"Lancome Visionnaire Yeux"},
{id:"4334",text:"Lancome Exfoliance Confort Comforting Exfoliating Cream"},
{id:"4335",text:"Lorac Lotsa Lash Fiber Mascara"},
{id:"4336",text:"Marcelle DD Cream Daily Defense SPF 25"},
{id:"4337",text:"M.A.C. Pro Eye Makeup Remover"},
{id:"4338",text:"Mary Kay TimeWise 3-in-1 Cleansing Bar"},
{id:"4339",text:"Laura Mercier Caviar Stick Eye Colour"},
{id:"4340",text:"Make Up For Ever Step 1 Skin Equalizer Redness Correcting Primer"},
{id:"4341",text:"Make Up For Ever Glitter"},
{id:"4342",text:"Laura Mercier Flawless Skin Repair Eye Serum"},
{id:"4343",text:"Lancome Hydra-Intense Masque Hydrating Gel Mask with Botanical Extract"},
{id:"4344",text:"M.A.C. Magically Cool Liquid Powder"},
{id:"4345",text:"Lancome Renergie Lift Volumetry Eye Volumetric Lifting and Reshaping Eye Cream"},
{id:"4346",text:"Make Up For Ever Eyeshadow"},
{id:"4347",text:"Maybelline New York Brow Drama by EyeStudio Sculpting Brow Mascara"},
{id:"4348",text:"Mally Beauty Instant Erase Concealer"},
{id:"4349",text:"Laneige Balancing Emulsion Dry/Normal"},
{id:"4350",text:"M.A.C. Technakohl Liner"},
{id:"4351",text:"Lancome Miracle Cushion Liquid Cushion Compact"},
{id:"4352",text:"Laura Geller Full Figured Lashes Mascara"},
{id:"4353",text:"Lorac On Screen Duo"},
{id:"4354",text:"Lubriderm Daily Moisture Shea + Enriching Cocoa Butter Lotion"},
{id:"4355",text:"Lancome Photogenic Lumessence Light-Mastering & Line-Smoothing Makeup SPF 15"},
{id:"4356",text:"Lancome Rnergie clat Multi-Lift Tinted Skincare"},
{id:"4357",text:"Make Up For Ever Smoky Extravagant Mascara"},
{id:"4358",text:"Mally Beauty City Chick Smokey Eye Kit"},
{id:"4359",text:"Laneige Perfect Renew Firming Eye Cream"},
{id:"4360",text:"Make Up For Ever Eye Pencil"},
{id:"4361",text:"Liz Earle Naturally Active Skin Care Smoothing Line Serum"},
{id:"4362",text:"M.A.C. Prep + Prime Moisture Infusion"},
{id:"4363",text:"Liz Earle Naturally Active Skin Care Skin Repair Moisturiser Dry/Sensitive"},
{id:"4364",text:"Lancome Genifique Youth Activating Cream Serum"},
{id:"4365",text:"Mary Kay Subtle Tanning Lotion"},
{id:"4366",text:"Marc Jacobs Beauty Marvelous Mousse Transformative Foundation"},
{id:"4367",text:"Lancome Color Design Sensational Effects Eye Shadow Quad Smooth Hold"},
{id:"4368",text:"Marcelle Revival Pro-Sculpt Night Care"},
{id:"4369",text:"Mary Kay Botanical Effects Cleanse 3"},
{id:"4370",text:"Lancer Skincare Fade Serum Intense LES-10 Brightening Complex"},
{id:"4371",text:"Lancome Teint Idole Ultra Makeup Stick SPF 21"},
{id:"4372",text:"Maybelline New York Fit Me! Pressed Powder"},
{id:"4373",text:"Laneige Water Bank Serum"},
{id:"4374",text:"M.A.C. M.A.C. Pro Lipmix"},
{id:"4375",text:"Mally Beauty Liquid Face Defender Blush"},
{id:"4376",text:"Lorac PRO Powder Cheek Stain"},
{id:"4377",text:"Mario Badescu Ginseng Moist Cream"},
{id:"4378",text:"M.A.C. Pro Longwear Foundation"},
{id:"4379",text:"Laura Mercier Second Skin Cheek Colour"},
{id:"4380",text:"Lancome Galatee Confort Comforting Milky Creme Cleanser"},
{id:"4381",text:"Marcelle Ultimate Lash Mascara"},
{id:"4382",text:"Laura Mercier StickGloss Lip Colour"},
{id:"4383",text:"Laura Mercier Invisible Loose Setting Powder"},
{id:"4384",text:"Laura Mercier Foundation Primer Radiance Bronze"},
{id:"4385",text:"Laura Geller WOW Factor All-In-One-Mascara"},
{id:"4386",text:"Lancome Advanced Genifique Youth Activating Concentrate"},
{id:"4387",text:"Lancome Bright Expert Dark Spot Corrector"},
{id:"4388",text:"Maybelline New York Expert Eyes Wear 8-Pan Eye Shadow"},
{id:"4389",text:"Laura Mercier Flawless Skin Mega Moisturizer SPF 15 Normal/Dry Skin"},
{id:"4390",text:"Lancome Pure Focus Deep Pore Refining Scrub with Purifying Micro-Beads"},
{id:"4391",text:"Make Up For Ever Matte Eye Shadow"},
{id:"4392",text:"Lubriderm Daily Moisture Lotion Sensitive"},
{id:"4393",text:"Make Up For Ever UV Prime SPF 50/PA +++"},
{id:"4394",text:"Marc Jacobs Beauty Re(marc)able Full Cover Foundation Concentrate"},
{id:"4395",text:"Mary Kay Oil Mattifier"},
{id:"4396",text:"M.A.C. Mineralize Foundation Loose"},
{id:"4397",text:"Lancer Skincare Sheer Fluid Sun Shield SPF 30"},
{id:"4398",text:"Make Up For Ever Lip Liner Pencil"},
{id:"4399",text:"Maybelline New York Expertwear Eye Shadows"},
{id:"4400",text:"Laura Geller Banish & Brighten"},
{id:"4401",text:"M.A.C. Paint"},
{id:"4402",text:"Laura Geller Baked Body Frosting"},
{id:"4403",text:"M.A.C. Pro Longwear Waterproof Brow Set"},
{id:"4404",text:"M.A.C. Mineralize Eye Shadow Duo"},
{id:"4405",text:"Lab Series Skincare for Men Daily Moisture Defense Lotion SPF 15"},
{id:"4406",text:"Lancome Renergie Lift Volumetry Volumetric Lifting and Reshaping Cream SPF 15 Normal to Dry Skin"},
{id:"4407",text:"Make Up For Ever Step 1 Skin Equalizer Mattifying Primer"},
{id:"4408",text:"Mario Badescu Azulene Calming Mask"},
{id:"4409",text:"Lab Series Skincare for Men Power Wash"},
{id:"4410",text:"M.A.C. Pro Longwear Paint Pot"},
{id:"4411",text:"Lab Series Skincare for Men Oil Control Solution"},
{id:"4412",text:"Maybelline New York Color Sensational High Shine Gloss"},
{id:"4413",text:"Lancome High Resolution Refill3x Triple Action Renewal Anti-Wrinkle Cream SPF 15 Sunscreen"},
{id:"4414",text:"M.A.C. Mineralize Blush"},
{id:"4415",text:"Laneige BB Cushion SPF 50+"},
{id:"4416",text:"Laura Mercier Multi-Vitamin Serum"},
{id:"4417",text:"Laneige Bright Renew Emulsion"},
{id:"4418",text:"Lancome Lip Lover"},
{id:"4419",text:"M.A.C. Pro Longwear Concealer"},
{id:"4420",text:"Lancome Blush Subtil Shimmer Delicate Oil-Free Powder Blush"},
{id:"4421",text:"Mary Kay TimeWise Even Complexion Mask"},
{id:"4422",text:"Lancome Absolue Ultimate Night Bx, Intense Night Recovery and Replenishing Serum"},
{id:"4423",text:"Mario Badescu Bee Pollen Night Cream"},
{id:"4424",text:"Mary Kay TimeWise Firming Eye Cream"},
{id:"4425",text:"Maybelline New York Volum' Express One by One Mascara"},
{id:"4426",text:"Marcelle Cream Blush"},
{id:"4427",text:"Laura Geller Color Enriched Anti-Aging Lipstick"},
{id:"4428",text:"Laura Mercier Sateen Eye Colour"},
{id:"4429",text:"M.A.C. Zoom Waterfast Lash"},
{id:"4430",text:"Lorac TANtalizer Highlighter & Matte Bronzer"},
{id:"4431",text:"Laura Mercier Face Polish"},
{id:"4432",text:"Mary Kay Timewise Even Complexion Dark Spot Reducer"},
{id:"4433",text:"Lorac Take A Brow"},
{id:"4434",text:"Liz Earle Naturally Active Skin Care Superskin Eye & Lip Treatment"},
{id:"4435",text:"Mary Kay TimeWise Age-Fighting Moisturizer Sunscreen Broad Spectrum SPF 30"},
{id:"4436",text:"Lancome Renergie Lift Volumetry Volumetric Lifting and Reshaping Cream SPF 15 Normal/Combination Skin"},
{id:"4437",text:"M.A.C. Wipes"},
{id:"4438",text:"Lancome Color Fever Gloss"},
{id:"4439",text:"Lab Series Skincare for Men Maximum Comfort Shave Cream Tube"},
{id:"4440",text:"Marc Jacobs Beauty Style Eye-Con No. 7 Plush Shadow"},
{id:"4441",text:"Make Up For Ever Aqua Creamliner"},
{id:"4442",text:"Make Up For Ever SensEyes, Waterproof Sensitive Eye Cleanser"},
{id:"4443",text:"Laura Mercier Smooth Focus Pressed Setting Powder"},
{id:"4444",text:"Marc Jacobs Beauty O!Mega Lash Volumizing Mascara"},
{id:"4445",text:"Liz Earle Naturally Active Skin Care Superskin Bust & Neck Treatment"},
{id:"4446",text:"Lancome Visionnaire 1 Minute Blur Smoothing Skincare Instant Perfector"},
{id:"4447",text:"Mary Kay Extra Emollient Night Cream"},
{id:"4448",text:"M.A.C. Gently Off Eye and Lip Makeup Remover"},
{id:"4449",text:"Laura Mercier Flawless Skin Tone Perfecting Eye Gel Creme"},
{id:"4450",text:"Lancome Le Stylo Waterproof Long Lasting EyeLiner"},
{id:"4451",text:"Make Up For Ever HD Invisible Cover Concealer"},
{id:"4452",text:"Laura Mercier Luster Eye Colour"},
{id:"4453",text:"Laura Geller Light Beams Ultimate Shine Lip Gloss"},
{id:"4454",text:"Liz Earle Naturally Active Skin Care Deep Cleansing Mask"},
{id:"4455",text:"Laura Mercier Flawless Skin Repair Creme"},
{id:"4456",text:"Make Up For Ever All Mat, Face Matifying Primer"},
{id:"4457",text:"Lancome DreamTone Ultimate Dark Spot Corrector Pigmentation Profile 3  Dark Skin Tone"},
{id:"4458",text:"Lancome Bienfait Pure Focus Mattifying Moisturizing Lotion"},
{id:"4459",text:"Laura Geller Welcome Matte Skin Enhancer"},
{id:"4460",text:"Mary Kay Satin Lips Lip Mask"},
{id:"4461",text:"Laura Mercier Mineral Primer"},
{id:"4462",text:"Lancome Renergie Cream, Anti-Wrinkle and Firming Treatment"},
{id:"4463",text:"Maybelline New York Volum Express Waterproof Mascara 3X"},
{id:"4464",text:"Maybelline New York Great Lash Lots of Lashses"},
{id:"4465",text:"Liz Earle Naturally Active Skin Care Cleanse & Polish Hot Cloth Cleanser"},
{id:"4466",text:"Laura Mercier Sheer Lip Colour"},
{id:"4467",text:"M.A.C. Pearlglide Intense Eye Liner"},
{id:"4468",text:"Lancome Bienfait Aqua Vital Continuous Infusing Moisturizer Oil-Free"},
{id:"4469",text:"Lancome High Resolution Refill 3X Triple Action Renewal Anti Wrinkle Night Cream"},
{id:"4470",text:"Mary Kay Lash & Brow Building Serum"},
{id:"4471",text:"Lubriderm Intense Skin Repair Calming Relief Lotion"},
{id:"4472",text:"Lorac Sheer Lipstick"},
{id:"4473",text:"Lab Series Skincare for Men Age Rescue Face Lotion"},
{id:"4474",text:"Mary Kay Ultimate Mascara"},
{id:"4475",text:"Lancome Absolue Premium Bx, Absolute Replenishing Lotion SPF 15"},
{id:"4476",text:"Laura Mercier Long Lash Mascara"},
{id:"4477",text:"M.A.C. Studio Finish Concealer SPF 35"},
{id:"4478",text:"Mary Kay After-Sun Replenishing Gel"},
{id:"4479",text:"Marcelle Revival Intense Anti-Aging Moisturizing Lotion SPF 15"},
{id:"4480",text:"Lancome Youth Activating Second Skin Mask"},
{id:"4481",text:"M.A.C. Prep + Prime BB Beauty Balm SPF 35"},
{id:"4482",text:"Mario Badescu Revitalin Moisturizer"},
{id:"4483",text:"Laura Geller Bronze-N-Brighten"},
{id:"4484",text:"Mary Kay Creme Lipstick"},
{id:"4485",text:"Lancome Tonique Douceur, Alcohol-Free Freshener"},
{id:"4486",text:"M.A.C. Superslick Liquid Eye Liner"},
{id:"4487",text:"Marcelle 2-in-1 Retractable Eyeliner"},
{id:"4488",text:"Mary Kay Liquid Foundation Brush"},
{id:"4489",text:"M.A.C. Pigment"},
{id:"4490",text:"Lorac Breakthrough Performance Foundation SPF 14"},
{id:"4491",text:"Lab Series Skincare for Men Oil Control Daily Hydrator"},
{id:"4492",text:"M.A.C. Pro Longwear Lipglass"},
{id:"4493",text:"Laura Mercier Mineral Powder SPF 15"},
{id:"4494",text:"Lancome Renergie Lift Volumetry Neck Volumetric Lifting and Reshaping Neck Cream"},
{id:"4495",text:"M.A.C. Prep + Prime Blemish Control Gel"},
{id:"4496",text:"M.A.C. Studio Waterweight SPF 30 Foundation"},
{id:"4497",text:"M.A.C. Powder Blush Mattes"},
{id:"4498",text:"Laura Mercier Crme Smooth Lip Color"},
{id:"4499",text:"Mario Badescu Seaweed Night Cream"},
{id:"4500",text:"Mario Badescu Cucumber Cream Soap"},
{id:"4501",text:"M.A.C. Powerpoint Eye Pencil"},
{id:"4502",text:"Make Up For Ever Ultra HD Invisible Cover Concealer"},
{id:"4503",text:"M.A.C. Studio Face and Body Foundation"},
{id:"4504",text:"Make Up For Ever 5-Camouflage Cream Palette"},
{id:"4505",text:"Lancome Hypnose Star Mascara Volume Ultra-Glamorous Regard Cinema Show-Stopping Volume Mascara"},
{id:"4506",text:"Laura Geller Balance-N-Bronze"},
{id:"4507",text:"M.A.C. Studio Sculpt Lash"},
{id:"4508",text:"Mary Kay Facial Cleansing Cloths"},
{id:"4509",text:"M.A.C. Mineralize Eye Shadow"},
{id:"4510",text:"Laura Mercier Flawless Skin Mega Moisturizer SPF 15 Normal/Combination Skin"},
{id:"4511",text:"Mally Beauty 24/7 Brow Express"},
{id:"4512",text:"Mary Kay Timewise Body Targeted-Action Toning Lotion"},
{id:"4513",text:"M.A.C. Studio Careblend/Pressed Powder"},
{id:"4514",text:"Mary Kay Botanical Effects Mask 2"},
{id:"4515",text:"M.A.C. Full Coverage Foundation"},
{id:"4516",text:"Lancome DreamTone Ultimate Dark Spot Corrector Pigmentation Profile 2  Medium Skin Tone"},
{id:"4517",text:"Make Up For Ever Artist Plexi-Gloss"},
{id:"4518",text:"Mario Badescu Buttermilk Moisturizer"},
{id:"4519",text:"Lancome Absolue LExtrait Mist Ultimate Radiance Mist"},
{id:"4520",text:"M.A.C. Prep + Prime Skin Smoother"},
{id:"4521",text:"Make Up For Ever Liquid Lift Foundation"},
{id:"4522",text:"Lancome Renergie Oil-Free Lotion, Anti-Wrinkle and Firming Treatment"},
{id:"4523",text:"M.A.C. Eye Shadows Satin"},
{id:"4524",text:"Laura Mercier Foundation Primer Oil-Free"},
{id:"4525",text:"Lancome LExtreme Instant Extensions Lengthening Mascara"},
{id:"4526",text:"Maybelline New York Color Elixir by ColorSensational"},
{id:"4527",text:"Mary Kay True Dimensions Lipstick"},
{id:"4528",text:"Maybelline New York Color Tattoo by Eyestudio 24hr Eyeshadow"},
{id:"4529",text:"Maybelline New York EyeStudio Color Tattoo Concentrated Crayon"},
{id:"4530",text:"M.A.C. Lipsticks Amplified Cremes"},
{id:"4531",text:"Laura Mercier Flawless Skin Oil-Free Foaming One-Step Cleanser"},
{id:"4532",text:"Liz Earle Naturally Active Skin Care Gentle Face Exfoliator"},
{id:"4533",text:"M.A.C. Opulash Optimum Black Mascara"},
{id:"4534",text:"M.A.C. Cleanse Off Oil/Tranquil"},
{id:"4535",text:"Mally Beauty Eye Perfection Weightless Shadow"},
{id:"4536",text:"Laura Mercier Flawless Skin Dual Action Eye Makeup Remover Oil-Free"},
{id:"4537",text:"Maybelline New York Color Tattoo Leather by Eyestudio 24HR Eyeshadow"},
{id:"4538",text:"Mally Beauty Lightwand Eye Brightener"},
{id:"4539",text:"Lancome Le Crayon Poudre Powder Pencil for the Brows"},
{id:"4540",text:"Maybelline New York Volum Express Pumped Up! Colossal Mascara"},
{id:"4541",text:"Laura Mercier Eye Pencil"},
{id:"4542",text:"Laura Mercier Flawless Skin One-Step Cleanser"},
{id:"4543",text:"Laura Geller Eye Elements Baked Eyeshadow & Highlighter Quad"},
{id:"4544",text:"Mario Badescu Aloe Moisturizer SPF 15"},
{id:"4545",text:"Mary Kay Foundation Primer Sunscreen Broad Spectrum SPF 15"},
{id:"4546",text:"M.A.C. Retro Matte Liquid Lipcolor"},
{id:"4547",text:"Laura Mercier Tightline Cake Eye Liner"},
{id:"4548",text:"Laneige Oil Free Liquid Cleanser"},
{id:"4549",text:"Make Up For Ever Eyelashes Strip"},
{id:"4550",text:"Laura Mercier Foundation PrimerHydrating"},
{id:"4551",text:"Mally Beauty Evercolor Starlight Waterproof Eyeliner"},
{id:"4552",text:"Make Up For Ever Step 1 Skin Equalizer Radiant Primer"},
{id:"4553",text:"Maybelline New York MasterDuo by EyeStudio 2-in-1 Glossy Liquid Liner"},
{id:"4554",text:"M.A.C. Mineralize Charged Water Revitalizing Energy"},
{id:"4555",text:"Make Up For Ever Mist & Fix"},
{id:"4556",text:"Maybelline New York Matte + Poreless Fit Me Foundation"},
{id:"4557",text:"Maybelline New York Great Lash Mascara"},
{id:"4558",text:"Lancome Absolue Eye Premium Bx, Absolute Replenishing Eye Cream"},
{id:"4559",text:"Mario Badescu Special Healing Powder"},
{id:"4560",text:"Liz Earle Naturally Active Skin Care Superskin Concentrate"},
{id:"4561",text:"Lancome Hypnose Drama Waterproof Mascara"},
{id:"4562",text:"Maybelline New York Fit Me! Concealer"},
{id:"4563",text:"Maybelline New York SuperStay 14HR Lipstick"},
{id:"4564",text:"M.A.C. Penultimate Brow Marker"},
{id:"4565",text:"Lab Series Skincare for Men Moisturizing After-Shave Soothing Spray"},
{id:"4566",text:"Liz Earle Naturally Active Skin Care Energising Hip and Thigh Gel"},
{id:"4567",text:"Marcelle Accent Eyebrow Crayon"},
{id:"4568",text:"Lancome Gel Radiance Clarifying Gel-to-Foam Cleanser Normal, Combination Skin"},
{id:"4569",text:"M.A.C. Prep + Prime Beauty Balm Compact SPF 30"},
{id:"4570",text:"Lancome Oscillation Intensity Vibrating Intensifying Powermascara"},
{id:"4571",text:"Laura Geller Italian Marble Lipstick"},
{id:"4572",text:"Make Up For Ever Mat Bronze"},
{id:"4573",text:"Laura Mercier Moisture Supreme Foundation"},
{id:"4574",text:"Lab Series Skincare for Men BB Tinted Moisturizer Broad Spectrum SPF 35"},
{id:"4575",text:"Make Up For Ever Lift Concealer"},
{id:"4576",text:"Mary Kay Oil-Free Hydrating Gel"},
{id:"4577",text:"Lancome Artliner 24H Bold Color Precision Eyeliner"},
{id:"4578",text:"Maybelline New York Color Sensational Vivid"},
{id:"4579",text:"Mary Kay TimeWise Replenishing Serum+C"},
{id:"4580",text:"Lancome Teint Miracle Lit-From-Within Makeup Natural Skin Perfection SPF 15 Sunscreen"},
{id:"4581",text:"Lancome Definicils Precious Cells High Definition Amplifying Mascara"},
{id:"4582",text:"Lancer Skincare The Method: Polish for Sensitive Skin"},
{id:"4583",text:"M.A.C. Fluidline Brow GelCreme"},
{id:"4584",text:"M.A.C. Powder Blush Frosts"},
{id:"4585",text:"Maybelline New York Great Lash Big Mascara"},
{id:"4586",text:"Marc Jacobs Beauty Perfection Powder Featherweight Finish"},
{id:"4587",text:"Lancome Gel Pure Focus, Oil Control Cleansing Gel for Oily Skin"},
{id:"4588",text:"Mario Badescu Cream X"},
{id:"4589",text:"M.A.C. Sheen Supreme Lipstick"},
{id:"4590",text:"Lancome Absolue Premium Bx Advanced Creamy Foam Cleanser"},
{id:"4591",text:"Maybelline New York Baby Lips Moisturizing Lip Balm SPF 20"},
{id:"4592",text:"Lancome Le Crayon Kohl"},
{id:"4593",text:"Lorac Publicity Stunt Lashes"},
{id:"4594",text:"Lancer Skincare The Method: Polish Blemish Control"},
{id:"4595",text:"Liz Earle Naturally Active Skin Care Sensitive Shave Cream"},
{id:"4596",text:"Lancer Skincare The Method: Cleanse"},
{id:"4597",text:"Laneige Balancing Emulsion Combination/Oily"},
{id:"4598",text:"Lorac Mocktail"},
{id:"4599",text:"Lancome La Base Pro Perfecting Makeup Primer Smoothing Effect, Oil Free"},
{id:"4600",text:"Lancome Hypnose Drama Instant Full Body Volume Mascara"},
{id:"4601",text:"Liz Earle Naturally Active Skin Care Skin Repair Moisturiser Normal/Combination"},
{id:"4602",text:"Make Up For Ever Pro-Bronze Fusion"},
{id:"4603",text:"Mary Kay Brow Definer Pencil"},
{id:"4604",text:"Lancome Tonique Confort, Comforting Rehydrating Toner, for Normal to Dry Skin"},
{id:"4605",text:"Lancome Tropiques Minerale Mineral Smoothing Loose Bronzer"},
{id:"4606",text:"Liz Earle Naturally Active Skin Care Superskin Moisturiser"},
{id:"4607",text:"Maybelline New York Dream Wonder Fluid-Touch Foundation SPF 20 Octinoxate Sunscreen Broad Spectrum"},
{id:"4608",text:"Marc Jacobs Beauty Lust For Lacquer Lip Vinyl"},
{id:"4609",text:"Marc Jacobs Beauty #Instamarc Light Filtering Contour"},
{id:"4610",text:"Make Up For Ever Compact Shine On"},
{id:"4611",text:"Maybelline New York Real Impact! By Great Lash"},
{id:"4612",text:"M.A.C. False Lashes Mascara Volume"},
{id:"4613",text:"M.A.C. Prep + Prime CC Colour Correcting SPF 30"},
{id:"4614",text:"Laneige Power Essential Skin Toner Combination/Oily"},
{id:"4615",text:"Lab Series Skincare for Men Electric Shave Solution"},
{id:"4616",text:"Lancome Bienfait Multi-Vital Eyes SPF 28"},
{id:"4617",text:"M.A.C. Pro Longwear SPF 20 Compact Foundation"},
{id:"4618",text:"Laneige Water Sleeping Mask"},
{id:"4619",text:"Mario Badescu Dermonectin Eye Cream"},
{id:"4620",text:"M.A.C. Mineralize Foundation"},
{id:"4621",text:"Marc Jacobs Beauty Genius Gel Super-Charged Foundation"},
{id:"4622",text:"Laura Geller Lip Strip"},
{id:"4623",text:"M.A.C. Lustreglass"},
{id:"4624",text:"Laura Mercier Flawless Skin Repair Serum"},
{id:"4625",text:"Make Up For Ever Aquarelle"},
{id:"4626",text:"Lubriderm Daily Moisture Lotion SPF 15"},
{id:"4627",text:"Lancome Aquatique Waterproof EyeColour Base"},
{id:"4628",text:"Laura Mercier Secret Brightening Powder"},
{id:"4629",text:"Marcelle Gentle Foaming Wash"},
{id:"4630",text:"Laura Mercier Illuminating Powder"},
{id:"4631",text:"Mario Badescu Orange Cleansing Soap"},
{id:"4632",text:"Laneige Multi Cleanser for All Skin"},
{id:"4633",text:"Lancome Color Design Infinite 24H"},
{id:"4634",text:"Maybelline New York Full n Soft Mascara"},
{id:"4635",text:"Make Up For Ever Extreme Cleanser, Balancing Cleansing Dry Oil"},
{id:"4636",text:"M.A.C. Prep + Prime Transparent Finishing Powder Pressed"},
{id:"4637",text:"Mally Beauty Perfect Prep Soothing Eye Gel"},
{id:"4638",text:"M.A.C. Prep + Prime Skin Refined Zone Treatment"},
{id:"4639",text:"Mary Kay Botanical Effects Hydrate 1"},
{id:"4640",text:"Maybelline New York Define-A-Brow Eyebrow Pencil"},
{id:"4641",text:"Lancome Bienfait UV SPF 50+"},
{id:"4642",text:"Make Up For Ever Aqua Seal"},
{id:"4643",text:"Mario Badescu Enzyme Revitalizing Mask"},
{id:"4644",text:"Lancome Creme Douceur Cream-to-Oil Massage Cleanser All Skin Types"},
{id:"4645",text:"Lancome Oscillation Powerfoundation SPF 21"},
{id:"4646",text:"Laura Mercier Lip Stain"},
{id:"4647",text:"M.A.C. Bronzing Powder"},
{id:"4648",text:"Lancome Nutrix, Soothing Treatment Cream, for Dry to Very Dry/Sensitive Skin"},
{id:"4649",text:"Mary Kay Indulge Soothing Eye Gel"},
{id:"4650",text:"M.A.C. Brow Finisher"},
{id:"4651",text:"Make Up For Ever Concealer Pencil"},
{id:"4652",text:"M.A.C. Brow Set"},
{id:"4653",text:"Maybelline New York Full n Soft Waterproof Mascara"},
{id:"4654",text:"Laura Geller Blush-N-Brighten"},
{id:"4655",text:"Laura Geller Air Whipped Bronzer with Brush"},
{id:"4656",text:"Laura Geller Baby Cakes Baked Eyeliner Palette"},
{id:"4657",text:"Lorac Baked Matte Satin Blush"},
{id:"4658",text:"Lubriderm Intense Skin Repair Ointment"},
{id:"4659",text:"Laura Mercier Undercover Pot"},
{id:"4660",text:"Mario Badescu Super Collagen Mask"},
{id:"4661",text:"Make Up For Ever Moisturizing Lip Balm"},
{id:"4662",text:"M.A.C. Eye Shadows Velvet"},
{id:"4663",text:"Laura Geller Caulk Pencil & Sharpener"},
{id:"4664",text:"M.A.C. Powder Blush Satins"},
{id:"4665",text:"M.A.C. Extra Dimension Eye Shadow"},
{id:"4666",text:"Lancome DreamTone Ultimate Dark Spot Corrector Pigmentation Profile 1  Fair Skin Tone"},
{id:"4667",text:"Maybelline New York Instant Age Rewind The Primer Skin Transformer"},
{id:"4668",text:"Lancome Color Fever Plumper Plumping Vibrant Lip Shine"},
{id:"4669",text:"Maybelline New York Color Sensational Creamy Mattes"},
{id:"4670",text:"Lorac Eye Shadows"},
{id:"4671",text:"Lancome Nutrix Royal, Intense Lipid Repair Cream, for Dry to Very Dry Skin"},
{id:"4672",text:"Laura Geller Matte Maker"},
{id:"4673",text:"Laneige Power Essential Skin Toner Dry/Normal"},
{id:"4674",text:"Lubriderm Mens 3-in-1 Lotion Light Fragrance"},
{id:"4675",text:"Lorac POREfection Baked Perfecting Powder"},
{id:"4676",text:"Laura Mercier Creme Cheek Colour"},
{id:"4677",text:"Lancer Skincare The Method: Nourish"},
{id:"4678",text:"M.A.C. Prep + Prime Face Protect Lotion SPF 50"},
{id:"4679",text:"Mary Kay Clearproof Acne Treatment Gel"},
{id:"4680",text:"Lancome Teint Idole Ultra 24H Wear & Comfort Retouch-Free Divine Perfection SPF 15"},
{id:"4681",text:"Laura Geller Baked Gelato Vivid Swirl Blush"},
{id:"4682",text:"Mario Badescu Self Tanning Lotion Oil Free"},
{id:"4683",text:"Marcelle Face Powder"},
{id:"4684",text:"Mary Kay Timewise Repair Foaming Cleanser-Volu-Firm"},
{id:"4685",text:"Mary Kay Translucent Loose Powder"},
{id:"4686",text:"Laura Geller Wonder Wand"},
{id:"4687",text:"Marcelle Gentle Make-Up Remover for Sensitive Eyes"},
{id:"4688",text:"M.A.C. Eye Shadows Frost"},
{id:"4689",text:"M.A.C. Mineralize Rich Lipstick"},
{id:"4690",text:"Make Up For Ever Multi Loose Powder"},
{id:"4691",text:"Make Up For Ever Uplight Face Luminizer Gel"},
{id:"4692",text:"Mary Kay Clearproof Clarifying Cleansing Gel"},
{id:"4693",text:"Lab Series Skincare for Men MAX LS Skin Recharging Water Lotion"},
{id:"4694",text:"Lancome Dual Finish Versatile Powder Makeup"},
{id:"4695",text:"Lancome La Laque Fever Lip Shine"},
{id:"4696",text:"M.A.C. Huggable Glass"},
{id:"4697",text:"Mario Badescu Glycolic Acid Toner"},
{id:"4698",text:"Mary Kay TimeWise Age-Fighting Moisturizer, for Normal/Dry Skin"},
{id:"4699",text:"M.A.C. Liquidlast Liner"},
{id:"4700",text:"Laura Mercier Foundation Primer- Radiance"},
{id:"4701",text:"Marc Jacobs Beauty Lovemarc Lip Gel"},
{id:"4702",text:"Make Up For Ever Ultra HD Invisible Cover Foundation"},
{id:"4703",text:"Maybelline New York Eyestudio Brow Drama Pomade Crayon"},
{id:"4704",text:"Mary Kay TimeWise Night Solution"},
{id:"4705",text:"Lubriderm Intense Skin Repair Lotion"},
{id:"4706",text:"Liz Earle Naturally Active Skin Care Nourishing Botanical Body Cream"},
{id:"4707",text:"Mario Badescu Elasto-Collagen Night Cream"},
{id:"4708",text:"Mally Beauty Cancellation Conditioning Concealer SPF 25"},
{id:"4709",text:"Laura Mercier Gel Lip Colour"},
{id:"4710",text:"Mary Kay Botanical Effects Hydrate 2"},
{id:"4711",text:"Liz Earle Naturally Active Skin Care Energising Body Lotion"},
{id:"4712",text:"Mary Kay Sunscreen Broad Spectrum SPF 50"},
{id:"4713",text:"Laura Mercier High Coverage Concealer"},
{id:"4714",text:"Make Up For Ever Sculpting Blush"},
{id:"4715",text:"Mario Badescu Cucumber Cleansing Lotion"},
{id:"4716",text:"Mario Badescu Kera Moisturizer"},
{id:"4717",text:"Lancome LAbsolu Nu Replenishing & Enhancing Lipcolor-Bare Lip Sensation"},
{id:"4718",text:"Liz Earle Naturally Active Skin Care Energising Body Wash"},
{id:"4719",text:"Lancer Skincare Advanced C Radiance Cream Vitamin C Collagen Cofactor 10x"},
{id:"4720",text:"Lancome LAbsolu Rouge Advanced Hydrating Lipcolor"},
{id:"4721",text:"Lorac CC Color Correcting Cream"},
{id:"4722",text:"Mally Beauty Ginormous Mascara"},
{id:"4723",text:"Maybelline New York Master Drama Cream Pencil"},
{id:"4724",text:"Mary Kay Botanical Effects Mask 1"},
{id:"4725",text:"Maybelline New York Master Glaze by Face Studio Glisten Blush Stick"},
{id:"4726",text:"Laura Mercier Creme Lip Colour"},
{id:"4727",text:"Laura Mercier Secret Camouflage"},
{id:"4728",text:"Mario Badescu Eye Make-Up Remover Cream"},
{id:"4729",text:"Mary Kay Timewise Repair Night Treatment with Retinol-Volu-Firm"},
{id:"4730",text:"Mally Beauty Liquid Face Defender"},
{id:"4731",text:"Mary Kay Mineral Highlighting Powder"},
{id:"4732",text:"Mario Badescu Glycolic Gel"},
{id:"4733",text:"Lancer Skincare Retexturizing Treatment Cream Glycolic Acid 10x"},
{id:"4734",text:"Make Up For Ever Glossy Full Couleur"},
{id:"4735",text:"Laura Mercier Brow Definer"},
{id:"4736",text:"Mary Kay Oil-Free Eye Makeup Remover"},
{id:"4737",text:"Laura Mercier Shimmer Bloc"},
{id:"4738",text:"Laura Geller Balance-N-Brighten"},
{id:"4739",text:"Laura Mercier Flawless Skin Tone Perfecting Crme"},
{id:"4740",text:"Lancome Bienfait Teinte Beauty Balm SPF 30"},
{id:"4741",text:"Mary Kay Botanical Effects Cleanse 1"},
{id:"4742",text:"Mario Badescu Enzyme Cleansing Gel"},
{id:"4743",text:"Maybelline New York Dream Bronze BB 8-in-1 Sunkissed Glow Perfector SPF 25 Octinoxate Sunscreen Broad Spectrum"},
{id:"4744",text:"Lancome Mousse Radiance Clarifying Self-Foaming Cleanser Normal, Combination Skin"},
{id:"4745",text:"Liz Earle Naturally Active Skin Care Cleanse & Polish Hot Cloth Cleanser (for men)"},
{id:"4746",text:"M.A.C. Prep + Prime Lip"},
{id:"4747",text:"M.A.C. Lipsticks Frosts"},
{id:"4748",text:"Mario Badescu Cellufirm Moisturizer"},
{id:"4749",text:"Mary Kay Botanical Effects Freshen 2"},
{id:"4750",text:"Liz Earle Naturally Active Skin Care Orange Flower Botanical Body Wash"},
{id:"4751",text:"M.A.C. Matchmaster SPF 15 Foundation"},
{id:"4752",text:"Marcelle Eye Makeup Remover Lotion Oil-Free"},
{id:"4753",text:"Marc Jacobs Beauty Under (cover) Perfecting Coconut Face Primer"},
{id:"4754",text:"Make Up For Ever Lip Line Perfector"},
{id:"4755",text:"Maybelline New York Dream Wonder Powder"},
{id:"4756",text:"Laura Mercier Smooth Finish Foundation Powder"},
{id:"4757",text:"Laneige Bright Renew Night Cream"},
{id:"4758",text:"Marc Jacobs Beauty Lash Lifter Gel Volume Mascara"},
{id:"4759",text:"Maybelline New York Superstay Better Skin Concealer + Corrector"},
{id:"4760",text:"Mary Kay TimeWise 3-In-1 Cleanser, for Combination to Oily Skin"},
{id:"4761",text:"Maybelline New York The Falsies Push Up Drama Mascara"},
{id:"4762",text:"Mally Beauty Shaping Secret Eyelift Wand"},
{id:"4763",text:"Lancome Teint Miracle Instant Retouch Pen Lit-From-Within Perfector"},
{id:"4764",text:"Mally Beauty Eye Amplifying Shadow Liner"},
{id:"4765",text:"Laneige Water Bank Moisture Cream"},
{id:"4766",text:"Mary Kay Satin Hands Fragrance-Free Hand Cream"},
{id:"4767",text:"Laura Geller The Real Deal Concealer Serious Coverage"},
{id:"4768",text:"M.A.C. Green Gel Cleanser"},
{id:"4769",text:"Lorac Coverup"},
{id:"4770",text:"Lancome Crme Radiance Clarifying Cream-to-Foam Cleanser for Normal/Combination Skin"},
{id:"4771",text:"Mario Badescu Hydrating Hand Cream"},
{id:"4772",text:"Lubriderm Mens 3-in-1 Lotion Fragrance Free"},
{id:"4773",text:"Mally Beauty Lipstick"},
{id:"4774",text:"Maybelline New York Ultra Liner Waterproof Eyeliner"},
{id:"4775",text:"Laura Geller Baked Cake Eyeshadow & Liner Duo"},
{id:"4776",text:"M.A.C. Extended Play Lash"},
{id:"4777",text:"Lab Series Skincare for Men Max LS Instant Eye Lift"},
{id:"4778",text:"M.A.C. Plushglass"},
{id:"4779",text:"M.A.C. Giambattista Valli Crystal Glaze Gloss"},
{id:"4780",text:"Mario Badescu Peptide Renewal Cream"},
{id:"4781",text:"M.A.C. Oil Control Lotion"},
{id:"4782",text:"M.A.C. Studio Fix Fluid SPF 15"},
{id:"4783",text:"M.A.C. Lipglass, Clear and Tinted"},
{id:"4784",text:"Lancome Genifique Yeux Light-Pearl Eye-Illuminating Youth Activating Serum"},
{id:"4785",text:"Lorac POREfection Mattifying Face Primer"},
{id:"4786",text:"Maybelline New York Volum Express Mascara The Falsies Big Eyes"},
{id:"4787",text:"Liz Earle Naturally Active Skin Care Eyebright Soothing Eye Lotion"},
{id:"4788",text:"M.A.C. Eye Brow Pencils"},
{id:"4789",text:"Lorac TANtalizer Baked Bronzer"},
{id:"4790",text:"Mary Kay Botanical Effects Freshen 1"},
{id:"4791",text:"Marcelle Revival Firming + Brightening Cream Serum"},
{id:"4792",text:"Maybelline New York The Falsies Push Up Drama Waterproof Mascara"},
{id:"4793",text:"Maybelline New York Define-A-Line Eye Liner"},
{id:"4794",text:"Liz Earle Naturally Active Skin Care Superskin Body Cream"},
{id:"4795",text:"Lancome Absolue LExtrait Serum Ultimate Rejuvenating Concentrated Elixir"},
{id:"4796",text:"Lab Series Skincare for Men Invigorating Face Scrub"},
{id:"4797",text:"Maybelline New York Dream Lumi Touch Highlighting Concealer"},
{id:"4798",text:"Lorac Blush"},
{id:"4799",text:"Mario Badescu Rolling Cream Peel with A.H.A."},
{id:"4800",text:"Mary Kay Cream Eye Color"},
{id:"4801",text:"Laura Mercier Flawless Skin Infusion De Rose Nourishing Oil"},
{id:"4802",text:"Mally Beauty Lip Magnifier"},
{id:"4803",text:"Maybelline New York The Flash Clean Clean Express Makeup Removing Lotion"},
{id:"4804",text:"M.A.C. Matte"},
{id:"4805",text:"Lancome Absolue Premium Bx, Absolute Replenishing Cream SPF 15"},
{id:"4806",text:"M.A.C. Penultimate Eyeliner"},
{id:"4807",text:"Mario Badescu Glycolic Foaming Cleanser"},
{id:"4808",text:"M.A.C. Studio Conceal and Correct Duo"},
{id:"4809",text:"Lancome Oscillation Powerbooster Vibrating Amplifying Primer"},
{id:"4810",text:"Make Up For Ever Eyebrow Pencil"},
{id:"4811",text:"Laura Geller Phenomenal Foundation"},
{id:"4812",text:"Mario Badescu Enzyme Protective Cream"},
{id:"4813",text:"Lab Series Skincare for Men Max LS Overnight Renewal Serum"},
{id:"4814",text:"Mary Kay Lip Protector Sunscreen SPF 15"},
{id:"4815",text:"Mary Kay Intense Moisturizing Cream"},
{id:"4816",text:"M.A.C. Mattene Lipstick"},
{id:"4817",text:"Lancome Baume In Love"},
{id:"4818",text:"Mary Kay Lash Love Lengthening Mascara"},
{id:"4819",text:"Marcelle Hydra-C 24H Energizing Hydrating Gel"},
{id:"4820",text:"Laura Mercier Mineral Illuminating Powder"},
{id:"4821",text:"Laura Mercier Tinted Moisturizer Oil-Free SPF 20"},
{id:"4822",text:"Mario Badescu Chamomile Eye Cream"},
{id:"4823",text:"M.A.C. Pro Longwear Eye Shadow"},
{id:"4824",text:"Make Up For Ever Pure Pigments"},
{id:"4825",text:"M.A.C. Mineralize Glass"},
{id:"4826",text:"Laura Mercier Bronzing Duo"},
{id:"4827",text:"Mario Badescu Aloe Vera Toner"},
{id:"4828",text:"Laneige Perfect Renew Emulsion"},
{id:"4829",text:"Lorac IM SO SENSITIVE Soothing Face Primer"},
{id:"4830",text:"Laura Mercier Secret Concealer"},
{id:"4831",text:"Make Up For Ever Velvet Finish Compact Powder"},
{id:"4832",text:"Lancome Absolue Night Precious Cells Advanced Regenerating and Reconstructing Night Cream"},
{id:"4833",text:"Laura Geller Baked Marble Eyeshadow"},
{id:"4834",text:"Maybelline New York Lash Sensational Mascara"},
{id:"4835",text:"Mario Badescu Oil Free Moisturizer"},
{id:"4836",text:"Lancome Eclat Miracle Serum of Light Complexion Illuminator"},
{id:"4837",text:"Laura Geller Ethereal Rose Baked Powder"},
{id:"4838",text:"Liz Earle Naturally Active Skin Care Botanical Aftersun Gel"},
{id:"4839",text:"Laura Mercier Full Blown Volume Lash Building Mascara"},
{id:"4840",text:"Make Up For Ever Aqua Lip Waterproof Lipliner Pencil"},
{id:"4841",text:"Maybelline New York Dream Fresh B.B. 8-in-1 Beauty Balm Skin Perfector SPF 30"},
{id:"4842",text:"Lorac Double Feature Concealer/Highlighter"},
{id:"4843",text:"Lancome Gloss In Love"},
{id:"4844",text:"Mary Kay Facial Highlighting Pen"},
{id:"4845",text:"Make Up For Ever Camouflage Cream"},
{id:"4846",text:"Maybelline New York Lash Stiletto Ultimate Length Mascara"},
{id:"4847",text:"Mario Badescu Carnation Eye Make-Up Remover Oil"},
{id:"4848",text:"Maybelline New York Shine Sensational Lip Gloss"},
{id:"4849",text:"Mary Kay Satin Lips Lip Balm"},
{id:"4850",text:"M.A.C. False Lashes Extreme Black Mascara"},
{id:"4851",text:"M.A.C. Lip Conditioner"},
{id:"4852",text:"Marc Jacobs Beauty Magic Marcer Precision Pen Waterproof Eyeliner"},
{id:"4853",text:"Make Up For Ever Smoky Lash Mascara"},
{id:"4854",text:"M.A.C. Matchmaster Shade Intelligence Compact"},
{id:"4855",text:"Laura Mercier Silk Crme Oil Free Photo Edition Foundation"},
{id:"4856",text:"M.A.C. Prep + Prime CC Colour Correcting Compact"},
{id:"4857",text:"Lancome Bienfait Aqua Vital Continuous Infusing Moisturizer Lotion"},
{id:"4858",text:"Maybelline New York Clean Express! Facial Towelettes"},
{id:"4859",text:"M.A.C. Strobe Cream"},
{id:"4860",text:"Mario Badescu Hydro-Emollient Cream"},
{id:"4861",text:"Lancome LAbsolu Creme De Brilliance Visibly Replenishing and Reshaping Lip Cream"},
{id:"4862",text:"Laura Mercier Foundation Primer Protect Broad Spectrum SPF 30/PA+++"},
{id:"4863",text:"M.A.C. Cremeblend Blush"},
{id:"4864",text:"M.A.C. Prep + Prime Natural Radiance"},
{id:"4865",text:"Laura Mercier Flawless Skin Daily Face Shield SPF 40"},
{id:"4866",text:"Mary Kay Timewise Repair Eye Renewal Cream-Volu-Firm"},
{id:"4867",text:"Lancome Rouge in Love High Potency Lipcolor 6 Hour Wear"},
{id:"4868",text:"Maybelline New York The Colossal Volum Express Cat Eyes Mascara Waterproof"},
{id:"4869",text:"Lancome Bi-Facil Double-Action Eye Makeup Remover"},
{id:"4870",text:"Lancome Maquicomplet Complete Coverage Concealer"},
{id:"4871",text:"Lancome Bienfait Multi-Vital SPF 30 Lotion"},
{id:"4872",text:"M.A.C. Matte2 Eye Shadow"},
{id:"4873",text:"Make Up For Ever Step 1 Skin Equalizer Nourishing Primer"},
{id:"4874",text:"Maybelline New York Expert Wear The Nudes Palette"},
{id:"4875",text:"Lancome Pure Empreinte Masque Purifying Mineral Mask with White Clay"},
{id:"4876",text:"Mary Kay MKMen Cooling After-Shave Gel"},
{id:"4877",text:"M.A.C. Blot Film"},
{id:"4878",text:"Mary Kay Mineral Eye Color"},
{id:"4879",text:"Marc Jacobs Beauty Lip Lock Moisture Balm SPF 18"},
{id:"4880",text:"Mally Beauty Evercolor Lasting Eyeliner"},
{id:"4881",text:"Mally Beauty Evercolor Shadow Stick"},
{id:"4882",text:"Mary Kay TimeWise Even Complexion Essence"},
{id:"4883",text:"M.A.C. Giambattista Valli Lipstick"},
{id:"4884",text:"Lab Series Skincare for Men Power Brightening Serum Advanced"},
{id:"4885",text:"Maybelline New York Expert Eyes 100% Oil-Free Eye Makeup Remover"},
{id:"4886",text:"Laura Geller Sugar-Free Blush-N-Brighten"},
{id:"4887",text:"Laneige Multiberry Yogurt Repairing Mask"},
{id:"4888",text:"Mary Kay TimeWise 3-in-1 Cleanser, for Normal to Dry Skin"},
{id:"4889",text:"Mally Beauty High Shine Liquid Lipstick Pen"},
{id:"4890",text:"Lancome Flash Bronzer Tinted Self-Tanning Leg Gel with Pure Vitamin E"},
{id:"4891",text:"M.A.C. Lipsticks Glazes"},
{id:"4892",text:"M.A.C. Fluidline"},
{id:"4893",text:"Mary Kay Lash Love Waterproof Mascara"},
{id:"4894",text:"Liz Earle Naturally Active Skin Care Instant Boost Skin Tonic"},
{id:"4895",text:"Lancome Absolue LExtrait Ultimate Rejuvenating Elixir"},
{id:"4896",text:"M.A.C. Select Moisturecover"},
{id:"4897",text:"Mario Badescu Glycolic Grapefruit Cleansing Lotion"},
{id:"4898",text:"Lancome Genifique Repair Youth Activating Night Cream"},
{id:"4899",text:"Maybelline New York Superstay Makeup 24HR"},
{id:"4900",text:"M.A.C. Eye Shadows Lustre"},
{id:"4901",text:"Lancome Visionnaire [LR 2412 4%-CX] Advanced Skin Corrector"},
{id:"4902",text:"Lubriderm Advanced Therapy Hand Cream"},
{id:"4903",text:"Maybelline New York The Falsies Volum Express Flared Mascara"},
{id:"4904",text:"M.A.C. Lightful C 2-in-1 Tint and Serum with Radiance Booster"},
{id:"4905",text:"M.A.C. Haute & Naughty Lash"},
{id:"4906",text:"Maybelline New York Dream Bouncy Blush"},
{id:"4907",text:"Lancome Absolue BX Makeup Absolute Replenishing Radiant Makeup SPF 18 Sunscreen"},
{id:"4908",text:"Laura Geller Color Drenched Lip Gloss"},
{id:"4909",text:"Maybelline New York Lash Sensational Luscious Waterproof Mascara"},
{id:"4910",text:"Make Up For Ever Pro Waterproof Eyeliner"},
{id:"4911",text:"Mally Beauty Visible Skin Adjustable Coverage Foundation"},
{id:"4912",text:"M.A.C. Prep + Prime Transparent Finishing Powder"},
{id:"4913",text:"Make Up For Ever Duo Mat Powder Foundation"},
{id:"4914",text:"Make Up For Ever Pro Sculpting Duo"},
{id:"4915",text:"M.A.C. Mineralize Charged Water Cleanser"},
{id:"4916",text:"Make Up For Ever Gentle Milk, Moisturizing Cleansing Milk"},
{id:"4917",text:"Marc Jacobs Beauty Re(Marc)able Full Cover Concealer"},
{id:"4918",text:"M.A.C. Eye Pencils"},
{id:"4919",text:"Lancome Progres Eye Cream"},
{id:"4920",text:"Make Up For Ever Aqua Eyes Waterproof Eyeliner Pencil"},
{id:"4921",text:"Lorac Perfectly Lit Oil-Free Luminizing Powder"},
{id:"4922",text:"Lab Series Skincare for Men Oil Control Face Wash"},
{id:"4923",text:"Make Up For Ever Rouge Artist Natural"},
{id:"4924",text:"Laneige Triple Sunscreen SPF 40"},
{id:"4925",text:"Laura Mercier Secret Finish"},
{id:"4926",text:"Make Up For Ever Stop Shining +"},
{id:"4927",text:"M.A.C. M.A.C. Pro Sculpting Powder"},
{id:"4928",text:"M.A.C. Lightful C Tinted Cream with Radiance Booster Broad Spectrum SPF 30"},
{id:"4929",text:"Lancome Cils Booster XL Super-Enhancing Mascara Base"},
{id:"4930",text:"M.A.C. Pro Longwear Lip Creme"},
{id:"4931",text:"Lorac Oil-Free Neutralizer"},
{id:"4932",text:"Laura Geller Eye Rimz Baked Wet/Dry Eye Accents"},
{id:"4933",text:"Lancome Absolue Eye Precious Cells Advanced Regenerating and Reconstructing Eye Cream"},
{id:"4934",text:"Lorac Pro Mascara Lengthening & Thickening"},
{id:"4935",text:"Maybelline New York Lasting Drama by Eyestudio Gel Eyeliner"},
{id:"4936",text:"Lancome Artliner Precision Point EyeLiner"},
{id:"4937",text:"Laura Mercier Shimmer Lip Colour"},
{id:"4938",text:"Maybelline New York Master Conceal by Face Studio"},
{id:"4939",text:"Make Up For Ever HD Microperfecting Primer"},
{id:"4940",text:"Lorac TANtalizer Award-Show Glow"},
{id:"4941",text:"Maybelline New York Color Blur Matte Lip Pencil"},
{id:"4942",text:"Lorac TANtalizer Body Bronzing Luminizer"},
{id:"4943",text:"Mary Kay Cream Blush"},
{id:"4944",text:"M.A.C. Cremesheen Glass"},
{id:"4945",text:"Laura Geller The Real Deal Blush Stick SPF 15"},
{id:"4946",text:"Mally Beauty Perfect Prep Liquid Luminizer"},
{id:"4947",text:"M.A.C. Cremesheen Lipstick"},
{id:"4948",text:"M.A.C. Lightful C Marine-Bright Formula Softening Lotion"},
{id:"4949",text:"Marcelle BB Cream Anti-Aging SPF 20"},
{id:"4950",text:"Lorac Eye Shadow/Liner"},
{id:"4951",text:"Maybelline New York Color Sensational Lipliner"},
{id:"4952",text:"Lancome Color Fever Shine Sensual Sheer Color Vibrant Lipshine"},
{id:"4953",text:"Mally Beauty In The Buff Eyeshadow Palette"},
{id:"4954",text:"Make Up For Ever Cool Lotion, Moisturizing Soothing Lotion"},
{id:"4955",text:"M.A.C. Cream Colour Base"},
{id:"4956",text:"Mary Kay Beauty Blotters Oil-Absorbing Tissues"},
{id:"4957",text:"Liz Earle Naturally Active Skin Care Intensive Nourishing Treatment Mask"},
{id:"4958",text:"Make Up For Ever Pan Stick Foundation"},
{id:"4959",text:"M.A.C. Dazzleglass Lipcolour"},
{id:"4960",text:"Mary Kay Cream Eye Color/Concealer Brush"},
{id:"4961",text:"Lancome Imanance Tinted Day Creme SPF 15"},
{id:"4962",text:"M.A.C. Lipsticks Satins"},
{id:"4963",text:"Maybelline New York Lash Discovery Waterproof Mascara"},
{id:"4964",text:"Lorac 3D Liquid Lustre Shimmering Top Coat"},
{id:"4965",text:"Make Up For Ever Aqua Smoky Lash Waterproof Extra Black Mascara"},
{id:"4966",text:"Mally Beauty Age Rebel Concealer"},
{id:"4967",text:"Maybelline New York Lash Sensational Luscious Mascara"},
{id:"4968",text:"Lancome Bienfait Multi-Vital Night, High Potency Night Moisturizing Cream"},
{id:"4969",text:"Maybelline New York Master Smoky Longwearing Shadow-Pencil"},
{id:"4970",text:"Lancome Liner Design Long Wear Calligraphy Gel EyeLiner"},
{id:"4971",text:"Mario Badescu Almond & Honey Non-Abrasive Face Scrub"},
{id:"4972",text:"Mary Kay Eye Smudger Brush"},
{id:"4973",text:"Maybelline New York Brow Drama Pro Palette"},
{id:"4974",text:"Laura Mercier Secret Finish Mattifying"},
{id:"4975",text:"Laura Mercier Flawless Contouring Palette"},
{id:"4976",text:"M.A.C. Prep + Prime Highlighter"},
{id:"4977",text:"Lancome Absolue Makeup Absolute Replenishing Cream Makeup SPF 20"},
{id:"4978",text:"Maybelline New York Volum Express The Falsies Mascara Waterproof"},
{id:"4979",text:"Lancome Bienfait Multi-Vital Teinte High Potency Tinted Moisturizer SPF 30"},
{id:"4980",text:"Lorac PRO Matte Lip Color"},
{id:"4981",text:"brand product"},
{id:"4982",text:"Olay Active Hydrating Beauty Fluid, Original"},
{id:"4983",text:"Nia24 Rapid Depigmentation Serum"},
{id:"4984",text:"Merle Norman Wrinkle Smoother Lift & Firm Serum"},
{id:"4985",text:"Neutrogena Ultra Sheer Dry Touch Sunscreen SPF 100+"},
{id:"4986",text:"Merle Norman Wrinkle Smoother Lips"},
{id:"4987",text:"Obagi C-Exfoliating Day Lotion With Vitamin C SPF 12"},
{id:"4988",text:"Michael Todd True Organics Cleanse and Tone One Step Cleanser/Toner"},
{id:"4989",text:"Merle Norman Brilliant-C Brightening Serum"},
{id:"4990",text:"N.Y.C. (New York Color) Smooth Skin Bronzing Face Powder"},
{id:"4991",text:"Nu Skin Sunright Lip Balm SPF 15"},
{id:"4992",text:"Nia24 Sun Damage Prevention UVA/UVB Sunscreen SPF 30 PA+++"},
{id:"4993",text:"Merle Norman Daily Moisture Toner"},
{id:"4994",text:"N.Y.C. (New York Color) Smooth Skin Perfecting Primer"},
{id:"4995",text:"NeoStrata Canada Daytime Smoothing Cream Level 3 SPF 15"},
{id:"4996",text:"Neutrogena Rapid Wrinkle Repair Moisturizer Night"},
{id:"4997",text:"Merle Norman Dual Action Eye Makeup Remover"},
{id:"4998",text:"NARS Total Replenishing Eye Cream"},
{id:"4999",text:"Murad Rapid Age Spot and Pigment Lightening Serum"},
{id:"5000",text:"NeoStrata Canada Eye Contour Smoothing Cream"},
{id:"5001",text:"Milani Length in Seconds Lash Extension Fibers"},
{id:"5002",text:"Neutrogena Neutrogena Naturals Multi-Vitamin Nourishing Night Cream"},
{id:"5003",text:"Nu Skin Nu Colour Skin Beneficial Concealer"},
{id:"5004",text:"Obagi C-Therapy Night Cream"},
{id:"5005",text:"Merle Norman Moisture Rich Facial Treatment"},
{id:"5006",text:"Olay Regenerist Micro-Sculpting Serum Fragrance Free"},
{id:"5007",text:"Nuance Salma Hayek Intense Hydration Overnight Rejuvenator"},
{id:"5008",text:"Murad Hydro-Dynamic Ultimate Moisture"},
{id:"5009",text:"Neutrogena Rapid Clear Foaming Scrub"},
{id:"5010",text:"Milani Runway Lashes Instant Volume Mascara"},
{id:"5011",text:"Merle Norman Energizing Concentrate"},
{id:"5012",text:"Nivea Happy Sensation Body Lotion for Normal to Dry Skin"},
{id:"5013",text:"Nivea Smooth Indulgence Hand Cream"},
{id:"5014",text:"Nivea Nivea For Men Express Absorption Revitalizing Lotion, Normal skin"},
{id:"5015",text:"NARS Semi Matte Lipstick"},
{id:"5016",text:"NeoStrata Canada Smoothing Cleansing Mask"},
{id:"5017",text:"Merle Norman Matte Oil-Free Moisturizer"},
{id:"5018",text:"MD Formulations Continuous Renewal Complex, Sensitive Skin Formula"},
{id:"5019",text:"Murad Invisiblur Perfecting Shield Broad Spectrum SPF 30"},
{id:"5020",text:"Noxzema The Original Deep Cleansing Cream, Jar"},
{id:"5021",text:"Murad Hybrids Skin Perfecting Primer Matte Finish"},
{id:"5022",text:"NYX Cosmetics HD Eye Shadow Base"},
{id:"5023",text:"Merle Norman Daily Moisture Milky Cleanser"},
{id:"5024",text:"Nuance Salma Hayek Prickly Pear Hydrating Body Cleansing Gel"},
{id:"5025",text:"NeoStrata Canada Vitamin C Concentrate"},
{id:"5026",text:"Neutrogena 3-in-1 Concealer for Eyes SPF 20"},
{id:"5027",text:"NYX Cosmetics Roll On Shimmer for Eyes, Face, Body"},
{id:"5028",text:"Merle Norman Nighttime Recovery Creme"},
{id:"5029",text:"Murad Essential-C Day Moisture Broad Spectrum SPF 30/PA+++"},
{id:"5030",text:"Nivea Nivea for Men Maximum Hydration Nourishing Lotion, Dry Skin"},
{id:"5031",text:"Neutrogena Ultra Sheer Body Mist Sunscreen Broad Spectrum SPF 100"},
{id:"5032",text:"Murad Essential-C Night Moisture"},
{id:"5033",text:"Murad Post-Acne Spot Lightening Gel"},
{id:"5034",text:"Neutrogena On-the-Spot Acne Treatment"},
{id:"5035",text:"NeoCeuticals by NeoStrata Acne Spot Treatment Gel"},
{id:"5036",text:"N.Y.C. (New York Color) High Definition Separating Mascara"},
{id:"5037",text:"Merle Norman Moisture Lotion"},
{id:"5038",text:"Murad Acne Spot Fast Fix"},
{id:"5039",text:"Obagi Nu-Derm Exfoderm Forte"},
{id:"5040",text:"Obagi Nu-Derm Clear"},
{id:"5041",text:"Obagi Elastiderm Decolletage Chest and Neck Wrinkle Reducing Lotion"},
{id:"5042",text:"Neutrogena Alcohol-Free Toner"},
{id:"5043",text:"Maybelline New York Fit Me! Pressed Powder"},
{id:"5044",text:"NARS All Day Luminous Weightless Powder Foundation SPF 24"},
{id:"5045",text:"NeoStrata Bionic Eye Cream, 4 PHA"},
{id:"5046",text:"Neutrogena MoistureShine Lip Soother Cooling Hydragel SPF 20"},
{id:"5047",text:"Neutrogena Pore Refining Exfoliating Cleanser"},
{id:"5048",text:"Neutrogena Intensive Moisture Wrap Body Treatment Norwegian Formula Fragrance Free"},
{id:"5049",text:"Michael Todd True Organics Skin Defender AHA Creme Facial Cleanser"},
{id:"5050",text:"Neutrogena Oil-Free Acne Wash Pink Grapefruit Facial Cleanser"},
{id:"5051",text:"Neutrogena Ultra Sheer Liquid Daily Sunscreen Broad Spectrum SPF 70"},
{id:"5052",text:"Neutrogena Ultra Sheer Face & Body Stick Sunscreen SPF 70"},
{id:"5053",text:"NeoStrata Psorent Psoriasis Topical Solution"},
{id:"5054",text:"Murad Overnight Soothing Gel"},
{id:"5055",text:"NYX Cosmetics Love in Paris Shadow Palette"},
{id:"5056",text:"Merle Norman Clear Complexion Spot Treatment"},
{id:"5057",text:"Olay Fresh Effects SWipe Out! Refreshing Make-Up Removal Cloths"},
{id:"5058",text:"Neutrogena Clear Face Break-Out Free Liquid Lotion Sunscreen SPF 30"},
{id:"5059",text:"Murad Perfecting Day Cream SPF 30"},
{id:"5060",text:"N.Y.C. (New York Color) Applelicious Glossy Lip Balm"},
{id:"5061",text:"Merle Norman Gentle Polish"},
{id:"5062",text:"NeoStrata Canada Daytime Anti-Wrinkle Cream SPF 15"},
{id:"5063",text:"Nuance Salma Hayek Renewed Radiance Dark Spot Corrector"},
{id:"5064",text:"Neutrogena All-in-1 Acne Control Daily Scrub"},
{id:"5065",text:"Murad Skin Smoothing Polish"},
{id:"5066",text:"Michael Todd True Organics Charcoal Detoxifying Facial Mask"},
{id:"5067",text:"NARS Soft Touch Shadow Pencil"},
{id:"5068",text:"Merle Norman Delicate Balance Moisturizer for Sensitive Skin"},
{id:"5069",text:"Meaningful Beauty Cindy Crawford Glowing Serum Skin Revitalizer"},
{id:"5070",text:"Neutrogena Deep Clean Gentle Scrub"},
{id:"5071",text:"NYX Cosmetics Born To Glow Liquid Illuminator"},
{id:"5072",text:"MDSolarSciences Mineral Beauty Balm Broad Spectrum SPF 50"},
{id:"5073",text:"Neocutis Nouvelle + Retinol Correction Cream"},
{id:"5074",text:"Michael Todd True Organics Moisture Lite Non-Oily Hydrating Lotion"},
{id:"5075",text:"N.Y.C. (New York Color) Sun 2 Sun Bronzing Powder"},
{id:"5076",text:"Olay Total Effects 7-in-1 Anti-Aging Body Lotion"},
{id:"5077",text:"NeoStrata Age Reverse HydraFirm"},
{id:"5078",text:"NeoStrata Canada Dark Circle Night Serum"},
{id:"5079",text:"Olay Ultra Moisture Lotion with Shea Butter"},
{id:"5080",text:"NARS Cream Blush"},
{id:"5081",text:"NARS Eyeliner Pencil"},
{id:"5082",text:"Maybelline New York Mineral Power Bronzer Shimmer Loose Powder"},
{id:"5083",text:"Murad Time Release Retinol Concentrate for Deep Wrinkles"},
{id:"5084",text:"Neutrogena Canada Ageless Intensives Lip Plumping Treatment SPF 20"},
{id:"5085",text:"Neutrogena Wet Skin Kids Beach & Pool Sunscreen Lotion SPF 45"},
{id:"5086",text:"Maybelline New York Instant Age Rewind The Primer Skin Transformer"},
{id:"5087",text:"NYX Cosmetics Butter Lipstick"},
{id:"5088",text:"Murad Rejuvenating Lift for Neck and Decollete"},
{id:"5089",text:"Neutrogena SkinClearing Oil-Free Makeup"},
{id:"5090",text:"Nu Skin Defining Effects Mascara"},
{id:"5091",text:"NYX Cosmetics Wonder Pencil"},
{id:"5092",text:"Olay Complete All Day Moisturizer with Sunscreen Broad Spectrum SPF 15, Normal"},
{id:"5093",text:"Nivea In-Shower Body Lotion Smoothing Dry Skin"},
{id:"5094",text:"Neutrogena Healthy Defense Daily Moisturizer SPF 50 with Purescreen for Sensitive Skin"},
{id:"5095",text:"Milani Baked Blush"},
{id:"5096",text:"Neutrogena Wave Power-Cleanser, and Deep Clean Foaming Pads"},
{id:"5097",text:"Neutrogena Crease Proof Eye Shadow"},
{id:"5098",text:"Milani Bronzer XL"},
{id:"5099",text:"Olay Age Defying Classic Cleanser"},
{id:"5100",text:"Neutrogena Canada Deep Clean Gentle Scrub Oil Free"},
{id:"5101",text:"Murad Perfecting Serum"},
{id:"5102",text:"Obagi Nu-Derm Toner"},
{id:"5103",text:"Nu Skin Nu Colour Replenishing Lipstick"},
{id:"5104",text:"Neutrogena Neutrogena Naturals Face & Body Bar"},
{id:"5105",text:"NeoStrata Exuviance OptiLight Essential 6 Serum"},
{id:"5106",text:"Nuance Salma Hayek Passion Fruit Nourishing Body Cleansing Cream"},
{id:"5107",text:"NYX Cosmetics HD Studio Photogenic Finishing Powder"},
{id:"5108",text:"NARS Pure Sheer SPF 15 Lip Treatment"},
{id:"5109",text:"Milani Grandissimo Lashes Mascara"},
{id:"5110",text:"Neutrogena Fresh Cooling Body Mist Sunscreen SPF 45"},
{id:"5111",text:"Neutrogena Healthy Skin Anti-Wrinkle Cream, Night"},
{id:"5112",text:"Milani Brow Fix Kit"},
{id:"5113",text:"Nivea NIVEA Sun-Kissed Radiance [Medium to Dark Skin]"},
{id:"5114",text:"Michael Todd True Organics Hyaluronic Acid + C Antioxidant Moisturizing Serum"},
{id:"5115",text:"N.Y.C. (New York Color) City Duet 2 in 1 Lip Color"},
{id:"5116",text:"Neutrogena Wet Skin Sunscreen Lotion SPF 45"},
{id:"5117",text:"Merle Norman Brilliant-C Moisturizer"},
{id:"5118",text:"NARS Velvet Matte Skin Tint Broad Spectrum SPF 30 Sunscreen"},
{id:"5119",text:"Neutrogena Canada Healthy Skin Anti-Wrinkle Cream Original Formula"},
{id:"5120",text:"Neutrogena Oil-Free Moisture, for Sensitive Skin"},
{id:"5121",text:"Olay Pro-X Even Skin Tone Anti-Oxidant Sunscreen Sheer Daily Moisturizer SPF 35"},
{id:"5122",text:"NeoStrata Canada Oil Free Anti-Aging Sunscreen Lotion SPF 30"},
{id:"5123",text:"Nu Skin Clear Action Acne Medication Night Treatment"},
{id:"5124",text:"NeoStrata Eye Cream, 4 PHA"},
{id:"5125",text:"NARS Instant Line & Pore Perfector"},
{id:"5126",text:"Milani Color Statement Lipstick"},
{id:"5127",text:"NYX Cosmetics Eyebrow Cake Powder"},
{id:"5128",text:"Obagi Nu-Derm Gentle Cleanser"},
{id:"5129",text:"Neutrogena Healthy Skin Smoothing Stick Treatment Concealer"},
{id:"5130",text:"Merle Norman Revitalizing Eye Gel"},
{id:"5131",text:"MD Formulations Critical Care Calming Gel"},
{id:"5132",text:"Meaningful Beauty Cindy Crawford Rich Moisture Masque"},
{id:"5133",text:"Merle Norman Miracol Revitalizing Lotion"},
{id:"5134",text:"Obagi Nu-Derm Eye Cream"},
{id:"5135",text:"Neutrogena Wet Skin Kids Beach & Pool Sunscreen Spray SPF 70"},
{id:"5136",text:"NARS Blush"},
{id:"5137",text:"Nia24 Physical Cleansing Scrub"},
{id:"5138",text:"Neutrogena Ultra Sheer Body Mist Sunscreen SPF 45"},
{id:"5139",text:"Neutrogena Rapid Clear Oil-Control Foaming Cleanser"},
{id:"5140",text:"Olay Regenerist Detoxifying Pore Scrub"},
{id:"5141",text:"Neocutis Lumiere Bio-Restorative Eye Cream with PSP"},
{id:"5142",text:"Michael Todd True Organics Honey and Oat Gentle Deep-Pore Cleanser"},
{id:"5143",text:"NARS Larger Than Life Lip Gloss"},
{id:"5144",text:"Nu Skin 180 AHA Facial Peel"},
{id:"5145",text:"Neutrogena Neutrogena Men Sensitive Skin Post Shave Balm"},
{id:"5146",text:"Murad Firm and Tone Serum"},
{id:"5147",text:"NARS Pressed Powder"},
{id:"5148",text:"NeoStrata Skin Active Triple Firming Neck Cream"},
{id:"5149",text:"Michael Todd True Organics Kaolin Clay Detoxifying Facial Mask (Normal)"},
{id:"5150",text:"NeoStrata Canada Daytime Smoothing Cream Level 2 SPF 15"},
{id:"5151",text:"NARS Contour Blush"},
{id:"5152",text:"Neutrogena Canada Oil-Free Acne Wash, Redness Soothing Cream Cleanser"},
{id:"5153",text:"NARS Duo Cream Eyeshadow"},
{id:"5154",text:"MDSolarSciences Mineral Creme Broad Spectrum SPF 50 UVA-UVB Sunscreen"},
{id:"5155",text:"NeoStrata Canada Smoothing Cream Level 3"},
{id:"5156",text:"NARS Light Reflecting Pressed Setting Powder"},
{id:"5157",text:"NeoStrata Canada Skin Brightener, Intense"},
{id:"5158",text:"Olay Pro-X Wrinkle Smoothing Cream"},
{id:"5159",text:"Merle Norman Wrinkle Smoother"},
{id:"5160",text:"Olay Fresh Effects Everything Off Deluxe Make-Up Removal Wet Cloths"},
{id:"5161",text:"N.Y.C. (New York Color) Cover Stick"},
{id:"5162",text:"Neutrogena Healthy Skin Anti-Wrinkle Anti-Blemish Cleanser"},
{id:"5163",text:"Murad Hydrating Toner"},
{id:"5164",text:"Maybelline New York Real Impact! By Great Lash"},
{id:"5165",text:"NeoStrata Exuviance Antioxidant Perfect 10 Serum"},
{id:"5166",text:"NARS Dual-Intensity Blush"},
{id:"5167",text:"Murad Anti-Aging Moisturizer SPF 20 | PA++ for Blemish-Prone Skin"},
{id:"5168",text:"NARS Larger Than Life Long-Wear Eyeliner"},
{id:"5169",text:"Nu Skin 180 UV Block Hydrator SPF 18"},
{id:"5170",text:"Neutrogena Canada Deep Clean Invigorating Foaming Scrub"},
{id:"5171",text:"Maybelline New York Volum Express Pumped Up! Colossal Mascara"},
{id:"5172",text:"Murad Body Firming Cream"},
{id:"5173",text:"Merle Norman Clear Complexion Gel Cleanser"},
{id:"5174",text:"NeoStrata Renewal Cream, 12 PHA"},
{id:"5175",text:"Neutrogena Rapid Clear Treatment Pads"},
{id:"5176",text:"Nu Skin Pure Cleansing Gel, for Combination to Oily Skin"},
{id:"5177",text:"NeoStrata Canada Smoothing Cream Level 2"},
{id:"5178",text:"NeoStrata Canada HQ Lightening Gel"},
{id:"5179",text:"Nivea A Kiss of Smoothness Hydrating Lip Care SPF 15"},
{id:"5180",text:"NYX Cosmetics Push-up Bra for Eyebrow"},
{id:"5181",text:"NeoStrata Canada Moisture Cream Infusion"},
{id:"5182",text:"Nu Skin Intensive Eye Complex"},
{id:"5183",text:"N.Y.C. (New York Color) City Curls Curling Mascara"},
{id:"5184",text:"NeoStrata Canada Toning Solution Level 1"},
{id:"5185",text:"Merle Norman Preventage Hand Treatment SPF 15"},
{id:"5186",text:"Nu Skin Epoch Polishing Bar"},
{id:"5187",text:"Neutrogena Mineral Sheers Powder Foundation"},
{id:"5188",text:"Neutrogena Oil-Free Acne Stress Control Power-Clear Scrub"},
{id:"5189",text:"Milani Lip Intense Liquid Color"},
{id:"5190",text:"Merle Norman Renew"},
{id:"5191",text:"Neutrogena Canada Moisture Oil-Free for Sensitive Skin"},
{id:"5192",text:"Maybelline New York EyeStudio Color Tattoo Concentrated Crayon"},
{id:"5193",text:"NARS Larger Than Life Lengthening Mascara"},
{id:"5194",text:"MDSolarSciences Daily Anti-Aging Moisturizer Broad Spectrum SPF 30 UVA-UVB Sunscreen With SolSci-X"},
{id:"5195",text:"Murad Gaga for Glow Facial Scrub"},
{id:"5196",text:"Meaningful Beauty Cindy Crawford Skin Softening Cleanser"},
{id:"5197",text:"NARS Velvet Matte Lip Pencil"},
{id:"5198",text:"Olay Regenerist Micro-Exfoliating Wet Cleansing Cloths"},
{id:"5199",text:"Murad Oil-Control Mattifier SPF 15"},
{id:"5200",text:"Murad Crazy for Clear Spot Treatment"},
{id:"5201",text:"MD Formulations Moisture Defense Antioxidant Lotion"},
{id:"5202",text:"Michael Todd True Organics Knu Anti Aging Face Lift Cream"},
{id:"5203",text:"NYX Cosmetics La Amourex Boudoir Individual Curl Mascara"},
{id:"5204",text:"NARS Bronzing Powder"},
{id:"5205",text:"Nuance Salma Hayek Age Affirm Firming Neck & Chest Balm"},
{id:"5206",text:"Neutrogena Oil-Free Eye Makeup Remover"},
{id:"5207",text:"Neutrogena Healthy Skin Anti-Wrinkle Cream, Original Formula SPF 15"},
{id:"5208",text:"Neutrogena Body Clear Body Wash"},
{id:"5209",text:"Nu Skin Tri-Phasic White Cleanser"},
{id:"5210",text:"Neutrogena Deep Moisture Night Cream"},
{id:"5211",text:"Nuance Salma Hayek Smoothing Anti-Aging Eye Cream"},
{id:"5212",text:"N.Y.C. (New York Color) In A New York Color Minute Eye Makeup Remover"},
{id:"5213",text:"Nu Skin Clay Pack"},
{id:"5214",text:"Nu Skin NaPCA Moisture Mist"},
{id:"5215",text:"Obagi ELASTIderm Eye Complete Complex Serum"},
{id:"5216",text:"Neutrogena Naturals Acne Foaming Scrub"},
{id:"5217",text:"Neutrogena Fresh Foaming Cleanser"},
{id:"5218",text:"Merle Norman Fine Line Minimizer"},
{id:"5219",text:"Nuance Salma Hayek Age Affirm Firming Day & Night Cream"},
{id:"5220",text:"Merle Norman AHA Toner Normal/Oily"},
{id:"5221",text:"Neutrogena Blackhead Eliminating Daily Scrub"},
{id:"5222",text:"Nu Skin 180 Skin Mist"},
{id:"5223",text:"Murad Clarifying Toner"},
{id:"5224",text:"N.Y.C. (New York Color) Classic Brow & Liner Pencils"},
{id:"5225",text:"Nuance Salma Hayek AM/PM Anti-Aging Super Eye Treatment"},
{id:"5226",text:"NeoStrata Canada Oil Free Gel Cleanser"},
{id:"5227",text:"Neutrogena Pore Refining Toner"},
{id:"5228",text:"Meaningful Beauty Cindy Crawford Pore Refining Toner"},
{id:"5229",text:"Obagi Sun Shield Matte Broad Spectrum SPF 50 Sunscreen Lotion"},
{id:"5230",text:"NeoStrata Canada Anti-Wrinkle Night Complex"},
{id:"5231",text:"Neutrogena Healthy Defense SPF 30 Daily Moisturizer, Light Tint"},
{id:"5232",text:"Neutrogena Rapid Clear Acne Defense Face Lotion"},
{id:"5233",text:"Maybelline New York Expert Eyes 100% Oil-Free Eye Makeup Remover"},
{id:"5234",text:"Maybelline New York Color Sensational High Shine Gloss"},
{id:"5235",text:"Murad T-Zone Pore Refining Serum"},
{id:"5236",text:"NARS Multi-Action Hydrating Toner"},
{id:"5237",text:"Olay Olay Body Wash Age Defying"},
{id:"5238",text:"Merle Norman Brilliant-C Eye Cream"},
{id:"5239",text:"Neutrogena Revitalizing Lip Balm SPF 20"},
{id:"5240",text:"Neutrogena Healthy Skin Liquid Makeup SPF 20"},
{id:"5241",text:"MD Formulations Vit-A-Plus Illuminating Masque"},
{id:"5242",text:"Neutrogena Naturals Acne Cream Cleanser"},
{id:"5243",text:"Maybelline New York Color Elixir by ColorSensational"},
{id:"5244",text:"Neutrogena Ultra Sheer Dry-Touch Sunscreen SPF 45"},
{id:"5245",text:"Merle Norman Cleansing Lotion"},
{id:"5246",text:"Murad Active Radiance Serum"},
{id:"5247",text:"NYX Cosmetics Mega Shine Lip Gloss"},
{id:"5248",text:"Neutrogena Ultra Sheer Body Mist Sunscreen Broad Spectrum SPF 30"},
{id:"5249",text:"Nude Skincare Purify Daily Moisturizer"},
{id:"5250",text:"MD Formulations Face and Body Scrub"},
{id:"5251",text:"Nuance Salma Hayek Advanced Hydration Smooth & Firm Protector SPF 20"},
{id:"5252",text:"Neutrogena Neutrogena Naturals Purifying Pore Scrub"},
{id:"5253",text:"NYX Cosmetics Jumbo Eye Pencil"},
{id:"5254",text:"Nivea Extended Moisture Hand Cream"},
{id:"5255",text:"Olay Daily Exfoliating Body Wash"},
{id:"5256",text:"Neocutis ReACTIVE Anti-Oxidant Serum"},
{id:"5257",text:"Neutrogena Canada All-in-One Make-up Removing Cleansing Wipes"},
{id:"5258",text:"Nivea NIVEA Sun-Kissed Radiance [Fair to Medium Skin]"},
{id:"5259",text:"Merle Norman Preventage Firming Defense Creme Normal/Oily SPF 15"},
{id:"5260",text:"Olay Foaming Face Wash, for Sensitive Skin"},
{id:"5261",text:"Neutrogena Ultra Sheer Dry-Touch Sunscreen SPF 55"},
{id:"5262",text:"NYX Cosmetics Honey Dew Me Up Primer"},
{id:"5263",text:"Maybelline New York The Flash Clean Clean Express Makeup Removing Lotion"},
{id:"5264",text:"Merle Norman Day Creme with HC-12"},
{id:"5265",text:"Neutrogena Ageless Intensives Deep Wrinkle Anti-Wrinkle Serum"},
{id:"5266",text:"NeoStrata Bio-Hydrating Cream, 15 PHA"},
{id:"5267",text:"Neutrogena Oil-Free Moisture, for Combination Skin"},
{id:"5268",text:"Neutrogena Healthy Skin Face Lotion SPF 15"},
{id:"5269",text:"Neutrogena Beach Defense Sunscreen Lotion Broad Spectrum SPF 30"},
{id:"5270",text:"Neutrogena Wave Deep Clean Gentle Exfoliating Pads (Refills)"},
{id:"5271",text:"Neutrogena Oil-Free Acne Wash Pink Grapefruit Cream Cleanser"},
{id:"5272",text:"Nuance Salma Hayek Renewing & Brightening Facial Serum"},
{id:"5273",text:"Neutrogena Sensitive Skin Sunscreen Lotion SPF 60+"},
{id:"5274",text:"Maybelline New York Lash Sensational Luscious Waterproof Mascara"},
{id:"5275",text:"Nuance Salma Hayek Ageless Clarity Acne Treatment Moisturizer"},
{id:"5276",text:"NYX Cosmetics BB Cream"},
{id:"5277",text:"Nuance Salma Hayek Age Affirm Firming Eye Balm"},
{id:"5278",text:"Olay Night of Olay Firming Cream"},
{id:"5279",text:"MDSolarSciences Daily Eye Repair Emulsion"},
{id:"5280",text:"N.Y.C. (New York Color) Browser Brush-On Brow Powder with Grooming Wax & Tweezers"},
{id:"5281",text:"Merle Norman Micro-Refiner"},
{id:"5282",text:"Neocutis Neo-Cleanse Gentle Skin Cleanser"},
{id:"5283",text:"Neutrogena Moisture Wrap Body Lotion Norwegian Formula"},
{id:"5284",text:"Murad Essential-C Toner"},
{id:"5285",text:"MD Formulations Vit-A-Plus Intensive Anti-Aging Serum"},
{id:"5286",text:"Murad Clarifying Body Spray"},
{id:"5287",text:"MD Formulations Daily Peel Pads"},
{id:"5288",text:"NYX Cosmetics Stick Blush"},
{id:"5289",text:"NARS Lip Lacquer"},
{id:"5290",text:"NYX Cosmetics The Skinny Mascara"},
{id:"5291",text:"Nuance Salma Hayek Correcting Spot Treatment 10% Sulfur Acne Treatment"},
{id:"5292",text:"Neutrogena Ultra Sheer Dry-Touch Sunscreen SPF 85"},
{id:"5293",text:"Nuance Salma Hayek Overnight Time-Release Retinol Capsules"},
{id:"5294",text:"Nu Skin 180 Cell Renewal Fluid"},
{id:"5295",text:"NeoStrata Facial Cleanser, 4 PHA"},
{id:"5296",text:"Merle Norman Foaming Cleanser Normal/Dry"},
{id:"5297",text:"Merle Norman Cleansing Cream"},
{id:"5298",text:"NeoStrata Canada Gel Cleanser"},
{id:"5299",text:"Obagi C-Sunguard SPF 30"},
{id:"5300",text:"N.Y.C. (New York Color) High Definition Liquid Eyeliner"},
{id:"5301",text:"Merle Norman Clear Complexion Moisturizer Acne Treatment Cream"},
{id:"5302",text:"NeoStrata Problem Dry Skin Cream"},
{id:"5303",text:"Milani Bella Eyes Gel Powder Eyeshadow"},
{id:"5304",text:"NYX Cosmetics Le Chick Flick Boudoir Mascara"},
{id:"5305",text:"NYX Cosmetics Doll Eye Mascara Longlash"},
{id:"5306",text:"NARS Lip Gloss"},
{id:"5307",text:"N.Y.C. (New York Color) Waterproof Eyeliner Pencil"},
{id:"5308",text:"OC Eight OC Eight Professional Mattifying Gel"},
{id:"5309",text:"Neutrogena Oil-Free Acne Wash Cream Cleanser"},
{id:"5310",text:"Neutrogena Pure & Free Liquid Daily Sunscreen SPF 50"},
{id:"5311",text:"NARS Duo Concealer"},
{id:"5312",text:"Nude Skincare Purify Cleansing Wash"},
{id:"5313",text:"NYX Cosmetics Doll Eye Mascara Volume"},
{id:"5314",text:"Neutrogena Deep Clean Long-Last Shine Control Cleanser/Mask"},
{id:"5315",text:"Murad Hydro-Dynamic Quenching Essence"},
{id:"5316",text:"Obagi Rosaclear Hydrating Complexion Corrector"},
{id:"5317",text:"MD Formulations Vit-A-Plus Clearing Complex"},
{id:"5318",text:"Olay Pro-X Hydra Firming Cream"},
{id:"5319",text:"Michael Todd True Organics Pink Grapefruit Lotion"},
{id:"5320",text:"Nu Skin Facial Scrub"},
{id:"5321",text:"Merle Norman Miracol Booster"},
{id:"5322",text:"Neutrogena Rapid Clear Stubborn Acne Spot Gel"},
{id:"5323",text:"NeoStrata Face Cream Plus, 15 AHA"},
{id:"5324",text:"Nivea Smooth Sensation Daily Lotion for Dry Skin"},
{id:"5325",text:"NARS Duo Eyeshadow"},
{id:"5326",text:"NYX Cosmetics Xtreme Lip Cream"},
{id:"5327",text:"Olay ProX Anti-Aging Nightly Purifying Micro-Peel"},
{id:"5328",text:"Murad Hybrids Eye Lift Illuminator"},
{id:"5329",text:"NARS Audacious Lipstick"},
{id:"5330",text:"NYX Cosmetics Nude Matte Shadow"},
{id:"5331",text:"Neutrogena Ultra Sheer Dry-Touch Sunblock SPF 70"},
{id:"5332",text:"N.Y.C. (New York Color) Sparkle Eye Dust"},
{id:"5333",text:"Nu Skin Tri-Phasic White Toner"},
{id:"5334",text:"Nivea Nivea Creme"},
{id:"5335",text:"Olay Regenerist Night Recovery Cream"},
{id:"5336",text:"Neutrogena SkinClearing Mineral Powder"},
{id:"5337",text:"Maybelline New York Great Lash Mascara"},
{id:"5338",text:"Maybelline New York Shine Sensational Lip Gloss"},
{id:"5339",text:"Obagi CLENZIderm M.D. Therapeutic Lotion"},
{id:"5340",text:"Michael Todd True Organics Eye-o-sonic Ultrasonic Ionic Serum Infuser"},
{id:"5341",text:"NeoStrata Canada Blemish Spot Gel"},
{id:"5342",text:"Miracle Skin Transformer Miracle Skin Transformer Hydroactive Microderm"},
{id:"5343",text:"Neutrogena Body Clear Body Scrub"},
{id:"5344",text:"NARS Double Refining Exfoliator"},
{id:"5345",text:"NYX Cosmetics The Natural Shadow Palette"},
{id:"5346",text:"Obagi Rosaclear Gentle Cleanser"},
{id:"5347",text:"Neocutis Bio-Gel Bio-Restorative Hydrogel"},
{id:"5348",text:"Neutrogena Makeup Remover Cleansing Towelettes-Hydrating"},
{id:"5349",text:"Neutrogena Pure & Free Baby Sunscreen Stick Broad Spectrum SPF 60"},
{id:"5350",text:"NYX Cosmetics Baked Blush"},
{id:"5351",text:"Nuance Salma Hayek Green Tea Cooling Eye Gel"},
{id:"5352",text:"Nia24 Depigmentation Spot Repair"},
{id:"5353",text:"Nu Skin AgeLoc Transforming Night"},
{id:"5354",text:"Nu Skin pH Balance Mattefying Toner, for Combination to Oily Skin"},
{id:"5355",text:"Murad Cleansing Shave"},
{id:"5356",text:"Neutrogena Blackhead Eliminating Cleanser Mask"},
{id:"5357",text:"Obagi Elastiderm Eye Gel"},
{id:"5358",text:"Nuance Salma Hayek Hydra Light Moisturizing Gel"},
{id:"5359",text:"Nu Skin pH Balance Toner, for Normal to Dry Skin"},
{id:"5360",text:"Neutrogena Clear Pore Oil-Eliminating Astringent"},
{id:"5361",text:"MD Formulations Vit-A-Plus Anti-Aging Serum"},
{id:"5362",text:"Nia24 Rapid Exfoliating Serum"},
{id:"5363",text:"Neutrogena Healthy Defense SPF 30 Daily Moisturizer with Helioplex"},
{id:"5364",text:"Neutrogena Rapid Tone Repair Moisturizer Night"},
{id:"5365",text:"Meaningful Beauty Cindy Crawford Eyes: Lifting Eye Creme"},
{id:"5366",text:"Maybelline New York Great Lash Waterproof Mascara"},
{id:"5367",text:"Nude Skincare Purify Toning Water"},
{id:"5368",text:"Neutrogena Oil Free Acne Moisturizer Pink Grapefruit"},
{id:"5369",text:"Nivea A Kiss of Milk and Honey Soothing Lip Care"},
{id:"5370",text:"Milani Infinite Liquid Eye Liner"},
{id:"5371",text:"Olay Regenerist Regenerating Lotion with Sunscreen Broad Spectrum SPF 50"},
{id:"5372",text:"Merle Norman Brilliant-C Cleanser"},
{id:"5373",text:"Obagi C-Clarifying Serum"},
{id:"5374",text:"Neutrogena Canada Ultra Sheer Face Sunscreen SPF 60"},
{id:"5375",text:"Murad Rapid Collagen Infusion"},
{id:"5376",text:"NARS Lip Liner Pencil"},
{id:"5377",text:"Merle Norman Foaming Cleanser Normal/Oily"},
{id:"5378",text:"Olay Active Hydrating Cream, Original"},
{id:"5379",text:"Neutrogena Oil-Free Moisture SPF 15"},
{id:"5380",text:"Obagi Elastiderm Eye Treatment Cream"},
{id:"5381",text:"Merle Norman Dual Spectrum SPF 30"},
{id:"5382",text:"Olay Total Effects 7-in-1 Anti-Aging Moisturizer Daily Moisturizer"},
{id:"5383",text:"Murad Essential-C Eye Cream SPF 15/P++"},
{id:"5384",text:"NuCerity Skincerity Nightly Breathable Masque"},
{id:"5385",text:"Nia24 Gentle Cleansing Cream"},
{id:"5386",text:"Obagi Nu-Derm Healthy Skin Protection SPF 35"},
{id:"5387",text:"MD Formulations Critical Care Skin Repair Complex"},
{id:"5388",text:"Neocutis Neo-Cleanse Exfoliating Skin Cleanser"},
{id:"5389",text:"MD Formulations Critical Care Shielding Creme"},
{id:"5390",text:"NARS Illuminator"},
{id:"5391",text:"NeoStrata Bionic Face Cream, 12 PHA"},
{id:"5392",text:"NeoStrata HQ Skin Lightening Gel 10 PHA"},
{id:"5393",text:"Olay Regenerist Luminous Brightening & Protecting Lotion With Sunscreen Broad Spectrum SPF 15 Fragrance Free"},
{id:"5394",text:"Milani The Multitasker Face Powder"},
{id:"5395",text:"N.Y.C. (New York Color) Liquid Lipshine"},
{id:"5396",text:"N.Y.C. (New York Color) Sun n Bronze Bronzing Powder"},
{id:"5397",text:"Neutrogena Canada Deep Clean Facial Cleanser, for Normal to Oily Skin"},
{id:"5398",text:"Neutrogena Healthy Skin Blends Translucent Oil-Control Powder"},
{id:"5399",text:"Neutrogena Norwegian Formula Hand Cream Fragrance Free"},
{id:"5400",text:"NARS Purifying Foam Cleanser"},
{id:"5401",text:"Michael Todd True Organics Concentrated C Antioxidant Moisturizing Serum"},
{id:"5402",text:"NYX Cosmetics Shine Killer"},
{id:"5403",text:"NeoStrata Canada Natural Insta-Tan"},
{id:"5404",text:"NARS Eye Paint"},
{id:"5405",text:"Olay Complete All Day Moisturizer with Sunscreen Broad Spectrum SPF 15, Sensitive"},
{id:"5406",text:"Nivea A Kiss of Moisture Essential Lip Care"},
{id:"5407",text:"Neutrogena Wave Sonic 2-Speed Spinning Power Cleanser"},
{id:"5408",text:"Murad Complete Reform"},
{id:"5409",text:"Nu Skin Nu Colour Contouring Lip Gloss"},
{id:"5410",text:"Neutrogena Oil-Free Acne Stress Control Power Gel Cleanser"},
{id:"5411",text:"Neutrogena Neutrogena Men Sensitive Skin Oil-Free Moisture SPF 30"},
{id:"5412",text:"Neutrogena The Transparent Facial Bar, Original Formula"},
{id:"5413",text:"NYX Cosmetics Mineral Stick Foundation"},
{id:"5414",text:"Neutrogena Beach Defense Sunscreen Lotion Broad Spectrum SPF 70"},
{id:"5415",text:"Neutrogena Ageless Intensives Deep Wrinkle Anti-Wrinkle Moisture SPF 20"},
{id:"5416",text:"Maybelline New York Dream Pure BB 8-in-1 Beauty Balm Skin Clearing Perfector 2% Salicylic Acid Acne Treatment"},
{id:"5417",text:"MD Formulations Moisture Defense Antioxidant Comfort Creme"},
{id:"5418",text:"Milani Rose Powder Blush"},
{id:"5419",text:"Neutrogena Deep Clean Facial Cleanser, for Normal to Oily Skin"},
{id:"5420",text:"Neutrogena Age Shield Face Sunscreen Lotion, SPF 110"},
{id:"5421",text:"NYX Cosmetics Butter Gloss"},
{id:"5422",text:"Merle Norman AHA Intensive Complex"},
{id:"5423",text:"Merle Norman Clarifying Clay Mask"},
{id:"5424",text:"N.Y.C. (New York Color) Big Bold Gloss"},
{id:"5425",text:"Neutrogena Rapid Wrinkle Repair Serum"},
{id:"5426",text:"Meaningful Beauty Cindy Crawford Restore: Anti-Aging Night Creme"},
{id:"5427",text:"NYX Cosmetics HD Studio Photogenic Grinding Powder"},
{id:"5428",text:"NYX Cosmetics Eyebrow Gel Waterproof"},
{id:"5429",text:"Murad Sensitive Skin Soothing Serum"},
{id:"5430",text:"NYX Cosmetics Incredible Waterproof Concealer Stick"},
{id:"5431",text:"Miracle Skin Transformer Miracle Skin Transformer SPF 20"},
{id:"5432",text:"Nu Skin Nu Colour Nutriol Eyelash Treatment"},
{id:"5433",text:"Neutrogena SkinClearing Blemish Concealer"},
{id:"5434",text:"NYX Cosmetics Badunkadunk Lush Lashes Mascara"},
{id:"5435",text:"Murad Refreshing Cleanser"},
{id:"5436",text:"Neocutis Bio-Serum Intensive Treatment"},
{id:"5437",text:"Neutrogena Shine Control Makeup SPF 20"},
{id:"5438",text:"NYX Cosmetics Super Skinny Eye Marker"},
{id:"5439",text:"Meaningful Beauty Cindy Crawford Lifting Eye Crme-Advanced Formula"},
{id:"5440",text:"NARS Pro-Prime Light Optimizing Primer Broad Spectrum SPF 15"},
{id:"5441",text:"Obagi Professional-C Serum 10%"},
{id:"5442",text:"Neutrogena Shine Control Powder"},
{id:"5443",text:"NeoStrata Canada Oil Free Daytime Smoothing Lotion Level 3 SPF 15"},
{id:"5444",text:"Neutrogena Oil-Free Acne Stress Control Power-Cream Wash"},
{id:"5445",text:"NeoStrata Canada Wrinkle Repair, Intense"},
{id:"5446",text:"MDSolarSciences Mineral Sunscreen Stick Broad Spectrum SPF 40 UVA-UVB Sunscreen"},
{id:"5447",text:"N.Y.C. (New York Color) Eyeliner Duet"},
{id:"5448",text:"Nu Skin Tru Face Revealing Gel"},
{id:"5449",text:"Murad Soothing Skin Lip & Cuticle Care"},
{id:"5450",text:"NeoStrata Canada Anti-Expression Line Cream"},
{id:"5451",text:"NARS Eyeshadow"},
{id:"5452",text:"NYX Cosmetics The Curve Felt Tip Eye Liner"},
{id:"5453",text:"Nu Skin Epoch Glacial Marine Mud"},
{id:"5454",text:"Nu Skin Polishing Peel Skin Refinisher"},
{id:"5455",text:"Michael Todd True Organics Liposome Antioxidant Moisturizer"},
{id:"5456",text:"Neutrogena Microdermabrasion System"},
{id:"5457",text:"Michael Todd True Organics Rooibos Tea Gel Eye Treatment"},
{id:"5458",text:"MDSolarSciences Quick Dry Body Spray With SolSci-X Broad Spectrum SPF 40 UVA-UVB Sunscreen"},
{id:"5459",text:"Nuance Salma Hayek Dark Cacao & Coffee Firming Body Cream"},
{id:"5460",text:"Nuance Salma Hayek Jojoba Dual Phase Body Oil"},
{id:"5461",text:"Obagi Nu-Derm Sunfader SPF 15"},
{id:"5462",text:"Neutrogena Body Clear Body Spray"},
{id:"5463",text:"Obagi Nu-Derm Hydrate Facial Moisturizer"},
{id:"5464",text:"Nu Skin Tri-Phasic White Night Cream"},
{id:"5465",text:"Neutrogena Canada Oil-Free Acne Wash Redness Soothing Gentle Scrub"},
{id:"5466",text:"Merle Norman AHA Toner, for Normal/Dry"},
{id:"5467",text:"NYX Cosmetics Doll Eye Mascara Waterproof"},
{id:"5468",text:"Murad Acne Clearing Solution"},
{id:"5469",text:"Maybelline New York Master Drama Cream Pencil"},
{id:"5470",text:"Neutrogena Oil-Free Acne Wash Pink Grapefruit Foaming Scrub"},
{id:"5471",text:"Neutrogena Oil-Free Acne Stress Control Night Cleansing Pads"},
{id:"5472",text:"Nu Skin Rejuvenating Cream"},
{id:"5473",text:"Neutrogena Ultimate Sport Sunscreen Lotion SPF 70+"},
{id:"5474",text:"Nivea Skin Firming Hydration Body Lotion with Q10 Plus"},
{id:"5475",text:"Murad Skin Perfecting Primer Acne Shine Control"},
{id:"5476",text:"Murad Perfecting Night Cream"},
{id:"5477",text:"NeoStrata Canada Instant Tightening Serum, Intense"},
{id:"5478",text:"Neutrogena Extra Gentle Eye Makeup Remover Pads"},
{id:"5479",text:"N.Y.C. (New York Color) Show Time Waterproof Volumizing Mascara"},
{id:"5480",text:"MD Formulations Vit-A-Plus Illuminating Serum"},
{id:"5481",text:"MDSolarSciences Evening Facial Repair Serum"},
{id:"5482",text:"NARS Eyebrow Pencil"},
{id:"5483",text:"Neutrogena Ultra Gentle Daily Cleanser Foaming Formula"},
{id:"5484",text:"MD Formulations Lip Balm SPF 20"},
{id:"5485",text:"Neutrogena Healthy Skin Glow Sheers SPF 30"},
{id:"5486",text:"NARS Skin Smoothing Face Prep"},
{id:"5487",text:"MD Formulations Moisture Defense Antioxidant Creme"},
{id:"5488",text:"Nuance Salma Hayek AM/PM Anti-Aging Super Lotion"},
{id:"5489",text:"Neutrogena Deep Clean Invigorating Foaming Scrub"},
{id:"5490",text:"NYX Cosmetics Za Za Zu Luscious Volume Mascara"},
{id:"5491",text:"Merle Norman Lip Revive"},
{id:"5492",text:"NeoStrata Canada Eye Contour Firming Cream"},
{id:"5493",text:"Merle Norman Miracol  Revitalizing Cream"},
{id:"5494",text:"NARS Larger Than Life Volumizing Mascara"},
{id:"5495",text:"Merle Norman Very Gentle Eye Makeup Remover"},
{id:"5496",text:"Obagi CLENZIderm M.D. Pore Therapy"},
{id:"5497",text:"NARS All Day Luminous Weightless Foundation"},
{id:"5498",text:"MD Formulations Moisture Defense Antioxidant Spray"},
{id:"5499",text:"Neutrogena Triple Age Repair Broad Spectrum SPF 25"},
{id:"5500",text:"NeoStrata Bionic Skin Lightening Cream SPF 15, 10 PHA"},
{id:"5501",text:"MD Formulations The Temps Lip Plumping Treatment"},
{id:"5502",text:"Michael Todd True Organics Organic Lemon AHA + DMAE Toner"},
{id:"5503",text:"Nuance Salma Hayek Renewed Radiance Nutrifying Night Cream"},
{id:"5504",text:"Nu Skin Tru Face Line Corrector"},
{id:"5505",text:"Michael Todd True Organics Antioxidant Carrot Multivitamin Serum"},
{id:"5506",text:"Michael Todd True Organics Charcoal Detox Cleanser Deep Pore Gel Cleanser"},
{id:"5507",text:"Neutrogena Clear Pore Daily Scrub"},
{id:"5508",text:"Neutrogena Rapid Clear Stubborn Acne Cleanser"},
{id:"5509",text:"Nia24 Sun Damage Repair for Decolletage and Hands"},
{id:"5510",text:"Neutrogena Pure & Free Baby Sunscreen Lotion SPF 60+ with PureScreen"},
{id:"5511",text:"Meaningful Beauty Cindy Crawford Antioxidant Day Crme Broad Spectrum SPF 20"},
{id:"5512",text:"NYX Cosmetics Twin Cake Powder SPF 10"},
{id:"5513",text:"MD Formulations Benzoyl Peroxide 10"},
{id:"5514",text:"Nerium AD Nerium AD Age-Defying Night Cream"},
{id:"5515",text:"MD Formulations Vit-A-Plus Clearing Complex Masque"},
{id:"5516",text:"Nu Skin 180 Face Wash"},
{id:"5517",text:"Neutrogena Ultra Light Cleansing Oil"},
{id:"5518",text:"MD Formulations Continuous Renewal Complex"},
{id:"5519",text:"NeoStrata Canada Oil Free Matifying Fluid"},
{id:"5520",text:"Murad Intensive-C Radiance Peel"},
{id:"5521",text:"Obagi C-Balancing Toner for Normal to Oily Skin"},
{id:"5522",text:"Neutrogena Visibly Even Daily Moisturizer SPF 30"},
{id:"5523",text:"Olay Total Effects 7-In-1 Anti-Aging UV Moisturizer Plus SPF 15, Fragrance-Free"},
{id:"5524",text:"Nuance Salma Hayek My Secret Super Brightening Hand Cream"},
{id:"5525",text:"NARS Sheer Glow Foundation"},
{id:"5526",text:"Nu Skin AgeLoc Gentle Cleanse & Tone"},
{id:"5527",text:"Michael Todd True Organics Peppermint Wake Up Wash Daily Body Wash"},
{id:"5528",text:"Michael Todd True Organics Wild Rose Petal Creamy Body Wash"},
{id:"5529",text:"Neutrogena Rapid Clear Acne Eliminating Spot Gel"},
{id:"5530",text:"Obagi Rosaclear Metronidazole Topical Gel"},
{id:"5531",text:"MD Formulations Moisture Defense Antioxidant Eye Creme"},
{id:"5532",text:"MD Formulations Continuous Renewal Serum"},
{id:"5533",text:"Nuance Salma Hayek AM/PM Anti-Aging Super Serum Concentrate"},
{id:"5534",text:"NARS Eyeliner Stylo"},
{id:"5535",text:"Neutrogena Canada Deep Clean Cream Cleanser"},
{id:"5536",text:"NeoStrata Ultra Smoothing Lotion, 10 AHA"},
{id:"5537",text:"NARS Radiant Cream Compact Foundation"},
{id:"5538",text:"Merle Norman Delicate Balance Calming Cleanser"},
{id:"5539",text:"Nu Skin Defining Effects Smooth Eye Liner"},
{id:"5540",text:"NARS Aqua Gel Luminous Oil-Free Moisturizer"},
{id:"5541",text:"Merle Norman Brilliant-C Toner"},
{id:"5542",text:"Neutrogena Ageless Intensives Deep Wrinkle Anti-Wrinkle Moisture, Night"},
{id:"5543",text:"N.Y.C. (New York Color) Sky Rise Lengthening Mascara"},
{id:"5544",text:"N.Y.C. (New York Color) Automatic Lipliner"},
{id:"5545",text:"Neocutis PECHE Redness Control"},
{id:"5546",text:"NYX Cosmetics Le Frou Frou Volume + Length Mascara"},
{id:"5547",text:"Neutrogena Age Fighter Face Moisturizer SPF 15"},
{id:"5548",text:"Neutrogena Canada Ageless Intensives Deep Wrinkle Moisture SPF 20"},
{id:"5549",text:"Obagi CLENZIderm M.D. Daily Care Foaming Cleanser"},
{id:"5550",text:"Neutrogena Healthy Skin Firming Cream SPF 15"},
{id:"5551",text:"Neutrogena The Transparent Facial Bar, Acne-Prone Skin Formula"},
{id:"5552",text:"Nu Skin Tru Face Priming Solution"},
{id:"5553",text:"Merle Norman Daily Moisture Cream"},
{id:"5554",text:"NARS Velvet Gloss Lip Pencil"},
{id:"5555",text:"Merle Norman Brilliant-C Neck and Chest Cream"},
{id:"5556",text:"NARS Sheer Lipstick"},
{id:"5557",text:"Murad Renewing Eye Cream"},
{id:"5558",text:"Murad Age-Diffusing Serum"},
{id:"5559",text:"Neutrogena Clear Face Break-Out Free Liquid-Lotion Sunscreen SPF 55"},
{id:"5560",text:"Merle Norman Moisture Emulsion"},
{id:"5561",text:"NYX Cosmetics Retractable Eye Liner"},
{id:"5562",text:"NARS Smudge Brush"},
{id:"5563",text:"Neutrogena Beach Defense Water + Sun Barrier Stick Sunscreen Broad Spectrum SPF 50+"},
{id:"5564",text:"Neutrogena Mineral Sheers Loose Powder Foundation with PureScreen SPF 20"},
{id:"5565",text:"Meaningful Beauty Cindy Crawford Decollete: Firming Chest and Neck Creme"},
{id:"5566",text:"NeoStrata Skin Active Exfoliating Wash"},
{id:"5567",text:"Neutrogena Pure & Free Baby Faces Ultra Gentle Sunscreen SPF 45+"},
{id:"5568",text:"NYX Cosmetics Hot Singles Eye Shadow"},
{id:"5569",text:"Neutrogena Healthy Skin Anti-Aging Perfector Moisturizing Retinol Treatment SPF 20"},
{id:"5570",text:"NeoStrata High Potency Cream, 20 AHA"},
{id:"5571",text:"N.Y.C. (New York Color) Liquid Eyeliner"},
{id:"5572",text:"MD Formulations Facial Cleanser, Sensitive Skin Formula"},
{id:"5573",text:"N.Y.C. (New York Color) Smooth Skin Pressed Face Powder"},
{id:"5574",text:"NARS Pure Matte Lipstick"},
{id:"5575",text:"NeoStrata Oily Skin Solution, 8 AHA"},
{id:"5576",text:"Neutrogena Triple Age Repair Night Moisturizer"},
{id:"5577",text:"NYX Cosmetics Studio Liquid Liner"},
{id:"5578",text:"MD Formulations Vit-A-Plus Anti-Aging Eye Complex"},
{id:"5579",text:"Neutrogena Ultimate Sport Sunscreen Lotion SPF 30"},
{id:"5580",text:"Michael Todd True Organics Organic Cranberry Antiox Hydrating Anti-Aging Toner"},
{id:"5581",text:"Neutrogena Oil-Free Cleansing Wipes-Pink Grapefruit"},
{id:"5582",text:"Neutrogena Build-A-Tan Gradual Sunless Tanning"},
{id:"5583",text:"Michael Todd True Organics Blue Green Algae Antibacterial Toner"},
{id:"5584",text:"NYX Cosmetics Stay Matte But Not Flat Powder Foundation"},
{id:"5585",text:"NYX Cosmetics Fly With Me Mascara"},
{id:"5586",text:"Milani Even-Touch Powder Foundation"},
{id:"5587",text:"Neutrogena Healthy Skin Pressed Powder"},
{id:"5588",text:"Neutrogena Canada Deep Clean Oil-Free Make-up Remover Wipes"},
{id:"5589",text:"Neutrogena Ultra Sheer Dry-Touch Sunscreen SPF 30"},
{id:"5590",text:"Neutrogena Canada Ultra Sheer Face Sunscreen SPF 45"},
{id:"5591",text:"NeoStrata Canada Body Smoothing Lotion"},
{id:"5592",text:"Neutrogena Rapid Wrinkle Repair Moisturizer SPF 30"},
{id:"5593",text:"Neutrogena MicroMist Airbrush Sunless Tan"},
{id:"5594",text:"NeoStrata Bionic Lotion, 15 PHA"},
{id:"5595",text:"NYX Cosmetics Mosaic Powder"},
{id:"5596",text:"NARS Concealer"},
{id:"5597",text:"NARS Smudge Proof Eyeshadow Base"},
{id:"5598",text:"Neutrogena Nourishing Eyeliner"},
{id:"5599",text:"Neutrogena Moisture Wrap Daily Repair Body Lotion"},
{id:"5600",text:"NeoStrata Bionic Face Serum, 10 PHA"},
{id:"5601",text:"Neutrogena Canada Fresh Foaming Cleanser"},
{id:"5602",text:"Neutrogena Healthy Volume Waterproof Mascara"},
{id:"5603",text:"Neutrogena Invigorating Face Wash"},
{id:"5604",text:"Noxzema Plus Moisturizers Deep Cleansing Cream"},
{id:"5605",text:"Neutrogena Oil-Free Acne Wash Daily Scrub"},
{id:"5606",text:"Neutrogena Deep Moisture Day Cream with Sunscreen Broad Spectrum SPF 20"},
{id:"5607",text:"Neutrogena Healthy Skin Visibly Even Foaming Cleanser"},
{id:"5608",text:"MD Formulations Facial Cleanser"},
{id:"5609",text:"Nude Skincare Advanced Renewal Overnight Repair Mask"},
{id:"5610",text:"Nu Skin Tru Face Skin Perfecting Gel"},
{id:"5611",text:"Miracle Skin Transformer Transformer Treat & Conceal Eye & Face"},
{id:"5612",text:"Meaningful Beauty Cindy Crawford Purify: Deep Cleansing Masque"},
{id:"5613",text:"Neutrogena Healthy Skin Custom Glow Bronzer"},
{id:"5614",text:"NARS Body Glow"},
{id:"5615",text:"Murad Hybrids Eye Lift Perfector"},
{id:"5616",text:"Murad Balancing Moisturizer Broad Spectrum SPF 15"},
{id:"5617",text:"Nu Skin Clear Action Acne Medication Day Treatment"},
{id:"5618",text:"Milani Baked Bronzer"},
{id:"5619",text:"Michael Todd True Organics Citrus Cream Moisturizer"},
{id:"5620",text:"Merle Norman Preventage Firming Defense Creme Normal/Dry SPF 15"},
{id:"5621",text:"Nu Skin Moisture Restore Day Protective Lotion SPF 15, for Normal to Dry Skin"},
{id:"5622",text:"Nu Skin 180 Night Complex"},
{id:"5623",text:"Neutrogena Makeup Remover Cleansing Towelettes, Night Calming"},
{id:"5624",text:"Murad Skin Perfecting Lotion"},
{id:"5625",text:"Murad Sheer Lustre Day Moisture SPF 15"},
{id:"5626",text:"Merle Norman Clear Complexion Toner"},
{id:"5627",text:"NARS The Multiple"},
{id:"5628",text:"NYX Cosmetics Gotcha Covered Concealer"},
{id:"5629",text:"Nia24 Eye Repair Complex"},
{id:"5630",text:"Nuance Salma Hayek Rosehip Moisturizing Body Lotion"},
{id:"5631",text:"Neutrogena Healthy Defense Daily Moisturizer SPF 50 with Helioplex"},
{id:"5632",text:"Neutrogena Visibly Even BB Cream SPF 30"},
{id:"5633",text:"Nuance Salma Hayek Walnut Body Scrub"},
{id:"5634",text:"Neocutis Bio-Cream Riche Bio-Restorative Skin Balm with PSP"},
{id:"5635",text:"NeoStrata Lotion Plus, 15 AHA"},
{id:"5636",text:"Nu Skin NaPCA Moisturizer"},
{id:"5637",text:"Neutrogena Eye Makeup Remover Lotion-Hydrating"},
{id:"5638",text:"NYX Cosmetics Mood Lip Gloss"},
{id:"5639",text:"Neutrogena Triple Protect Face Lotion SPF 20"},
{id:"5640",text:"NeoCeuticals by NeoStrata NeoCeuticals Oil Control Gel"},
{id:"5641",text:"Nivea Express Hydration Daily Lotion with Sea Minerals and Lotus Flower  Scent for Normal to Dry Skin"},
{id:"5642",text:"Neocutis Bio-Cream Bio-Restorative Skin Cream with PSP"},
{id:"5643",text:"Merle Norman Derma-Peel System"},
{id:"5644",text:"Michael Todd True Organics Jojoba Charcoal Facial Scrub"},
{id:"5645",text:"Neocutis Lumiere Riche Bio-restorative Eye Balm"},
{id:"5646",text:"Neutrogena Wet Skin Sunscreen Spray SPF 85+"},
{id:"5647",text:"Maybelline New York Brow Drama Pro Palette"},
{id:"5648",text:"N.Y.C. (New York Color) Brow & Liner Pencil Twin Pack"},
{id:"5649",text:"NYX Cosmetics Extra Creamy Round Lipstick"},
{id:"5650",text:"N.Y.C. (New York Color) Individual Eyes Custom Compact"},
{id:"5651",text:"Maybelline New York The Falsies Volum Express Flared Mascara"},
{id:"5652",text:"NARS NARSissist L'Amour, Toujours L'Amour Eyeshadow Palette"},
{id:"5653",text:"Nu Skin Creamy Cleansing Lotion, for Normal to Dry Skin"},
{id:"5654",text:"N.Y.C. (New York Color) Smooch Proof LIPStain 16H"},
{id:"5655",text:"Mederma Mederma for Kids"},
{id:"5656",text:"NYX Cosmetics Vinyl Liquid Liner"},
{id:"5657",text:"Murad Recovery Treatment Gel"},
{id:"5658",text:"MD Formulations Facial Cleansing Gel"},
{id:"5659",text:"NYX Cosmetics Matte Lipstick"},
{id:"5660",text:"Nude Skincare Miracle Mask"},
{id:"5661",text:"Nu Skin Sunright Body Block SPF 30"},
{id:"5662",text:"Neutrogena Neutrogena Naturals Lip Balm"},
{id:"5663",text:"Neutrogena Healthy Lengths Mascara"},
{id:"5664",text:"Neutrogena Healthy Volume Mascara"},
{id:"5665",text:"Nu Skin Epoch Blemish Treatment"},
{id:"5666",text:"NYX Cosmetics HD Photogenic Concealer"},
{id:"5667",text:"Maybelline New York Clean Express Classic Eye Makeup Remover"},
{id:"5668",text:"Neutrogena Canada Eye Make-up Remover Lotion Hydrating"},
{id:"5669",text:"Nivea Soft Moisturizing Creme"},
{id:"5670",text:"Neutrogena Hydro Boost Water Gel Cream"},
{id:"5671",text:"Murad Hydro-Dynamic Moisture for Eyes"},
{id:"5672",text:"NeoStrata Canada Spot Lightener"},
{id:"5673",text:"Obagi Obagi Tretinoin Cream 0.025%, 0.05% and 0.1%"},
{id:"5674",text:"NYX Cosmetics Intense Butter Gloss"},
{id:"5675",text:"Michael Todd True Organics Tropical Fruit Enzyme Exfoliant Scrub"},
{id:"5676",text:"Obagi Rosaclear Skin Balancing Sun Protection SPF 30"},
{id:"5677",text:"Neutrogena Canada Healthy Skin Eye Cream"},
{id:"5678",text:"Neutrogena Nourishing Long Wear Liquid Makeup Broad Spectrum SPF 20"},
{id:"5679",text:"Nuance Salma Hayek AM/PM Anti-Aging Super Cream"},
{id:"5680",text:"Neutrogena Oil-Free Acne Wash"},
{id:"5681",text:"Noxzema Triple Clean Anti-Blemish Pads"},
{id:"5682",text:"Murad Soothing Gel Cleanser"},
{id:"5683",text:"NeoStrata Canada Firming Cream, Intense"},
{id:"5684",text:"Murad Intensive Resurfacing Peel"},
{id:"5685",text:"Olay Clean & Mild Make-Up Remover Cloths"},
{id:"5686",text:"Nu Skin Tri-Phasic White Day Milk Lotion SPF 15"},
{id:"5687",text:"Neutrogena Healthy Skin Primer with Sunscreen Broad Spectrum SPF 15"},
{id:"5688",text:"Merle Norman Delicate Balance Soothing Toner"},
{id:"5689",text:"Olay Fresh Effects Clear Skin Swirled Mattifier! Redness and Pore Reducing Mattifier"},
{id:"5690",text:"Nuance Salma Hayek Renewed Radiance Moisturizing Day Cream SPF 30"},
{id:"5691",text:"Obagi Nu-Derm Blender"},
{id:"5692",text:"Mederma Mederma PM Intensive Overnight Scar Cream"},
{id:"5693",text:"Murad Age-Diffusing Firming Mask"},
{id:"5694",text:"MD Formulations Glycare Lotion"},
{id:"5695",text:"Michael Todd True Organics Antiox Serum"},
{id:"5696",text:"NYX Cosmetics Stay Matte But Not Flat Liquid Foundation"},
{id:"5697",text:"N.Y.C. (New York Color) Skin Matching Foundation"},
{id:"5698",text:"Neutrogena Liquid Neutrogena Facial Cleansing Formula, Fragrance-Free"},
{id:"5699",text:"Obagi CLENZIderm M.D. Therapeutic Moisturizer"},
{id:"5700",text:"NeoStrata Canada Daytime Smoothing Cream Level 1 SPF 15"},
{id:"5701",text:"NeoStrata Canada Anti-Wrinkle Cream"},
{id:"5702",text:"Nu Skin Celltrex Ultra Recovery Fluid"},
{id:"5703",text:"Nu Skin Tri-Phasic White Radiance Mask"},
{id:"5704",text:"NARS Highlighting Blush"},
{id:"5705",text:"Murad Acne & Wrinkle Reducer"},
{id:"5706",text:"NYX Cosmetics Soft Matte Lip Cream"},
{id:"5707",text:"Mederma Mederma Stretch Marks Therapy"},
{id:"5708",text:"Olay Regenerist Regenerating Serum Fragrance-Free"},
{id:"5709",text:"Nu Skin Night Supply Nourishing Cream, for Normal to Dry Skin"},
{id:"5710",text:"Nu Skin Finishing Powder"},
{id:"5711",text:"Neutrogena Foot Cream Norwegian Formula"},
{id:"5712",text:"Merle Norman Anti-Redness Cream"},
{id:"5713",text:"Olay Age Defying Anti-Wrinkle Eye Cream"},
{id:"5714",text:"Nuance Salma Hayek Anti-Aging Smooth & Firm Elixir"},
{id:"5715",text:"Obagi C-Cleansing Gel"},
{id:"5716",text:"NARS Pure Radiant Tinted Moisturizer SPF 30 Sunscreen/PA+++"},
{id:"5717",text:"Merle Norman Refining Lotion"},
{id:"5718",text:"NYX Cosmetics Jumbo Lip Pencil"},
{id:"5719",text:"Neutrogena Razor Defense Face Scrub"},
{id:"5720",text:"Miracle Skin Transformer Lip Rewind Advanced Peptide Lip Treatment Broad-Spectrum SPF 20 Sunscreen"},
{id:"5721",text:"Nude Skincare Perfect Cleansing Oil for Face & Eyes"},
{id:"5722",text:"Neutrogena Rapid Tone Repair Moisturizer with Sunscreen Broad Spectrum SPF 30"},
{id:"5723",text:"N.Y.C. (New York Color) Ultra Last Lip Wear"},
{id:"5724",text:"Neutrogena Oil-Free Moisture SPF 35"},
{id:"5725",text:"Michael Todd True Organics Avocado and Mango Hydrating Facial Mask"},
{id:"5726",text:"Nu Skin AgeLoc Future Serum"},
{id:"5727",text:"Neutrogena Deep Clean Cream Cleanser"},
{id:"5728",text:"Neocutis Journee Bio-Restorative Day Cream Broad Spectrum Sunscreen SPF 30"},
{id:"5729",text:"Murad Age-Balancing Night Cream"},
{id:"5730",text:"NARS Luminous Moisture Cream"},
{id:"5731",text:"Olay Complete All Day Moisturizer with Sunscreen Broad Spectrum SPF 15, Combination/Oily"},
{id:"5732",text:"Maybelline New York The Mega Plush Volum Express Mascara"},
{id:"5733",text:"Olay Moisturizing Lotion Sensitive Skin"},
{id:"5734",text:"NYX Cosmetics Matte Bronzer"},
{id:"5735",text:"Miracle Skin Transformer Miracle Revival Mud Skin Restoring Treatment Mask"},
{id:"5736",text:"Neutrogena Hydro Boost Gel-Cream Eye"},
{id:"5737",text:"Neutrogena Make-Up Remover Cleansing Towelettes"},
{id:"5738",text:"Obagi Professional-C Serum 5%, for Eyes"},
{id:"5739",text:"NeoStrata Canada All-in-One Night Serum"},
{id:"5740",text:"NARS Monoi Body Glow II"},
{id:"5741",text:"Neutrogena Hydro Boost Gel Cream Extra-Dry Skin"},
{id:"5742",text:"NYX Cosmetics Liquid Suede Cream Lipstick"},
{id:"5743",text:"Neutrogena Moisture Smooth Color Stick"},
{id:"5744",text:"Miracle Skin Transformer Vanish Super C + Ergothioneine Instant Imperfection Corrector"},
{id:"5745",text:"NeoStrata Ultra Moisturizing Face Cream"},
{id:"5746",text:"Neutrogena Shine Control Primer Rice Protein Complex"},
{id:"5747",text:"Nu Skin Custom Colour Desired Effects Eye Shadow"},
{id:"5748",text:"NARS Matte Multiple"},
{id:"5749",text:"Neutrogena Clear Pore Cleanser/Mask"},
{id:"5750",text:"Neutrogena Ultra Gentle Hydrating Cleanser, Creamy Formula"},
{id:"5751",text:"Neutrogena Neutrogena Naturals Purifying Facial Cleanser"},
{id:"5752",text:"Nu Skin Clear Action Acne Medication Toner"},
{id:"5753",text:"N.Y.C. (New York Color) Smooth Skin Liquid Makeup"},
{id:"5754",text:"Neutrogena Razor Defense Shave Gel"},
{id:"5755",text:"Nu Skin Face Lift, Original Formula"},
{id:"5756",text:"Olay Total Effects 7-in-1 Anti-Aging UV Moisturizer Plus Touch of Foundation SPF 15"},
{id:"5757",text:"Neutrogena Wet Skin Sunscreen Spray SPF 30"},
{id:"5758",text:"Olay Regenerist Advanced Anti-Aging Eye Roller"},
{id:"5759",text:"NYX Cosmetics Eye Shadow Base"},
{id:"5760",text:"N.Y.C. (New York Color) Show Time Volumizing Mascara"},
{id:"5761",text:"NeoStrata Canada Eye Contour All-in-One Serum"},
{id:"5762",text:"Nu Skin Nu Colour Custom Colour MoisturShade Wet/Dry Pressed Powder"},
{id:"5763",text:"Neutrogena Beach Defense Sunscreen Spray Broad Spectrum SPF 30"},
{id:"5764",text:"Olay Fresh Effects Bead Me Up! Exfoliating Cleanser"},
{id:"5765",text:"Miracle Skin Transformer Miracle Skin Transformer Hydroactive Cleanser"},
{id:"5766",text:"Neutrogena Wet Skin Sunscreen Spray, SPF 50"},
{id:"5767",text:"NeoStrata Canada Skin Resurfacing Duo"},
{id:"5768",text:"Milani Lash Trifecta"},
{id:"5769",text:"Nu Skin Creamy Hydrating Masque"},
{id:"5770",text:"NYX Cosmetics Slim Lip Pencil"},
{id:"5771",text:"Nude Skincare Perfect Polish"},
{id:"5772",text:"Nu Skin Exfoliant Scrub Extra Gentle"},
{id:"5773",text:"Nia24 Skin Strengthening Complex"},
{id:"5774",text:"NYX Cosmetics Rouge Cream Blush"},
{id:"5775",text:"Neutrogena Razor Defense Post Shave Lotion"},
{id:"5776",text:"Nu Skin AgeLoc Radiant Day SPF 22"},
{id:"5777",text:"Nu Skin Celltrex CoQ10 Complete"},
{id:"5778",text:"NYX Cosmetics Whipped Lip & Cheek Souffle"},
{id:"5779",text:"Neutrogena Beach Defense Sunscreen Spray Broad Spectrum SPF 70"},
{id:"5780",text:"NARS Aqua Gel Luminous Mask"},
{id:"5781",text:"Milani Smooth Finish Cream-to-Powder Makeup Oil Free"},
{id:"5782",text:"Maybelline New York Lash Sensational Mascara"},
{id:"5783",text:"Neutrogena Healthy Skin Compact Makeup SPF 55"},
{id:"5784",text:"Meaningful Beauty Cindy Crawford Eye Enhancing Serum"},
{id:"5785",text:"Neutrogena Ultimate Sport Sunscreen Spray, SPF 100+"},
{id:"5786",text:"MD Formulations Glycare Acne Gel"},
{id:"5787",text:"Maybelline New York Lash Sensational Luscious Mascara"},
{id:"5788",text:"Merle Norman Wrinkle Smoother Eye"},
{id:"5789",text:"NYX Cosmetics Black Label Lipstick"},
{id:"5790",text:"MD Formulations Continuous Renewal Serum, Sensitive Skin Formula"},
{id:"5791",text:"NeoCeuticals by NeoStrata Skin Brightening Gel"},
{id:"5792",text:"Olay Regenerist Micro-Sculpting Cream with Sunscreen Broad Spectrum SPF 30 Moisturizer"},
{id:"5793",text:"Neutrogena Light Night Cream"},
{id:"5794",text:"NARS Dual-Intensity Eyeshadow"},
{id:"5795",text:"Neutrogena Rapid Clear 2-in-1 Fight & Fade Gel"},
{id:"5796",text:"Murad Renewing Cleansing Cream"},
{id:"5797",text:"Murad Razor Burn Rescue"},
{id:"5798",text:"Merle Norman Age-Defying Serum"},
{id:"5799",text:"Obagi Nu-Derm Physical UV Block SPF 32"},
{id:"5800",text:"Neutrogena Rapid Clear 2-in-1 Fight & Fade Toner"},
{id:"5801",text:"Merle Norman Changing Skin Night Creme"},
{id:"5802",text:"NYX Cosmetics Lid Lacquer"},
{id:"5803",text:"Neutrogena Naturals Acne Spot Treatment"},
{id:"5804",text:"NARS Glitter Pencil"},
{id:"5805",text:"MD Formulations Vita-A-Plus Firming Treatment Masque"},
{id:"5806",text:"Neutrogena Complete Acne Therapy System"},
{id:"5807",text:"Neutrogena Canada Rapid Clear Spot Gel for Acne-Prone Skin"},
{id:"5808",text:"Murad AHA/BHA Exfoliating Cleanser"},
{id:"5809",text:"Olay Total Effects CC Eye Brightening Cream"},
{id:"5810",text:"Murad Time Release Acne Cleanser"},
{id:"5811",text:"N.Y.C. (New York Color) Cheek Glow Single Pan Blush"},
{id:"5812",text:"N.Y.C. (New York Color) Up to 8HR City Proof Gloss"},
{id:"5813",text:"NYX Cosmetics Eyebrow Marker"},
{id:"5814",text:"Nu Skin Clear Action Acne Medication Foaming Cleanser"},
{id:"5815",text:"Neutrogena Deep Clean Long-Last Shine Control Daily Scrub"},
{id:"5816",text:"Merle Norman Super Lube"},
{id:"5817",text:"NARS Loose Powder"},
{id:"5818",text:"NYX Cosmetics Gel Liner & Smudger"},
{id:"5819",text:"Nuance Salma Hayek My Secret Super Moisturizing Body Cream"},
{id:"5820",text:"Neutrogena Pore Refining Moisturizer"},
{id:"5821",text:"MD Formulations The TEMPS: Oil Control Pore Refiner"},
{id:"5822",text:"Neutrogena Wet Skin Kids Stick Sunscreen Broad Spectrum SPF 70"},
{id:"5823",text:"Mederma Mederma + SPF 30 Scar Cream"},
{id:"5824",text:"Murad Clarifying Cleanser"},
{id:"5825",text:"Neutrogena Canada Oil-Free Acne Wash Redness Soothing Facial Cleanser"},
{id:"5826",text:"NARS Gentle Cream Cleanser"},
{id:"5827",text:"Neutrogena Canada Healthy Skin Face Lotion SPF 15"},
{id:"5828",text:"NeoStrata Gel Plus, 15 AHA"},
{id:"5829",text:"Nuance Salma Hayek Anti-Aging Wrinkle Filler Eye Serum"},
{id:"5830",text:"MDSolarSciences Mineral Lotion For Face & Body Broad Spectrum SPF 50 UVA-UVB Sunscreen"},
{id:"5831",text:"Maybelline New York Superstay Concealer 24HR"},
{id:"5832",text:"Nuance Salma Hayek Ageless Clarity Daily Resurfacing Pads"},
{id:"5833",text:"Neutrogena Healthy Skin Enhancer SPF 20"},
{id:"5834",text:"Murad Face Defense SPF 15"},
{id:"5835",text:"NARS Light-Reflecting Loose Setting Powder"},
{id:"5836",text:"N.Y.C. (New York Color) Kiss Gloss"},
{id:"5837",text:"N.Y.C. (New York Color) Big Bold Ultra Volumizing Mascara"},
{id:"5838",text:"NYX Cosmetics HD Studio Photogenic Foundation"},
{id:"5839",text:"Michael Todd True Organics Organic Aloe Nourishing Healing Oil"},
{id:"5840",text:"Michael Todd True Organics Kaolin Clay Detoxifying Facial Mask (Maximum Strength)"},
{id:"5841",text:"MD Formulations The Temps Brighten & Tighten Eye Serum"},
{id:"5842",text:"Obagi Nu-Derm Action"},
{id:"5843",text:"Nu Skin Moisture Restore Intense Moisturizer"},
{id:"5844",text:"Murad Exfoliating Acne Treatment Gel"},
{id:"5845",text:"Michael Todd True Organics Hydration Boost Facial Serum"},
{id:"5846",text:"NARS Radiant Creamy Concealer"},
{id:"5847",text:"Michael Todd True Organics Knu Anti-Aging Serum"},
{id:"5848",text:"Murad Begging for Balance Moisturizer"},
{id:"5849",text:"Nivea In-Shower Body Lotion Hydrating Normal to Dry Skin"},
{id:"5850",text:"Nuance Salma Hayek Exfoliating Body Cleansing Scrub Macadamia Nut"},
{id:"5851",text:"NYX Cosmetics Pore Filler"},
{id:"5852",text:"Neutrogena Canada Moisture Oil-Free FPS 30"},
{id:"5853",text:"Maybelline New York Brow Drama by EyeStudio Sculpting Brow Mascara"},
{id:"5854",text:"NYX Cosmetics Pump it Up Lip Plumper"},
{id:"5855",text:"Neutrogena Deep Clean Oil-Free Makeup Remover Cleansing Wipes"},
{id:"5856",text:"Nude Skincare Radiant Day Moisturizer"},
{id:"5857",text:"Nu Skin Nu Colour Skin Beneficial Tinted Moisturizer SPF 15"},
{id:"5858",text:"Neutrogena Canada Oil-Free Acne Stress Control Power-Clear Scrub"},
{id:"5859",text:"Neutrogena Ultra Sheer Body Mist Sunscreen SPF 70"},
{id:"5860",text:"N.Y.C. (New York Color) Metro Quartet Eye Shadow"},
{id:"5861",text:"Merle Norman SunSense Tinted Self-Tanner"},
{id:"5862",text:"NYX Cosmetics Angel Veil Skin Perfecting Primer"},
{id:"5863",text:"Olay Ultra Moisture Body Wash"},
{id:"5864",text:"Neutrogena Body Clear Body Wash Pink Grapefruit"},
{id:"5865",text:"NeoStrata Antibacterial Facial Cleanser PHA"},
{id:"5866",text:"Murad Intensive Wrinkle Reducer for Eyes"},
{id:"5867",text:"Merle Norman Aqua Lube"},
{id:"5868",text:"Olay Total Effects Lathering Cleansing Cloths"},
{id:"5869",text:"NeoStrata Ultra Smoothing Cream, 10 AHA"},
{id:"5870",text:"NYX Cosmetics Blush"},
{id:"5871",text:"Murad Acne Body Wash"},
{id:"5872",text:"Obagi Nu-Derm Tolereen"},
{id:"5873",text:"Noxzema Clean Blemish Control Daily Scrub"},
{id:"5874",text:"NYX Cosmetics Studio Perfect Photo-Loving Primer"},
{id:"5875",text:"MD Formulations Moisture Defense Antioxidant Hydrating Gel"},
{id:"5876",text:"Neutrogena Neutrogena Naturals Fresh Cleansing + Makeup Remover"},
{id:"5877",text:"NeoStrata Canada Toning Solution Level 2"},
{id:"5878",text:"Nu Skin Enhancer Skin Conditioning Gel"},
{id:"5879",text:"Murad Acne Treatment Concealer"},
{id:"5880",text:"Nude Skincare Replenish Daily Moisturizer"},
{id:"5881",text:"NeoCeuticals by NeoStrata Antibacterial Facial Cleanser PHA"},
{id:"5882",text:"Neutrogena Healthy Skin Eye Cream"},
{id:"5883",text:"NARS Retractable Lip Brush 11"},
{id:"5884",text:"NARS Satin Lipstick"},
{id:"5885",text:"Neutrogena Healthy Skin Brightening Eye Perfector SPF 25"},
{id:"5886",text:"Neutrogena SkinClearing Complexion Perfection Salicylic Acid Acne Treatment"},
{id:"5887",text:"Neutrogena Rapid Dark Circle Repair Eye Cream"},
{id:"5888",text:"Neutrogena Fast Absorbing Hand Cream Norwegian Formula Complete Comfort"},
{id:"5889",text:"Neocutis Hyalis Hydrating Serum"},
{id:"5890",text:"Milani Eye Tech Liquid Eye Liner"},
{id:"5891",text:"Meaningful Beauty Cindy Crawford Crme de Serum"},
{id:"5892",text:"NeoStrata Canada HQplus Lightening Gel"},
{id:"5893",text:"MD Formulations Moisture Defense Antioxidant Treatment Masque"},
{id:"5894",text:"Nivea In-Shower Body Lotion Nourishing Very Dry Skin"},
{id:"5895",text:"Maybelline New York Eye Studio Color Molten Duo"},
{id:"5896",text:"N.Y.C. (New York Color) Ultra Moist Lip Wear"},
{id:"5897",text:"Murad Gentle Acne Treatment Gel"},
{id:"5898",text:"Murad Essential-C Daily Renewal Complex"},
{id:"5899",text:"Neutrogena Age Shield Face Sunscreen Lotion SPF 70"},
{id:"5900",text:"Nude Skincare ProGenius Treatment Oil"},
{id:"5901",text:"Nu Skin Tri-Phasic White Essence"},
{id:"5902",text:"Neutrogena Canada Oil-Free Eye Makeup Remover"},
{id:"5903",text:"OC Eight Adult Acne Treatment Gel 7%"},
{id:"5904",text:"NARS Satin Lip Pencil"},
{id:"5905",text:"N.Y.C. (New York Color) Automatic Eyeliner"},
{id:"5906",text:"NYX Cosmetics Auto Eyebrow Pencil"},
{id:"5907",text:"N.Y.C. (New York Color) High Definition Volume Volumizing Mascara"},
{id:"5908",text:"MD Formulations The Temps Wrinkle Filler & Deep Crease Relaxer"},
{id:"5909",text:"Nu Skin Subtle Effects Blush"},
{id:"5910",text:"NYX Cosmetics Pin-Up Tease Boudior Individual Curl Mascara"},
{id:"5911",text:"NeoStrata Canada Anti-Wrinkle Lip Enhancer"},
{id:"5912",text:"Neutrogena Oil-Free Acne Wash, Redness Soothing Facial Cleanser"},
{id:"5913",text:"Olay Total Effects 7-In-1 Anti-Aging Moisturizer, Mature Skin Therapy"},
{id:"5914",text:"Merle Norman Fresh n Fair Skin Refresher"},
{id:"5915",text:"Murad Water Resistant Broad Spectrum SPF 30 PA+++"},
{id:"5916",text:"Obagi Nu-Derm Exfoderm"},
{id:"5917",text:"Obagi Professional-C Serum 20%"},
{id:"5918",text:"Murad Essential-C Cleanser"},
{id:"5919",text:"Olay Regenerist Regenerating Cream Cleanser"},
{id:"5920",text:"Neutrogena Healthy Skin Blends Sheer Highlighting Blush"},
{id:"5921",text:"NYX Cosmetics Trio EyeShadow"},
{id:"5922",text:"Merle Norman Intensive Moisturizer"},
{id:"5923",text:"NeoStrata Age Reverse Toning Neck Cream"},
{id:"5924",text:"Olay Total Effects 7-in-1 Anti-Aging Cleanser Refreshing Citrus Scrub"},
{id:"5925",text:"Neutrogena Norwegian Formula Hand Cream Scented"},
{id:"5926",text:"Murad Essential-C Sun Balm Broad Spectrum SPF 35 PA +++"},
{id:"5927",text:"Neutrogena Oil-Free Acne Wash, Redness Soothing Cream Cleanser"},
{id:"5928",text:"Neutrogena Rapid Wrinkle Repair Eye Cream"},
{id:"5929",text:"NeoStrata Canada Firming Anti-Wrinkle Cream"},
{id:"5930",text:"NARS Optimal Brightening Concentrate"},
{id:"5931",text:"NeoStrata Acne Treatment Solution Pads"},
{id:"5932",text:"Obagi Nu-Derm Foaming Gel"},
{id:"5933",text:"Murad Acne Spot Treatment"},
{id:"5934",text:"NYX Cosmetics High Definition Blush"},
{id:"5935",text:"Neutrogena Oil-Free Acne Stress Control Triple-Action Toner"},
{id:"5936",text:"Nivea Extended Moisture Lotion"},
{id:"5937",text:"NeoStrata Canada Night Repair Cream"},
{id:"5938",text:"Nu Skin Tru Face Essence Ultra"},
{id:"5939",text:"Obagi Professional-C Serum 15%"},
{id:"5940",text:"Murad Intensive Wrinkle Reducer"},
{id:"5941",text:"NYX Cosmetics Eye & Lip Makeup Remover"},
{id:"5942",text:"Neutrogena Canada Rapid Clear 2-in-1 Fight & Fade Gel"},
{id:"5943",text:"Neutrogena Pore Refining Daily Cleanser"},
{id:"5944",text:"Maybelline New York Volum Express The Falsies Mascara Waterproof Black Drama"},
{id:"5945",text:"Olay Age Defying Anti-Wrinkle Daily SPF 15 Lotion"},
{id:"5946",text:"NeoStrata Canada Daytime Firming Lotion SPF 15"},
{id:"5947",text:"NeoCeuticals by NeoStrata Post Acne Fade Gel"},
{id:"5948",text:"Mederma Mederma Advanced Scar Gel"},
{id:"5949",text:"Noxzema Original Deep Cleansing Cream, Pump"},
{id:"5950",text:"Maybelline New York Vivid Matte Liquid Lipstick by ColorSensational"},
{id:"5951",text:"Meaningful Beauty Cindy Crawford Superfine Exfoliant"},
{id:"5952",text:"NYX Cosmetics Above & Beyond Full Coverage Concealer"},
{id:"5953",text:"NARS Cream Eyeshadow"},
{id:"5954",text:"Maybelline New York SuperStay 24 Color"},
{id:"5955",text:"Neutrogena Canada Oil-Free Fresh Moisture Ultra-Light Moisturizer"},
{id:"5956",text:"NYX Cosmetics Tinted Moisturizer"},
{id:"5957",text:"Miracle Skin Transformer Acne Control Translucent Skin Enhancer"},
{id:"5958",text:"Miracle Skin Transformer Miracle Skin Triple Active Night Treatment"},
{id:"5959",text:"Michael Todd True Organics Organic Pearl and Silk Facial Mask"},
{id:"5960",text:"Nu Skin Nu Colour MoisturShade Liquid Finish SPF 15"},
{id:"5961",text:"Murad Hybrids Skin Perfecting Primer Dewy Finish"},
{id:"5962",text:"Neocutis Perle Skin Brightening Cream with Melaplex"},
{id:"5963",text:"Merle Norman Polished Perfection Facial Scrub"},
{id:"5964",text:"NYX Cosmetics Eyebrow Shaper"},
{id:"5965",text:"Michael Todd True Organics SPF 18 Tinted Lip Protector Trio"},
{id:"5966",text:"Murad Clarifying Mask"},
{id:"5967",text:"Milani Total Lash Cover Mascara with 3 Zone Brush"},
{id:"5968",text:"Noxzema Clean Blemish Control Foaming Wash"},
{id:"5969",text:"MD Formulations My Personal Peel System"},
{id:"5970",text:"Michael Todd True Organics Intensive Organic Cream Eye Treatment"},
{id:"5971",text:"Neutrogena Fresh Cooling Body Mist Sunscreen SPF 70"},
{id:"5972",text:"Murad Correcting Moisturizer SPF 15"},
{id:"5973",text:"Nu Skin Face Lift, Sensitive Formula"},
{id:"5974",text:"Nivea A Kiss of Recovery Medicated Lip Protectant SPF 15"},
{id:"5975",text:"MDSolarSciences Mineral Creme Broad Spectrum SPF 30 UVA-UVB Sunscreen"},
{id:"5976",text:"Neutrogena Canada All-in-One Make-up Removing Cleansing Lotion"},
{id:"5977",text:"Neutrogena Norwegian Formula Lip Moisturizer SPF 15"},
{id:"5978",text:"Neutrogena MoistureShine Gloss"},
{id:"5979",text:"Neutrogena Neutrogena Naturals Multi-Vitamin Nourishing Moisturizer"},
{id:"5980",text:"NYX Cosmetics Invincible Fullest Coverage Foundation"},
{id:"5981",text:"brand product"},
{id:"5982",text:"Ole Henriksen Perfect Truth CC Cream"},
{id:"5983",text:"Pond's Luminous Moisture Day Lotion with SPF 15"},
{id:"5984",text:"Perricone MD Cosmeceuticals Intensive Moisture Therapy"},
{id:"5985",text:"philosophy the microdelivery dual-phase peel"},
{id:"5986",text:"Physicians Formula Mineral Wear Talc-Free Mineral Correcting Powder"},
{id:"5987",text:"Physicians Formula Pore Refining Skin Freshener, for Normal to Dry Skin"},
{id:"5988",text:"Rodan and Fields Reverse Broad Spectrum SPF 50+ Sunscreen"},
{id:"5989",text:"P’_rminerals Disappearing Act 4-in-1 Concealer"},
{id:"5990",text:"Prestige Cosmetics Let Loose Shimmer Shadow Dust"},
{id:"5991",text:"Rimmel Professional Eyebrow Pencil"},
{id:"5992",text:"Peter Thomas Roth Laser-Free Resurfacer"},
{id:"5993",text:"RoC Retinol Correxion Deep Wrinkle Serum"},
{id:"5994",text:"Perricone MD Cosmeceuticals Firming Neck Therapy"},
{id:"5995",text:"Paula's Choice Skincare Sunscreen Spray Broad Spectrum SPF 43"},
{id:"5996",text:"Prestige Cosmetics Primed & Ready Face Primer"},
{id:"5997",text:"Origins Checks and Balances, Frothy Face Wash"},
{id:"5998",text:"Olay Fresh Effects Bright On Schedule Eye Awakening Cream"},
{id:"5999",text:"Origins Conditioning Lip Balm with Turmeric"},
{id:"6000",text:"Physicians Formula Eye Booster 2-in-1 Day & Night Lash Boosting Serum"},
{id:"6001",text:"Origins GinZing Energy-Boosting Moisturizer"},
{id:"6002",text:"Perricone MD Cosmeceuticals Hypoallergenic Firming Eye Cream"},
{id:"6003",text:"Revlon ColorStay Under Eye Concealer SPF 15"},
{id:"6004",text:"Ole Henriksen Invigorating Night Treatment"},
{id:"6005",text:"Peter Thomas Roth Gentle Foaming Cleanser"},
{id:"6006",text:"philosophy help me retinol night treatment"},
{id:"6007",text:"Physicians Formula Mineral Wear Talc-Free Mineral Airbrushing Blush SPF 30"},
{id:"6008",text:"Proactiv & Proactiv+ Medicated Pore Cleaning System"},
{id:"6009",text:"RoC Multi Correxion Nourish Stress Repair Night Cream"},
{id:"6010",text:"Physicians Formula Youthful Wear Cosmeceutical Youth-Boosting Spotless Powder SPF 15"},
{id:"6011",text:"Revlon Age Defying Firming + Lifting Makeup SPF 15"},
{id:"6012",text:"Paula's Choice Skincare Clear Pore Normalizing Cleanser"},
{id:"6013",text:"Proactiv & Proactiv+ Revitalizing Toner"},
{id:"6014",text:"Perricone MD Cosmeceuticals Neuropeptide Facial Cleanser"},
{id:"6015",text:"Origins Make A Difference Plus + Rejuvenating Moisturizer"},
{id:"6016",text:"philosophy purity made simple deep-clean mask"},
{id:"6017",text:"Revlon ColorStay Lipliner"},
{id:"6018",text:"Peter Thomas Roth Irish Moor Mud Purifying Cleansing Gel"},
{id:"6019",text:"Prestige Cosmetics Waterproof Lipliner"},
{id:"6020",text:"Paula's Choice Skincare Resist Triple-Action Dark Spot Eraser 2% BHA Gel"},
{id:"6021",text:"Olay Quench Body Wash"},
{id:"6022",text:"Paula's Choice Skincare Moisture Boost Hydrating Treatment Cream"},
{id:"6023",text:"Peter Thomas Roth Neuroliquid Volufill Youth Eye Serum"},
{id:"6024",text:"Revlon Grow Luscious by Fabulash Mascara & Lash Enhancer Waterproof"},
{id:"6025",text:"Peter Thomas Roth Beta Hydroxy Acid 2% Acne Wash"},
{id:"6026",text:"Revlon Colorstay 16 HR Eye Shadow"},
{id:"6027",text:"Rodan and Fields Unblemish Spot Fading Toner"},
{id:"6028",text:"Origins Zero Oil, Instant Matte Finish for Shiny Places"},
{id:"6029",text:"Physicians Formula Mineral Wear Talc-Free Mineral Cream Concealer SPF 10"},
{id:"6030",text:"Olay Regenerist Micro-Sculpting Cream Fragrance Free"},
{id:"6031",text:"Peter Thomas Roth Neuroliquid Volufill Youth Serum"},
{id:"6032",text:"Ren ClearCalm 3 Replenishing Night Serum"},
{id:"6033",text:"Origins Silk Screen Refining Powder Makeup"},
{id:"6034",text:"Perricone MD Cosmeceuticals No Bronzer Bronzer SPF 30"},
{id:"6035",text:"Ole Henriksen Protect the Truth SPF 50+ Sunscreen"},
{id:"6036",text:"RoC Multi Correxion 5 In 1 Restoring Night Cream"},
{id:"6037",text:"Physicians Formula Fineline Brow Pencil"},
{id:"6038",text:"philosophy purity made simple facial cleansing gel & eye makeup remover"},
{id:"6039",text:"Revlon Photoready Airbrush Effect Makeup SPF 20"},
{id:"6040",text:"philosophy purity made simple, 3-in-1 cleanser for face and eyes"},
{id:"6041",text:"Palmer's Cocoa Butter Formula Body Lotion"},
{id:"6042",text:"Paula's Choice Skincare Resist Perfectly Balanced Foaming Cleanser"},
{id:"6043",text:"Paula's Choice Skincare Resist Super Antioxidant Concentrate Serum, for All Skin Types"},
{id:"6044",text:"Revlon Lash Fantasy Total Definition Primer & Mascara"},
{id:"6045",text:"Paula's Choice Skincare Resist BHA 9 for Stubborn Imperfections"},
{id:"6046",text:"Palladio Beauty Lip Stain Extra Long Wearing Tip Marker"},
{id:"6047",text:"Paula's Choice Skincare Skin Balancing Pore-Reducing Toner"},
{id:"6048",text:"Revlon CustomEyes Mascara"},
{id:"6049",text:"Origins Organics Foaming Face Wash"},
{id:"6050",text:"Paula's Choice Skincare Resist 10% Niacinamide Booster"},
{id:"6051",text:"Revlon Bold Lacquer By Grow Luscious Length & Volume Mascara"},
{id:"6052",text:"Sally Hansen Airbrush Legs (Spray)"},
{id:"6053",text:"Physicians Formula Organic Wear 100% Natural Origin Bronzer"},
{id:"6054",text:"Palladio Beauty Herbal Foundation Oil-Free"},
{id:"6055",text:"P’_rminerals Mineral Quick Clean Waterless Cleanser"},
{id:"6056",text:"Origins A Perfect World, White Tea Skin Guardian"},
{id:"6057",text:"Revlon Ultra HD Lacquer"},
{id:"6058",text:"Revlon Age Defying Wrinkle Remedy Line Filler"},
{id:"6059",text:"Peter Thomas Roth Mega Rich Intensive Anti-Aging Cellular Eye Creme"},
{id:"6060",text:"Rimmel WonderLash Mascara Waterproof"},
{id:"6061",text:"Origins GinZing Brightening Mascara to Lengthen & Lift"},
{id:"6062",text:"Origins GinZing Refreshing Eye Cream"},
{id:"6063",text:"Proactiv & Proactiv+ Emergency Blemish Relief"},
{id:"6064",text:"Prestige Cosmetics Flawless & Ready BB Beauty Balm SPF 15 Broad Spectrum"},
{id:"6065",text:"Paula's Choice Skincare Skincare Applicator for Back & Body"},
{id:"6066",text:"Perricone MD Cosmeceuticals Formula 15"},
{id:"6067",text:"Rimmel Lash Accelerator Endless Mascara with Grow-Lash Complex"},
{id:"6068",text:"Perricone MD Cosmeceuticals Advanced Eye Area Therapy"},
{id:"6069",text:"Prestige Cosmetics Wonder-Full Lip Plumping Gloss"},
{id:"6070",text:"Ole Henriksen Grease Relief Face Tonic"},
{id:"6071",text:"Revlon Colorburst Lipstick"},
{id:"6072",text:"Paula's Choice Skincare Clear Regular Strength Daily Skin Clearing Treatment with 2.5% Benzoyl Peroxide"},
{id:"6073",text:"Paula's Choice Skincare Resist Flawless Finish Pressed Powder"},
{id:"6074",text:"Peter Thomas Roth Anti-Aging Defense Uber-Dry Sunscreen Broad Spectrum SPF 30"},
{id:"6075",text:"Peter Thomas Roth Professional 3% Retinoid Plus"},
{id:"6076",text:"Proactiv & Proactiv+ Deep Cleansing Wash"},
{id:"6077",text:"Origins Blade Runner Energizing Shave Cream"},
{id:"6078",text:"Revlon Colorstay Makeup Combination/Oily Skin"},
{id:"6079",text:"Olay Age Defying Classic Eye Gel"},
{id:"6080",text:"Origins Mega-Mushroom Skin Relief Treatment Lotion"},
{id:"6081",text:"Origins The Great Pretender, Shimmery Self-Tanner for Body"},
{id:"6082",text:"Physicians Formula Organic Wear 100% Natural Origin Mascara"},
{id:"6083",text:"Ole Henriksen Blue/Black Berry Enzyme Mask"},
{id:"6084",text:"Physicians Formula Mineral Wear Talc-Free Airbrushing Pressed Powder Mineral SPF 30"},
{id:"6085",text:"Palmer's Cocoa Butter Formula Moisturizing Body Oil"},
{id:"6086",text:"Peter Thomas Roth Un-Wrinkle Eye"},
{id:"6087",text:"Origins Brighter By Nature SPF 30 Skin Tone Correcting Makeup"},
{id:"6088",text:"Revlon Age Defying CC Cream Color Corrector Broad Spectrum SPF 30"},
{id:"6089",text:"Prestige Cosmetics Fresh Glow Baked Mineral Blush"},
{id:"6090",text:"Revlon Colorstay Whipped Crme Makeup"},
{id:"6091",text:"Paula's Choice Skincare Resist Weightless Advanced Repairing Toner"},
{id:"6092",text:"Olay Regenerist Luminous Tone Perfecting Cream Moisturizer"},
{id:"6093",text:"Physicians Formula Summer Eclipse Bronzing and Shimmery Face Powder"},
{id:"6094",text:"Paula's Choice Skincare Clinical 1% Retinol Treatment"},
{id:"6095",text:"Physicians Formula Bronze Booster Glow-Boosting Season to Season Bronzer"},
{id:"6096",text:"Prestige Cosmetics Liquid Eyeliner"},
{id:"6097",text:"Paula's Choice Skincare Resist Advanced Replenishing Toner"},
{id:"6098",text:"P’_rminerals Mineral Moisture Complex Anti-Aging Powerhouse"},
{id:"6099",text:"Ole Henriksen Pure Perfection"},
{id:"6100",text:"P’_rminerals Mineral Light"},
{id:"6101",text:"Peter Thomas Roth Un-Wrinkle, for All Skin Types"},
{id:"6102",text:"P’_rminerals Happy Feet Restorative Foot Treatment"},
{id:"6103",text:"Peter Thomas Roth 40% Triple Acid Resurfacing Peel"},
{id:"6104",text:"Physicians Formula Extra Rich Rehydrating Moisturizer, for Normal to Dry Skin"},
{id:"6105",text:"Origins Spot Remover Acne Treatment Pads Salicylic Acid"},
{id:"6106",text:"Pond's Original Fresh Wet Cleansing Towelettes"},
{id:"6107",text:"Paula's Choice Skincare PC4Men Soothe & Smooth"},
{id:"6108",text:"Sally Hansen Vitamin E Moisturizing Nail & Cuticle Oil"},
{id:"6109",text:"Proactiv & Proactiv+ Oil Free Moisture with SPF 15"},
{id:"6110",text:"philosophy hope for everywhere caring, covering, continuous concealer"},
{id:"6111",text:"Paula's Choice Skincare Resist Optimal Results Hydrating Cleanser"},
{id:"6112",text:"Paula's Choice Skincare Resist Retinol Skin-Smoothing Body Treatment"},
{id:"6113",text:"Ren No. 1 Purity Cleansing Balm"},
{id:"6114",text:"Origins Never A Dull Moment, Skin-Brightening Face Polisher With Fruit Extracts"},
{id:"6115",text:"Peter Thomas Roth Blemish Buffing Beads"},
{id:"6116",text:"Physicians Formula Eye Makeup Remover Lotion, for Normal to Dry Skin"},
{id:"6117",text:"Pond's Luminous Clean Cream Cleanser"},
{id:"6118",text:"P’_rminerals Be Firm Complete Facial Serum"},
{id:"6119",text:"Physicians Formula Sun Shield Sunless Tanning Lotion SPF 20"},
{id:"6120",text:"Palladio Beauty Revitalizing Night Repair Cream"},
{id:"6121",text:"P’_rminerals Hello Bright Eyes Pencil"},
{id:"6122",text:"Pixi Flawless Beauty Stick SPF 28"},
{id:"6123",text:"Revlon Eterna 27 Moisture Cream with Progenitin"},
{id:"6124",text:"Physicians Formula Eyebrightener Multi-Colored Eyelighter"},
{id:"6125",text:"Physicians Formula Mineral Wear Talc-Free Mineral Tinted Moisturizer SPF 15"},
{id:"6126",text:"Proactiv & Proactiv+ Re-Texturizing Toner"},
{id:"6127",text:"Proactiv & Proactiv+ Mark Fading Pads"},
{id:"6128",text:"RoC Max Resurfacing Facial Cleanser"},
{id:"6129",text:"Rimmel Natural Bronzer"},
{id:"6130",text:"Paula's Choice Skincare Clinical Ultra-Rich Soothing Body Butter"},
{id:"6131",text:"Palmer's Cocoa Butter Formula Fragrance Free Body Lotion"},
{id:"6132",text:"P’_rminerals Tone Up Total Eye Fitness"},
{id:"6133",text:"Paula's Choice Skincare All Over Hair & Body Shampoo, for All Skin & Hair Types"},
{id:"6134",text:"Palladio Beauty Anti-Wrinkle Eye Cream"},
{id:"6135",text:"Paula's Choice Skincare Resist Daily Pore-Refining Treatment 2% BHA"},
{id:"6136",text:"Peter Thomas Roth Instant FIRMx Eye"},
{id:"6137",text:"Proactiv & Proactiv+ Cleansing Body Bar"},
{id:"6138",text:"Pixi Beauty Blush"},
{id:"6139",text:"Olay Sensitive Body Wash Unscented"},
{id:"6140",text:"Physicians Formula Shimmer Strips Custom Eye Enhancing Mascara Duo"},
{id:"6141",text:"Prestige Cosmetics Classic Lipliner"},
{id:"6142",text:"Revlon PhotoReady Concealer SPF 20"},
{id:"6143",text:"Pixi Flawless Beauty Fluid"},
{id:"6144",text:"Sally Hansen Airbrush Legs (Lotion)"},
{id:"6145",text:"Origins Zero Oil Deep Pore Cleanser"},
{id:"6146",text:"Proactiv & Proactiv+ Daily Protection Plus Sunscreen SPF 30"},
{id:"6147",text:"Olay Age Defying Moisturizing Bar"},
{id:"6148",text:"Peter Thomas Roth Viz-1000"},
{id:"6149",text:"philosophy the oxygen peel kit"},
{id:"6150",text:"Rimmel ScandalEyes Stick"},
{id:"6151",text:"Origins A Perfect World For Eyes, Firming Moisture Treatment with White Tea"},
{id:"6152",text:"Paula's Choice Skincare Hydralight Healthy Skin Refreshing Toner"},
{id:"6153",text:"philosophy hope in a jar, original formula"},
{id:"6154",text:"Ole Henriksen Lemon Strip Flash Peel"},
{id:"6155",text:"Physicians Formula Concealer 101 Perfecting Concealer Duo"},
{id:"6156",text:"Origins Liquid Lip Color"},
{id:"6157",text:"Physicians Formula Magic Mosaic Multi-Colored Custom Blush"},
{id:"6158",text:"Physicians Formula Organic Wear CC Curl + Care Mascara"},
{id:"6159",text:"Peter Thomas Roth Anti-Aging Buffing Beads"},
{id:"6160",text:"Sally Hansen Cuticle Massage Cream"},
{id:"6161",text:"Olay Regenerist Intensive Repair Treatment"},
{id:"6162",text:"Rodan and Fields Reverse Skin Lightening Toner"},
{id:"6163",text:"Physicians Formula Hormonal Aging Radiance Restoring Skin Brightener"},
{id:"6164",text:"Peter Thomas Roth Laser-Free Retexturizer Exfoliating Scrub"},
{id:"6165",text:"Physicians Formula Cashmere Wear Ultra-Smoothing Bronzer"},
{id:"6166",text:"Rodan and Fields Unblemish Acne Treatment Sulfur Wash"},
{id:"6167",text:"Physicians Formula Luxury Eye Cream, for Normal to Dry Skin"},
{id:"6168",text:"Paula's Choice Skincare Earth Sourced Antioxidant-Enriched Natural Moisturizer"},
{id:"6169",text:"Physicians Formula Sexy Booster Va Va Voom Volume Mascara"},
{id:"6170",text:"philosophy the microdelivery exfoliating body wash"},
{id:"6171",text:"Prestige Cosmetics BrowPerfection Total Shaping Studio"},
{id:"6172",text:"Physicians Formula Argan Wear Ultra-Nourishing Argan Oil BB Cream SPF 30"},
{id:"6173",text:"Rimmel Fix & Perfect Pro Primer"},
{id:"6174",text:"Purpose Redness Reducing Moisturizer with SPF 30"},
{id:"6175",text:"Paula's Choice Skincare Clear Extra Strength Daily Skin Clearing Treatment with 5% Benzoyl Peroxide"},
{id:"6176",text:"philosophy the microdelivery exfoliating wash"},
{id:"6177",text:"P’_rminerals Mineral Wash for Face, Hair, Body and Brushes"},
{id:"6178",text:"philosophy the microdelivery purifying peel, one-minute purifying enzyme peel"},
{id:"6179",text:"Physicians Formula Super BB All-in-1 Beauty Balm Cream SPF 30"},
{id:"6180",text:"Rodan and Fields Enhancements Micro-Dermabrasion Paste"},
{id:"6181",text:"Physicians Formula Organic Wear 100% Natural Origin Jumbo Lash Mascara"},
{id:"6182",text:"Prestige Cosmetics Total Intensity Eyeliner"},
{id:"6183",text:"Prestige Cosmetics SunFlower Illuminating Bronzing Powder"},
{id:"6184",text:"Origins No Puffery, Cooling Mask for Puffy Eyes"},
{id:"6185",text:"Pixi Sheer Cheek Gel"},
{id:"6186",text:"Peter Thomas Roth Power K Eye Rescue"},
{id:"6187",text:"Revlon ColorStay Skinny Liquid Liner"},
{id:"6188",text:"Pixi Eye Bright Kit"},
{id:"6189",text:"Peter Thomas Roth CC Cream Broad Spectrum SPF 30 Complexion Corrector"},
{id:"6190",text:"Peter Thomas Roth Ultra-Lite SPF 30 Oil-Free Sunscreen"},
{id:"6191",text:"P’_rminerals Pur Body Butter"},
{id:"6192",text:"Paula's Choice Skincare Blush It On Contour Palette"},
{id:"6193",text:"Paula's Choice Skincare Smooth Finish Conditioner, for All Hair Types"},
{id:"6194",text:"Physicians Formula Concealer Twins Cream Concealer 2-in-1 Correct and Cover SPF 10"},
{id:"6195",text:"P’_rminerals CC Cream Broad Spectrum SPF 40"},
{id:"6196",text:"Revlon Lash Fantasy Total Definition Waterproof Primer & Mascara"},
{id:"6197",text:"Physicians Formula Glow Boosting BB Bronzer SPF 20"},
{id:"6198",text:"Ole Henriksen Roll-On Acne Clearing Solution"},
{id:"6199",text:"Rimmel ScandalEyes Eye Shadow Paint"},
{id:"6200",text:"Revlon Lash Potion Mascara"},
{id:"6201",text:"Pixi Flawless Beauty Primer"},
{id:"6202",text:"Palladio Beauty Pore-Refining Facial Scrub"},
{id:"6203",text:"P’_rminerals Moisture Infusion Oil-Free Moisture Blast"},
{id:"6204",text:"P’_rminerals Pressed Mineral Blush"},
{id:"6205",text:"Physicians Formula Conceal Rx Physicians Strength Concealer"},
{id:"6206",text:"Physicians Formula Argan Wear Ultra-Nourishing Argan Oil"},
{id:"6207",text:"Paula's Choice Skincare Resist Moisture Renewal Oil Booster"},
{id:"6208",text:"Revlon ColorStay Ultimate Liquid Lipstick"},
{id:"6209",text:"Olay Pro-X Even Skin Tone Spot Fading Treatment"},
{id:"6210",text:"Rodan and Fields Anti-Age AMP MD System"},
{id:"6211",text:"Proactiv & Proactiv+ Skin Revitalizing Pads"},
{id:"6212",text:"Revlon PhotoReady Powder SPF 14"},
{id:"6213",text:"Origins Automagically Eye Lining Pencil"},
{id:"6214",text:"Paula's Choice Skincare Calm Redness Relief Toner Normal to Oily"},
{id:"6215",text:"Palmer's Cocoa Butter Formula Anti-Aging Smoothing Lotion with Alpha/Beta Hydroxy"},
{id:"6216",text:"Revlon Moon Drops Lip Conditioner SPF 6"},
{id:"6217",text:"Physicians Formula Plump Potion Lash Plumping and Stimulating Mascara"},
{id:"6218",text:"Peter Thomas Roth Max Anti-Shine Mattifying Gel"},
{id:"6219",text:"Revlon ColorStay Brow Enhancer"},
{id:"6220",text:"Palmer's Cocoa Butter Formula Solid Formula"},
{id:"6221",text:"philosophy when hope is not enough omega 3-6-9 replenishing oil"},
{id:"6222",text:"Revlon ColorStay Mineral Lipglaze"},
{id:"6223",text:"Paula's Choice Skincare Calm Redness Relief Moisturizer Normal to Oily"},
{id:"6224",text:"Perricone MD Cosmeceuticals Neuropeptide Firming Moisturizer"},
{id:"6225",text:"RoC Retinol Correxion MAX Wrinkle Resurfacing System"},
{id:"6226",text:"Paula's Choice Skincare Brow-Defining Cream Duo"},
{id:"6227",text:"Origins Plantscription Youth Renewing Night Cream"},
{id:"6228",text:"Physicians Formula Powder Finish Concealer Stick SPF 15"},
{id:"6229",text:"philosophy when hope is not enough facial firming and lifting serum"},
{id:"6230",text:"Physicians Formula Mineral Wear Airbrushing Loose Powder SPF 30"},
{id:"6231",text:"Paula's Choice Skincare Sun 365 Self-Tanning Foam"},
{id:"6232",text:"Origins Zero Oil Oil-Free Moisture Lotion with Saw Palmetto and Mint"},
{id:"6233",text:"Olay Total Effects 7 in 1 Anti-Aging Moisturizer with Sunscreen SPF 30"},
{id:"6234",text:"Physicians Formula Shimmer Strips Gel Cream Liner"},
{id:"6235",text:"Pixi Lip and Line"},
{id:"6236",text:"Physicians Formula Youthful Wear Cosmeceutical Youth-Boosting Illuminating Face Powder"},
{id:"6237",text:"Peter Thomas Roth Camu Camu Power C x 30 Vitamin C Brightening Moisturizer"},
{id:"6238",text:"philosophy full of promise restoring eye duo"},
{id:"6239",text:"Revlon ColorStay Aqua Mineral Finishing Powder"},
{id:"6240",text:"Perricone MD Cosmeceuticals Hypoallergenic Nourishing Moisturizer"},
{id:"6241",text:"Perricone MD Cosmeceuticals No Blush Blush SPF 30"},
{id:"6242",text:"Olay Olay Regenerist Luminous Brightening Foaming Cleanser"},
{id:"6243",text:"Olay Total Effects Nourishing Cream Cleanser"},
{id:"6244",text:"Purpose Dual Treatment Moisture Lotion with SPF 15"},
{id:"6245",text:"Replenix Replenix AE Dermal Restructuring"},
{id:"6246",text:"Paula's Choice Skincare Resist Cellular Defense Daily Moisturizer with SPF 25 & Antioxidants"},
{id:"6247",text:"Olay Age Defying 2-in-1 Anti-Wrinkle Day Cream + Serum"},
{id:"6248",text:"Perricone MD Cosmeceuticals Chia Serum"},
{id:"6249",text:"philosophy no reason to hide multi-imperfection transforming serum"},
{id:"6250",text:"Paula's Choice Skincare Moisture Boost One Step Face Cleanser"},
{id:"6251",text:"Paula's Choice Skincare Cuticle & Nail Treatment"},
{id:"6252",text:"Paula's Choice Skincare Resist Anti-Aging Clear Skin Hydrator"},
{id:"6253",text:"Rodan and Fields Redefine Night Renewing Serum"},
{id:"6254",text:"Physicians Formula Powder Palette Mineral Glow Pearls"},
{id:"6255",text:"Origins Smarty Plants CC SPF 20"},
{id:"6256",text:"Paula's Choice Skincare Resist Skin Resurfacing & Smoothing System"},
{id:"6257",text:"philosophy clear days ahead fast-acting salicylic acid acne spot treatment"},
{id:"6258",text:"Origins Plantscription Anti-Aging Serum"},
{id:"6259",text:"Sally Hansen Moisturizing Foot Creme"},
{id:"6260",text:"Prestige Cosmetics Waterproof Eyeliner"},
{id:"6261",text:"Revlon PhotoReady BB Cream Skin Perfector SPF 30"},
{id:"6262",text:"Physicians Formula Sexy Booster Sexy Glow Glossy Stain"},
{id:"6263",text:"Prestige Cosmetics Sun Play Sun Baked Mineral Bronzing Powder"},
{id:"6264",text:"Paula's Choice Skincare Moisture Boost Hydrating Toner"},
{id:"6265",text:"Paula's Choice Skincare Resist Youth-Extending Daily Mattifying Fluid Broad Spectrum SPF 50"},
{id:"6266",text:"Prestige Cosmetics Brow Perfection Ideal Match Marbleized Brow Pencil"},
{id:"6267",text:"Physicians Formula Intensive Wrinkle Corrector Eye Cream, Formula RX 122"},
{id:"6268",text:"Peter Thomas Roth FIRMx Growth Factor Extreme Neuropeptide Serum"},
{id:"6269",text:"Paula's Choice Skincare Resist Weekly Resurfacing Treatment with 10% Alpha Hydroxy Acid"},
{id:"6270",text:"Peter Thomas Roth De-Spot Plus"},
{id:"6271",text:"Peter Thomas Roth Gentle Complexion Correction Pads"},
{id:"6272",text:"Peter Thomas Roth Max All Night Repair"},
{id:"6273",text:"Pixi Fairy Light Solos"},
{id:"6274",text:"Origins A Perfect World SPF 15 Age-Defense Tinted Moisturizer with White Tea BB"},
{id:"6275",text:"Physicians Formula Nourishing Night Cream, for Dry to Very Dry Skin"},
{id:"6276",text:"Pond's Clarant B3 Anti-Dark Spots Moisturizer, Normal to Dry"},
{id:"6277",text:"Physicians Formula Redness Correcting Moisturizer, Formula RX 312"},
{id:"6278",text:"Ole Henriksen On the Go Cleanser"},
{id:"6279",text:"Prestige Cosmetics Lipstick"},
{id:"6280",text:"Physicians Formula Mineral Wear Talc-Free Mineral Loose Powder"},
{id:"6281",text:"Peter Thomas Roth Max Complexion Correction Pads"},
{id:"6282",text:"Paula's Choice Skincare Earth Sourced Purely Natural Refreshing Toner"},
{id:"6283",text:"philosophy full of promise tightening and firming neck cream"},
{id:"6284",text:"Paula's Choice Skincare Skin Balancing Ultra-Sheer Daily Defense Broad Spectrum SPF 30"},
{id:"6285",text:"Physicians Formula Plump Potion Needle-Free Lip Plumping Cocktail"},
{id:"6286",text:"Revlon PhotoReady Eye Primer + Brightener"},
{id:"6287",text:"Olay Total Effects Night Firming Cream"},
{id:"6288",text:"Origins Stay Tuned Balancing Face Makeup"},
{id:"6289",text:"Ren Hydra-Calm Global Protection Day Cream"},
{id:"6290",text:"Paula's Choice Skincare Resist Pure Radiance Skin Brightening Treatment"},
{id:"6291",text:"RoC Multi Correxion Lift Anti-Gravity Day Moisturizer SPF 30"},
{id:"6292",text:"Rodan and Fields Soothe Sensitive Skin Treatment"},
{id:"6293",text:"Ole Henriksen Total Truth Eye Creme SPF 15"},
{id:"6294",text:"Rodan and Fields Soothe Gentle Cream Wash"},
{id:"6295",text:"Physicians Formula Super CC+ Color-Correction + Care CC+ Cream SPF 30"},
{id:"6296",text:"Proactiv & Proactiv+ Makeup Removing Cloths"},
{id:"6297",text:"Paula's Choice Skincare PC4Men Shave"},
{id:"6298",text:"P’_rminerals Pout Plumping Lip Gloss"},
{id:"6299",text:"Origins Make A Difference Plus + Ultra-Rich Rejuvenating Cream"},
{id:"6300",text:"Paula's Choice Skincare Earth Sourced Perfectly Natural Cleansing Gel"},
{id:"6301",text:"Physicians Formula Baked Bronzer Bronzing & Shimmery Face Powder"},
{id:"6302",text:"Olay Quench Daily Lotion Shimmer"},
{id:"6303",text:"Physicians Formula Cashmere Wear Ultra-Smoothing Blush 100% Pure Cashmere Extract"},
{id:"6304",text:"Revlon Moon Drops Lipstick"},
{id:"6305",text:"philosophy hope in a jar oil-free moisturizer for normal to oily skin"},
{id:"6306",text:"philosophy purity made simple mineral oil-free facial cleansing oil"},
{id:"6307",text:"Origins Mega-Mushroom Skin Relief Face Cleanser"},
{id:"6308",text:"Pond's Bio-Hydrante Light Hydrating Cream"},
{id:"6309",text:"Sally Hansen Dry Chapped Hand Creme"},
{id:"6310",text:"Paula's Choice Skincare Hydralight Moisture-Infusing Lotion"},
{id:"6311",text:"Olay Regenerist Luminous Facial Oil Daily Treatment"},
{id:"6312",text:"Revlon Colorburst Lip Butters"},
{id:"6313",text:"Rodan and Fields Essentials Lip Shield SPF 25"},
{id:"6314",text:"Paula's Choice Skincare Best Face Forward Foundation SPF 25"},
{id:"6315",text:"Sally Hansen Callus & Foot Softener"},
{id:"6316",text:"Palladio Beauty Herbal Tinted Lip Balm"},
{id:"6317",text:"Physicians Formula Hormonal Aging Anti-Sagging & Restoring Serum"},
{id:"6318",text:"philosophy hope in a jar SPF 25 daily moisturizer for all skin types"},
{id:"6319",text:"Proactiv & Proactiv+ Clear Zone Body Pads"},
{id:"6320",text:"Peter Thomas Roth Cucumber Gel Masque"},
{id:"6321",text:"Ole Henriksen Truth Serum Collagen Booster"},
{id:"6322",text:"Paula's Choice Skincare PC4Men Face Wash"},
{id:"6323",text:"Olay Regenerist Eye Lifting Serum"},
{id:"6324",text:"Paula's Choice Skincare Resist 1% Retinol Booster"},
{id:"6325",text:"Revlon Super Lustrous Lipstick"},
{id:"6326",text:"Physicians Formula Gentle Cover Concealer Stick"},
{id:"6327",text:"Revlon Super Lustrous Lipgloss"},
{id:"6328",text:"philosophy hope in a tube"},
{id:"6329",text:"Rimmel 24HR Super Curler Mascara"},
{id:"6330",text:"Peter Thomas Roth Sulfur Cooling Masque"},
{id:"6331",text:"Rodan and Fields SOOTHE Mineral Sunscreen SPF 30"},
{id:"6332",text:"Peter Thomas Roth Pore Putty"},
{id:"6333",text:"Ole Henriksen African Red Tea Exfoliating Lip Salvation"},
{id:"6334",text:"Replenix Sheer Physical Sunscreen Cream SPF 50"},
{id:"6335",text:"Physicians Formula Mineral Wear Talc-Free Mineral Liquid Foundation"},
{id:"6336",text:"Sally Hansen Colorfast Tint + Moisture Balm"},
{id:"6337",text:"philosophy eye hope advanced anti-aging eye cream"},
{id:"6338",text:"Rimmel Extra Wow Lash Mascara"},
{id:"6339",text:"Ole Henriksen Firm Action Mask"},
{id:"6340",text:"Proactiv & Proactiv+ Mild Exfoliating Peel"},
{id:"6341",text:"Revlon PhotoReady Airbrush Mousse Makeup"},
{id:"6342",text:"Palladio Beauty Eye Ink Felt Tip Liquid Eye Liner"},
{id:"6343",text:"Origins Underwear for Lashes"},
{id:"6344",text:"Revlon PhotoReady Sculpting Blush Palette"},
{id:"6345",text:"Revlon Colorstay Overtime Lengthening Mascara"},
{id:"6346",text:"Paula's Choice Skincare Clear Extra Strength Anti-Redness Exfoliating Solution with 2% Salicylic Acid"},
{id:"6347",text:"philosophy just release me dual-phase, extremely gentle, oil-free eye makeup remover"},
{id:"6348",text:"Peter Thomas Roth Lashes to Die For The Mascara"},
{id:"6349",text:"Olay Regenerist Micro-Sculpting Serum"},
{id:"6350",text:"Revlon PhotoReady Insta-Fix Makeup SPF 20"},
{id:"6351",text:"Pixi Line Relax Serum"},
{id:"6352",text:"Perricone MD Cosmeceuticals Cold Plasma"},
{id:"6353",text:"Ren T-Zone Balancing Day Fluid"},
{id:"6354",text:"Physicians Formula Shimmer Strips Custom Eye Enhancing Eyeliner Trio"},
{id:"6355",text:"Paula's Choice Skincare Gentle Touch Makeup Remover"},
{id:"6356",text:"Olay Fresh Effects Clear Skin Acne Hater Deep Scrub"},
{id:"6357",text:"Olay Age Defying Classic Daily Renewal Cream"},
{id:"6358",text:"philosophy Hands Of Hope Hand and Cuticle Cream"},
{id:"6359",text:"Palmer's Cocoa Butter Formula Skin Therapy Oil"},
{id:"6360",text:"Peter Thomas Roth Cucumber De-Tox De-Puffing Eye Cubes"},
{id:"6361",text:"Palladio Beauty Baked Blush"},
{id:"6362",text:"Paula's Choice Skincare Resist Anti-Aging Eye Cream"},
{id:"6363",text:"Paula's Choice Skincare Skin Recovery Enriched Calming Toner"},
{id:"6364",text:"Rimmel Match Perfection Foundation SPF 15"},
{id:"6365",text:"Physicians Formula Powder Palette Multi-Colored Blush"},
{id:"6366",text:"Prestige Cosmetics Skin Loving Minerals Eyeliner"},
{id:"6367",text:"Physicians Formula Dark Spot Corrector & Skin Brightener, Formula RX 123"},
{id:"6368",text:"Rimmel Match Perfection Skin Tone Adapting 2-in-1 Concealer & Highlighter"},
{id:"6369",text:"Proactiv & Proactiv+ Redness Relief Serum"},
{id:"6370",text:"Origins United State Balancing Face Tonic"},
{id:"6371",text:"Origins Plantscription Anti-Aging Concealer"},
{id:"6372",text:"Perricone MD Cosmeceuticals No Concealer Concealer SPF 35"},
{id:"6373",text:"Paula's Choice Skincare Resist Hyaluronic Acid Booster"},
{id:"6374",text:"Pixi Tinted Brilliance Balm"},
{id:"6375",text:"Revlon ColorStay Moisture Stain"},
{id:"6376",text:"Ole Henriksen Invigorating Night Creme"},
{id:"6377",text:"Revlon Brow Fantasy Pencil & Gel"},
{id:"6378",text:"Perricone MD Cosmeceuticals Concentrated Restorative Treatment"},
{id:"6379",text:"philosophy deep fill fix instant line filler and long-term wrinkle smoother"},
{id:"6380",text:"Revlon ColorBurst Lacquer Balm"},
{id:"6381",text:"Rimmel Just Let It GoGentle Eye Makeup Remover Oil Free"},
{id:"6382",text:"Physicians Formula Mineral Wear Oh So Radiant Powder SPF 20"},
{id:"6383",text:"Pixi Eye Bright Liner"},
{id:"6384",text:"Ren Omega 3 Night Repair Serum"},
{id:"6385",text:"Pond's Dry Skin Cream, The Caring Classic"},
{id:"6386",text:"Sally Hansen Smooth & Perfect Hand Creme"},
{id:"6387",text:"Olay Quench Daily Lotion"},
{id:"6388",text:"P’_rminerals Mineral Melt Makeup Remover"},
{id:"6389",text:"Physicians Formula Blemish Rx Blemish-Healing Concealer"},
{id:"6390",text:"Rimmel Stay Matte Liquid Mousse Foundation"},
{id:"6391",text:"Paula's Choice Skincare Calm Redness Relief Cleanser Normal to Dry"},
{id:"6392",text:"Ole Henriksen See the Difference Serum"},
{id:"6393",text:"Revlon PhotoReady Perfecting Primer"},
{id:"6394",text:"Revlon Matte Powder Blush"},
{id:"6395",text:"Perricone MD Cosmeceuticals Hypoallergenic Peptide Complex"},
{id:"6396",text:"Prestige Cosmetics Sun Play Bronzing Fusion"},
{id:"6397",text:"Pixi Eye Zone Brightener"},
{id:"6398",text:"Rimmel Moisture Renew Lipstick"},
{id:"6399",text:"P’_rminerals Day Complex with SPF 20"},
{id:"6400",text:"Peter Thomas Roth Acne Spot And Area Treatment"},
{id:"6401",text:"Physicians Formula Redness Relief Cleanser, Formula RX 301"},
{id:"6402",text:"Palladio Beauty Herbal Eyeshadow Quad"},
{id:"6403",text:"Rodan and Fields Redefine Triple Defense Treatment"},
{id:"6404",text:"P’_rminerals See No More Pore Minimizer"},
{id:"6405",text:"Perricone MD Cosmeceuticals Serum Prep"},
{id:"6406",text:"Physicians Formula Eye Definer Automatic Eye Pencil"},
{id:"6407",text:"Pond's Cold Cream Cleanser"},
{id:"6408",text:"Peter Thomas Roth Mega Rich Intensive Anti-Aging Cellular Creme"},
{id:"6409",text:"Pixi Flawless and Poreless"},
{id:"6410",text:"Sally Hansen Lip Inflation Extreme"},
{id:"6411",text:"P’_rminerals Pur Polish Body Scrub Exfoliator"},
{id:"6412",text:"Revlon ColorStay Liquid Eye Pen"},
{id:"6413",text:"Palladio Beauty Rice Paper"},
{id:"6414",text:"Origins Automagically Lip Lining Pencil"},
{id:"6415",text:"Sephora Matte Blotting Papers"},
{id:"6416",text:"Peter Thomas Roth Cucumber De-Tox Hydrating Serum"},
{id:"6417",text:"Physicians Formula Eye Booster 2-in-1 Lash Boosting Eyeliner + Serum"},
{id:"6418",text:"Olay Regenerist Regenerating Serum"},
{id:"6419",text:"philosophy ultimate miracle worker eye SPF 15"},
{id:"6420",text:"Rodan and Fields SOOTHE Moisture Replenishing Cream"},
{id:"6421",text:"P’_rminerals Mineral Shea Butter Lipstick"},
{id:"6422",text:"P’_rminerals Universal Marble Mineral Powder, Pink"},
{id:"6423",text:"Pond's Crema S Nourishing Moisturizing Cream"},
{id:"6424",text:"RoC Multi Correxion 5 in 1 Daily Moisturizer with Sunscreen Broad Spectrum SPF 30"},
{id:"6425",text:"Origins Clear Improvement, Active Charcoal Mask to Clear Pores"},
{id:"6426",text:"Pond's Cucumber Cleanser"},
{id:"6427",text:"Rimmel Scandaleyes Lycra Flex Mascara"},
{id:"6428",text:"Revlon ColorStay Creme Gel Eyeliner"},
{id:"6429",text:"P’_rminerals Mineral Mascara"},
{id:"6430",text:"Pixi Illuminating Tint & Conceal SPF 20"},
{id:"6431",text:"Olay Age Defying Classic Night Cream"},
{id:"6432",text:"Rimmel Hide the Blemish Concealer"},
{id:"6433",text:"Peter Thomas Roth Un-Wrinkle Night"},
{id:"6434",text:"Revlon Grow Luscious Plumping Waterproof Mascara"},
{id:"6435",text:"Ole Henriksen Express the Truth"},
{id:"6436",text:"Prestige Cosmetics My Biggest Lashes Big Lash Effect Mascara"},
{id:"6437",text:"Paula's Choice Skincare Resist Barrier Repair Moisturizer with Retinol"},
{id:"6438",text:"Revlon PhotoReady Color Correcting Primer"},
{id:"6439",text:"philosophy clear days ahead oil-free salicylic acid acne treatment & moisturizer"},
{id:"6440",text:"Perricone MD Cosmeceuticals No Lipstick Lipstick SPF 15"},
{id:"6441",text:"Rimmel Lash Accelerator"},
{id:"6442",text:"Peter Thomas Roth Glycolic Acid 3% Facial Wash"},
{id:"6443",text:"philosophy the microdelivery mini peel pads"},
{id:"6444",text:"Ole Henriksen Pick Me Up Face Tonic"},
{id:"6445",text:"philosophy lip shine"},
{id:"6446",text:"Ole Henriksen African Red Tea Foaming Cleanser"},
{id:"6447",text:"Origins Starting Over Age-Erasing Moisturizer with Mimosa"},
{id:"6448",text:"Olay Complete All Day Moisture Cream with Sunscreen Broad Spectrum SPF 15, Sensitive"},
{id:"6449",text:"Peter Thomas Roth Brightening Bubbling Mask"},
{id:"6450",text:"Olay Regenerist Instant Fix Wrinkle Revolution Complex"},
{id:"6451",text:"Proactiv & Proactiv+ Daily Oil Control"},
{id:"6452",text:"Pixi Lid Last Shadow Pen"},
{id:"6453",text:"Paula's Choice Skincare Resist Weightless Body Treatment with 2% Beta Hydroxy Acid"},
{id:"6454",text:"Origins Plantscription Anti-Aging Power Serum"},
{id:"6455",text:"Olay Pro-X Age Repair Lotion with SPF 30"},
{id:"6456",text:"philosophy renewed hope in a jar"},
{id:"6457",text:"philosophy clear days ahead oil-free salicylic acid acne treatment cleanser"},
{id:"6458",text:"Physicians Formula Magic Mosaic Multi-Colored Custom Pressed Powder"},
{id:"6459",text:"Olay Total Effects Wet Cleansing Cloths"},
{id:"6460",text:"Physicians Formula pH Matchmaker Powdered Bronzer"},
{id:"6461",text:"Rimmel BB Cream 9-in-1 Skin Perfecting Super Makeup Broad Spectrum SPF 25"},
{id:"6462",text:"Physicians Formula Nude Wear Touch of Glow Foundation"},
{id:"6463",text:"Rodan and Fields Essentials Broad Spectrum SPF 30 Body Sunscreen"},
{id:"6464",text:"Physicians Formula Circle Rx Circle Control Concealer"},
{id:"6465",text:"Ole Henriksen Balancing Cucumber Face Tonic"},
{id:"6466",text:"Olay Complete All Day Moisture Cream with Sunscreen Broad Spectrum SPF 15, Normal"},
{id:"6467",text:"Revlon Illuminance Creme Eyeshadow"},
{id:"6468",text:"Origins Pure Cream, Rinseable Cleanser You Can Also Tissue Off"},
{id:"6469",text:"Paula's Choice Skincare Skin Balancing Super Antioxidant Concentrate Serum with Retinol"},
{id:"6470",text:"Rimmel GlamEyes Mono Eye Shadow"},
{id:"6471",text:"Peter Thomas Roth Blue Marine Algae Intense Hydrating Mask"},
{id:"6472",text:"Perricone MD Cosmeceuticals Chloro Plasma"},
{id:"6473",text:"P’_rminerals Triple Impact Mascara"},
{id:"6474",text:"Origins All and Nothing Sheer Finishing Powder"},
{id:"6475",text:"Rodin Olio Lusso Luxury Body Oil"},
{id:"6476",text:"Origins Zero Oil Pore Refining Toner with Saw Palmetto and Mint"},
{id:"6477",text:"Physicians Formula Powder Palette Multi-Colored Face Powder"},
{id:"6478",text:"Peter Thomas Roth Un-Wrinkle Lip"},
{id:"6479",text:"philosophy renewed hope in a jar re-energizing moisture mask"},
{id:"6480",text:"Origins Starting Over Age-Erasing Oil-Free Moisturizer with Mimosa"},
{id:"6481",text:"Origins Eye Doctor, Moisture Care for Skin Around Eyes"},
{id:"6482",text:"Rimmel Gentle Eye Makeup Remover"},
{id:"6483",text:"Paula's Choice Skincare Resist Skin Firming Line Minimizer"},
{id:"6484",text:"Proactiv & Proactiv+ Blackhead Dissolving Gel"},
{id:"6485",text:"Rodan and Fields Essentials Foaming Sunless Tan"},
{id:"6486",text:"Sally Hansen Vita B Lip Moisturizer"},
{id:"6487",text:"Proactiv & Proactiv+ Medicated Cleansing Bar"},
{id:"6488",text:"Pond's Luminous Clean Wet Cleansing Towelettes"},
{id:"6489",text:"Paula's Choice Skincare Calm Redness Relief Moisturizer Normal to Dry"},
{id:"6490",text:"Physicians Formula Talc-Free Mineral Wear Correcting Pebbles"},
{id:"6491",text:"Paula's Choice Skincare Lipscreen SPF 50"},
{id:"6492",text:"philosophy full of promise treatment duo"},
{id:"6493",text:"Rimmel The Max Volume Flash Waterproof Mascara"},
{id:"6494",text:"Paula's Choice Skincare Sun 365 Self-Tanning Concentrate"},
{id:"6495",text:"Peter Thomas Roth Un-Wrinkle Turbo Face Serum"},
{id:"6496",text:"Peter Thomas Roth Oilless Oil 100% Purified Squalane"},
{id:"6497",text:"Rimmel Exaggerate Smoke N' Shine Automatic Gel Eye Liner"},
{id:"6498",text:"Paula's Choice Skincare Hydralight Shine-Free Mineral Complex SPF 30"},
{id:"6499",text:"philosophy miracle worker anti-aging for blemish prone skin"},
{id:"6500",text:"philosophy hope oil-free moisturizer SPF 30"},
{id:"6501",text:"Revlon Age Defying Targeted Dark Spot Concealer Treatment"},
{id:"6502",text:"Revlon ColorStay Concealer"},
{id:"6503",text:"Physicians Formula Ultra-Hydrating Day & Night Cream Formula Rx221"},
{id:"6504",text:"philosophy turbo booster c powder"},
{id:"6505",text:"Ole Henriksen Sheer Transformation"},
{id:"6506",text:"Perricone MD Cosmeceuticals Hyalo Plasma"},
{id:"6507",text:"P’_rminerals Pur-ly Abundant Lash & Brow Duo Wand"},
{id:"6508",text:"Perricone MD Cosmeceuticals Cold Plasma Eye"},
{id:"6509",text:"Olay Total Effects 7-in-1 Night Pore Perfector Moisturizer"},
{id:"6510",text:"Ren Vita-Mineral Day Cream"},
{id:"6511",text:"Purpose Gentle Cleansing Wash"},
{id:"6512",text:"Rimmel Exaggerate Automatic Lip Liner"},
{id:"6513",text:"Perricone MD Cosmeceuticals Neuropeptide Facial Conformer"},
{id:"6514",text:"Ole Henriksen Ultimate Lift Eye Gel PM"},
{id:"6515",text:"Physicians Formula Shimmer Strips Custom Eye Enhancing Shadow & Liner"},
{id:"6516",text:"Peter Thomas Roth Un-Wrinkle Creme Cleanser"},
{id:"6517",text:"RoC Retinol Correxion Deep Wrinkle Daily Moisturizer SPF 30"},
{id:"6518",text:"Palladio Beauty Baked Eyeshadow Trio"},
{id:"6519",text:"Perricone MD Cosmeceuticals Cold Plasma Sub-D"},
{id:"6520",text:"Origins Pinch Your Cheeks"},
{id:"6521",text:"Physicians Formula Happy Booster Glow & Mood Boosting Powder"},
{id:"6522",text:"Ole Henriksen Truth Revealed Super Cream SPF 15"},
{id:"6523",text:"Revlon PhotoReady Cream Blush"},
{id:"6524",text:"Olay Regenerist Luminous Overnight Mask Gel Moisturizer"},
{id:"6525",text:"Paula's Choice Skincare Skin Perfecting 2% BHA Lotion"},
{id:"6526",text:"Revlon Ultra HD Matte Lip Color"},
{id:"6527",text:"Paula's Choice Skincare Resist Weekly Retexturizing Foaming Treatment 4% BHA"},
{id:"6528",text:"Revlon Grow Luscious Lash Liner"},
{id:"6529",text:"Revlon ColorStay Aqua Mineral Makeup SPF 13"},
{id:"6530",text:"Sally Hansen Nails & Cuticles Hand Creme"},
{id:"6531",text:"Olay Oil-Minimizing Toner"},
{id:"6532",text:"Paula's Choice Skincare Clear Acne Body Spray"},
{id:"6533",text:"Revlon Cream Blush"},
{id:"6534",text:"Revlon Colorstay Makeup Normal/Dry Skin"},
{id:"6535",text:"Origins Drink Up, 10 Minute Mask to Quench Skins Thirst"},
{id:"6536",text:"Revlon CustomEyes Shadow & Liner"},
{id:"6537",text:"Revlon Revlon Colorstay Smoky Shadow Stick"},
{id:"6538",text:"P’_rminerals Colour Correcting Primer/Green and Peach"},
{id:"6539",text:"Palladio Beauty Herbal Foundation Primer"},
{id:"6540",text:"philosophy when hope is not enough replenishing cream"},
{id:"6541",text:"Rimmel Clean Finish Foundation"},
{id:"6542",text:"Revlon ColorStay Eyeliner"},
{id:"6543",text:"Peter Thomas Roth Oily Problem Skin Instant Mineral SPF 30"},
{id:"6544",text:"Olay Fresh Effects Clear Skin Spot Zinger"},
{id:"6545",text:"Origins Quick, Hide! Long-wearing Concealer"},
{id:"6546",text:"Prestige Cosmetics MultiTask Wet/Dry Powder Foundation"},
{id:"6547",text:"Perricone MD Cosmeceuticals Firming Facial Toner"},
{id:"6548",text:"philosophy kiss me tonight"},
{id:"6549",text:"Physicians Formula Firming & Lifting Booster, Formula RX 124"},
{id:"6550",text:"Palladio Beauty Ultra-Firming Anti-Wrinkle Serum"},
{id:"6551",text:"Peter Thomas Roth Botanical Buffing Beads"},
{id:"6552",text:"Olay Total Effects 7-in-1 Feather Weight Moisturizer Broad Spectrum SPF 15"},
{id:"6553",text:"philosophy purity made simple one-step facial cleansing cloths"},
{id:"6554",text:"Rodan and Fields Reverse Skin Lightening Treatment"},
{id:"6555",text:"Paula's Choice Skincare Oil-Blotting Papers"},
{id:"6556",text:"Peter Thomas Roth FIRMx Peeling Gel"},
{id:"6557",text:"Origins A Perfect World SPF 25 Age-Defense Moisturizer with White Tea"},
{id:"6558",text:"Ren F10 Smooth and Renew Mask"},
{id:"6559",text:"Olay 4-in-1 Daily Facial Cloths, Sensitive"},
{id:"6560",text:"Revlon New Complexion One Step Compact Makeup SPF 15"},
{id:"6561",text:"Revlon Just Bitten Kissable Balm Stain"},
{id:"6562",text:"RoC Multi Correxion Lift Anti-Gravity Serum"},
{id:"6563",text:"Olay Total Effects 7-in-1 Feather Weight Moisturizer Broad Spectrum SPF 15 Fragrance Free"},
{id:"6564",text:"Rimmel Sunshimmer Shimmering Maxi Bronzer"},
{id:"6565",text:"Proactiv & Proactiv+ Advanced Blemish Treatment"},
{id:"6566",text:"Physicians Formula Elastin/Collagen Moisture Lotion, for Normal to Dry Skin"},
{id:"6567",text:"Physicians Formula CoverToxTen50 Wrinkle Formula Face Powder"},
{id:"6568",text:"Ren Sirtuin Phytohormone Replenishing Cream"},
{id:"6569",text:"Physicians Formula Wrinkle Corrector & Firming Cleanser, Formula RX 101"},
{id:"6570",text:"Ren ClearCalm 3 Clarifying Clay Cleanser"},
{id:"6571",text:"Sally Hansen Quick Care Cuticle Treatment"},
{id:"6572",text:"Perricone MD Cosmeceuticals Vitamin C Ester Eye Serum"},
{id:"6573",text:"philosophy the supernatural poreless flawless tinted SPF 15"},
{id:"6574",text:"Proactiv & Proactiv+ Pore Targeting Treatment"},
{id:"6575",text:"Physicians Formula Argan Wear Ultra-Nourishing Illuminating Argan Oil"},
{id:"6576",text:"Revlon Nearly Naked Pressed Powder"},
{id:"6577",text:"Physicians Formula Shimmer Strips Custom Bronzer, Blush, & Eye Shadow"},
{id:"6578",text:"philosophy the supernatural airbrushed canvas spf 15"},
{id:"6579",text:"Origins Multi-Grain Pressed Bronzer"},
{id:"6580",text:"Peter Thomas Roth Irish Moor Mud Purifying Black Mask"},
{id:"6581",text:"Physicians Formula pH Matchmaker pH Powered Powder"},
{id:"6582",text:"Ren Rose Synergy O12 Moisture Defence Serum"},
{id:"6583",text:"philosophy hope in a jar night"},
{id:"6584",text:"Peter Thomas Roth Rose Stem Cell Bio-Repair Gel Mask"},
{id:"6585",text:"Olay Total Effects 7-in-1 Advanced Anti-Aging Deep Penetrating Moisture Body Wash"},
{id:"6586",text:"Rimmel Soft Kohl Kajal Eye Pencil"},
{id:"6587",text:"Origins Fire Fighter"},
{id:"6588",text:"Paula's Choice Skincare Skin Balancing Oil-Reducing Cleanser"},
{id:"6589",text:"Olay Pro-X Deep Wrinkle Treatment"},
{id:"6590",text:"Rodin Olio Lusso Luxury Face Oil"},
{id:"6591",text:"Peter Thomas Roth Laser-Free Resurfacing Eye Serum"},
{id:"6592",text:"Perricone MD Cosmeceuticals No Foundation Foundation SPF 30"},
{id:"6593",text:"Origins High Potency Night-A-Mins Mineral Enriched Oil-Free Renewal Cream"},
{id:"6594",text:"Origins Matte, Sheer, and Shimmer Sticks"},
{id:"6595",text:"Revlon ColorStay ShadowLinks"},
{id:"6596",text:"Olay Regenerist Instant Fix Exfoliate and Renew System"},
{id:"6597",text:"philosophy clear days ahead overnight repair salicylic acid acne treatment pads"},
{id:"6598",text:"Sally Hansen Healthy Cuticles Now!"},
{id:"6599",text:"Ole Henriksen Pure Truth Melting Cleanser"},
{id:"6600",text:"Rimmel Volume Accelerator Mascara"},
{id:"6601",text:"RoC Multi Correxion Nourish Stress Repair Eye Cream"},
{id:"6602",text:"Rimmel Lycra Lash Extender Mascara"},
{id:"6603",text:"Origins Mega Mushroom Skin Relief Face Mask"},
{id:"6604",text:"Physicians Formula Mineral Wear Talc-Free Mineral Airbrushing Loose Powder SPF 30"},
{id:"6605",text:"philosophy no reason to hide instant skin-tone perfecting moisturizer spf 20"},
{id:"6606",text:"Replenix Ultra Sheer Sunscreen SPF 50+"},
{id:"6607",text:"Origins Mega-Bright Skin Tone Correcting Serum"},
{id:"6608",text:"Peter Thomas Roth FIRMx Firming Night Moisturizer"},
{id:"6609",text:"Origins Out of Trouble, 10 Minute Mask to Rescue Problem Skin"},
{id:"6610",text:"Physicians Formula To Any Lengths Lash Extending Mascara"},
{id:"6611",text:"Origins Modern Friction, Nature's Gentle Dermabrasion"},
{id:"6612",text:"Prestige Cosmetics Skin Loving Minerals Lasting Moisture Lip Gloss"},
{id:"6613",text:"Origins A Perfect World, Antioxidant Moisturizer with White Tea"},
{id:"6614",text:"philosophy total matteness oil-free, mattifying pore eraser"},
{id:"6615",text:"Origins Fringe Benefits Lash-Loving Mascara"},
{id:"6616",text:"philosophy miracle worker miraculous anti-aging moisturizer"},
{id:"6617",text:"Origins Plantscription SPF 25 Anti-Aging Cream Broad Spectrum"},
{id:"6618",text:"Pixi Flawless Finishing Powder"},
{id:"6619",text:"RoC Retinol Correxion Sensitive Eye Cream"},
{id:"6620",text:"P’_rminerals Mineral Eye Defining Pencil with Smudger"},
{id:"6621",text:"Paula's Choice Skincare FANtastic Lash Mascara"},
{id:"6622",text:"Origins Starting Over Age-Erasing Eye Cream"},
{id:"6623",text:"Revlon ColorStay Ultimate Suede Lipstick"},
{id:"6624",text:"Peter Thomas Roth Chamomile Cleansing Lotion with Natural Herbal Extract for All Skin Types"},
{id:"6625",text:"P’_rminerals 4-in-1 Liquid Foundation 14 Hour Wear Broad Spectrum SPF 15"},
{id:"6626",text:"P’_rminerals Revitalizing Wash Souffle Facial Cleanser"},
{id:"6627",text:"P’_rminerals 4-in-1 Mineral Tinted Moisturizer Broad Spectrum SPF 20"},
{id:"6628",text:"Physicians Formula Super BB All-in-1 Beauty Balm Powder SPF 30"},
{id:"6629",text:"Pixi Lash Booster Mascara"},
{id:"6630",text:"Physicians Formula AquaWear Waterproof Mascara"},
{id:"6631",text:"Paula's Choice Skincare Skin Recovery Replenishing Moisturizer"},
{id:"6632",text:"Origins Fill in the Blanks Eyebrow Enhancer"},
{id:"6633",text:"Origins VitaZing SPF 15"},
{id:"6634",text:"P’_rminerals 4-in-1 Pressed Mineral Makeup Foundation SPF 15 Split Pan"},
{id:"6635",text:"Sally Hansen Cracked Heel Repair Creme"},
{id:"6636",text:"Physicians Formula Brow Definer Automatic Brow Pencil"},
{id:"6637",text:"Rimmel The Max Volume Flash Bold Curves Mascara"},
{id:"6638",text:"Olay Complete Daily Defense All Day Moisturizer with Sunscreen SPF 30 for Sensitive Skin"},
{id:"6639",text:"Paula's Choice Skincare Resist Intensive Repair Cream"},
{id:"6640",text:"Prestige Cosmetics Liquid Glitter Precision Eyeliner"},
{id:"6641",text:"Pixi Succulent Lip Twin"},
{id:"6642",text:"Prestige Cosmetics Sun Play Baked Bronze Mineral Powder"},
{id:"6643",text:"Ole Henriksen African Red Tea Face Mist"},
{id:"6644",text:"Olay Pro-X Advanced Cleansing System"},
{id:"6645",text:"Perricone MD Cosmeceuticals Advanced Face Firming Activator"},
{id:"6646",text:"Paula's Choice Skincare Skin Recovery Super Antioxidant Concentrate Serum"},
{id:"6647",text:"PanOxyl PanOxyl Bar 5%, Regular Strength Acne Wash"},
{id:"6648",text:"philosophy miracle worker spf 50 miraculous anti-aging fluid"},
{id:"6649",text:"Prestige Cosmetics Lightshine Weightless High Shine Lipgloss"},
{id:"6650",text:"Peter Thomas Roth Clinical Peel & Reveal Dermal Resurfacer"},
{id:"6651",text:"Ole Henriksen Pure Truth Youth Activating Oil"},
{id:"6652",text:"Olay Regenerist Micro-Sculpting Cream"},
{id:"6653",text:"Ren Resurfacing AHA Concentrate"},
{id:"6654",text:"Paula's Choice Skincare Moisture Boost Daily Restoring Complex with SPF 30"},
{id:"6655",text:"Paula's Choice Skincare Resist Super-Light Daily Wrinkle Defense SPF 30"},
{id:"6656",text:"Prestige Cosmetics Shadows"},
{id:"6657",text:"Origins High Potency Night-A-Mins Mineral-Enriched Renewal Cream"},
{id:"6658",text:"Paula's Choice Skincare Brighten Up 2-Minute Teeth Whitener"},
{id:"6659",text:"Rimmel GlamEyes Quad Eye Shadow"},
{id:"6660",text:"Physicians Formula pH Matchmaker Powdered Blush"},
{id:"6661",text:"Peter Thomas Roth Glycolic Acid 10% Moisturizer"},
{id:"6662",text:"Olay Age Defying Anti-Wrinkle Night Cream"},
{id:"6663",text:"Proactiv & Proactiv+ Skin Purifying Mask"},
{id:"6664",text:"philosophy miracle worker miraculous anti-aging retinoid pads and solution"},
{id:"6665",text:"Perricone MD Cosmeceuticals Acyl-Glutathione Eye Lid Serum"},
{id:"6666",text:"Ren Active 7 Radiant Eye Gel"},
{id:"6667",text:"Sally Hansen Pumice Foot Polish"},
{id:"6668",text:"Paula's Choice Skincare Resist Anti-Aging Lip Gloss + Broad Spectrum SPF 40"},
{id:"6669",text:"Peter Thomas Roth Ultimate Creme in a Tube"},
{id:"6670",text:"Physicians Formula Mineral Wear Talc-Free Mineral Illuminating Powder Duo SPF 16"},
{id:"6671",text:"Perricone MD Cosmeceuticals Blue Plasma"},
{id:"6672",text:"Palladio Beauty UV Defense Daily Moisturizer with SPF 15"},
{id:"6673",text:"Olay Regenerist Instant Fix Wrinkle and Pore Vanisher"},
{id:"6674",text:"Peter Thomas Roth Retinol Fusion PM"},
{id:"6675",text:"Revlon Powder Blush"},
{id:"6676",text:"Rimmel ScandalEyes Rockin Curves Mascara"},
{id:"6677",text:"Pond's Luminous Clean Daily Exfoliating Cleanser"},
{id:"6678",text:"Paula's Choice Skincare Calm Redness Relief Repairing Serum"},
{id:"6679",text:"RevitaLash Revitalash Advanced Eyelash Conditioner"},
{id:"6680",text:"Paula's Choice Skincare Resist C15 Super Booster"},
{id:"6681",text:"Paula's Choice Skincare Lip & Body Treatment Balm"},
{id:"6682",text:"Physicians Formula Talc-Free Mineral Wear Illuminating Veil"},
{id:"6683",text:"Paula's Choice Skincare Calm Redness Relief Cleanser Normal to Oily"},
{id:"6684",text:"Rimmel ScandalEyes Xx-Treme"},
{id:"6685",text:"Rimmel 1000 Kisses Stay On Lip Liner Pencil"},
{id:"6686",text:"Rimmel Lasting Finish Lipstick"},
{id:"6687",text:"Proactiv & Proactiv+ Renewing Cleanser"},
{id:"6688",text:"Perricone MD Cosmeceuticals Vitamin C Ester 15"},
{id:"6689",text:"Purpose Gentle Cleansing Bar"},
{id:"6690",text:"Perricone MD Cosmeceuticals Acyl-Glutathione"},
{id:"6691",text:"Olay Total Effects Revitalizing Foaming Cleanser"},
{id:"6692",text:"Perricone MD Cosmeceuticals Intensive Pore Minimizer"},
{id:"6693",text:"Prestige Cosmetics Brow Perfection Revitalizing Clear Brow Gel"},
{id:"6694",text:"Olay Wet Cleansing Cloths, Sensitive"},
{id:"6695",text:"Palladio Beauty Matte Blush"},
{id:"6696",text:"Olay Regenerist Luminous Dark Circle Correcting Hydraswirl"},
{id:"6697",text:"Peter Thomas Roth Lashes to Die For Turbo Nighttime Eyelash Treatment"},
{id:"6698",text:"Physicians Formula Youthful Wear Cosmeceutical Youth-Boosting Face Powder"},
{id:"6699",text:"Origins Plantscription SPF 25 Anti-Aging Oil-Free Cream"},
{id:"6700",text:"Physicians Formula Baked Collection Wet-Dry Eye Shadow"},
{id:"6701",text:"P’_rminerals Pur Radiance Mineral Powder"},
{id:"6702",text:"Physicians Formula Organic Wear 100% Natural Origin Tinted Moisturizer SPF 15"},
{id:"6703",text:"RoC Instant Facial Smoother"},
{id:"6704",text:"Revlon Grow Luscious Plumping Mascara"},
{id:"6705",text:"Olay Ultra Moisture Bar"},
{id:"6706",text:"Peter Thomas Roth Anti-Aging Cleansing Gel"},
{id:"6707",text:"Physicians Formula Solar Powder SPF 20 Face Powder"},
{id:"6708",text:"Revlon ColorStay Pressed Powder"},
{id:"6709",text:"Pixi Endless Silky Eye Pen"},
{id:"6710",text:"Peter Thomas Roth Camu Camu Power C x 30 Vitamin C Brightening Serum"},
{id:"6711",text:"Ole Henriksen Fresh Start Eye Creme"},
{id:"6712",text:"RoC Retinol Correxion Deep Wrinkle Night Cream"},
{id:"6713",text:"Peter Thomas Roth Retinol Fusion Alternative Anti-Wrinkle Solution"},
{id:"6714",text:"Revlon Luxurious Color Eyeliner"},
{id:"6715",text:"Physicians Formula Mineral Wear Talc-Free Correcting Bronzer"},
{id:"6716",text:"Olay Regenerist Regenerating Lotion Sunscreen Broad Spectrum SPF 15"},
{id:"6717",text:"Physicians Formula Healthy Wear SPF 50 Tinted Moisturizer"},
{id:"6718",text:"Olay Age Defying Protective Renewal Lotion SPF 15"},
{id:"6719",text:"Physicians Formula Bright Collection Shimmery Quad Eye Shadow"},
{id:"6720",text:"philosophy the microdelivery triple-acid brightening peel"},
{id:"6721",text:"RoC Multi Correxion Lift Anti-Gravity Night Cream"},
{id:"6722",text:"Peter Thomas Roth Camu Camu Power C x 30 Vitamin C Brightening Sleeping Mask"},
{id:"6723",text:"P’_rminerals Under Control Mattifying Gel"},
{id:"6724",text:"Paula's Choice Skincare Resist Body Oil Spray"},
{id:"6725",text:"Origins Pinch Your Cheeks Powder Blush"},
{id:"6726",text:"Physicians Formula Super CC+ Color-Correction + Care CC+ Concealer SPF 30"},
{id:"6727",text:"Revlon Ultimate All-In-One Mascara"},
{id:"6728",text:"Proactiv & Proactiv+ Clarifying Night Cream"},
{id:"6729",text:"Revlon Age Defying with DNA Advantage Powder"},
{id:"6730",text:"Olay Pro-X Eye Restoration Complex"},
{id:"6731",text:"philosophy amazing grace perfumed firming body emulsion"},
{id:"6732",text:"Revlon Grow Luscious by Fabulash Mascara and Lash Enhancer"},
{id:"6733",text:"Physicians Formula Mineral Wear Talc-Free Correcting Primer"},
{id:"6734",text:"Revlon Bold Lacquer By Grow Luscious Length & Volume Mascara Waterproof"},
{id:"6735",text:"Revlon Super Lustrous Matte Lipstick"},
{id:"6736",text:"Origins Faux Glow, Radiant Self-Tanner For Face"},
{id:"6737",text:"philosophy back to nurture replenishment"},
{id:"6738",text:"P’_rminerals Miracle Mist Hydrate & Set"},
{id:"6739",text:"Origins Eye Brightening Color Stick"},
{id:"6740",text:"Paula's Choice Skincare Resist Ultra-Light Super Antioxidant Concentrate Serum"},
{id:"6741",text:"Pond's Evening Soothe Wet Cleansing Towelettes"},
{id:"6742",text:"Physicians Formula Bronze Booster GlowBoosting Pressed Bronzer"},
{id:"6743",text:"P’_rminerals Mineral Mudd Masque Detoxifying Masque"},
{id:"6744",text:"Paula's Choice Skincare Extra Care Non-Greasy Sunscreen SPF 50, for Normal to Oily/Combination Skin"},
{id:"6745",text:"RoC Retinol Correxion Eye Cream"},
{id:"6746",text:"Prestige Cosmetics Soft Blend Kohl Eyeliner"},
{id:"6747",text:"Rimmel Show Off Lip Velvet Matte"},
{id:"6748",text:"Physicians Formula Redness Relief Moisturizer, Formula RX 311"},
{id:"6749",text:"Paula's Choice Skincare Clear Regular Strength Anti-Redness Exfoliating Solution with 2% Salicylic Acid"},
{id:"6750",text:"Paula's Choice Skincare Skin Perfecting 1% BHA Lotion"},
{id:"6751",text:"Peter Thomas Roth Un-Wrinkle Peel Pads, for All Skin Types"},
{id:"6752",text:"Origins Brighter By Nature SPF 35 Skin Tone Correcting Moisturizer"},
{id:"6753",text:"Revlon PhotoReady SkinLights Face Illuminator"},
{id:"6754",text:"PanOxyl PanOxyl Bar 10%, Maximum Strength Acne Wash"},
{id:"6755",text:"Perricone MD Cosmeceuticals Blue Plasma Orbital"},
{id:"6756",text:"Origins Drink It Up Hydrating Lip Balm"},
{id:"6757",text:"Palmer's Cocoa Butter Formula Lotion for Dry, Ashy Skin"},
{id:"6758",text:"Revlon CustomEyes Waterproof Mascara"},
{id:"6759",text:"Peter Thomas Roth Ultra-Lite Anti-Aging Cellular Repair"},
{id:"6760",text:"Sally Hansen Maximum Growth Cuticle Pen"},
{id:"6761",text:"Paula's Choice Skincare Skin Perfecting 2% BHA Liquid"},
{id:"6762",text:"Physicians Formula Bronze Booster Glow-Boosting Pressed Shimmer Bronzer"},
{id:"6763",text:"Paula's Choice Skincare Skin Recovery Hydrating Treatment Mask"},
{id:"6764",text:"Prestige Cosmetics Let Loose! Duo Shimmering Shadow Dust"},
{id:"6765",text:"Proactiv & Proactiv+ Repairing Treatment"},
{id:"6766",text:"Rimmel Exaggerate Waterproof Eye Definer"},
{id:"6767",text:"philosophy take a deep breath cushion foundation broad spectrum spf 20"},
{id:"6768",text:"Paula's Choice Skincare Resist Intensive Wrinkle-Repair Retinol Serum"},
{id:"6769",text:"Physicians Formula Youthful Wear Cosmeceutical Youth-Boosting Foundation & Brush SPF 15"},
{id:"6770",text:"Peter Thomas Roth Aloe Tonic Mist"},
{id:"6771",text:"Physicians Formula Shimmer Strips Custom Eye Enhancing Kohl Kajal Eyeliner Trio"},
{id:"6772",text:"Pixi Large Lash Mascara"},
{id:"6773",text:"Sally Hansen Ultimate Shield Hand Cream"},
{id:"6774",text:"Peter Thomas Roth Rose Stem Cell Bio-Repair Cleansing Gel"},
{id:"6775",text:"Proactiv & Proactiv+ Soothing Shave Gel"},
{id:"6776",text:"Ren No.1 Purity Cleansing Balm"},
{id:"6777",text:"Palladio Beauty Eyeshadow Primer"},
{id:"6778",text:"Origins Mega-Mushroom Skin Relief Face Lotion"},
{id:"6779",text:"Peter Thomas Roth AHA/BHA Acne Clearing Gel"},
{id:"6780",text:"Peter Thomas Roth Laser-Free Regenerator Moisturizing Gel-Cream"},
{id:"6781",text:"Paula's Choice Skincare Resist Skin Revealing Body Lotion with 10% AHA"},
{id:"6782",text:"Physicians Formula Mineral Wear Talc-Free All-in-1 ABC Cushion Foundation SPF 50"},
{id:"6783",text:"Ren Mayblossom and Blue Cypress Facial WashCombination"},
{id:"6784",text:"P’_rminerals Mineral Glow"},
{id:"6785",text:"Paula's Choice Skincare Clear Ultra-Light Daily Mattifying Fluid Broad-Spectrum SPF 30+"},
{id:"6786",text:"Olay Regenerist Deep Hydration Regenerating Cream"},
{id:"6787",text:"Perricone MD Cosmeceuticals Deep Moisture Therapy"},
{id:"6788",text:"PanOxyl PanOxyl Acne Foaming Wash, 10% Benzoyl Peroxide"},
{id:"6789",text:"Prestige Cosmetics Total Intensity Long Term Relationship Fierce Color Eyeshadow"},
{id:"6790",text:"philosophy full of promise dual-action restoring cream"},
{id:"6791",text:"Rimmel Volume Flash Scandaleyes Mascara"},
{id:"6792",text:"Perricone MD Cosmeceuticals Intensive Pore Treatment"},
{id:"6793",text:"Ole Henriksen Aloe Vera Deep Cleanser"},
{id:"6794",text:"RoC Retinol Correxion Sensitive Night Cream"},
{id:"6795",text:"Paula's Choice Skincare Skin Recovery Softening Cream Cleanser"},
{id:"6796",text:"Perricone MD Cosmeceuticals Lip Plumper"},
{id:"6797",text:"Perricone MD Cosmeceuticals Citrus Facial Wash"},
{id:"6798",text:"Paula's Choice Skincare Clinical Ultra-Rich Moisturizer"},
{id:"6799",text:"Physicians Formula Wrinkle Corrector & Firming Moisturizer SPF 15 Formula RX 111"},
{id:"6800",text:"Olay Regenerist Luminous Brightening Cream Cleanser"},
{id:"6801",text:"Origins Mega-Mushroom Skin Relief Face Cream"},
{id:"6802",text:"Replenix Resurfix Skin Barrier Healing Ointment"},
{id:"6803",text:"Rimmel Special Eyes Precision Eye Liner Pencil"},
{id:"6804",text:"Ole Henriksen Ultimate Lift Firming Serum"},
{id:"6805",text:"Ren Mayday Mayday Rescue Balm"},
{id:"6806",text:"philosophy renewed hope in a jar eye"},
{id:"6807",text:"Physicians Formula CoverToxTen50 Wrinkle Therapy Foundation"},
{id:"6808",text:"Physicians Formula Mineral Wear Talc-Free Mineral Correcting Concealer"},
{id:"6809",text:"Olay Total Effects Eye Transforming Cream"},
{id:"6810",text:"Rodan and Fields Redefine Multi-Function Eye Cream"},
{id:"6811",text:"Revlon PhotoReady Eye Art Lid + Line + Lash"},
{id:"6812",text:"Paula's Choice Skincare Brow/Hair Tint"},
{id:"6813",text:"Revlon Colorburst Lipgloss"},
{id:"6814",text:"P’_rminerals 4-in-1 Pressed Mineral Makeup SPF 15"},
{id:"6815",text:"Origins Well Off, Fast and Gentle Eye Makeup Remover"},
{id:"6816",text:"Origins Liquid Lip Shimmer"},
{id:"6817",text:"Revlon ColorBurst Matte Balm"},
{id:"6818",text:"Peter Thomas Roth Lips to Die For Pink Bombshell Lip Balm"},
{id:"6819",text:"Ren ClearCalm 3 Anti-Acne Treatment Mask"},
{id:"6820",text:"Origins Mega Mushroom Advanced Skin Relief Face Serum"},
{id:"6821",text:"RoC Retinol Correxion Deep Wrinkle Filler"},
{id:"6822",text:"Revlon Colorstay Overtime Lipcolor"},
{id:"6823",text:"Prestige Cosmetics BrowPerfection Brow Building System"},
{id:"6824",text:"Prestige Cosmetics Total Intensity Mascara"},
{id:"6825",text:"Paula's Choice Skincare Skin Perfecting 8% AHA Gel"},
{id:"6826",text:"Proactiv & Proactiv+ Advanced Dark Spot Correcting Serum"},
{id:"6827",text:"Revlon Age Defying with DNA Advantage Cream Makeup SPF 20"},
{id:"6828",text:"Sally Hansen Lip Inflation Plumping Treatment"},
{id:"6829",text:"Origins Drink Up Intensive Overnight Mask to Quench Skins Thirst"},
{id:"6830",text:"Proactiv & Proactiv+ Oil Blotter Sheets"},
{id:"6831",text:"Origins Super Spot Remover, Acne Treatment Gel"},
{id:"6832",text:"Prestige Cosmetics Classic Eyeliner"},
{id:"6833",text:"Palladio Beauty Liquid Eyeliner"},
{id:"6834",text:"Revlon Ultra HD Lipstick"},
{id:"6835",text:"Pixi Eye Bright Primer"},
{id:"6836",text:"P’_rminerals Pur Rocks"},
{id:"6837",text:"Revlon Nearly Naked Makeup Broad Spectrum SPF 20"},
{id:"6838",text:"Perricone MD Cosmeceuticals High Potency Eye Lift"},
{id:"6839",text:"Pond's Exfoliating Renewal Wet Cleansing Towelettes"},
{id:"6840",text:"Rimmel Stay Matte Pressed Powder"},
{id:"6841",text:"Rimmel Fix & Perfect Foundation Primer"},
{id:"6842",text:"Physicians Formula Mineral Wear Talc-Free Mineral Blush"},
{id:"6843",text:"Revlon ColorStay Liquid Liner"},
{id:"6844",text:"Proactiv & Proactiv+ Refining Mask"},
{id:"6845",text:"Ren Jojoba Micro Bead Purifying Facial Scrub"},
{id:"6846",text:"Physicians Formula Enriched Dry Skin Concentrate, for Dry to Very Dry Skin"},
{id:"6847",text:"Origins Original Skin Renewal Serum with Willowherb"},
{id:"6848",text:"Ren ClearCalm 3 Total Clarity Day Fluid"},
{id:"6849",text:"Physicians Formula Organic Wear 100% Natural Origin CC Color + Correction Cream SPF 20"},
{id:"6850",text:"Prestige Cosmetics Automatic Eye Liner"},
{id:"6851",text:"Peter Thomas Roth Max Sheer All Day Moisture Defense Lotion with SPF 30"},
{id:"6852",text:"Revlon PhotoReady Translucent Finisher"},
{id:"6853",text:"Rodan and Fields Unblemish Oil Control Lotion SPF 20"},
{id:"6854",text:"philosophy the present"},
{id:"6855",text:"Peter Thomas Roth Modern Classic Shave Cream"},
{id:"6856",text:"Proactiv & Proactiv+ Complexion Perfecting Hydrator"},
{id:"6857",text:"Origins Make A Difference Plus + Rejuvenating Treatment"},
{id:"6858",text:"philosophy miracle worker overnight"},
{id:"6859",text:"Palladio Beauty Herbal Dual Wet & Dry Foundation"},
{id:"6860",text:"Ole Henriksen Walnut Complexion Scrub"},
{id:"6861",text:"Perricone MD Cosmeceuticals Hypoallergenic Gentle Cleanser"},
{id:"6862",text:"Paula's Choice Skincare Healthy Finish Pressed Powder SPF 15"},
{id:"6863",text:"philosophy hope in a jar, extra-rich moisturizer for normal to dry skin"},
{id:"6864",text:"Physicians Formula Super BB All-in-1 Beauty Balm Concealer SPF 30"},
{id:"6865",text:"Physicians Formula Pearls of Perfection Multi-Colored Powder Pearls"},
{id:"6866",text:"Origins Flower Fusion Hydrating Lip Color"},
{id:"6867",text:"Palladio Beauty Herbal Lip Gloss"},
{id:"6868",text:"Ren Bio Retinoid Anti-Aging Concentrate"},
{id:"6869",text:"Olay Wet Cleansing Cloths, Normal"},
{id:"6870",text:"Revlon PhotoReady Makeup SPF 20"},
{id:"6871",text:"Rimmel Stay Glossy Long-Lasting Lipgloss"},
{id:"6872",text:"Paula's Choice Skincare Hydralight One Step Face Cleanser"},
{id:"6873",text:"Palladio Beauty Foaming Cleanser & Makeup Remover"},
{id:"6874",text:"philosophy miracle worker anti-aging foundation SPF 30"},
{id:"6875",text:"Pond's Luminous Finish BB+ Cream"},
{id:"6876",text:"Paula's Choice Skincare Calm Redness Relief 1% BHA Lotion Exfoliant"},
{id:"6877",text:"Peter Thomas Roth Moisture Infusion Facial Bar"},
{id:"6878",text:"Peter Thomas Roth Anti-Aging Instant Mineral SPF 45"},
{id:"6879",text:"Paula's Choice Skincare PC4Men Daytime Protect SPF 30"},
{id:"6880",text:"Perricone MD Cosmeceuticals Cold Plasma Body"},
{id:"6881",text:"Sephora Kohl Pencil"},
{id:"6882",text:"Physicians Formula Plump Potion NeedleFree Plumping Lipstick"},
{id:"6883",text:"RoC Multi Correxion Skin Renewing Serum"},
{id:"6884",text:"Olay Pro-X Exfoliating Renewal Cleanser"},
{id:"6885",text:"Rodan and Fields Reverse Deep Exfoliating Wash"},
{id:"6886",text:"Palmer's Cocoa Butter Formula Massage Lotion For Stretch Marks"},
{id:"6887",text:"Origins Organics Purifying Tonic"},
{id:"6888",text:"Physicians Formula Youthful Wear Cosmeceutical Youth-Boosting Concealer"},
{id:"6889",text:"Perricone MD Cosmeceuticals No Foundation Foundation Serum SPF 30"},
{id:"6890",text:"P’_rminerals Pur Intensity Gel Eyeliner"},
{id:"6891",text:"Palmer's Cocoa Butter Formula Massage Cream For Stretch Marks"},
{id:"6892",text:"Ole Henriksen Vitamin Plus"},
{id:"6893",text:"Paula's Choice Skincare Skin Balancing Oil-Absorbing Mask"},
{id:"6894",text:"Physicians Formula Powder Palette Mineral Glow Pearls Blush"},
{id:"6895",text:"RoC Daily Resurfacing Disks"},
{id:"6896",text:"Ole Henriksen Visual Truth Eye Creme"},
{id:"6897",text:"Proactiv & Proactiv+ Green Tea Moisturizer"},
{id:"6898",text:"philosophy Hope in a Jar A-Z Cream Broad Spectrum SPF 20"},
{id:"6899",text:"Paula's Choice Skincare PC4MEN Lip Balm SPF 50"},
{id:"6900",text:"Peter Thomas Roth Un-Wrinkle Foundation SPF 20"},
{id:"6901",text:"Rimmel GlamEyes Trio Eye Shadow"},
{id:"6902",text:"Revlon PhotoReady 3D Volume Mascara"},
{id:"6903",text:"Peter Thomas Roth CC Eye Correcting Concealer Broad Spectrum SPF 30"},
{id:"6904",text:"philosophy miracle worker miraculous anti-aging concentrate"},
{id:"6905",text:"Paula's Choice Skincare Barely There Sheer Matte Tint SPF 30"},
{id:"6906",text:"Origins Plantscription SPF 15 Anti-Aging Foundation"},
{id:"6907",text:"Rimmel Provocalips 16 HR Kissproof Lip Colour"},
{id:"6908",text:"Physicians Formula Eye Booster Instant Lash Extension Kit"},
{id:"6909",text:"Ole Henriksen Truth Crme Advanced Hydration"},
{id:"6910",text:"Prestige Cosmetics Vinylwear High Shine Lip Gloss"},
{id:"6911",text:"Physicians Formula Organic Wear 100% Natural Origin Fake Out Mascara"},
{id:"6912",text:"Prestige Cosmetics Shadow Duo"},
{id:"6913",text:"Origins All and Nothing Sheer Pressed Powder"},
{id:"6914",text:"Physicians Formula Gentle Refreshing Toner, for Dry to Very Dry Skin"},
{id:"6915",text:"Paula's Choice Skincare PC4Men Nighttime Repair"},
{id:"6916",text:"Peter Thomas Roth De-Spot Skin Brightening Corrector"},
{id:"6917",text:"Rimmel Scandaleyes Waterproof Gel Eyeliner"},
{id:"6918",text:"Origins Never A Dull Moment, Skin-Brightening Face Cleanser with Fruit Extracts"},
{id:"6919",text:"Physicians Formula Gentle Cleansing Lotion, for Normal to Dry Skin"},
{id:"6920",text:"Perricone MD Cosmeceuticals Photo Plasma Broad Spectrum SPF 30"},
{id:"6921",text:"Physicians Formula Eye Definer Felt-Tip Eye Marker"},
{id:"6922",text:"Physicians Formula Matte Collection Quad Eye Shadow"},
{id:"6923",text:"Physicians Formula Happy Booster Glow & Mood Boosting Blush"},
{id:"6924",text:"Paula's Choice Skincare Resist Instant Smoothing Anti-Aging Foundation"},
{id:"6925",text:"Peter Thomas Roth Max All Day Moisture Defense Cream with SPF 30"},
{id:"6926",text:"Physicians Formula Mineral Wear Talc-Free Bronzing Veil"},
{id:"6927",text:"Paula's Choice Skincare Resist Vitamin C Spot Treatment"},
{id:"6928",text:"Physicians Formula Mineral Wear 100% Mineral FaceBrightener"},
{id:"6929",text:"Paula's Choice Skincare Calm Redness Relief SPF 30 Mineral Moisturizer Normal to Dry"},
{id:"6930",text:"Perricone MD Cosmeceuticals Nutritive Cleanser"},
{id:"6931",text:"Perricone MD Cosmeceuticals Blue Plasma Cleansing Treatment"},
{id:"6932",text:"Origins A Perfect World Deep Cleanser with White Tea"},
{id:"6933",text:"Origins Plantscription Anti-Aging Eye Treatment"},
{id:"6934",text:"Physicians Formula Organic Wear 100% Natural Origin Blush"},
{id:"6935",text:"Revlon PhotoReady Primer + Shadow"},
{id:"6936",text:"Paula's Choice Skincare Calm Redness Relief Toner Normal to Dry"},
{id:"6937",text:"Physicians Formula Eye Booster 2-in-1 Lash Boosting Felt-Tip Eyeliner + Serum"},
{id:"6938",text:"Rimmel Lasting Finish Blendable Powder Blush"},
{id:"6939",text:"Ole Henriksen Truth Is In The Eyes Peel"},
{id:"6940",text:"Paula's Choice Skincare All Bases Covered Foundation SPF 25"},
{id:"6941",text:"Physicians Formula Argan Wear Ultra-Nourishing Argan Oil Blush"},
{id:"6942",text:"Paula's Choice Skincare Resist Triple-Action Dark Spot Eraser 7% AHA Lotion"},
{id:"6943",text:"philosophy miracle worker miraculous anti-aging retinoid eye repair"},
{id:"6944",text:"philosophy the supernatural powder airbrushed canvas SPF 15"},
{id:"6945",text:"Paula's Choice Skincare Skin Perfecting 2% BHA Gel"},
{id:"6946",text:"Origins Beyond the Fringe"},
{id:"6947",text:"Pond's Clarant B3 Anti-Dark Spots Moisturizer, Normal to Oily"},
{id:"6948",text:"philosophy kiss me exfoliating lip scrub and facial"},
{id:"6949",text:"Physicians Formula Super CC+ Color-Correction + Care CC+ Powder SPF 30"},
{id:"6950",text:"RapidLash RapidLash Eyelash & Eyebrow Enhancing Serum"},
{id:"6951",text:"Ole Henriksen Nurture Me"},
{id:"6952",text:"Peter Thomas Roth Glycolic Acid 10% Hydrating Gel"},
{id:"6953",text:"Physicians Formula Shimmer Strips Custom Eyeliner, Eye Shadow & Highligher"},
{id:"6954",text:"Physicians Formula Touch of Glow Nude Wear Concealer Highlighter Touch-Up Tool"},
{id:"6955",text:"Rimmel WonderLash Mascara"},
{id:"6956",text:"Origins Easy Slider Pre-Shave Oil"},
{id:"6957",text:"Revlon PhotoReady Kajal Intense Eyeliner"},
{id:"6958",text:"Paula's Choice Skincare Resist Daily Smoothing Treatment with 5% Alpha Hydroxy Acid"},
{id:"6959",text:"Paula's Choice Skincare Skin Recovery Daily Moisturizing Lotion with SPF 30 & Antioxidants"},
{id:"6960",text:"Ole Henriksen Enlighten Me Skin Tone Corrector"},
{id:"6961",text:"Palmer's Cocoa Butter Formula Body Butter"},
{id:"6962",text:"Perricone MD Cosmeceuticals High Potency Amine Complex Face Lift"},
{id:"6963",text:"Physicians Formula PlentiFull Thickening Mascara"},
{id:"6964",text:"Origins All Purpose High Elevation Cream Dry Skin Relief"},
{id:"6965",text:"Peter Thomas Roth Anti-Aging Defense Sunscreen Stick Broad Spectrum SPF 50"},
{id:"6966",text:"P’_rminerals Mineral Lip Pencil with Lip Brush"},
{id:"6967",text:"Perricone MD Cosmeceuticals No Mascara Mascara"},
{id:"6968",text:"Paula's Choice Skincare Calm Redness Relief SPF 30 Mineral Moisturizer Normal to Oily"},
{id:"6969",text:"Ren Rosa Centifolia Cleansing Gel"},
{id:"6970",text:"Paula's Choice Skincare Shine Stopper Instant Matte Finish with Microsponge Technology"},
{id:"6971",text:"Perricone MD Cosmeceuticals High Potency Evening Repair"},
{id:"6972",text:"Paula's Choice Skincare Skin Balancing Invisible Finish Moisture Gel"},
{id:"6973",text:"Physicians Formula Deep Wrinkle Corrector Day & Night Cream, Formula RX 121"},
{id:"6974",text:"Origins Clean Energy, Gentle Cleansing Oil"},
{id:"6975",text:"Origins Plantscription Youth Renewing Face Oil"},
{id:"6976",text:"philosophy time in a bottle"},
{id:"6977",text:"Origins Mega-Mushroom Skin Relief Eye Serum"},
{id:"6978",text:"Perricone MD Cosmeceuticals Face Finishing Moisturizer"},
{id:"6979",text:"Olay Total Effects 7-in-1 Anti-Aging Moisturizer Daily Moisturizer Fragrance-Free"},
{id:"6980",text:"philosophy razor sharp extra close silicone shaving gel"},
{id:"6981",text:"Origins Original Skin Retexturizing Mask With Rose Clay"},
{id:"6982",text:"brand product"},
{id:"6983",text:"Smashbox Always Sharp Lip Liner"},
{id:"6984",text:"Tata Harper Rejuvenating Serum"},
{id:"6985",text:"Sunday Riley Luna Sleeping Oil"},
{id:"6986",text:"Tarte Cosmetics Amazonian Clay 12-Hour Blush"},
{id:"6987",text:"Smashbox Hyperlash Mascara"},
{id:"6988",text:"The Body Shop Camomile Silky Cleansing Oil"},
{id:"6989",text:"SK-II Skin Signature Mask 3-D Redefining Mask"},
{id:"6990",text:"Sephora Acne-Fighting Roll-On Gel"},
{id:"6991",text:"Shiseido Pureness Matifying Moisturizer, Oil Free"},
{id:"6992",text:"Skinfix Cleansing Oil Wash"},
{id:"6993",text:"Sephora Flashy Liner Waterproof"},
{id:"6994",text:"Sephora Cream Lip Stain"},
{id:"6995",text:"Shiseido Benefiance WrinkleResist24 Balancing Softener Enriched"},
{id:"6996",text:"StriVectin StriVectin-SD Power Serum for Wrinkles"},
{id:"6997",text:"Skinn by Dimitri James SOS Overnight Pore Reducing Serum"},
{id:"6998",text:"Smashbox Photo Finish Hydrating Under Eye Primer"},
{id:"6999",text:"TheBalm Stainiac Lip and Cheek Stain"},
{id:"7000",text:"Sephora Nano Eyeliner"},
{id:"7001",text:"Tarte Cosmetics Maracuja Creaseless Waterproof Concealer"},
{id:"7002",text:"Tarte Cosmetics Amazonian Clay BB Tinted Moisturizer Broad Spectrum SPF 20 Sunscreen"},
{id:"7003",text:"St. Tropez Tan Optimiser Body Moisturizer"},
{id:"7004",text:"Tend Skin Air Shave Gel"},
{id:"7005",text:"SkinMedica TNS Ceramide Treatment Cream"},
{id:"7006",text:"Too Faced Primed & Poreless Skin Smoothing Face Primer"},
{id:"7007",text:"The Body Shop Liquid Eyeliner"},
{id:"7008",text:"The Body Shop Moisture White Foaming Facial Wash"},
{id:"7009",text:"Sonia Kashuk Take Cover Concealing Stick"},
{id:"7010",text:"The Body Shop Carbon Black Eye Definer"},
{id:"7011",text:"Tend Skin Tend Skin Liquid"},
{id:"7012",text:"Shiseido Pureness Pore Minimizing Cooling Essence"},
{id:"7013",text:"Stila Stay All Day 10-in-1 HD Beauty Balm with Broad Spectrum SPF 30"},
{id:"7014",text:"Supergoop! Mint Fusion Lip Balm with Shea Butter Broad Spectrum SPF 30"},
{id:"7015",text:"Shiseido The Makeup Powdery Foundation SPF 14-17"},
{id:"7016",text:"Sonia Kashuk Brow Kit Arch Alert Palette"},
{id:"7017",text:"Sephora Supreme Cleansing Oil"},
{id:"7018",text:"The Body Shop Concealer All-In-One"},
{id:"7019",text:"The Body Shop Tea Tree Blemish Gel"},
{id:"7020",text:"Stila Stay All Day Vinyl Lipgloss"},
{id:"7021",text:"Tarte Cosmetics EmphasEYES Eyeliner High Definition Inner Rim Eyeliner Pencil"},
{id:"7022",text:"Sephora Age Defy Night Moisture Cream Sleeping Beauty"},
{id:"7023",text:"Stila Eye Shadow Trio"},
{id:"7024",text:"Shiseido The Skincare Protective Lip Conditioner SPF 10"},
{id:"7025",text:"St. Ives Intensive Healing Body Lotion"},
{id:"7026",text:"The Body Shop Moisture White Shiso Make-Up Cleansing Oil"},
{id:"7027",text:"Stridex Daily Care Acne Pads with Salicylic Acid, Sensitive with Aloe"},
{id:"7028",text:"Smashbox 24 Hour CC Spot Concealer"},
{id:"7029",text:"StriVectin StriVectinLabs Extreme Cream"},
{id:"7030",text:"Tom Ford Brow Sculptor"},
{id:"7031",text:"Shiseido Smoothing Eyeliner Pencil"},
{id:"7032",text:"Sonia Kashuk Brow Definer"},
{id:"7033",text:"Too Faced Amazing Face SPF 15 Foundation Powder"},
{id:"7034",text:"Tarte Cosmetics MultiplEYE Lash Enhancer"},
{id:"7035",text:"SkinCeuticals Clarifying Cleanser"},
{id:"7036",text:"SkinCeuticals Conditioning Solution"},
{id:"7037",text:"TheBalm Carrot Eye Makeup Remover"},
{id:"7038",text:"Supergoop! CC Cream Daily Correct Broad Spectrum SPF 35"},
{id:"7039",text:"The Body Shop All-In-One Blusher"},
{id:"7040",text:"TheBalm Balm Shelter Tinted Gloss SPF 17"},
{id:"7041",text:"Skyn Iceland Glacial Face Wash"},
{id:"7042",text:"Skinfix Gentle Eczema Balm"},
{id:"7043",text:"Stila Coming Clean Revitalizing Gel Cleanser"},
{id:"7044",text:"Tarte Cosmetics Smooth Operator Amazonian Clay Waterproof Concealer"},
{id:"7045",text:"The Body Shop Vitamin E Hydrating Toner"},
{id:"7046",text:"St. Ives Naturally Soothing Body Lotion, Oatmeal & Shea Butter"},
{id:"7047",text:"Three Custom Color Specialists Mini Pressed Powder Single"},
{id:"7048",text:"Shiseido Sun Protection Liquid Foundation SPF 42 PA+++"},
{id:"7049",text:"SkinCeuticals Blemish + Age Defense Salicylic Acid Acne Treatment"},
{id:"7050",text:"Tarte Cosmetics Amazonian Clay Shimmering Powder"},
{id:"7051",text:"Smashbox Halo Hydrating Perfecting Powder"},
{id:"7052",text:"Tarte Cosmetics Maracuja Miracle Foundation 12-Hour Broad Spectrum SPF 15"},
{id:"7053",text:"Shiseido The Skincare Moisture Relaxing Mask"},
{id:"7054",text:"St. Ives Exfoliating Pads Gentle Daily Exfoliator"},
{id:"7055",text:"Too Faced Lash Injection Antidote"},
{id:"7056",text:"Sonia Kashuk Dramatically Defining Long Wear Gel Liner"},
{id:"7057",text:"The Body Shop Vitamin E Sink-In Moisture Mask"},
{id:"7058",text:"Skinn by Dimitri James Dressed to the Nines 9 Color Collagen Boost Lipstick Compact"},
{id:"7059",text:"SkinCeuticals Eye Cream Firming Treatment"},
{id:"7060",text:"TheBalm Hot Mama Shadow/Blush"},
{id:"7061",text:"SkinMedica Vitamin C + E Complex Age Defense"},
{id:"7062",text:"SkinMedica TNS Body Lotion"},
{id:"7063",text:"Shiseido Benefiance NutriPerfect Night Cream"},
{id:"7064",text:"Shiseido Pureness Refreshing Cleansing Water, Oil-Free, Alcohol-Free"},
{id:"7065",text:"Sephora Jumbo Liner 12HR Wear Waterproof"},
{id:"7066",text:"Smashbox Bronze Lights"},
{id:"7067",text:"St. Tropez Wash Off Instant Glow Face"},
{id:"7068",text:"St. Ives Apricot Scrub Renew & Firm"},
{id:"7069",text:"The Body Shop Colourglide Lip Color"},
{id:"7070",text:"SkinCeuticals Physical Eye UV Defense SPF 50"},
{id:"7071",text:"Tom Ford Ultra Shine Lip Gloss"},
{id:"7072",text:"Stila Stay All Day Liquid Lipstick"},
{id:"7073",text:"Skinn by Dimitri James Skinn Vacuum Deep Pore Cleansing Mask"},
{id:"7074",text:"SkinCeuticals Retinol 1.0 Maximum Strength Refining Night Cream with 1.0% Pure Retinol"},
{id:"7075",text:"Stila Smudge Stick Waterproof Eye Liner"},
{id:"7076",text:"SK-II Facial Treatment Mask"},
{id:"7077",text:"Smashbox Single Eye Shadow"},
{id:"7078",text:"SkinCeuticals Antioxidant Lip Repair Restorative Treatment, for Damaged or Aging Lips"},
{id:"7079",text:"Simple Kind to Skin Replenishing Rich Moisturiser"},
{id:"7080",text:"TheBalm FratBoy Shadow/Blush"},
{id:"7081",text:"The Body Shop Aloe Calming Toner, for Sensitive Skin"},
{id:"7082",text:"Sephora Bronzer"},
{id:"7083",text:"Tarte Cosmetics Maracuja Blush & Glow Brightening Luminizer and Cheek Tint"},
{id:"7084",text:"Skinfix Diaper Rash Balm"},
{id:"7085",text:"Tata Harper Moisturizing Mask"},
{id:"7086",text:"The Body Shop Vitamin E Cream Cleanser"},
{id:"7087",text:"SkinCeuticals Retinol 0.5 Refining Night Cream with 0.5% Pure Retinol"},
{id:"7088",text:"Tarte Cosmetics LipSurgence Natural Matte Lip Tint"},
{id:"7089",text:"Sephora Smoothing & Brightening Concealer"},
{id:"7090",text:"Tarte Cosmetics Empowered Hybrid Gel Foundation"},
{id:"7091",text:"Suntegrity Natural Mineral Sunscreen For Body, Broad Spectrum SPF 30"},
{id:"7092",text:"Shiseido Pressed Powder"},
{id:"7093",text:"Simple Kind to Skin Soothing Facial Toner"},
{id:"7094",text:"Shiseido Bio-Performance Super Refining Essence"},
{id:"7095",text:"St. Ives Naturally Clear Green Tea Cleanser"},
{id:"7096",text:"SkinMedica Ceratopic Replenishing Cream"},
{id:"7097",text:"Supergoop! Save Face Oil-Free A.M. Moisturizer SPF 35+"},
{id:"7098",text:"Shiseido Elixir Superieur Lifting CE Lotion I"},
{id:"7099",text:"Smashbox Be Legendary Lipstick"},
{id:"7100",text:"TheBalm Whats Your Type? Tall Dark and Handsome"},
{id:"7101",text:"Supergoop! Antioxidant-Infused Sunscreen Mist with Vitamin C Broad Spectrum SPF 50"},
{id:"7102",text:"TheBalm Vanilla Oil-Absorbing Face Moisturizer"},
{id:"7103",text:"TheBalm Iris Night Face Cream"},
{id:"7104",text:"Shiseido Nourishing Mascara Base"},
{id:"7105",text:"Skinn by Dimitri James Hylighten"},
{id:"7106",text:"Stila Undercover Conditioning Toner & Moisture Prep"},
{id:"7107",text:"Stila Custom Color Blush"},
{id:"7108",text:"Stila Aqua Glow Watercolor Blush"},
{id:"7109",text:"Three Custom Color Specialists Simply Beautiful Gloss"},
{id:"7110",text:"StriVectin StriVectinLabs High Performance Booster Oil"},
{id:"7111",text:"Sephora Bright Future Gel Serum Concealer"},
{id:"7112",text:"Sephora Retractable Waterproof Eyeliner"},
{id:"7113",text:"The Body Shop For Men Maca Root Razor Relief"},
{id:"7114",text:"The Body Shop Vitamin C Microdermabrasion"},
{id:"7115",text:"Tata Harper Restorative Eye Crme"},
{id:"7116",text:"Three Custom Color Specialists Lipstick Light Brights"},
{id:"7117",text:"TheBalm Sexy Mama Anti-Shine Translucent Powder"},
{id:"7118",text:"Sephora Teint Infusion Ethereal Natural Finish Foundation"},
{id:"7119",text:"Sephora Full Action Extreme Effect Mascara"},
{id:"7120",text:"Sonia Kashuk Lashify Mascara"},
{id:"7121",text:"TheBalm Mary-Lou Manizer"},
{id:"7122",text:"StriVectin StriVectin-SD Instant Retexturizing Scrub"},
{id:"7123",text:"Sephora Perfecting Eye Primer"},
{id:"7124",text:"Tatcha GOLD Camellia Nourishing Lip Balm"},
{id:"7125",text:"Sephora Colorful Eye Shadow Palette"},
{id:"7126",text:"Sonia Kashuk Beautifying Blush"},
{id:"7127",text:"Stila Perfectly Poreless Putty Perfector"},
{id:"7128",text:"Shiseido Pureness Anti-Shine Refreshing Lotion"},
{id:"7129",text:"The Body Shop Aloe Gentle Exfoliator, for Sensitive Skin"},
{id:"7130",text:"SkinCeuticals Serum 20 AOX+"},
{id:"7131",text:"Shiseido White Lucent Anti-Dark Circles Eye Cream"},
{id:"7132",text:"SkinCeuticals Phyto Corrective Gel Calming Complexion Gel"},
{id:"7133",text:"Sephora Super Loaded Age Defy Serum"},
{id:"7134",text:"The Body Shop Loose Face Powder"},
{id:"7135",text:"The Body Shop Nutriganics Smoothing Mask"},
{id:"7136",text:"Stila Lip Glaze"},
{id:"7137",text:"The Body Shop Pomegranate Firming & Refreshing Eye Roll-On"},
{id:"7138",text:"Too Faced Starry-Eyed Liquid Eyeliner"},
{id:"7139",text:"Shiseido White Lucent Intensive Brightening Mask"},
{id:"7140",text:"The Body Shop Vitamin E Lip Care Stick SPF 15"},
{id:"7141",text:"St. Ives Naturally Indulgent Body Lotion, Coconut Milk & Orchid Extract"},
{id:"7142",text:"SkinCeuticals Equalizing Toner Pore-Refining Toner for Combination or Oily Skin"},
{id:"7143",text:"Stila Lip & Cheek Stain"},
{id:"7144",text:"SK-II Facial Treatment Clear Lotion"},
{id:"7145",text:"The Body Shop Aloe Protective Serum, for Sensitive Skin"},
{id:"7146",text:"Smashbox Be Legendary Lipstick -- Matte"},
{id:"7147",text:"Sephora Smoothing Translucent Setting Powder"},
{id:"7148",text:"Sephora Intensive Instant Moisturizer"},
{id:"7149",text:"SkinMedica Purifying Foaming Wash"},
{id:"7150",text:"Stridex Daily Care Acne Pads with Salicylic Acid, Maximum Strength"},
{id:"7151",text:"SkinMedica Purifying Toner"},
{id:"7152",text:"Sonia Kashuk All Covered Up Concealer"},
{id:"7153",text:"Shiseido Perfect Refining Foundation SPF 16"},
{id:"7154",text:"Sonia Kashuk Eye Shadow Duo"},
{id:"7155",text:"SkinMedica Ultra Sheer Moisturizer"},
{id:"7156",text:"The Body Shop For Men Maca Root Energetic Face Protector SPF 15"},
{id:"7157",text:"Shiseido Urban Environment Tinted UV Protector SPF 43"},
{id:"7158",text:"SkinMedica TNS Essential Serum"},
{id:"7159",text:"Shiseido Sun Protection Eye Cream Broad Spectrum SPF 34"},
{id:"7160",text:"Sunday Riley Effortless Breathable Tinted Primer"},
{id:"7161",text:"Sephora Lasting and Perfecting Corrector"},
{id:"7162",text:"Shiseido Elixir Superieur Lifting CE Emulsion II, for Normal Skin"},
{id:"7163",text:"SkinCeuticals Renew Overnight Dry Nighttime Skin-Refining Moisturizer, for Normal to Dry Skin"},
{id:"7164",text:"Skyn Iceland Nordic Skin Peel"},
{id:"7165",text:"Sephora Super Nourishing Lip Balm"},
{id:"7166",text:"SkinMedica Acne Treatment Lotion"},
{id:"7167",text:"StriVectin SD Advanced Intensive Concentrate for Wrinkles and Stretch Marks"},
{id:"7168",text:"The Body Shop Pomegranate Softening Cream Cleanser"},
{id:"7169",text:"Shiseido The Skincare Gentle Cleansing Cream"},
{id:"7170",text:"Sephora Express Cleansing Wipes"},
{id:"7171",text:"Shiseido Benefiance Firming Massage Mask"},
{id:"7172",text:"Three Custom Color Specialists Creme Concealer Singles"},
{id:"7173",text:"Tarte Cosmetics Mineral Powder Bronzer"},
{id:"7174",text:"Three Custom Color Specialists Brow Gel"},
{id:"7175",text:"Shiseido Bronzer"},
{id:"7176",text:"St. Tropez Applicator Mitt"},
{id:"7177",text:"Sonia Kashuk Eye Shadow Eye Couture Palette"},
{id:"7178",text:"St. Ives Daily Hydrating Body Lotion, Vitamin E"},
{id:"7179",text:"Sephora Ultra Shine Lip Gloss"},
{id:"7180",text:"Sephora Skin Perfect CC Cream Sunscreen SPF 20"},
{id:"7181",text:"Skinn by Dimitri James Patina Eyeshadows"},
{id:"7182",text:"Stila Sheer Color Tinted Moisturizer Oil-Free SPF 20"},
{id:"7183",text:"Smashbox Camera Ready BB Cream SPF 35"},
{id:"7184",text:"The Body Shop Oil-Free Foundation SPF 15"},
{id:"7185",text:"Skinn by Dimitri James Color Touch Eye, Cheek, & Lip Glow"},
{id:"7186",text:"Sonia Kashuk Hidden Agenda Concealer Palette"},
{id:"7187",text:"SkinCeuticals Metacell Renewal B3"},
{id:"7188",text:"Shiseido Perfect Mascara Full Definition"},
{id:"7189",text:"StriVectin StriVectin-TL Tightening Face Serum"},
{id:"7190",text:"The Body Shop Baked-To-Last Blush"},
{id:"7191",text:"Shiseido Wetforce Ultimate Sun Protection Cream SPF 50+ for Face"},
{id:"7192",text:"SkinCeuticals Simply Clean Pore-Refining Gel Cleanser for Combination or Oily Skin"},
{id:"7193",text:"Skinn by Dimitri James Crease Release Triple Advanced Eye Contour SerumAM & PM"},
{id:"7194",text:"St. Tropez Tan Optimiser Shower Cream"},
{id:"7195",text:"The Body Shop Nutriganics Refreshing Toner"},
{id:"7196",text:"Shiseido Radiant Lifting Foundation SPF 17"},
{id:"7197",text:"Sephora Double Contouring Cream Blush"},
{id:"7198",text:"Shiseido Pureness Refreshing Cleansing Sheets, Oil-Free, Alcohol-Free"},
{id:"7199",text:"Tom Ford Traceless Foundation SPF 15"},
{id:"7200",text:"TheBalm DownBoy Shadow/Blush"},
{id:"7201",text:"TheBalm Kiwi Facial Scrub"},
{id:"7202",text:"Smashbox Full Exposure Palette"},
{id:"7203",text:"Tarte Cosmetics Cheek Stain"},
{id:"7204",text:"Sephora Sculpting Disk"},
{id:"7205",text:"Smashbox Photo Finish More Than Primer Dark Spot Correcting Oil-Free"},
{id:"7206",text:"Simple Kind to Skin Facial Exfoliating Wipes"},
{id:"7207",text:"Sonia Kashuk Renew Micro Exfoliating Toner"},
{id:"7208",text:"Shiseido Natural Eyebrow Pencil"},
{id:"7209",text:"Shiseido Sun Protection Eye Cream SPF 32 PA+++"},
{id:"7210",text:"TheBalm Cranberry Invigorating Eye Cream"},
{id:"7211",text:"Shiseido Benefiance Protective Hand Revitalizer SPF 8"},
{id:"7212",text:"The Body Shop Brow & Liner Kit"},
{id:"7213",text:"SkinMedica TNS Line Refine"},
{id:"7214",text:"The Body Shop Lip Butter"},
{id:"7215",text:"The Body Shop Baked Eye Color"},
{id:"7216",text:"St. Ives Naturally Smooth Body Lotion, Natural Fruit AHA Complex"},
{id:"7217",text:"Smashbox Photo Angle Pure Pigment Gel Liner"},
{id:"7218",text:"Shiseido Urban Environment UV Protection Cream for Face/Body SPF 35 PA+++"},
{id:"7219",text:"Shiseido Sheer and Perfect Foundation SPF 18"},
{id:"7220",text:"The Body Shop Colourglide Shine Lip Color"},
{id:"7221",text:"The Body Shop Nutriganics Smoothing Day Cream"},
{id:"7222",text:"Shiseido Pureness Balancing Softener, Alcohol Free"},
{id:"7223",text:"Sonia Kashuk Undetectable Stick Foundation"},
{id:"7224",text:"SkinMedica Sensitive Skin Cleanser"},
{id:"7225",text:"Tom Ford Lip Color"},
{id:"7226",text:"Shiseido Bio-Performance Advanced Super Revitalizer (Cream) Whitening Formula N"},
{id:"7227",text:"Smashbox High Definition Concealer"},
{id:"7228",text:"The Body Shop Vitamin E Body Butter"},
{id:"7229",text:"Sephora Upside Down Mascara"},
{id:"7230",text:"Stila Prime Pot Waterproof Eye Shadow Primer"},
{id:"7231",text:"Stila Got Inked Cushion Eye Liner"},
{id:"7232",text:"ShiKai Borage Dry Skin Therapy Eye Cream"},
{id:"7233",text:"SkinMedica TNS Recovery Complex"},
{id:"7234",text:"Tarte Cosmetics Tarteguard 30 Sunscreen Lotion Broad Spectrum SPF 30"},
{id:"7235",text:"Skyn Iceland Anti-Blemish Gel"},
{id:"7236",text:"Skinn by Dimitri James Absolute Perfection Pro Collagen Face Brightener with Skin-Plexx 12"},
{id:"7237",text:"TheBalm White Tea Spearmint Aloe Face Mask"},
{id:"7238",text:"Shiseido White Lucent Brightening Moisturizing Emulsion"},
{id:"7239",text:"Stila Aqua Glow Serum Foundation"},
{id:"7240",text:"SkinCeuticals Renew Overnight Oily Nighttime Skin-Refining Moisturizer, for Normal or Oily Skin"},
{id:"7241",text:"Sonia Kashuk Jet Set Liquid Liner"},
{id:"7242",text:"Stila Smudge Pots"},
{id:"7243",text:"ShiKai Borage Dry Skin Therapy Foot Cream Fragrance Free"},
{id:"7244",text:"Smashbox Jet Set Waterproof Eye Liner"},
{id:"7245",text:"Sephora Glossy Gloss"},
{id:"7246",text:"Sonia Kashuk Eyeshadow Palette"},
{id:"7247",text:"Shiseido Benefiance WrinkleResist24 Night Emulsion"},
{id:"7248",text:"SkinMedica AHA/BHA Face Cream"},
{id:"7249",text:"SkinCeuticals Ultimate UV Defense Sunscreen Broad Spectrum SPF 30"},
{id:"7250",text:"Sephora Pore Refining Serum Pores Take a Backseat"},
{id:"7251",text:"Sonia Kashuk Perfecting Transparent Mattifier"},
{id:"7252",text:"Tata Harper Hydrating Flower Essence"},
{id:"7253",text:"Shiseido White Lucent Brightening Cleansing Foam"},
{id:"7254",text:"Skinn by Dimitri James Natural Light Skinn Illuminizer"},
{id:"7255",text:"Sephora 8 HR Mattifying Moisturizer Sunscreen"},
{id:"7256",text:"Skyn Iceland Icelandic Relief Eye Pen"},
{id:"7257",text:"St. Tropez Self Tan Bronzing Spray"},
{id:"7258",text:"Stila Countless Color Pigments"},
{id:"7259",text:"Smashbox Limitless Waterproof Liquid Liner Pen"},
{id:"7260",text:"The Body Shop Extra Virgin Minerals Compact Foundation"},
{id:"7261",text:"Sephora Smart Liner"},
{id:"7262",text:"SK-II Cellumination Mask-In Lotion"},
{id:"7263",text:"Smashbox Liquid Halo HD Foundation Broad Spectrum SPF 15"},
{id:"7264",text:"The Body Shop Lip Care"},
{id:"7265",text:"The Body Shop Skin Primer Moisturize It"},
{id:"7266",text:"Shiseido Future Solutions LX Ultimate Regenerating Serum"},
{id:"7267",text:"Skinn by Dimitri James Crease Release Triple Action Day CreamAM"},
{id:"7268",text:"Skinn by Dimitri James Luxe Premier Treatment Mascara"},
{id:"7269",text:"Tarte Cosmetics Amazonian Clay 12-Hour Full Coverage Foundation SPF 15"},
{id:"7270",text:"Supergoop! Everyday Sunscreen Broad Spectrum SPF 50"},
{id:"7271",text:"Skinn by Dimitri James Milky Protein Cleanser"},
{id:"7272",text:"Sephora Kohl Waterproof Eyeliner"},
{id:"7273",text:"Tarte Cosmetics Amazonian Clay Pressed Mineral Powder"},
{id:"7274",text:"The Body Shop Pomegranate Firming Night Cream"},
{id:"7275",text:"Shiseido White Lucent Brightening Balancing Softener Enriched W"},
{id:"7276",text:"The Body Shop Vitamin E Overnight Serum-in-Oil"},
{id:"7277",text:"The Body Shop Extra Virgin Minerals Powder Foundation"},
{id:"7278",text:"Stila Baked Eye Shadow Trio"},
{id:"7279",text:"Tarte Cosmetics Lights, Camera, Splashes! 4-in-1 Waterproof Mascara"},
{id:"7280",text:"The Body Shop Super Volume Mascara"},
{id:"7281",text:"Skyn Iceland Icelandic Relief Eye Cream"},
{id:"7282",text:"The Body Shop Nutriganics Smoothing Night Cream"},
{id:"7283",text:"StriVectin StriVectin-TL 360 Tightening Eye Serum"},
{id:"7284",text:"SkinMedica Redness Relief CalmPlex"},
{id:"7285",text:"SkinMedica Rejuvenative Moisturizer"},
{id:"7286",text:"Smashbox Artificial Light Luminizing Powder"},
{id:"7287",text:"SkinCeuticals AOX+ Eye Gel"},
{id:"7288",text:"The Body Shop Camomile Sumptuous Cleansing Butter"},
{id:"7289",text:"Tarte Cosmetics emphasEYES Inner Rim Brightener"},
{id:"7290",text:"Sephora Deluxe Antibacterial Brush Set"},
{id:"7291",text:"The Body Shop Pomegranate Firming Serum"},
{id:"7292",text:"Tarte Cosmetics emphasEYES Amazonian Clay Waterproof Liner"},
{id:"7293",text:"ShiKai Borage Dry Skin Therapy Lotion"},
{id:"7294",text:"Stila Glamoureyes Mascara"},
{id:"7295",text:"St. Ives Collagen Elastin Facial Moisturizer"},
{id:"7296",text:"St. Ives Apricot Scrub Blemish & Blackhead Control"},
{id:"7297",text:"The Body Shop Extra Virgin Minerals Liquid Foundation"},
{id:"7298",text:"SkinCeuticals Purifying Cleanser"},
{id:"7299",text:"Too Faced Sun Bunny Bronzer"},
{id:"7300",text:"Skinn by Dimitri James Lip Rejen Lip & Laugh Line Smoothing Cream"},
{id:"7301",text:"Shiseido Perfect Smoothing Compact Foundation SPF 15"},
{id:"7302",text:"Sephora Acne-Fighting Mattifying Moisturizer"},
{id:"7303",text:"Three Custom Color Specialists Lip Color Palettes"},
{id:"7304",text:"Stila Stay All Day 10-in-1 HD Beauty Balm"},
{id:"7305",text:"Simple Kind to Skin Cleansing Facial Wipes"},
{id:"7306",text:"Sunday Riley Bionic Anti-Aging Cream"},
{id:"7307",text:"Shiseido Luminizing Satin Face Color"},
{id:"7308",text:"Sonia Kashuk Resurface Gentle Exfoliating Wash"},
{id:"7309",text:"Smashbox Artificial Light Luminizing Lotion"},
{id:"7310",text:"Shiseido Ultimate Cleansing Oil, for Face & Body"},
{id:"7311",text:"Shiseido Sun Protection Compact Foundation for Face SPF 36"},
{id:"7312",text:"Skinfix Gentle Hair & Body Wash"},
{id:"7313",text:"Tatcha POLISHED Classic Rice Enzyme Powder"},
{id:"7314",text:"Sonia Kashuk Dramatically Brightening Kajal"},
{id:"7315",text:"Stila Brighten &  Correct Concealer"},
{id:"7316",text:"Sephora Tinted Self-Tanning Face Gel"},
{id:"7317",text:"Shiseido Luminizing Lip Gloss"},
{id:"7318",text:"Tarte Cosmetics Tarte Brazilliance Self-Tanner & Application Mitt"},
{id:"7319",text:"Stila One Step Bronze"},
{id:"7320",text:"Sonia Kashuk Dewy Luxe Lip & Cheek Balm"},
{id:"7321",text:"Smashbox Brow Tech To Go"},
{id:"7322",text:"TheBalm Instain Long-Wearing Powder Staining Blush"},
{id:"7323",text:"Sephora Purifying Cleansing Gel"},
{id:"7324",text:"Tatcha RADIANT Deep Brightening Serum"},
{id:"7325",text:"Tom Ford Lip Lacquer"},
{id:"7326",text:"Sephora Clarifying Booster"},
{id:"7327",text:"Tarte Cosmetics The Bright Side Double-Ended Eye Brightener"},
{id:"7328",text:"The Body Shop Pomegranate Invigorating Toner"},
{id:"7329",text:"Stila In The Know Eye Shadow Palette"},
{id:"7330",text:"The Body Shop For Men Maca Root Face Wash"},
{id:"7331",text:"TheBalm Olive Age Defense Face Cream"},
{id:"7332",text:"Smashbox O-Glow"},
{id:"7333",text:"Skyn Iceland Angelica Line Smoother With Natural GABA Complex"},
{id:"7334",text:"St. Tropez Everyday Gradual Tan for Face"},
{id:"7335",text:"Shiseido IBUKI Eye Correcting Cream"},
{id:"7336",text:"Tata Harper Elixir Vitae"},
{id:"7337",text:"Shiseido White Lucent Brightening Moisturizing Gel"},
{id:"7338",text:"The Body Shop Nutriganics Drops of Youth"},
{id:"7339",text:"Skinfix Body Repair Balm"},
{id:"7340",text:"Spectro Spectro HydraCare Facial Moisturizer, Blemish-Prone Skin"},
{id:"7341",text:"Skinn by Dimitri James Luxe Premier Fiber Optic Mascara"},
{id:"7342",text:"Shiseido Shimmering Rouge"},
{id:"7343",text:"Tarte Cosmetics Lights Camera Lashes Precision Longwear Eyeliner"},
{id:"7344",text:"Sephora Dark Spot Minimizing Serum"},
{id:"7345",text:"Skinn by Dimitri James Sheer Effects Lip Gloss"},
{id:"7346",text:"Sephora Waterproof Eye Makeup Remover"},
{id:"7347",text:"Stila Perfect & Correct Foundation"},
{id:"7348",text:"Shiseido Sheer and Perfect Compact Foundation SPF 21"},
{id:"7349",text:"The Body Shop Moisture Foundation SPF 15"},
{id:"7350",text:"Skinfix Rash Repair Balm"},
{id:"7351",text:"Skinn by Dimitri James Automatic Bronzing Powder Brush"},
{id:"7352",text:"Stila Lash Stunner Extreme Lash Primer"},
{id:"7353",text:"St. Ives Skin Renewing Body Lotion, Collagen Elastin"},
{id:"7354",text:"The Body Shop Tea Tree Oil"},
{id:"7355",text:"TheBalm Rose Face Cleanser Normal to Oily Skin"},
{id:"7356",text:"St. Ives Deep Restoring Body Lotion, 24 Hour Moisture"},
{id:"7357",text:"The Body Shop Lip Liner"},
{id:"7358",text:"Stila Convertible Eye Color"},
{id:"7359",text:"Sephora Tinted Self-Tanning Body Mist"},
{id:"7360",text:"SK-II Skin Signature Eye Cream"},
{id:"7361",text:"TheBalm Cindy-Lou Manizer"},
{id:"7362",text:"TheBalm Peppermint Hydrating Face Moisturizer"},
{id:"7363",text:"StriVectin Clinical Corrector Anti-Aging Lip Tint SPF 20"},
{id:"7364",text:"Sephora Nano Lip Liner"},
{id:"7365",text:"Tom Ford Bronzing Primer"},
{id:"7366",text:"Tarte Cosmetics Colored Clay CC Primer"},
{id:"7367",text:"Tarte Cosmetics Glamazon Pure Performance 12-Hour Lipstick"},
{id:"7368",text:"StriVectin StriVectin-SD Volumizing Hand Cream"},
{id:"7369",text:"SkinMedica Retinol Complex 0.5"},
{id:"7370",text:"Smashbox Full Exposure Mascara"},
{id:"7371",text:"SkinCeuticals Retexturing Activator Bi-Functional Resurfacing and Replenishing Serum"},
{id:"7372",text:"The Body Shop Seaweed Clarifying Toner, for Combination/Oily Skin"},
{id:"7373",text:"Three Custom Color Specialists Classic Lip Gloss"},
{id:"7374",text:"Shiseido Perfect Hydrating BB Cream Broad Spectrum SPF 35"},
{id:"7375",text:"Sephora Baked Moonshadow Trio"},
{id:"7376",text:"Sephora Colorful Duo Eyeshadow"},
{id:"7377",text:"Smashbox The Nude Lip Liner"},
{id:"7378",text:"Tarte Cosmetics Eye Couture Day-to-Night Eyeshadow Palette"},
{id:"7379",text:"The Body Shop Tea Tree Skin Clearing Foaming Cleanser"},
{id:"7380",text:"Shiseido The Skincare Rinse-Off Cleansing Gel"},
{id:"7381",text:"Supergoop! Gradual Self-Tanning Mousse Broad Spectrum SPF 20"},
{id:"7382",text:"Shiseido Shimmering Cream Eye Color"},
{id:"7383",text:"Stila Long Wear Lip Color"},
{id:"7384",text:"Shiseido White Lucent Intensive Spot Targeting Serum"},
{id:"7385",text:"The Body Shop Colour Crush Eye Shadow"},
{id:"7386",text:"Smashbox Be Legendary Long Wear Lip Lacquer"},
{id:"7387",text:"Shiseido White Lucent Brightening Massage Cream N"},
{id:"7388",text:"Skinfix Healing Foot & Leg Balm"},
{id:"7389",text:"Shiseido The Skincare Night Moisture Recharge, Enriched"},
{id:"7390",text:"Shiseido The Skincare Night Moisture Recharge, Light"},
{id:"7391",text:"TheBalm Put A Lid On It"},
{id:"7392",text:"Tarte Cosmetics LipSurgence Natural Lip Luster Lip Tint"},
{id:"7393",text:"Shiseido Translucent Pressed Powder"},
{id:"7394",text:"St. Ives Apricot Scrub Invigorating, for All Skin Types"},
{id:"7395",text:"Sephora Sun Disk"},
{id:"7396",text:"SK-II Facial Treatment Cleansing Oil"},
{id:"7397",text:"TheBalm Rose Face Cleanser Normal to Dry Skin"},
{id:"7398",text:"SkinCeuticals Physical Matte UV Defense Sunscreen Broad Spectrum SPF 50"},
{id:"7399",text:"Smashbox Soft Lights"},
{id:"7400",text:"Sephora Ultra Vinyl Lip Pencil"},
{id:"7401",text:"Shiseido Sun Protection Lip Treatment SPF 36 PA++"},
{id:"7402",text:"SkinMedica Uplifting Eye Serum"},
{id:"7403",text:"Suntegrity Natural Moisturizing Face Sunscreen & Primer, Broad Spectrum SPF 30"},
{id:"7404",text:"Shiseido Pureness Deep Cleansing Foam"},
{id:"7405",text:"Shiseido The Skincare Purifying Cleansing Foam"},
{id:"7406",text:"Shiseido The Skincare Night Moisture Recharge, Regular"},
{id:"7407",text:"StriVectin StriVectin-AR Advanced Retinol Concentrated Serum"},
{id:"7408",text:"StriVectin StriVectin-AR Advanced Retinol Day Treatment SPF 30"},
{id:"7409",text:"Smashbox Photo Filter Powder Foundation"},
{id:"7410",text:"Smashbox #Shapematters Palette"},
{id:"7411",text:"Tarte Cosmetics Pure Maracuja Oil"},
{id:"7412",text:"Shiseido Benefiance Creamy Cleansing Emulsion"},
{id:"7413",text:"Sonia Kashuk Illuminating Bronzer"},
{id:"7414",text:"The Body Shop Blue Corn 3 in 1 Deep Cleansing Scrub Mask, for Normal/Oily Skin"},
{id:"7415",text:"The Body Shop All in One Face Base"},
{id:"7416",text:"SkinCeuticals Neck, Chest & Hand Repair"},
{id:"7417",text:"Sephora Instant Eye Makeup Remover"},
{id:"7418",text:"Supergoop! SPF 30+ City Sunscreen Serum"},
{id:"7419",text:"St. Tropez Self Tan Bronzing Lotion"},
{id:"7420",text:"Stila Stay All Day Waterproof Liquid Eye Liner"},
{id:"7421",text:"Shiseido Benefiance Extra Creamy Cleansing Foam"},
{id:"7422",text:"Shiseido Brilliant Bronze Quick Self-Tanning Gel, for Face/Body"},
{id:"7423",text:"Sonia Kashuk Matte, Oil Blotting Papers"},
{id:"7424",text:"The Body Shop Pomegranate Firming Day Lotion SPF 15"},
{id:"7425",text:"Tom Ford Shade & Illuminate"},
{id:"7426",text:"Three Custom Color Specialists Lip Liner"},
{id:"7427",text:"St. Tropez Wash Off Instant Glow Shimmer Stick"},
{id:"7428",text:"Shiseido Pureness Foaming Cleansing Fluid"},
{id:"7429",text:"Sephora Rouge Cream Lipstick"},
{id:"7430",text:"Stila Mile High Lashes Mascara"},
{id:"7431",text:"SkinMedica TNS  Eye Repair"},
{id:"7432",text:"Shiseido Natural Finish Cream Concealer"},
{id:"7433",text:"Skinn by Dimitri James Nutri-Lift Night Time Nourishing FluidPM"},
{id:"7434",text:"SkinCeuticals Eye Balm Rehabilitative Emollient, for Aging Skin"},
{id:"7435",text:"Skinfix Hand Repair Cream"},
{id:"7436",text:"Sephora Outrageous Volume Mascara"},
{id:"7437",text:"The Body Shop Tea Tree Face Mask"},
{id:"7438",text:"Shiseido Perfect Rouge Lipstick"},
{id:"7439",text:"StriVectin StriVectinLabs Anti-Wrinkle Hydra Gel Treatment"},
{id:"7440",text:"SkinCeuticals Redness Neutralizer"},
{id:"7441",text:"St. Tropez Tan Detox"},
{id:"7442",text:"StriVectin Present Perfect Antioxidant Defense Lotion"},
{id:"7443",text:"Tarte Cosmetics BB Tinted Treatment 12-Hour Primer Broad Spectrum SPF 30"},
{id:"7444",text:"Sephora Age Defy Eye Cream"},
{id:"7445",text:"Shiseido Pureness Blemish Clearing Gel"},
{id:"7446",text:"StriVectin StriVectin-AR Advanced Retinol Night Treatment"},
{id:"7447",text:"Stila Convertible Color"},
{id:"7448",text:"Sephora CC Eye Cream + Concealer"},
{id:"7449",text:"Three Custom Color Specialists Shimmering Lights Lipstick"},
{id:"7450",text:"Tom Ford Extreme Mascara"},
{id:"7451",text:"The Body Shop Vitamin C Energizing Face Spritz"},
{id:"7452",text:"Tarte Cosmetics Maracuja C-Brighter Eye Treatment"},
{id:"7453",text:"SK-II LXP Ultimate Serum"},
{id:"7454",text:"The Body Shop For Men Maca Root Shave Cream"},
{id:"7455",text:"Shiseido Benefiance Full Correction Lip Treatment"},
{id:"7456",text:"Tatcha INDIGO Soothing Silk Body Butter"},
{id:"7457",text:"Tatcha AGELESS Revitalizing Eye Cream"},
{id:"7458",text:"Supergoop! Advanced Anti-Aging Eye Cream Broad Spectrum Sunscreen SPF 37"},
{id:"7459",text:"Skyn Iceland Pure Cloud Cleanser"},
{id:"7460",text:"Smashbox Brow Tech Wax"},
{id:"7461",text:"Stila Illuminating Powder Foundation"},
{id:"7462",text:"Sonia Kashuk Extreme Wear Eye Primer"},
{id:"7463",text:"The Body Shop Vitamin C Facial Radiance Capsules"},
{id:"7464",text:"Skinn by Dimitri James Mineral Eye Lighter"},
{id:"7465",text:"Sonia Kashuk Eye Shadow Quad"},
{id:"7466",text:"Shiseido The Skincare Eye Moisture Recharge"},
{id:"7467",text:"Three Custom Color Specialists Loose Powder"},
{id:"7468",text:"The Body Shop Vitamin E Moisture Serum"},
{id:"7469",text:"Tarte Cosmetics Amazonian Clay 12-Hour Waterproof Concealer"},
{id:"7470",text:"SkinCeuticals Body Retexturing Treatment"},
{id:"7471",text:"Spectro Spectro Derm for Dry, Sensitive SkinFragrance Free"},
{id:"7472",text:"Skinn by Dimitri James Eye Definition Line & Shade Wheel"},
{id:"7473",text:"Smashbox 24 Hour Photo Finish Shadow Primer"},
{id:"7474",text:"Stila Illuminating Tinted Moisturizer Oil-Free SPF 20"},
{id:"7475",text:"Tarte Cosmetics Gifted Amazonian Clay Smart Mascara"},
{id:"7476",text:"Skinn by Dimitri James Nutri-Lift Extreme SerumAM"},
{id:"7477",text:"SkinMedica LYTERA Skin Brightening Complex"},
{id:"7478",text:"Skinn by Dimitri James Lip 3X Volumizing SerumAM & PM"},
{id:"7479",text:"Shiseido Automatic Fine Eyeliner"},
{id:"7480",text:"Tarte Cosmetics Smooth Operator Amazonian Clay Finishing Powder"},
{id:"7481",text:"Tom Ford Eye Defining Pencil"},
{id:"7482",text:"TheBalm Dandelion Skin Brightening Moisturizer"},
{id:"7483",text:"The Body Shop Pomegranate Firming Day Cream"},
{id:"7484",text:"SkinCeuticals Clarifying Clay Masque Deep Pore Cleansing Skin-Refining Masque"},
{id:"7485",text:"The Body Shop Tea Tree Skin Clearing Toner"},
{id:"7486",text:"Tom Ford Cheek Color"},
{id:"7487",text:"Shiseido The Skincare Multi-Energizing Cream"},
{id:"7488",text:"Tarte Cosmetics Lights, Camera, Lashes! 4-in-1 Natural Mascara"},
{id:"7489",text:"Shiseido Eudermine Revitalizing Essence"},
{id:"7490",text:"Stila Forever Your Curl Curl Memory Mascara"},
{id:"7491",text:"The Body Shop Tea Tree Skin Clearing Facial Wash"},
{id:"7492",text:"SkinCeuticals Micro-Exfoliating Scrub"},
{id:"7493",text:"The Body Shop Baked-To-Last Bronzer"},
{id:"7494",text:"Too Faced Lip of Luxury"},
{id:"7495",text:"Tatcha AGELESS Enriching Renewal Cream"},
{id:"7496",text:"Smashbox Photo Set Pressed Powder"},
{id:"7497",text:"Shiseido The Skincare Instant Eye and Lip Makeup Remover"},
{id:"7498",text:"Supergoop! Skin Soothing Mineral Sunscreen Broad Spectrum SPF 40"},
{id:"7499",text:"Three Custom Color Specialists Opaline Cream Eye Definer"},
{id:"7500",text:"Stila Silk Shimmer Gloss"},
{id:"7501",text:"Shiseido Benefiance Pure Retinol Instant Treatment Eye Mask"},
{id:"7502",text:"StriVectin StriVectin-AR Advanced Retinol Eye Treatment"},
{id:"7503",text:"SkinCeuticals Serum 10 AOX+"},
{id:"7504",text:"Sonia Kashuk Liquid Light Concealer"},
{id:"7505",text:"Three Custom Color Specialists Watercolours for Cheeks"},
{id:"7506",text:"Tatcha PETAL FRESH Original Aburatorigami"},
{id:"7507",text:"Stila MAJOR Major Lash Mascara"},
{id:"7508",text:"Skinn by Dimitri James Pure Energy Replenishing Mineral Bath Toner"},
{id:"7509",text:"SkinCeuticals Hydra Balm"},
{id:"7510",text:"Smashbox Studio Skin 15 Hour Wear Foundation"},
{id:"7511",text:"Three Custom Color Specialists Simply Beautiful Lipstick"},
{id:"7512",text:"Tatcha GOLD Camellia Beauty Oil"},
{id:"7513",text:"Sonia Kashuk Lip Definer"},
{id:"7514",text:"SkinCeuticals C E Ferulic Combination Antioxidant Treatment"},
{id:"7515",text:"The Body Shop Camomile Waterproof Eye Make-Up Remover"},
{id:"7516",text:"Shiseido Benefiance Wrinkle Lifting Concentrate"},
{id:"7517",text:"Tom Ford Translucent Finishing Powder"},
{id:"7518",text:"Skinn by Dimitri James Blush & Blink Compact"},
{id:"7519",text:"St. Ives Naturally Clear Green Tea Scrub"},
{id:"7520",text:"Supergoop! Everyday Sunscreen Broad Spectrum SPF 30"},
{id:"7521",text:"Tatcha INDIGO Soothing Renewal Treatment"},
{id:"7522",text:"Spectro Spectro Jel for Sensitive, Blemish Prone SkinSoap & Fragrance Free"},
{id:"7523",text:"Sonia Kashuk Satin Luxe Lip Color SPF 16 Sunscreen"},
{id:"7524",text:"Skinn by Dimitri James Sulfate Free Rapid Exfoliating CleanserAM"},
{id:"7525",text:"SK-II Skin Refining Treatment"},
{id:"7526",text:"SkinMedica TNS Ultimate Daily Moisturizer + SPF 20"},
{id:"7527",text:"Smashbox Camera Ready BB Water SPF 30"},
{id:"7528",text:"Skinn by Dimitri James Collagenesis 24 Hour Youth Preservation System"},
{id:"7529",text:"Shiseido The Skincare Hydro-Nourishing Softener"},
{id:"7530",text:"Sonia Kashuk Remove Eye Makeup Remover"},
{id:"7531",text:"Shiseido Accentuating Cream Eyeliner"},
{id:"7532",text:"Sephora Ultimate Moisture Serum"},
{id:"7533",text:"Skinfix Daily Lotion"},
{id:"7534",text:"Smashbox L.A. Lights Blendable Lip & Cheek Color"},
{id:"7535",text:"SkinMedica Skin Polisher"},
{id:"7536",text:"Tata Harper Beautifying Face Oil"},
{id:"7537",text:"Shiseido Benefiance WrinkleResist24 Intensive Eye Contour Cream"},
{id:"7538",text:"Sonia Kashuk Shimmering Loose Mineral Bronzer"},
{id:"7539",text:"Shiseido Sun Protection Stick Foundation SPF 35 PA++"},
{id:"7540",text:"Skinn by Dimitri James Twin Set Collagen Boost Lipstick and Wet Lip Gloss"},
{id:"7541",text:"Sephora Rouge Shine Lipstick"},
{id:"7542",text:"Supergoop! Sun-Defying Sunscreen Oil Broad Spectrum SPF 50"},
{id:"7543",text:"Smashbox High Definition Healthy F/X Foundation SPF 15"},
{id:"7544",text:"Shiseido Smoothing Lip Pencil"},
{id:"7545",text:"Stila All Over Shimmer Liquid Luminizer"},
{id:"7546",text:"Tarte Cosmetics Clean Slate Timeless Smoothing Primer"},
{id:"7547",text:"SkinMedica Rejuvenative Toner"},
{id:"7548",text:"Shiseido The Skincare Extra Gentle Cleansing Foam"},
{id:"7549",text:"Stila Illuminating Liquid Foundation"},
{id:"7550",text:"Stila Perfecting Concealer"},
{id:"7551",text:"TheBalm Geranium Face Toner"},
{id:"7552",text:"TheBalm Alcohol-Free Face Toner"},
{id:"7553",text:"StriVectin StriVectin-EV Get Even Brightening Serum"},
{id:"7554",text:"Shiseido IBUKI Refining Moisturizer"},
{id:"7555",text:"Tom Ford Traceless Foundation Stick"},
{id:"7556",text:"Smashbox Brow Tech Matte Pencil"},
{id:"7557",text:"TheBalm Coconut Milk Cleansing Face Cream"},
{id:"7558",text:"Stila Hydrating Finishing Powder"},
{id:"7559",text:"Shiseido Refining Makeup Primer SPF 21"},
{id:"7560",text:"Tatcha LUMINOUS Deep Hydration Revitalizing Eye Mask"},
{id:"7561",text:"Shiseido Refreshing Sun Protection Spray SPF 16 PA+, for Body/Hair"},
{id:"7562",text:"Smashbox O-Plump"},
{id:"7563",text:"Tata Harper Replenishing Nutrient Complex"},
{id:"7564",text:"TheBalm Hazelnut Vitamin E Face Cream"},
{id:"7565",text:"Simple Micellar Cleansing Water"},
{id:"7566",text:"TheBalm Cucumber Face Toner"},
{id:"7567",text:"Shiseido Pureness Oil-Control Blotting Paper"},
{id:"7568",text:"Shiseido Bio-Performance Super Restoring Cream"},
{id:"7569",text:"Three Custom Color Specialists Eye Pencil"},
{id:"7570",text:"Three Custom Color Specialists Creme to Powder Blush"},
{id:"7571",text:"Shiseido Pore Smoothing Corrector"},
{id:"7572",text:"The Body Shop Nutriganics Foaming Facial Wash"},
{id:"7573",text:"Shiseido Benefiance Concentrated Neck Contour Treatment"},
{id:"7574",text:"Smashbox Blush Rush"},
{id:"7575",text:"SkinMedica Dermal Repair Cream"},
{id:"7576",text:"The Body Shop Vitamin E Gentle Cleansing Wipes"},
{id:"7577",text:"Sephora Flawless Moisturizing Lotion SPF 15"},
{id:"7578",text:"Shiseido Bio-Performance Super Corrective Eye Cream"},
{id:"7579",text:"Shiseido IBUKI Purifying Cleanser"},
{id:"7580",text:"Simple Micellar Makeup Remover Wipes"},
{id:"7581",text:"Tatcha LUMINOUS Deep Hydration Firming Serum"},
{id:"7582",text:"SkinCeuticals Phyto + Botanical Gel, for Hyperpigmentation"},
{id:"7583",text:"SkinCeuticals Sheer Physical UV Defense SPF 50"},
{id:"7584",text:"Shiseido Pureness Moisturizing Gel-Cream"},
{id:"7585",text:"St. Tropez Tan Optimiser Soft Mitt"},
{id:"7586",text:"SkinCeuticals Epidermal Repair"},
{id:"7587",text:"Smashbox Photo Finish Color Correcting Foundation Primer"},
{id:"7588",text:"The Body Shop Aloe Soothing Night Cream, for Sensitive Skin"},
{id:"7589",text:"Shiseido Foundation Brush"},
{id:"7590",text:"Shiseido Wetforce Ultimate Sun Protection Lotion SPF 50+ for Face/Body"},
{id:"7591",text:"Sonia Kashuk Eye Definer"},
{id:"7592",text:"Shiseido Bio-Performance Advanced Super Revitalizing Cream"},
{id:"7593",text:"Too Faced Brow-nie Eye Brow Pencil"},
{id:"7594",text:"Sonia Kashuk Sunless Tan Body Tanning Towelettes"},
{id:"7595",text:"Skinn by Dimitri James Collagenesis 24 Hour Youth Preservation Cream"},
{id:"7596",text:"Tarte Cosmetics LipSurgence Power Pigment"},
{id:"7597",text:"Shiseido Benefiance WrinkleResist24 Day Emulsion SPF 15"},
{id:"7598",text:"Shiseido Stick Foundation SPF 15-18"},
{id:"7599",text:"The Body Shop Aloe Eye Defense"},
{id:"7600",text:"Sephora Instant Moisturizer"},
{id:"7601",text:"Sephora Rouge Infusion Lip Ink"},
{id:"7602",text:"SK-II Whitening Source Derm Revival Mask"},
{id:"7603",text:"Skinfix Gentle Lotion"},
{id:"7604",text:"Sephora Colorful Blush"},
{id:"7605",text:"Shiseido Sheer Mattifying Compact SPF 22"},
{id:"7606",text:"Shiseido Future Solution LX Extra Rich Cleansing Foam"},
{id:"7607",text:"Smashbox Cream Eye Liner"},
{id:"7608",text:"Stila Sparkle Waterproof Liquid Eye Liner"},
{id:"7609",text:"SkinCeuticals Serum 15 AOX+"},
{id:"7610",text:"Sonia Kashuk Undetectable Loose Powder Colorless"},
{id:"7611",text:"Skinn by Dimitri James Luxe Waterproof Eyeliner"},
{id:"7612",text:"St. Tropez Tan Correcting Cleansing Wipes"},
{id:"7613",text:"TheBalm How Bout Them Apples? Lip and Cheek Cream Palette"},
{id:"7614",text:"Shiseido Perfect Rouge Tender Sheer Lip Color"},
{id:"7615",text:"Smashbox Photo Finish Targeted Pore & Line Primer"},
{id:"7616",text:"TheBalm Juniper Oil-Free Face Moisturizer"},
{id:"7617",text:"Stila Ready For Take Off Moisturizing Cleansing Cloths"},
{id:"7618",text:"The Body Shop Nutriganics Softening Cleansing Gel"},
{id:"7619",text:"The Body Shop Nutriganics Smoothing Eye Cream"},
{id:"7620",text:"Shiseido Perfect Mascara Defining Volume"},
{id:"7621",text:"The Body Shop Aloe Body Butter"},
{id:"7622",text:"Shiseido Urban Environment Oil-Free UV Protector for Face SPF 42 PA+++"},
{id:"7623",text:"SkinCeuticals Resveratrol B E"},
{id:"7624",text:"Tata Harper Refreshing Cleanser"},
{id:"7625",text:"TheBalm Betty-Lou Manizer"},
{id:"7626",text:"Skyn Iceland Hydro Cool Firming Eye Gels"},
{id:"7627",text:"Shiseido Benefiance WrinkleResist24 Balancing Softener"},
{id:"7628",text:"Shiseido Revitalizing Cream"},
{id:"7629",text:"TheBalm Apple AHA Daily Face Moisturizer"},
{id:"7630",text:"Skinn by Dimitri James Orchid Gel Tint Color Correcting Mattefying Day Primer"},
{id:"7631",text:"SkinCeuticals Phloretin CF Gel"},
{id:"7632",text:"Shiseido Ultimate Sun Protection SPF 50 Sunscreen + WetForce for Sensitive Skin & Children"},
{id:"7633",text:"The Body Shop Lip Scuff"},
{id:"7634",text:"Sephora Contour Matte Gel Eyeliner Waterproof"},
{id:"7635",text:"Three Custom Color Specialists Lipstick"},
{id:"7636",text:"Tatcha LUMINOUS Dewy Skin Mist"},
{id:"7637",text:"Sephora Sephora Collection CC Care + Color SPF 20"},
{id:"7638",text:"Sephora Instant Depuffing Roll-On Gel"},
{id:"7639",text:"Sunday Riley Ceramic Slip Cleanser"},
{id:"7640",text:"The Body Shop Hi-Shine Lip Treatment"},
{id:"7641",text:"Sonia Kashuk Brightening Powder"},
{id:"7642",text:"TheBalm CabanaBoy Shadow/Blush"},
{id:"7643",text:"SK-II Signs Up-Lifter"},
{id:"7644",text:"Shiseido Ultimune Power Infusing Concentrate"},
{id:"7645",text:"TheBalm Liquid timeBalm Spot Concealer"},
{id:"7646",text:"Shiseido Future Solution LX Concentrated Balancing Softener"},
{id:"7647",text:"Smashbox Photo Finish Foundation Primer Pore Minimizing"},
{id:"7648",text:"Smashbox Photo Finish Foundation Primer"},
{id:"7649",text:"SK-II Pitera Mid-Day Essence"},
{id:"7650",text:"Skinn by Dimitri James Repair C Plus Copper and Zinc Vitamin Concentrate AM & PM"},
{id:"7651",text:"SK-II Facial Lift Emulsion"},
{id:"7652",text:"Stila Long Wear Lip Liner"},
{id:"7653",text:"Stila Stila Sun Bronzer"},
{id:"7654",text:"Tarte Cosmetics Tarteist Clay Paint Liner"},
{id:"7655",text:"SkinMedica Retinol Complex .25"},
{id:"7656",text:"Shiseido Pureness Pore Purifying Warming Scrub"},
{id:"7657",text:"Skinn by Dimitri James Olive & Enzyme Rich Balm Cleanser and Deep Cleansing MaskPM"},
{id:"7658",text:"Sephora Perfection Mist Airbrush Foundation"},
{id:"7659",text:"Stila Sheer Pressed Powder"},
{id:"7660",text:"Skyn Iceland Arctic Face Mist"},
{id:"7661",text:"Three Custom Color Specialists Simply Beautiful Sheer Lipstick"},
{id:"7662",text:"TheBalm Balm Jovi Face Palette"},
{id:"7663",text:"TheBalm Whats Your Type? Body Builder"},
{id:"7664",text:"SK-II Facial Treatment Essence"},
{id:"7665",text:"Sonia Kashuk Detox Purifying Black Mask"},
{id:"7666",text:"The Body Shop Concealer Pencil"},
{id:"7667",text:"Three Custom Color Specialists Lip Gloss Wand"},
{id:"7668",text:"Smashbox Insta-Matte Lipstick Transformer"},
{id:"7669",text:"Smashbox Camera Ready CC Cream Broad Spectrum SPF 30 Dark Spot Correcting"},
{id:"7670",text:"Sephora Professional Clear Defining Mascara"},
{id:"7671",text:"Smashbox Sheer Focus Tinted Moisturizer SPF 15"},
{id:"7672",text:"Skinn by Dimitri James Sweet & Smooth"},
{id:"7673",text:"Stila One Step Correct"},
{id:"7674",text:"Skinn by Dimitri James Wrinkle Soak Age-Defying Hydrating Mask"},
{id:"7675",text:"Stila Glitter Eye Liner"},
{id:"7676",text:"Sephora Lash Stretcher Mascara"},
{id:"7677",text:"The Body Shop Seaweed Clarifying Night Treatment, for Combination/Oily Skin"},
{id:"7678",text:"The Body Shop Vitamin E Cream Exfoliator"},
{id:"7679",text:"Shiseido Bio-Performance Super Corrective Serum"},
{id:"7680",text:"Tarte Cosmetics Tarteist Lip Paint"},
{id:"7681",text:"Tom Ford Noir Absolute for Eyes"},
{id:"7682",text:"Shiseido Future Solution LX Eye and Lip Contour Regenerating Cream"},
{id:"7683",text:"Tatcha PURE One Step Camellia Cleansing Oil"},
{id:"7684",text:"Tarte Cosmetics Amazonian Clay Waterproof Brow Mousse"},
{id:"7685",text:"Tata Harper Regenerating Cleanser"},
{id:"7686",text:"Sephora Microsmooth Bronzer Duo"},
{id:"7687",text:"SkinCeuticals Advanced Pigment Corrector"},
{id:"7688",text:"Smashbox Photo Finish Primer Water"},
{id:"7689",text:"SkinCeuticals LHA Cleansing Gel"},
{id:"7690",text:"TheBalm Almond Microdermabrasion Face Exfoliating Scrub"},
{id:"7691",text:"The Body Shop Aloe Protective Restoring Mask, for Sensitive Skin"},
{id:"7692",text:"Smashbox Brow Tech Shaping Powder"},
{id:"7693",text:"The Body Shop Vitamin E Moisture Cream"},
{id:"7694",text:"Sonia Kashuk Dramatically Intensifying Kajal"},
{id:"7695",text:"Stila Bronzing Tinted Moisturizer SPF 20"},
{id:"7696",text:"The Body Shop Vitamin E Eye Cream"},
{id:"7697",text:"Sunday Riley Artemis Hydroactive Cellular Face Oil"},
{id:"7698",text:"Simple Kind to Skin Moisturising Facial Wash"},
{id:"7699",text:"SkinCeuticals Hydrating B5 Masque"},
{id:"7700",text:"The Body Shop Vitamin C Skin Boost"},
{id:"7701",text:"Too Faced Liquif-Eye"},
{id:"7702",text:"Sephora Perfecting Cover Concealer"},
{id:"7703",text:"SK-II Signs Eye Mask"},
{id:"7704",text:"St. Tropez Tan Optimiser Body Butter"},
{id:"7705",text:"TheBalm Plump Your Pucker"},
{id:"7706",text:"Shiseido Brilliant Bronze Tinted Self-Tanning Gel, for Face/Body"},
{id:"7707",text:"Sonia Kashuk Brow Gel"},
{id:"7708",text:"Sonia Kashuk Chic Luminosity Highlighter Stick"},
{id:"7709",text:"Spectro Spectro Jel for Combination SkinSoap & Fragrance Free"},
{id:"7710",text:"Shiseido Luminizing Satin Eye Color Trio"},
{id:"7711",text:"The Body Shop Seaweed Deep Cleansing Facial Wash, for Combination/Oily Skin"},
{id:"7712",text:"Tata Harper Repairative Moisturizer"},
{id:"7713",text:"Too Faced 3-Way Lash Lining Tool"},
{id:"7714",text:"Sonia Kashuk Shine Luxe Sheer Lip Color"},
{id:"7715",text:"TheBalm timeBalm Foundation"},
{id:"7716",text:"Tom Ford Eye Color Quad"},
{id:"7717",text:"Sonia Kashuk Shimmering Loose Mineral Blush"},
{id:"7718",text:"TheBalm NUDE tude Eyeshadow Palette"},
{id:"7719",text:"TheBalm ShadyLady Eyeshadow Palette"},
{id:"7720",text:"The Body Shop Pomegranate Softening Facial Wash"},
{id:"7721",text:"Sephora Full Action Waterproof Extreme Effect Mascara"},
{id:"7722",text:"Stila Color Balm Lipstick"},
{id:"7723",text:"Sephora Retractable Waterproof Lip Liner"},
{id:"7724",text:"Skyn Iceland Pure Cloud Cream"},
{id:"7725",text:"Shiseido Benefiance NutriPerfect Eye Serum"},
{id:"7726",text:"SK-II Facial Treatment Repair C"},
{id:"7727",text:"Smashbox Camera Ready Full Coverage Concealer"},
{id:"7728",text:"The Body Shop Vitamin E Body Lotion"},
{id:"7729",text:"Smashbox Photo Finish More Than Primer Blemish Control Salicylic Acid Acne Treatment"},
{id:"7730",text:"TheBalm Cheater! Mascara"},
{id:"7731",text:"Skinn by Dimitri James 3 Minute Lip Party"},
{id:"7732",text:"Tarte Cosmetics EmphasEYES Amazonian Clay Waterproof Brow Pencil"},
{id:"7733",text:"SkinMedica Environmental Defense Sunscreen SPF 50+"},
{id:"7734",text:"St. Ives Apricot Scrub Gentle, for Sensitive Skin"},
{id:"7735",text:"ShiKai Borage Therapy Facial Moisturizer"},
{id:"7736",text:"St. Tropez Everyday Gradual Tan Mousse"},
{id:"7737",text:"St. Ives Timeless Skin Collagen Elastin Facial Moisturizer"},
{id:"7738",text:"SkinMedica Daily Physical Defense SPF 30+"},
{id:"7739",text:"SkinCeuticals Face Cream Rehabilitating Cream, for Aging Skin"},
{id:"7740",text:"Suntegrity 5 in 1 Natural Moisturizing Face Sunscreen  Tinted, Broad Spectrum SPF 30"},
{id:"7741",text:"Shiseido Extra Smooth Sun Protection Cream SPF 38 PA+++, for Face"},
{id:"7742",text:"Stila CC Color Correcting Cream with SPF 20"},
{id:"7743",text:"SK-II Brightening Derm Moisturiser"},
{id:"7744",text:"Sephora Color Lip Last"},
{id:"7745",text:"Sephora 10 HR Wear Perfection Foundation"},
{id:"7746",text:"Skyn Iceland Glacial Cleansing Cloths"},
{id:"7747",text:"Tarte Cosmetics Clean Slate 360 Poreless 12-hr Perfecting Primer"},
{id:"7748",text:"The Body Shop Honey Bronze Bronzing Gel"},
{id:"7749",text:"Stila Hydrating Primer Oil-Free SPF 30"},
{id:"7750",text:"SK-II Whitening Source Clear Lotion"},
{id:"7751",text:"St. Tropez Everyday Gradual Tan for Body"},
{id:"7752",text:"The Body Shop Drops of Youth Eye Concentrate"},
{id:"7753",text:"TheBalm Bahama Mama Bronzer"},
{id:"7754",text:"Sephora Matifying Compact Foundation"},
{id:"7755",text:"Sephora Supreme Cleansing Foam All In One Cleanser"},
{id:"7756",text:"Smashbox Megatint Long Wear Lip Color"},
{id:"7757",text:"The Body Shop Nutriganics Smoothing Serum"},
{id:"7758",text:"Tata Harper Rebuilding Moisturizer"},
{id:"7759",text:"SkinMedica TNS LipPlump System"},
{id:"7760",text:"Skinn by Dimitri James Lash 3X Lash Base Conditioner"},
{id:"7761",text:"Smashbox L.A. Lights Blush & Highlight Palette"},
{id:"7762",text:"Stila Face The Day Multi-Use Moisturizing Gel Cream"},
{id:"7763",text:"Shiseido Extra Smooth Sun Protection Lotion for Face & Body SPF 38 PA+++"},
{id:"7764",text:"Too Faced Pink Leopard Bronzing Powder"},
{id:"7765",text:"Sunday Riley Start Over Active Eye Cream"},
{id:"7766",text:"The Body Shop Seaweed Pore-Cleansing Facial Exfoliator, for Combination/Oily Skin"},
{id:"7767",text:"Tata Harper Resurfacing Mask"},
{id:"7768",text:"StriVectin Clinical Corrector Advanced Anti-Aging Face Tint With Broad Spectrum SPF 30"},
{id:"7769",text:"Smashbox Halo Hydrating Perfecting Bronzer"},
{id:"7770",text:"Skyn Iceland Oxygen Infusion Night Cream"},
{id:"7771",text:"St. Tropez Everyday Gradual Tan Perfect Legs"},
{id:"7772",text:"Stila Set & Bronze Baked Powder Trio"},
{id:"7773",text:"TheBalm Strawberry Nourishing Facial Serum"},
{id:"7774",text:"Tarte Cosmetics Amazonian Butter Lipstick"},
{id:"7775",text:"SK-II Skin Signature Melting Rich Cream"},
{id:"7776",text:"Shiseido White Lucent Brightening Toning Lotion, Cool"},
{id:"7777",text:"Shiseido Advanced Hydro-Liquid Compact SPF 15"},
{id:"7778",text:"Sephora Long Lasting Eye Liner"},
{id:"7779",text:"St. Tropez Skin Illuminator"},
{id:"7780",text:"Smashbox Photo Finish Lid Primer"},
{id:"7781",text:"Sephora Microsmooth Foundation Face Powder"},
{id:"7782",text:"The Body Shop Vitamin E Illuminating Moisture Cream"},
{id:"7783",text:"Sonia Kashuk Eyeliner Palette"},
{id:"7784",text:"The Body Shop Tea Tree Cleansing Wipes"},
{id:"7785",text:"Stila Aqua Glow Serum Concealer"},
{id:"7786",text:"Simple Kind to Eyes Eye Make Up Remover"},
{id:"7787",text:"Shiseido White Lucent Brightening Protective Emulsion W SPF 15"},
{id:"7788",text:"Skinn by Dimitri James Extreme Perfection Self-Setting Line Fill Make-Up with Sunscreen"},
{id:"7789",text:"Smashbox Waterproof Shadow Liner"},
{id:"7790",text:"Smashbox Brow Tech Trio"},
{id:"7791",text:"TheBalm Brazil Nut Eye Perfection Gel"},
{id:"7792",text:"Skinn by Dimitri James EnergEyes Anti-Fatigue Eye Firming Wrinkle Treatment Mask"},
{id:"7793",text:"Sephora Retractable Brow PencilWaterproof"},
{id:"7794",text:"St. Tropez Self Tan Intensifier"},
{id:"7795",text:"Shiseido IBUKI Gentle Cleanser"},
{id:"7796",text:"StriVectin StriVectin-TL Tightening Face Cream"},
{id:"7797",text:"Tatcha SUPPLE Moisture Rich Silk Cream"},
{id:"7798",text:"TheBalm Meet Matt(e) NUDE Eyeshadow Palette"},
{id:"7799",text:"Too Faced La Crme Color Drenched Lipstick"},
{id:"7800",text:"Sephora 10HR Fix & Correct Concealer"},
{id:"7801",text:"The Body Shop Big & Curvy Mascara"},
{id:"7802",text:"Stila All Over Shimmer Duo"},
{id:"7803",text:"Skinn by Dimitri James Collagen Boosting Platinum Lipstick"},
{id:"7804",text:"Stridex Daily Care Acne Pads with Salicylic Acid, Essential with Vitamins"},
{id:"7805",text:"Tarte Cosmetics Amazonian Clay BB Illuminating Moisturizer"},
{id:"7806",text:"Shiseido The Skincare Purifying Mask"},
{id:"7807",text:"Smashbox Full Exposure Waterproof Mascara"},
{id:"7808",text:"Spectro Spectro HydraCare Facial Moisturizer, Dry Skin"},
{id:"7809",text:"SkinMedica Hydrating Complex"},
{id:"7810",text:"Sephora Matte Blotting Film"},
{id:"7811",text:"SkinCeuticals Skin Firming Cream"},
{id:"7812",text:"Shiseido The Make Up Dual Balancing Foundation"},
{id:"7813",text:"TheBalm Lavender Foaming Face Cleanser"},
{id:"7814",text:"Skinn by Dimitri James DermAppeal Thermo-Enzymatic Microdermabrasion Treatment"},
{id:"7815",text:"Smashbox Photo Finish Primer Oil"},
{id:"7816",text:"SK-II Whitening Source Brightening Derm Specialist"},
{id:"7817",text:"Sephora Ultimate Brightening Serum"},
{id:"7818",text:"Shiseido Benefiance Pure Retinol Intensive Revitalizing Face Mask"},
{id:"7819",text:"TheBalm BalmShelter Tinted Moisturizer SPF 18"},
{id:"7820",text:"The Body Shop Camomile Gentle Eye Make-Up Remover"},
{id:"7821",text:"Stila Smudge Crayon Waterproof Eye Color"},
{id:"7822",text:"Supergoop! Forever Young Hand Cream with Sea Buckthorn SPF 40"},
{id:"7823",text:"Smashbox O-Gloss"},
{id:"7824",text:"Sunday Riley Juno Hydroactive Cellular Face Oil"},
{id:"7825",text:"Simple Radiance Brightening Cleansing Wipes"},
{id:"7826",text:"Stila Stay All Day Waterproof Brow Color"},
{id:"7827",text:"Smashbox Soft Effects Powder Eye Liner"},
{id:"7828",text:"SkinMedica TNS Illuminating Eye Cream"},
{id:"7829",text:"Sephora Triple Action Cleansing Water"},
{id:"7830",text:"The Body Shop Shimmer Waves"},
{id:"7831",text:"StriVectin StriVectin-TL Tightening Neck Cream"},
{id:"7832",text:"SkinCeuticals C + AHA Exfoliating Antioxidant Treatment"},
{id:"7833",text:"Tatcha INDIGO Soothing Silk Hand Cream"},
{id:"7834",text:"Shiseido Translucent Loose Powder"},
{id:"7835",text:"Smashbox Fusion Soft Lights"},
{id:"7836",text:"Too Faced Bulletproof Brows Eyebrow Definer"},
{id:"7837",text:"The Body Shop Seaweed Pore Perfector, for Combination/Oily Skin"},
{id:"7838",text:"Tarte Cosmetics Best in Faux Lash Extending Fibers"},
{id:"7839",text:"SkinMedica GlyPro Antioxidant Serum"},
{id:"7840",text:"The Body Shop All-In-One BB Cream"},
{id:"7841",text:"Smashbox Photo Finish Foundation Primer Light"},
{id:"7842",text:"Smashbox Photo Matte Anti Shine"},
{id:"7843",text:"Stila Stay All Day Foundation & Concealer"},
{id:"7844",text:"SkinCeuticals LHA Solution"},
{id:"7845",text:"Simple Kind to Skin Smoothing Facial Scrub"},
{id:"7846",text:"Shiseido IBUKI Protective Moisturizer Broad Spectrum SPF 18"},
{id:"7847",text:"The Body Shop Seaweed Mattifying Moisture Lotion SPF 15, for Combination/Oily Skin"},
{id:"7848",text:"ShiKai Borage Dry Skin Therapy Hand Cream Fragrance Free"},
{id:"7849",text:"The Body Shop Tea Tree Pore Minimiser"},
{id:"7850",text:"Sephora Rouge Gel Lip Liner"},
{id:"7851",text:"The Body Shop Seaweed Mattifying Day Cream, for Combination/Oily Skin"},
{id:"7852",text:"SK-II Cellumination Cream"},
{id:"7853",text:"Shiseido Pureness Matifying Stick, Oil Free"},
{id:"7854",text:"SkinCeuticals Sport UV Defense Sunscreen Broad Spectrum SPF 50"},
{id:"7855",text:"Sephora Flirt-It Lash Duo"},
{id:"7856",text:"Stila Huge Extreme Lash Mascara"},
{id:"7857",text:"Suntegrity Unscented Body Sunscreen SPF 30"},
{id:"7858",text:"Tom Ford Concealing Pen"},
{id:"7859",text:"Sephora Colorful Mono Eyeshadow"},
{id:"7860",text:"SkinCeuticals Physical Fusion UV Defense SPF 50"},
{id:"7861",text:"Skinn by Dimitri James Huile Nutri-Vital Extreme"},
{id:"7862",text:"Simple Kind to Skin Foaming Cleanser"},
{id:"7863",text:"Simple Kind to Skin Oil Balancing Cleansing Wipes"},
{id:"7864",text:"Smashbox Cream Eye Liner Palette"},
{id:"7865",text:"Tarte Cosmetics Park Ave Princess Bronze & Glow Matte Bronzer & Cheek Tint"},
{id:"7866",text:"Shiseido Daily Bronze Moisturizing Emulsion, for Face/Body"},
{id:"7867",text:"Sephora Lifting Roll-On Serum"},
{id:"7868",text:"Smashbox Photo Finish Luminizing Foundation Primer"},
{id:"7869",text:"Skinfix Kids Eczema Balm"},
{id:"7870",text:"Stila Light Source Spot Correcting and Brightening Serum"},
{id:"7871",text:"Three Custom Color Specialists Eyeshadow Singles"},
{id:"7872",text:"St. Ives Apricot Face Wash, Blemish & Blackhead Control"},
{id:"7873",text:"Sunday Riley Good Genes Treatment"},
{id:"7874",text:"Tom Ford Illuminating Highlight Pen"},
{id:"7875",text:"Smashbox Photo Op Under Eye Brightener"},
{id:"7876",text:"Skinn by Dimitri James Lash 3X Eyelash Conditioning Gel"},
{id:"7877",text:"The Body Shop Seaweed Purifying Facial Cleanser, for Combination/Oily Skin"},
{id:"7878",text:"TheBalm Blueberry Face Treatment Mask"},
{id:"7879",text:"TheBalm Overshadow Shimmering All-Mineral Eyeshadow"},
{id:"7880",text:"SkinCeuticals A.G.E. Interrupter"},
{id:"7881",text:"The Body Shop Brow & Lash Gel"},
{id:"7882",text:"Sonia Kashuk Velvety Matte Lip Crayon"},
{id:"7883",text:"Smashbox Photo Op Eye Shadow Trio"},
{id:"7884",text:"Sephora Double-Ended Brushes"},
{id:"7885",text:"The Body Shop Vitamin C Daily Moisturizer SPF 30, Dull Skin"},
{id:"7886",text:"Smashbox Layer Lash Primer"},
{id:"7887",text:"The Body Shop Warming Mineral Face Mask"},
{id:"7888",text:"StriVectin StriVectinLabs 5 Minute Weekly Glycolic Peel"},
{id:"7889",text:"Skinn by Dimitri James Creme Extreme"},
{id:"7890",text:"SK-II Essential Power Cream"},
{id:"7891",text:"Smashbox Limitless Eye Liner"},
{id:"7892",text:"The Body Shop Vitamin C Facial Cleansing Polish"},
{id:"7893",text:"The Body Shop Vitamin E Face Mist"},
{id:"7894",text:"Tatcha LUMINOUS Deep Hydration Lifting Mask"},
{id:"7895",text:"St. Tropez Gradual Tan In Shower Lotion"},
{id:"7896",text:"Shiseido Benefiance Energizing Essence"},
{id:"7897",text:"SK-II Advanced Eye Treatment Film"},
{id:"7898",text:"The Body Shop Tea Tree Blemish Fade Night Lotion"},
{id:"7899",text:"Tarte Cosmetics Clean Slate Flawless 12-hr Brightening Primer"},
{id:"7900",text:"Spectro EczemaCare Medicated Cream"},
{id:"7901",text:"Tarte Cosmetics Smooth Operator Amazonian Clay Finishing Powder in Bronze"},
{id:"7902",text:"Smashbox Eye Beam Double-Ended Brightener"},
{id:"7903",text:"Tatcha POLISHED Deep Rice Enzyme Powder"},
{id:"7904",text:"Simple Kind to Eyes Eye Make-up Remover Pads"},
{id:"7905",text:"Sonia Kashuk Pearlescence Long Wear Crme Shadow"},
{id:"7906",text:"Sonia Kashuk Perfecting Luminous Foundation"},
{id:"7907",text:"Tarte Cosmetics Amazonian Clay Bronzer"},
{id:"7908",text:"Sephora Microsmooth Blush Duo"},
{id:"7909",text:"Shiseido Benefiance NutriPerfect Day Cream SPF 15 Sunscreen"},
{id:"7910",text:"TheBalm timeBalm Concealer"},
{id:"7911",text:"Tarte Cosmetics Clean Slate 360 Creaseless 12-hour Smoothing Eye Primer"},
{id:"7912",text:"Sonia Kashuk Creme Blush"},
{id:"7913",text:"The Body Shop Divide & Multiply Mascara"},
{id:"7914",text:"Sephora Age Defy Moisture Cream SPF 15"},
{id:"7915",text:"The Body Shop Tea Tree Skin Clearing Lotion"},
{id:"7916",text:"SkinCeuticals Emollience Rich, Restorative Moisturizer, for Normal or Dry Skin"},
{id:"7917",text:"Shiseido Pureness Matifying Compact Oil-Free SPF 16"},
{id:"7918",text:"Sonia Kashuk Primer Vitamin Serum"},
{id:"7919",text:"Shiseido Bio-Performance Super Exfoliating Discs"},
{id:"7920",text:"SkinMedica Facial Cleanser"},
{id:"7921",text:"The Body Shop Big & Curvy Waterproof Mascara"},
{id:"7922",text:"Stila Kajal Eye Liner"},
{id:"7923",text:"St. Ives Makeup Remover & Facial Cleanser"},
{id:"7924",text:"St. Ives Naturally Clear Apricot Cleanser Blemish-Fighting"},
{id:"7925",text:"Shiseido White Lucent Brightening Moisturizing Cream"},
{id:"7926",text:"SK-II LXP Ultimate Revival Cream"},
{id:"7927",text:"SK-II Essential Power Rich Cream"},
{id:"7928",text:"Supergoop! Defense Refresh Setting Mist with Rosemary SPF 50"},
{id:"7929",text:"Sephora Le Waterproof Mascara"},
{id:"7930",text:"The Body Shop Aloe Soothing Moisture Lotion SPF 15, for Sensitive Skin"},
{id:"7931",text:"Sephora Kohl Expert"},
{id:"7932",text:"The Body Shop Seaweed Ionic Clay Mask"},
{id:"7933",text:"Shiseido The Skincare Hydro-Balancing Softener, Alcohol-Free"},
{id:"7934",text:"Skinn by Dimitri James Hydro-Surge Deep Moisture Lifting Treatment"},
{id:"7935",text:"Tarte Cosmetics SmolderEYES Amazonian Clay Waterproof Eye Liner"},
{id:"7936",text:"Tarte Cosmetics Lights, Camera, Flashes Statement Mascara"},
{id:"7937",text:"Three Custom Color Specialists Professional Concealer Palette"},
{id:"7938",text:"StriVectin StriVectin-SD Eye Concentrate for Wrinkles"},
{id:"7939",text:"TheBalm theBalm Girls Lipstick"},
{id:"7940",text:"SK-II Facial Treatment Cleanser"},
{id:"7941",text:"Sonia Kashuk Radiant Tinted Moisturizer SPF 15"},
{id:"7942",text:"The Body Shop Skin Primer Matte It"},
{id:"7943",text:"Skinn by Dimitri James Chin-Up Firm, Lift and Tone"},
{id:"7944",text:"SkinCeuticals A.G.E. Eye Complex"},
{id:"7945",text:"Shiseido Luminizing Satin Eye Color"},
{id:"7946",text:"SkinCeuticals Body Tightening Concentrate"},
{id:"7947",text:"The Body Shop Vitamin E Nourishing Night Cream"},
{id:"7948",text:"Shiseido IBUKI Refining Moisturizer Enriched"},
{id:"7949",text:"ShiKai Borage Dry Skin Therapy Facial Cleanser"},
{id:"7950",text:"Shiseido Lacquer Rouge"},
{id:"7951",text:"Shiseido White Lucent Brightening Serum for Neck & Decolletage"},
{id:"7952",text:"Supergoop! Acai Fusion Lip Balm with Shea Butter Broad Spectrum SPF 30"},
{id:"7953",text:"St. Tropez Everyday Gradual Spray"},
{id:"7954",text:"Tatcha POLISHED Gentle Rice Enzyme Powder"},
{id:"7955",text:"SkinCeuticals Foaming Cleanser"},
{id:"7956",text:"Sephora Waterproof Smoky Cream Liner"},
{id:"7957",text:"Shiseido Future Solution LX Total Regenerating Cream Night"},
{id:"7958",text:"The Body Shop Aloe Soothing Day Cream, for Sensitive Skin"},
{id:"7959",text:"SkinCeuticals Daily Moisture Lightweight Pore-Minimizing Moisturizer, for Normal or Oily Combination Skin"},
{id:"7960",text:"Skinn by Dimitri James Wet Shine Lip Gloss"},
{id:"7961",text:"SkinCeuticals Gentle Cleanser"},
{id:"7962",text:"Sonia Kashuk Ultra Luxe Lip Gloss"},
{id:"7963",text:"Stila Eye Shadow Pan"},
{id:"7964",text:"Sephora Luster Matte Long-Wear Lip Color"},
{id:"7965",text:"Simple Kind to Skin Protecting Light Moisturizer with SPF 15"},
{id:"7966",text:"SkinCeuticals Hydrating B5 Gel Moisture Enhancing Gel"},
{id:"7967",text:"SkinCeuticals Phloretin CF"},
{id:"7968",text:"Too Faced Love Lisa Luxury Lip Balm SPF 15"},
{id:"7969",text:"Sonia Kashuk Twist-Up Long Wear Gel Liner"},
{id:"7970",text:"St. Tropez Self Tan Bronzing Mist"},
{id:"7971",text:"TheBalm Pomegranate Restorative Facial Serum"},
{id:"7972",text:"Skinn by Dimitri James Triple Cover Conceal Wheel"},
{id:"7973",text:"The Body Shop Born Lippy Balms"},
{id:"7974",text:"Tarte Cosmetics Lipsurgence Lip Gloss"},
{id:"7975",text:"Shiseido Bio-Performance Intensive Skin Corrective Program"},
{id:"7976",text:"SkinMedica Retinol Complex 1.0"},
{id:"7977",text:"Tarte Cosmetics Lights, Camera, Lashes Inner Rim Liner"},
{id:"7978",text:"The Body Shop Radiant Highlighter"},
{id:"7979",text:"Tarte Cosmetics Amazonian Clay Full Coverage Airbrush Foundation"},
{id:"7980",text:"Smashbox Photo Finish Foundation Primer SPF 15 & Dermaxyl"},
{id:"7981",text:"Smashbox Always Sharp Waterproof Kohl Liner"},
{id:"7982",text:"SK-II Ultimate Revival Serum"},
{id:"7983",text:"brand product"},
{id:"7984",text:"Vichy Aqualia Antiox Anti-Fatigue Ice-Effect Eye Stick"},
{id:"7985",text:"Urban Decay Super Saturated High Gloss Lip Color"},
{id:"7986",text:"Ulta Cheek Stain"},
{id:"7987",text:"Yes To Yes to Grapefruit Naturally Smooth Lip Balm"},
{id:"7988",text:"Yves Saint Laurent Fusion Ink Foundation Octinoxate Sunscreen SPF 18"},
{id:"7989",text:"Too Faced Cocoa Contour Chiseled to Perfection Palette"},
{id:"7990",text:"Yes To Yes to Grapefruit Exfoliating Body Wash"},
{id:"7991",text:"Vichy ProEVEN Total Dark Spot Corrector"},
{id:"7992",text:"Wet 'n Wild MegaBrilliance Lip Gloss"},
{id:"7993",text:"Vaseline MEN Cooling Hydration Body Lotion"},
{id:"7994",text:"Yves Saint Laurent Perfect Touch Radiant Brush Foundation"},
{id:"7995",text:"Urban Decay Brightening & Tightening Complexion Primer Potion"},
{id:"7996",text:"Yves Saint Laurent Rouge Pur Couture Glossy Stain"},
{id:"7997",text:"Too Faced Lash Injection"},
{id:"7998",text:"Yes To Yes To Grapefruit Dark Spot Correcting Serum"},
{id:"7999",text:"Urban Decay Naked Skin Color Correcting Fluid"},
{id:"8000",text:"Wet 'n Wild Silk Finish Lipstick"},
{id:"8001",text:"Yes To Yes to Coconut Ultra Light Spray Body Lotion"},
{id:"8002",text:"Wet 'n Wild Coverall Pressed Powder"},
{id:"8003",text:"Trish McEvoy Flawless Lip"},
{id:"8004",text:"Vichy Capital Soleil SPF 45 Silkscreen Dry-Finish Lotion"},
{id:"8005",text:"Yes To Yes to Carrots Fragrance Free Intense Hydration Cream"},
{id:"8006",text:"Topix Replenix Sheer Physical Sunscreen SPF 50+"},
{id:"8007",text:"Topix Replenix All Trans Retinol Eye Repair Cream"},
{id:"8008",text:"Trish McEvoy Flawless Lip Color"},
{id:"8009",text:"Yes To Yes to Carrots Repairing Night Cream"},
{id:"8010",text:"Vichy Normaderm Pro Mat Ultra-Mattifying Oil-Free Lotion SPF 15"},
{id:"8011",text:"Vichy Purete Thermale Purifying Foam Cream Cleanser"},
{id:"8012",text:"Yves Saint Laurent Mascara Aquaresistant Mascara Waterproof"},
{id:"8013",text:"Vichy Aqualia Thermal Cream Fortifying & Soothing 24Hr Hydrating Care"},
{id:"8014",text:"Yes To Yes to Blueberries Cleansing Facial Towelettes"},
{id:"8015",text:"Topix Citrix CRS 20% Serum with Growth Factor"},
{id:"8016",text:"Too Faced Lashlight a Mascara That Lights Up Your Lashes"},
{id:"8017",text:"Urban Decay Ink For Eyes"},
{id:"8018",text:"Vaseline Intensive Care Healing Serum Radiance Restore"},
{id:"8019",text:"Yves Saint Laurent Eyeliner Effet Faux Cils Bold Felt-Tip Eyeliner Pen"},
{id:"8020",text:"Wet 'n Wild Mega Shield Lip Color SPF 15"},
{id:"8021",text:"Topix Replenix All Trans Retinol Smoothing Serum 10X"},
{id:"8022",text:"Vichy ProEVEN Daily Eye Corrector Dark Circles and Spots"},
{id:"8023",text:"Ulta Renew Active Repair Eye Serum"},
{id:"8024",text:"Vaseline Cocoa Radiant Spray & Go Moisturizer"},
{id:"8025",text:"Vaseline Intensive Care Advanced Repair Lightly Scented"},
{id:"8026",text:"Topix Citrix Antioxidant Sunscreen SPF 30"},
{id:"8027",text:"Too Faced Lip Primer Lip Insurance"},
{id:"8028",text:"Urban Decay Urban Defense Tinted Moisturizer SPF 20"},
{id:"8029",text:"Yves Saint Laurent Volume Effet Faux Cils Luxurious Mascara for a False Lash Effect"},
{id:"8030",text:"Trish McEvoy Be Prepared Pink Lip Kit"},
{id:"8031",text:"Yves Saint Laurent Rouge Pur Couture The Mats"},
{id:"8032",text:"Vichy Puret Thermale Purifying Exfoliating Cream"},
{id:"8033",text:"Vichy Capital Soleil SPF 30 Soft Sheer Sunscreen Lotion"},
{id:"8034",text:"Urban Decay Naked Basics Palette"},
{id:"8035",text:"Yes To Yes to Cucumbers Soothing Eye Gel"},
{id:"8036",text:"Urban Decay Lipstick"},
{id:"8037",text:"Ulta Double Duty Concealer and Dark Circle Treatment"},
{id:"8038",text:"Vaseline Intensive Care Healing Serum Advanced Relief"},
{id:"8039",text:"Yes To Yes to Cucumbers Eye Makeup Removing Pads"},
{id:"8040",text:"Trish McEvoy Eye Base Essentials"},
{id:"8041",text:"Urban Decay Razor Sharp Ultra Definition Finishing Powder"},
{id:"8042",text:"Yves Saint Laurent Touche Eclat Radiant Touch"},
{id:"8043",text:"Yves Saint Laurent Touche Eclat Strobing Light Mega Highlighter"},
{id:"8044",text:"Vichy Normaderm Daily Exfoliating Cleansing Gel"},
{id:"8045",text:"Urban Decay NAKED Palette with Brush"},
{id:"8046",text:"Urban Decay Stardust Eyeshadow"},
{id:"8047",text:"Urban Decay Naked Skin Ultra Definition Loose Finishing Powder"},
{id:"8048",text:"Trish McEvoy Beauty Booster Cream SPF 30 Anti-Fatigue Moisturizer and Enriched Primer"},
{id:"8049",text:"Wet 'n Wild MegaSlicks Lip Gloss"},
{id:"8050",text:"Vivit’© Replenish Hydrating Facial Cleanser"},
{id:"8051",text:"Trish McEvoy Even Skin Face Primer"},
{id:"8052",text:"Wet 'n Wild Coverall Cream Foundation"},
{id:"8053",text:"Yves Saint Laurent Rouge Pur Couture Lipstick"},
{id:"8054",text:"Yves Saint Laurent Dessin des Levres Lip Liner"},
{id:"8055",text:"Urban Decay Heavy Metal Glitter Liner"},
{id:"8056",text:"Ulta Daily Regenerating Face Serum"},
{id:"8057",text:"Vaseline Cocoa Butter Deep Conditioning Extra Rich Cream"},
{id:"8058",text:"Vichy Aqualia Antiox \"New Skin\" Antioxidant Fresh Serum"},
{id:"8059",text:"Ulta Resolve Resurfacing Polishing Cream Cleanser"},
{id:"8060",text:"Vaseline MEN Fast Absorbing Body and Face Lotion"},
{id:"8061",text:"Yves Saint Laurent Rouge Volupte Perle Silky Sensual Radiant Lipstick SPF 15"},
{id:"8062",text:"Yes To Yes to Carrots Fragrance Free Exfoliating Cleanser"},
{id:"8063",text:"Wet 'n Wild MegaEyes Defining Marker"},
{id:"8064",text:"Yes To Yes to Carrots Makeup Removing Cleanser"},
{id:"8065",text:"Yes To Yes to Coconut Naturally Smooth Lip Balm"},
{id:"8066",text:"Yves Saint Laurent Mascara Volume Effet Faux Cils Luxurious Mascara Waterproof for a False Lash Effect"},
{id:"8067",text:"Urban Decay Deluxe Eyeshadow"},
{id:"8068",text:"Yves Saint Laurent Temps Majeur Serum"},
{id:"8069",text:"Ulta Bronze Glow Daily Moisturizer for Face"},
{id:"8070",text:"Too Faced Air Buffed BB Cream SPF 20"},
{id:"8071",text:"Vichy Liftactiv Serum 10 Eyes & Lashes"},
{id:"8072",text:"Too Faced Absolutely Flawless Concealer"},
{id:"8073",text:"Yves Saint Laurent Forever Youth Liberator Eye Cream"},
{id:"8074",text:"Vaseline Intensive Rescue Soothing Body Lotion with Chamomile"},
{id:"8075",text:"Too Faced Primed & Poreless Pure Skin Smoothing Face Primer for Sensitive Skin"},
{id:"8076",text:"Urban Decay Brow Box"},
{id:"8077",text:"Urban Decay Eyeshadow Primer Potion Original"},
{id:"8078",text:"Urban Decay Perversion Mascara"},
{id:"8079",text:"Urban Decay Naked Skin Ultra Definition Pressed Powder"},
{id:"8080",text:"Too Faced Lashgasm Voluptuous Fluffing Mascara"},
{id:"8081",text:"Trish McEvoy Trish McEvoy Brushes"},
{id:"8082",text:"Urban Decay Ultimate Ozone Multipurpose Primer Pencil"},
{id:"8083",text:"Trish McEvoy Instant Pick-Me-Up Lips Shimmer"},
{id:"8084",text:"Wet 'n Wild Juicy Lip Balm SPF 15"},
{id:"8085",text:"Ulta Extreme Wear Mousse Blush"},
{id:"8086",text:"Trish McEvoy Dual Resort Bronzer"},
{id:"8087",text:"Vichy Reti-C Intensive Corrective Care SPF 15"},
{id:"8088",text:"Yves Saint Laurent Matt Touch"},
{id:"8089",text:"Ulta Legendary Lengths Lengthening Mascara"},
{id:"8090",text:"Yves Saint Laurent Eyeliner Moir Liquid Eyeliner"},
{id:"8091",text:"Too Faced Duo Shadow"},
{id:"8092",text:"Yves Saint Laurent Forever Youth Liberator Serum"},
{id:"8093",text:"Vichy Liftactiv CxP Eyes Anti-Wrinkle Eyelid Lifting Care"},
{id:"8094",text:"Ulta Gel Eyeliner Pencil"},
{id:"8095",text:"Ulta Lash Icon Volume Mascara"},
{id:"8096",text:"Urban Decay 24/7 Glide-On Eye Pencil"},
{id:"8097",text:"Topix Replenix AE Facial Firming Therapy"},
{id:"8098",text:"Vichy LiftActiv CxP Total Serum"},
{id:"8099",text:"Urban Decay Guardian Angel Spray Moisturizer SPF 8"},
{id:"8100",text:"Vivit’© Vibrance Therapy"},
{id:"8101",text:"Wet 'n Wild Natural Blend Pressed Powder"},
{id:"8102",text:"Topix Replenix Power of Three Cream"},
{id:"8103",text:"Vichy Capital Soleil Daily Moisturizer Cream With Sunscreen SPF 15"},
{id:"8104",text:"Yes To Yes to Tomatoes Repairing Acne Lotion"},
{id:"8105",text:"Urban Decay Skyscraper Multi-Benefit Mascara"},
{id:"8106",text:"Vichy Purete Thermale Hydra-SoothingToner, for Dry and Sensitive Skin"},
{id:"8107",text:"Vichy Neovadiol GF Day Densifying Re-Sculpting Care Normal/Combination"},
{id:"8108",text:"Trish McEvoy Luxe Moisture Balm SPF 15"},
{id:"8109",text:"Yves Saint Laurent Teint Majeur Luxurious Foundation SPF 18"},
{id:"8110",text:"Urban Decay All Nighter Long Lasting Makeup Setting Spray"},
{id:"8111",text:"Topix Replenix Fortified Exfoliation Scrub"},
{id:"8112",text:"Urban Decay Revolution High-Color Lipgloss"},
{id:"8113",text:"Yes To Yes to Tomatoes Clear Skin Daily Balancing Moisturizer"},
{id:"8114",text:"Trish McEvoy Shimmer Bronze Pressed Powder"},
{id:"8115",text:"Yes To Yes to Cucumbers Daily Gel Cleanser"},
{id:"8116",text:"Wet 'n Wild MegaPlump Mascara"},
{id:"8117",text:"Urban Decay Naked Smoky Eyeshadow Palette"},
{id:"8118",text:"Yves Saint Laurent Forever Light Creator Skintone Corrector Serum"},
{id:"8119",text:"Trish McEvoy Glaze Lip Color"},
{id:"8120",text:"Vichy LiftActiv with Rhamnose 5% Day for Normal Combination Skin"},
{id:"8121",text:"Ulta Advanced Firming Eye Serum"},
{id:"8122",text:"Urban Decay De-Slick Oil Control Makeup Setting Spray"},
{id:"8123",text:"Yes To Yes to Cucumbers Soothing Daily Calming Moisturizer"},
{id:"8124",text:"Topix Replenix Intensive Eye Lightening Serum"},
{id:"8125",text:"Yes To Yes to Tomatoes Clear Skin Acne Control Gel Cleanser"},
{id:"8126",text:"Yves Saint Laurent Volupte Sheer Candy Glossy Balm Crystal Color"},
{id:"8127",text:"Wet 'n Wild MegaSparkle  Confetti"},
{id:"8128",text:"Yves Saint Laurent Golden Gloss Shimmering Lip Gloss"},
{id:"8129",text:"Yves Saint Laurent Mascara Volume Effet Faux Cils Baby Doll"},
{id:"8130",text:"Ulta Renew Hydra Quench Foaming Oil Cleanser"},
{id:"8131",text:"Wet 'n Wild Fergie Turn Up the Volume Mascara"},
{id:"8132",text:"Trish McEvoy Intense Eye Treatment"},
{id:"8133",text:"Too Faced Absolutely Invisible"},
{id:"8134",text:"Wet 'n Wild Ultimate Minerals Bronzer"},
{id:"8135",text:"Ulta Extreme Wear Gel Eye Liner"},
{id:"8136",text:"Wet 'n Wild H2O Proof Liquid Eyeliner"},
{id:"8137",text:"Trish McEvoy Sheer Lip Color"},
{id:"8138",text:"Urban Decay Super-Saturated Ultra Intense Waterproof Cream Eyeliner"},
{id:"8139",text:"Topix Replenix Infusion Eye Repair Serum"},
{id:"8140",text:"Yes To Yes to Cucumbers Gentle Milk Cleanser"},
{id:"8141",text:"Ulta Automatic Eye Liner"},
{id:"8142",text:"Yves Saint Laurent Volupte Tint-in-Oil"},
{id:"8143",text:"Vaseline Aloe Fresh Spray & Go Moisturizer"},
{id:"8144",text:"Trish McEvoy Irresistible Lip Gloss"},
{id:"8145",text:"Trish McEvoy Line Refiner"},
{id:"8146",text:"Yves Saint Laurent Top Secrets Pore Refiner Skincare Brush"},
{id:"8147",text:"Vichy Liftactiv CxP SPF 20 Biolifting Daily Care Anti-Wrinkle and Firming"},
{id:"8148",text:"Ulta Lash Blow Out Lengthening Mascara"},
{id:"8149",text:"Urban Decay Dew Me Moisturizing Makeup Setting Spray"},
{id:"8150",text:"Wet 'n Wild MegaLiner Liquid Eyeliner"},
{id:"8151",text:"Yes To Yes to Cucumbers Daily Calming Moisturizer SPF 30"},
{id:"8152",text:"Vichy Liftactiv Retinol HA SPF 18 Total Anti-Wrinkle Intervention"},
{id:"8153",text:"Topix Replenix CF Green Tea Antioxidant Moisturizing Lotion"},
{id:"8154",text:"Urban Decay Naked Skin Beauty Balm Broad Spectrum SPF 20"},
{id:"8155",text:"Yes To Yes To Blueberries Age Refresh Overnight Hydrating Cream"},
{id:"8156",text:"Ulta Raincoat Waterproof Mascara Topcoat"},
{id:"8157",text:"Yes To Yes to Coconut Ultra Hydrating Crme Cleanser"},
{id:"8158",text:"Ulta Precision Liquid Eyeliner"},
{id:"8159",text:"Vichy Aqualia Thermal 24Hr Hydrating Fortifying Lotion SPF 30"},
{id:"8160",text:"Yes To Yes to Grapefruit Rejuvenating Body Wash"},
{id:"8161",text:"Yes To Yes to Carrots Nourishing Body Wash"},
{id:"8162",text:"Yves Saint Laurent Mascara Volume Effet Faux Cils Shocking"},
{id:"8163",text:"Wet 'n Wild Color Icon Eyeshadow Singles"},
{id:"8164",text:"Ulta Contour Lip Liner"},
{id:"8165",text:"Too Faced Better Than Sex Mascara"},
{id:"8166",text:"Yes To Yes to Carrots Super Rich Body Butter"},
{id:"8167",text:"Yes To Yes to Cucumbers Soothing Body Wash"},
{id:"8168",text:"Urban Decay Matte Revolution Lipstick"},
{id:"8169",text:"Vaseline Cocoa Butter Deep Conditioning Body Lotion"},
{id:"8170",text:"Yes To Yes To Blueberries Age Refresh Daily Repairing Moisturizer"},
{id:"8171",text:"Trish McEvoy Bronze Luminizer"},
{id:"8172",text:"Ulta Advanced Relief Cooling Eye Gel"},
{id:"8173",text:"Yves Saint Laurent Ombre Solo Mono Eyeshadow"},
{id:"8174",text:"Wet 'n Wild Perfect Pair Eye Wand"},
{id:"8175",text:"Yves Saint Laurent Ombres Duolumieres Eye Shadow Duo"},
{id:"8176",text:"Urban Decay De-Slick in a Tube Mattifying Gel"},
{id:"8177",text:"Ulta Eye Brow Gel"},
{id:"8178",text:"Urban Decay Cannonball Ultra Waterproof Mascara"},
{id:"8179",text:"Yes To Yes to Blueberries Face & Neck Oil"},
{id:"8180",text:"Ulta Cheek Color"},
{id:"8181",text:"Topix Citrix Antioxidant Facial Toner"},
{id:"8182",text:"Yves Saint Laurent Rouge Pur Couture Glossy Stain Rebel Nudes"},
{id:"8183",text:"Urban Decay Eyelash Primer Potion"},
{id:"8184",text:"Yes To Yes To Carrots Pomegranate Lip Butter with SPF 15"},
{id:"8185",text:"Yves Saint Laurent Voile De Blush"},
{id:"8186",text:"Too Faced Love Flush Long-Lasting 16-Hour Blush"},
{id:"8187",text:"Trish McEvoy Glaze Eye Shadow"},
{id:"8188",text:"Topix Citrix CRS 15% Serum with Growth Factor"},
{id:"8189",text:"Wet 'n Wild Natural Blend Mineral Foundation"},
{id:"8190",text:"Yves Saint Laurent Top Secrets Flash Touch Wake-Up Eye Care"},
{id:"8191",text:"Yves Saint Laurent Eyeliner Effet Faux Cils Long-Wear Cream Eyeliner"},
{id:"8192",text:"Vaseline MEN Extra Strength Body and Face Lotion"},
{id:"8193",text:"Urban Decay Naked Ultra Nourishing Gloss"},
{id:"8194",text:"Too Faced Candlelight Glow Highlighting Powder Duo"},
{id:"8195",text:"Vichy LiftActiv with Rhamnose 5% Night"},
{id:"8196",text:"Trish McEvoy Brightening Line Minimizing Concealer"},
{id:"8197",text:"Ulta Fabulous Face Compact Foundation"},
{id:"8198",text:"Yves Saint Laurent Blush Radiance"},
{id:"8199",text:"Too Faced Lava Gloss Eyeliner"},
{id:"8200",text:"Ulta Ulta LipstickCream"},
{id:"8201",text:"Trish McEvoy Even Skin Mineral Foundation SPF 15"},
{id:"8202",text:"Yes To Yes to Coconut Cleansing Wipes"},
{id:"8203",text:"Urban Decay Naked2 Palette"},
{id:"8204",text:"Too Faced Size Queen Mascara"},
{id:"8205",text:"Ulta Ultimate Brows Shade & Shaper Pencil"},
{id:"8206",text:"Urban Decay Revolution Lipstick"},
{id:"8207",text:"Yes To Yes to Carrots Fragrance Free Daily Moisturizer SPF 15"},
{id:"8208",text:"Urban Decay Meltdown Makeup Remover"},
{id:"8209",text:"Yes To Yes to Carrots Fragrance Free Towelettes"},
{id:"8210",text:"Urban Decay Big Fatty Mascara"},
{id:"8211",text:"Wet 'n Wild Intuitive Blend Shade Adjusting Foundation + Primer"},
{id:"8212",text:"Wet 'n Wild MegaShimmer Shimmer Dust"},
{id:"8213",text:"Too Faced Tinted Beauty Balm SPF 20"},
{id:"8214",text:"Yves Saint Laurent Rouge Volupte Shine"},
{id:"8215",text:"Yves Saint Laurent Poudre Sur Mesure Semi-Loose Powder"},
{id:"8216",text:"Vichy Neovadiol GF Night Densifying Re-Sculpting Care"},
{id:"8217",text:"Topix Replenix All Trans Retinol Smoothing Serum 2X"},
{id:"8218",text:"Wet 'n Wild MegaLast Retractable Eyeliner"},
{id:"8219",text:"Yves Saint Laurent Terre Saharienne Bronzing Powder SPF 12"},
{id:"8220",text:"Yves Saint Laurent Temps Majeur Creme"},
{id:"8221",text:"Vichy LiftActiv Retinol HA Eyes Total Anti-Wrinkle Renovating Care"},
{id:"8222",text:"Urban Decay All Nighter Eyeliner"},
{id:"8223",text:"Urban Decay Eyeshadows"},
{id:"8224",text:"Too Faced Lip Injection"},
{id:"8225",text:"Vaseline Intensive Care Advanced Repair Fragrance-Free"},
{id:"8226",text:"Urban Decay 24/7 Waterline Eye Pencil"},
{id:"8227",text:"Ulta Liquid Eye Liner"},
{id:"8228",text:"Yes To Yes to Tomatoes Acne Daily Pore Scrub"},
{id:"8229",text:"Ulta Plush Drama Volume Mascara"},
{id:"8230",text:"Vaseline Cocoa Butter Vitalizing Gel Body Oil"},
{id:"8231",text:"Trish McEvoy Luminizer Pen"},
{id:"8232",text:"Ulta Resolve Pore Correction Concentrate Serum"},
{id:"8233",text:"Ulta Instant Nourish Facial Moisturizer"},
{id:"8234",text:"Vichy Nutrilogie 2 Intensive Nourishing Moisturizer Cream"},
{id:"8235",text:"Wet 'n Wild Color Icon Eyeshadow Trio"},
{id:"8236",text:"Yes To Yes to Blueberries Age Refresh Daily Facial Moisturizer SPF 30"},
{id:"8237",text:"Trish McEvoy Instant Eye Lift"},
{id:"8238",text:"Topix Replenix All Trans Retinol Smoothing Serum 3X"},
{id:"8239",text:"Vichy Liftactiv Serum 10"},
{id:"8240",text:"Topix Antioxidant Sunscreen Moisturizer SPF 50 Plus"},
{id:"8241",text:"Trish McEvoy Essential Pencil"},
{id:"8242",text:"Yes To Yes to Carrots Exfoliating Cleanser"},
{id:"8243",text:"Trish McEvoy Liquid Face Color"},
{id:"8244",text:"Trish McEvoy Eye Brightener"},
{id:"8245",text:"Wet 'n Wild MegaGlo Illuminating Powder"},
{id:"8246",text:"Ulta Color Instincts Foundation"},
{id:"8247",text:"Yves Saint Laurent Teint Radiance Radiance-Enhancing Flawless Foundation SPF 20"},
{id:"8248",text:"Urban Decay Naked Skin Body Beauty Balm SPF 20"},
{id:"8249",text:"Wet 'n Wild MegaWink Mascara"},
{id:"8250",text:"Yes To Yes To Blueberries Age Refresh Intensive Skin Repair Serum"},
{id:"8251",text:"Ulta Fabulous Face Liquid Concealer"},
{id:"8252",text:"Vichy Aqualia Thermal Roll-On Eye Fortifying Hydrogel"},
{id:"8253",text:"Yes To Yes To Grapefruit Brightening Facial Towelettes"},
{id:"8254",text:"Ulta Dual Action Polishing Cleanser"},
{id:"8255",text:"Wet 'n Wild Ultimate Match SPF 15 Foundation"},
{id:"8256",text:"Trish McEvoy Precision Brow Shaper"},
{id:"8257",text:"Ulta Double Duty Primer & Foundation"},
{id:"8258",text:"Yves Saint Laurent Top Secrets Flash Radiance Skincare Brush"},
{id:"8259",text:"Vichy ProEVEN Mineral BB Cream Skin Perfecting Beauty Balm With SPF 20"},
{id:"8260",text:"Vichy Purete Thermale Hydra-Perfecting Toner for Normal and Combination Skin"},
{id:"8261",text:"Trish McEvoy Classic Lip Color"},
{id:"8262",text:"Yes To Yes to Blueberries Deep Wrinkle Night Cream"},
{id:"8263",text:"Trish McEvoy Treatment Foundation SPF 15"},
{id:"8264",text:"Too Faced Brow Envy Brow Shaping & Defining Kit"},
{id:"8265",text:"Yes To Yes to Grapefruit Dark Spot Correcting Body Creme"},
{id:"8266",text:"Too Faced HangoveRx Replenishing Face Primer"},
{id:"8267",text:"Urban Decay Naked3 Palette"},
{id:"8268",text:"Vichy Purete Thermale 3-in-1 One-Step Cleanser"},
{id:"8269",text:"Wet 'n Wild Idol Eyes Cream Eyeshadow"},
{id:"8270",text:"Vichy Capital Soleil SPF 60 Soft Sheer Sunscreen Lotion"},
{id:"8271",text:"Yves Saint Laurent Baby Doll Kiss & Blush"},
{id:"8272",text:"Trish McEvoy Gentle Cleansing Lotion"},
{id:"8273",text:"Trish McEvoy Beauty Booster SPF 15 Lip Gloss"},
{id:"8274",text:"Trish McEvoy Lash Builder"},
{id:"8275",text:"Urban Decay Naked Skin Weightless Ultra Definition Liquid Makeup"},
{id:"8276",text:"Yes To Yes to Coconut Head-to-Toe Restoring Balm"},
{id:"8277",text:"Urban Decay Eyeshadow Primer Potion Minor Sin"},
{id:"8278",text:"Wet 'n Wild Eye Makeup Remover, Oil-Free"},
{id:"8279",text:"Too Faced Borderline Lip Pencil"},
{id:"8280",text:"Vaseline Healthy Hand & Nail Conditioning Hand Lotion"},
{id:"8281",text:"Vichy Idealia Life Serum Skin Idealizer Serum"},
{id:"8282",text:"Yes To Yes to Coconut Eczema Relief Spray"},
{id:"8283",text:"Trish McEvoy Deluxe Eye Shadow"},
{id:"8284",text:"Urban Decay Lip Envy"},
{id:"8285",text:"Trish McEvoy Gentle Eye Makeup Remover"},
{id:"8286",text:"Wet 'n Wild Ultimate Brow Kit"},
{id:"8287",text:"Urban Decay De-Slick Mattifying Powder"},
{id:"8288",text:"Yes To Yes To Cucumbers Natural Sunscreen SPF 30 Stick"},
{id:"8289",text:"Ulta Resolve Clarifying Deep Treatment Foaming Mask"},
{id:"8290",text:"Yves Saint Laurent Hydra Feel Fresh Hydrating Water Gel"},
{id:"8291",text:"Trish McEvoy Gentle Cleansing Wash"},
{id:"8292",text:"Wet 'n Wild Color Icon Lip Liner"},
{id:"8293",text:"Too Faced Beach Bunny Bronzer"},
{id:"8294",text:"Yes To Yes to Tomatoes Clear Skin Skin Clearing Facial Mask"},
{id:"8295",text:"Too Faced Absolutely Invisible Candlelight Softly Illuminating Translucent Powder"},
{id:"8296",text:"Too Faced Matte Eye Shadow Collection"},
{id:"8297",text:"Urban Decay Matte Eyeshadow"},
{id:"8298",text:"Ulta Super Shiny Lip Butter"},
{id:"8299",text:"Vivit’© Aftercare Environmental Protection Sunscreen with SPF 30"},
{id:"8300",text:"Yves Saint Laurent Ombres 5 Lumieres 5 Colour Harmony for Eyes"},
{id:"8301",text:"Yves Saint Laurent Dessin Du Regard Crayon Yeux Haute Tenue Eye Pencil"},
{id:"8302",text:"Ulta Oil-Free Eye Makeup Remover"},
{id:"8303",text:"Too Faced Full Bloom Ultra Flush Blush"},
{id:"8304",text:"Topix Replenix Fortified Cleanser"},
{id:"8305",text:"Ulta Color Core Lipstick"},
{id:"8306",text:"Vaseline Total Moisture Clean Feeling Lotion"},
{id:"8307",text:"Trish McEvoy Lip Liner"},
{id:"8308",text:"Ulta Amped Lashes Volume + Defining Mascara"},
{id:"8309",text:"Vichy Normaderm 3-in-1 Unclogging Purifying Toner"},
{id:"8310",text:"Ulta Resolve Purifying Cleansing Wash"},
{id:"8311",text:"Yes To Yes to Carrots Fragrance Free Daily Cream Facial Cleanser"},
{id:"8312",text:"Trish McEvoy Lash Enhancing Eye Liner"},
{id:"8313",text:"Wet 'n Wild Color Icon Eyeshadow Collection"},
{id:"8314",text:"Vichy Purete Thermale Eye Make-Up Remover, for Sensitive Eyes"},
{id:"8315",text:"Trish McEvoy Cream Lip Color"},
{id:"8316",text:"Vichy Normaderm Night Daily Corrective Care for Oily Skin"},
{id:"8317",text:"Wet 'n Wild MegaProtein Mascara"},
{id:"8318",text:"Trish McEvoy Blush"},
{id:"8319",text:"Yves Saint Laurent Youth Liberator Serum Foundation Broad Spectrum SPF 20"},
{id:"8320",text:"Yes To Yes to Tomatoes Daily Clarifying Cleanser"},
{id:"8321",text:"Urban Decay Supercurl Curling Mascara"},
{id:"8322",text:"Vichy Liftactiv Retinol HA Night"},
{id:"8323",text:"Ulta Resolve Pore Minimizing Daily Cleansing Treatment"},
{id:"8324",text:"Trish McEvoy High-Volume Mascara"},
{id:"8325",text:"Urban Decay Lush Lash Mascara Growth Serum-Infused Conditioning Mascara"},
{id:"8326",text:"Ulta Ulta Eyeshadow"},
{id:"8327",text:"Urban Decay Lip Junkie Lip Gloss"},
{id:"8328",text:"Ulta Minerals Blush"},
{id:"8329",text:"Vichy Aqualia Thermal Mineral Balm Rehydrating and Repairing Care"},
{id:"8330",text:"Yes To Yes To Grapefruit Even Skin Tone Moisturizer with SPF 15"},
{id:"8331",text:"Trish McEvoy Instant Pick-Me-Up Lips"},
{id:"8332",text:"Yves Saint Laurent Waterproof Eye Pencil"},
{id:"8333",text:"Yes To Yes to Cucumbers Natural Sunscreen SPF 30"},
{id:"8334",text:"Yves Saint Laurent Matt Touch Compact Long Lasting Matt Finish Foundation SPF 20"},
{id:"8335",text:"Vaseline Total Moisture Spray & Go Moisturizer"},
{id:"8336",text:"Trish McEvoy Lip Gloss"},
{id:"8337",text:"Yes To Yes to Blueberries Softening Facial Exfoliator"},
{id:"8338",text:"Topix Replenix Power of Three Serum"},
{id:"8339",text:"Yes To Yes to Coconut Oil Body Wash"},
{id:"8340",text:"Ulta Ulta LipstickSheer"},
{id:"8341",text:"Urban Decay 24/7 Glide-On Lip Pencil"},
{id:"8342",text:"Ulta Baked Eyeshadow"},
{id:"8343",text:"Topix Citrix Antioxidant Eye Cream"},
{id:"8344",text:"Trish McEvoy Even Skin Portable Foundation"},
{id:"8345",text:"Yes To Yes To Blueberries Age Refresh Smoothing Daily Cleanser"},
{id:"8346",text:"Wet 'n Wild MegaPlump Waterproof Mascara"},
{id:"8347",text:"Vichy Aqualia Antiox Pro-Youth 24H Hydrating Fluid SPF 12"},
{id:"8348",text:"Ulta Tinted Lip Balm"},
{id:"8349",text:"Vichy Capital Soleil SPF 50 Ultra Light Sunscreen Fluid"},
{id:"8350",text:"Wet 'n Wild Ultimate Sheer Tinted Moisturizer SPF 15"},
{id:"8351",text:"Topix Replenix Serum CF"},
{id:"8352",text:"Yves Saint Laurent Gloss Volupte"},
{id:"8353",text:"Yves Saint Laurent Teint Parfait Complexion Enhancer"},
{id:"8354",text:"Ulta Waterproof Amped Lashes Volume + Defining Mascara"},
{id:"8355",text:"Yves Saint Laurent Pure Chromatics Wet and Dry Eye Shadow"},
{id:"8356",text:"Ulta Ultimate Clean Foaming Face Wash"},
{id:"8357",text:"Yves Saint Laurent Matt Touch Foundation SPF 10"},
{id:"8358",text:"Vichy Capital Soleil SPF 30 Luxurious Protective Oil"},
{id:"8359",text:"Vaseline Aloe Fresh Hydrating Body Gel"},
{id:"8360",text:"Urban Decay Lip Love Honey-Infused Lip Therapy"},
{id:"8361",text:"Yes To Yes to Cucumbers After-Sun Serum (With Shimmer)"},
{id:"8362",text:"Wet 'n Wild Cover All Cover Stick"},
{id:"8363",text:"Yes To Yes to Carrots Moisturizing Eye Cream"},
{id:"8364",text:"Too Faced Foiled Liners"},
{id:"8365",text:"Trish McEvoy Matte Eye Shadow"},
{id:"8366",text:"Vichy Normaderm Daily Anti-Acne Hydrating Lotion"},
{id:"8367",text:"Ulta Fabulous Face Pressed Powder"},
{id:"8368",text:"Yes To Yes to Coconut Protecting Hand & Cuticle Cream"},
{id:"8369",text:"Ulta Premium Protection Daily Moisturizer + SPF 15"},
{id:"8370",text:"Wet 'n Wild MegaVolume Mascara"},
{id:"8371",text:"Yves Saint Laurent Rouge Pur Couture Pure Color Lipstick SPF 15"},
{id:"8372",text:"Too Faced Snow Bunny Bronzer"},
{id:"8373",text:"Vivit’© Daily Firming Lotion"},
{id:"8374",text:"Yves Saint Laurent Le Teint Touche clat Illuminating Foundation SPF 19"},
{id:"8375",text:"Urban Decay Afterglow 8-Hour Powder Highlighter"},
{id:"8376",text:"Yes To Yes To Carrots Daily Facial Moisturizer with SPF 15"},
{id:"8377",text:"Urban Decay Surreal Skin Cream to Powder Foundation"},
{id:"8378",text:"Vichy Neovadiol GF Day Densifying Re-Sculpting Care Dry Skin"},
{id:"8379",text:"Yes To Yes To Tomatoes Clear Skin Acne Blemish Clearing Facial Towelettes"},
{id:"8380",text:"Trish McEvoy Beauty Booster Serum"},
{id:"8381",text:"Ulta Artistry Eye Shadow Kit"},
{id:"8382",text:"Too Faced Primed and Poreless Powder"},
{id:"8383",text:"Wet 'n Wild Perfect Pair Lip Wand"},
{id:"8384",text:"Too Faced Glamour Gloss"},
{id:"8385",text:"Wet 'n Wild Cover All Liquid Concealer Wand"},
{id:"8386",text:"Ulta Renew Barrier Boosting Daily Moisturizer + Sunscreen Broad Spectrum SPF 30"},
{id:"8387",text:"Vichy Purete Thermale Calming Cleansing Solution, for Sensitive Face and Eyes"},
{id:"8388",text:"Too Faced Long Stemmed Lashes Lash Enhancing Serum & Eyeliner Duo"},
{id:"8389",text:"Yves Saint Laurent Rouge Pur Couture Vernis  Lvres Pop Water Glossy Stain"},
{id:"8390",text:"Vaseline Intensive Care Healing Serum Deep Repair"},
{id:"8391",text:"Vichy LiftActiv Eyes"},
{id:"8392",text:"Urban Decay Subversion Lash Primer"},
{id:"8393",text:"Urban Decay B6 Vitamin-Infused Complexion Prep Spray"},
{id:"8394",text:"Wet 'n Wild MegaLength Mascara"},
{id:"8395",text:"Ulta Enriched Cleansing Cloths"},
{id:"8396",text:"Yves Saint Laurent Creme de Blush Soft Blush"},
{id:"8397",text:"Urban Decay Surreal Skin Creamy Concealer"},
{id:"8398",text:"Ulta Renew Intensive Treatment Night Repair Cream"},
{id:"8399",text:"Trish McEvoy Even Skin Foundation"},
{id:"8400",text:"Topix Replenix Cream CF"},
{id:"8401",text:"Yves Saint Laurent Temps Majeur Intensive Skin Supplement for the Eyes"},
{id:"8402",text:"Topix Replenix Restorative Nighttime Bio-Therapy"},
{id:"8403",text:"Wet 'n Wild Mega Last Lip Color"},
{id:"8404",text:"Yves Saint Laurent Eyeliner Noir"},
{id:"8405",text:"Trish McEvoy Fast-Track Lips"},
{id:"8406",text:"Vichy Purete Thermale Purifying Foaming Water"},
{id:"8407",text:"Trish McEvoy Translucent Finishing Powder"},
{id:"8408",text:"Yves Saint Laurent Full Metal Shadow"},
{id:"8409",text:"Topix Replenix AE Dermal Restructuring Therapy"},
{id:"8410",text:"Vichy Normaderm Deep Cleansing Gel, for Clear Skin"},
{id:"8411",text:"Wet 'n Wild Color Icon Bronzer SPF 15"},
{id:"8412",text:"Urban Decay 24/7 Waterproof Liquid Eyeliner"},
{id:"8413",text:"Trish McEvoy Eye Definer Pencil"},
{id:"8414",text:"Too Faced Full Bloom Lip & Cheek Creme Color"},
{id:"8415",text:"Wet 'n Wild Color Icon Blusher"},
{id:"8416",text:"Urban Decay Complexion Primer Potion-Pore Perfecting"},
{id:"8417",text:"Yes To Yes to Carrots Daily Moisture Body Lotion"},
{id:"8418",text:"Yes To Yes To Grapefruit CC Cream SPF 18 Broad Spectrum"},
{id:"8419",text:"Ulta Voluptuous Volume Volume Mascara"},
{id:"8420",text:"Trish McEvoy 24-Hour Eye Shadow and Liner"},
{id:"8421",text:"Too Faced Melted Liquified Long Wear Lipstick"},
{id:"8422",text:"Wet 'n Wild Glassy Gloss Lip Gel"},
{id:"8423",text:"Wet 'n Wild Natural Blend Lip Shimmer"},
{id:"8424",text:"Ulta Incognito Concealer"},
{id:"8425",text:"Trish McEvoy Eye Definer/Eye Liner"},
{id:"8426",text:"Trish McEvoy Gorgeous Lip Color"},
{id:"8427",text:"Yes To Yes to Tomatoes Acne Roller Ball Spot Stick"},
{id:"8428",text:"Ulta Super Stretch Fiber Mascara"},
{id:"8429",text:"Too Faced Born This Way Foundation"},
{id:"8430",text:"Trish McEvoy Even Skin Vitamin C Cream"},
{id:"8431",text:"Too Faced Exotic Color Intense Eye Shadow Singles"},
{id:"8432",text:"Vichy Normaderm Anti-Blemish Intensive Treatment Cream"},
{id:"8433",text:"Urban Decay Mascara Resurrection"},
{id:"8434",text:"Vichy Normaderm Triple Action Anti-Acne Hydrating Lotion"},
{id:"8435",text:"Yes To Yes to Cucumbers Soothing Facial Towelettes"},
{id:"8436",text:"Yes To Yes To Grapefruit Daily Facial Scrub"},
{id:"8437",text:"Vichy Normaderm Total Mat Anti-Shine Mattifier"},
{id:"8438",text:"Yes To Yes to Carrots Intense Repair Hand Cream"},
{id:"8439",text:"Trish McEvoy Beauty To Go Refillable Pen"},
{id:"8440",text:"Ulta Bountiful Lash Fortifying Mascara"},
{id:"8441",text:"Yes To Yes To Blueberries Age Refresh Eye Firming Treatment"},
{id:"8442",text:"Urban Decay 24/7 Glide-On Shadow Pencil"},
{id:"8443",text:"Vichy Thermal Spa Water"},
{id:"8444",text:"Trish McEvoy Flawless Concealer"},
{id:"8445",text:"Yes To Yes to Carrots Daily Cream Facial Cleanser"},
{id:"8446",text:"Topix Replenix All Trans Retinol Smoothing Serum 5X"},
{id:"8447",text:"Yves Saint Laurent Anti-Cernes Multi-Action Concealer"},
{id:"8448",text:"Urban Decay Eyeshadow Primer Potion Eden"},
{id:"8449",text:"Trish McEvoy Instant Pick Me Up"},
{id:"8450",text:"Vichy LiftActiv with Rhamnose 5% Dry"},
{id:"8451",text:"Trish McEvoy Instant Solutions Beauty Balm Broad Spectrum SPF 35"},
{id:"8452",text:"Urban Decay Eyeshadow Primer Potion Anti-Aging"},
{id:"8453",text:"Yves Saint Laurent Rouge Volupte Lipstick"},
{id:"8454",text:"Yves Saint Laurent Dessin du Regard Haute Tenue Long-Lasting Eye Pencil"},
{id:"8455",text:"Too Faced Lip Injection Extreme"},
{id:"8456",text:"Wet 'n Wild Mega Eyes Creme Eyeliner"},
{id:"8457",text:"Trish McEvoy Beauty Booster Tinted Moisturizer SPF 20"},
{id:"8458",text:"Urban Decay Naked Flushed"},
{id:"8459",text:"Yes To Yes to Coconut Cooling Lip Oil"},
{id:"8460",text:"Urban Decay 24/7 Concealer"},
{id:"8461",text:"Ulta Extreme Wear Eyeshadow Primer"},
{id:"8462",text:"Yves Saint Laurent Eyebrow Pencil"},
{id:"8463",text:"Yves Saint Laurent Top Secrets Natural Action Exfoliator Granule-Free"},
{id:"8464",text:"Yes To Yes To Grapefruit Dark Circle Correcting Eye Cream"},
{id:"8465",text:"Too Faced Magic Wand Illuminating Foundation"},
{id:"8466",text:"Vichy Idealia Eyes Eye Contour Idealizer"},
{id:"8467",text:"Vaseline Vaseline Jelly Original"},
{id:"8468",text:"Topix Citrix Antioxidant Pads"},
{id:"8469",text:"Ulta Ulta LipstickShimmer"},
{id:"8470",text:"Topix Citrix Antioxidant Cleanser"},
{id:"8471",text:"Too Faced Chocolate Soleil Matte Bronzing Powder"},
{id:"8472",text:"Yes To Yes to Cucumbers Calming Night Cream"},
{id:"8473",text:"Ulta Color Coat Cream Eye Shadow"},
{id:"8474",text:"Too Faced Shadow Insurance Anti-Crease Eye Shadow Primer"},
{id:"8475",text:"Yves Saint Laurent Temps Majeur Ultra Riche Crme"},
{id:"8476",text:"Ulta Brilliant Color Lip Gloss"},
{id:"8477",text:"Vaseline Intensive Rescue Repairing Moisture Body Lotion Fragrance Free"},
{id:"8478",text:"Trish McEvoy Finish Line"},
{id:"8479",text:"Trish McEvoy Correct & Brighten Pen"},
{id:"8480",text:"Too Faced Perfect Lips Universal Liner"},
	]
  });




  $(".brand_dropdown").select2({
    width: "100%",
    allowClear: true,
    placeholder: "Click Here",
    data: 
    [	
    	{id:"",text:""},
		{id:"Acne.org",text:"Acne.org"},
		{id:"AcneFree",text:"AcneFree"},
		{id:"Acure Organics",text:"Acure Organics"},
		{id:"Aesop",text:"Aesop"},
		{id:"Ahava",text:"Ahava"},
		{id:"Alba Botanica",text:"Alba Botanica"},
		{id:"Algenist",text:"Algenist"},
		{id:"Almay",text:"Almay"},
		{id:"Alpha Skin Care",text:"Alpha Skin Care"},
		{id:"Amazing Cosmetics",text:"Amazing Cosmetics"},
		{id:"AmLactin",text:"AmLactin"},
		{id:"AmorePacific",text:"AmorePacific"},
		{id:"Anastasia of Beverly Hills",text:"Anastasia of Beverly Hills"},
		{id:"Andalou Naturals",text:"Andalou Naturals"},
		{id:"Anthony Logistics for Men",text:"Anthony Logistics for Men"},
		{id:"Arbonne",text:"Arbonne"},
		{id:"Aubrey Organics",text:"Aubrey Organics"},
		{id:"Avalon Organics",text:"Avalon Organics"},
		{id:"Aveda",text:"Aveda"},
		{id:"Aveeno",text:"Aveeno"},
		{id:"Avon",text:"Avon"},
		{id:"Avon Mark",text:"Avon Mark"},

		{id:"Banana Boat",text:"Banana Boat"},
		{id:"Bare Escentuals (Bare Minerals)",text:"Bare Escentuals (Bare Minerals)"},
		{id:"BeautiControl",text:"BeautiControl"},
		{id:"beautyblender",text:"beautyblender"},
		{id:"Beautycounter",text:"Beautycounter"},

		{id:"Becca",text:"Becca"},
		{id:"Benefit",text:"Benefit"},
		{id:"Bio-Oil",text:"Bio-Oil"},
		{id:"Bioderma",text:"Bioderma"},
		{id:"Biore",text:"Biore"},
		{id:"Bite Beauty",text:"Bite Beauty"},
		{id:"Bliss",text:"Bliss"},
		{id:"Blue Lizard",text:"Blue Lizard"},
		{id:"Bobbi Brown",text:"Bobbi Brown"},
		{id:"Boots",text:"Boots"},
		{id:"Boscia",text:"Boscia"},
		{id:"Burberry Beauty",text:"Burberry Beauty"},
		{id:"Burt's Bees",text:"Burt's Bees"},

		{id:"Cargo",text:"Cargo"},
		{id:"Carol's Daughter",text:"Carol's Daughter"},
		{id:"Caudalie Paris",text:"Caudalie Paris"},
		{id:"CeraVe",text:"CeraVe"},
		{id:"Cetaphil",text:"Cetaphil"},
		{id:"Chanel",text:"Chanel"},
		{id:"Chantecaille",text:"Chantecaille"},
		{id:"Charlotte Tilbury",text:"Charlotte Tilbury"},
		{id:"Clarins",text:"Clarins"},
		{id:"Clarisonic",text:"Clarisonic"},
		{id:"Clean & Clear",text:"Clean & Clear"},
		{id:"Clearasil",text:"Clearasil"},
		{id:"Clinique",text:"Clinique"},
		{id:"ColourPop",text:"ColourPop"},
		{id:"Coola",text:"Coola"},
		{id:"Coppertone",text:"Coppertone"},
		{id:"Cover FX",text:"Cover FX"},
		{id:"CoverGirl",text:"CoverGirl"},
		{id:"Curel",text:"Curel"},

		{id:"DDF - Doctor's Dermatologic Formula",text:"DDF - Doctor's Dermatologic Formula"},
		{id:"Decleor",text:"Decleor"},
		{id:"Derm Exclusive",text:"Derm Exclusive"},
		{id:"derma e",text:"derma e"},
		{id:"Dermablend",text:"Dermablend"},
		{id:"DERMAdoctor",text:"DERMAdoctor"},
		{id:"Dermalogica",text:"Dermalogica"},
		{id:"DerMend",text:"DerMend"},
		{id:"DeVita Natural Skin Care",text:"DeVita Natural Skin Care"},
		{id:"DHC",text:"DHC"},
		{id:"Dior",text:"Dior"},
		{id:"Dolce & Gabbana",text:"Dolce & Gabbana"},
		{id:"Dollar Shave Club",text:"Dollar Shave Club"},
		{id:"Dove",text:"Dove"},
		{id:"Dr. Brandt",text:"Dr. Brandt"},
		{id:"Dr. Denese New York",text:"Dr. Denese New York"},
		{id:"Dr. Dennis Gross Skincare",text:"Dr. Dennis Gross Skincare"},
		{id:"Dr. Hauschka",text:"Dr. Hauschka"},
		{id:"Dr. Jart",text:"Dr. Jart"},
		{id:"Drunk Elephant",text:"Drunk Elephant"},

		{id:"e.l.f. Cosmetics",text:"e.l.f. Cosmetics"},
		{id:"Egyptian Magic",text:"Egyptian Magic"},
		{id:"Elizabeth Arden",text:"Elizabeth Arden"},
		{id:"EltaMD",text:"EltaMD"},
		{id:"elure",text:"elure"},
		{id:"Embryolisse",text:"Embryolisse"},
		{id:"Epicuren Discovery",text:"Epicuren Discovery"},
		{id:"Erno Laszlo",text:"Erno Laszlo"},
		{id:"Estee Lauder",text:"Estee Lauder"},
		{id:"Eucerin",text:"Eucerin"},
		{id:"Exuviance by NeoStrata",text:"Exuviance by NeoStrata"},

		{id:"First Aid Beauty",text:"First Aid Beauty"},
		{id:"Flower Beauty",text:"Flower Beauty"},
		{id:"Free & Clear",text:"Free & Clear"},
		{id:"fresh",text:"fresh"},

		{id:"Garnier Nutritioniste",text:"Garnier Nutritioniste"},
		{id:"Giorgio Armani",text:"Giorgio Armani"},
		{id:"Givenchy Makeup",text:"Givenchy Makeup"},
		{id:"GlamGlow",text:"GlamGlow"},
		{id:"Glossier",text:"Glossier"},
		{id:"Gold Bond",text:"Gold Bond"},
		{id:"Guerlain",text:"Guerlain"},

		{id:"Hada Labo Tokyo",text:"Hada Labo Tokyo"},
		{id:"Hawaiian Tropic",text:"Hawaiian Tropic"},
		{id:"Hourglass Cosmetics",text:"Hourglass Cosmetics"},
		{id:"Hydroxatone",text:"Hydroxatone"},

		{id:"Indeed Laboratories",text:"Indeed Laboratories"},
		{id:"iS Clinical",text:"iS Clinical"},
		{id:"IT Cosmetics",text:"IT Cosmetics"},

		{id:"Jan Marini Skin Research, Inc.",text:"Jan Marini Skin Research, Inc."},
		{id:"Jane Iredale",text:"Jane Iredale"},
		{id:"Jason Natural",text:"Jason Natural"},
		{id:"Jergens",text:"Jergens"},
		{id:"Josie Maran Cosmetics",text:"Josie Maran Cosmetics"},
		{id:"Jurlique International",text:"Jurlique International"},

		{id:"Kat Von D",text:"Kat Von D"},
		{id:"Kate Somerville",text:"Kate Somerville"},
		{id:"Kevyn Aucoin",text:"Kevyn Aucoin"},
		{id:"Kiehl's",text:"Kiehl's"},
		{id:"KINeSYS",text:"KINeSYS"},
		{id:"Kiss My Face",text:"Kiss My Face"},
		{id:"KORA Organics",text:"KORA Organics"},
		{id:"Korres",text:"Korres"},

		{id:"L'Occitane",text:"L'Occitane"},
		{id:"L'Oreal Paris",text:"L'Oreal Paris"},
		{id:"La Mer",text:"La Mer"},
		{id:"La Prairie",text:"La Prairie"},
		{id:"La Roche-Posay",text:"La Roche-Posay"},
		{id:"Lab Series Skincare for Men",text:"Lab Series Skincare for Men"},
		{id:"Lancer Skincare",text:"Lancer Skincare"},
		{id:"Lancome",text:"Lancome"},
		{id:"Laneige",text:"Laneige"},
		{id:"Laura Geller",text:"Laura Geller"},
		{id:"Laura Mercier",text:"Laura Mercier"},
		{id:"Liz Earle Naturally Active Skin Care",text:"Liz Earle Naturally Active Skin Care"},
		{id:"Lorac",text:"Lorac"},
		{id:"Lubriderm",text:"Lubriderm"},

		{id:"M.A.C.",text:"M.A.C."},
		{id:"Make Up For Ever",text:"Make Up For Ever"},
		{id:"Mally Beauty",text:"Mally Beauty"},
		{id:"Marc Jacobs Beauty",text:"Marc Jacobs Beauty"},
		{id:"Marcelle",text:"Marcelle"},
		{id:"Mario Badescu",text:"Mario Badescu"},
		{id:"Mary Kay",text:"Mary Kay"},
		{id:"Maybelline New York",text:"Maybelline New York"},
		{id:"MD Formulations",text:"MD Formulations"},
		{id:"MDSolarSciences",text:"MDSolarSciences"},
		{id:"Meaningful Beauty Cindy Crawford",text:"Meaningful Beauty Cindy Crawford"},
		{id:"Mederma",text:"Mederma"},
		{id:"Merle Norman",text:"Merle Norman"},
		{id:"Michael Todd True Organics",text:"Michael Todd True Organics"},
		{id:"Milani",text:"Milani"},
		{id:"Milk Makeup",text:"Milk Makeup"},
		{id:"Miracle Skin Transformer",text:"Miracle Skin Transformer"},
		{id:"Murad",text:"Murad"},

		{id:"N.Y.C. (New York Color)",text:"N.Y.C. (New York Color)"},
		{id:"NARS",text:"NARS"},
		{id:"NeoCeuticals by NeoStrata",text:"NeoCeuticals by NeoStrata"},
		{id:"Neocutis",text:"Neocutis"},
		{id:"NeoStrata",text:"NeoStrata"},
		{id:"NeoStrata Canada",text:"NeoStrata Canada"},
		{id:"Nerium AD",text:"Nerium AD"},
		{id:"Neutrogena",text:"Neutrogena"},
		{id:"Neutrogena Canada",text:"Neutrogena Canada"},
		{id:"Nia24",text:"Nia24"},
		{id:"Nivea",text:"Nivea"},
		{id:"Noxzema",text:"Noxzema"},
		{id:"Nu Skin",text:"Nu Skin"},
		{id:"Nuance Salma Hayek",text:"Nuance Salma Hayek"},
		{id:"NuCerity",text:"NuCerity"},
		{id:"Nude Skincare",text:"Nude Skincare"},
		{id:"NYX Cosmetics",text:"NYX Cosmetics"},

		{id:"Obagi",text:"Obagi"},
		{id:"OC Eight",text:"OC Eight"},
		{id:"Olay",text:"Olay"},
		{id:"Ole Henriksen",text:"Ole Henriksen"},
		{id:"Origins",text:"Origins"},

		{id:"Palladio Beauty",text:"Palladio Beauty"},
		{id:"Palmer's",text:"Palmer's"},
		{id:"PanOxyl",text:"PanOxyl"},
		{id:"Paula's Choice Skincare",text:"Paula's Choice Skincare"},
		{id:"Perricone MD Cosmeceuticals",text:"Perricone MD Cosmeceuticals"},
		{id:"Peter Thomas Roth",text:"Peter Thomas Roth"},
		{id:"philosophy",text:"philosophy"},
		{id:"Physicians Formula",text:"Physicians Formula"},
		{id:"Pixi",text:"Pixi"},
		{id:"Pond's",text:"Pond's"},
		{id:"Prestige Cosmetics",text:"Prestige Cosmetics"},
		{id:"Proactiv & Proactiv+",text:"Proactiv & Proactiv+"},
		{id:"Purpose",text:"Purpose"},
		{id:"Pürminerals",text:"Pürminerals"},

		{id:"RapidLash",text:"RapidLash"},
		{id:"Ren",text:"Ren"},
		{id:"Replenix",text:"Replenix"},
		{id:"RevitaLash",text:"RevitaLash"},
		{id:"Revlon",text:"Revlon"},
		{id:"Rimmel",text:"Rimmel"},
		{id:"RoC",text:"RoC"},
		{id:"Rodan and Fields",text:"Rodan and Fields"},
		{id:"Rodin Olio Lusso",text:"Rodin Olio Lusso"},

		{id:"Sally Hansen",text:"Sally Hansen"},
		{id:"Sephora",text:"Sephora"},
		{id:"ShiKai",text:"ShiKai"},
		{id:"Shiseido",text:"Shiseido"},
		{id:"Simple",text:"Simple"},
		{id:"SK-II",text:"SK-II"},
		{id:"SkinCeuticals",text:"SkinCeuticals"},
		{id:"Skinfix",text:"Skinfix"},
		{id:"SkinMedica",text:"SkinMedica"},
		{id:"Skinn by Dimitri James",text:"Skinn by Dimitri James"},
		{id:"Skyn Iceland",text:"Skyn Iceland"},
		{id:"Smashbox",text:"Smashbox"},
		{id:"Sonia Kashuk",text:"Sonia Kashuk"},
		{id:"Spectro",text:"Spectro"},
		{id:"St. Ives",text:"St. Ives"},
		{id:"St. Tropez",text:"St. Tropez"},
		{id:"Stila",text:"Stila"},
		{id:"Stridex",text:"Stridex"},
		{id:"StriVectin",text:"StriVectin"},
		{id:"Sunday Riley",text:"Sunday Riley"},
		{id:"Suntegrity",text:"Suntegrity"},
		{id:"Supergoop!",text:"Supergoop!"},

		{id:"Tarte Cosmetics",text:"Tarte Cosmetics"},
		{id:"Tata Harper",text:"Tata Harper"},
		{id:"Tatcha",text:"Tatcha"},
		{id:"Tend Skin",text:"Tend Skin"},
		{id:"The Body Shop",text:"The Body Shop"},
		{id:"The Ordinary",text:"The Ordinary"},
		{id:"TheBalm",text:"TheBalm"},
		{id:"Three Custom Color Specialists",text:"Three Custom Color Specialists"},
		{id:"Tom Ford",text:"Tom Ford"},
		{id:"Too Faced",text:"Too Faced"},
		{id:"Topix",text:"Topix"},
		{id:"Trish McEvoy",text:"Trish McEvoy"},

		{id:"Ulta",text:"Ulta"},
		{id:"Urban Decay",text:"Urban Decay"},

		{id:"Vaseline",text:"Vaseline"},
		{id:"Vichy",text:"Vichy"},

		{id:"Wet 'n Wild",text:"Wet 'n Wild"},

		{id:"Yes To",text:"Yes To"},
		{id:"Yves Saint Laurent",text:"Yves Saint Laurent"}
    ]
        
  });

  $(".product_type_dropdown").select2({
    width: "100%",
    allowClear: true,
    placeholder: "Click Here",
    data: 
    [ {id:"",text:""},
    	{
    	  id: "Skincare", 
    	  text: "Skincare",
          children: [
          
          		{id:"Acne & Blemish Treatments",text:"Acne & Blemish Treatments"},
				{id:"AHA Exfoliants",text:"AHA Exfoliants"},
				{id:"Best & Worst Skincare Products",text:"Best & Worst Skincare Products"},
				{id:"BHA Exfoliants",text:"BHA Exfoliants"},
				{id:"Body Exfoliants & Scrubs",text:"Body Exfoliants & Scrubs"},
				{id:"Body Lotions & Creams",text:"Body Lotions & Creams"},
				{id:"Body Washes",text:"Body Washes"},
				{id:"Body-Care Products",text:"Body-Care Products"},
				{id:"Cleansers",text:"Cleansers"},
				{id:"Exfoliants",text:"Exfoliants"},
				{id:"Eye Creams & Treatments",text:"Eye Creams & Treatments"},
				{id:"Face Masks",text:"Face Masks"},
				{id:"Face Oils",text:"Face Oils"},
				{id:"Hand Lotions & Creams",text:"Hand Lotions & Creams"},
				{id:"Lip Balms & Treatments",text:"Lip Balms & Treatments"},
				{id:"Makeup Removers",text:"Makeup Removers"},
				{id:"Moisturizers, Daytime & Nighttime",text:"Moisturizers, Daytime & Nighttime"},
				{id:"Moisturizers, Daytime With Sunscreen",text:"Moisturizers, Daytime With Sunscreen"},
				{id:"Moisturizers, Nighttime",text:"Moisturizers, Nighttime"},
				{id:"Oil-Absorbing Products",text:"Oil-Absorbing Products"},
				{id:"Retinol Products",text:"Retinol Products"},
				{id:"Scrubs",text:"Scrubs"},
				{id:"Self-Tanners",text:"Self-Tanners"},
				{id:"Serums & Boosters",text:"Serums & Boosters"},
				{id:"Shave Products",text:"Shave Products"},
				{id:"Skin-Lightening Products",text:"Skin-Lightening Products"},
				{id:"Specialty Products",text:"Specialty Products"},
				{id:"Sunscreens, Face & Body",text:"Sunscreens, Face & Body"},
				{id:"Sunscreens, Water-Resistant",text:"Sunscreens, Water-Resistant"},
				{id:"Toners & Face Mists",text:"Toners & Face Mists"},
				{id:"Tools, Brushes & Cleansing Devices",text:"Tools, Brushes & Cleansing Devices"},
				{id:"Vitamin C Products",text:"Vitamin C Products"}
			]
        },
        {
            id: "Makeup", 
            text: "Makeup",
            children: [
          
				{id:"Blushes",text:"Blushes"},
				{id:"Bronzers",text:"Bronzers"},
				{id:"Brushes & Sponges",text:"Brushes & Sponges"},
				{id:"Concealers & Correctors",text:"Concealers & Correctors"},
				{id:"Contour & Highlight Products",text:"Contour & Highlight Products"},
				{id:"Eye Lash Enhancers & Primers",text:"Eye Lash Enhancers & Primers"},
				{id:"Eyebrow Fillers & Shapers",text:"Eyebrow Fillers & Shapers"},
				{id:"Eyeliners",text:"Eyeliners"},
				{id:"Eyeshadow Palettes",text:"Eyeshadow Palettes"},
				{id:"Eyeshadow Primers & Bases",text:"Eyeshadow Primers & Bases"},
				{id:"Eyeshadows",text:"Eyeshadows"},
				{id:"Foundation Primers",text:"Foundation Primers"},
				{id:"Foundations With Sunscreen",text:"Foundations With Sunscreen"},
				{id:"Foundations Without Sunscreen",text:"Foundations Without Sunscreen"},
				{id:"Lip Glosses",text:"Lip Glosses"},
				{id:"Lip Pencils",text:"Lip Pencils"},
				{id:"Lipsticks",text:"Lipsticks"},
				{id:"Luminizers & Highlighters",text:"Luminizers & Highlighters"},
				{id:"Mascaras, Regular",text:"Mascaras, Regular"},
				{id:"Mascaras, Waterproof",text:"Mascaras, Waterproof"},
				{id:"Powders, Pressed & Loose",text:"Powders, Pressed & Loose"},
				{id:"Specialty Products",text:"Specialty Products"},
				{id:"Tinted Moisturizers/BB Creams",text:"Tinted Moisturizers/BB Creams"}
			]
        }
    ]
  });
    $(".profile_age").select2({
        width: "100%",
        allowClear: true,
        placeholder: "Click Here",
        data: 
        [
            {id:"",text:""},
            {id:"-19",text:"-19"},
            {id:"20-29",text:"20-29"},
            {id:"30-39",text:"30-39"},
            {id:"40-49",text:"40-49"},
            {id:"50+",text:"50+"}
        ]
    });
    $(".profile_skin_type").select2({
        width: "100%",
        allowClear: true,
        placeholder: "Click Here",
        data: 
        [
            {id:"",text:""},
            {id:"Normal",text:"Normal"},
            {id:"Oily",text:"Oily"},
            {id:"Dry",text:"Dry"},
            {id:"Sensitive",text:"Sensitive"},
            {id:"Combination",text:"Combination"}
        ]
    });
    $(".profile_ethnicity").select2({
        width: "100%",
        allowClear: true,
        placeholder: "Click Here",
        data: 
        [
            {id:"",text:""},
            {id:"Asian",text:"Asian"},
            {id:"European",text:"European"},
            {id:"Indo-pakistani",text:"Indo-pakistani"},
            {id:"African",text:"African"},
            {id:"Nordic",text:"Nordic"},
            {id:"Mediterranean",text:"Mediterranean"},
        ]
    });
    
});
// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, vendor/assets/javascripts,
// or any plugin's vendor/assets/javascripts directory can be referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file.
//
// Read Sprockets README (https://github.com/rails/sprockets#sprockets-directives) for details
// about supported directives.
//











// NProgress.configure({
//  showSpinner: true,
//  ease: 'ease',
//  speed: 300
// });
